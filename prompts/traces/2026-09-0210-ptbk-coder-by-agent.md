# Run trace of `prompts/2026-09-0210-ptbk-coder-by-agent.md#1`

-   **Prompt:** Allow to specify the agent in the prompt
-   **Outcome:** Succeeded
-   **Runner:** OpenAI Codex `gpt-5.6-terra` thinking `max` (ChatGPT account)
-   **Attempts:** 1
-   **Steps:** Implementation ~$1.32 an hour; Testing 17 minutes
-   **Verification command:** `npm run test-for-ptbk-coder`
-   **Started:** 2026-09-12T09:20:18.085Z
-   **Finished:** 2026-09-12T10:27:05.306Z
-   **Duration:** an hour

## Runtime log

````text
=== runner shell started at 2026-09-12T09:20:18.620Z ===
Script path: /c/Users/me/work/ai/promptbook/.promptbook/coder-prompts/2026-09-0210-ptbk-coder-by-agent.sh

--- raw input ---
if [ -n "${PTBK_AGENTS_SERVER_ENV_FILE:-}" ] && [ -f "${PTBK_AGENTS_SERVER_ENV_FILE}" ]; then
set -a
source "${PTBK_AGENTS_SERVER_ENV_FILE}"
set +a
elif [ -f .env ]; then
set -a
source .env
set +a
fi

CODEX_LOGIN_STATUS="$(
    unset OPENAI_API_KEY OPENAI_BASE_URL CODEX_API_KEY
    # 'codex login status' prints the "Logged in using ChatGPT" line to stderr, so merge stderr into stdout (2>&1) to capture it
    codex login status 2>&1 || true
)"
case "$CODEX_LOGIN_STATUS" in
    *"Logged in using ChatGPT"*)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=1
        ;;
    *)
        IS_CODEX_CHATGPT_LOGIN_ACTIVE=0
        ;;
esac

CODEX_LOGIN_METHOD=chatgpt
CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=chatgpt)
unset CODEX_API_KEY
if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" != "1" ] &&
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" = "1" ] &&
    [ -n "${OPENAI_API_KEY:-}" ]; then
    CODEX_LOGIN_METHOD_ARGUMENTS=(-c forced_login_method=api)
    CODEX_LOGIN_METHOD=api
    CODEX_API_KEY="${OPENAI_API_KEY}"
    export CODEX_API_KEY
fi

if [ "$IS_CODEX_CHATGPT_LOGIN_ACTIVE" = "1" ] ||
    [ "${PTBK_OPENAI_CODEX_USE_API_KEY:-0}" != "1" ] ||
    [ -z "${OPENAI_API_KEY:-}" ]; then
unset OPENAI_API_KEY
unset OPENAI_BASE_URL
fi

printf '%s %s\n' 'ptbk-codex-login-method:' "${CODEX_LOGIN_METHOD}"

codex \
    "${CODEX_LOGIN_METHOD_ARGUMENTS[@]}" \
    -c model_reasoning_effort="max" \
    --ask-for-approval never \
    exec --model gpt-5.6-terra \
    --local-provider none \
    --sandbox danger-full-access \
    -C /c/Users/me/work/ai/promptbook \
    --skip-git-repo-check \
    <<'CODEX_PROMPT'

## Your Task

Allow to specify the agent in the prompt

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md
```

-   You can already specify the harness or model, allow also to specify the agent.
-   Both `agents/coding/developer.book`, `developer.book` and `developer` should work
    -   Also `Developer Foo bar` should work - The name of the agent from the 1st line of the book file.
-   Allowto combine, the specifying, the harness, model, or agent,
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json), Alongside the `developer` add there `unslopper`
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

## Your Behavior

You are Promptbook Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.

Output Format: errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

## Context

### How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

### Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

### Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

### Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

### Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE PROJECT`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

### Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder _(not for example in the `apps` folder)_, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

### Additional context

-   Look at attached images if there are present
-   Attached images are in folder `prompts` on the root of the project
    -   For example the ![alt text](screenshots/foo.png) is image file `prompts/screenshots/foo.png`

#### Agents server installed on VPS _(as additional context)_

-   There are two distinct things:
    1. Entire VPS
    2. Each server
-   On one VPS there can be multiple servers and each server has its own domain
-   Most things (like agents, projects, metadata, etc.) are living on each server and some things (like environment variables, superadmin, etc.) are bound to entire VPS
-   Purpose of this is to be able to run multiple clients on one VPS and each client can have its own server with its own domain and settings
-   The entire VPS is managed by superadmin and each server is managed by normal admin of that server _(or superadmin can also manage each server)_
-   Superadmin has always name `admin` and password hardcoded in environment variable `ADMIN_PASSWORD`
-   The VPS is installed by running [the `install.sh`](install.sh) script on a fresh VPS
-   Servers can be added and removed by superadmin on `/superadmin/servers` page

#### The Agents Server menu _(as additional context)_

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

#### Database migrations for Agents server _(as additional context)_

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

#### Metadata of Agents server _(as additional context)_

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

#### Book Language blueprint _(as additional context)_

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://live.ptbk.io/api/docs/book-language.md?language=en`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

##### Commitments _(as additional context and part of Book Language)_

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE PROJECT`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE MCP https://mcp.example.com/server`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`

CODEX_PROMPT

--- raw output ---
ptbk-codex-login-method: chatgpt
Reading prompt from stdin...
OpenAI Codex v0.153.4
--------
workdir: C:\Users\me\work\ai\promptbook
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: max
reasoning summaries: none
session id: 01a094eb-1815-7140-a657-d0e04fb2b51b
--------
user

## Your Task

Allow to specify the agent in the prompt

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md
```

-   You can already specify the harness or model, allow also to specify the agent.
-   Both `agents/coding/developer.book`, `developer.book` and `developer` should work
    -   Also `Developer Foo bar` should work - The name of the agent from the 1st line of the book file.
-   Allowto combine, the specifying, the harness, model, or agent,
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json), Alongside the `developer` add there `unslopper`
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

## Your Behavior

You are Promptbook Developer

### Rules

-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.

Output Format: errors as markdown, for example `variables` should be in backticks and important notes can be in bold.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

### Prompt suffix
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Keep in mind the SOLID principles.
-   Do a proper analysis of the current functionality before you start implementing.
-   Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.
-   When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
-   Constants should always be `UPPER_SNAKE_CASE`.
-   Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.
-   Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.
-   When writing multiline strings, use `spaceTrim` utility.
-   Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
If you find some critical issue that is not described in the prompt, report it to `agents-messages` in the root of the project.

## Context

### How to Contribute

-   **Add changes in [Changelog](/changelog/_current-preversion.md)**
-   **Test your changes** to ensure they work as expected, use `npm test`

### Code Style

-   Follow the existing code style and structure.
-   Use comments to explain complex logic.

### Emoji in `[brackets]`

-   `[any emoji]` Connects multiple places that are related to each other across the repository
-   `[number]` Connects multiple places that are related to each other across the file
-   `[🧠]` Marks a place where there is something to decide and think about.
-   `[🕕]` List of models _(that should be progresively updated)_
    -   Prompt: Update available models and their prices, search online
-   `[🔼]` Marks an entity (function, class, type,...) in other project (like Promptbook.studio) which should be moved to this repository
-   `[🚉]` Marks an types / interfaces / structures fully serializable as JSON, not marking `string_` and `number_` prefixed aliases
-   `[🧹]` Need to implement garbage collection
-   `[🐣]` Easter eggs
-   `[💩]` Shitty code that needs refactoring
-   `$` When entity (function, class) starts by `$`, it means it is not pure and can have side effects.
-   3x `!` Marks a place that needs to be fixed before releasing a pre-release version.
-   4x (and more) `!` Marks a place that needs to be fixed as soon as possible.
-   `@@@` Marks a place where text / documentation / ... must be written.
-   [⚫] Code in this file should never be published in any package
-   [🟢] Code in this file should never be published into packages that could be imported into browser environment
-   [🔵] Code in this file should never be published outside of `@promptbook/browser`
-   [🟡] Code in this file should never be published outside of `@promptbook/cli`
-   [💞] Ignore a discrepancy between file name and entity name

### Generated code

Across the repository, there are several places where code has been generated automatically.
Do not edit these places manually, as they will be overwritten by the code generation process. Edit the source files instead.

> ⚠️ WARNING: This code has been generated so that any manual changes will be overwritten

### Dictionary

-   **Promptbook Engine vs. Agent Server** sre two distinct parts you should know the difference:
    -   **Promptbook Engine** is the core engine that powers AI agents, it is located in `/src` folder. It is framework-agnostic and can be used in different applications. It can run as standalone javascript/typescript library, CLI environment, or be integrated into different applications. It can be also runned in Agent Server:
    -   **Agent Server** is a specific application that uses the Promptbook Engine to provide a web and API interface for creating and interacting with AI agents. It is located in `/apps/agents-server` folder.
-   **Commitments** are basic syntax elements that add specific functionalities to AI agents
    -   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, etc.
    -   Commitments are in the folder `/src/commitments`
    -   Each commitment starts with a keyword, e.g., `KNOWLEDGE`, `USE PROJECT`, etc. on a begining of the line and end by new co
    -   Agent source with commitments is parsed by two functions:
        -   `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
        -   `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.

### Common rules

-   Always analyze the context and requirements before generating any code.
-   Write clear, maintainable, and well-documented code.
-   Write JSDoc comments for all entities - functions, classes, types, top-level constants, etc.
    -   When this entity is exported from the file and it is under `src` folder _(not for example in the `apps` folder)_, it must be marked either as `@public` or `@private` at the end of the JSDoc comment.
    -   For example: "@private internal utility of <Chat/>" / "@public exported from `@promptbook/browser`"
    -   If you don't know, prefer to mark it as private, we can always change it to public later, but changing from public to private may cause breaking changes.
-   You (the AI coding agent) are running inside a Node process, so do not kill all Node processes such as `taskkill /F /IM node.exe`. If you need to stop something you spawned, kill only that specific process, for example by PID or by port.

### Additional context

-   Look at attached images if there are present
-   Attached images are in folder `prompts` on the root of the project
    -   For example the ![alt text](screenshots/foo.png) is image file `prompts/screenshots/foo.png`

#### Agents server installed on VPS _(as additional context)_

-   There are two distinct things:
    1. Entire VPS
    2. Each server
-   On one VPS there can be multiple servers and each server has its own domain
-   Most things (like agents, projects, metadata, etc.) are living on each server and some things (like environment variables, superadmin, etc.) are bound to entire VPS
-   Purpose of this is to be able to run multiple clients on one VPS and each client can have its own server with its own domain and settings
-   The entire VPS is managed by superadmin and each server is managed by normal admin of that server _(or superadmin can also manage each server)_
-   Superadmin has always name `admin` and password hardcoded in environment variable `ADMIN_PASSWORD`
-   The VPS is installed by running [the `install.sh`](install.sh) script on a fresh VPS
-   Servers can be added and removed by superadmin on `/superadmin/servers` page

#### The Agents Server menu _(as additional context)_

The menu of the agent server looks like this:

1. The navigation hierarchy
    - Icon and Server name _(for example Promptbook Agents Server)_
    - arrow ">" and Agents or picked agent name (organized in folders)
    - arrow ">" and the view Profile / Chat / Book of the agent or nothing if no agent is picked
2. The menu items
    - Documentation
    - System
3. Control panel and user menu
    - Control panel
    - User menu with the avatar and the name of the user

#### Database migrations for Agents server _(as additional context)_

-   Migrations are located in `apps/agents-server/src/database/migrations`
-   Be aware that table names in migrations have prefix `prefix_` _(look at existing migrations for reference)_
-   Migrations should be backwards compatible:
    -   Meaning that earlier versions of the server should be able to run with the database after migration without any issues.
    -   This is important because we want to have the same database for production and preview environments.
    -   This database will be migrated to the latest version of the preview environment, but the production environment should be able to run with it without any issues.
    -   The only thing that can happen is that older versions of the server will not be able to use new features.
    -   Adding new columns, tables, etc. is fine, but do not remove or rename existing ones, and do not change the meaning of existing columns or tables.
    -   When in doubt, prefer to add new things instead of changing existing ones.

#### Metadata of Agents server _(as additional context)_

-   There is a table called `Metadata`.
-   It has `key` and `value` fields.
-   It is a similar concept to configuration, but this configuration can be changed by administrators in the Agents Server website.

#### Book Language blueprint _(as additional context)_

Book language is a domain-specific language used for defining AI agents in the Promptbook Engine and Agents server.
It has lightweight syntax and keywords (the commitments) that allow you to define the "soul" of the agent.
The book language is designed to be human-readable and easy to write, while also being powerful enough to express complex agent behaviors.

Every agent has its source defined in the book language, which is called "agent source". The agent source is parsed and processed by the Promptbook Engine to create the actual AI agent that can interact with users and perform tasks.
This agent source is internally converted to a structured format called "agent model requirements" which are the actual raw technical instructions for the AI model to run the agent.

There is a standalone book language documentation on each agent server on `/api/docs/book.md` route, for example `https://live.ptbk.io/api/docs/book-language.md?language=en`.
Use it as a reference for the syntax and semantics of the book language, and modify `apps/agents-server/src/utils/bookLanguageDocumentation/createStandaloneBookLanguageMarkdown.ts` if it is relevant to the change you are doing.

##### Commitments _(as additional context and part of Book Language)_

Commitments are basic syntax elements that add specific functionalities to AI agents written in `book` language.

-   They are used in `agentSource`, there are commitments like `PERSONA`, `RULE`, `KNOWLEDGE`, `USE CALENDAR`, `USE PROJECT`, `META IMAGE`, `CLOSED`, etc.
-   They are in the folder `src/commitments`.
-   Each commitment starts with a keyword, e.g. `PERSONA`, `KNOWLEDGE`, `USE PROJECT`, etc. on a beginning of the line and ends by a new commitment or the end of the book.
-   There is a general pattern that the commitment keyword is followed by a space and then by the content of the commitment, for example:
    -   `PERSONA You are a helpful assistant that helps with cooking recipes.`
    -   `USE MCP https://mcp.example.com/server`
-   In the commitment context, you can reference external agents, for example:
    -   `TEAM You can talk to {Criminal lawyer} and {Financial advisor}`


codex
I’ll trace how `ptbk coder` currently resolves its harness, model, and default agent, then extend that shared path so the three override styles compose cleanly. I’ll also update the developer scripts, landing-page docs, changelog, and focused tests before running the project test suite.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "git status --short; rg -n --hidden --glob '"'!node_modules'"' \"coder run|openai-codex|thinking-level|--harness|--model|developer\\.book|agents/coding\" src apps/coder-landing .vscode package.json changelog" in C:\Users\me\work\ai\promptbook
 succeeded in 4849ms:
 M prompts/2026-09-0210-ptbk-coder-by-agent.md
package.json:184:        "start": "npx ts-node ./src/cli/test/ptbk.ts agents-server start --harness github-copilot --model gpt-5.4 --thinking-level high --port 4440",
package.json:234:        "coder:run": "npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md --test-before yes-and-fix",
.vscode\terminals.json:18:                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness claude-code --model claude-sonnet-5 --thinking-level low",
.vscode\terminals.json:19:                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level low"
.vscode\terminals.json:30:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
.vscode\terminals.json:41:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
.vscode\terminals.json:52:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
.vscode\terminals.json:63:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
.vscode\terminals.json:74:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
.vscode\terminals.json:82:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
.vscode\terminals.json:89:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
.vscode\terminals.json:99:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
.vscode\terminals.json:110:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
.vscode\terminals.json:121:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
.vscode\terminals.json:132:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
.vscode\terminals.json:143:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
.vscode\terminals.json:244:                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
.vscode\terminals.json:254:                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
src\cli\test\ptbk.test.ts:169:            expect(output).toContain('✔ agents/developer.book: created');
src\cli\other\vpsInstall.test.ts:193:        expect(installScript).toContain('PTBK_HARNESS="${PTBK_HARNESS:-${PTBK_AGENT:-openai-codex}}"');
src\cli\other\vpsInstall.test.ts:196:        expect(runnerDependenciesFunction).toContain('openai-codex)');
apps/coder-landing\specs\sections\what-is-ptbk-coder.md:22:2. **No vendor lock-in** — **Keep your agent portable.** — "The behavior of the agent lives in a .book file you commit next to the code, not in one vendor prompt box. Change --harness and the same queue runs on Claude Code or OpenAI Codex. With opencode you point it at whatever provider you configured, local models included."
apps/coder-landing\specs\sections\quickstart.md:14:| 2 | Initialize your project            | `INIT_COMMAND`         | Creates `prompts/` queue, `prompts/done/` archive, default `agents/developer.book` persona, `AGENTS.md` project context, agent git identity entries in `.env`, and local ignore rules for every supported harness; nothing already owned by the project is overwritten — existing `package.json` scripts and `.vscode/settings.json` settings are kept and the files a script points at are created only together with that script, so it is safe to re-run |
apps/coder-landing\specs\sections\hero.md:49:The [live terminal demo](../components/live-terminal.md) playing the scripted `ptbk coder run` rich terminal session.
apps/coder-landing\specs\sections\harnesses.md:8:-   **Lead paragraph**: ptbk coder does not replace your coding agent. It drives it. Pick a harness with `--harness`, add a `--model` if you want one, and for the harnesses that support it a `--thinking-level` (low, medium, high, xhigh, max).
apps/coder-landing\specs\sections\harnesses.md:18:3. **Harness chip** (top right, ≥ `sm` only): `--harness <id>` in a small monospace chip.
apps/coder-landing\specs\sections\comparison.md:27:Below the legend, small muted text: "Inside a single task nothing changes. ptbk coder runs your harness as it is, with its own tools, its own sandbox and its own MCP servers. The **features above** explain every capability in this table in detail." — where "features above" links to `#features`, so no capability is explained twice.
apps/coder-landing\specs\sections\agent-book.md:3:Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
apps/coder-landing\specs\sections\agent-book.md:18:3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
apps/coder-landing\specs\sections\agent-book.md:22:1. Caption above (small monospace, gray): `readonly preview of agents/developer.book`
apps/coder-landing\specs\sections\advanced-features.md:16:| 1   | Verified by your tests         | `ptbk coder run --harness claude-code --test npm test`                                           |
apps/coder-landing\specs\sections\advanced-features.md:17:| 2   | Test before coding             | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                 |
apps/coder-landing\specs\sections\advanced-features.md:19:| 4   | Autopilot git                  | `ptbk coder run --harness claude-code --auto-pull --auto-push`                                   |
apps/coder-landing\specs\sections\advanced-features.md:21:| 6   | Isolated worktrees             | `ptbk coder run --harness claude-code --isolate`                                                 |
apps/coder-landing\specs\sections\advanced-features.md:22:| 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
apps/coder-landing\specs\sections\advanced-features.md:23:| 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
apps/coder-landing\specs\sections\advanced-features.md:24:| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
apps/coder-landing\specs\sections\advanced-features.md:25:| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
apps/coder-landing\specs\sections\advanced-features.md:27:| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
apps/coder-landing\specs\sections\advanced-features.md:28:| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
apps/coder-landing\specs\sections\advanced-features.md:29:| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
apps/coder-landing\specs\sections\advanced-features.md:30:| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
apps/coder-landing\specs\sections\advanced-features.md:32:| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
apps/coder-landing\specs\product.md:25:-   The agent's behavior lives in a versioned `.book` file, not in one vendor's prompt box. The same queue and behavior can move between supported harnesses with `--harness`; opencode can use the provider, including a local model, configured in that harness.
apps/coder-landing\specs\product.md:33:3. `ptbk coder run` (or `ptbk coder server`) feeds prompts one by one to the selected harness, with:
apps/coder-landing\specs\product.md:45:| `ptbk coder init`   | Scaffold `prompts/`, `prompts/done/`, `agents/developer.book`, `AGENTS.md`, `.env` agent-identity entries, `.gitignore`, `package.json` scripts, `.vscode/settings.json` |
apps/coder-landing\specs\product.md:46:| `ptbk coder run`    | Process the prompt queue once, then exit                                                                                                                                 |
apps/coder-landing\src\data\liveDemoScript.ts:120: * Note: The live terminal is framed exactly like the real `ptbk coder run` dashboard, which fits itself
apps/coder-landing\src\data\liveDemoScript.ts:200: * Builds the scripted `ptbk coder run` session played once in the live terminal.
apps/coder-landing\src\data\liveDemoScript.ts:202: * The dashboard is drawn for the requested terminal width, exactly like the real `ptbk coder run` UI
apps/coder-landing\src\data\harnessCatalog.ts:2: * Identifier of one supported harness, matching the `--harness` CLI option of `ptbk coder`.
apps/coder-landing\src\data\harnessCatalog.ts:6:    | 'openai-codex'
apps/coder-landing\src\data\harnessCatalog.ts:18:     * CLI identifier passed to `--harness`
apps/coder-landing\src\data\harnessCatalog.ts:38:     * Whether the `--model` option is required for this harness
apps/coder-landing\src\data\harnessCatalog.ts:43:     * Example values for the `--model` option
apps/coder-landing\src\data\harnessCatalog.ts:59: * All thinking levels accepted by `--thinking-level` for supported harnesses.
apps/coder-landing\src\data\harnessCatalog.ts:78:        sampleCommand: 'ptbk coder run --harness claude-code --model fable --thinking-level max',
apps/coder-landing\src\data\harnessCatalog.ts:82:        harnessName: 'openai-codex',
apps/coder-landing\src\data\harnessCatalog.ts:86:            'The OpenAI Codex CLI. It needs an explicit model — or --model default, which keeps the model configured in Codex itself — and it only spends credits beyond your rate limits when you pass --allow-credits.',
apps/coder-landing\src\data\harnessCatalog.ts:89:        sampleCommand: 'ptbk coder run --harness openai-codex --model gpt-5.6-terra',
apps/coder-landing\src\data\harnessCatalog.ts:100:        sampleCommand: 'ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh',
apps/coder-landing\src\data\harnessCatalog.ts:110:        sampleCommand: 'ptbk coder run --harness gemini --model gemini-3-flash-preview',
apps/coder-landing\src\data\harnessCatalog.ts:121:        sampleCommand: 'ptbk coder run --harness qwen-code --model qwen3.8-max',
apps/coder-landing\src\data\harnessCatalog.ts:132:        sampleCommand: 'ptbk coder run --harness opencode',
apps/coder-landing\src\data\harnessCatalog.ts:142:        sampleCommand: 'ptbk coder run --harness cline',
apps/coder-landing\src\data\developerAgentBook.ts:5: * Source of the default developer agent which `ptbk coder init` creates at `agents/developer.book`.
apps/coder-landing\src\data\developerAgentBook.ts:7: * Note: This mirrors `agents/default/developer.book` from the Promptbook repository
apps/coder-landing\src\data\coreBenefits.ts:46:            'The behavior of the agent lives in a .book file you commit next to the code, not in one vendor prompt box. Change --harness and the same queue runs on Claude Code or OpenAI Codex. With opencode you point it at whatever provider you configured, local models included.',
apps/coder-landing\src\data\comparison.ts:17:export type ComparedHarnessName = Extract<HarnessName, 'claude-code' | 'openai-codex' | 'opencode'>;
apps/coder-landing\src\data\comparison.ts:161:export const COMPARED_HARNESS_NAMES: ReadonlyArray<ComparedHarnessName> = ['claude-code', 'openai-codex', 'opencode'];
apps/coder-landing\src\data\comparison.ts:219:        ptbkCoderCell: { level: 'built-in', note: 'ptbk coder run' },
apps/coder-landing\src\data\comparison.ts:231:        ptbkCoderCell: { level: 'built-in', note: '--harness' },
apps/coder-landing\src\data\commands.ts:36:export const RUN_COMMAND = 'ptbk coder run --harness claude-code';
apps/coder-landing\src\data\commands.ts:51:export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
apps/coder-landing\src\data\commands.ts:56:export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
apps/coder-landing\src\data\commands.ts:61:export const GIT_CHANGES_CONTINUE_COMMAND = 'ptbk coder run --harness claude-code --git-changes continue';
apps/coder-landing\src\data\commands.ts:66:export const DRY_RUN_COMMAND = 'ptbk coder run --dry-run';
apps/coder-landing\src\data\commands.ts:71:export const PING_COMMAND = 'ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh';
apps/coder-landing\src\data\commands.ts:77:    'ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h';
apps/coder-landing\src\data\commands.ts:84:    'ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test';
apps/coder-landing\src\data\commands.ts:90:    'ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1';
apps/coder-landing\src\data\commands.ts:96:    'ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md';
apps/coder-landing\src\data\advancedFeatures.ts:43:        sampleCommand: 'ptbk coder run --harness claude-code --test npm test',
apps/coder-landing\src\data\advancedFeatures.ts:61:        sampleCommand: 'ptbk coder run --harness claude-code --auto-pull --auto-push',
apps/coder-landing\src\data\advancedFeatures.ts:73:        sampleCommand: 'ptbk coder run --harness claude-code --isolate',
apps/coder-landing\src\data\advancedFeatures.ts:79:        sampleCommand: 'ptbk coder server --port 4441 --harness claude-code',
apps/coder-landing\src\data\advancedFeatures.ts:84:        sampleCommand: 'ptbk coder run --harness claude-code --min-priority 1 --max-priority 5',
apps/coder-landing\src\data\advancedFeatures.ts:96:        sampleCommand: 'ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m',
apps/coder-landing\src\data\advancedFeatures.ts:120:        sampleCommand: 'ptbk coder run --harness claude-code --no-auto',
apps/coder-landing\src\data\advancedFeatures.ts:126:        sampleCommand: 'ptbk coder run --harness claude-code --no-questions',
apps/coder-landing\specs\content\harness-catalog.md:7:-   Harness identifiers are the exact values accepted by `--harness`.
apps/coder-landing\specs\content\harness-catalog.md:9:-   The model example `default` means the default model of that harness. For `openai-codex` it overrides no model at
apps/coder-landing\specs\content\harness-catalog.md:16:| 2 | `openai-codex`   | OpenAI Codex       | OpenAI          | **yes**        | `gpt-5.2-codex`, `default`        | `#FFFFFF`    |
apps/coder-landing\specs\content\harness-catalog.md:26:2. **OpenAI Codex** — "The OpenAI Codex CLI. It needs an explicit model — or --model default, which keeps the model configured in Codex itself — and it only spends credits beyond your rate limits when you pass --allow-credits."
apps/coder-landing\specs\content\harness-catalog.md:37:| `claude-code`    | `ptbk coder run --harness claude-code --model fable --thinking-level max`            |
apps/coder-landing\specs\content\harness-catalog.md:38:| `openai-codex`   | `ptbk coder run --harness openai-codex --model gpt-5.6-terra`                        |
apps/coder-landing\specs\content\harness-catalog.md:39:| `github-copilot` | `ptbk coder run --harness github-copilot --model gpt-5.6 --thinking-level xhigh`     |
apps/coder-landing\specs\content\harness-catalog.md:40:| `gemini`         | `ptbk coder run --harness gemini --model gemini-3-flash-preview`                     |
apps/coder-landing\specs\content\harness-catalog.md:41:| `qwen-code`      | `ptbk coder run --harness qwen-code --model qwen3.8-max`                             |
apps/coder-landing\specs\content\harness-catalog.md:42:| `opencode`       | `ptbk coder run --harness opencode`                                                  |
apps/coder-landing\specs\content\harness-catalog.md:43:| `cline`          | `ptbk coder run --harness cline`                                                     |
apps/coder-landing\specs\content\harness-catalog.md:50:-   `openai-codex`: hexagon with a circle inside
apps/coder-landing\specs\content\developer-agent.md:5:This is the **default developer agent** which `ptbk coder init` creates at `agents/developer.book` (it mirrors `agents/default/developer.book` in the Promptbook repository). It is written in the **Book language** — Promptbook's human-readable language for defining AI agents, where the first line is the agent name and keyword *commitments* (`META`, `RULE`, `PERSONA`, `KNOWLEDGE`, …) each start a block.
apps/coder-landing\specs\content\comparison-matrix.md:20:| 3   | `openai-codex` | from catalog | from catalog | from catalog                     |
apps/coder-landing\specs\content\comparison-matrix.md:44:| 2   | The whole backlog, unattended        | ✅ `ptbk coder run`                   | 🛠 Script one session per task       |
apps/coder-landing\specs\content\comparison-matrix.md:46:| 4   | The same agent on another vendor     | ✅ `--harness`                        | ❌ One of the harnesses *(override below)* |
apps/coder-landing\specs\content\commands.md:12:| `RUN_COMMAND`                   | `ptbk coder run --harness claude-code`                                                                                                                                                                 | [Quickstart step 4](../sections/quickstart.md)                              |
apps/coder-landing\specs\content\commands.md:15:| `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:16:| `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:17:| `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:18:| `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:19:| `PING_COMMAND`                  | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`                                                                                                                    | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:20:| `PING_PERIOD_COMMAND`           | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h`                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:21:| `SERVER_COMMAND`                | `ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test`                                                     | [Quickstart step 5](../sections/quickstart.md)                              |
apps/coder-landing\specs\content\commands.md:22:| `LIVE_DEMO_RUN_COMMAND`         | `ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1` | Typed in the [live terminal](../components/live-terminal.md)                |
apps/coder-landing\specs\content\commands.md:23:| `AGENT_RUN_COMMAND`             | `ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md`                                                                                                 | [Agent personas](../sections/agent-book.md)                                 |
apps/coder-landing\specs\content\commands.md:32:-   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
apps/coder-landing\specs\content\commands.md:33:-   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
apps/coder-landing\specs\content\commands.md:35:-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
apps/coder-landing\specs\content\commands.md:37:-   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
apps/coder-landing\specs\content\commands.md:43:-   `ptbk coder ping` — send one tiny dummy prompt to the harness and model selected with `--harness`, `--model` and `--thinking-level` and report the answer, the response time and the reported usage. It runs no coding task or commit; when the selected harness has missing local ignore rules, it asks before adding them to `.gitignore`. `--allow-credits` and `--no-ui` behave as for `run`, where `--no-ui` streams the raw harness output instead of the compact result.
apps/coder-landing\specs\components\live-terminal.md:3:A scripted terminal in the [hero](../sections/hero.md) which shows what a real `ptbk coder run` rich terminal session looks like, without running the user project.
apps/coder-landing\specs\components\live-terminal.md:9:-   Title: `ptbk coder run`, the command the session is running.
apps/coder-landing\specs\components\live-terminal.md:11:-   The body is a character grid fitted to the width available to it, the same way a terminal emulator fits its grid into its window: as many character cells as fit at the preferred terminal text size, clamped to the same 56–96 columns the real `ptbk coder run` frame is clamped to, and a smaller text size when even 56 columns would not fit. The dashboard is drawn for that column count, so the terminal **never scrolls horizontally** at any viewport width.
apps/coder-landing\specs\components\live-terminal.md:23:2.  The shared agent visual appears as the same animated terminal ASCII text centered in the terminal stream as in `ptbk coder run`. The demo `.book` source is passed to the shared terminal-avatar renderer and its `META VISUAL ascii-octopus` selects the same `AsciiOctopus` visual the real Promptbook Developer coder agent uses — the visual always comes from the agent book, never from a landing-specific fallback. The browser maps the renderer's ANSI foreground colors to text spans; it must not use a canvas or recreate the octopus in landing-specific text.
apps/coder-landing\specs\components\live-terminal.md:24:3.  The terminal prints the rich `ptbk coder run` dashboard boxes: `Session`, `Current task`, `Live output`, `Errors`, and `Controls`. The Session box includes every subscription limit the selected harness reports.
apps/coder-landing\specs\components\live-terminal.md:37:The story the script must always tell: _the user enters `ptbk coder run` → the agent visual starts → the real rich terminal dashboard reaches the final output_. This mirrors the actual CLI workflow in [`../product.md`](../product.md).
apps/coder-landing\specs\components\book-editor-embed.md:14:| Readonly notice | When a visitor attempts to edit, the editor shows: *"This is a preview of agents/developer.book. Run `ptbk coder init` to get your own editable copy."* |
apps/coder-landing\src\components\DeveloperAgentBookPreview\DeveloperAgentBookPreview.tsx:9: * Renders the default developer agent from `agents/developer.book` in a readonly `<BookEditor/>`.
apps/coder-landing\src\components\DeveloperAgentBookPreview\DeveloperAgentBookPreview.tsx:26:                    'This is a preview of agents/developer.book. Run `ptbk coder init` to get your own editable copy.',
apps/coder-landing\src\components\CoreBenefitGraphic\CoreBenefitGraphic.tsx:177:                    <tspan fill="#7AEBFF">$ </tspan>ptbk coder run
apps/coder-landing\src\components\sections\Quickstart.tsx:53:                <code className="text-promptbook-blue">agents/developer.book</code> persona,{' '}
apps/coder-landing\src\components\sections\HarnessesSection.tsx:20:                    <code className="text-promptbook-blue">--harness</code>, add a{' '}
apps/coder-landing\src\components\sections\HarnessesSection.tsx:21:                    <code className="text-promptbook-blue">--model</code> if you want one, and for the harnesses that
apps/coder-landing\src\components\sections\HarnessesSection.tsx:22:                    support it a <code className="text-promptbook-blue">--thinking-level</code> (
apps/coder-landing\src\components\sections\HarnessesSection.tsx:41:                                    --harness {harness.harnessName}
apps/coder-landing\src\components\sections\ComparisonSection.tsx:39:                    Inside a single task nothing changes. ptbk coder runs your harness as it is, with its own tools, its
apps/coder-landing\src\components\sections\AgentsSection.tsx:6: * Renders the agents section - the `--agent agents/developer.book` option together with
apps/coder-landing\src\components\sections\AgentsSection.tsx:39:                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
apps/coder-landing\src\components\sections\AgentsSection.tsx:45:                    <p className="mb-4 font-mono text-sm text-gray-500">readonly preview of agents/developer.book</p>
apps/coder-landing\src\components\LiveTerminalDemo\SharedAgentTerminalVisual.tsx:12: * Shared terminal visual used by the real `ptbk coder run` UI and the landing live demo.
apps/coder-landing\src\components\LiveTerminalDemo\SharedAgentTerminalVisual.tsx:28: * Renders the exact terminal ASCII visual used by `ptbk coder run` for the default developer agent.
apps/coder-landing\src\components\LiveTerminalDemo\LiveTerminalDemo.tsx:82: * Renders one preview of `ptbk coder run` in action - a text terminal that starts
apps/coder-landing\src\components\LiveTerminalDemo\LiveTerminalDemo.tsx:158:                    <span className="ml-2 flex-1 truncate font-mono text-xs text-gray-400">ptbk coder run</span>
apps/coder-landing\src\components\LiveTerminalDemo\LiveTerminalDemo.tsx:167:                    aria-label="Simulated ptbk coder run terminal session"
apps/coder-landing\src\components\HarnessLogo\HarnessLogo.tsx:41:                {harnessName === 'openai-codex' && (
src\avatars\renderAvatarVisualTerminalText.test.ts:18: * Terminal frame size used by the `ptbk coder run` dashboard.
src\avatars\renderAvatarVisualTerminalText.test.ts:23: * Terminal frame height used by the `ptbk coder run` dashboard.
src\cli\cli-commands\common\questionsCliOptions.ts:82:            - Keep \`--no-auto\` for a supervised run, for example \`ptbk coder run --harness claude-code --no-auto\`.
src\cli\cli-commands\common\questionsCliOptions.ts:83:            - Keep \`--no-questions\` for an unattended run, for example \`ptbk coder run --harness claude-code --no-questions\`.
src\cli\cli-commands\common\promptRunnerCliOptions.ts:97:    - openai-codex: OpenAI Codex integration (requires --model when executing)
src\cli\cli-commands\common\promptRunnerCliOptions.ts:102:    - gemini: Google Gemini CLI integration (requires --model when executing)
src\cli\cli-commands\common\promptRunnerCliOptions.ts:103:    - qwen-code: Qwen Code CLI integration (requires --model when executing)
src\cli\cli-commands\common\promptRunnerCliOptions.ts:107: * Commander description for the `--harness` option.
src\cli\cli-commands\common\promptRunnerCliOptions.ts:116: * Runner harness names listed as alternatives of the `--harness` option in error messages.
src\cli\cli-commands\common\promptRunnerCliOptions.ts:120:export const PROMPT_RUNNER_HARNESS_OPTION_HINT = `--harness <${PROMPT_RUNNER_HARNESS_NAMES.join('|')}>`;
src\cli\cli-commands\common\promptRunnerCliOptions.ts:123: * Commander description for the `--model` option.
src\cli\cli-commands\common\promptRunnerCliOptions.ts:128:    Model to use or filter by (required when executing with openai-codex, gemini and qwen-code)
src\cli\cli-commands\common\promptRunnerCliOptions.ts:134:    For openai-codex, "default" overrides no model at all and keeps the one configured in Codex itself, which is what a ChatGPT-account login accepts
src\cli\cli-commands\common\promptRunnerCliOptions.ts:156:        new Option('--harness <harness-name>', PROMPT_RUNNER_HARNESS_OPTION_DESCRIPTION).env(PTBK_HARNESS_ENV),
src\cli\cli-commands\common\promptRunnerCliOptions.ts:158:    command.addOption(new Option('--model <model>', PROMPT_RUNNER_MODEL_OPTION_DESCRIPTION).env(PTBK_MODEL_ENV));
src\cli\cli-commands\common\promptRunnerCliOptions.ts:173:            '--thinking-level <thinking-level>',
src\cli\cli-commands\common\promptbook-cli\$ensurePromptbookCliInstallations.ts:12: * Checks local and global Promptbook CLI installations before an interactive coder run and offers to update them.
src\cli\cli-commands\common\promptbook-cli\$ensurePromptbookCliInstallations.ts:21: * @returns `true` when at least one installation was updated and the current coder run should stop
changelog\_current-preversion.md:40:-   `ptbk coder run` and `ptbk coder server` no longer walk into a full disk. A coding round writes prompt and log
changelog\_current-preversion.md:72:    which runs prompts, so `coder run`, `coder server` and `coder ping` all report it the same way. The terminal and
changelog\_current-preversion.md:115:    script points at, like `agents/developer.book`, `AGENTS.md` and `prompts/templates/common.md`, are created only
changelog\_current-preversion.md:147:    groups without starting a harness. Its optional `--harness`, `--model`, `--priority`, `--min-priority`, and
changelog\_current-preversion.md:152:-   Added support for plain markdown prompts without a status checkbox in **ptbk coder**. `ptbk coder run` and
changelog\_current-preversion.md:161:    harness, including `.qwen`; `ptbk coder run`, `ptbk coder server`, and `ptbk coder ping` detect a missing selected
changelog\_current-preversion.md:173:-   Fixed `ptbk coder --harness openai-codex --model default` failing for everyone signed in to OpenAI Codex with a
changelog\_current-preversion.md:176:    `ptbk coder ping` and `ptbk coder run` died before doing any work.
changelog\_current-preversion.md:178:    -   `--model default` now overrides **no model at all** for OpenAI Codex: Codex is started without `--model` and
changelog\_current-preversion.md:179:        keeps the model from its own configuration _(`~/.codex/config.toml`)_, exactly like `--model default` already
changelog\_current-preversion.md:181:    -   Naming a model explicitly, for example `--model gpt-5.2-codex`, is unchanged, and `--model` is still required
changelog\_current-preversion.md:232:    -   `ptbk coder run --harness qwen-code --model qwen3.8-max` runs the whole prompt queue through the Qwen Code CLI,
changelog\_current-preversion.md:235:    -   Like `openai-codex` and `gemini`, Qwen Code **requires an explicit `--model`**; `--model default` resolves to
changelog\_current-preversion.md:237:        just like the other harnesses. Qwen Code exposes no reasoning-effort flag, so `--thinking-level` does not
changelog\_current-preversion.md:526:    -   The duration uses the same `5h`, `30m`, `1h30m` format as the `--wait-*` options of `ptbk coder run` and is
changelog\_current-preversion.md:646:-   Fixed `ptbk coder run --test-before yes-and-fix` failing at the first prompt when its pre-coding verification
changelog\_current-preversion.md:657:-   Added automatic Promptbook CLI update checks when `ptbk coder run` starts in an interactive terminal, both for the default automatic queue and for `--no-auto` per-prompt confirmation. Before starting the coding queue, Promptbook now finds directly declared local `ptbk` / `@promptbook/cli` dependencies and directly installed global CLI packages, compares every installation with npm, and offers one confirmation when any of them is outdated. An approved update preserves each local package's existing `dependencies` or `devDependencies` placement, updates global installations globally, handles local and global installations together, and then asks for a fresh invocation so the running process cannot continue with already-loaded old code or a newly changed project manifest. Non-TTY runs do not perform the check. The npm registry lookup, version parsing, install-command construction, and TTY approval prompt are shared with the existing coding-harness updater; Promptbook's numbered prerelease revisions are compared semantically, so versions such as `0.114.0-8` and `0.114.0-9` no longer collapse into the same release.
changelog\_current-preversion.md:668:-   Fixed the scrolling of the live terminal on the `ptbk coder` landing page, which **scrolled sideways and cut the agent visual off**. The terminal drew its dashboard at a fixed 96 columns in a fixed text size, no matter how much room the hero column really gave it, and the 214-character sample command was drawn on one endless row, so the session had to be scrolled horizontally to be read at all. On top of that, the box drawing characters of the dashboard frame, the shade of the progress bar and the status marks are missing from the latin subset of the terminal font, so the browser drew them from a fallback font in which they are up to 1.6 times wider than one character cell — that alone pushed the right border of every box out of the frame, which is why the boxes looked ragged even where the text fitted. Finally the body had a fixed height shorter than the whole sample and auto-scrolled to the latest line, so the agent visual was scrolled out of view exactly when the run was finished. The terminal now fits itself into the width it gets, the same way a terminal emulator fits its character grid into its window: it takes as many character cells as fit at the preferred text size, clamped to the same **56–96 columns** the real `ptbk coder run` frame is clamped to, and scales the text down when even 56 columns would not fit. The whole dashboard is rebuilt for that column count, so a phone gets the same 56-column dashboard the real CLI draws in a narrow terminal, and the typed command wraps onto the next rows like real terminal output instead of widening the terminal. Every character which the terminal font does not provide is measured in the browser and pulled back onto the character grid with letter spacing, so each row is exactly as wide as the cells it is counted as and the box borders line up again; the highlighted `DONE` badge and the control keys carry their padding as spaces of the sample itself for the same reason. The body is now as tall as the whole scripted session, so nothing scrolls in either direction and **the octopus agent visual stays fully visible** once the sample run has finished. Specified in [`apps/coder-landing/specs/components/live-terminal.md`](apps/coder-landing/specs/components/live-terminal.md).
changelog\_current-preversion.md:671:    ANSI ASCII renderer as `ptbk coder run`, rather than showing the browser canvas avatar. The visual keeps the
changelog\_current-preversion.md:718:-   Every commit created by `ptbk coder` now contains **only the files of the operation which created it**. Each committing command used to run a plain `git add .` first, so `ptbk coder add --commit` committed the new prompt file together with whatever else happened to be changed in the project, `ptbk coder verify --commit` committed the archived prompt file together with the work in progress of the developer sitting next to it, and a `ptbk coder run` round committed the prompt file, the files the coding agent changed, and every unrelated change which was in the working tree — the last one was only masked by the clean working tree check, which `--ignore-git-changes` turns off. Now each command captures the state of the working tree before it changes anything and commits exactly the files which really changed in between: the created prompt file for `add`, the generated prompt files for `generate-boilerplates`, the created and updated configuration files for `init`, the archived or repaired prompt file for `verify`, and the prompt file together with the files the coding agent changed for `run`, `server` and `--isolate`. Anything which was already changed before the command started is left in the working tree, including a file which was staged by hand. There is one shared mechanism behind all of it — one commit scope captured before the operation and resolved against the working tree afterwards — which the round commit also reuses to normalize the line endings of exactly those files, and `commitChanges` now takes one single list of the relevant paths instead of the two overlapping path options it had before. Because the relevant paths are derived from git itself, a file ignored through `.gitignore` (such as the `.env` which `ptbk coder init` writes) can never end up among them, so scoping the commit cannot break on an ignored path.
changelog\_current-preversion.md:732:-   Added `ptbk coder ping`, which sends one tiny dummy prompt through a selected harness and model — for example `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh` — and reports the answer of the harness, the measured response time and the usage the harness reported for it. The ping is built on the very same runner the coding queue uses, so it really exercises the configured harness, model, thinking level and authentication (including the rate-limit retry behavior) instead of only checking that the harness command is installed. Besides testing that a connection works and how fast it answers, it is meant to be run **before** you actually need the harness: the tiny prompt starts the hourly/weekly quota window, so the refresh clock is already running by the time the real coding starts. The dummy work needs no tool and explicitly forbids reading, writing or changing anything, and both temporary artifacts the ping creates are removed again, so the project is left exactly as it was — nothing is written, changed or committed. `--no-ui` streams the raw harness output instead of the compact result, and `--allow-credits` behaves as in `ptbk coder run`. The `ptbk coder` landing page documents the new command among the advanced features.
changelog\_current-preversion.md:734:-   Added `--commit`, `--auto-push` and `--auto-pull` to `ptbk coder init`, `ptbk coder add`, `ptbk coder generate-boilerplates` and `ptbk coder verify`. These commands change project files but used to leave the result uncommitted; now `--auto-pull` pulls the latest changes before the command changes anything, `--commit` commits what the command created with the coding-agent git identity, and `--auto-push` pushes that commit to the remote repository. `ptbk coder verify` applies the synchronization around each single verification, so archiving a finished prompt file and appending a repair prompt are committed and pushed one by one instead of at the end of the whole session. All four commands share one implementation of the pulling, committing and pushing, a repository without any change is never committed as an empty commit, and `--auto-push` without `--commit` is refused with an actionable error. `ptbk coder run` and `ptbk coder server`, which already commit every successful round by default, are unchanged. As part of this, the temporary commit message file which `ptbk coder` writes inside the project is now always kept out of the created commit, so a project which does not keep the Promptbook temporary directory out of version control is no longer left with a dirty working tree after committing.
changelog\_current-preversion.md:742:-   Added `--test-before <no|yes-and-fail|yes-and-fix>` to `ptbk coder run`. Pre-coding verification is disabled by default, can stop the run with the complete test output when existing tests fail, or can create and run one shared-format repair prompt before the normal coding queue; an explicit `--test` command is used when provided and `npm test` is the default for the enabled modes. The Coder landing page and local VS Code coder terminals now document and use the repair mode.
changelog\_current-preversion.md:753:-   Added `--isolate` to `ptbk coder run`, which keeps the working tree you started the coder from untouched while a task is being implemented. Every prompt gets its own temporary git worktree in `.promptbook/coder-isolation-worktrees/<task-name>` on branch `ptbk-coder-isolation/<task-name>`, with its own copy of the project `.env`, and the coding agent, the `--test` verification command and the round commit all happen inside it. As soon as the task is implemented and verified, it is merged back into the branch the coder runs on as one commit — exactly like a non-isolated round — and the worktree together with its branch is deleted. When the merge fails, the task is marked as `[!]` instead of `[x]`, a commit describing the failed merge and the manual recovery steps is written into the original worktree, the temporary worktree is kept so the changes can be merged by hand, and the coder simply continues with the next task instead of stopping.
changelog\_current-preversion.md:764:-   Added automatic harness installation checks to `ptbk coder`. Before `ptbk coder run` and `ptbk coder server` start, and at the end of `ptbk coder init`, Promptbook now detects whether the used harness command is installed globally and compares its version with the newest one published to npm; when a harness is missing or outdated it offers to run `npm install -g <package>@latest`. One shared harness registry describes the command and npm package of all supported harnesses (OpenAI Codex, GitHub Copilot, Cline, Claude Code, Opencode, Gemini CLI), `ptbk coder init` checks OpenAI Codex and Claude Code, and non-interactive terminals only print the manual installation command.
changelog\_current-preversion.md:776:-   Added the selected `--thinking-level` to successful `ptbk coder` prompt status lines, alongside the harness and model (for example, `by Claude Code \`claude-opus-4-8\` thinking \`xhigh\``). The Coder landing page now documents this completion metadata.
changelog\_current-preversion.md:885:-   Updated the `ptbk coder` landing page live terminal so it now plays a one-shot `ptbk coder run` sample that mirrors the real rich CLI dashboard: typed command, shared animated agent visual rendered as colored terminal text, `Session` / `Current task` / `Live output` / `Controls` boxes, and final output without blinking, clearing, or looping. The terminal avatar now uses the shared avatar-to-ASCII rendering pipeline instead of a landing-only drawing.
changelog\_current-preversion.md:887:-   Added the `ptbk coder` landing page app at `apps/coder-landing` (http://localhost:4025/). The dark-mode, mobile-friendly Next.js page introduces `ptbk coder` to developers who know Claude Code or OpenAI Codex: copyable syntax-highlighted terminal samples walk from `npm install ptbk` through `ptbk coder init` to the kanban `ptbk coder server`, an animated fake terminal replays a scripted coding session, the `--agent agents/developer.book` option is demonstrated with the default developer agent in a readonly `<BookEditor/>`, and all six harnesses (`claude-code`, `openai-codex`, `github-copilot`, `gemini`, `opencode`, `cline`) are presented as cards with logos, model examples, and shell samples. The footer mirrors https://www.ptbk.io/en and states that `ptbk coder` is a Promptbook subproduct by AI Web s.r.o. The app follows the official Promptbook branding and ships with an `apps/coder-landing/specs/` folder of interlinked markdown specs which serve as the single source of truth for 1:1 replication of the page.
changelog\_current-preversion.md:903:-   Fixed the `ptbk coder run` rich terminal UI so sessions started with `--limit <run-count>` now show that configured run limit in the Session scope summary.
changelog\_current-preversion.md:911:-   Added `--limit <candidate-count>` to `ptbk coder find-refactor-candidates`, so a single run creates at most that many refactor prompts and, when more candidates are found, keeps only the most important ones. Importance is a combined severity score — the sum of `actual / threshold` ratios across every exceeded line-count, entity-count, function-count, and complexity threshold — computed while scanning, so files that overshoot more thresholds (or overshoot them further) are preferred. The limit is applied after skipping candidates that already have prompts and can be combined with `--level`. The positive-integer option parser previously private to `ptbk coder run` was extracted into a shared `createPositiveIntegerOptionParser` reused by both commands, and the VS Code "Find refactor candidates for Coding Agent" terminal now passes `--limit 10`.
changelog\_current-preversion.md:948:-   Bumped the default coding model used by the `coder:run` npm script created by `ptbk coder init` from `gpt-5.4` to `gpt-5.5`. The bundled `agents/developer.book` (copied into the consumer project from `agents/default/developer.book` via the existing CLI runtime-asset path resolution in `ensureCoderDeveloperAgentFile`) and the automatic post-init boilerplate generation are unchanged; only the `--model` flag baked into the generated script is updated, so `npm run coder:run` now drives OpenAI Codex with `gpt-5.5` by default.
changelog\_current-preversion.md:964:-   Added `--limit <run-count>` to `ptbk coder run`, letting coding-agent sessions stop cleanly after the configured number of successful prompt runs. The legacy `run-codex-prompts` option parser now accepts the same flag, and the VS Code terminal list includes a prepared Codex/Claude zig-zag script that runs one prompt per harness with `--limit 1`.
changelog\_current-preversion.md:1098:    -   Accepts `--priority <n>` to apply the same minimum-priority filter used by `ptbk coder run`.
changelog\_current-preversion.md:1101:-   Added `--agent <agent-book-path>` option to `ptbk coder run` and `ptbk coder server`:
changelog\_current-preversion.md:1105:    -   Example: `ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md --agent agents/coding-expert.book`
changelog\_current-preversion.md:1109:-   Added `ptbk coder server` command that runs the same prompt-processing logic as `ptbk coder run` but keeps the process alive and serves a kanban web UI on a configurable port (default `4441`):
changelog\_current-preversion.md:1116:    -   All options from `ptbk coder run` are supported (`--harness`, `--model`, `--thinking-level`, `--context`, `--test`, `--wait`, `--priority`, `--no-commit`, etc.) plus a new `--port` option (or `PTBK_CODER_SERVER_PORT` env var).
changelog\_current-preversion.md:1160:-   Added `--wait [duration]` flag to `ptbk coder run` for controlling the delay between prompt rounds:
changelog\_current-preversion.md:1166:-   Fixed `CliAgent` failing with `Missing --harness in non-dry run mode` when no `harness` is provided: `CliAgent` now falls back to the `PTBK_HARNESS` environment variable, mirroring `ptbk agent exec` behavior. Similarly falls back to `PTBK_MODEL` and `PTBK_THINKING_LEVEL` env vars. Extracted shared env-var constants (`PTBK_HARNESS_ENV`, `PTBK_MODEL_ENV`, `PTBK_THINKING_LEVEL_ENV`), harness names (`CLI_AGENT_HARNESS_NAMES`), and thinking-level values (`CLI_AGENT_THINKING_LEVEL_VALUES`) into the new `src/book-3.0/cliAgentEnv.ts` so CLI and library share a single source of truth. Fixed a pre-existing TypeScript error in `BookNodeAgentSource.ts` where `normalizeBookNodeAgentSource` did not accept the plain `string` allowed by its `book` option type.
changelog\_current-preversion.md:1189:-   Changed Promptbook CLI runner selection from `--agent` / `PTBK_AGENT` to `--harness` / `PTBK_HARNESS` across `ptbk coder run`, `ptbk agent-folder`, `ptbk agents-server start/dev`, standalone VPS runner setup, default scripts, and documentation.
changelog\_current-preversion.md:1391:    -   Added `other/vps/install.sh`, an idempotent Ubuntu 24.04 x64 installer that installs system dependencies, Node.js 22, `ptbk`, `pm2`, the selected coding runner, configures `.env`, opens the configured port when `ufw` is active, and runs `ptbk agents-server start --harness github-copilot --model gpt-5.4 --thinking-level xhigh` under `pm2` with boot startup.
changelog\_current-preversion.md:1421:    -   Starts the packaged Agents Server Next app and the local multi-agent message watcher from one command, with coding runner selection configurable by `--harness` / `PTBK_HARNESS`, `--model` / `PTBK_MODEL`, and `--thinking-level` / `PTBK_THINKING_LEVEL`.
changelog\_current-preversion.md:1489:-   Improved `ptbk coder run` and `ptbk agent-folder run-*` prompt script handling:
changelog\_current-preversion.md:1491:    -   Moved coder runner `.sh` files from beside prompt `.md` files into the shared `.promptbook` temporary root.
changelog\_current-preversion.md:1959:-   Fixed `ptbk coder run` rich UI time estimation so elapsed time, total duration, and ETA now advance correctly during normal `--no-wait` runs:
changelog\_current-preversion.md:1984:-   Fixed `ptbk coder run` pausing/resuming so the runner now behaves as an explicit three-state flow: `running`, `pausing`, and `paused`:
changelog\_current-preversion.md:1988:    -   Updated the rich `ptbk coder run` dashboard to render distinct `PAUSING` and `PAUSED` states, including pause-aware badges, footer controls, and animation timing so the UI stays visibly active while the current task is finishing and then becomes fully paused before the next task.
changelog\_current-preversion.md:2014:-   Enhanced the `ptbk coder run` octopus branding so the dashboard now opens with a wider animated mascot that uses space more efficiently while prompts are actively running:
changelog\_current-preversion.md:2020:-   Refined `ptbk coder run` rich terminal branding so the dashboard now opens with standalone `ptbk.io` octopus art and a more readable session summary:
changelog\_current-preversion.md:2065:-   Fixed rich `ptbk coder run` terminal UI flicker so long-running coding sessions no longer keep blinking the whole dashboard while waiting or streaming output:
changelog\_current-preversion.md:2072:-   Fixed `ptbk coder run` successful prompt rounds so temporary runtime `.log.txt` files no longer dirty the Git working tree between tasks:
changelog\_current-preversion.md:2092:-   Added `--preserve-logs` to `ptbk coder run`, so successful coding rounds can now keep their generated runner shell and shared runtime log files for debugging and analytics, while failed rounds automatically preserve those artifacts even without the flag:
changelog\_current-preversion.md:2098:-   Added `--preserve-logs` to `ptbk coder run` / `scripts/run-codex-prompts`, so successful runs can now keep their generated temp prompt artifacts for debugging and analytics while the default behavior still cleans them up automatically:
changelog\_current-preversion.md:2103:-   Added live temporary `.log.txt` shell tracing to `ptbk coder run`, so each prompt round now writes the raw runner/test shell input and streamed output into a sibling debug log such as `prompt-1.log.txt` while the agent is running and then deletes that log again after the round finishes:
changelog\_current-preversion.md:2121:-   Changed `ptbk coder run` / `scripts/run-codex-prompts` git pushing to opt-in mode, so coding-agent runs now keep commits local by default and only push when `--auto-push` is explicitly provided:
changelog\_current-preversion.md:2189:-   Added a rich terminal UI for `ptbk coder run` that replaces the raw console output with a branded interactive dashboard:
changelog\_current-preversion.md:2199:-   Added optional `--test <test-command...>` support to `ptbk coder run`, so Promptbook Coder can verify each completed prompt with a shell command, retry the same prompt up to three times with the failing test output fed back into the coding agent, and annotate prompt status lines with retry counts such as `[x] (2 attempts)` or `[!] (failed after 3 attempts)`; Promptbook's local `.vscode/terminals.json` coder workflows now pass `--test npm run test`.
changelog\_current-preversion.md:2687:-   Fixed `ptbk coder run` so interactive waits are enabled by default again and `--no-wait` now correctly disables them for both local CLI usage and installed `ptbk` binaries.
changelog\_current-preversion.md:2688:-   Added configurable `--context` support to `ptbk coder run`, so extra coding instructions can now be passed inline or loaded from a project file such as `AGENTS.md`; Promptbook now stores its own coder context in [`AGENTS.md`](AGENTS.md) and the local VS Code coder workflows pass `--context AGENTS.md` instead of relying on hardcoded runner context.
changelog\_current-preversion.md:2689:-   Added optional `--thinking-level <low|medium|high|xhigh>` support to `ptbk coder run`, forwarding reasoning effort to OpenAI Codex and GitHub Copilot while keeping the command working without the flag; Promptbook's local VS Code coder workflows now pass `--thinking-level xhigh` explicitly.
changelog\_current-preversion.md:2690:-   Fixed `ptbk coder run` git staging/commit reliability when `.git/index.lock` blocks the auto-commit step:
changelog\_current-preversion.md:2923:-   Added `github-copilot` as a supported `ptbk coder run` runner, including CLI validation, runner dispatch, documentation, and a ready-to-run VS Code terminal preset.
changelog\_current-preversion.md:3900:-   Added automatic per-round LF normalization in `scripts/run-codex-prompts` (`ptbk coder run`):
changelog\_current-preversion.md:4161:-   Updated `scripts/run-codex-prompts` Gemini runner so `--harness gemini` now requires explicit `--model <name>` (with `--model default` support), matching OpenAI Codex model-selection behavior and wiring the selected model through execution metadata.
changelog\_current-preversion.md:4163:-   Updated `ptbk coder run` / `scripts/run-codex-prompts` Git identity fallback so missing coding-agent identity variables no longer fail the run: when `CODING_AGENT_GIT_NAME`, `CODING_AGENT_GIT_EMAIL`, and signing-key environment variables are not fully configured, commits now fall back to the default Git config and the CLI prints a cyan post-run tip recommending `CODING_AGENT_GIT_NAME`, `CODING_AGENT_GIT_EMAIL`, and either `CODING_AGENT_GIT_SIGNING_KEY` or `CODING_AGENT_GPG_KEY_ID` for cleaner commit history.
changelog\_current-preversion.md:4254:-   Added `--priority <minimum-priority>` to `scripts/run-codex-prompts` so coding tasks can be filtered by priority threshold (default `0` keeps current behavior), and updated runner stats to show skipped runnable tasks as `Priority <N` while preserving `--harness`/`--model` behavior.
changelog\_current-preversion.md:4493:-   Added support for Gemini CLI to the coding agent script with the `--harness gemini` flag, allowing non-interactive prompt execution and automated git commits.
changelog\_current-preversion.md:4627:-   Added Cline CLI as a second agent runner in the coding agent script with `--harness <openai-codex|cline>` flag.
changelog\_current-preversion.md:4628:-   Added Claude code as a third agent runner in the coding agent script with `--harness <openai-codex|cline|claude-code>` flag.
changelog\_current-preversion.md:4867:    -   Switched the GitHub Copilot runner to feed prompts through standard input instead, keeping `--model`, `--reasoning-effort`, and the existing non-interactive flags unchanged.
changelog\_current-preversion.md:4870:-   Enhanced the `ptbk agent-folder run` rich terminal UI so it now reflects queued-message workflows instead of mirroring `ptbk coder run`:
changelog\_current-preversion.md:4872:    -   The top branding now uses compact initials derived from the local `agent.book` title, making the dashboard identify the actual agent definition rather than only the runner selected by `--harness`.
src\cli\cli-commands\coder\ThinkingLevel.ts:4: * Supported thinking-level CLI values for coding-agent runners with configurable reasoning effort.
src\cli\cli-commands\coder\ThinkingLevel.ts:6: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\ThinkingLevel.ts:13: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\ThinkingLevel.ts:18: * Parses and validates an optional thinking-level option value.
src\cli\cli-commands\coder\ThinkingLevel.ts:20: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\server.ts:43: * Runs the same prompt processing logic as `ptbk coder run` but keeps the process alive
src\cli\cli-commands\coder\server.ts:62:                - Runs the same prompt processing as \`ptbk coder run\`
src\cli\cli-commands\coder\server.ts:70:                - Terminal controls match \`ptbk coder run\`: P pause/resume, S skip waiting, X end after current prompt
src\cli\cli-commands\coder\server.ts:180:            // [1] Parse the wait options and --no-auto (same logic as `coder run`)
src\cli\cli-commands\coder\server.test.ts:75:        await program.parseAsync(['node', 'test', 'server', '--dry-run', '--harness', 'qwen-code'], {
src\cli\cli-commands\coder\server.test.ts:94:        await program.parseAsync(['node', 'test', 'server', '--dry-run', '--harness', 'qwen-code', '--no-questions'], {
src\cli\cli-commands\coder\run.ts:38: * Initializes `coder run` command for Promptbook CLI utilities
src\cli\cli-commands\coder\run.ts:114:            A verified task is merged back into the branch the coder runs on and the worktree is deleted.
src\cli\cli-commands\coder\run.ts:285: * @private internal utility of `coder run` command
src\cli\cli-commands\coder\run.test.ts:59: * Creates a Commander program with the `coder run` subcommand registered.
src\cli\cli-commands\coder\run.test.ts:112:    it('checks Promptbook CLI installations before a default coder run', async () => {
src\cli\cli-commands\coder\run.test.ts:131:        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--harness', 'qwen-code'], { from: 'node' });
src\cli\cli-commands\coder\run.test.ts:142:        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--harness', 'qwen-code', '--no-questions'], {
src\cli\cli-commands\coder\run.test.ts:287:        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--thinking-level', 'max'], { from: 'node' });
src\cli\cli-commands\coder\printCoderRunFailure.ts:5: * Prints why a coder run has ended to the terminal.
src\cli\cli-commands\coder\printCoderRunFailure.ts:7: * @private internal utility of `coder run` and `coder server` commands
src\cli\cli-commands\coder\ping.test.ts:93:                '--harness',
src\cli\cli-commands\coder\ping.test.ts:94:                'openai-codex',
src\cli\cli-commands\coder\ping.test.ts:95:                '--model',
src\cli\cli-commands\coder\ping.test.ts:97:                '--thinking-level',
src\cli\cli-commands\coder\ping.test.ts:105:                agentName: 'openai-codex',
src\cli\cli-commands\coder\ping.test.ts:115:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code'], { from: 'node' });
src\cli\cli-commands\coder\ping.test.ts:123:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'qwen-code', '--model', 'qwen3.8-flash'], {
src\cli\cli-commands\coder\ping.test.ts:135:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code', '--no-questions'], {
src\cli\cli-commands\coder\ping.test.ts:148:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code'], { from: 'node' });
src\cli\cli-commands\coder\ping.test.ts:161:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code', '--no-ui'], { from: 'node' });
src\cli\cli-commands\coder\ping.test.ts:182:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code'], { from: 'node' });
src\cli\cli-commands\coder\ping.test.ts:196:                '--harness',
src\cli\cli-commands\coder\ping.test.ts:198:                '--model',
src\cli\cli-commands\coder\ping.test.ts:200:                '--thinking-level',
src\cli\cli-commands\coder\ping.test.ts:222:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code', '--period', '1h30m'], {
src\cli\cli-commands\coder\ping.test.ts:234:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code', '--period', '0s'], {
src\cli\cli-commands\coder\ping.test.ts:246:        await program.parseAsync(['node', 'test', 'ping', '--harness', 'claude-code', '--period', 'often'], {
src\cli\cli-commands\coder\mergeStringRecordJsonFile.test.ts:67:                'coder:run': 'npx ptbk coder run',
src\cli\cli-commands\coder\mergeStringRecordJsonFile.test.ts:94:                'coder:run': 'npx ptbk coder run',
src\cli\cli-commands\coder\list.ts:33:            - Optional --harness and --model filters show only prompts compatible with that runner
src\cli\cli-commands\coder\list.test.ts:81:                '--harness',
src\cli\cli-commands\coder\list.test.ts:83:                '--model',
src\cli\cli-commands\coder\init.ts:42:const CODER_INIT_CHECKED_HARNESS_NAMES: ReadonlyArray<PromptRunnerHarnessName> = ['openai-codex', 'claude-code'];
src\cli\cli-commands\coder\GitChangesMode.ts:8: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\GitChangesMode.ts:15: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\GitChangesMode.ts:22: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\GitChangesMode.ts:29: * @private internal shared utility of `ptbk coder run`
src\cli\cli-commands\coder\getDefaultCoderPackageJsonScripts.ts:8: * Name of the npm script which `ptbk coder run` uses to verify the project.
src\cli\cli-commands\coder\getDefaultCoderPackageJsonScripts.ts:55:            'npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max',
src\cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:19:export const CODER_DEVELOPER_AGENT_FILE_PATH = 'agents/developer.book';
src\cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:26:export const DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH = 'agents/default/developer.book';
src\cli\cli-commands\coder\boilerplateTemplates.test.ts:118:        expect(defaultCoderPackageJsonScripts['coder:run']).toContain('--agent agents/developer.book');
src\cli\cli-commands\coder\boilerplateTemplates.test.ts:200:        const existingCoderRunScript = 'npx ptbk coder run --harness github-copilot --agent agents/my-own.book';
src\cli\cli-commands\common\coderGitSyncCliOptions.ts:36: * Note: Unlike `ptbk coder run`, which commits by default and opts out through `--no-commit`,
src\cli\cli-commands\common\harness\$checkHarnessInstallation.test.ts:55:        const definition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\$checkHarnessInstallation.test.ts:70:        const definition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\$checkHarnessInstallation.test.ts:82:        const definition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\$checkHarnessInstallation.test.ts:95:        const definition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\$ensureHarnessInstallations.ts:12: * Harness names which are `undefined`, for example when no `--harness` is selected in a dry run,
src\cli\cli-commands\common\harness\resolveHarnessUpdatePlan.test.ts:5:    const codexDefinition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\resolveHarnessInstallationMethodFromPaths.test.ts:5:    const codexDefinition = getHarnessDefinition('openai-codex');
src\cli\cli-commands\common\harness\HarnessDefinition.ts:58:     * Harness identifier used by the `--harness` option.
src\cli\cli-commands\common\harness\HarnessDefinition.ts:106:    'openai-codex': {
src\cli\cli-commands\common\harness\HarnessDefinition.ts:107:        harnessName: 'openai-codex',
src\cli\cli-commands\common\harness\HarnessDefinition.test.ts:46:        expect(HARNESS_DEFINITIONS['openai-codex'].standaloneInstallation).toEqual({
src\cli\cli-commands\common\harness\HarnessDefinition.test.ts:67:        expect(buildHarnessInstallCommand(getHarnessDefinition('openai-codex'))).toBe(
src\cli\cli-commands\common\harness\formatHarnessManualUpdateInstruction.test.ts:12:        definition: getHarnessDefinition('openai-codex'),
src\cli\cli-commands\agents-server\run.test.ts:112:            ['node', 'test', 'start', '--harness', 'github-copilot', '--model', 'gpt-5.4', '--thinking-level', 'xhigh'],
src\cli\cli-commands\agents-server\run.test.ts:131:        process.env.PTBK_HARNESS = 'openai-codex';
src\cli\cli-commands\agents-server\run.test.ts:140:            agentName: 'openai-codex',
src\cli\cli-commands\agents-server\run.test.ts:152:        process.env.PTBK_HARNESS = 'openai-codex';
src\cli\cli-commands\agents-server\run.test.ts:164:                '--harness',
src\cli\cli-commands\agents-server\run.test.ts:166:                '--model',
src\cli\cli-commands\agents-server\run.test.ts:168:                '--thinking-level',
src\cli\cli-commands\agents-server\run.test.ts:188:        await program.parseAsync(['node', 'test', 'start', '--harness', 'github-copilot', '--force-build'], {
src\cli\cli-commands\agents-server\run.test.ts:223:            ['node', 'test', 'dev', '--harness', 'github-copilot', '--model', 'gpt-5.4', '--thinking-level', 'xhigh'],
src\cli\cli-commands\agent-folder\run.test.ts:84:                '--harness',
src\cli\cli-commands\agent-folder\run.test.ts:86:                '--model',
src\cli\cli-commands\agent-folder\run.test.ts:88:                '--thinking-level',
src\cli\cli-commands\agent-folder\run.test.ts:120:        await program.parseAsync(['node', 'test', 'run', '--harness', 'github-copilot'], {
src\cli\cli-commands\agent-folder\run.test.ts:140:        await program.parseAsync(['node', 'test', 'run-once', '--harness', 'github-copilot', '--no-commit'], {
src\cli\cli-commands\agent-folder\run.test.ts:158:            ['node', 'test', 'run-once', '--harness', 'github-copilot', '--auto-push', '--auto-pull'],
src\cli\cli-commands\agent-folder\run.test.ts:177:        await program.parseAsync(['node', 'test', 'tick', '--harness', 'github-copilot'], {
src\cli\cli-commands\agent-folder\run.test.ts:197:                '--harness',
src\cli\cli-commands\agent-folder\run.test.ts:199:                '--model',
src\cli\cli-commands\agent\run.test.ts:61:                '--harness',
src\cli\cli-commands\agent\run.test.ts:62:                'openai-codex',
src\cli\cli-commands\agent\run.test.ts:63:                '--model',
src\cli\cli-commands\agent\run.test.ts:65:                '--thinking-level',
src\cli\cli-commands\agent\run.test.ts:77:            agentName: 'openai-codex',
src\cli\cli-commands\agent\run.test.ts:98:                '--harness',
src\cli\cli-commands\agent\run.test.ts:100:                '--model',
src\cli\cli-commands\agent\run.test.ts:136:                '--harness',
src\cli\cli-commands\agent\run.test.ts:137:                'openai-codex',
src\cli\cli-commands\agent\run.test.ts:138:                '--model',
src\book-3.0\cliAgentEnv.ts:9:    'openai-codex',
src\book-3.0\cliAgentEnv.ts:19: * All supported thinking-level values for CLI coding-agent runners.
src\book-3.0\cliAgentEnv.ts:26: * Environment variable used as the default runner identifier when `--harness` is omitted or not set in `CliAgent`.
src\book-3.0\cliAgentEnv.ts:28: * Set this to one of the harness names (`openai-codex`, `github-copilot`, `cline`, `claude-code`, `opencode`, `gemini`,
src\book-3.0\cliAgentEnv.ts:36: * Environment variable used as the default runner model when `--model` is omitted or not set in `CliAgent`.
src\book-3.0\cliAgentEnv.ts:43: * Environment variable used as the default thinking level when `--thinking-level` is omitted or not set in `CliAgent`.
src\book-3.0\CliAgent.test.ts:53:            harness: 'openai-codex',
src\book-3.0\CliAgent.test.ts:67:                agentName: 'openai-codex',

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg --files src/cli/cli-commands/coder src/cli/cli-commands/common src | rg \"(coder|Agent|agent).*\\.(ts|test\\.ts)"'$|parseAgentSource|AgentSource"; Get-Content src/cli/cli-commands/coder/run.ts; Get-Content src/cli/cli-commands/coder/run.test.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 883ms:
src/cli/cli-commands/coder\waitOptions.ts
src/cli/cli-commands/coder\verify.ts
src/cli/cli-commands/coder\verify.test.ts
src/cli/cli-commands/coder\ThinkingLevel.ts
src/cli/cli-commands/coder\server.ts
src/cli/cli-commands/coder\server.test.ts
src/cli/cli-commands/coder\run.ts
src/cli/cli-commands/coder\run.test.ts
src/cli/cli-commands/coder\printInitializationSummary.ts
src/cli/cli-commands/coder\printCoderRunFailure.ts
src/cli/cli-commands/coder\ping.ts
src/cli/cli-commands/coder\ping.test.ts
src/cli/cli-commands/coder\mergeStringRecordJsonFile.ts
src/cli/cli-commands/coder\mergeStringRecordJsonFile.test.ts
src/cli/cli-commands/coder\list.ts
src/cli/cli-commands/coder\list.test.ts
src/cli/cli-commands/coder\initializeCoderProjectConfiguration.ts
src/cli/cli-commands/coder\init.ts
src/cli/cli-commands/coder\init.test.ts
src/cli/cli-commands/coder\GitChangesMode.ts
src/cli/cli-commands/coder\getTypescriptModule.ts
src/cli/cli-commands/coder\getTypescriptModule.test.ts
src/cli/cli-commands/coder\getDefaultCoderVscodeSettings.ts
src/cli/cli-commands/coder\getDefaultCoderPackageJsonScripts.ts
src/cli/cli-commands/coder\generate-boilerplates.ts
src/cli/cli-commands/coder\generate-boilerplates.test.ts
src/cli/cli-commands/coder\formatDisplayPath.ts
src/cli/cli-commands/coder\find-unwritten.ts
src/cli/cli-commands/coder\find-refactor-candidates.ts
src/cli/cli-commands/coder\find-refactor-candidates.test.ts
src/cli/cli-commands/coder\find-fresh-emoji-tags.ts
src/cli/cli-commands/coder\ensureDirectory.ts
src/cli/cli-commands/coder\ensureCoderVscodeSettingsFile.ts
src/cli/cli-commands/coder\ensureCoderPackageJsonFile.ts
src/cli/cli-commands/coder\ensureCoderMarkdownFile.ts
src/cli/cli-commands/coder\ensureCoderGitignoreFile.ts
src/cli/cli-commands/coder\ensureCoderEnvFile.ts
src/cli/cli-commands/coder\ensureCoderDeveloperAgentFile.ts
src/cli/cli-commands/coder\coderReferencedArtifacts.ts
src/cli/cli-commands/coder\boilerplateTemplates.ts
src/cli/cli-commands/coder\boilerplateTemplates.test.ts
src/cli/cli-commands/coder\boilerplateCount.ts
src/cli/cli-commands/coder\boilerplateCount.test.ts
src/cli/cli-commands/coder\agentsFile.ts
src/cli/cli-commands/coder\add.ts
src/cli/cli-commands/coder\add.test.ts
src/cli/cli-commands/coder\$ensureCoderHarnessGitignoreRules.ts
src/cli/cli-commands/coder\$ensureCoderHarnessGitignoreRules.test.ts
src/cli/cli-commands/common\coderGitSyncCliOptions.ts
src/cli/cli-commands/common\coderGitSyncCliOptions.test.ts
src\utils\validators\url\isValidAgentUrl.ts
src\utils\validators\url\isValidAgentUrl.test.ts
src\commitments\_common\teamInternalAgentAccess.ts
src\commitments\_base\createEmptyAgentModelRequirements.ts
src\commitments\MESSAGE\AgentMessageCommitmentDefinition.ts
src\utils\agents\terminalAgentAvatarVisual.ts
src\utils\agents\terminalAgentAvatarVisual.test.ts
src\utils\agents\resolveAgentAvatarImageUrl.ts
src\utils\agents\resolveAgentAvatarImageUrl.test.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedProjectNames.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedProjectNames.test.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedExternalSources.ts
src\utils\agent-message-runtime\resolveAgentMessageTouchedExternalSources.test.ts
src\utils\agent-message-runtime\resolveAgentMessageRuntimeActivity.ts
src\utils\agent-message-runtime\resolveAgentMessageRuntimeActivity.test.ts
src\utils\agent-message-runtime\parseAgentMessageRuntimeLogEvents.ts
src\utils\agent-message-runtime\AgentMessageTouchedExternalSource.ts
src\utils\agent-message-runtime\agentMessageRuntimePaths.ts
src\utils\agent-message-runtime\AgentMessageProjectChange.ts
src\types\string_agent_url_private.ts
src\types\string_agent_url.ts
src\types\string_agent_permanent_id_private.ts
src\types\string_agent_name_private.ts
src\types\string_agent_name_in_book_private.ts
src\types\string_agent_name.ts
src\types\string_agent_hash_private.ts
src\collection\agent-collection\CreateAgentInput.ts
src\collection\agent-collection\CreateAgentInput.test.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\prepareAgentSourceForPersistence.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\createAgentPersistenceRecords.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\createAgentPersistenceRecords.test.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\buildAgentNameOrPermanentIdFilter.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\buildAgentNameOrPermanentIdFilter.test.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentsDatabaseSchema.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabaseOptions.ts
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts
src\collection\agent-collection\AgentCollection.ts
src\playground\permanent\agent-with-browser-playground.ts
src\cli\cli-commands\start-agents-server.ts
src\cli\cli-commands\common\coderGitSyncCliOptions.ts
src\cli\cli-commands\common\coderGitSyncCliOptions.test.ts
src\cli\cli-commands\coder.ts
src\cli\cli-commands\coder\waitOptions.ts
src\cli\cli-commands\coder\verify.ts
src\cli\cli-commands\coder\verify.test.ts
src\cli\cli-commands\coder\ThinkingLevel.ts
src\cli\cli-commands\coder\server.ts
src\cli\cli-commands\coder\server.test.ts
src\cli\cli-commands\coder\run.ts
src\cli\cli-commands\coder\run.test.ts
src\cli\cli-commands\coder\printInitializationSummary.ts
src\cli\cli-commands\coder\printCoderRunFailure.ts
src\cli\cli-commands\coder\ping.ts
src\cli\cli-commands\coder\ping.test.ts
src\cli\cli-commands\coder\mergeStringRecordJsonFile.ts
src\cli\cli-commands\coder\mergeStringRecordJsonFile.test.ts
src\cli\cli-commands\coder\list.ts
src\cli\cli-commands\coder\list.test.ts
src\cli\cli-commands\coder\initializeCoderProjectConfiguration.ts
src\cli\cli-commands\coder\init.ts
src\cli\cli-commands\coder\init.test.ts
src\cli\cli-commands\coder\GitChangesMode.ts
src\cli\cli-commands\coder\getTypescriptModule.ts
src\cli\cli-commands\coder\getTypescriptModule.test.ts
src\cli\cli-commands\coder\getDefaultCoderVscodeSettings.ts
src\cli\cli-commands\coder\getDefaultCoderPackageJsonScripts.ts
src\cli\cli-commands\coder\generate-boilerplates.ts
src\cli\cli-commands\coder\generate-boilerplates.test.ts
src\cli\cli-commands\coder\formatDisplayPath.ts
src\cli\cli-commands\coder\find-unwritten.ts
src\cli\cli-commands\coder\find-refactor-candidates.ts
src\cli\cli-commands\coder\find-refactor-candidates.test.ts
src\cli\cli-commands\coder\find-fresh-emoji-tags.ts
src\cli\cli-commands\coder\ensureDirectory.ts
src\cli\cli-commands\coder\ensureCoderVscodeSettingsFile.ts
src\cli\cli-commands\coder\ensureCoderPackageJsonFile.ts
src\cli\cli-commands\coder\ensureCoderMarkdownFile.ts
src\cli\cli-commands\coder\ensureCoderGitignoreFile.ts
src\cli\cli-commands\coder\ensureCoderEnvFile.ts
src\cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts
src\cli\cli-commands\coder\coderReferencedArtifacts.ts
src\cli\cli-commands\coder\boilerplateTemplates.ts
src\cli\cli-commands\coder\boilerplateTemplates.test.ts
src\cli\cli-commands\coder\boilerplateCount.ts
src\cli\cli-commands\coder\boilerplateCount.test.ts
src\cli\cli-commands\coder\agentsFile.ts
src\cli\cli-commands\coder\add.ts
src\cli\cli-commands\coder\add.test.ts
src\cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.ts
src\cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts
src\cli\cli-commands\agents-server.ts
src\cli\cli-commands\agents-server\startAgentsServer.ts
src\cli\cli-commands\agents-server\startAgentsServer\waitForLocalAgentRunnerLimits.ts
src\cli\cli-commands\agents-server\startAgentsServer\stopChildProcess.ts
src\cli\cli-commands\agents-server\startAgentsServer\startUserChatJobWorkerPump.ts
src\cli\cli-commands\agents-server\startAgentsServer\startNextServer.ts
src\cli\cli-commands\agents-server\startAgentsServer\StartAgentsServerOptions.ts
src\cli\cli-commands\agents-server\startAgentsServer\resolveAgentsServerChildHostname.ts
src\cli\cli-commands\agents-server\startAgentsServer\readInternalRouteErrorDetails.ts
src\cli\cli-commands\agents-server\startAgentsServer\PreparedAgentsServerLaunch.ts
src\cli\cli-commands\agents-server\startAgentsServer\prepareAgentsServerLaunch.ts
src\cli\cli-commands\agents-server\startAgentsServer\LocalAgentRunnerLimits.ts
src\cli\cli-commands\agents-server\startAgentsServer\loadAgentsServerProjectEnvironment.ts
src\cli\cli-commands\agents-server\startAgentsServer\forwardChildOutput.ts
src\cli\cli-commands\agents-server\startAgentsServer\createLocalAgentRunOptions.ts
src\cli\cli-commands\agents-server\startAgentsServer\createInternalRouteErrorMessage.ts
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerSupervisorState.ts
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerLogStreams.ts
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerChildEnvironment.ts
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerChildEnvironment.test.ts
src\cli\cli-commands\agents-server\run.ts
src\cli\cli-commands\agents-server\run.test.ts
src\cli\cli-commands\agents-server\printAgentsServerInitializationSummary.ts
src\cli\cli-commands\agents-server\initializeAgentsServerProjectConfiguration.ts
src\cli\cli-commands\agents-server\init.ts
src\cli\cli-commands\agents-server\init.test.ts
src\cli\cli-commands\agents-server\ensureAgentsServerGitignoreFile.ts
src\cli\cli-commands\agents-server\ensureAgentsServerEnvFile.ts
src\cli\cli-commands\agents-server\buildAgentsServer.ts
src\cli\cli-commands\agents-server\buildAgentsServer.test.ts
src\cli\cli-commands\agents-server\buildAgentsServer\writeAgentsServerBuildCache.ts
src\cli\cli-commands\agents-server\buildAgentsServer\synchronizeMaterializedAgentsServerRuntime.ts
src\cli\cli-commands\agents-server\buildAgentsServer\shouldCopyAgentsServerRuntimePath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\runNextBuild.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveNodeModulesPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveNextCliPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildOutputPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildAppPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerAppPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\readAgentsServerBuildCache.ts
src\cli\cli-commands\agents-server\buildAgentsServer\PreparedAgentsServerRuntime.ts
src\cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts
src\cli\cli-commands\agents-server\buildAgentsServer\normalizeAgentsServerBuildInputPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\NODE_MODULES_DIRECTORY_NAME.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isPathInsideNodeModules.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isFile.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isExcludedAgentsServerRuntimeSourcePath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isExcludedAgentsServerBuildInputPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isExcludedAgentsServerBuildInputDirectoryName.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildInputTestFile.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildCacheCurrent.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerAppPathMaterialized.ts
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerAppPath.ts
src\cli\cli-commands\agents-server\buildAgentsServer\getAgentsServerBuildInputRelativePaths.ts
src\cli\cli-commands\agents-server\buildAgentsServer\ensureAgentsServerBuild.ts
src\cli\cli-commands\agents-server\buildAgentsServer\createAgentsServerRuntimeEnvironment.ts
src\cli\cli-commands\agents-server\buildAgentsServer\createAgentsServerBuildSourceFingerprint.ts
src\cli\cli-commands\agents-server\buildAgentsServer\AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts
src\cli\cli-commands\agents-server\buildAgentsServer\AGENTS_SERVER_BUILD_CACHE_VERSION.ts
src\cli\cli-commands\agents-server\buildAgentsServer\AGENTS_SERVER_BUILD_CACHE_FILENAME.ts
src\cli\cli-commands\agents-server\buildAgentsServer\AgentsServerBuildCacheOptions.ts
src\cli\cli-commands\agents-server\buildAgentsServer\AgentsServerBuildCache.ts
src\cli\cli-commands\agent.ts
src\cli\cli-commands\agent-folder.ts
src\cli\cli-commands\agent-folder\tick.ts
src\cli\cli-commands\agent-folder\runMultiple.ts
src\cli\cli-commands\agent-folder\run.ts
src\cli\cli-commands\agent-folder\run.test.ts
src\cli\cli-commands\agent-folder\printAgentInitializationSummary.ts
src\cli\cli-commands\agent-folder\initializeAgentRunnerCommand.ts
src\cli\cli-commands\agent-folder\initializeAgentProjectConfiguration.ts
src\cli\cli-commands\agent-folder\init.ts
src\cli\cli-commands\agent-folder\agentRunCliOptions.ts
src\cli\cli-commands\agent-folder\agentProjectPaths.ts
src\cli\cli-commands\agent\run.test.ts
src\cli\cli-commands\agent\exec.ts
src\cli\cli-commands\agent\chat.ts
src\cli\cli-commands\agent\agentCliOptions.ts
src\utils\random\$randomAgentRule.ts
src\utils\random\$randomAgentPersona.ts
src\remote-server\startAgentServer.ts
src\book-components\Chat\utils\loadAgentProfile.ts
src\book-components\Chat\utils\agentProjectToolCall.ts
src\book-components\Chat\utils\agentProjectToolCall.test.ts
src\transpilers\agent-os\register.ts
src\transpilers\agent-os\AgentOsTranspiler.ts
src\transpilers\agent-os\AgentOsTranspiler.test.ts
src\transpilers\openai-agents\register.ts
src\transpilers\openai-agents\OpenAiAgentsTranspiler.ts
src\transpilers\openai-agents\OpenAiAgentsTranspiler.test.ts
src\book-components\Chat\AgentChip\index.ts
src\book-3.0\LiteAgent.ts
src\book-3.0\LiteAgent.test.ts
src\book-3.0\describeAgentPlannedMessageSchedule.ts
src\book-3.0\createAgentMessageSidecarBaseName.ts
src\book-3.0\cliAgentEnv.ts
src\book-3.0\CliAgent.ts
src\book-3.0\CliAgent.test.ts
src\book-3.0\BookNodeAgentSource.ts
src\book-3.0\AgentTeamConversationWorkspace.ts
src\book-3.0\AgentPlannedMessagesSidecar.ts
src\book-3.0\AgentPlannedMessagesSidecar.test.ts
src\book-3.0\AgentMessageRunReport.ts
src\book-3.0\AgentMessageRunReport.test.ts
src\book-3.0\agentFolderPaths.ts
src\book-2.0\utils\generatePlaceholderAgentProfileImageUrl.ts
src\llm-providers\agent\self-learning\SelfLearningManager.ts
src\llm-providers\agent\self-learning\SelfLearningManager.test.ts
src\llm-providers\agent\RemoteAgentOptions.ts
src\llm-providers\agent\RemoteAgent.ts
src\llm-providers\agent\RemoteAgent.test.ts
src\llm-providers\agent\register-constructor.ts
src\llm-providers\agent\register-configuration.ts
src\llm-providers\openai\utils\loadOpenAiAgentsModule.ts
src\llm-providers\agent\playground\playground.ts
src\llm-providers\agent\emitAgentLlmExecutionToolsAssistantPreparationProgress.ts
src\llm-providers\agent\CreateAgentLlmExecutionToolsOptions.ts
src\llm-providers\agent\createAgentLlmExecutionTools.ts
src\llm-providers\agent\AgentOptions.ts
src\llm-providers\agent\AgentLlmExecutionToolsPromptPreparer.ts
src\llm-providers\agent\AgentLlmExecutionToolsOpenAiAssistantRunner.ts
src\llm-providers\agent\AgentLlmExecutionToolsAgentKitRunner.ts
src\llm-providers\agent\AgentLlmExecutionTools.ts
src\llm-providers\agent\AgentLlmExecutionTools.test.ts
src\llm-providers\agent\Agent.ts
src\llm-providers\agent\Agent.test.ts
src\import-plugins\AgentFileImportPlugin.ts
src\book-2.0\agent-source\TeammateProfileResolver.ts
src\book-2.0\agent-source\string_book.ts
src\book-2.0\agent-source\removeCommentsFromSystemMessage.ts
src\book-2.0\agent-source\pseudoAgentReferences.ts
src\book-2.0\agent-source\parseTeamCommitment.ts
src\book-2.0\agent-source\parseParameters.ts
src\book-2.0\agent-source\ParsedUnknownCommitment.ts
src\book-2.0\agent-source\parseAgentSourceWithCommitments.use.test.ts
src\book-2.0\agent-source\parseAgentSourceWithCommitments.unknown.test.ts
src\book-2.0\agent-source\parseAgentSourceWithCommitments.ts
src\book-2.0\agent-source\parseAgentSourceWithCommitments.title.test.ts
src\book-2.0\agent-source\parseAgentSourceWithCommitments.test.ts.todo
src\book-2.0\agent-source\parseAgentSourceWithCommitments.blocks.test.ts
src\book-2.0\agent-source\parseAgentSourcePrelude.ts
src\book-2.0\agent-source\parseAgentSource.ts
src\book-2.0\agent-source\parseAgentSource.test.ts
src\book-2.0\agent-source\parseAgentSource.import.test.ts
src\book-2.0\agent-source\parseAgentSource\ParsedAgentProfile.ts
src\book-2.0\agent-source\parseAgentSource\ParseAgentSourceState.ts
src\book-2.0\agent-source\parseAgentSource\extractParsedAgentProfile.ts
src\book-2.0\agent-source\parseAgentSource\extractInitialMessage.ts
src\book-2.0\agent-source\parseAgentSource\extractAgentProfileText.ts
src\book-2.0\agent-source\parseAgentSource\ensureMetaFullname.ts
src\book-2.0\agent-source\parseAgentSource\createCapabilitiesFromCommitment.ts
src\book-2.0\agent-source\parseAgentSource\consumeConversationSampleCommitment.ts
src\book-2.0\agent-source\parseAgentSource\applyMetaCommitment.ts
src\book-2.0\agent-source\padBook.ts
src\book-2.0\agent-source\normalizeAgentName.ts
src\book-2.0\agent-source\normalizeAgentName.test.ts
src\book-2.0\agent-source\getBookCommitmentLineType.ts
src\book-2.0\agent-source\extractOpenTeacherInstructions.ts
src\book-2.0\agent-source\extractOpenTeacherInstructions.test.ts
src\book-2.0\agent-source\extractMetaLinks.ts
src\book-2.0\agent-source\createTeamToolName.ts
src\book-2.0\agent-source\createDefaultAgentName.ts
src\book-2.0\agent-source\createCommitmentRegex.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.unknown.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.knowledge.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.delete.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.closed.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.blocks.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\ParsedAgentSourceWithCommitments.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\materializeInlineKnowledgeSources.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\filterCommitmentsForAgentModelRequirements.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts
src\book-2.0\agent-source\CreateAgentModelRequirementsOptions.ts
src\book-2.0\agent-source\createAgentModelRequirements.writing.test.ts
src\book-2.0\agent-source\createAgentModelRequirements.ts
src\book-2.0\agent-source\createAgentModelRequirements.tools.test.ts
src\book-2.0\agent-source\createAgentModelRequirements.commitmentAggregation.test.ts
src\book-2.0\agent-source\computeAgentHash.ts
src\book-2.0\agent-source\computeAgentHash.test.ts
src\book-2.0\agent-source\communication-samples.test.ts
src\book-2.0\agent-source\BookEditable.ts
src\book-2.0\agent-source\agentSourceVisibility.ts
src\book-2.0\agent-source\agentSourceVisibility.test.ts
src\book-2.0\agent-source\AgentSourceParseResult.ts
src\book-2.0\agent-source\AgentReferenceResolver.ts
src\book-2.0\agent-source\AgentModelRequirements.ts
src\book-2.0\agent-source\AgentBasicInformation.ts
src\llm-providers\openai\OpenAiAgentKitExecutionToolsToolBuilder.ts
src\llm-providers\openai\OpenAiAgentKitExecutionToolsOutputTypeMapper.ts
src\llm-providers\openai\OpenAiAgentKitExecutionToolsOptions.ts
src\llm-providers\openai\OpenAiAgentKitExecutionToolsInputBuilder.ts
src\llm-providers\openai\OpenAiAgentKitExecutionTools.ts
src\llm-providers\openai\OpenAiAgentKitExecutionTools.test.ts
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../../errors/assertsError';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { createPositiveIntegerOptionParser } from '../common/createPositiveIntegerOptionParser';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import {
    addQuestionsOption,
    assertUserConfirmationIsAllowed,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import { $ensurePromptbookCliInstallations } from '../common/promptbook-cli/$ensurePromptbookCliInstallations';
import type { PromptRunnerCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerExecutionOptions,
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import {
    DEFAULT_CODER_TEST_COMMAND,
    TEST_BEFORE_MODE_VALUES,
    type TestBeforeMode,
} from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { printCoderRunFailure } from './printCoderRunFailure';

/**
 * Initializes `coder run` command for Promptbook CLI utilities
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderRunCommand(program: Program): $side_effect {
    const command = program.command('run');
    command.description(
        spaceTrim(
            (block) => `
            Execute coding prompts through selected AI agent

            ${block(PROMPT_RUNNER_DESCRIPTION)}

            ${block(QUESTIONS_DESCRIPTION)}

            Features:
            - Automatically stages and commits changes with agent identity unless --no-commit is used
            - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
            - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
            - Optional --git-changes continue resumes the single prompt an interrupted coder left in the [^] status
            - Optional post-commit git push with explicit --auto-push opt-in
            - Optional pre-prompt git pull with explicit --auto-pull opt-in
            - Optional --isolate runs every prompt in its own temporary git worktree and merges it back when verified
            - Saves the run trace of every round into prompts/traces/, named after its prompt file
            - Optional --preserve-logs keeps temp prompt/log artifacts after successful rounds
            - Optional --no-ui keeps plain streaming console output for logging and debugging
            - Refuses to start on a nearly full disk and pauses the run when the free disk space becomes critical, unless --no-questions is used
            - Checks that the selected harness is installed and up to date before the first prompt unless --no-questions is used
            - Offers to add missing project-local ignore rules for the selected harness
            - In interactive mode, checks local and global Promptbook CLI installations and offers to update them
            - Supports GPG signing of commits
            - Optional pre-coding test run that can stop or repair pre-existing failures
            - Optional post-prompt verification with test-feedback retries
            - Progress tracking and interactive P/S/X terminal controls
            - Dry-run mode to preview prompts
        `,
        ),
    );

    command.option('--dry-run', 'Print unwritten prompts without executing', false);
    addPromptRunnerSelectionOptions(command);
    addQuestionsOption(command);
    command.option(
        '--agent <agent-book-path>',
        'Path to a .book file whose compiled system message is prepended to each coding prompt',
    );
    command.option(
        '--context <context-or-file>',
        'Append extra instructions either inline or from a file path relative to the current project',
    );
    command.option(
        '--test <test-command...>',
        'Run a verification command after each prompt; quote it when the command itself contains top-level flags',
    );
    command.addOption(
        new Option(
            '--test-before <mode>',
            `Run tests before coding: ${TEST_BEFORE_MODE_VALUES.join(
                ', ',
            )} (defaults to no; uses npm test when --test is omitted)`,
        )
            .choices([...TEST_BEFORE_MODE_VALUES])
            .default('no'),
    );
    command.option(
        '--preserve-logs',
        'Keep generated temp prompt/log artifacts after successful rounds for debugging and analytics',
        false,
    );
    addPromptRunnerExecutionOptions(command);
    command.option(
        '--isolate',
        spaceTrim(`
            Implement each prompt in its own temporary git worktree with its own isolated environment.
            A verified task is merged back into the branch the coder runs on and the worktree is deleted.
            A task that cannot be merged is marked as failed and its worktree is kept for a manual merge.
        `),
        false,
    );
    addPromptPriorityOptions(command);
    command.option(
        '--limit <run-count>',
        'Stop after processing this many prompt runs',
        createPositiveIntegerOptionParser('--limit'),
    );
    command.option(
        '--wait-after-prompt <duration>',
        spaceTrim(`
            Wait this long after each prompt has been implemented, verified and committed before starting the next prompt.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no wait).
        `),
    );
    command.option(
        '--wait-between-prompts <duration>',
        spaceTrim(`
            Pace prompts so that each next prompt starts at least this long after the previous one began.
            If the previous prompt already took longer than this, the next prompt starts immediately.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no pacing).
        `),
    );
    command.option(
        '--wait-after-error <duration>',
        spaceTrim(`
            Wait this long before retrying a prompt after an error occurs (up to 3 retries before giving up).
            Accepts durations like 1h, 30m, 5s. Defaults to 10m.
        `),
    );
    // Note: --no-auto disables the default auto behaviour and waits for user confirmation before each prompt
    command.option(
        '--no-auto',
        'Wait for user confirmation before each prompt instead of running automatically through the queue',
    );
    command.option(
        '--auto-migrate',
        'Run testing-server database migrations automatically after each successfully processed prompt',
    );
    command.option(
        '--allow-destructive-auto-migrate',
        'Allow auto-migrate even when heuristic SQL safety check flags destructive pending migrations',
    );

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                dryRun,
                agent,
                context,
                test,
                testBefore,
                preserveLogs,
                isolate: isIsolated,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
                limit,
                waitAfterPrompt: waitAfterPromptValue,
                waitBetweenPrompts: waitBetweenPromptsValue,
                waitAfterError: waitAfterErrorValue,
                auto,
                autoMigrate,
                allowDestructiveAutoMigrate,
            } = cliOptions as {
                readonly dryRun: boolean;
                readonly agent?: string;
                readonly context?: string;
                readonly test?: string | string[];
                readonly testBefore: TestBeforeMode;
                readonly preserveLogs: boolean;
                readonly isolate: boolean;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
                readonly limit?: number;
                readonly waitAfterPrompt?: string;
                readonly waitBetweenPrompts?: string;
                readonly waitAfterError?: string;
                readonly auto: boolean;
                readonly autoMigrate: boolean;
                readonly allowDestructiveAutoMigrate: boolean;
            } & PromptRunnerCliOptions;

            const configuredTestCommand = normalizeCommandOptionValue(test);
            const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
            const runnerOptions = normalizePromptRunnerCliOptions(cliOptions as PromptRunnerCliOptions, {
                isAgentRequired: !dryRun,
            });
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);

            // [1] Parse the wait options and --no-auto:
            //   default: run automatically through the queue (no waiting between prompts)
            //   --no-auto: wait for user confirmation before each prompt (interactive mode)
            //   --wait-after-prompt: pause after a successful round before starting the next prompt
            //   --wait-between-prompts: pace from start of one prompt to start of next
            //   --wait-after-error: wait before retrying after an error (default 10m)
            const waitForUser = !auto;

            assertUserConfirmationIsAllowed({ ...questionsOptions, isWaitingForUser: waitForUser });

            // Note: Checked before anything is installed or written, because installing a harness, updating the
            //       Promptbook CLI and coding itself all need the disk space which is missing
            await $assertSufficientFreeDiskSpace(process.cwd());

            if (await $ensurePromptbookCliInstallations(questionsOptions)) {
                return process.exit(0);
            }

            await $ensureHarnessInstallations([runnerOptions.agentName], questionsOptions);
            await $ensureCoderHarnessGitignoreRules(process.cwd(), runnerOptions.agentName, questionsOptions);

            const waitAfterPrompt = parseOptionalWaitDuration(waitAfterPromptValue, 0);
            const waitBetweenPrompts = parseOptionalWaitDuration(waitBetweenPromptsValue, 0);
            const waitAfterError = parseOptionalWaitDuration(waitAfterErrorValue, DEFAULT_WAIT_AFTER_ERROR_MS);

            // Convert commander options to RunOptions format
            const runOptions = {
                dryRun,
                waitForUser,
                waitAfterPrompt,
                waitBetweenPrompts,
                waitAfterError,
                noCommit: runnerOptions.noCommit,
                gitChanges: runnerOptions.gitChanges,
                agentName: runnerOptions.agentName,
                model: runnerOptions.model,
                agent,
                context,
                testCommand,
                testBefore,
                preserveLogs,
                isIsolated,
                noUi: runnerOptions.noUi,
                thinkingLevel: runnerOptions.thinkingLevel,
                priority: priority ?? 0,
                minimumPriority,
                maximumPriority,
                limit,
                normalizeLineEndings: runnerOptions.normalizeLineEndings,
                allowCredits: runnerOptions.allowCredits,
                autoMigrate,
                allowDestructiveAutoMigrate,
                autoPush: runnerOptions.autoPush,
                autoPull: runnerOptions.autoPull,
                isAskingQuestionsEnabled: questionsOptions.isAskingQuestionsEnabled,
            };

            // Note: Import the function dynamically to avoid loading heavy dependencies until needed
            const { runCodexPrompts } = await import('../../../../scripts/run-codex-prompts/main/runCodexPrompts');

            try {
                // Override process.argv to pass options to the legacy parseRunOptions if needed
                await runCodexPrompts(runOptions);
            } catch (error) {
                assertsError(error);
                printCoderRunFailure(error);
                return process.exit(1);
            }

            return process.exit(0);
        }),
    );
}

/**
 * Joins one Commander option that may be parsed either as a single string or a variadic token array.
 *
 * @private internal utility of `coder run` command
 */
function normalizeCommandOptionValue(value: string | string[] | undefined): string | undefined {
    if (value === undefined) {
        return undefined;
    }

    const parts = Array.isArray(value) ? value : [value];
    const normalizedValue = parts
        .map((part) => part.trim())
        .filter(Boolean)
        .join(' ')
        .trim();
    return normalizedValue === '' ? undefined : normalizedValue;
}

// Note: [🟡] Code for CLI command [run](src/cli/cli-commands/coder/run.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
import { Command } from 'commander';
import { runCodexPrompts } from '../../../../scripts/run-codex-prompts/main/runCodexPrompts';
import { LimitReachedError } from '../../../errors/LimitReachedError';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import { $ensurePromptbookCliInstallations } from '../common/promptbook-cli/$ensurePromptbookCliInstallations';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { $initializeCoderRunCommand } from './run';

jest.mock('../../../../scripts/run-codex-prompts/main/runCodexPrompts', () => ({
    runCodexPrompts: jest.fn(),
}));

jest.mock('../common/disk-space/$assertSufficientFreeDiskSpace', () => ({
    $assertSufficientFreeDiskSpace: jest.fn(),
}));

jest.mock('../common/promptbook-cli/$ensurePromptbookCliInstallations', () => ({
    $ensurePromptbookCliInstallations: jest.fn(),
}));

jest.mock('../common/harness/$ensureHarnessInstallations', () => ({
    $ensureHarnessInstallations: jest.fn(),
}));

jest.mock('./$ensureCoderHarnessGitignoreRules', () => ({
    $ensureCoderHarnessGitignoreRules: jest.fn(),
}));

/**
 * Typed Jest mock for the coding prompt runner entrypoint.
 */
function getRunCodexPromptsMock(): jest.MockedFunction<typeof runCodexPrompts> {
    return runCodexPrompts as jest.MockedFunction<typeof runCodexPrompts>;
}

/**
 * Typed Jest mock for the interactive Promptbook CLI update check.
 */
function getEnsurePromptbookCliInstallationsMock(): jest.MockedFunction<typeof $ensurePromptbookCliInstallations> {
    return $ensurePromptbookCliInstallations as jest.MockedFunction<typeof $ensurePromptbookCliInstallations>;
}

/**
 * Typed Jest mock for the selected-harness local ignore-rule check.
 */
function getEnsureCoderHarnessGitignoreRulesMock(): jest.MockedFunction<typeof $ensureCoderHarnessGitignoreRules> {
    return $ensureCoderHarnessGitignoreRules as jest.MockedFunction<typeof $ensureCoderHarnessGitignoreRules>;
}

/**
 * Typed Jest mock for the free disk space preflight check.
 */
function getAssertSufficientFreeDiskSpaceMock(): jest.MockedFunction<typeof $assertSufficientFreeDiskSpace> {
    return $assertSufficientFreeDiskSpace as jest.MockedFunction<typeof $assertSufficientFreeDiskSpace>;
}

/**
 * Creates a Commander program with the `coder run` subcommand registered.
 */
function createProgramWithRunCommand(): Command {
    const program = new Command();
    $initializeCoderRunCommand(program);
    return program;
}

describe('$initializeCoderRunCommand', () => {
    let processExitSpy: jest.SpyInstance<never, [code?: string | number | null | undefined]>;
    let consoleErrorSpy: jest.SpyInstance<void, [message?: unknown, ...optionalParams: unknown[]]>;

    beforeEach(() => {
        getRunCodexPromptsMock().mockResolvedValue(undefined);
        getEnsurePromptbookCliInstallationsMock().mockResolvedValue(false);
        getEnsureCoderHarnessGitignoreRulesMock().mockResolvedValue(undefined);
        getAssertSufficientFreeDiskSpaceMock().mockResolvedValue(undefined);
        processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
        consoleErrorSpy = jest.spyOn(console, 'error').mockImplementation(() => undefined);
    });

    afterEach(() => {
        processExitSpy.mockRestore();
        consoleErrorSpy.mockRestore();
        jest.clearAllMocks();
    });

    it('passes waitForUser as false when --no-auto is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                waitForUser: false,
            }),
        );
    });

    it('passes waitForUser as true when --no-auto is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-auto'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                waitForUser: true,
            }),
        );
    });

    it('checks Promptbook CLI installations before a default coder run', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect($ensurePromptbookCliInstallations).toHaveBeenCalledTimes(1);
    });

    it('also checks Promptbook CLI installations when per-prompt confirmation is enabled', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-auto'], { from: 'node' });

        expect($ensurePromptbookCliInstallations).toHaveBeenCalledTimes(1);
    });

    it('checks local ignore rules for the selected harness before a run', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--harness', 'qwen-code'], { from: 'node' });

        expect($ensureHarnessInstallations).toHaveBeenCalledWith(['qwen-code'], { isAskingQuestionsEnabled: true });
        expect($ensureCoderHarnessGitignoreRules).toHaveBeenCalledWith(process.cwd(), 'qwen-code', {
            isAskingQuestionsEnabled: true,
        });
    });

    it('asks nothing before a run started with --no-questions', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--harness', 'qwen-code', '--no-questions'], {
            from: 'node',
        });

        expect($ensurePromptbookCliInstallations).toHaveBeenCalledWith({ isAskingQuestionsEnabled: false });
        expect($ensureHarnessInstallations).toHaveBeenCalledWith(['qwen-code'], { isAskingQuestionsEnabled: false });
        expect($ensureCoderHarnessGitignoreRules).toHaveBeenCalledWith(process.cwd(), 'qwen-code', {
            isAskingQuestionsEnabled: false,
        });
        expect(getRunCodexPromptsMock()).toHaveBeenCalledTimes(1);
    });

    it('checks the free disk space before anything is installed or written', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect($assertSufficientFreeDiskSpace).toHaveBeenCalledWith(process.cwd());
    });

    it('never starts a run on a nearly full disk', async () => {
        getAssertSufficientFreeDiskSpaceMock().mockRejectedValue(
            new LimitReachedError('There is not enough free disk space to run `ptbk coder`.'),
        );
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect($ensurePromptbookCliInstallations).not.toHaveBeenCalled();
        expect(getRunCodexPromptsMock()).not.toHaveBeenCalled();
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('passes the interactive questions setting to the run so it knows whether it may pause', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-questions'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                isAskingQuestionsEnabled: false,
            }),
        );
    });

    it('refuses to combine --no-questions with the per-prompt confirmation of --no-auto', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-auto', '--no-questions'], { from: 'node' });

        expect(getRunCodexPromptsMock()).not.toHaveBeenCalled();
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('stops the current run after a Promptbook CLI installation was updated', async () => {
        getEnsurePromptbookCliInstallationsMock().mockResolvedValue(true);
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).not.toHaveBeenCalled();
        expect(processExitSpy).toHaveBeenCalledWith(0);
    });

    it('defaults noCommit to false when --no-commit is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                noCommit: false,
            }),
        );
    });

    it('passes noCommit as true when --no-commit is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-commit'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                noCommit: true,
            }),
        );
    });

    it('defaults autoPush to false when --auto-push is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                autoPush: false,
            }),
        );
    });

    it('passes autoPush as true when --auto-push is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--auto-push'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                autoPush: true,
            }),
        );
    });

    it('defaults autoPull to false when --auto-pull is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                autoPull: false,
            }),
        );
    });

    it('passes autoPull as true when --auto-pull is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--auto-pull'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                autoPull: true,
            }),
        );
    });

    it('passes thinkingLevel through when provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--thinking-level', 'max'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                thinkingLevel: 'max',
            }),
        );
    });

    it('passes the verification command through when provided as unquoted tokens', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--test', 'npm', 'run', 'test'], {
            from: 'node',
        });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                testCommand: 'npm run test',
            }),
        );
    });

    it('defaults pre-coding verification to no', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                testBefore: 'no',
                testCommand: undefined,
            }),
        );
    });

    it('passes pre-coding verification mode and uses npm test when no command is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--test-before', 'yes-and-fix'], {
            from: 'node',
        });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                testBefore: 'yes-and-fix',
                testCommand: 'npm test',
            }),
        );
    });

    it('keeps an explicit verification command when pre-coding verification is enabled', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(
            [
                'node',
                'test',
                'run',
                '--dry-run',
                '--test',
                'npm',
                'run',
                'test-for-ptbk-coder',
                '--test-before',
                'yes-and-fail',
            ],
            { from: 'node' },
        );

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                testBefore: 'yes-and-fail',
                testCommand: 'npm run test-for-ptbk-coder',
            }),
        );
    });

    it('defaults preserveLogs to false when --preserve-logs is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                preserveLogs: false,
            }),
        );
    });

    it('passes preserveLogs as true when --preserve-logs is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--preserve-logs'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                preserveLogs: true,
            }),
        );
    });

    it('defaults noUi to false when --no-ui is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                noUi: false,
            }),
        );
    });

    it('passes noUi as true when --no-ui is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--no-ui'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                noUi: true,
            }),
        );
    });

    it('defaults isIsolated to false when --isolate is omitted', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                isIsolated: false,
            }),
        );
    });

    it('passes isIsolated as true when --isolate is provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--isolate'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                isIsolated: true,
            }),
        );
    });

    it('passes run limit through when provided', async () => {
        const program = createProgramWithRunCommand();

        await program.parseAsync(['node', 'test', 'run', '--dry-run', '--limit', '2'], { from: 'node' });

        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                dryRun: true,
                limit: 2,
            }),
        );
    });
});

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/cli/cli-commands/coder/run.ts | Select-Object -First 360' in C:\Users\me\work\ai\promptbook
 succeeded in 384ms:
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../../errors/assertsError';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { createPositiveIntegerOptionParser } from '../common/createPositiveIntegerOptionParser';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import {
    addQuestionsOption,
    assertUserConfirmationIsAllowed,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import { $ensurePromptbookCliInstallations } from '../common/promptbook-cli/$ensurePromptbookCliInstallations';
import type { PromptRunnerCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerExecutionOptions,
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import {
    DEFAULT_CODER_TEST_COMMAND,
    TEST_BEFORE_MODE_VALUES,
    type TestBeforeMode,
} from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { printCoderRunFailure } from './printCoderRunFailure';

/**
 * Initializes `coder run` command for Promptbook CLI utilities
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderRunCommand(program: Program): $side_effect {
    const command = program.command('run');
    command.description(
        spaceTrim(
            (block) => `
            Execute coding prompts through selected AI agent

            ${block(PROMPT_RUNNER_DESCRIPTION)}

            ${block(QUESTIONS_DESCRIPTION)}

            Features:
            - Automatically stages and commits changes with agent identity unless --no-commit is used
            - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
            - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
            - Optional --git-changes continue resumes the single prompt an interrupted coder left in the [^] status
            - Optional post-commit git push with explicit --auto-push opt-in
            - Optional pre-prompt git pull with explicit --auto-pull opt-in
            - Optional --isolate runs every prompt in its own temporary git worktree and merges it back when verified
            - Saves the run trace of every round into prompts/traces/, named after its prompt file
            - Optional --preserve-logs keeps temp prompt/log artifacts after successful rounds
            - Optional --no-ui keeps plain streaming console output for logging and debugging
            - Refuses to start on a nearly full disk and pauses the run when the free disk space becomes critical, unless --no-questions is used
            - Checks that the selected harness is installed and up to date before the first prompt unless --no-questions is used
            - Offers to add missing project-local ignore rules for the selected harness
            - In interactive mode, checks local and global Promptbook CLI installations and offers to update them
            - Supports GPG signing of commits
            - Optional pre-coding test run that can stop or repair pre-existing failures
            - Optional post-prompt verification with test-feedback retries
            - Progress tracking and interactive P/S/X terminal controls
            - Dry-run mode to preview prompts
        `,
        ),
    );

    command.option('--dry-run', 'Print unwritten prompts without executing', false);
    addPromptRunnerSelectionOptions(command);
    addQuestionsOption(command);
    command.option(
        '--agent <agent-book-path>',
        'Path to a .book file whose compiled system message is prepended to each coding prompt',
    );
    command.option(
        '--context <context-or-file>',
        'Append extra instructions either inline or from a file path relative to the current project',
    );
    command.option(
        '--test <test-command...>',
        'Run a verification command after each prompt; quote it when the command itself contains top-level flags',
    );
    command.addOption(
        new Option(
            '--test-before <mode>',
            `Run tests before coding: ${TEST_BEFORE_MODE_VALUES.join(
                ', ',
            )} (defaults to no; uses npm test when --test is omitted)`,
        )
            .choices([...TEST_BEFORE_MODE_VALUES])
            .default('no'),
    );
    command.option(
        '--preserve-logs',
        'Keep generated temp prompt/log artifacts after successful rounds for debugging and analytics',
        false,
    );
    addPromptRunnerExecutionOptions(command);
    command.option(
        '--isolate',
        spaceTrim(`
            Implement each prompt in its own temporary git worktree with its own isolated environment.
            A verified task is merged back into the branch the coder runs on and the worktree is deleted.
            A task that cannot be merged is marked as failed and its worktree is kept for a manual merge.
        `),
        false,
    );
    addPromptPriorityOptions(command);
    command.option(
        '--limit <run-count>',
        'Stop after processing this many prompt runs',
        createPositiveIntegerOptionParser('--limit'),
    );
    command.option(
        '--wait-after-prompt <duration>',
        spaceTrim(`
            Wait this long after each prompt has been implemented, verified and committed before starting the next prompt.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no wait).
        `),
    );
    command.option(
        '--wait-between-prompts <duration>',
        spaceTrim(`
            Pace prompts so that each next prompt starts at least this long after the previous one began.
            If the previous prompt already took longer than this, the next prompt starts immediately.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no pacing).
        `),
    );
    command.option(
        '--wait-after-error <duration>',
        spaceTrim(`
            Wait this long before retrying a prompt after an error occurs (up to 3 retries before giving up).
            Accepts durations like 1h, 30m, 5s. Defaults to 10m.
        `),
    );
    // Note: --no-auto disables the default auto behaviour and waits for user confirmation before each prompt
    command.option(
        '--no-auto',
        'Wait for user confirmation before each prompt instead of running automatically through the queue',
    );
    command.option(
        '--auto-migrate',
        'Run testing-server database migrations automatically after each successfully processed prompt',
    );
    command.option(
        '--allow-destructive-auto-migrate',
        'Allow auto-migrate even when heuristic SQL safety check flags destructive pending migrations',
    );

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                dryRun,
                agent,
                context,
                test,
                testBefore,
                preserveLogs,
                isolate: isIsolated,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
                limit,
                waitAfterPrompt: waitAfterPromptValue,
                waitBetweenPrompts: waitBetweenPromptsValue,
                waitAfterError: waitAfterErrorValue,
                auto,
                autoMigrate,
                allowDestructiveAutoMigrate,
            } = cliOptions as {
                readonly dryRun: boolean;
                readonly agent?: string;
                readonly context?: string;
                readonly test?: string | string[];
                readonly testBefore: TestBeforeMode;
                readonly preserveLogs: boolean;
                readonly isolate: boolean;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
                readonly limit?: number;
                readonly waitAfterPrompt?: string;
                readonly waitBetweenPrompts?: string;
                readonly waitAfterError?: string;
                readonly auto: boolean;
                readonly autoMigrate: boolean;
                readonly allowDestructiveAutoMigrate: boolean;
            } & PromptRunnerCliOptions;

            const configuredTestCommand = normalizeCommandOptionValue(test);
            const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
            const runnerOptions = normalizePromptRunnerCliOptions(cliOptions as PromptRunnerCliOptions, {
                isAgentRequired: !dryRun,
            });
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);

            // [1] Parse the wait options and --no-auto:
            //   default: run automatically through the queue (no waiting between prompts)
            //   --no-auto: wait for user confirmation before each prompt (interactive mode)
            //   --wait-after-prompt: pause after a successful round before starting the next prompt
            //   --wait-between-prompts: pace from start of one prompt to start of next
            //   --wait-after-error: wait before retrying after an error (default 10m)
            const waitForUser = !auto;

            assertUserConfirmationIsAllowed({ ...questionsOptions, isWaitingForUser: waitForUser });

            // Note: Checked before anything is installed or written, because installing a harness, updating the
            //       Promptbook CLI and coding itself all need the disk space which is missing
            await $assertSufficientFreeDiskSpace(process.cwd());

            if (await $ensurePromptbookCliInstallations(questionsOptions)) {
                return process.exit(0);
            }

            await $ensureHarnessInstallations([runnerOptions.agentName], questionsOptions);
            await $ensureCoderHarnessGitignoreRules(process.cwd(), runnerOptions.agentName, questionsOptions);

            const waitAfterPrompt = parseOptionalWaitDuration(waitAfterPromptValue, 0);
            const waitBetweenPrompts = parseOptionalWaitDuration(waitBetweenPromptsValue, 0);
            const waitAfterError = parseOptionalWaitDuration(waitAfterErrorValue, DEFAULT_WAIT_AFTER_ERROR_MS);

            // Convert commander options to RunOptions format
            const runOptions = {
                dryRun,
                waitForUser,
                waitAfterPrompt,
                waitBetweenPrompts,
                waitAfterError,
                noCommit: runnerOptions.noCommit,
                gitChanges: runnerOptions.gitChanges,
                agentName: runnerOptions.agentName,
                model: runnerOptions.model,
                agent,
                context,
                testCommand,
                testBefore,
                preserveLogs,
                isIsolated,
                noUi: runnerOptions.noUi,
                thinkingLevel: runnerOptions.thinkingLevel,
                priority: priority ?? 0,
                minimumPriority,
                maximumPriority,
                limit,
                normalizeLineEndings: runnerOptions.normalizeLineEndings,
                allowCredits: runnerOptions.allowCredits,
                autoMigrate,
                allowDestructiveAutoMigrate,
                autoPush: runnerOptions.autoPush,
                autoPull: runnerOptions.autoPull,
                isAskingQuestionsEnabled: questionsOptions.isAskingQuestionsEnabled,
            };

            // Note: Import the function dynamically to avoid loading heavy dependencies until needed
            const { runCodexPrompts } = await import('../../../../scripts/run-codex-prompts/main/runCodexPrompts');

            try {
                // Override process.argv to pass options to the legacy parseRunOptions if needed
                await runCodexPrompts(runOptions);
            } catch (error) {
                assertsError(error);
                printCoderRunFailure(error);
                return process.exit(1);
            }

            return process.exit(0);
        }),
    );
}

/**
 * Joins one Commander option that may be parsed either as a single string or a variadic token array.
 *
 * @private internal utility of `coder run` command
 */
function normalizeCommandOptionValue(value: string | string[] | undefined): string | undefined {
    if (value === undefined) {
        return undefined;
    }

    const parts = Array.isArray(value) ? value : [value];
    const normalizedValue = parts
        .map((part) => part.trim())
        .filter(Boolean)
        .join(' ')
        .trim();
    return normalizedValue === '' ? undefined : normalizedValue;
}

// Note: [🟡] Code for CLI command [run](src/cli/cli-commands/coder/run.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!node_modules'"' \"agent\\??:|agent:|options\\.agent|runOptions\\.agent|agentBook|agent-book|DEFAULT.*AGENT|developer\\.book\" scripts/run-codex-prompts src/cli/cli-commands/coder src/cli/cli-commands/common | Select-Object -First 500" in C:\Users\me\work\ai\promptbook
 succeeded in 1271ms:
src/cli/cli-commands/coder\boilerplateTemplates.test.ts:9:    DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH,
src/cli/cli-commands/coder\boilerplateTemplates.test.ts:97:            join(process.cwd(), DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
src/cli/cli-commands/coder\boilerplateTemplates.test.ts:118:        expect(defaultCoderPackageJsonScripts['coder:run']).toContain('--agent agents/developer.book');
src/cli/cli-commands/coder\agentsFile.ts:11:const DEFAULT_CODER_AGENTS_FILE_LINES = [
src/cli/cli-commands/coder\agentsFile.ts:18:const DEFAULT_CODER_AGENTS_FILE_CONTENT = DEFAULT_CODER_AGENTS_FILE_LINES.join('\n');
src/cli/cli-commands/coder\agentsFile.ts:26:    return DEFAULT_CODER_AGENTS_FILE_CONTENT;
src/cli/cli-commands/coder\ensureCoderDeveloperAgentFile.ts:19:export const CODER_DEVELOPER_AGENT_FILE_PATH = 'agents/developer.book';
src/cli/cli-commands/coder\ensureCoderDeveloperAgentFile.ts:26:export const DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH = 'agents/default/developer.book';
src/cli/cli-commands/coder\ensureCoderDeveloperAgentFile.ts:48:        join(__dirname, '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
src/cli/cli-commands/coder\ensureCoderDeveloperAgentFile.ts:49:        join(__dirname, '..', '..', '..', '..', DEFAULT_CODER_DEVELOPER_AGENT_SOURCE_FILE_PATH),
src/cli/cli-commands/coder\server.ts:84:        '--agent <agent-book-path>',
src/cli/cli-commands/coder\server.ts:158:                readonly agent?: string;
src/cli/cli-commands/coder\run.ts:83:        '--agent <agent-book-path>',
src/cli/cli-commands/coder\run.ts:183:                readonly agent?: string;
scripts/run-codex-prompts\cli\RunOptions.ts:111:    agent?: string;
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:126:    const visibleOutputLines = buildVisibleOutputLines(options.agentOutputLines);
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:172:    if (options.agentVisualLines !== undefined && options.agentVisualLines.length > 0) {
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:173:        return options.agentVisualLines.map((agentVisualLine) => centerAnsiText(agentVisualLine, totalWidth));
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:176:    if (options.agentVisual !== undefined) {
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:177:        const agentVisualLines = options.agentVisual.renderFrame({
scripts/run-codex-prompts\README.md:83:ptbk coder run --harness openai-codex --model gpt-5.2-codex --agent agents/coding/developer.book --context AGENTS.md
scripts/run-codex-prompts\README.md:92:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --auto-push
scripts/run-codex-prompts\README.md:101:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --no-ui
scripts/run-codex-prompts\README.md:116:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --isolate
scripts/run-codex-prompts\main\runPromptRound.ts:675:    if (!runOptions.agentName) {
scripts/run-codex-prompts\main\runPromptRound.ts:682:            harness: runOptions.agentName,
scripts/run-codex-prompts\main\runCodexPrompts.ts:110:        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
scripts/run-codex-prompts\main\runCodexPrompts.ts:119:            harnessName: options.agentName,
scripts/run-codex-prompts\main\runCodexPrompts.ts:1089:    if (!runOptions.agentName) {
scripts/run-codex-prompts\main\runCodexPrompts.ts:1094:        harness: runOptions.agentName,
scripts/run-codex-prompts\main\resolvePromptRunner.ts:71:    const agentName = options.agentName;
scripts/run-codex-prompts\main\resolvePromptRunner.ts:241:    const runnerName = options.agentName ? getHarnessDefinition(options.agentName).label : 'unknown';
scripts/run-codex-prompts\main\resolvePromptRunner.ts:243:    if (options.agentName === 'github-copilot' || isModelRequiringHarnessName(options.agentName)) {
scripts/run-codex-prompts\main\resolvePromptRunner.ts:247:    if (options.agentName === 'cline') {
scripts/run-codex-prompts\main\resolvePromptRunner.ts:251:    if (options.agentName === 'opencode' || options.agentName === 'claude-code') {
scripts/run-codex-prompts\main\resolvePromptRunner.ts:279:        exitForMissingModel(options.agentName, options.availableModels, options.exampleUsages);
scripts/run-codex-prompts\prompts\printStats.test.ts:17:        expect(String(consoleInfoSpy.mock.calls[0]![0])).toContain('Done: 85 | For agent: 5 | To be written: 16');
scripts/run-codex-prompts\prompts\printStats.test.ts:24:            'Done: 85 | For agent: 3 | Priority <4: 2 | To be written: 10',
scripts/run-codex-prompts\prompts\printStats.test.ts:35:            'Done: 85 | For agent: 3 | Priority outside 1-5: 2 | To be written: 10',
scripts/run-codex-prompts\prompts\printStats.ts:16:        colors.cyan(`Done: ${stats.done} | For agent: ${stats.forAgent}${priorityStats} | To be written: ${stats.toBeWritten}`),

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/main/resolveCoderAgent.ts; Get-Content scripts/run-codex-prompts/cli/RunOptions.ts | Select-Object -First 180; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -First 180' in C:\Users\me\work\ai\promptbook
 succeeded in 611ms:
Get-Content: 
Line |
   2 |  Get-Content scripts/run-codex-prompts/main/resolveCoderAgent.ts; Get- …
     |  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\main\resolveCoderAgent.ts' because it does not exist.
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import type { ThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import type { PromptRunnerHarnessName } from '../../../src/cli/cli-commands/common/promptRunnerCliOptions';
import type { PriorityFilter } from '../prompts/priorityFilter';
import type { TestBeforeMode } from '../testing/TestBeforeMode';
import type { CoderRunUiState } from '../ui/CoderRunUiState';

/**
 * CLI options for running the prompt runner.
 */
export type RunOptions = {
    /**
     * When true, do not execute prompts and only print prompts that still need to be written.
     */
    dryRun: boolean;
    /**
     * Additional runner instructions provided either inline or as a file path.
     */
    context?: string;
    /**
     * Optional verification command executed after each prompt attempt.
     */
    testCommand?: string;
    /**
     * Controls whether the test command runs before the first coding prompt.
     */
    testBefore?: TestBeforeMode;
    /**
     * Keeps generated prompt/debug artifacts after a successful round instead of cleaning them up.
     */
    preserveLogs: boolean;
    /**
     * Disables the rich terminal UI so runner output streams directly to the console.
     */
    noUi: boolean;
    /**
     * Optional reasoning effort override for runners that support configurable thinking levels.
     */
    thinkingLevel?: ThinkingLevel;
    waitForUser: boolean;
    /**
     * Milliseconds to wait after one prompt has been implemented, verified and committed before the next prompt starts.
     * Zero means no waiting after a prompt.
     */
    waitAfterPrompt: number;
    /**
     * Milliseconds to wait between the start of one prompt and the start of the next prompt, regardless of how long the task itself takes.
     * If the task takes longer than this value, the next prompt starts immediately. Zero means no per-cycle pacing.
     */
    waitBetweenPrompts: number;
    /**
     * Milliseconds to wait before retrying a prompt after an error occurs.
     * Zero means retry immediately.
     */
    waitAfterError: number;
    /**
     * Leave successful round changes in the git working tree instead of creating an agent commit.
     */
    noCommit: boolean;
    /**
     * Decides what happens when the working tree has uncommitted changes before a prompt starts.
     *
     * `continue` resumes the single prompt which was left in the middle of its implementation
     * and only applies to that first round, every later round expects a clean working tree again.
     */
    gitChanges: GitChangesMode;
    /**
     * Automatically normalize CRLF line endings to LF in files changed during each coding round.
     */
    normalizeLineEndings: boolean;
    /**
     * Allows OpenAI Codex runner to spend credits when needed.
     */
    allowCredits: boolean;
    /**
     * When true, log harness-level messages in addition to the final answer.
     * When false or omitted, only the final answer is printed.
     */
    isVerbose?: boolean;
    /**
     * Requests machine-readable harness events for a consumer that renders safe live progress.
     */
    isMachineReadableProgressEnabled?: boolean;
    /**
     * Enables automatic migration of testing servers after each successfully completed prompt.
     */
    autoMigrate: boolean;
    /**
     * Allows auto-migration workflow to continue even when heuristic SQL safety checks detect destructive statements.
     */
    allowDestructiveAutoMigrate: boolean;
    /**
     * When true, push each successful coding-agent commit to the configured remote.
     */
    autoPush: boolean;
    /**
     * When true, pull the latest repository changes before processing prompts.
     */
    autoPull: boolean;
    /**
     * When true, implement every prompt in its own temporary git worktree and merge the verified result
     * back into the branch the coder runs on.
     */
    isIsolated?: boolean;
    agentName?: PromptRunnerHarnessName;
    model?: string;
    /**
     * Optional path to an agent `.book` file whose compiled system message is prepended to each codex prompt.
     * Supports the same path resolution as `--context`: relative to the current working directory.
     */
    agent?: string;
    /**
     * Legacy alias for `minimumPriority`.
     */
    priority: number;
    /**
     * Optional minimum prompt priority required for processing.
     */
    minimumPriority?: number;
    /**
     * Optional maximum prompt priority allowed for processing.
     */
    maximumPriority?: number;
    /**
     * Normalized prompt priority filter.
     */
    priorityFilter?: PriorityFilter;
    /**
     * Optional maximum number of successful prompt runs to process before stopping.
     */
    limit?: number;
    /**
     * Optional HTTP server URL shown in the rich terminal UI for `ptbk coder server`.
     */
    serverUrl?: string;
    /**
     * Optional shared run-state object used by `ptbk coder server` to expose terminal progress over HTTP.
     */
    uiState?: CoderRunUiState;
    /**
     * When true, the run loop continues watching for new prompts instead of exiting when none are available.
     * Used by `ptbk coder server` to keep the process alive as a server.
     */
    keepAlive?: boolean;
    /**
     * Whether the run may stop and wait for an answer it does not strictly need, disabled by `--no-questions`.
     *
     * A run which may not ask keeps going through a critically full disk instead of pausing at the next
     * checkpoint, because there is nobody who could free the disk space and resume it.
     *
     * When omitted, the run behaves as if the questions were enabled.
     */
    isAskingQuestionsEnabled?: boolean;
};
import colors from 'colors';
import moment from 'moment';
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import { DatabaseError } from '../../../src/errors/DatabaseError';
import { NotAllowed } from '../../../src/errors/NotAllowed';
import { just } from '../../../src/utils/organization/just';
import type { RunOptions } from '../cli/RunOptions';
import { parseRunOptions } from '../cli/parseRunOptions';
import type { CoderRunPauseCheckpointOptions, WaitForCoderRunPauseCheckpoint } from '../common/CoderRunPauseCheckpoint';
import { CliProgressDisplay } from '../common/cliProgressDisplay';
import { loadCachedAveragePromptDurationMs } from '../common/coderRunEstimateCache';
import { createFreeDiskSpaceGuard, type FreeDiskSpaceGuard } from '../common/createFreeDiskSpaceGuard';
import { resolveCoderAgent } from '../common/resolveCoderAgent';
import { sleepWithCountdown } from '../common/sleepWithCountdown';
import { resolveCoderContext } from '../common/resolveCoderContext';
import { listenForCoderRunControls } from '../common/listenForCoderRunControls';
import {
    announcePauseTargetLabel,
    checkPause,
    getEndAfterCurrentPromptState,
    resetCoderRunControls,
    resetPauseTargetLabel,
} from '../common/waitForPause';
import { waitForSkippableWorldTimeDeadline } from '../common/waitForSkippableWorldTimeDeadline';
import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
import { commitChanges } from '../git/commitChanges';
import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
import { pullLatestChanges } from '../git/pullLatestChanges';
import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
import { buildPromptLabelForDisplay } from '../prompts/buildPromptLabelForDisplay';
import { buildPromptSummary } from '../prompts/buildPromptSummary';
import { findNextTodoPrompt } from '../prompts/findNextTodoPrompt';
import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';
import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { printPromptsToBeWritten } from '../prompts/printPromptsToBeWritten';
import { printStats } from '../prompts/printStats';
import { normalizePriorityFilter, type PriorityFilter } from '../prompts/priorityFilter';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { resolveInterruptedPrompt } from '../prompts/resolveInterruptedPrompt';
import { summarizePrompts } from '../prompts/summarizePrompts';
import type { PromptFile } from '../prompts/types/PromptFile';
import type { PromptSelection } from '../prompts/types/PromptSelection';
import type { PromptStats } from '../prompts/types/PromptStats';
import { waitForPromptStart } from '../prompts/waitForPromptStart';
import type { PromptRunner } from '../runners/types/PromptRunner';
import { buildCoderRunAgentVisual } from '../ui/buildCoderRunAgentVisual';
import { renderCoderRunUi, type CoderRunUiHandle } from '../ui/renderCoderRunUi';
import { refreshCoderRunUiSubscriptionUsage } from '../ui/refreshCoderRunUiSubscriptionUsage';
import { createTestBeforeRepairPrompt } from '../testing/createTestBeforeRepairPrompt';
import { DEFAULT_CODER_TEST_COMMAND, isTestBeforeMode, type TestBeforeMode } from '../testing/TestBeforeMode';
import { limitTestOutput } from '../testing/limitTestOutput';
import { runTestBefore } from '../testing/runTestBefore';
import { resolvePromptRunner } from './resolvePromptRunner';
import { runPromptRound } from './runPromptRound';

/**
 * Constant for prompts dir.
 */
const PROMPTS_DIR = join(process.cwd(), 'prompts');

/**
 * Commit message for files changed by a successful or failed pre-coding test in repair mode.
 */
const PRE_CODING_TEST_CHANGES_COMMIT_MESSAGE = 'test: Apply changes made by pre-coding tests';

/**
 * Prompt queue snapshot for one top-level loop iteration.
 */
type PromptQueueSnapshot = {
    promptFiles: PromptFile[];
    stats: PromptStats;
    nextPrompt?: PromptSelection;
};

/**
 * Main entry point for running prompts with the selected agent.
 *
 * @param providedOptions - Optional pre-parsed options. If not provided, will parse from process.argv
 *
 * @public exported from `@promptbook/cli`
 */
export async function runCodexPrompts(providedOptions?: RunOptions): Promise<void> {
    const options = normalizeRunOptions(providedOptions ?? parseRunOptions(process.argv.slice(2)));
    validateRunCodexPromptOptions(options);
    resetCoderRunControls();

    const runStartDate = moment();
    const { isRichUiEnabled, progressDisplay, uiHandle } = createRunDisplays(options, runStartDate);
    const waitForRequestedPause = createPauseWaiter({
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        // Note: Every pause checkpoint of the whole run goes through this one waiter, so watching the free disk
        //       space here covers each round, each verification and each runner without repeating the check
        guardFreeDiskSpace: createFreeDiskSpaceGuard({
            inspectedPath: process.cwd(),
            isAskingQuestionsEnabled: options.isAskingQuestionsEnabled ?? true,
        }),
    });

    startPauseListenerIfNeeded(isRichUiEnabled);

    try {
        const resolvedCoderContext = await resolveCoderContext(options.context, process.cwd());
        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
        const resolvedAgentSystemMessage = resolvedCoderAgent?.systemMessage;

        if (await runDryRunIfRequested(options)) {
            return;
        }

        const { runner, actualRunnerModel, runnerMetadata } = resolvePromptRunner(options);
        const promptRunnerIdentity: PromptRunnerIdentity = {
            harnessName: options.agentName,
            modelName: actualRunnerModel,
        };
        console.info(colors.green(`Running prompts with ${runner.name}`));

        initializeRunUi(uiHandle, runner.name, actualRunnerModel, options);
        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
        await refreshCoderRunUiSubscriptionUsage({
            runner,
            uiState: uiHandle?.state,
        });
        await seedCachedAveragePromptDuration({
            options,
            actualRunnerModel,
            progressDisplay,
            uiHandle,
        });

        let hasShownUpcomingTasks = false;
        let hasWaitedForStart = false;
        let previousRoundStartTime: number | undefined;
        let previousRoundEndTime: number | undefined;
        let completedRunCount = 0;
        let hasRunTestBefore = false;
        // Note: Only the very first round resumes the interrupted prompt, every later round starts from a clean tree
        let isContinuingInterruptedPrompt = options.gitChanges === 'continue';

        while (just(true)) {
            if (options.autoPull && !options.dryRun) {
                await waitForRequestedPause({
                    checkpointLabel: 'pulling the latest repository changes',
                    phase: 'loading',
                    statusMessage: 'Pulling latest changes...',
                });
            }
            await pullLatestChangesIfEnabled({
                options,
                isRichUiEnabled,
            });

            if (!hasRunTestBefore && options.testBefore !== 'no') {
                await waitForRequestedPause({
                    checkpointLabel: 'loading prompts before running initial tests',
                    phase: 'loading',
                    statusMessage: 'Loading prompts before running initial tests...',
                });
                await loadPromptQueueSnapshot({
                    options,
                    isRichUiEnabled,
                    progressDisplay,
                    uiHandle,
                    promptRunnerIdentity,
                    isContinuingInterruptedPrompt,
                });
            }

            if (!hasRunTestBefore) {
                hasWaitedForStart = await runTestBeforeIfNeeded({
                    options,
                    runner,
                    runnerMetadata,
                    resolvedCoderContext,

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.ts; Get-Content scripts/run-codex-prompts/common/resolveCoderContext.ts; Get-Content scripts/run-codex-prompts/cli/parseRunOptions.ts | Select-Object -First 340' in C:\Users\me\work\ai\promptbook
 succeeded in 503ms:
import { readFile } from 'fs/promises';
import { resolve } from 'path';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';

/**
 * Agent data resolved from the optional `--agent` book file.
 */
export type ResolvedCoderAgent = {
    /**
     * Raw source of the agent `.book` file.
     */
    readonly agentSource: string_book;

    /**
     * Compiled system message injected into each coding prompt.
     */
    readonly systemMessage: string;
};

/**
 * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
 *
 * Returns `undefined` when no agent path is provided.
 */
export async function resolveCoderAgent(
    agentPath: string | undefined,
    currentWorkingDirectory: string,
): Promise<ResolvedCoderAgent | undefined> {
    if (!agentPath) {
        return undefined;
    }

    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
        if (error.code === 'ENOENT' || error.code === 'EISDIR') {
            throw new NotFoundError(
                spaceTrim(`
                    Agent book \`${agentPath}\` was not found or is not a file.

                    Pass a path to a \`.book\` file in \`--agent\`.
                `),
            );
        }
        throw error;
    });

    return {
        agentSource: agentSource as string_book,
        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
    };
}
import { resolveInlineOrFileText } from './resolveInlineOrFileText';

/**
 * Resolves optional coding context provided inline or via a file path.
 */
export async function resolveCoderContext(
    contextReference: string | undefined,
    currentWorkingDirectory: string,
): Promise<string | undefined> {
    return resolveInlineOrFileText({
        textReference: contextReference,
        currentWorkingDirectory,
        contextLabel: 'Coding context',
        optionName: '--context',
    });
}
import colors from 'colors';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import {
    DEFAULT_GIT_CHANGES_MODE,
    GIT_CHANGES_MODE_VALUES,
    isGitChangesMode,
} from '../../../src/cli/cli-commands/coder/GitChangesMode';
import type { ThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import { THINKING_LEVEL_VALUES, parseThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import {
    PROMPT_RUNNER_HARNESS_NAMES,
    PROMPT_RUNNER_HARNESS_OPTION_HINT,
    type PromptRunnerHarnessName,
} from '../../../src/cli/cli-commands/common/promptRunnerCliOptions';
import { parseDuration } from '../common/parseDuration';
import { normalizePriorityFilter } from '../prompts/priorityFilter';
import { isTestBeforeMode, TEST_BEFORE_MODE_VALUES, type TestBeforeMode } from '../testing/TestBeforeMode';
import type { RunOptions } from './RunOptions';

/**
 * Default wait duration applied before retrying a failed prompt round.
 */
const DEFAULT_WAIT_AFTER_ERROR_MS = 10 * 60 * 1000;

/**
 * CLI usage text for this script.
 */
const USAGE =
    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';

/**
 * Top-level flags supported by this command.
 */
const KNOWN_OPTION_FLAGS = new Set([
    '--dry-run',
    '--harness',
    '--model',
    '--context',
    '--test',
    '--test-before',
    '--preserve-logs',
    '--isolate',
    '--no-ui',
    '--thinking-level',
    '--priority',
    '--min-priority',
    '--max-priority',
    '--limit',
    '--allow-credits',
    '--auto-migrate',
    '--allow-destructive-auto-migrate',
    '--wait-after-prompt',
    '--wait-between-prompts',
    '--wait-after-error',
    '--no-auto',
    '--no-commit',
    '--git-changes',
    '--no-normalize-line-endings',
    '--auto-push',
    '--auto-pull',
]);

/**
 * Parses CLI arguments into runner options.
 */
export function parseRunOptions(args: string[]): RunOptions {
    let agentName: PromptRunnerHarnessName | undefined = undefined;
    const dryRun = args.includes('--dry-run');

    const harnessValue = readOptionValue(args, '--harness');
    if (harnessValue) {
        const value = harnessValue;
        if (PROMPT_RUNNER_HARNESS_NAMES.includes(value as PromptRunnerHarnessName)) {
            agentName = value as PromptRunnerHarnessName;
        }
    }

    const model = readOptionValue(args, '--model');
    const context = readOptionValue(args, '--context');
    const hasTestCommandFlag = args.includes('--test');
    const testCommand = readVariadicOptionValue(args, '--test');
    const hasTestBeforeFlag = args.includes('--test-before');
    const testBefore = parseTestBeforeOption(readOptionValue(args, '--test-before'), hasTestBeforeFlag);
    const preserveLogs = args.includes('--preserve-logs');
    const isIsolated = args.includes('--isolate');
    const noUi = args.includes('--no-ui');
    const hasThinkingLevelFlag = args.includes('--thinking-level');
    const thinkingLevelValue = readOptionValue(args, '--thinking-level');
    const isPriorityFlagProvided = args.includes('--priority');
    const legacyMinimumPriority = parsePriorityBoundary(
        readOptionValue(args, '--priority'),
        isPriorityFlagProvided,
        '--priority',
    );
    const isMinimumPriorityFlagProvided = args.includes('--min-priority');
    const explicitMinimumPriority = parsePriorityBoundary(
        readOptionValue(args, '--min-priority'),
        isMinimumPriorityFlagProvided,
        '--min-priority',
    );
    const isMaximumPriorityFlagProvided = args.includes('--max-priority');
    const maximumPriority = parsePriorityBoundary(
        readOptionValue(args, '--max-priority'),
        isMaximumPriorityFlagProvided,
        '--max-priority',
    );
    const minimumPriority = resolveMinimumPriority(legacyMinimumPriority, explicitMinimumPriority);
    const priorityFilter = parsePriorityFilter({
        priority: legacyMinimumPriority,
        minimumPriority,
        maximumPriority,
    });
    const hasLimitFlag = args.includes('--limit');
    const limit = parseLimit(readOptionValue(args, '--limit'), hasLimitFlag);
    const noCommit = args.includes('--no-commit');
    const hasGitChangesFlag = args.includes('--git-changes');
    const gitChanges = parseGitChangesOption(readOptionValue(args, '--git-changes'), hasGitChangesFlag);
    const normalizeLineEndings = !args.includes('--no-normalize-line-endings');
    const allowCredits = args.includes('--allow-credits');
    const autoMigrate = args.includes('--auto-migrate');
    const allowDestructiveAutoMigrate = args.includes('--allow-destructive-auto-migrate');
    const autoPush = args.includes('--auto-push');
    const autoPull = args.includes('--auto-pull');
    // [1] Parse --wait <duration> and --no-auto:
    //   default: run automatically through the queue (no waiting)
    //   --no-auto: wait for user confirmation before each prompt (interactive mode)
    //   --wait 1h: wait 1h between prompt rounds to avoid rate limits
    const waitForUser = args.includes('--no-auto');
    const waitAfterPrompt = parseWaitOption(args, '--wait-after-prompt', 0);
    const waitBetweenPrompts = parseWaitOption(args, '--wait-between-prompts', 0);
    const waitAfterError = parseWaitOption(args, '--wait-after-error', DEFAULT_WAIT_AFTER_ERROR_MS);
    let thinkingLevel: ThinkingLevel | undefined;

    if (hasTestCommandFlag && testCommand === undefined) {
        exitWithUsageError(
            'Missing value for --test. Use a shell command such as `npm run test` and quote it when it contains top-level CLI flags.',
        );
    }

    if (hasThinkingLevelFlag && thinkingLevelValue === undefined) {
        exitWithUsageError(`Missing value for --thinking-level. Use one of: ${THINKING_LEVEL_VALUES.join(', ')}.`);
    }

    try {
        thinkingLevel = parseThinkingLevel(thinkingLevelValue);
    } catch (error) {
        exitWithUsageError(error instanceof Error ? error.message : String(error));
    }

    if (!agentName && !dryRun) {
        exitWithUsageError(`You must choose a harness using ${PROMPT_RUNNER_HARNESS_OPTION_HINT}`);
    }

    return {
        dryRun,
        waitForUser,
        waitAfterPrompt,
        waitBetweenPrompts,
        waitAfterError,
        noCommit,
        gitChanges,
        normalizeLineEndings,
        allowCredits,
        autoMigrate,
        allowDestructiveAutoMigrate,
        autoPush,
        autoPull,
        preserveLogs,
        isIsolated,
        noUi,
        agentName,
        model,
        context,
        testCommand,
        testBefore,
        thinkingLevel,
        priority: minimumPriority ?? 0,
        minimumPriority,
        maximumPriority,
        priorityFilter,
        limit,
    };
}

/**
 * Parses and validates the optional pre-coding verification mode.
 */
function parseTestBeforeOption(value: string | undefined, hasTestBeforeFlag: boolean): TestBeforeMode {
    if (value === undefined) {
        if (hasTestBeforeFlag) {
            exitWithUsageError(`Missing value for --test-before. Use one of: ${TEST_BEFORE_MODE_VALUES.join(', ')}.`);
        }

        return 'no';
    }

    if (!isTestBeforeMode(value)) {
        exitWithUsageError(
            `Invalid value for --test-before: "${value}". Use one of: ${TEST_BEFORE_MODE_VALUES.join(', ')}.`,
        );
    }

    return value;
}

/**
 * Parses and validates the requested behavior for a dirty working tree.
 */
function parseGitChangesOption(value: string | undefined, hasGitChangesFlag: boolean): GitChangesMode {
    if (value === undefined) {
        if (hasGitChangesFlag) {
            exitWithUsageError(`Missing value for --git-changes. Use one of: ${GIT_CHANGES_MODE_VALUES.join(', ')}.`);
        }

        return DEFAULT_GIT_CHANGES_MODE;
    }

    if (!isGitChangesMode(value)) {
        exitWithUsageError(
            `Invalid value for --git-changes: "${value}". Use one of: ${GIT_CHANGES_MODE_VALUES.join(', ')}.`,
        );
    }

    return value;
}

/**
 * Reads a duration-typed CLI flag, applying the provided default when the flag is absent.
 */
function parseWaitOption(args: string[], flag: string, defaultMs: number): number {
    if (!args.includes(flag)) {
        return defaultMs;
    }

    const value = readOptionValue(args, flag);
    if (value === undefined || value.startsWith('-')) {
        exitWithUsageError(`Missing value for ${flag}. Use a duration like 1h, 30m, 5s.`);
    }

    return parseDuration(value as string);
}

/**
 * Reads a value of a CLI option that follows a given flag.
 */
function readOptionValue(args: string[], flag: string): string | undefined {
    if (!args.includes(flag)) {
        return undefined;
    }
    const index = args.indexOf(flag);
    return args[index + 1];
}

/**
 * Reads a multi-token shell command value that follows a given flag.
 */
function readVariadicOptionValue(args: string[], flag: string): string | undefined {
    if (!args.includes(flag)) {
        return undefined;
    }

    const index = args.indexOf(flag);
    const valueParts: string[] = [];

    for (let i = index + 1; i < args.length; i++) {
        const valuePart = args[i];

        if (valuePart === undefined) {
            continue;
        }

        if (KNOWN_OPTION_FLAGS.has(valuePart)) {
            break;
        }

        valueParts.push(valuePart);
    }

    const normalizedValue = valueParts.join(' ').trim();
    return normalizedValue === '' ? undefined : normalizedValue;
}

/**
 * Parses and validates one priority boundary.
 */
function parsePriorityBoundary(
    priorityValue: string | undefined,
    isPriorityFlagProvided: boolean,
    optionName: string,
): number | undefined {
    if (priorityValue === undefined) {
        if (isPriorityFlagProvided) {
            exitWithUsageError(`Missing value for ${optionName}. Use a non-negative integer.`);
        }
        return undefined;
    }

    const priority = Number(priorityValue);
    if (!Number.isInteger(priority) || priority < 0) {
        exitWithUsageError(`Invalid value for ${optionName}: "${priorityValue}". Use a non-negative integer.`);
    }

    return priority;
}

/**
 * Resolves `--priority` as the legacy alias for `--min-priority`.
 */
function resolveMinimumPriority(
    legacyMinimumPriority: number | undefined,
    explicitMinimumPriority: number | undefined,
): number | undefined {
    if (
        legacyMinimumPriority !== undefined &&
        explicitMinimumPriority !== undefined &&
        legacyMinimumPriority !== explicitMinimumPriority
    ) {
        exitWithUsageError('Conflicting values for --priority and --min-priority. Use one minimum priority value.');
    }

    return explicitMinimumPriority ?? legacyMinimumPriority;
}

/**
 * Normalizes the priority range and preserves this legacy CLI parser's usage-error behavior.
 */
function parsePriorityFilter(options: {
    readonly priority?: number;
    readonly minimumPriority?: number;
    readonly maximumPriority?: number;
}): ReturnType<typeof normalizePriorityFilter> {
    try {
        return normalizePriorityFilter(options);
    } catch (error) {
        exitWithUsageError(error instanceof Error ? error.message : String(error));
    }
}

/**
 * Parses and validates the optional successful prompt-run limit.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!node_modules'"' \"resolveCoderAgent|ResolvedCoderAgent|--agent\" scripts/run-codex-prompts src/cli/cli-commands/coder apps/coder-landing/src apps/coder-landing/specs | Select-Object -First 500; rg --files scripts/run-codex-prompts | rg \"resolveCoderAgent|parseRunOptions.*test|RunOptions.*test\"" in C:\Users\me\work\ai\promptbook
 succeeded in 445ms:
apps/coder-landing/specs\content\commands.md:21:| `SERVER_COMMAND`                | `ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test`                                                     | [Quickstart step 5](../sections/quickstart.md)                              |
apps/coder-landing/specs\content\commands.md:22:| `LIVE_DEMO_RUN_COMMAND`         | `ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1` | Typed in the [live terminal](../components/live-terminal.md)                |
apps/coder-landing/specs\content\commands.md:23:| `AGENT_RUN_COMMAND`             | `ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md`                                                                                                 | [Agent personas](../sections/agent-book.md)                                 |
apps/coder-landing/specs\content\commands.md:38:-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
apps/coder-landing/specs\product.md:34:    - an optional agent persona from a `.book` file (`--agent`, see [`content/developer-agent.md`](./content/developer-agent.md)),
apps/coder-landing/src\data\commands.ts:84:    'ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test';
apps/coder-landing/src\data\commands.ts:90:    'ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1';
apps/coder-landing/src\data\commands.ts:96:    'ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md';
src/cli/cli-commands/coder\server.ts:84:        '--agent <agent-book-path>',
src/cli/cli-commands/coder\boilerplateTemplates.test.ts:118:        expect(defaultCoderPackageJsonScripts['coder:run']).toContain('--agent agents/developer.book');
src/cli/cli-commands/coder\boilerplateTemplates.test.ts:200:        const existingCoderRunScript = 'npx ptbk coder run --harness github-copilot --agent agents/my-own.book';
src/cli/cli-commands/coder\run.ts:83:        '--agent <agent-book-path>',
src/cli/cli-commands/coder\getDefaultCoderPackageJsonScripts.ts:56:            `--agent ${formatDisplayPath(CODER_DEVELOPER_AGENT_FILE_PATH)}`,
scripts/run-codex-prompts\common\resolveCoderAgent.ts:9: * Agent data resolved from the optional `--agent` book file.
scripts/run-codex-prompts\common\resolveCoderAgent.ts:11:export type ResolvedCoderAgent = {
scripts/run-codex-prompts\common\resolveCoderAgent.ts:28:export async function resolveCoderAgent(
scripts/run-codex-prompts\common\resolveCoderAgent.ts:31:): Promise<ResolvedCoderAgent | undefined> {
scripts/run-codex-prompts\common\resolveCoderAgent.ts:43:                    Pass a path to a \`.book\` file in \`--agent\`.
scripts/run-codex-prompts\ui\coderRunUiRefresh.ts:21: * Phases where an animated `--agent` visual should keep rendering like it does on the web.
scripts/run-codex-prompts\main\runCodexPrompts.ts:16:import { resolveCoderAgent } from '../common/resolveCoderAgent';
scripts/run-codex-prompts\main\runCodexPrompts.ts:110:        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
scripts/run-codex-prompts\main\runCodexPrompts.ts:756: * Prepares the `--agent` book avatar ASCII-art renderer and shows it above the dashboard boxes.
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:51:     * ANSI ASCII-art renderer of the `--agent` avatar visual shown above the dashboard boxes.
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:56:     * Optional static ANSI ASCII-art lines of the `--agent` avatar visual.
scripts/run-codex-prompts\ui\buildCoderRunUiFrame.ts:167: * Shows the ASCII-art `--agent` avatar visual as soon as one is available. Until the avatar is
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:35: * Runtime renderer for the `--agent` avatar visual shown in the coder-run terminal UI.
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:42: * Builds the ANSI ASCII-art visual of the `--agent` book shown above the coder-run dashboard.
scripts/run-codex-prompts\ui\buildCoderRunAgentVisual.ts:52: * @param agentSource Source of the `--agent` book file.
scripts/run-codex-prompts\README.md:83:ptbk coder run --harness openai-codex --model gpt-5.2-codex --agent agents/coding/developer.book --context AGENTS.md
scripts/run-codex-prompts\README.md:92:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --auto-push
scripts/run-codex-prompts\README.md:101:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --no-ui
scripts/run-codex-prompts\README.md:116:ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --isolate
apps/coder-landing/src\components\sections\AgentsSection.tsx:6: * Renders the agents section - the `--agent agents/developer.book` option together with
apps/coder-landing/src\components\sections\AgentsSection.tsx:34:                        <code className="text-promptbook-blue">--agent</code>:
apps/coder-landing/specs\sections\agent-book.md:3:Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
apps/coder-landing/specs\sections\agent-book.md:16:1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
scripts/run-codex-prompts\cli\parseRunOptions.test.ts
scripts/run-codex-prompts\common\resolveCoderAgent.ts

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -Path agents -Recurse -File -Filter *.book | Select-Object -ExpandProperty FullName; Get-Content agents/coding/developer.book | Select-Object -First 20; Get-Content agents/developer.book | Select-Object -First 20; Get-Content src/cli/cli-commands/coder/server.ts | Select-Object -First 260' in C:\Users\me\work\ai\promptbook
 succeeded in 568ms:
C:\Users\me\work\ai\promptbook\agents\coding\developer.book
C:\Users\me\work\ai\promptbook\agents\coding\unslopper.book
C:\Users\me\work\ai\promptbook\agents\default\aktualizator-prezentaci.book
C:\Users\me\work\ai\promptbook\agents\default\chat-na-webu.book
C:\Users\me\work\ai\promptbook\agents\default\chat-nad-firemnimi-dokumenty.book
C:\Users\me\work\ai\promptbook\agents\default\chat-v-cestine.book
C:\Users\me\work\ai\promptbook\agents\default\copywriter.book
C:\Users\me\work\ai\promptbook\agents\default\developer.book
C:\Users\me\work\ai\promptbook\agents\default\firemni-pravnik.book
C:\Users\me\work\ai\promptbook\agents\default\generic-chatter.book
C:\Users\me\work\ai\promptbook\agents\default\product-manager.book
C:\Users\me\work\ai\promptbook\agents\default\social-media-manager.book
C:\Users\me\work\ai\promptbook\agents\default\spravce-kalendare.book
C:\Users\me\work\ai\promptbook\agents\default\webmaster.book
C:\Users\me\work\ai\promptbook\agents\default\.core\adam.book
C:\Users\me\work\ai\promptbook\agents\default\.core\creator.book
C:\Users\me\work\ai\promptbook\agents\default\.core\expert.book
C:\Users\me\work\ai\promptbook\agents\default\.core\teacher.book
Promptbook Developer

META VISUAL ascii-octopus

RULE
Keep in mind the DRY _(don't repeat yourself)_ principle.

RULE
Keep in mind the SOLID principles.

RULE
Do a proper analysis of the current functionality before you start implementing.

RULE
Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.

RULE
When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
Format errors as markdown, for example `variables` should be in backticks and important notes can be in bold.

Get-Content: 
Line |
   2 |  …  | Select-Object -First 20; Get-Content agents/developer.book | Selec …
     |                                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\ai\promptbook\agents\developer.book' because it does not exist.
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import { NETWORK_LIMITS } from '../../../constants';
import { assertsError } from '../../../errors/assertsError';
import { NotAllowed } from '../../../errors/NotAllowed';
import type { number_port } from '../../../types/number_positive';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import {
    addQuestionsOption,
    assertUserConfirmationIsAllowed,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import type { PromptRunnerCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerExecutionOptions,
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { printCoderRunFailure } from './printCoderRunFailure';

/**
 * Default port used by `ptbk coder server`.
 *
 * @private internal constant of `ptbk coder server`
 */
const DEFAULT_CODER_SERVER_PORT = '4441';

/**
 * Initializes `coder server` command for Promptbook CLI utilities.
 *
 * Runs the same prompt processing logic as `ptbk coder run` but keeps the process alive
 * and serves a kanban web UI on the configured port.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderServerCommand(program: Program): $side_effect {
    const command = program.command('server');
    command.description(
        spaceTrim(
            (block) => `
                Start a coder server that watches for prompts and serves a kanban web UI

                ${block(PROMPT_RUNNER_DESCRIPTION)}

                ${block(QUESTIONS_DESCRIPTION)}

                Features:
                - Runs the same prompt processing as \`ptbk coder run\`
                - Refuses to start on a nearly full disk and pauses the run when the free disk space becomes critical, unless --no-questions is used
                - Checks that the selected harness is installed and up to date on startup unless --no-questions is used
                - Offers to add missing project-local ignore rules for the selected harness
                - Does not exit when all prompts are done; polls for new prompt files instead
                - Serves a kanban board at http://localhost:<port> for visual progress tracking
                - Allows editing prompt files directly from the browser (Trello-style)
                - Play / pause button in the browser stays in sync with the CLI pause state
                - Terminal controls match \`ptbk coder run\`: P pause/resume, S skip waiting, X end after current prompt
            `,
        ),
    );

    command.addOption(
        new Option('--port <port>', 'Port to start the coder server on')
            .env('PTBK_CODER_SERVER_PORT')
            .default(DEFAULT_CODER_SERVER_PORT),
    );
    command.option('--dry-run', 'Print unwritten prompts without executing', false);
    addPromptRunnerSelectionOptions(command);
    addQuestionsOption(command);
    command.option(
        '--agent <agent-book-path>',
        'Path to a .book file whose compiled system message is prepended to each coding prompt',
    );
    command.option(
        '--context <context-or-file>',
        'Append extra instructions either inline or from a file path relative to the current project',
    );
    command.option(
        '--test <test-command...>',
        'Run a verification command after each prompt; quote it when the command itself contains top-level flags',
    );
    command.option(
        '--preserve-logs',
        'Keep generated temp prompt/log artifacts after successful rounds for debugging and analytics',
        false,
    );
    addPromptRunnerExecutionOptions(command);
    addPromptPriorityOptions(command);
    command.option(
        '--wait-after-prompt <duration>',
        spaceTrim(`
            Wait this long after each prompt has been implemented, verified and committed before starting the next prompt.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no wait).
        `),
    );
    command.option(
        '--wait-between-prompts <duration>',
        spaceTrim(`
            Pace prompts so that each next prompt starts at least this long after the previous one began.
            If the previous prompt already took longer than this, the next prompt starts immediately.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no pacing).
        `),
    );
    command.option(
        '--wait-after-error <duration>',
        spaceTrim(`
            Wait this long before retrying a prompt after an error occurs (up to 3 retries before giving up).
            Accepts durations like 1h, 30m, 5s. Defaults to 10m.
        `),
    );
    command.option(
        '--no-auto',
        'Wait for user confirmation before each prompt instead of running automatically through the queue',
    );
    command.option(
        '--auto-migrate',
        'Run testing-server database migrations automatically after each successfully processed prompt',
    );
    command.option(
        '--allow-destructive-auto-migrate',
        'Allow auto-migrate even when heuristic SQL safety check flags destructive pending migrations',
    );

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                port: rawPort,
                dryRun,
                agent,
                context,
                test,
                preserveLogs,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
                waitAfterPrompt: waitAfterPromptValue,
                waitBetweenPrompts: waitBetweenPromptsValue,
                waitAfterError: waitAfterErrorValue,
                auto,
                autoMigrate,
                allowDestructiveAutoMigrate,
            } = cliOptions as {
                readonly port: string;
                readonly dryRun: boolean;
                readonly agent?: string;
                readonly context?: string;
                readonly test?: string | string[];
                readonly preserveLogs: boolean;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
                readonly waitAfterPrompt?: string;
                readonly waitBetweenPrompts?: string;
                readonly waitAfterError?: string;
                readonly auto: boolean;
                readonly autoMigrate: boolean;
                readonly allowDestructiveAutoMigrate: boolean;
            } & PromptRunnerCliOptions;

            const port = parseCoderServerPort(rawPort);
            const testCommand = normalizeCommandOptionValue(test);
            const runnerOptions = normalizePromptRunnerCliOptions(cliOptions as PromptRunnerCliOptions, {
                isAgentRequired: !dryRun,
            });
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);

            // [1] Parse the wait options and --no-auto (same logic as `coder run`)
            const waitForUser = !auto;

            assertUserConfirmationIsAllowed({ ...questionsOptions, isWaitingForUser: waitForUser });

            // Note: Checked before anything is installed or written, because installing a harness and coding
            //       itself both need the disk space which is missing
            await $assertSufficientFreeDiskSpace(process.cwd());

            await $ensureHarnessInstallations([runnerOptions.agentName], questionsOptions);
            await $ensureCoderHarnessGitignoreRules(process.cwd(), runnerOptions.agentName, questionsOptions);

            const waitAfterPrompt = parseOptionalWaitDuration(waitAfterPromptValue, 0);
            const waitBetweenPrompts = parseOptionalWaitDuration(waitBetweenPromptsValue, 0);
            const waitAfterError = parseOptionalWaitDuration(waitAfterErrorValue, DEFAULT_WAIT_AFTER_ERROR_MS);

            const runOptions = {
                port,
                dryRun,
                waitForUser,
                waitAfterPrompt,
                waitBetweenPrompts,
                waitAfterError,
                noCommit: runnerOptions.noCommit,
                gitChanges: runnerOptions.gitChanges,
                agentName: runnerOptions.agentName,
                model: runnerOptions.model,
                agent,
                context,
                testCommand,
                preserveLogs,
                noUi: runnerOptions.noUi,
                thinkingLevel: runnerOptions.thinkingLevel,
                priority: priority ?? 0,
                minimumPriority,
                maximumPriority,
                normalizeLineEndings: runnerOptions.normalizeLineEndings,
                allowCredits: runnerOptions.allowCredits,
                autoMigrate,
                allowDestructiveAutoMigrate,
                autoPush: runnerOptions.autoPush,
                autoPull: runnerOptions.autoPull,
                isAskingQuestionsEnabled: questionsOptions.isAskingQuestionsEnabled,
            };

            // Note: Import dynamically to avoid loading heavy dependencies until needed
            const { runCodexPromptsServer } = await import(
                '../../../../scripts/run-codex-prompts/main/runCodexPromptsServer'
            );

            try {
                await runCodexPromptsServer(runOptions);
            } catch (error) {
                assertsError(error);
                printCoderRunFailure(error);
                return process.exit(1);
            }

            return process.exit(0);
        }),
    );
}

/**
 * Parses and validates the coder server port number.
 *
 * @private internal utility of `coder server` command
 */
function parseCoderServerPort(rawPort: string): number_port {
    const port = Number.parseInt(rawPort, 10);

    if (!Number.isInteger(port) || port <= 0 || port > NETWORK_LIMITS.MAX_PORT) {
        throw new NotAllowed(
            spaceTrim(`
                Invalid coder server port: \`${rawPort}\`.

                Use \`--port\` or \`PTBK_CODER_SERVER_PORT\` with an integer between \`1\` and \`${NETWORK_LIMITS.MAX_PORT}\`.
            `),
        );
    }


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!node_modules'"' \"readFile\\(.*book|readdir.*book|\\.book.*resolve|resolve.*Agent.*Path|agent.*first line|agent name|agentName.*book|find.*Agent\" src scripts apps | Select-Object -First 700" in C:\Users\me\work\ai\promptbook
 succeeded in 2483ms:
src\utils\random\$generateBookBoilerplate.ts:24:     * Name pool to use for generating agent name
src\utils\random\$generateBookBoilerplate.ts:53:        agentName = randomFullnameWithColor.fullname as string_agent_name_in_book;
src\utils\files\$induceBookDownload.ts:20:    const { agentName } = parseAgentSource(book);
src\utils\files\$induceBookDownload.ts:21:    const bookFile = new File([book], `${titleToName(agentName || 'AI Avatar')}.book`, {
apps\coder-landing\specs\content\developer-agent.md:5:This is the **default developer agent** which `ptbk coder init` creates at `agents/developer.book` (it mirrors `agents/default/developer.book` in the Promptbook repository). It is written in the **Book language** — Promptbook's human-readable language for defining AI agents, where the first line is the agent name and keyword *commitments* (`META`, `RULE`, `PERSONA`, `KNOWLEDGE`, …) each start a block.
src\transpilers\_common\TranspiledTeamExport.ts:59:     * Human-readable agent name.
src\transpilers\_common\prepareSdkTranspilerContext.ts:29:     * Parsed agent name used in the generated CLI harness.
src\transpilers\_common\prepareSdkTranspilerContext.ts:76:    const { agentName } = await parseAgentSource(book);
src\transpilers\_common\createTranspiledTeamRuntimeSection.ts:165:    const rootAgent = transpiledTeam.agents.find((agent) => agent.url === transpiledTeam.rootAgentUrl);
src\transpilers\_common\createTranspiledTeamRuntimeSection.ts:207:            function findPromptbookBuiltInTeamMember(ownerAgentUrl, toolName) {
src\transpilers\_common\createTranspiledTeamRuntimeSection.ts:283:                const { ownerAgent, teammate, teamMemberAgent } = findPromptbookBuiltInTeamMember(ownerAgentUrl, toolName);
src\transpilers\_common\BookTranspilerOptions.ts:36:     * Optional resolver that provides actual agent names and descriptions for teammate URLs.
src\import-plugins\AgentFileImportPlugin.ts:22:        // Stripping the agent name (which is usually the first line)
apps\utils\src\app\avatars\avatarPlaygroundUrlState.ts:30: * Query parameter for the agent name.
scripts\update-version-in-config\update-version-in-config.ts:53:    const BOOK_LANGUAGE_VERSION: string = await readFile(`./book/version.txt`, 'utf-8');
scripts\run-codex-prompts\common\resolveCoderAgent.ts:36:    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
scripts\run-codex-prompts\common\resolveCoderAgent.ts:37:    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
src\constants.ts:131:     * Minimum length of a name (e.g. agent name, persona name)
src\constants.ts:166:     * Default length of a short name (e.g. for default agent names)
src\commitments\_base\createEmptyAgentModelRequirements.ts:25: * Creates a basic agent model requirements with just the agent name
src\commitments\_base\createEmptyAgentModelRequirements.ts:26: * This is used when we have an agent name but no commitments
scripts\run-agent-messages\ui\buildAgentRunUiFrame.ts:172: * Builds the rich agent status table with status, agent name, and URL columns.
scripts\run-agent-messages\ui\buildAgentRunInitialsVisual.ts:15: * Builds a compact centered banner that shows the local agent name.
scripts\run-agent-messages\ui\buildAgentRunInitialsVisual.ts:36: * Normalizes one agent name into a stable single-line banner label.
src\commitments\PERSONA\PERSONA.ts:115:        // Get the agent name from metadata (which should contain the first line of agent source)
src\commitments\META_FULLNAME\META_FULLNAME.ts:10: * from the agent name on the first line of the agent source, for example when the agent
src\commitments\META_FULLNAME\META_FULLNAME.ts:47:            - If not present, the agent name from the first line of the agent source is used.
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.ts:15:export function resolveAgentProjectsUrlPath(projectPath: string): string | undefined {
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.ts:16:    const agentId = resolveAgentIdFromRepositoryName(basename(projectPath));
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.test.ts:2:import { resolveAgentProjectsUrlPath } from './resolveAgentProjectsUrlPath';
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.test.ts:4:describe('resolveAgentProjectsUrlPath', () => {
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.test.ts:6:        expect(resolveAgentProjectsUrlPath(join('/', 'srv', 'agents', 'agent-1dkmraaikkd8yp'))).toBe(
scripts\run-agent-messages\messages\resolveAgentProjectsUrlPath.test.ts:12:        expect(resolveAgentProjectsUrlPath(join('/', 'tmp', 'session-xyz'))).toBe(undefined);
scripts\run-agent-messages\messages\resolveAgentProjectRuntimePromptApi.ts:15:export function resolveAgentProjectRuntimePromptApi(projectPath: string): AgentProjectRuntimePromptApi | undefined {
scripts\run-agent-messages\messages\resolveAgentProjectRuntimePromptApi.ts:16:    const agentPermanentId = resolveAgentIdFromRepositoryName(basename(projectPath));
src\collection\agent-collection\constructors\agent-collection-in-supabase\buildAgentNameOrPermanentIdFilter.test.ts:5:    it('matches the agent name exactly and the permanent id case-insensitively', () => {
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:155:    public async findAgentBasicInformation(
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:226:        const activeAgentPermanentId = await this.findAgentPermanentId(agentNameOrPermanentId, {
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:234:        const deletedAgentPermanentId = await this.findAgentPermanentId(agentNameOrPermanentId, {
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:254:    private async findAgentPermanentId(
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:661:                    'id,createdAt,agentName,agentHash,previousAgentHash,agentSource,promptbookEngineVersion,versionName',
src\collection\agent-collection\constructors\agent-collection-in-supabase\AgentCollectionInSupabase.ts:683:            .select('id,createdAt,agentName,agentHash,previousAgentHash,promptbookEngineVersion,versionName')
src\collection\agent-collection\constructors\agent-collection-in-directory\AgentCollectionInDirectory.ts.todo:140:        const agentSourcePath = `${this.rootPath}/${agentName}.book`;
src\cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:39:    await copyFile(await resolveDefaultCoderDeveloperAgentFilePath(), absoluteFilePath);
src\cli\cli-commands\coder\ensureCoderDeveloperAgentFile.ts:46:async function resolveDefaultCoderDeveloperAgentFilePath(): Promise<string> {
src\cli\cli-commands\agents-server\startAgentsServer.ts:15:import { resolveAgentsServerRuntimePaths } from './startAgentsServer/AgentsServerRuntimePaths';
src\cli\cli-commands\agents-server\startAgentsServer.ts:39:    const runtimePaths = await resolveAgentsServerRuntimePaths();
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:3:import { resolveAgentsServerAppPath } from '../buildAgentsServer/resolveAgentsServerAppPath';
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:31:export async function resolveAgentsServerRuntimePaths(): Promise<AgentsServerRuntimePaths> {
src\cli\cli-commands\agents-server\startAgentsServer\AgentsServerRuntimePaths.ts:33:    const appPath = await resolveAgentsServerAppPath();
src\cli\cli-commands\agents-server\buildAgentsServer.ts:15:export { resolveAgentsServerAppPath } from './buildAgentsServer/resolveAgentsServerAppPath';
src\cli\cli-commands\agents-server\buildAgentsServer.ts:16:export { resolveAgentsServerBuildAppPath } from './buildAgentsServer/resolveAgentsServerBuildAppPath';
src\cli\cli-commands\agents-server\buildAgentsServer.test.ts:13:import { resolveAgentsServerBuildAppPath } from './buildAgentsServer/resolveAgentsServerBuildAppPath';
src\cli\cli-commands\agents-server\buildAgentsServer.test.ts:172:        const materializedAppPath = await resolveAgentsServerBuildAppPath({
src\cli\cli-commands\agents-server\buildAgentsServer\writeAgentsServerBuildCache.ts:8:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src\cli\cli-commands\agents-server\buildAgentsServer\writeAgentsServerBuildCache.ts:16:    const buildOutputPath = resolveAgentsServerBuildOutputPath(options);
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildOutputPath.ts:14:export function resolveAgentsServerBuildOutputPath(options: AgentsServerBuildCacheOptions): string {
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerBuildAppPath.ts:11:export async function resolveAgentsServerBuildAppPath(options: {
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerAppPath.ts:11:export async function resolveAgentsServerAppPath(): Promise<string> {
src\cli\cli-commands\agents-server\buildAgentsServer\resolveAgentsServerAppPath.ts:27:            Cannot find the bundled Agents Server app.
src\cli\cli-commands\agents-server\buildAgentsServer\readAgentsServerBuildCache.ts:7:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src\cli\cli-commands\agents-server\buildAgentsServer\readAgentsServerBuildCache.ts:19:            join(resolveAgentsServerBuildOutputPath(options), AGENTS_SERVER_BUILD_CACHE_FILENAME),
src\cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:3:import { resolveAgentsServerAppPath } from './resolveAgentsServerAppPath';
src\cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:4:import { resolveAgentsServerBuildAppPath } from './resolveAgentsServerBuildAppPath';
src\cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:20:    const appPath = await resolveAgentsServerBuildAppPath({
src\cli\cli-commands\agents-server\buildAgentsServer\prepareAgentsServerRuntime.ts:21:        sourceAppPath: options.appPath ?? (await resolveAgentsServerAppPath()),
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildCacheCurrent.ts:7:import { resolveAgentsServerBuildOutputPath } from './resolveAgentsServerBuildOutputPath';
src\cli\cli-commands\agents-server\buildAgentsServer\isAgentsServerBuildCacheCurrent.ts:21:    const buildOutputPath = resolveAgentsServerBuildOutputPath(options);
src\cli\cli-commands\agent-folder\runMultiple.ts:33:                'Ignore watched agent repositories whose agent name, normalized name, or id matches the pattern',
src\cli\cli-commands\agent-folder\agentProjectPaths.ts:69:        Book language defines a local AI agent as plain text. The first non-empty line is the agent name. Each following commitment starts with an uppercase keyword and continues until the next commitment.
src\cli\cli-commands\agent\exec.ts:16:    resolveRequiredAgentPath,
src\cli\cli-commands\agent\exec.ts:49:                agentPath: resolveRequiredAgentPath(options),
src\cli\cli-commands\agent\chat.ts:13:import { normalizeAgentCommandRunnerOptions, resolveRequiredAgentPath } from './agentCliOptions';
src\cli\cli-commands\agent\chat.ts:44:                agentPath: resolveRequiredAgentPath(options),
src\cli\cli-commands\agent\agentCliOptions.ts:56:export function resolveRequiredAgentPath(cliOptions: AgentCommandCliOptions): string {
apps\book-components\src\app\api\books\route.ts:13:        const files = fs.readdirSync(examplesDir).filter((file) => file.endsWith('.book'));
scripts\run-agent-messages\main\tickAgentMessages.ts:40:import { resolveAgentProjectsUrlPath } from '../messages/resolveAgentProjectsUrlPath';
scripts\run-agent-messages\main\tickAgentMessages.ts:266:    const agentServerRuntimeApi = resolveAgentProjectRuntimePromptApi(projectPath);
scripts\run-agent-messages\main\tickAgentMessages.ts:270:        projectsUrlPath: resolveAgentProjectsUrlPath(projectPath),
scripts\run-agent-messages\main\tickAgentMessages.test.ts:239:        expect(await readFile(join(temporaryProjectPath, 'messages', 'finished', 'question.book'), 'utf-8')).toContain(
scripts\run-agent-messages\main\tickAgentMessages.test.ts:243:            await readFile(join(temporaryProjectPath, 'messages', 'finished', 'question.book.report.json'), 'utf-8'),
scripts\run-agent-messages\main\tickAgentMessages.test.ts:384:        expect(await readFile(join(temporaryProjectPath, 'messages', 'queued', 'first.book'), 'utf-8')).toContain(
scripts\run-agent-messages\main\tickAgentMessages.test.ts:387:        expect(await readFile(join(temporaryProjectPath, 'messages', 'finished', 'second.book'), 'utf-8')).toContain(
scripts\run-agent-messages\main\tickAgentMessages.test.ts:498:        expect(await readFile(join(temporaryProjectPath, 'messages', 'finished', 'thread.book'), 'utf-8')).toContain(
scripts\run-agent-messages\main\synchronizeGithubAgentRunnerRepositories.ts:216: * Loads `agent.book` from GitHub so ignore patterns can also match agent names before clone.
scripts\run-agent-messages\main\synchronizeGithubAgentRunnerRepositories.test.ts:251:    it('does not clone missing repositories ignored by remote agent name', async () => {
scripts\run-agent-messages\main\runMultipleAgentMessages.test.ts:246:    it('ignores local repositories by agent name, normalized agent name, and agent id', async () => {
scripts\run-agent-messages\main\runMultipleAgentMessages.test.ts:459:        const failedMessage = await readFile(join(agentProjectPath, 'messages', 'failed', 'question.book'), 'utf-8');
src\book-components\Chat\utils\loadAgentProfile.ts:60: * Extracts agent name from URL.
src\book-components\Chat\Chat\TeamToolCallModalContent.tsx:22: * Default label used when the agent name is unavailable.
apps\agents-server\tests\e2e\support\ChatHistoryNavigationSupport.ts:56:     * Human-readable agent name.
apps\agents-server\tests\e2e\support\ChatHistoryNavigationExpectation.ts:9:     * Human-readable agent name.
apps\agents-server\tests\e2e\support\AgentManagementApi.ts:39:     * Human-readable agent name.
src\book-components\Chat\AgentChat\AgentChat.test.tsx:249:    it('should generate default persistenceKey from agent name', async () => {
src\book-3.0\CliAgent.ts:93:        const agentPath = await this.resolveExecutableAgentPath(resolvedSource);
src\book-3.0\CliAgent.ts:132:    private async resolveExecutableAgentPath(resolvedSource: ResolvedBookNodeAgentSource): Promise<string> {
src\book-3.0\BookNodeAgentSource.ts:110:        return (await readFile(absoluteAgentPath, 'utf-8')) as string_book;
src\book-2.0\book-language-documentation\renderGroupedCommitmentDocumentationMarkdown.ts:384: * example's first line as its mandatory agent name.
src\book-2.0\book-language-documentation\renderGroupedCommitmentDocumentationMarkdown.ts:386: * @param agentName - Parsed agent name from the original example.
src\book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:146:                - \`@Name\` is shorthand for \`{Name}\` when the agent name is one word.
src\book-2.0\book-language-documentation\englishBookLanguageManualDictionary.ts:211:                'The first line (`Hello World Agent`) is the agent name.',
src\book-2.0\agent-source\string_book.ts:15:    // Basic validation - agent source should have at least a name (first line)
src\book-2.0\agent-source\pseudoAgentReferences.ts:54: * Mapping from normalized pseudo-agent names to their runtime kinds.
src\book-2.0\agent-source\parseAgentSourceWithCommitments.ts:54: * The first non-empty line is always consumed as plain-text agent name. Commitment parsing
src\book-2.0\agent-source\parseAgentSourceWithCommitments.ts:78:    // Process lines starting from after the agent name line
src\book-2.0\agent-source\parseAgentSourceWithCommitments.title.test.ts:26:    it('treats the first non-empty line as the agent name when there is no description block', () => {
src\book-2.0\agent-source\parseAgentSourcePrelude.ts:16:     * First non-empty line consumed as the agent name.
src\book-2.0\agent-source\parseAgentSourcePrelude.ts:35: * non-whitespace characters is always treated as plain-text agent name, even when it starts
src\book-2.0\agent-source\parseAgentSource.test.ts:559:    it('uses agent name as fallback for fullname', () => {
src\book-2.0\agent-source\parseAgentSource.test.ts:695:        // First non-empty is agent name
src\book-2.0\agent-source\parseAgentSource.test.ts:705:        // Empty lines before agent name
src\book-2.0\agent-source\parseAgentSource.test.ts:717:        // Commitment-looking first non-empty line is still the agent name
src\book-2.0\agent-source\parseAgentSource.test.ts:729:        // Non-commitment line before agent name (should take first one)
src\book-2.0\agent-source\normalizeAgentName.ts:6: * Normalizes agent name from arbitrary string to valid agent name
src\book-2.0\agent-source\createDefaultAgentName.ts:8: * Creates temporary default agent name based on agent source hash
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:41: * Creates the initial requirements object with the parsed agent name stored in metadata and an optional model override.
src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:43: * @param agentName - Parsed agent name from the source prelude.
src\book-2.0\agent-source\CreateAgentModelRequirementsOptions.ts:22:     * Optional resolver that provides actual agent names and descriptions for teammate URLs.
src\book-2.0\agent-source\createAgentModelRequirements.ts:65:    // Use agent name as fallback if no profile description is available
src\book-2.0\agent-source\createAgentModelRequirements.ts:129: * Extracts the agent name from the first non-empty line of the agent source.
src\book-2.0\agent-source\createAgentModelRequirements.tools.test.ts:206:    it('should not interpret the agent name as an MCP server when the title starts with `MCP`', async () => {
scripts\run-agent-chat\executeAgentChatTurn.test.ts:151:        const threadBook = await readFile(join(projectPath, 'messages', 'queued', 'thread.book'), 'utf-8');
apps\agents-server\src\utils\userWallet\resolveWalletAgentPermanentId.test.ts:54:    it('resolves an agent name to the canonical permanent id used by wallet records', async () => {
src\book-2.0\agent-source\AgentSourceParseResult.ts:12:     * The agent name taken from the first non-empty line after leading whitespace-only lines.
src\book-2.0\agent-source\AgentSourceParseResult.ts:17:     * The line number where the agent name was found (1-based)
apps\agents-server\src\utils\userChat\userChatProgressCard.ts:23:     * Resolved human-readable agent name, when already loaded for this turn.
apps\agents-server\src\utils\userChat\userChatProgressCard.ts:151: * The optional `context` carries already-resolved runtime details (agent name, provider,
apps\agents-server\src\utils\userChat\runImmediateUserChatAnswer.test.ts:40:    it('falls back to the agent name when no lightweight instruction commitments are present', () => {
apps\agents-server\src\utils\userChat\persistUserChatJobProgressCard.ts:11: * The optional `context` carries real runtime details (agent name, provider, tool count,
apps\agents-server\src\utils\usageAnalytics\UsageAnalyticsSource.ts:130: * Resolves allowed agent names for the selected scope.
apps\agents-server\src\utils\usageAnalytics\UsageAnalyticsAggregationFormatter.ts:161: * Formats per-agent usage items sorted by calls and agent name.
apps\agents-server\src\utils\transpilers\resolveTranspiledAgentCodeExport.test.ts:2:import { findBookTranspilerForExport, listBookTranspilersForExport } from './resolveTranspiledAgentCodeExport';
apps\agents-server\src\utils\transpilers\createTranspiledAgentExportArtifacts.ts:45: * Fallback path segment when the agent name sanitizes to an empty value.
apps\agents-server\src\utils\transpilers\createTranspiledAgentExportArtifacts.ts:141:     * Human-facing agent name used for filenames and manifest metadata.
apps\agents-server\src\utils\transpilers\createTranspiledAgentExportArtifacts.ts:853: * @param agentName - Human-facing agent name.
apps\agents-server\src\utils\taskTerminal\resolveAdminTaskTerminalSession.ts:9:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\taskTerminal\resolveAdminTaskTerminalSession.ts:234:        join(resolveLocalAgentRootPath(), options.agentDirectoryName),
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:171: * When duplicate agent names with different book sources are found and the conflict resolution is `ASK`, this function
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:624: * Creates duplicate conflicts for entries whose agent name already exists with different source.
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:665: * @param knownAgentSourcesByName - Known normalized sources keyed by agent name.
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:686: * @returns Known sources keyed by agent name.
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:735:                Book file \`${path}\` does not contain an agent name.
apps\agents-server\src\utils\agentsTransfer\importAgentsFromFiles.ts:737:                The first line of the book must define the agent name.
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.ts:172:    const directMatch = await collection.findAgentBasicInformation(reference);
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.ts:313:    const agentMatch = await findLocalAgentRouteMatch(collection, reference);
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.ts:335:async function findLocalAgentRouteMatch(
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.ts:339:    const directMatch = await collection.findAgentBasicInformation(reference);
src\avatars\renderAvatarVisualTerminalText.test.ts:12:    agentName: 'Promptbook Developer',
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.test.ts:13:    findAgentBasicInformation: jest.fn(async () => ({
apps\agents-server\src\utils\agentRouting\resolveAgentRouteTarget.test.ts:75:        expect(mockAgentCollection.findAgentBasicInformation).toHaveBeenCalledWith('Lawyer');
apps\agents-server\src\utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:67:        findMissingCoreAgent(missingCoreAgents, missingAgentReference.reference)
apps\agents-server\src\utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:76:    const missingAdamAgent = findMissingCoreAgent(missingCoreAgents, CORE_AGENTS_SERVER_WELL_KNOWN_AGENT_NAMES.ADAM);
apps\agents-server\src\utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:114:function findMissingCoreAgent(
apps\agents-server\src\utils\agentReferenceResolver\resolveCoreAgentAwareMissingReferences.ts:119:    return missingCoreAgents.find((missingCoreAgent) => missingCoreAgent.agentName === normalizedReference);
apps\agents-server\src\utils\agentReferenceResolver\createServerAgentReferenceResolver.ts:131:     * Used during TEAM commitment processing to enrich tool definitions with real agent names
apps\agents-server\src\utils\agentReferenceResolver\createAgentNameCollisionDiagnostics.ts:8: * Creates name-collision diagnostics for the agent name line.
apps\agents-server\src\utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:39:    const findAgentRecord = (agentIdentifier: string): LocalAgentRecord => {
apps\agents-server\src\utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:74:            return findAgentRecord(agentIdentifier).permanentId as string_agent_permanent_id;
apps\agents-server\src\utils\agentReferenceResolver\createAgentInheritanceDiagnostics.test.ts:77:            return findAgentRecord(agentIdentifier).agentSource;
apps\agents-server\src\utils\agentReferenceResolver\bookScopedAgentReferences.ts:71: * @param parentAgentIdentifier - Parent agent name/permanent id.
apps\agents-server\src\utils\agentReferenceResolver\bookScopedAgentReferences.ts:151: * @returns Map keyed by normalized embedded agent name.
apps\agents-server\src\utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:22:    resolveLocalAgentRootPath: jest.fn(),
apps\agents-server\src\utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:29:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps\agents-server\src\utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:30:    typeof resolveLocalAgentRootPath
apps\agents-server\src\utils\agentProjects\startDefaultAgentProjectRuntimes.test.ts:61:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps\agents-server\src\utils\agentProjects\resolveAgentProjectInfo.ts:5:import { resolveAgentProjectsRootPath, resolveSafeAgentProjectPath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\resolveAgentProjectInfo.ts:10:import { resolveAgentProjectFaviconRelativePath } from './resolveAgentProjectFaviconRelativePath';
apps\agents-server\src\utils\agentProjects\resolveAgentProjectInfo.ts:36:    const faviconRelativePath = await resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectInfo.ts:68:    const projectPath = resolveSafeAgentProjectPath({ agentPermanentId, projectName });
apps\agents-server\src\utils\agentProjects\resolveAgentProjectInfo.ts:83:    return createAgentProjectInfo(resolveAgentProjectsRootPath(agentPermanentId), projectName);
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.ts:29:export async function resolveAgentProjectFaviconRelativePath(options: {
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:4:import { resolveAgentProjectFaviconRelativePath } from './resolveAgentProjectFaviconRelativePath';
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:6:describe('resolveAgentProjectFaviconRelativePath', () => {
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:26:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:38:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:49:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:58:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:69:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\resolveAgentProjectFaviconRelativePath.test.ts:78:            resolveAgentProjectFaviconRelativePath({
apps\agents-server\src\utils\agentProjects\readAgentProjectFile.ts:5:import { resolveSafeAgentProjectFilePath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\readAgentProjectFile.ts:42:    const filePath = resolveSafeAgentProjectFilePath(options);
apps\agents-server\src\utils\agentProjects\listAllLocalAgentProjectIdentities.ts:2:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\listAllLocalAgentProjectIdentities.ts:18:    const localAgentRootPath = resolveLocalAgentRootPath();
apps\agents-server\src\utils\agentProjects\listAllAgentProjectSummaries.ts:7:import { createLocalAgentDirectoryName, resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\listAllAgentProjectSummaries.ts:83:        rootPath: resolveLocalAgentRootPath(),
apps\agents-server\src\utils\agentProjects\listAllAgentProjectSummaries.ts:133:    const rootPath = resolveLocalAgentRootPath();
apps\agents-server\src\utils\agentProjects\listAgentProjects.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\listAgentProjects.ts:16:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps\agents-server\src\utils\agentProjects\listAgentProjectNames.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\listAgentProjectNames.ts:18:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.ts:6:import { resolveSafeAgentProjectDirectoryPath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.ts:76:    const directoryPath = resolveSafeAgentProjectDirectoryPath(options);
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:8:    resolveLocalAgentRootPath: jest.fn(),
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:15:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:16:    typeof resolveLocalAgentRootPath
apps\agents-server\src\utils\agentProjects\listAgentProjectDirectoryEntries.test.ts:24:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps\agents-server\src\utils\agentProjects\hasAgentProjects.ts:2:import { resolveAgentProjectsRootPath } from './agentProjectsPaths';
apps\agents-server\src\utils\agentProjects\hasAgentProjects.ts:12:    const projectsRootPath = resolveAgentProjectsRootPath(agentPermanentId);
apps\agents-server\src\utils\agentProjects\createStaticAgentProjectServer.ts:154:        'Content-Type': resolveAgentProjectFileContentType(basename(filePath)),
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:5:import { createLocalAgentDirectoryName, resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:18:export function resolveAgentFolderPath(agentPermanentId: string): string {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:19:    return join(resolveLocalAgentRootPath(), createLocalAgentDirectoryName(agentPermanentId));
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:28:export function resolveAgentProjectsRootPath(agentPermanentId: string): string {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:29:    return join(resolveAgentFolderPath(agentPermanentId), AGENT_PROJECTS_DIRECTORY_PATH);
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:39:export function resolveSafeAgentProjectPath(options: {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:45:    return join(resolveAgentProjectsRootPath(options.agentPermanentId), options.projectName);
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:106:export function resolveSafeAgentProjectFilePath(options: {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:117:    return join(resolveSafeAgentProjectPath({ agentPermanentId, projectName }), ...filePathSegments);
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:127:export function resolveSafeAgentProjectDirectoryPath(options: {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.ts:138:    return join(resolveSafeAgentProjectPath({ agentPermanentId, projectName }), ...directoryPathSegments);
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:6:    resolveAgentProjectsRootPath,
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:7:    resolveSafeAgentProjectDirectoryPath,
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:8:    resolveSafeAgentProjectFilePath,
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:9:    resolveSafeAgentProjectPath,
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:42:describe('resolveSafeAgentProjectFilePath', () => {
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:44:        const projectPath = resolveSafeAgentProjectPath({
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:49:        expect(projectPath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website'));
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:53:        const filePath = resolveSafeAgentProjectFilePath({
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:59:        expect(filePath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website', 'assets', 'style.css'));
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:63:        const directoryPath = resolveSafeAgentProjectDirectoryPath({
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:69:        expect(directoryPath).toBe(join(resolveAgentProjectsRootPath('abc123'), 'my-website', 'assets', 'icons'));
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:74:            resolveSafeAgentProjectFilePath({
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:82:            resolveSafeAgentProjectFilePath({
apps\agents-server\src\utils\agentProjects\agentProjectsPaths.test.ts:90:            resolveSafeAgentProjectDirectoryPath({
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:39:import { resolveAgentProjectRuntimeRegistryFilePath } from './agentProjectRuntimePaths';
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:164:    const existingRuntimeRecord = findAgentProjectRuntimeRecord(options.agentPermanentId, options.projectName);
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:310:    const runtimeRecord = findAgentProjectRuntimeRecord(agentPermanentId, projectName);
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:332:    const runtimeRecord = findAgentProjectRuntimeRecord(options.agentPermanentId, options.projectName);
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:480:    const runtimeRecord = findAgentProjectRuntimeRecord(options.agentPermanentId, options.projectName);
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:700:function findAgentProjectRuntimeRecord(
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:887:    const registryFilePath = resolveAgentProjectRuntimeRegistryFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.ts:912:    const registryFilePath = resolveAgentProjectRuntimeRegistryFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.test.ts:4:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.test.ts:24:    resolveLocalAgentRootPath: jest.fn(),
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.test.ts:31:const resolveLocalAgentRootPathMock = resolveLocalAgentRootPath as jest.MockedFunction<
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.test.ts:32:    typeof resolveLocalAgentRootPath
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeRegistry.test.ts:77:        resolveLocalAgentRootPathMock.mockReturnValue(temporaryDirectory);
apps\agents-server\src\utils\agentProjects\agentProjectRuntimePaths.ts:74:export function resolveAgentProjectRuntimeRegistryFilePath(): string {
apps\agents-server\src\utils\agentProjects\agentProjectRuntimePaths.ts:89:export function resolveAgentProjectDomainRegistryFilePath(): string {
apps\agents-server\src\utils\agentProjects\agentProjectRuntimePaths.ts:104:export function resolveAgentProjectRuntimeDesiredStateFilePath(): string {
apps\agents-server\src\utils\agentProjects\agentProjectRuntimePaths.ts:119:export function resolveAgentProjectDomainsFilePath(): string {
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDomains.ts:9:    resolveAgentProjectDomainRegistryFilePath,
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDomains.ts:10:    resolveAgentProjectDomainsFilePath,
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDomains.ts:310:    const registryFilePath = resolveAgentProjectDomainRegistryFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDomains.ts:450:    const registryFilePath = resolveAgentProjectDomainRegistryFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDomains.ts:451:    const domainsFilePath = resolveAgentProjectDomainsFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDesiredState.ts:6:import { resolveAgentProjectRuntimeDesiredStateFilePath } from './agentProjectRuntimePaths';
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDesiredState.ts:70:    const desiredStateFilePath = resolveAgentProjectRuntimeDesiredStateFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDesiredState.ts:118:    const desiredStateRecord = desiredStateRecords.find((record) => isSameAgentProjectIdentity(record, identity));
apps\agents-server\src\utils\agentProjects\agentProjectRuntimeDesiredState.ts:180:    const desiredStateFilePath = resolveAgentProjectRuntimeDesiredStateFilePath();
apps\agents-server\src\utils\agentProjects\agentProjectCodeServerRegistry.ts:261:    const existingSession = findAgentProjectCodeServerSessionRecord({
apps\agents-server\src\utils\agentProjects\agentProjectCodeServerRegistry.ts:303:    const extensionsDirectory = join(resolveAgentProjectCodeServerStateRoot(), 'extensions', sanitizePathPart(userKey));
apps\agents-server\src\utils\agentProjects\agentProjectCodeServerRegistry.ts:481:function findAgentProjectCodeServerSessionRecord(options: {
apps\agents-server\src\utils\agentOwnership.ts:61: * Loads one owner-scoped agent using permanent id first and agent name as fallback.
apps\agents-server\src\utils\agentOwnership.ts:64: * @param identifier - Permanent id or agent name.
apps\agents-server\src\utils\agentOwnership.ts:67:export async function findOwnedAgentByIdentifier(userId: number, identifier: string): Promise<OwnedAgentRow | null> {
apps\agents-server\src\utils\agentOwnership.ts:85: * Loads one agent by permanent id or agent name without an owner restriction.
apps\agents-server\src\utils\agentOwnership.ts:87: * Owner-aware callers should use {@link findOwnedAgentByIdentifier}; this helper is reserved for administrative
apps\agents-server\src\utils\agentOwnership.ts:90: * @param identifier - Permanent id or agent name.
apps\agents-server\src\utils\agentOwnership.ts:93:export async function findAgentByIdentifier(identifier: string): Promise<OwnedAgentRow | null> {
apps\agents-server\src\utils\agentOwnership.ts:113:    'id, agentName, createdAt, updatedAt, permanentId, agentHash, agentSource, agentProfile, promptbookEngineVersion, folderId, sortOrder, deletedAt, visibility, userId';
apps\agents-server\src\utils\agentOwnership.ts:137:    const permanentIdMatch = rows.find((row) => isSameAgentPermanentId(row.permanentId, identifier));
apps\agents-server\src\utils\agentOwnership.ts:163:export async function findOwnedFolderById(userId: number, folderId: number): Promise<OwnedAgentFolderRow | null> {
apps\agents-server\src\utils\agentIdentifier.ts:8:     * Human-readable agent name.
apps\agents-server\src\utils\agentIdentifier.ts:22: * @returns Permanent id when available, otherwise the legacy agent name.
apps\agents-server\src\utils\agentIdentifier.test.ts:5:        it('prefers the permanent id over the agent name', () => {
apps\agents-server\src\utils\agentIdentifier.test.ts:11:        it('falls back to the agent name when no permanent id is present', () => {
apps\agents-server\src\utils\agentGoalChat\createAgentGoalChatNoteContent.ts:15: * @param options - Lifecycle event and the agent name shown in the note.
apps\agents-server\src\utils\agentGoalChat\canAccessAgentGoalChat.ts:7: * source book page (`/agents/<agentName>/book`): a signed-in user is required. Keeping the rule in
apps\agents-server\src\generated\reservedPaths.ts:2:     * Reserved paths that should not be treated as agent names.
apps\agents-server\src\utils\adminEntityLookups\loadAgentNamesByPermanentId.ts:13: * Loads agent names keyed by permanent id, for admin listings that only store the permanent id.
apps\agents-server\src\database\sqlite\resolveServerSqliteDatabasePath.ts:4:import { resolveAgentsServerSqliteDatabasePath } from './resolveAgentsServerSqliteDatabasePath';
apps\agents-server\src\database\sqlite\resolveServerSqliteDatabasePath.ts:40:    const registryDatabasePath = resolveAgentsServerSqliteDatabasePath();
apps\agents-server\src\database\sqlite\resolveAgentsServerSqliteDatabasePath.ts:7:export function resolveAgentsServerSqliteDatabasePath(): string {
apps\agents-server\src\utils\agentBrowserProfile\findAgentBrowserProfileWalletRecord.ts:17:export async function findAgentBrowserProfileWalletRecord(agentPermanentId: string): Promise<UserWalletRow | null> {
apps\agents-server\src\utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:8:import { findAgentBrowserProfileWalletRecord } from './findAgentBrowserProfileWalletRecord';
apps\agents-server\src\utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:31:     * Agent identifier - either `Agent.permanentId` or agent name.
apps\agents-server\src\utils\agentBrowserProfile\$provideAgentBrowserProfile.ts:63:    const existingWalletRecord = await findAgentBrowserProfileWalletRecord(agentPermanentId);
apps\agents-server\src\utils\agentBook\createAgentBookDownloadFilename.ts:9: * Fallback book filename base used when an agent name sanitizes to an empty value.
apps\agents-server\src\utils\agentBook\createAgentBookDownloadFilename.ts:26: * @param agentName - Human-readable agent name used as the filename base.
apps\agents-server\src\database\sqlite\$provideAgentsServerSqliteDatabase.ts:3:import { resolveAgentsServerSqliteDatabasePath } from './resolveAgentsServerSqliteDatabasePath';
apps\agents-server\src\database\sqlite\$provideAgentsServerSqliteDatabase.ts:90:    return $provideSqliteDatabaseAtPath(resolveAgentsServerSqliteDatabasePath());
apps\agents-server\src\database\seedCoreAgents.ts:101: * Reads the set of active agent names already persisted on the current server.
apps\agents-server\src\database\seedCoreAgents.ts:104: * @returns Set of active agent names indexed for quick lookups.
apps\agents-server\src\utils\agentBook\createAgentBookDownloadFilename.test.ts:7:    it('creates a safe .book filename from an agent name', () => {
apps\agents-server\src\utils\backup\serverBackup\serverBackupPreviews.ts:77: * @returns Agent preview map keyed by agent name.
apps\agents-server\src\utils\backup\createBooksBackupZipStream.ts:38: * Fallback book filename base used when an agent name sanitizes to an empty value.
apps\agents-server\src\utils\serverManagement\createManagedServer\seedServerDefaultAgents.test.ts:23:        readdirMock.mockImplementation(async () => ['zeta.book', 'ignore.txt', 'alpha.book'] as never);
apps\agents-server\src\utils\serverManagement\createManagedServer\seedServerCoreAgents.test.ts:23:        readdirMock.mockImplementation(async () => ['teacher.book'] as never);
apps\agents-server\src\utils\cache\AssistantCacheManager.ts:80:     * @param agentName - The agent name for logging and fallback
apps\agents-server\src\utils\cache\AgentKitCacheManager.ts:85:     * @param agentName - The agent name for logging and fallback
apps\agents-server\src\utils\seo\getPublicAgentProfileSeoRecords.ts:23:     * Human-readable agent name.
apps\agents-server\src\utils\resourceMonitor\readAgentProjectsResourceUsage.ts:3:import { resolveLocalAgentRootPath } from '../localChatRunner/ensureLocalAgentFolder';
apps\agents-server\src\utils\resourceMonitor\readAgentProjectsResourceUsage.ts:41:            rootPath: resolveLocalAgentRootPath(),
apps\agents-server\src\utils\resolveServerAgentContext.ts:24:     * Requested agent identifier (agent name, permanent id, or book-scoped id).
apps\agents-server\src\utils\bookLanguageDocumentation\BookLanguageDocumentationAgent.ts:12:     * Human-readable agent name shown in the export menu.
apps\agents-server\src\utils\resolveInheritedAgentSource.ts:523:    const isCyclicFrom = Boolean(parentAgentUrl && findResolutionCycle(parentAgentUrl, context.resolutionOptions));
apps\agents-server\src\utils\resolveAgentStateFromSource.test.ts:53:    const findAgentRecord = (agentNameOrPermanentId: string) => {
apps\agents-server\src\utils\resolveAgentStateFromSource.test.ts:86:            return findAgentRecord(agentNameOrPermanentId).permanentId as string_agent_permanent_id;
apps\agents-server\src\utils\resolveAgentStateFromSource.test.ts:89:            return findAgentRecord(agentNameOrPermanentId).agentSource;
apps\agents-server\src\utils\resolveAgentStateFromSource.storedAgents.test.ts:150:    it('should resolve an agent name to the agent that is live, not to a deleted namesake', async () => {
apps\agents-server\src\utils\renameAgentSource.ts:8: * @param nextAgentName - The new agent name to insert.
apps\agents-server\src\utils\getAdminChatTasksResponse\getAdminChatTasks.test.ts:92:    it('matches admin task-manager search against joined usernames and agent names in SQLite mode', async () => {
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:1:import { findAgentByIdentifier, findOwnedAgentByIdentifier, type OwnedAgentRow } from './agentOwnership';
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:10: * 2. Owners are matched via {@link findOwnedAgentByIdentifier} using the authenticated user id.
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:11: * 3. Administrators (resolved via {@link isUserAdmin}) fall through to {@link findAgentByIdentifier} so they can manage any agent — including legacy `ADMIN_PASSWORD` admins that have no database user id.
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:13: * @param identifier - Permanent id or agent name supplied by the route.
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:18:export async function findAgentForCallerWriteAccess(identifier: string): Promise<OwnedAgentRow | null> {
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:25:        const ownedAgent = await findOwnedAgentByIdentifier(currentUser.id, identifier);
apps\agents-server\src\utils\findAgentForCallerWriteAccess.ts:35:    return findAgentByIdentifier(identifier);
apps\agents-server\src\utils\externalChatRunner\ensureExternalAgentRepository.test.ts:9:    it('normalizes the permanent id without adding the agent name', () => {
apps\agents-server\src\utils\managementApi\managementApiAgents.ts:181:            'id, agentName, createdAt, updatedAt, permanentId, agentHash, agentSource, agentProfile, promptbookEngineVersion, folderId, sortOrder, deletedAt, visibility, userId',
apps\agents-server\src\utils\localChatRunner.ts:4:    resolveLocalAgentRootPath,
apps\agents-server\src\utils\email\processInboundAgentEmail.ts:23:    findAgentEmailIdentity,
apps\agents-server\src\utils\email\processInboundAgentEmail.ts:73:        identity: findAgentEmailIdentity(identities, normalizeAgentEmailRecipientLocalPart(recipient)),
apps\agents-server\src\utils\email\listAgentEmailIdentities.ts:67:export function findAgentEmailIdentity(
apps\agents-server\src\utils\email\agentEmailAddress.ts:28: * Creates all accepted local parts for an agent name or identifier.
apps\agents-server\src\utils\email\agentEmailAddress.test.ts:17:    it('includes the display name, agent name, and permanent id aliases', () => {
apps\agents-server\src\components\Skeleton\LOADING_GUIDELINE.md:12:-   `editor`: `/agents/[agentName]/book`
apps\agents-server\src\components\Skeleton\LOADING_GUIDELINE.md:13:-   `split-editor-chat`: `/agents/[agentName]/book+chat`
apps\agents-server\src\components\Skeleton\AgentPageLoadingSkeleton.tsx:29: * - `EDITOR`: `/agents/[agentName]/book`
apps\agents-server\src\components\Skeleton\AgentPageLoadingSkeleton.tsx:30: * - `SPLIT_EDITOR_CHAT`: `/agents/[agentName]/book+chat`
apps\agents-server\src\utils\localChatRunner\processLocalUserChatJob.ts:50:    resolveLocalAgentRootPath,
apps\agents-server\src\utils\localChatRunner\processLocalUserChatJob.ts:267:    const agentDirectoryPath = join(resolveLocalAgentRootPath(), metadata.agentDirectoryName);
apps\agents-server\src\utils\localChatRunner\processLocalUserChatJob.ts:506:    const agentDirectoryPath = join(resolveLocalAgentRootPath(), metadata.agentDirectoryName);
apps\agents-server\src\utils\localChatRunner\prepareLocalTeamConversationWorkspace.ts:156:        const basicInformation = await options.collection.findAgentBasicInformation(routeReference.agentIdentifier);
apps\agents-server\src\utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:8:import { resolveLocalAgentRootPath } from './ensureLocalAgentFolder';
apps\agents-server\src\utils\localChatRunner\persistLocalUserChatJobProgressFromRuntimeLog.ts:66:            join(resolveLocalAgentRootPath(), metadata.agentDirectoryName),
apps\agents-server\src\components\NewAgentDialog\useWizardTeamAgents.ts:47: * @returns Full name or fallback agent name.
apps\agents-server\src\components\NewAgentDialog\useNewAgentWizard.ts:30:     * Boilerplate agent name generated through the existing name pool mechanism.
apps\agents-server\src\utils\localChatRunner\ensureLocalAgentFolder.ts:59:    const directoryPath = join(resolveLocalAgentRootPath(), directoryName);
apps\agents-server\src\utils\localChatRunner\ensureLocalAgentFolder.ts:77:export function resolveLocalAgentRootPath(): string {
apps\agents-server\src\components\NewAgentDialog\useNewAgentDialog.tsx:41:     * Created agent name.
apps\agents-server\src\utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:55:    const sidecarPath = resolveLocalAgentPlannedMessagesSidecarPath(options.agentDirectoryPath, options.metadata);
apps\agents-server\src\utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:103:    await rm(resolveLocalAgentPlannedMessagesSidecarPath(agentDirectoryPath, metadata), { force: true }).catch(
apps\agents-server\src\utils\localChatRunner\applyLocalAgentPlannedMessageCommands.ts:219:function resolveLocalAgentPlannedMessagesSidecarPath(
apps\agents-server\src\components\NewAgentDialog\resolveWizardTeamReference.ts:9:     * Local agent name used for compact references.
apps\agents-server\src\utils\localAgentRouteReferences.ts:110:        localAgentUrlReferences.find((localAgentUrlReference) => localAgentUrlReference.agentUrl === normalizedAgentUrl) ||
apps\agents-server\src\components\NewAgentDialog\NewAgentWizardState.ts:82:     * Required agent name.
apps\agents-server\src\components\NewAgentDialog\NewAgentWizardState.ts:248: * Creates the initial wizard state using metadata defaults and generated agent name.
apps\agents-server\src\components\NewAgentDialog\NewAgentWizardState.ts:368: * Plain agent names are converted into compact references such as `{Legal Reviewer}`
apps\agents-server\src\components\NewAgentDialog\NewAgentWizard.tsx:37:     * Boilerplate agent name generated through the existing name pool mechanism.
apps\agents-server\src\utils\defaultAgents\resolveDefaultAgentsStatus.ts:15:     * Normalized agent name that matches the persisted `agentName` column.
apps\agents-server\src\utils\defaultAgents\resolveDefaultAgentsStatus.ts:179: * Returns the normalized names of core agents that are missing from a set of present agent names.
apps\agents-server\src\utils\defaultAgents\resolveDefaultAgentsStatus.ts:241: * @returns Map from normalized agent name to its basic information.
apps\agents-server\src\components\AgentNaming\legacyAgentTextTranslations.cs.yaml:127:"Search by agent name, URL or IP": "Hledat podle názvu agenta, URL nebo IP"
apps\agents-server\src\components\Homepage\useAgentsListOverlayDetailsState.ts:6:import { findFolderById, getAgentIdentifier } from './agentOrganizationUtils';
apps\agents-server\src\utils\defaultAgents\resolveDefaultAgentsStatus.test.ts:19:    it('returns the core agent names that are not present', () => {
apps\agents-server\src\utils\defaultAgents\loadDefaultAgentBooks.ts:95:            return (await readFile(filePath, 'utf-8')) as string_book;
apps\agents-server\src\utils\defaultAgents\coreAgentExplanations.ts:7: * human-readable rationale keyed by the normalized agent name so the Core Agents admin page can explain the purpose of
apps\agents-server\src\utils\defaultAgents\coreAgentExplanations.ts:46: * @param agentName - Normalized agent name (for example \`adam\`).
apps\agents-server\src\components\Homepage\useAgentsListImportExportState.ts:401:                Some dropped books have the same agent name as existing agents but different book source.
apps\agents-server\src\utils\customDomainRouting.ts:226:            return resolveAgent(agentNameOrPermanentId).agentSource as string_book;
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuItems.ts:67: * @param agentIdentifier - Current permanent id or routed agent name.
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuItems.ts:197: * @param agentIdentifier - Current permanent id or routed agent name.
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuItems.ts:322: * @param agentName - Current routed agent name.
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuActions.ts:124:function findAgentLink(
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuActions.ts:219:            editBookLink: findAgentLink(links, 'book'),
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuActions.ts:220:            integrationLink: findAgentLink(links, 'integration'),
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuActions.ts:232: * @param agentName - Current routed agent name.
apps\agents-server\src\components\AgentContextMenu\useAgentContextMenuActions.ts:356: * @param name - Validated new agent name.
apps\agents-server\src\components\AgentContextMenu\AgentContextMenu.tsx:47:     * Identifier used before the rename (permanent id or agent name).
apps\agents-server\src\components\Homepage\useAgentsListDragState.ts:18:import { findAgentByIdentifier, findFolderById } from './agentOrganizationUtils';
apps\agents-server\src\components\Homepage\useAgentsListDragState.ts:201:    return findAgentByIdentifier(agents, activeDragItem.identifier) || null;
apps\agents-server\src\components\Homepage\useAgentsListAgentState.ts:8:import { findAgentByIdentifier, getAgentIdentifier } from './agentOrganizationUtils';
apps\agents-server\src\components\Homepage\useAgentsListAgentState.ts:56:            const agent = findAgentByIdentifier(agents, agentIdentifier);
apps\agents-server\src\components\Homepage\useAgentsListAgentState.ts:96:            const agent = findAgentByIdentifier(agents, agentIdentifier);
apps\agents-server\src\components\NewAgentDialog\ManGoNewAgentWizard\services\createManGoAgentSource.ts:7: * Fallback name used when the imported wizard state has no explicit agent name.
apps\agents-server\src\components\Homepage\agentOrganizationUtils.ts:227:export function findAgentByIdentifier(
apps\agents-server\src\components\Homepage\agentOrganizationUtils.ts:231:    return agents.find((agent) => getAgentIdentifier(agent) === identifier);
apps\agents-server\src\components\Homepage\agentOrganizationUtils.ts:480:            const agent = findAgentByIdentifier(agents, identifier);
apps\agents-server\src\components\Homepage\agentOrganizationUtils.ts:554:    const agent = findAgentByIdentifier(agents, identifier);
apps\agents-server\src\components\NewAgentDialog\ManGoNewAgentWizard\components\WizardShell.tsx:95: * Split-screen wizard chrome: a persistent accent rail (brand identity, agent name,
apps\agents-server\src\components\NewAgentDialog\ManGoNewAgentWizard\components\steps\ZadaniStep.tsx:11: * Entry screen of the wizard ("Řekněte mi o agentovi"). Captures the agent name + a
apps\agents-server\src\components\NewAgentDialog\ManGoNewAgentWizard\components\steps\DoneStep.tsx:87:        if (state.agentName.trim().length === 0 && state.bookSource.trim().length === 0) {
apps\agents-server\src\components\NewAgentDialog\createNewAgentWizardSource.ts:47:     * Required agent name written into the book title.
apps\agents-server\src\database\migrations\2025-11-0001-initial-schema.sql:24:COMMENT ON COLUMN "prefix_Agent"."agentName" IS 'The unique name of the agent derived from the first line of the `agentSource`, automatically updated on `agentSource` change';
apps\agents-server\src\database\metadataDefaults.ts:264:    // <- TODO: [🆎] Allow to set well-known agent names via Metadata
apps\agents-server\src\database\metadataDefaults.ts:443:        note: `Language for generating new agent names. Allowed values: ${formatMetadataOptionValues(
apps\agents-server\src\database\createMissingAgentsFromBooks.ts:50: * @param options - Collection, books, and the set of already-present agent names.
apps\agents-server\src\components\Header\resolveActiveAgentNavigation.ts:59: * @returns Permanent id when available, otherwise agent name.
apps\agents-server\src\components\Header\buildAgentMenuStructure.tsx:113: * Builds an agent label that combines the avatar and agent name.
apps\agents-server\src\app\globals.css:260:/* Poppins font for agent names and headings */
apps\agents-server\src\app\api\v1\folders\[folderId]\route.ts:7:import { findOwnedFolderById, type OwnedAgentFolderRow } from '@/src/utils/agentOwnership';
apps\agents-server\src\app\api\v1\folders\[folderId]\agents\[agentId]\route.ts:4:import { findOwnedAgentByIdentifier, findOwnedFolderById } from '@/src/utils/agentOwnership';
apps\agents-server\src\app\api\v1\folders\[folderId]\agents\[agentId]\route.ts:66:        const agent = await findOwnedAgentByIdentifier(identityResult.identity.userId, parsedParams.data.agentId);
apps\agents-server\src\app\api\v1\folders\[folderId]\agents\[agentId]\route.ts:89:        const updatedAgent = await findOwnedAgentByIdentifier(
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:10:import { findOwnedAgentByIdentifier, findOwnedFolderById, type OwnedAgentRow } from '@/src/utils/agentOwnership';
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:77:        const agentRow = await findOwnedAgentByIdentifier(identityResult.identity.userId, parsedParams.data.agentId);
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:191:        const existingAgent = await findOwnedAgentByIdentifier(identityResult.identity.userId, parsedParams.data.agentId);
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:219:    const existingAgent = await findOwnedAgentByIdentifier(userId, agentId);
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:383:    const updatedAgent = await findOwnedAgentByIdentifier(userId, getOwnedAgentIdentifier(existingAgent));
apps\agents-server\src\app\api\v1\agents\[agentId]\route.ts:404: * @returns Permanent id when available, otherwise the legacy agent name.
apps\agents-server\src\app\api\v1\agents\route.ts:5:import { findOwnedFolderById, findOwnedAgentByIdentifier } from '@/src/utils/agentOwnership';
apps\agents-server\src\app\api\v1\agents\route.ts:146:        const persistedRow = await findOwnedAgentByIdentifier(identityResult.identity.userId, createdAgent.permanentId);
apps\agents-server\src\app\api\internal\agent-goal-chat-planned-messages\route.ts:179:    const agent = await collection.findAgentBasicInformation(requestedAgentIdentifier);
apps\agents-server\src\app\api\internal\agent-goal-chat-planned-messages\route.test.ts:69:            findAgentBasicInformation: FIND_AGENT_BASIC_INFORMATION_MOCK,
apps\agents-server\scripts\generate-reserved-paths\generate-reserved-paths.ts:64:     * Reserved paths that should not be treated as agent names.
apps\agents-server\src\app\api\agents\[agentName]\route.ts:10:import { findAgentForCallerWriteAccess } from '@/src/utils/findAgentForCallerWriteAccess';
apps\agents-server\src\app\api\agents\[agentName]\route.ts:20: * @returns Permanent id when available, otherwise the legacy agent name.
apps\agents-server\src\app\api\agents\[agentName]\route.ts:37:        const targetAgent = await findAgentForCallerWriteAccess(agentName);
apps\agents-server\src\app\api\agents\[agentName]\route.ts:106:        const targetAgent = await findAgentForCallerWriteAccess(agentName);
apps\agents-server\src\app\api\agents\[agentName]\restore\route.ts:11: * @param params - Route params containing the agent name or permanent id.
apps\agents-server\src\app\api\agents\import\route.ts:65:                    message: 'Some dropped books have the same agent name as existing agents but different book source.',
apps\agents-server\src\app\api\chat-history\route.ts:56: * - agentName: filter by agent name
apps\agents-server\src\app\api\chat-history\export\route.ts:12: * - agentName: filter by agent name (optional)
apps\agents-server\src\app\api\chat-feedback\route.ts:55: * - agentName: filter by agent name
apps\agents-server\src\app\api\admin\servers\route.ts:138: * Loads readable agent names for project-domain rows without making a stale domain record fail the server registry.
apps\agents-server\src\app\api\chat-feedback\export\route.ts:11: * - agentName: filter by agent name (optional)
apps\agents-server\src\app\agents\[agentName]\_utils.ts:33: * Gets agent name.
apps\agents-server\src\app\agents\[agentName]\website-integration\page.tsx:41: * @param params - Route params containing the agent name.
apps\agents-server\src\app\agents\[agentName]\useAgentChatToolInteractionMessagesChange.ts:4:import { findAgentChatActionableToolCalls } from './findAgentChatActionableToolCalls';
apps\agents-server\src\app\agents\[agentName]\useAgentChatToolInteractionMessagesChange.ts:51:                findAgentChatActionableToolCalls(messages);
apps\agents-server\src\app\agents\[agentName]\textarea\page.tsx:48:        resolveAgentAvatarImageUrl({ agent: agentProfile, baseUrl: publicUrl.href }) || fallbackAvatarPath;
apps\agents-server\src\app\agents\[agentName]\textarea\AgentTextareaClient.tsx:23:     * Human-friendly agent name rendered above the textarea.
apps\agents-server\src\app\agents\[agentName]\page.tsx:479:        resolveAgentAvatarImageUrl({ agent: agentProfile, baseUrl: publicUrl.href }) || fallbackAvatarPath;
apps\agents-server\src\app\agents\[agentName]\page.tsx:676: * @param params - Route params containing the agent name.
apps\agents-server\src\app\agents\[agentName]\images\_shared.tsx:37: * @param params - Route parameters containing the agent name.
apps\agents-server\src\app\agents\[agentName]\images\page.tsx:32:        description: 'Widescreen preview that showcases the agent name and icon for desktop-sized embeds.',
apps\agents-server\src\app\agents\[agentName]\images\default-avatar.png\getAgentDefaultAvatarPrompt.ts:28:    const heroLabel = fullname || agentName || 'Promptbook Agent';
apps\agents-server\src\app\agents\[agentName]\generateAgentMetadata.test.ts:43: * Mocked route-agent name resolver used by agent metadata tests.
apps\agents-server\src\app\agents\[agentName]\findAgentChatActionableToolCalls.ts:37:export function findAgentChatActionableToolCalls(
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:63: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:107: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:146: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:210: * @param options - Routed agent name and selected transpiler name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:272: * @param options - Routed agent name and selected transpiler name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:318: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:330: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:368: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:391: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:419: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\useAgentCodeExportState.ts:448: * @param agentName - Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\SourceBookSection.tsx:17:     * Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\SourceBookSection.tsx:41:                    href={`/agents/${encodeURIComponent(agentName)}/book`}
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\api\download\route.ts:23: * @param context - Dynamic route params with the target agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\AgentCodePageHeader.tsx:14:     * Routed agent name.
apps\agents-server\src\app\agents\[agentName]\export-as-transpiled-code\AgentCodePageClient.tsx:15:     * Routed agent name.
apps\agents-server\src\app\agents\[agentName]\chat\chatPageTitle.ts:53:    const agentSegmentIndex = findTitleSegmentIndex(titleSegments, normalizedAgentTitle);
apps\agents-server\src\app\agents\[agentName]\book\useBookEditorSaving.ts:63:    const baseUrl = `/agents/${encodeURIComponent(agentName)}/api/book`;
apps\agents-server\src\app\agents\[agentName]\book\useBookEditorHistory.ts:288:    return `/agents/${encodeURIComponent(agentName)}/api/book/history`;
apps\agents-server\src\app\agents\[agentName]\book\useBookEditorHistory.ts:459:async function fetchRestoredHistorySource(agentName: string, historyId: number): Promise<string_book> {
apps\agents-server\src\app\agents\[agentName]\book\useBookEditorDiagnostics.ts:127:                    `/agents/${encodeURIComponent(agentName)}/api/book/reference-diagnostics`,
apps\agents-server\src\app\agents\[agentName]\book\useBookEditorDiagnostics.ts:200:                const response = await fetch(`/agents/${encodeURIComponent(agentName)}/api/book/missing-agent`, {
apps\agents-server\src\app\agents\[agentName]\api\user-chats\route.ts:72:            chatSummarySeeds.find((chat) => !isAgentGoalChatId(chat.id)) || chatSummarySeeds[0] || null;
apps\agents-server\src\app\agents\[agentName]\api\openai\v1\models\route.ts:50:        // The model ID is the agent name, which clients will use when making chat completion requests
apps\agents-server\src\app\agents\[agentName]\api\openai\models\route.ts:50:        // The model ID is the agent name, which clients will use when making chat completion requests
apps\agents-server\src\app\agents\[agentName]\api\book\route.ts:77:        console.info(`[🕺] GET /agents/${agentName}/api/book?recursionLevel=${recursionLevel}`);
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:47:            throw new Error('Referenced agent name is required');
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:52:        const existingAgentIdentity = await findExistingActiveAgentIdentity(collection, candidate, normalizedCandidate);
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:70:        const deletedAgentIdentity = await findExistingDeletedAgentIdentity(collection, candidate, normalizedCandidate);
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:150: * @param candidate - Raw referenced agent name entered by the user.
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:154:async function findExistingActiveAgentIdentity(
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:160:    return findMatchingAgentIdentity(listedAgents, candidate, normalizedCandidate);
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:167: * @param candidate - Raw referenced agent name entered by the user.
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:171:async function findExistingDeletedAgentIdentity(
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:177:    return findMatchingAgentIdentity(deletedAgents, candidate, normalizedCandidate);
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:184: * @param candidate - Raw referenced agent name entered by the user.
apps\agents-server\src\app\agents\[agentName]\api\book\missing-agent\route.ts:188:function findMatchingAgentIdentity(
apps\agents-server\src\app\agents\[agentName]\api\book\history\route.ts:15: * GET `/agents/[agentName]/api/book/history`
apps\agents-server\src\app\agents\[agentName]\api\book\history\route.ts:50: * POST `/agents/[agentName]/api/book/history`
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.ts:24: * @param context - Dynamic route params with the target agent name or permanent id.
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.ts:37:            collection.findAgentBasicInformation(agentIdentifier),
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:52:describe('GET /agents/[agentName]/api/book/download', () => {
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:65:            findAgentBasicInformation: jest.fn(async () => ({ agentName: 'Školník' })),
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:81:        expect(collection.findAgentBasicInformation).toHaveBeenCalledWith('agent-123');
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:92:            findAgentBasicInformation: jest.fn(async () => ({ agentName: 'Encoded Agent' })),
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:104:        expect(collection.findAgentBasicInformation).toHaveBeenCalledWith('Encoded Agent');
apps\agents-server\src\app\agents\[agentName]\api\book\download\route.test.ts:125:            findAgentBasicInformation: jest.fn(async () => null),
apps\agents-server\src\app\actions.ts:137:    const { agentName, permanentId } = await createAgentWithDefaultVisibility(collection, bookContent, {
apps\agents-server\src\app\admin\chat-history\ChatHistoryFiltersCard.tsx:73:                            placeholder={formatText('Search by agent name, URL or IP')}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-agent-messages/messages/createAgentRunnerSystemMessage.ts; Get-Content src/book-2.0/agent-source/parseAgentSource.ts | Select-Object -First 230; Get-Content src/book-2.0/agent-source/parseAgentSourcePrelude.ts; Get-Content src/cli/cli-commands/agent/agentCliOptions.ts; Get-Content src/cli/cli-commands/agent-folder/agentProjectPaths.ts | Select-Object -First 150' in C:\Users\me\work\ai\promptbook
 succeeded in 531ms:
import { spaceTrim } from 'spacetrim';
import { createAgentModelRequirements } from '../../../src/book-2.0/agent-source/createAgentModelRequirements';
import type { AgentModelRequirements } from '../../../src/book-2.0/agent-source/AgentModelRequirements';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';

/**
 * Compiles agent source into the behavior text passed to CLI harness runners.
 */
export async function createAgentRunnerSystemMessage(agentSource: string_book): Promise<string> {
    return formatAgentModelRequirementsForRunner(await createAgentModelRequirements(agentSource));
}

/**
 * Formats prepared model requirements for a text-only CLI harness prompt.
 */
export function formatAgentModelRequirementsForRunner(modelRequirements: AgentModelRequirements): string {
    const promptSuffix = modelRequirements.promptSuffix.trim();

    return [
        modelRequirements.systemMessage.trim(),
        promptSuffix
            ? spaceTrim(
                  (block) => `
                      ## Prompt suffix
                      ${block(promptSuffix)}
                  `,
              )
            : '',
    ]
        .filter(Boolean)
        .join('\n\n');
}
import type { AgentBasicInformation } from './AgentBasicInformation';
import { computeAgentHash } from './computeAgentHash';
import { createDefaultAgentName } from './createDefaultAgentName';
import { normalizeAgentName } from './normalizeAgentName';
import { parseAgentSourceWithCommitments } from './parseAgentSourceWithCommitments';
import { parseParameters } from './parseParameters';
import { ensureMetaFullname } from './parseAgentSource/ensureMetaFullname';
import { extractAgentProfileText } from './parseAgentSource/extractAgentProfileText';
import { extractInitialMessage } from './parseAgentSource/extractInitialMessage';
import { extractParsedAgentProfile } from './parseAgentSource/extractParsedAgentProfile';
import type { string_book } from './string_book';

/**
 * Parses basic information from agent source
 *
 * There are 2 similar functions:
 * - `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
 * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.
 *
 * @public exported from `@promptbook/core`
 */
export function parseAgentSource(agentSource: string_book): AgentBasicInformation {
    const parseResult = parseAgentSourceWithCommitments(agentSource);
    const resolvedAgentName = parseResult.agentName || createDefaultAgentName(agentSource);
    const personaDescription = extractAgentProfileText(parseResult.commitments);
    const initialMessage = extractInitialMessage(parseResult.commitments);
    const parsedProfile = extractParsedAgentProfile(parseResult.commitments);

    ensureMetaFullname(parsedProfile.meta, resolvedAgentName);

    return {
        agentName: normalizeAgentName(resolvedAgentName),
        agentHash: computeAgentHash(agentSource),
        permanentId: parsedProfile.meta.id,
        personaDescription,
        initialMessage,
        thinkingMessages: parsedProfile.thinkingMessages,
        meta: parsedProfile.meta,
        links: parsedProfile.links,
        parameters: parseParameters(agentSource),
        capabilities: parsedProfile.capabilities,
        samples: parsedProfile.samples,
        knowledgeSources: parsedProfile.knowledgeSources,
    };
}

// TODO: [🕛] Unite `AgentBasicInformation`, `ChatParticipant`, `LlmExecutionTools` +  `LlmToolsMetadata`
import type { string_agent_name } from '../../types/string_agent_name';
import type { string_book } from './string_book';

/**
 * Parsed prelude of one agent source.
 *
 * @private internal utility of book agent-source parsing
 */
type AgentSourcePrelude = {
    /**
     * Source split into logical lines.
     */
    readonly lines: string[];

    /**
     * First non-empty line consumed as the agent name.
     */
    readonly agentName: string_agent_name | null;

    /**
     * Zero-based index of the agent-name line.
     */
    readonly agentNameLineIndex: number;

    /**
     * One-based line number of the agent-name line.
     */
    readonly agentNameLineNumber?: number;
};

/**
 * Consumes the agent-name prelude from raw source.
 *
 * Leading whitespace-only lines are skipped. The first subsequent line that contains any
 * non-whitespace characters is always treated as plain-text agent name, even when it starts
 * with a commitment keyword or other book syntax.
 *
 * @param agentSource - Raw agent source.
 * @returns Parsed prelude with shared line metadata.
 *
 * @private internal utility of book agent-source parsing
 */
export function parseAgentSourcePrelude(agentSource: string_book): AgentSourcePrelude {
    const lines = agentSource.split(/\r?\n/);

    for (let lineIndex = 0; lineIndex < lines.length; lineIndex++) {
        const line = lines[lineIndex];
        if (line === undefined) {
            continue;
        }

        const trimmedLine = line.trim();
        if (!trimmedLine) {
            continue;
        }

        return {
            lines,
            agentName: trimmedLine as string_agent_name,
            agentNameLineIndex: lineIndex,
            agentNameLineNumber: lineIndex + 1,
        };
    }

    return {
        lines,
        agentName: null,
        agentNameLineIndex: -1,
    };
}
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { NotAllowed } from '../../../errors/NotAllowed';
import type {
    NormalizedPromptRunnerSelectionCliOptions,
    PromptRunnerSelectionCliOptions,
} from '../common/promptRunnerCliOptions';
import { normalizePromptRunnerSelectionCliOptions } from '../common/promptRunnerCliOptions';

/**
 * Commander option bag shared by `ptbk agent` subcommands.
 *
 * @private internal utility of `ptbk agent`
 */
export type AgentCommandCliOptions = PromptRunnerSelectionCliOptions & {
    readonly agent?: string;
    readonly context?: string;
    readonly message?: string;
};

/**
 * Normalized shared options used by local `ptbk agent` subcommands.
 *
 * @private internal utility of `ptbk agent`
 */
export type NormalizedAgentCommandRunnerOptions = NormalizedPromptRunnerSelectionCliOptions & {
    readonly isVerbose: boolean;
};

/**
 * Normalizes shared runner flags for local agent subcommands.
 *
 * @private internal utility of `ptbk agent`
 */
export function normalizeAgentCommandRunnerOptions(
    cliOptions: AgentCommandCliOptions,
    command: Program,
): NormalizedAgentCommandRunnerOptions {
    return {
        ...normalizePromptRunnerSelectionCliOptions(cliOptions, {
            isAgentRequired: true,
        }),
        isVerbose: resolveCommandVerboseOption(command),
    };
}

// Note: [🟡] Code for CLI command options [agent](src/cli/cli-commands/agent/agentCliOptions.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names

/**
 * Returns the required agent book path from Commander options.
 *
 * @private internal utility of `ptbk agent`
 */
export function resolveRequiredAgentPath(cliOptions: AgentCommandCliOptions): string {
    const agentPath = cliOptions.agent?.trim();

    if (!agentPath) {
        throw new NotAllowed('Pass an agent book path in `--agent`.');
    }

    return agentPath;
}

/**
 * Returns the required single-turn user message from Commander options.
 *
 * @private internal utility of `ptbk agent`
 */
export function resolveRequiredAgentMessage(cliOptions: AgentCommandCliOptions): string {
    const message = cliOptions.message?.trim();

    if (!message) {
        throw new NotAllowed('Pass a non-empty user message in `--message`.');
    }

    return message;
}

/**
 * Resolves the inherited `--verbose` flag from the current Commander command chain.
 *
 * @private internal utility of `ptbk agent`
 */
function resolveCommandVerboseOption(command: Program): boolean {
    const globalOptions = command.optsWithGlobals() as {
        readonly verbose?: boolean;
    };

    return globalOptions.verbose ?? false;
}
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import {
    AGENT_BOOK_FILE_PATH,
    AGENT_FAILED_MESSAGES_DIRECTORY_PATH,
    AGENT_FINISHED_MESSAGES_DIRECTORY_PATH,
    AGENT_MESSAGES_DIRECTORY_PATH,
    AGENT_PROJECTS_DIRECTORY_PATH,
    AGENT_QUEUED_MESSAGES_DIRECTORY_PATH,
} from '../../../book-3.0/agentFolderPaths';

export {
    AGENT_BOOK_FILE_PATH,
    AGENT_FAILED_MESSAGES_DIRECTORY_PATH,
    AGENT_FINISHED_MESSAGES_DIRECTORY_PATH,
    AGENT_MESSAGES_DIRECTORY_PATH,
    AGENT_PROJECTS_DIRECTORY_PATH,
    AGENT_QUEUED_MESSAGES_DIRECTORY_PATH,
};

/**
 * Relative path to local knowledge files initialized by `ptbk agent-folder init`.
 *
 * @private internal utility of `ptbk agent-folder`
 */
export const AGENT_KNOWLEDGE_DIRECTORY_PATH = 'knowledge';

/**
 * Relative path to generated local agent documentation initialized by `ptbk agent-folder init`.
 *
 * @private internal utility of `ptbk agent-folder`
 */
export const AGENT_DOCS_DIRECTORY_PATH = 'docs';

/**
 * Relative path to the local Book language manual initialized by `ptbk agent-folder init`.
 *
 * @private internal utility of `ptbk agent-folder`
 */
export const AGENT_BOOK_LANGUAGE_MANUAL_FILE_PATH = join(AGENT_DOCS_DIRECTORY_PATH, 'book-language-manual.md');

/**
 * Returns the default local agent book content.
 *
 * @private internal utility of `ptbk agent-folder`
 */
export function getDefaultAgentBookContent(): string {
    return spaceTrim(`
        Local Agent

        GOAL Answer user questions from queued message files using the repository knowledge when useful.
        RULE Add the answer to the same message file under a new \`MESSAGE @Agent\` block.
        RULE Keep the answer focused on the most recent \`MESSAGE @User\` block.
        RULE Use files in the \`knowledge\` folder as the primary local knowledge source when they are relevant.
        RULE If the available information is not enough, say what is missing instead of inventing facts.
        CLOSED
    `);
}

/**
 * Returns the default local Book language manual content.
 *
 * @private internal utility of `ptbk agent-folder`
 */
export function getDefaultBookLanguageManualContent(): string {
    return spaceTrim(`
        # Book language manual

        Book language defines a local AI agent as plain text. The first non-empty line is the agent name. Each following commitment starts with an uppercase keyword and continues until the next commitment.

        Common commitments:

        - \`GOAL\` describes what the agent should achieve.
        - \`RULE\` adds one behavioral constraint.
        - \`KNOWLEDGE\` adds inline facts or points to knowledge the agent should use.
        - \`LANGUAGE\` sets the expected response language.
        - \`TEAM\` references other agents that can be consulted.
        - \`CLOSED\` asks the agent to keep behavior stable instead of self-modifying.

        Example:

        \`\`\`book
        Support Agent

        GOAL Answer customer support questions.
        RULE Be concise and factual.
        KNOWLEDGE Product documentation is stored in the \`knowledge\` folder.
        CLOSED
        \`\`\`

        For \`ptbk agent-folder\`, the CLI compiles \`agent.book\` into the coding-runner instructions, and the runner edits only the queued message file by appending an answer that starts with \`MESSAGE @Agent\`.
    `);
}

// Note: [🟡] Code for CLI command [agent](src/cli/cli-commands/agent-folder/agentProjectPaths.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content .vscode/terminals.json | Select-Object -First 190; Get-Content apps/coder-landing/src/components/sections/AgentsSection.tsx; Get-Content apps/coder-landing/src/data/commands.ts; Get-Content apps/coder-landing/specs/sections/agent-book.md; Get-Content apps/coder-landing/specs/content/commands.md | Select-Object -First 100' in C:\Users\me\work\ai\promptbook
 succeeded in 456ms:
{
    "autokill": true,
    "terminals": [
        {
            "--note-0": "There are 3 types of terminals: 1) Runned automatically when coding (execute=true,onlySingle=false,focus=false) 2) Prepared automatically to be run by user but not executed by default (execute=false,onlySingle=false,focus=false), theese are marked by (💤) 3) Manually run by user (execute=true,onlySingle=true,focus=true)",
            "--note-1": "The (💸) means that script has some cost associated with it, for example, using a paid API or service.",
            "--note-2": "The (💤) means that script is not executed by default but need user to press [Enter] to run it",
            "--note-3": "The (⏳) means that script is expected to run for a longer time than usual",
            "--note-4": "The (❗) means that script should be run with caution, for example, it may cause breaking changes, data loss, etc.",
            "--note-5": "The (❗❗) means that script should be run with extreme caution, messing up with LTS versions, etc."
        },

        {
            "name": "🐙🏓 Ping Codex & Claude",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness claude-code --model claude-sonnet-5 --thinking-level low",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level low"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-sol` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-terra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-luna` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-fable` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-max` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-pro` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `glm-5.2` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Generic\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 10 --template prompts/_templates/common.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"Agents server\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/agents-server.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Utils app\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/utils-app.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk coder\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-coder.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"ptbk agent-folder\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agent.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk agents-server\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agents-server.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
import { AGENT_RUN_COMMAND } from '@/data/commands';
import { DeveloperAgentBookPreview } from '@/components/DeveloperAgentBookPreview/DeveloperAgentBookPreview';
import { TerminalBlock } from '@/components/TerminalBlock/TerminalBlock';

/**
 * Renders the agents section - the `--agent agents/developer.book` option together with
 * a readonly `<BookEditor/>` showing the default developer agent.
 *
 * Note: Specified in [`specs/sections/agent-book.md`](../../../specs/sections/agent-book.md)
 */
export function AgentsSection() {
    return (
        <section id="agents" className="mx-auto max-w-6xl scroll-mt-20 px-4 py-20">
            <h2 className="font-display text-3xl font-bold text-white md:text-4xl">
                Give your agent a <span className="text-promptbook-green">soul</span>, in plain text
            </h2>
            <p className="mt-4 max-w-3xl text-lg text-gray-300">
                Every run can carry an agent written in the{' '}
                <a
                    href="https://github.com/webgptorg/book"
                    className="text-promptbook-blue underline decoration-promptbook-blue-dark underline-offset-4 hover:text-promptbook-green"
                >
                    Book language
                </a>
                , Promptbook&apos;s human-readable language for defining AI agents. Its rules, its persona and what it
                knows live in one <code className="text-promptbook-blue">.book</code> file, and ptbk coder compiles that
                file into the system message of every coding prompt.
            </p>

            <div className="mt-12 grid gap-8 lg:grid-cols-2 lg:items-start">
                <div>
                    <p className="mb-4 text-gray-400">
                        Point <strong className="text-white">ptbk coder</strong> at any agent file with{' '}
                        <code className="text-promptbook-blue">--agent</code>:
                    </p>
                    <TerminalBlock command={AGENT_RUN_COMMAND} />
                    <p className="mt-6 text-gray-400">
                        <code className="text-promptbook-blue">ptbk coder init</code> creates this default developer
                        agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                        other file in your repository to change how your agent codes.
                    </p>
                </div>

                <div>
                    <p className="mb-4 font-mono text-sm text-gray-500">readonly preview of agents/developer.book</p>
                    <DeveloperAgentBookPreview />
                </div>
            </div>
        </section>
    );
}
/**
 * Canonical shell commands shown on the landing page.
 *
 * Note: This is the single source of truth for every terminal sample on the page,
 *       specified in [`specs/content/commands.md`](../../specs/content/commands.md)
 */

/**
 * Command which installs Promptbook (and with it `ptbk coder`) into a project.
 */
export const INSTALL_COMMAND = 'npm install ptbk';

/**
 * Command which installs Promptbook globally as an alternative to the local install.
 */
export const INSTALL_GLOBAL_COMMAND = 'npm install --global ptbk';

/**
 * Command which initializes the `ptbk coder` configuration in the current project.
 */
export const INIT_COMMAND = 'ptbk coder init';

/**
 * Command which initializes the `ptbk coder` configuration and synchronizes the result with the remote repository.
 */
export const INIT_GIT_SYNC_COMMAND = 'ptbk coder init --auto-pull --commit --auto-push';

/**
 * Command which appends one ready-to-run prompt file to the queue from a plain-language description.
 */
export const ADD_COMMAND = 'ptbk coder add "Add a dark mode toggle to the settings page"';

/**
 * Minimal command which runs the prompt queue through Claude Code.
 */
export const RUN_COMMAND = 'ptbk coder run --harness claude-code';

/**
 * Command which lists ready prompts in priority order without starting a coding harness.
 */
export const LIST_COMMAND = 'ptbk coder list';

/**
 * Command which generates prompt boilerplate files with more than one prompt in each of them.
 */
export const GENERATE_BOILERPLATES_COMMAND = 'ptbk coder generate-boilerplates --count 10*7';

/**
 * Command which runs prompts through a selected harness and model.
 */
export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';

/**
 * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
 */
export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';

/**
 * Command which resumes the prompt a killed or crashed coder left in the middle of its implementation.
 */
export const GIT_CHANGES_CONTINUE_COMMAND = 'ptbk coder run --harness claude-code --git-changes continue';

/**
 * Command which previews the prompt queue without executing anything.
 */
export const DRY_RUN_COMMAND = 'ptbk coder run --dry-run';

/**
 * Command which checks the connection, response time and quota of one harness and model.
 */
export const PING_COMMAND = 'ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh';

/**
 * Command which keeps pinging one harness and model every 5 hours to keep its quota window refreshing.
 */
export const PING_PERIOD_COMMAND =
    'ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h';

/**
 * Full-featured command which starts the coder server with kanban UI, agent persona,
 * project context and post-prompt test verification.
 */
export const SERVER_COMMAND =
    'ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test';

/**
 * Full-featured command shown in the hero live terminal sample.
 */
export const LIVE_DEMO_RUN_COMMAND =
    'ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1';

/**
 * Command which runs the queue with the developer agent persona from a `.book` file.
 */
export const AGENT_RUN_COMMAND =
    'ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md';

/**
 * Command which interactively verifies completed prompts, newest first, and archives them to `prompts/done/`.
 */
export const VERIFY_COMMAND = 'ptbk coder verify --order from-latest';
# Section: Agent personas (Book)

Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.

## Copy

-   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.

## Layout

Two columns on desktop (stacked on mobile):

### Left — the option

1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.

### Right — the agent itself

1. Caption above (small monospace, gray): `readonly preview of agents/developer.book`
2. The [readonly Book editor embed](../components/book-editor-embed.md) showing the verbatim source from [`../content/developer-agent.md`](../content/developer-agent.md).
# Content: canonical commands

The single source of truth for every shell command shown on the page. Any section rendering a command **must** use one of these values verbatim — never a re-typed variant.

| Name                            | Command                                                                                                                                                                                                | Shown in                                                                    |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |
| `INSTALL_COMMAND`               | `npm install ptbk`                                                                                                                                                                                     | [Hero](../sections/hero.md), [Quickstart step 1](../sections/quickstart.md) |
| `INSTALL_GLOBAL_COMMAND`        | `npm install --global ptbk`                                                                                                                                                                            | (available alternative; may be mentioned in prose)                          |
| `INIT_COMMAND`                  | `ptbk coder init`                                                                                                                                                                                      | [Quickstart step 2](../sections/quickstart.md)                              |
| `INIT_GIT_SYNC_COMMAND`         | `ptbk coder init --auto-pull --commit --auto-push`                                                                                                                                                     | [Advanced features](../sections/advanced-features.md)                       |
| `ADD_COMMAND`                   | `ptbk coder add "Add a dark mode toggle to the settings page"`                                                                                                                                         | [Quickstart step 3](../sections/quickstart.md)                              |
| `RUN_COMMAND`                   | `ptbk coder run --harness claude-code`                                                                                                                                                                 | [Quickstart step 4](../sections/quickstart.md)                              |
| `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
| `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
| `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
| `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
| `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
| `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
| `PING_COMMAND`                  | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`                                                                                                                    | [Advanced features](../sections/advanced-features.md)                       |
| `PING_PERIOD_COMMAND`           | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h`                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
| `SERVER_COMMAND`                | `ptbk coder server --harness claude-code --model fable --thinking-level max --agent agents/developer.book --context AGENTS.md --test npm run test`                                                     | [Quickstart step 5](../sections/quickstart.md)                              |
| `LIVE_DEMO_RUN_COMMAND`         | `ptbk coder run --harness claude-code --model fable --thinking-level xhigh --agent agents/developer.book --context AGENTS.md --test "npm run test-for-ptbk-coder" --wait-between-prompts 4h --limit 1` | Typed in the [live terminal](../components/live-terminal.md)                |
| `AGENT_RUN_COMMAND`             | `ptbk coder run --harness claude-code --model fable --agent agents/developer.book --context AGENTS.md`                                                                                                 | [Agent personas](../sections/agent-book.md)                                 |
| `VERIFY_COMMAND`                | `ptbk coder verify --order from-latest`                                                                                                                                                                | [Advanced features](../sections/advanced-features.md)                       |

Per-harness sample commands are defined in [`harness-catalog.md`](./harness-catalog.md); per-feature snippets in [`../sections/advanced-features.md`](../sections/advanced-features.md).

## Option reference (context for copywriting)

These `ptbk coder` options may be referenced in page prose and must be described accurately:

-   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
-   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
-   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
-   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
-   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
-   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
-   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
-   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
-   `--dry-run` — print unwritten prompts without executing.
-   `ptbk coder ping` — send one tiny dummy prompt to the harness and model selected with `--harness`, `--model` and `--thinking-level` and report the answer, the response time and the reported usage. It runs no coding task or commit; when the selected harness has missing local ignore rules, it asks before adding them to `.gitignore`. `--allow-credits` and `--no-ui` behave as for `run`, where `--no-ui` streams the raw harness output instead of the compact result.
-   `--period <duration>` — `ping` only; instead of pinging once, keep pinging once per period until the process is stopped with `CTRL+C`. The duration uses the same format as the `--wait-*` options (`5h`, `30m`, `1h30m`) and must be positive. The period is measured from the start of one ping to the start of the next one, so `--period 5h` really pings every 5 hours regardless of how long a ping takes, and a failed ping is reported without ending the loop. Its main use is keeping the 5-hour quota window of Claude Code permanently open for the price of a few tokens per ping.
-   `--min-priority <n>`, `--max-priority <n>` — for `run`, process only prompts within the inclusive priority range; for `list`, show only prompts within that range; `--priority <n>` remains an alias for `--min-priority`; for `add`, `--priority <n>` sets the priority of the new prompt (rendered as trailing `!` markers).
-   `--limit <n>` — stop after N prompt runs (`run` only).
-   `--count <n>` / `--count <n>*<m>` — `generate-boilerplates` only; number of generated prompt files, optionally with the number of prompts written into each of them. `--count N` is the same as `--count N*1`, the default is `5*1`, and more than one prompt per file is an advanced option: each of the `M` prompts is a separate task, separated by a `---` line, and shares the file's fresh emoji tag.
-   `--no-auto` — wait for user confirmation before each prompt. Cannot be combined with `--no-questions`, which forbids the very confirmation it waits for.
-   `--no-questions` — never ask an interactive question, taken by `init`, `add`, `run`, `ping` and `server`. Every optional step which needs an answer is skipped and reported together with the command which does it manually: installing a missing coding harness, updating an outdated harness, updating an outdated Promptbook CLI and adding the missing harness ignore rules to `.gitignore`. Because an outdated harness or CLI can only be updated after a confirmation, the npm latest-version lookup is skipped as well, and `ptbk coder add` refuses a missing description instead of asking for it.
-   `--no-commit`, `--git-changes <mode>`, `--auto-push`, `--auto-pull` — git behavior switches of `run` and `server`, which commit every successful round by default.
-   `--git-changes <fail|ignore|continue>` — what happens when the working tree has uncommitted changes before a prompt starts. `fail` (the default) refuses to start and asks for a commit, a stash or one of the other two modes. `ignore` starts anyway and leaves the changes where they are. `continue` resumes the prompt an interrupted coder left behind in the `[^]` status, keeping its half-finished changes in place; it expects **exactly one** `[^]` prompt and fails when it finds none or more than one. Only that first round runs on the dirty tree, every later round expects a clean one again, and `continue` cannot be combined with `--isolate`, whose fresh worktree would leave the uncommitted changes behind.
-   Prompt status attribution — a prompt one harness started and another one continued records them in chronological order, as `` by OpenAI Codex `gpt-5.6-luna`, interrupted, continued by Claude Code `claude-opus-5` ``, in its `[^]`, `[x]` and `[!]` status line alike. Every later continuation appends another `interrupted, continued by …` report instead of dropping an earlier harness.
-   `--order <from-earliest|from-latest|random>` — `verify` only; order in which the prompt files are walked through. `from-earliest` (the default) starts with the oldest prompt file, `from-latest` starts with the newest one, and `random` shuffles the queue for every pass.
-   `--commit`, `--auto-push`, `--auto-pull` — git synchronization of `init`, `add`, `generate-boilerplates` and `verify`, which never touch git unless these flags are used. `--auto-pull` pulls before the command changes anything, `--commit` commits the resulting changes with the coding-agent git identity, and `--auto-push` (which requires `--commit`) pushes the created commit. For `verify` the synchronization is applied around each single verification instead of once per run.
-   Every commit `ptbk coder` creates contains **only the files of the operation which created it** — the added prompt file for `add`, the archived or repaired prompt file for `verify`, the prompt file together with the files the coding agent has changed for `run` and `server`. Changes which were already in the working tree before the command started are left there, so an unrelated work in progress is never swept into a coder commit.
-   `--isolate` — `run` only; implement each prompt in its own temporary git worktree (`.promptbook/coder-isolation-worktrees/<task-name>` on branch `ptbk-coder-isolation/<task-name>`) with its own copy of `.env`. A verified task is merged back into the current branch as one commit and the worktree is deleted; a task that cannot be merged is marked `[!]`, the failure is committed, and its worktree is kept for a manual merge while the coder continues. Because the worktree nests the whole repository one level deeper, git long path support is enabled for the isolated run so that Windows `MAX_PATH` does not break the checkout. Cannot be combined with `--no-commit`.
-   `--wait-after-prompt`, `--wait-between-prompts`, `--wait-after-error <duration>` — wall-clock pacing; durations like `1h`, `30m`, `5s`; pause and computer sleep count against the wait; `S` skips whatever the coder waits for while `S Skip current waiting` is offered, which besides these three waits covers the harness session-limit wait and the `server` keep-alive poll; errors retry up to 3 times (default wait `10m`).
-   `--no-ui`, `--preserve-logs` — plain streaming output / keep temp artifacts.
-   `--port <port>` — `server` only; kanban UI port, default `4441`.

Environment variables that may be mentioned: `CODING_AGENT_GIT_NAME`, `CODING_AGENT_GIT_EMAIL`, `CODING_AGENT_GIT_SIGNING_KEY` (agent git identity, created by `ptbk coder init` in `.env`), and `PTBK_HARNESS` / `PTBK_MODEL` / `PTBK_THINKING_LEVEL` as defaults for the corresponding options.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/cli/parseRunOptions.test.ts | Select-Object -First 520; rg -n --hidden --glob '"'!node_modules'"' \"mkdtemp|temporary.*Directory|tmpdir\\(|makeTemp|temporaryProject\" scripts/run-codex-prompts src/cli | Select-Object -First 300; Get-Content package.json | Select-Object -First 290" in C:\Users\me\work\ai\promptbook
 succeeded in 922ms:
import { parseRunOptions } from './parseRunOptions';

/**
 * Creates a process.exit mock that throws, so tests can assert exit flows.
 */
function mockProcessExit(): jest.SpyInstance<never, [code?: string | number | null | undefined]> {
    return jest.spyOn(process, 'exit').mockImplementation((() => {
        throw new Error('process.exit');
    }) as never);
}

describe('parseRunOptions', () => {
    let processExitSpy: jest.SpyInstance<never, [code?: string | number | null | undefined]>;
    let consoleErrorSpy: jest.SpyInstance<void, [message?: unknown, ...optionalParams: unknown[]]>;

    beforeEach(() => {
        processExitSpy = mockProcessExit();
        consoleErrorSpy = jest.spyOn(console, 'error').mockImplementation(() => undefined);
    });

    afterEach(() => {
        processExitSpy.mockRestore();
        consoleErrorSpy.mockRestore();
    });

    it('defaults priority to zero when the flag is not provided', () => {
        const options = parseRunOptions(['--harness', 'gemini']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'gemini',
            priority: 0,
            noCommit: false,
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: true,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
            testBefore: 'no',
        });
    });

    it('parses the pre-coding verification mode', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--test-before', 'yes-and-fix']);

        expect(options).toMatchObject({
            testBefore: 'yes-and-fix',
        });
    });

    it('rejects an invalid pre-coding verification mode', () => {
        expect(() => parseRunOptions(['--harness', 'github-copilot', '--test-before', 'sometimes'])).toThrow(
            'process.exit',
        );
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects a missing pre-coding verification mode', () => {
        expect(() => parseRunOptions(['--harness', 'github-copilot', '--test-before'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('fails on a dirty working tree unless --git-changes says otherwise', () => {
        const options = parseRunOptions(['--harness', 'gemini']);

        expect(options).toMatchObject({ gitChanges: 'fail' });
    });

    it('parses the requested behavior for a dirty working tree', () => {
        const options = parseRunOptions(['--harness', 'claude-code', '--git-changes', 'continue']);

        expect(options).toMatchObject({ gitChanges: 'continue' });
    });

    it('rejects an invalid behavior for a dirty working tree', () => {
        expect(() => parseRunOptions(['--harness', 'claude-code', '--git-changes', 'whatever'])).toThrow(
            'process.exit',
        );
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects a missing behavior for a dirty working tree', () => {
        expect(() => parseRunOptions(['--harness', 'claude-code', '--git-changes'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('parses GitHub Copilot as a supported runner', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--model', 'gpt-5.4']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            model: 'gpt-5.4',
            priority: 0,
            noCommit: false,
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: true,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('allows running without --harness in dry-run mode', () => {
        const options = parseRunOptions(['--dry-run']);

        expect(options).toMatchObject({
            dryRun: true,
            agentName: undefined,
            priority: 0,
            noCommit: false,
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: true,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('parses priority and keeps other flags intact', () => {
        const options = parseRunOptions([
            '--harness',
            'openai-codex',
            '--model',
            'gpt-5.2-codex',
            '--context',
            'AGENTS.md',
            '--priority',
            '3',
            '--git-changes',
            'ignore',
        ]);

        expect(options).toMatchObject({
            dryRun: false,
            waitForUser: false,
            gitChanges: 'ignore',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: true,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
            agentName: 'openai-codex',
            model: 'gpt-5.2-codex',
            context: 'AGENTS.md',
            priority: 3,
            minimumPriority: 3,
            priorityFilter: { minimumPriority: 3 },
        });
    });

    it('parses min and max priority filters', () => {
        const options = parseRunOptions([
            '--harness',
            'github-copilot',
            '--min-priority',
            '1',
            '--max-priority',
            '5',
        ]);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            priority: 1,
            minimumPriority: 1,
            maximumPriority: 5,
            priorityFilter: { minimumPriority: 1, maximumPriority: 5 },
        });
    });

    it('parses a max priority filter without a minimum priority filter', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--max-priority', '5']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            priority: 0,
            minimumPriority: undefined,
            maximumPriority: 5,
            priorityFilter: { maximumPriority: 5 },
        });
    });

    it('parses run limit when provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--limit', '2']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            limit: 2,
        });
    });

    it('leaves changes uncommitted only when --no-commit is provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--no-commit']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            noCommit: true,
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
        });
    });

    it('parses inline context instructions', () => {
        const options = parseRunOptions(['--harness', 'gemini', '--context', 'Follow AGENTS instructions']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'gemini',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            context: 'Follow AGENTS instructions',
            normalizeLineEndings: true,
        });
    });

    it('parses an unquoted verification command and stops at the next top-level flag', () => {
        const options = parseRunOptions([
            '--harness',
            'github-copilot',
            '--test',
            'npm',
            'run',
            'test',
            '--no-auto',
        ]);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            testCommand: 'npm run test',
            waitForUser: true,
        });
    });

    it('parses thinking level for supported runners', () => {
        const options = parseRunOptions(['--harness', 'claude-code', '--thinking-level', 'max']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'claude-code',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            thinkingLevel: 'max',
            normalizeLineEndings: true,
        });
    });

    it('parses --dry-run with other flags', () => {
        const options = parseRunOptions(['--dry-run', '--priority', '2', '--no-auto']);

        expect(options).toMatchObject({
            dryRun: true,
            waitForUser: true,
            priority: 2,
            minimumPriority: 2,
            noCommit: false,
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: true,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('enables automatic git push only when --auto-push is provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--auto-push']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            autoPush: true,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
        });
    });

    it('enables automatic git pull only when --auto-pull is provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--auto-pull']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            autoPush: false,
            autoPull: true,
            preserveLogs: false,
            noUi: false,
        });
    });

    it('preserves temp prompt artifacts only when --preserve-logs is provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--preserve-logs']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            autoPush: false,
            autoPull: false,
            preserveLogs: true,
            noUi: false,
        });
    });

    it('disables the terminal UI only when --no-ui is provided', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--no-ui']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'github-copilot',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: true,
        });
    });

    it('allows disabling automatic line-ending normalization', () => {
        const options = parseRunOptions(['--harness', 'gemini', '--no-normalize-line-endings']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'gemini',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            normalizeLineEndings: false,
            allowCredits: false,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('enables credit spending when --allow-credits is provided', () => {
        const options = parseRunOptions(['--harness', 'openai-codex', '--allow-credits']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'openai-codex',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            allowCredits: true,
            autoMigrate: false,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('enables automatic testing-server migrations when --auto-migrate is provided', () => {
        const options = parseRunOptions(['--harness', 'openai-codex', '--auto-migrate']);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'openai-codex',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            autoMigrate: true,
            allowDestructiveAutoMigrate: false,
        });
    });

    it('allows destructive migration override when explicitly requested', () => {
        const options = parseRunOptions([
            '--harness',
            'openai-codex',
            '--auto-migrate',
            '--allow-destructive-auto-migrate',
        ]);

        expect(options).toMatchObject({
            dryRun: false,
            agentName: 'openai-codex',
            autoPush: false,
            autoPull: false,
            preserveLogs: false,
            noUi: false,
            autoMigrate: true,
            allowDestructiveAutoMigrate: true,
        });
    });

    it('defaults to no per-prompt waiting and 10 minutes wait-after-error when no wait flags are provided', () => {
        const options = parseRunOptions(['--harness', 'gemini']);

        expect(options).toMatchObject({
            waitForUser: false,
            waitAfterPrompt: 0,
            waitBetweenPrompts: 0,
            waitAfterError: 600_000,
        });
    });

    it('enables user-confirmation wait when --no-auto is provided', () => {
        const options = parseRunOptions(['--harness', 'gemini', '--no-auto']);

        expect(options).toMatchObject({
            waitForUser: true,
            waitAfterPrompt: 0,
            waitBetweenPrompts: 0,
            waitAfterError: 600_000,
        });
    });

    it('rejects --wait-after-prompt when no duration value is provided', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--wait-after-prompt'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects --wait-between-prompts when no duration value is provided', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--wait-between-prompts'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects --wait-after-error when no duration value is provided', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--wait-after-error'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('sets time-based wait when --wait-after-prompt is provided with a duration in hours', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--wait-after-prompt', '1h']);

        expect(options).toMatchObject({
            waitForUser: false,
            waitAfterPrompt: 3_600_000,
            waitBetweenPrompts: 0,
            waitAfterError: 600_000,
        });
    });

    it('sets time-based wait when --wait-between-prompts is provided with a duration in minutes', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--wait-between-prompts', '30m']);

        expect(options).toMatchObject({
            waitForUser: false,
            waitAfterPrompt: 0,
            waitBetweenPrompts: 1_800_000,
            waitAfterError: 600_000,
        });
    });

    it('sets time-based wait when --wait-after-error is provided with a combined duration', () => {
        const options = parseRunOptions(['--harness', 'github-copilot', '--wait-after-error', '1h30m']);

        expect(options).toMatchObject({
            waitForUser: false,
            waitAfterPrompt: 0,
            waitBetweenPrompts: 0,
            waitAfterError: 5_400_000,
        });
    });

    it('combines --no-auto with time-based wait flags', () => {
        const options = parseRunOptions([
            '--harness',
            'github-copilot',
            '--no-auto',
            '--wait-after-prompt',
            '5s',
            '--wait-between-prompts',
            '30m',
            '--wait-after-error',
            '10m',
        ]);

        expect(options).toMatchObject({
            waitForUser: true,
            waitAfterPrompt: 5_000,
            waitBetweenPrompts: 1_800_000,
            waitAfterError: 600_000,
        });
    });

    it('rejects invalid priority values', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--priority', 'invalid'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects missing priority value', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--priority'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects invalid min priority values', () => {
        expect(() => parseRunOptions(['--harness', 'gemini', '--min-priority', '1.5'])).toThrow('process.exit');
        expect(processExitSpy).toHaveBeenCalledWith(1);
    });

    it('rejects invalid max priority values', () => {
src/cli\test\ptbk.test.ts:3:import { mkdtemp, rm } from 'fs/promises';
src/cli\test\ptbk.test.ts:35:    return mkdtemp(join(tmpdir(), 'promptbook-cli-'));
src/cli\test\ptbk.test.ts:152:        const temporaryDirectory = await createTemporaryDirectory();
src/cli\test\ptbk.test.ts:159:            process.chdir(temporaryDirectory);
src/cli\test\ptbk.test.ts:179:            await rm(temporaryDirectory, { recursive: true, force: true }).catch(() => undefined);
src/cli\test\ptbk.test.ts:184:        const temporaryDirectory = await createTemporaryDirectory();
src/cli\test\ptbk.test.ts:191:            process.chdir(temporaryDirectory);
src/cli\test\ptbk.test.ts:208:            await rm(temporaryDirectory, { recursive: true, force: true }).catch(() => undefined);
scripts/run-codex-prompts\prompts\writePromptRunTrace.test.ts:1:import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
scripts/run-codex-prompts\prompts\writePromptRunTrace.test.ts:13:    const projectPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-traces-'));
src/cli\cli-commands\common\promptbook-cli\$resolvePromptbookCliInstallations.test.ts:1:import { mkdir, mkdtemp, realpath, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\common\promptbook-cli\$resolvePromptbookCliInstallations.test.ts:41:    return realpath(await mkdtemp(join(tmpdir(), 'promptbook-cli-installation-project-')));
src/cli\cli-commands\common\promptbook-cli\$resolvePromptbookCliInstallations.test.ts:54:                .map((temporaryDirectory) => rm(temporaryDirectory, { recursive: true, force: true })),
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:1:import { mkdtemp, rm, writeFile } from 'fs/promises';
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:9:let temporaryPromptsDirectoryPath: string;
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:13:        temporaryPromptsDirectoryPath = await mkdtemp(join(tmpdir(), 'ptbk-coder-prompts-'));
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:17:        await rm(temporaryPromptsDirectoryPath, { recursive: true, force: true });
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:22:            join(temporaryPromptsDirectoryPath, 'ignored.md'),
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:27:            join(temporaryPromptsDirectoryPath, 'ready.md'),
scripts/run-codex-prompts\prompts\loadPromptFiles.test.ts:32:        const promptFiles = await loadPromptFiles(temporaryPromptsDirectoryPath);
scripts/run-codex-prompts\git\coderCommitScope.test.ts:1:import { mkdir, mkdtemp, rename, rm, writeFile } from 'fs/promises';
scripts/run-codex-prompts\git\coderCommitScope.test.ts:11:    const projectPath = await mkdtemp(join(tmpdir(), 'ptbk-coder-commit-scope-'));
scripts/run-codex-prompts\git\commitChanges.test.ts:3:import { mkdir, mkdtemp, rm, utimes, writeFile } from 'fs/promises';
scripts/run-codex-prompts\git\commitChanges.test.ts:66:    const temporaryProjectPath = await mkdtemp(join(tmpdir(), 'ptbk-coder-git-'));
scripts/run-codex-prompts\git\commitChanges.test.ts:67:    await mkdir(join(temporaryProjectPath, '.git'), { recursive: true });
scripts/run-codex-prompts\git\commitChanges.test.ts:68:    return temporaryProjectPath;
scripts/run-codex-prompts\git\commitChanges.test.ts:73:    let temporaryProjectPath: string | undefined;
scripts/run-codex-prompts\git\commitChanges.test.ts:89:        if (temporaryProjectPath) {
scripts/run-codex-prompts\git\commitChanges.test.ts:90:            await rm(temporaryProjectPath, { recursive: true, force: true });
scripts/run-codex-prompts\git\commitChanges.test.ts:91:            temporaryProjectPath = undefined;
scripts/run-codex-prompts\git\commitChanges.test.ts:110:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:111:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:117:            excludePaths: [join(temporaryProjectPath, 'prompts', '2026-04-6490.log.txt')],
scripts/run-codex-prompts\git\commitChanges.test.ts:132:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:133:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:147:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:148:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:172:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:173:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:186:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:187:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:194:            excludePaths: [join(temporaryProjectPath, 'prompts', '2026-04-6490.log.txt')],
scripts/run-codex-prompts\git\commitChanges.test.ts:205:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:206:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:406:        temporaryProjectPath = await createTemporaryGitProject();
scripts/run-codex-prompts\git\commitChanges.test.ts:407:        process.chdir(temporaryProjectPath);
scripts/run-codex-prompts\git\commitChanges.test.ts:409:        const staleIndexLockPath = join(temporaryProjectPath, '.git', 'index.lock');
src/cli\cli-commands\coder\mergeStringRecordJsonFile.test.ts:2:import { mkdtemp, readFile, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\coder\mergeStringRecordJsonFile.test.ts:11:    const directory = await mkdtemp(join(tmpdir(), 'promptbook-coder-json-'));
src/cli\cli-commands\coder\init.test.ts:2:import { mkdtemp, mkdir, readdir, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\coder\init.test.ts:24:    return mkdtemp(join(tmpdir(), 'promptbook-coder-init-'));
src/cli\cli-commands\coder\init.test.ts:53:    let temporaryProjectDirectory: string;
src/cli\cli-commands\coder\init.test.ts:58:        temporaryProjectDirectory = await createTemporaryProjectDirectory();
src/cli\cli-commands\coder\init.test.ts:68:        await rm(temporaryProjectDirectory, { recursive: true, force: true });
src/cli\cli-commands\coder\init.test.ts:72:        await runCoderInitCommand(temporaryProjectDirectory);
src/cli\cli-commands\coder\init.test.ts:74:        expect(await listPromptFileNames(temporaryProjectDirectory)).toHaveLength(DEFAULT_BOILERPLATE_COUNT.filesCount);
src/cli\cli-commands\coder\init.test.ts:78:        await mkdir(join(temporaryProjectDirectory, 'prompts'), { recursive: true });
src/cli\cli-commands\coder\init.test.ts:80:        await runCoderInitCommand(temporaryProjectDirectory);
src/cli\cli-commands\coder\init.test.ts:82:        expect(await listPromptFileNames(temporaryProjectDirectory)).toHaveLength(DEFAULT_BOILERPLATE_COUNT.filesCount);
src/cli\cli-commands\coder\init.test.ts:86:        const promptsDirectoryPath = join(temporaryProjectDirectory, 'prompts');
src/cli\cli-commands\coder\init.test.ts:92:        await runCoderInitCommand(temporaryProjectDirectory);
src/cli\cli-commands\coder\init.test.ts:94:        expect(await listPromptFileNames(temporaryProjectDirectory)).toEqual([existingPromptFileName]);
src/cli\cli-commands\coder\init.test.ts:98:        await runCoderInitCommand(temporaryProjectDirectory);
src/cli\cli-commands\coder\init.test.ts:106:        await runCoderInitCommand(temporaryProjectDirectory, ['--no-questions']);
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:2:import { mkdir, mkdtemp, readFile, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\coder\boilerplateTemplates.test.ts:21:    const directory = await mkdtemp(join(tmpdir(), 'promptbook-coder-'));
src/cli\cli-commands\coder\generate-boilerplates.test.ts:2:import { mkdtemp, readdir, readFile, rm } from 'fs/promises';
src/cli\cli-commands\coder\generate-boilerplates.test.ts:12:    const directory = await mkdtemp(join(tmpdir(), 'promptbook-coder-boilerplates-'));
src/cli\cli-commands\coder\add.test.ts:3:import { mkdtemp, readdir, readFile, rm } from 'fs/promises';
src/cli\cli-commands\coder\add.test.ts:13:    const directory = await mkdtemp(join(tmpdir(), 'promptbook-coder-add-'));
scripts/run-codex-prompts\common\runGoScript\withTempScript.test.ts:1:import { mkdtemp, readFile, rm, stat } from 'fs/promises';
scripts/run-codex-prompts\common\runGoScript\withTempScript.test.ts:8:    let temporaryDirectoryPath: string;
scripts/run-codex-prompts\common\runGoScript\withTempScript.test.ts:11:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-temp-script-'));
scripts/run-codex-prompts\common\runGoScript\withTempScript.test.ts:15:        await rm(temporaryDirectoryPath, { recursive: true, force: true });
scripts/run-codex-prompts\common\runGoScript\withTempScript.test.ts:19:        const scriptPath = resolvePromptbookTemporaryPath(temporaryDirectoryPath, 'agent-messages', 'message-0001.sh');
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:1:import { mkdtemp, readFile, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:19:    let temporaryDirectoryPath: string;
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:23:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-harness-gitignore-'));
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:31:        await rm(temporaryDirectoryPath, { recursive: true, force: true });
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:35:        await $ensureCoderHarnessGitignoreRules(temporaryDirectoryPath, 'qwen-code', {
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:43:        await expect(readFile(join(temporaryDirectoryPath, '.gitignore'), 'utf-8')).resolves.toBe(
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:51:        await $ensureCoderHarnessGitignoreRules(temporaryDirectoryPath, 'qwen-code', {
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:55:        await expect(readFile(join(temporaryDirectoryPath, '.gitignore'), 'utf-8')).rejects.toThrow();
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:59:        await writeFile(join(temporaryDirectoryPath, '.gitignore'), '/.qwen\n', 'utf-8');
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:61:        await $ensureCoderHarnessGitignoreRules(temporaryDirectoryPath, 'qwen-code', {
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:72:        await $ensureCoderHarnessGitignoreRules(temporaryDirectoryPath, 'qwen-code', {
src/cli\cli-commands\coder\$ensureCoderHarnessGitignoreRules.test.ts:77:        await expect(readFile(join(temporaryDirectoryPath, '.gitignore'), 'utf-8')).rejects.toThrow();
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:1:import { mkdtemp, readFile, rm, stat, writeFile } from 'fs/promises';
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:8:    let temporaryDirectoryPath: string;
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:11:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-prompt-runtime-log-'));
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:15:        await rm(temporaryDirectoryPath, { recursive: true, force: true });
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:19:        const scriptPath = join(temporaryDirectoryPath, 'prompt-1.sh');
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:33:        const scriptPath = join(temporaryDirectoryPath, 'prompt-2.sh');
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:48:        const scriptPath = join(temporaryDirectoryPath, 'prompt-3.sh');
scripts/run-codex-prompts\common\runGoScript\withPromptRuntimeLog.test.ts:62:        const scriptPath = join(temporaryDirectoryPath, 'prompt-4.sh');
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:1:import { mkdtemp, readFile, rm, stat } from 'fs/promises';
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:11:    let temporaryDirectoryPath: string;
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:14:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-runtime-log-'));
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:18:        await rm(temporaryDirectoryPath, { recursive: true, force: true });
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:22:        const scriptPath = join(temporaryDirectoryPath, 'prompt-1.sh');
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:48:        const scriptPath = join(temporaryDirectoryPath, 'prompt-keep.sh');
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:63:        const scriptPath = join(temporaryDirectoryPath, 'prompt-1.test.sh');
scripts/run-codex-prompts\common\runGoScript\runBashScriptWithOutput.test.ts:86:        const scriptPath = join(temporaryDirectoryPath, 'prompt-quiet.sh');
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:2:import { mkdtemp, readFile, rm, writeFile } from 'fs/promises';
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:36:    let temporaryDirectoryPath: string;
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:41:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-harness-ownership-'));
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:47:        await rm(temporaryDirectoryPath, { recursive: true, force: true, maxRetries: 3, retryDelay: 50 });
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:51:        const scriptPath = join(temporaryDirectoryPath, 'harness.sh');
scripts/run-codex-prompts\common\runGoScript\$spawnLoggedBashScript.test.ts:52:        const harnessHeartbeatPath = join(temporaryDirectoryPath, 'harness-heartbeat.txt');
scripts/run-codex-prompts\common\coderRunEstimateCache.ts:40:    return join(tmpdir(), ESTIMATE_CACHE_DIRECTORY_NAME);
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:2:import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:9:    let temporaryDirectoryPath: string;
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:12:        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-context-'));
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:16:        await rm(temporaryDirectoryPath, { recursive: true, force: true });
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:20:        await expect(resolveCoderContext(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:24:        await expect(resolveCoderContext('Inline instructions', temporaryDirectoryPath)).resolves.toBe(
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:30:        const contextFilePath = join(temporaryDirectoryPath, 'AGENTS.md');
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:36:        await expect(resolveCoderContext('AGENTS.md', temporaryDirectoryPath)).resolves.toBe(
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:45:        await mkdir(join(temporaryDirectoryPath, 'context-directory'));
scripts/run-codex-prompts\common\resolveCoderContext.test.ts:47:        await expect(resolveCoderContext('context-directory', temporaryDirectoryPath)).rejects.toBeInstanceOf(
scripts/run-codex-prompts\common\coderRunEstimateCache.test.ts:13:const ESTIMATE_CACHE_DIRECTORY = join(tmpdir(), 'ptbk-coder-estimates');
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:3:import { lstat, mkdir, mkdtemp, readFile, realpath, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:27:    readonly temporaryDirectoryPath: string;
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:42:    const temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-agents-server-build-'));
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:43:    const appPath = join(temporaryDirectoryPath, 'apps', 'agents-server');
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:44:    const sharedSourcePath = join(temporaryDirectoryPath, 'src', 'shared.ts');
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:50:        mkdir(join(temporaryDirectoryPath, 'apps', '_common'), { recursive: true }),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:51:        mkdir(join(temporaryDirectoryPath, 'books'), { recursive: true }),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:52:        mkdir(join(temporaryDirectoryPath, 'src'), { recursive: true }),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:57:        writeFile(join(temporaryDirectoryPath, 'apps', '_common', 'shared.ts'), 'Common source fixture\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:58:        writeFile(join(temporaryDirectoryPath, 'books', 'agent.book'), 'PERSONA Cached build fixture\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:59:        writeFile(join(temporaryDirectoryPath, 'package.json'), '{"name":"fixture"}\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:60:        writeFile(join(temporaryDirectoryPath, 'package-lock.json'), '{"name":"fixture"}\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:61:        writeFile(join(temporaryDirectoryPath, 'security.config.ts'), 'Security source fixture\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:62:        writeFile(join(temporaryDirectoryPath, 'servers.ts'), 'Servers source fixture\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:63:        writeFile(join(temporaryDirectoryPath, 'tsconfig.json'), '{}\n', 'utf-8'),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:72:        temporaryDirectoryPath,
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:77:    const temporaryDirectoryPaths: Array<string> = [];
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:84:            temporaryDirectoryPaths
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:86:                .map((temporaryDirectoryPath) => rm(temporaryDirectoryPath, { recursive: true, force: true })),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:113:        temporaryDirectoryPaths.push(fixture.temporaryDirectoryPath);
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:126:        temporaryDirectoryPaths.push(fixture.temporaryDirectoryPath);
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:135:        const temporaryDirectoryPath = await realpath(
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:136:            await mkdtemp(join(tmpdir(), 'promptbook-agents-server-packaged-')),
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:138:        temporaryDirectoryPaths.push(temporaryDirectoryPath);
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:140:        const nodeModulesPath = join(temporaryDirectoryPath, 'node_modules');
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:143:        const materializedRuntimeRootPath = join(temporaryDirectoryPath, '.promptbook', 'agents-server', 'runtime');
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:170:        process.chdir(temporaryDirectoryPath);
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:193:        temporaryDirectoryPaths.push(fixture.temporaryDirectoryPath);
src/cli\cli-commands\agents-server\buildAgentsServer.test.ts:217:        temporaryDirectoryPaths.push(fixture.temporaryDirectoryPath);
src/cli\cli-commands\agents-server\init.test.ts:3:import { mkdtemp, readFile, rm, writeFile } from 'fs/promises';
src/cli\cli-commands\agents-server\init.test.ts:13:    const directory = await mkdtemp(join(tmpdir(), 'promptbook-agents-server-init-'));
{
    "name": "promptbook",
    "version": "0.114.0-33",
    "description": "Promptbook: Create persistent AI agents that turn your company's scattered knowledge into action",
    "--note-0": "    <- [🐊]",
    "private": false,
    "sideEffects": false,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/webgptorg/promptbook.git"
    },
    "author": "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
    "contributors": [
        "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
        "Jiří Jahn <jiri@ptbk.io> (https://www.ptbk.io/)"
    ],
    "--note-1": "[❇️]",
    "keywords": [
        "ai",
        "llm",
        "prompt",
        "template",
        "language-model",
        "chatgpt",
        "autogpt",
        "machine-learning",
        "natural-language-processing",
        "nlp",
        "openai",
        "o3",
        "o3-mini",
        "deepseek",
        "gpt-3",
        "gpt-4",
        "gpt-4o",
        "gpt-4o-mini",
        "o1",
        "o1-mini",
        "o1-preview",
        "anthropic",
        "claude",
        "claude-3",
        "claude-3-opus",
        "claude-3-sonnet",
        "claude-3-haiku",
        "gemini",
        "gemini-pro",
        "gemini-flash",
        "mixtral",
        "mistral",
        "ollama",
        "ai-orchestration",
        "prompt-engineering",
        "llmops",
        "multimodal",
        "reasoning",
        "rag",
        "embeddings",
        "function-calling",
        "large-language-models",
        "ai-application-framework",
        "text-generation",
        "ai-agents",
        "LLMOps",
        "book-language",
        "markdown-dsl",
        "ai-workflow",
        "ai-automation",
        "pipeline",
        "workflow",
        "orchestration",
        "ai-pipeline",
        "prompt-template",
        "prompt-chaining",
        "ai-scripting",
        "conversational-ai",
        "chatbot",
        "ai-assistant",
        "knowledge-base",
        "document-processing",
        "web-scraping",
        "pdf-processing",
        "csv-processing",
        "json-processing",
        "xml-processing",
        "markdown-processing",
        "wikipedia-scraping",
        "website-crawler",
        "azure-openai",
        "vercel-ai",
        "docker",
        "cli-tool",
        "typescript",
        "javascript",
        "nodejs",
        "browser",
        "cross-platform",
        "api-integration",
        "remote-execution",
        "caching",
        "rate-limiting",
        "error-handling",
        "validation",
        "testing",
        "mocking",
        "personas",
        "expectations",
        "postprocessing",
        "preprocessing",
        "templating",
        "parameterization",
        "configuration",
        "logging",
        "monitoring",
        "debugging",
        "vscode-extension",
        "syntax-highlighting",
        "intellisense",
        "developer-tools",
        "ai-development",
        "prompt-management",
        "model-agnostic",
        "multi-model",
        "ai-sdk",
        "ai-framework",
        "ai-platform",
        "generative-ai",
        "content-generation",
        "text-processing",
        "natural-language",
        "human-readable",
        "plain-english",
        "no-code",
        "low-code",
        "automation-framework",
        "workflow-engine",
        "task-automation",
        "ai-ops",
        "mlops",
        "devops",
        "ci-cd",
        "version-control",
        "git-friendly",
        "collaboration",
        "team-development",
        "enterprise",
        "scalable",
        "production-ready",
        "open-source",
        "community",
        "extensible",
        "plugin-system",
        "integration",
        "api-wrapper",
        "sdk-wrapper",
        "abstraction-layer",
        "unified-interface",
        "cross-provider",
        "vendor-agnostic",
        "future-proof",
        "paradigm-shift",
        "next-generation",
        "revolutionary",
        "innovative",
        "cutting-edge",
        "state-of-the-art"
    ],
    "license": "BUSL-1.1",
    "bugs": {
        "url": "https://github.com/webgptorg/promptbook/issues"
    },
    "homepage": "https://ptbk.io/",
    "funding": [
        {
            "type": "individual",
            "url": "https://buymeacoffee.com/hejny"
        },
        {
            "type": "github",
            "url": "https://github.com/webgptorg/promptbook/blob/main/README.md#%EF%B8%8F-contributing"
        }
    ],
    "scripts": {
        "start": "npx ts-node ./src/cli/test/ptbk.ts agents-server start --harness github-copilot --model gpt-5.4 --thinking-level high --port 4440",
        "test": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && npm run test-app-agents-server && echo \"🎉 All tests passed!\"",
        "test-without-package-generation": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-unit",
        "test-without-package-generation-and-unit": "echo \"Note: Running from most lightweight to heaviest tests\" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build",
        "test-for-ptbk-coder": "node ./scripts/test-for-ptbk-coder.js",
        "test-unit": "node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit",
        "_TODO-0": "           <- TODO: Remove `--forceExit` flag, now its present to ensure return code 0 when all tests ",
        "test-types": "tsc",
        "test-lint": "eslint src",
        "test-spellcheck": "bash ./scripts/spellcheck.sh",
        "test-books": "echo \"Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated\"",
        "_test-books": "ts-node ./src/cli/test/ptbk.ts test --provider BRING_YOUR_OWN_KEYS --no-prepare \"./**/*.book\" --ignore \"./book/**/*,./examples/pipelines/errors/**/*\" --verbose",
        "test-package-generation": "ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler",
        "test-name-discrepancies": "ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts",
        "test-book-components-build": "echo \"Note: Skipping book components build tests because of constants problems with this test\"",
        "_TODO-test-book-components-build": "(cd ./apps/book-components/ && npm run test-build)",
        "test-app-agents-server": "(cd ./apps/agents-server/ && npm run test)",
        "preversion": "git pull && git pull --tags && npm run test-without-package-generation && echo \"<- Note: In next command the packages are generated\" && npm run generate-packages-commit",
        "postversion": "git push && git push --tags && npm run update-version-in-config-commit && npm run generate-packages-commit && npm run use-packages",
        "make": "npm run make-promptbook-collection && npm run update-version-in-config && npm run generate-packages && npm run generate-examples-bookc && npm run generate-documentation && npm run import-markdowns && npm run generate-openapi-types",
        "generate-packages": "ts-node ./scripts/generate-packages/generate-packages.ts",
        "generate-packages-commit": "ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler --commit",
        "import-github": "ts-node ./scripts/import-github/import-github.ts",
        "import-github-commit": "ts-node ./scripts/import-github/import-github.ts --commit",
        "_NOTE-0": " <- Note: This is here to ensure that publish.yml is up to date before tagging the new version",
        "generate-documentation": "ts-node ./scripts/generate-documentation/generate-documentation.ts",
        "generate-documentation-commit": "ts-node ./scripts/generate-documentation/generate-documentation.ts --commit",
        "generate-examples-bookc": "ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts ",
        "generate-examples-bookc-commit": "ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts --commit",
        "generate-openapi-types": "npx openapi-typescript src/remote-server/openapi.yml -o src/remote-server/openapi-types.ts --immutable --export-type",
        "generate-openapi-types-commit": "npx openapi-typescript src/remote-server/openapi.yml -o src/remote-server/openapi-types.ts --immutable --export-type && git add . && git commit --allow-empty --message \"🏭 Generate OpenAPI types\" && git push",
        "generate-vscode-grammar": "ts-node ./scripts/generate-vscode-grammar/generate-vscode-grammar.ts",
        "generate-vscode-grammar-commit": "ts-node ./scripts/generate-vscode-grammar/generate-vscode-grammar.ts && git add . && git commit --allow-empty --message \"🏭 Generate VSCode grammar\" && git push",
        "import-markdowns": "npx deno --allow-env --allow-read --allow-write --allow-sys --unstable-sloppy-imports ./book/scripts/import-markdown/import-markdown.ts",
        "import-markdowns-commit": "npx deno --allow-env --allow-read --allow-write --allow-sys --unstable-sloppy-imports ./book/scripts/import-markdown/import-markdown.ts && cd ./book && git add . && git commit --allow-empty --message \"📄 Import markdowns\" && git push && cd .. && git add . && git commit --allow-empty --message \"📄 Import markdowns\"",
        "make-promptbook-collection": "ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts",
        "make-promptbook-collection-commit": "ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --commit",
        "repair-imports-commit": "ts-node ./scripts/repair-imports/repair-imports.ts --commit",
        "update-version-in-config": "ts-node ./scripts/update-version-in-config/update-version-in-config.ts",
        "update-version-in-config-commit": "ts-node ./scripts/update-version-in-config/update-version-in-config.ts --commit",
        "update-and-install-patch-dependencies": "npx npm-check-updates --upgrade --target patch && npm install",
        "update-and-install-patch-dependencies-commit": "npm run update-and-install-patch-dependencies && git add package.json package-lock.json && git commit --allow-empty --message \"🔼 Update patch dependencies\" && git push",
        "_TODO-3": "<- TODO: Detect if the worktree is clean before running patch-dependencies-commit",
        "use-packages": "ts-node ./scripts/use-packages/use-packages.ts",
        "build:book-editor-macos": "node ./scripts/book-editor-macos/build-book-editor-macos.js",
        "build:vscode-extension": "node ./scripts/vscode-extension/build-vscode-extension.js",
        "components-playground": "cd ./apps/book-components/ && npm run dev",
        "_TODO-1": "<- TODO: Remove all unused scripts",
        "_TODO-2": "<- TODO: [⛎] Automatically sync commands to generate-packages.ts",
        "coder:generate-boilerplates": "npx ptbk coder generate-boilerplates --count 5*1 --template ./prompts/templates/common.md",
        "coder:run": "npx ptbk coder run --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/developer.book --context AGENTS.md --test-before yes-and-fix",
        "coder:find-refactor-candidates": "npx ptbk coder find-refactor-candidates",
        "coder:verify": "npx ptbk coder verify",
        "coder:add": "npx ptbk coder add --template ./prompts/templates/common.md"
    },
    "engines": {
        "node": ">=18.18.0",
        "npm": ">=8.0.0"
    },
    "devDependencies": {
        "@eslint/eslintrc": "3",
        "@next/bundle-analyzer": "16.0.11",
        "@playwright/test": "^1.62.1",
        "@rollup/plugin-json": "6.1.0",
        "@rollup/plugin-typescript": "8.3.4",
        "@rollup/plugin-url": "8.0.2",
        "@tailwindcss/typography": "0.5.19",
        "@testing-library/react": "16.3.2",
        "@types/better-sqlite3": "^7.6.13",
        "@types/crypto-js": "4.2.2",
        "@types/express": "5.0.6",
        "@types/geojson": "7946.0.16",
        "@types/jest": "29.5.14",
        "@types/js-yaml": "4.0.9",
        "@types/jsdom": "21.1.7",
        "@types/katex": "0.16.8",
        "@types/leaflet": "1.9.21",
        "@types/localtunnel": "^2.0.4",
        "@types/mime-types": "2.1.4",
        "@types/node": "20",
        "@types/node-fetch": "2.6.13",
        "@types/node-gzip": "1.1.3",
        "@types/nodemailer": "^7.0.11",
        "@types/papaparse": "5.3.16",
        "@types/pg": "8.15.6",
        "@types/prettier": "2.7.3",
        "@types/prompts": "2.4.9",
        "@types/qrcode": "1.5.6",
        "@types/react": "19",
        "@types/react-dom": "19",
        "@types/readline-sync": "1.4.8",
        "@types/showdown": "2.0.6",
        "@types/swagger-ui-express": "4.1.8",
        "@types/web-push": "^3.6.4",
        "@typescript-eslint/eslint-plugin": "6.21.0",
        "@typescript-eslint/parser": "6.21.0",
        "autoprefixer": "10.4.27",
        "cspell": "9.0.2",
        "deno": "2.2.15",
        "eslint": "8.57.1",
        "eslint-config-next": "15.4.11",
        "jest": "29.7.0",
        "jest-environment-jsdom": "30.2.0",
        "js-yaml": "4.1.1",
        "llamaindex": "0.12.1",
        "lucide-react": "0.536.0",
        "mermaid": "11.9.0",

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/common/resolveCoderContext.test.ts; Get-Content scripts/run-codex-prompts/common/resolveInlineOrFileText.ts; Get-Content src/errors/NotFoundError.ts; Get-Content src/errors/NotAllowed.ts; Get-Content src/errors/UnexpectedError.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 348ms:
import { spaceTrim } from 'spacetrim';
import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
import { tmpdir } from 'os';
import { join } from 'path';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { resolveCoderContext } from './resolveCoderContext';

describe('resolveCoderContext', () => {
    let temporaryDirectoryPath: string;

    beforeEach(async () => {
        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-context-'));
    });

    afterEach(async () => {
        await rm(temporaryDirectoryPath, { recursive: true, force: true });
    });

    it('returns undefined when context is not provided', async () => {
        await expect(resolveCoderContext(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
    });

    it('returns inline context when the referenced file does not exist', async () => {
        await expect(resolveCoderContext('Inline instructions', temporaryDirectoryPath)).resolves.toBe(
            'Inline instructions',
        );
    });

    it('reads context content from an existing file', async () => {
        const contextFilePath = join(temporaryDirectoryPath, 'AGENTS.md');
        await writeFile(contextFilePath, spaceTrim(`
            ## Rules
            - Keep it DRY
        `), 'utf-8');

        await expect(resolveCoderContext('AGENTS.md', temporaryDirectoryPath)).resolves.toBe(
            spaceTrim(`
                ## Rules
                - Keep it DRY
            `),
        );
    });

    it('rejects directories referenced as context files', async () => {
        await mkdir(join(temporaryDirectoryPath, 'context-directory'));

        await expect(resolveCoderContext('context-directory', temporaryDirectoryPath)).rejects.toBeInstanceOf(
            NotFoundError,
        );
    });
});
import { readFile, stat } from 'fs/promises';
import { resolve } from 'path';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { spaceTrim } from '../../../src/utils/organization/spaceTrim';

/**
 * Options for resolving a CLI value that can be either inline text or a file path.
 */
type ResolveInlineOrFileTextOptions = {
    readonly textReference: string | undefined;
    readonly currentWorkingDirectory: string;
    readonly contextLabel: string;
    readonly optionName: string;
};

/**
 * Resolves optional CLI text provided inline or via a file path.
 */
export async function resolveInlineOrFileText(
    options: ResolveInlineOrFileTextOptions,
): Promise<string | undefined> {
    const normalizedTextReference = options.textReference?.trim();
    if (!normalizedTextReference) {
        return undefined;
    }

    const textPath = resolve(options.currentWorkingDirectory, normalizedTextReference);
    const textPathStats = await stat(textPath).catch((error: NodeJS.ErrnoException) => {
        if (error.code === 'ENOENT') {
            return undefined;
        }
        throw error;
    });

    if (!textPathStats) {
        return normalizedTextReference;
    }

    if (!textPathStats.isFile()) {
        throw new NotFoundError(
            spaceTrim(`
                ${options.contextLabel} path \`${normalizedTextReference}\` exists but it is not a file.

                Pass a file path or inline text in \`${options.optionName}\`.
            `),
        );
    }

    return readFile(textPath, 'utf-8');
}

/**
 * This error indicates that promptbook not found in the collection
 *
 * @public exported from `@promptbook/core`
 */
export class NotFoundError extends Error {
    public readonly name = 'NotFoundError';
    public constructor(message: string) {
        super(message);
        Object.setPrototypeOf(this, NotFoundError.prototype);
    }
}
/**
 * This error indicates that promptbook operation is not allowed
 *
 * @public exported from `@promptbook/core`
 */
export class NotAllowed extends Error {
    public readonly name = 'NotAllowed';
    public constructor(message: string) {
        super(message);
        Object.setPrototypeOf(this, NotAllowed.prototype);
    }
}
import { spaceTrim } from 'spacetrim';
import { ADMIN_EMAIL } from '../config';
import { getErrorReportUrl } from './utils/getErrorReportUrl';

/**
 * This error type indicates that the error should not happen and its last check before crashing with some other error
 *
 * @public exported from `@promptbook/core`
 */
export class UnexpectedError extends Error {
    public readonly name = 'UnexpectedError';
    public constructor(message: string) {
        super(
            spaceTrim(
                (block) => `
                    ${block(message)}

                    Note: This error should not happen.
                    It's probably a bug in the pipeline collection

                    Please report issue:
                    ${block(getErrorReportUrl(new Error(message)).href)}

                    Or contact us on ${ADMIN_EMAIL}

                `,
            ),
        );
        Object.setPrototypeOf(this, UnexpectedError.prototype);
    }
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git log --oneline --all --max-count=40 -- scripts/run-codex-prompts/common/resolveCoderAgent.ts src/cli/cli-commands/coder/run.ts .vscode/terminals.json apps/coder-landing/src/components/sections/AgentsSection.tsx; git log -p --max-count=3 -- scripts/run-codex-prompts/common/resolveCoderAgent.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 929ms:
ed7fdf7c8 [✨🌴] Promptbook coder should save the run traces.
46a50b3dc [✨🌴] Promptbook coder should handle low disk space gracefully.
9ab63a126 [✨💥] Add option `--no-questions`
6c226a98b [✨🍺] When the harness is not logged in, or not authenticated, handle the error message more gracefully.
7ed61fbac ✨ Add new coding agent commands for `qwen3.8-max`, `qwen3.8-flash`, `deepseek-v4-pro`, `deepseek-v4-flash`, and `glm-5.2`
23bdf0daa New models
cfb986a7a [✨🤛] Implement `ptbk coder list`
ea3454527 [✨🍭] The promptbook coder `ptbk coder init` should add entries to the ignore file for all harnesses
1ade0d627 [✨🕗] `ptbk coder` updates OpenAI Codex via `npm install -g @openai/codex@latest` even when Codex is the standalone install (`codex update`) (commited manually)
b1e3e4cad Terminals
eaf96d2b9 [✨🛼] Allow to pick what happen with dirty working dir in coder
85dbdf24c [✨📛] Unslop the promptbook coder landing page
0fe778db9 Terminals
ea72edb55 Terminals
9567c243e [✨🛕] Add flag `--no-harness-update` which will turn off automatic check if the harness is up to date
d5ee36f20 [✨➝] Automatically check that the Promptbook CLI is up to date.
01c0146cb Revert "[✨➝] Automatically check that the Promptbook CLI is up to date."
0acb33212 Update ping command to use `claude-sonnet-5` model
4a16bac8b [✨➝] Automatically check that the Promptbook CLI is up to date.
500a4ea94 `--min-priority` and `--max-priority`
40e226ba0 `--auto-pull --auto-push`
3286feaa7 Cleanup `agents/examples`
aa19fe797 Mark old terminals
7b0e910e1 Terminals
7b7b38c6e [✨💝] When Promptbook Coder is committing, commit only relevant files for the current operation.
7f49cf011 Terminals
c0f509f9d Terminals
6cfd53e49 Terminals
86f4d63e4 [✨🌫] Add `--commit` option to `ptbk coder` commands
0d7ea27a2 Revert "[✨🏷] Create command `ptbk coder ping`"
403cc6edf Terminals
c43548eaa Terminals
de5b61bb7 Terminals
a648851ea [✨🏷] Create command `ptbk coder ping`
4b554dba2 Terminals
372145924 Autofail
90f585095 [✨🛐] Add `--isolate` option to promptbook coder
511d6d48c [✨➝] Automatically check that the harness is installed and up to date.
f536aaba9 Terminals - Add `gpt-5.6-sol`
be698ce08 Cleanup terminals
commit ea4cf08c80de9cdb560e163fac13b64a884c401e
Author: Promptbook Coding Agent <me+agent@pavolhejny.com>
Date:   Fri Jul 3 09:47:32 2026 +0200

    [✨🟣] When `--agent` present in `ptbk coder`, show the visual as ASCII
    
    ```bash
    me@DESKTOP-2QD9KQQ MINGW64 ~/work/ai/promptbook (main)
    $ npx ts-node ./src/cli/test/ptbk.ts coder server --harness openai-codex --model gpt-5.5 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md --priority 0 --test npm run test-for-ptbk-coder
    Starting prompt runner in server (keep-alive) mode…
    
                                   ▄▄▄▄ ▄▄▄▄▄▄ ▄▄▄▄  ▄▄ ▄▄   ▄▄  ▄▄▄
                                   ██▄█▀  ██   ██▄██ ██▄█▀   ██ ██▀██
                                   ██     ██   ██▄█▀ ██ ██ ▄ ██ ▀███▀
    
    ┌ Session ─────────────────────────────────────────────────────────────────────────────────────┐
    │ State     PAUSED  Paused before checking the git working tree                                │
    │ Runner   codex  ·  gpt-5.5  ·  thinking xhigh                                                │
    │ Context  AGENTS.md                                                                           │
    │ Server   http://localhost:4441                                                               │
    │ Test     npm run test-for-ptbk-coder                                                         │
    │ This run Task 3/3  ·  2 done  ·  1 left                                                      │
    │ Backlog  Repo 610 total                                                                      │
    │ Scope    Priority ≥0  ·  Write 100 prompts first                                             │
    │ Timing   Elapsed 2h 48m  ·  Total 4h 12m  ·  ETA Today 19:25                                 │
    │ Progress ████████████████████████████████████████░░░░░░░░░░░░░░░░░░░ 67% complete (2/3 done) │
    └──────────────────────────────────────────────────────────────────────────────────────────────┘
    ┌ Current task ────────────────────────────────────────────────────────────────────────────────┐
    │ prompts/2026-06-0870-ptbk-coder-npx-vs-no-npx.md#1                                           │
    │ Attempt 1/3  ·  Paused before checking the git working tree                                  │
    └──────────────────────────────────────────────────────────────────────────────────────────────┘
    ┌ Live output ─────────────────────────────────────────────────────────────────────────────────┐
    │ ›    - Local:        http://localhost:4440                                                   │
    │ ›    - Network:      http://192.168.56.1:4440                                                │
    │ ›  ✓ Starting...                                                                             │
    │ ›  ✓ Ready in 2.9s                                                                           │
    │ › (node:8176) [DEP0040] DeprecationWarning: The `punycode` module is deprecated. Please u... │
    │ › (Use `node --trace-deprecation ...` to show where the warning was created)                 │
    │ › Prerendered home page and saved to C:\Users\me\work\ai\promptbook\apps\agents-server\.n... │
    │ › 🎉 All tests passed!                                                                       │
    └──────────────────────────────────────────────────────────────────────────────────────────────┘
    ┌ Controls ────────────────────────────────────────────────────────────────────────────────────┐
    │  P  Resume   CTRL+C  Exit                                                                    │
    └──────────────────────────────────────────────────────────────────────────────────────────────┘
    ```
    
    -   Agents has visuals on website
    -   But on the terminal, there is only ugly ASCII text "PTBK.IO"
    -   When `--agent` present in `ptbk coder`, show the visual as ASCII
    -   Make some universal function / technique to convert the actual visuals into ASCII for the terminal dynamically
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Add the changes into the [changelog](changelog/_current-preversion.md)
    
    ![Agent visuals on web](screenshots/2026-06-0890-ptbk-coder-agent-visual.png)
    ![Agent visuals on web](screenshots/2026-06-0890-ptbk-coder-agent-visual-1.png)

diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
new file mode 100644
index 000000000..af6757a43
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -0,0 +1,54 @@
+import { readFile } from 'fs/promises';
+import { resolve } from 'path';
+import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
+import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = {
+    /**
+     * Raw source of the agent `.book` file.
+     */
+    readonly agentSource: string_book;
+
+    /**
+     * Compiled system message injected into each coding prompt.
+     */
+    readonly systemMessage: string;
+};
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentPath: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    if (!agentPath) {
+        return undefined;
+    }
+
+    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
+    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+        if (error.code === 'ENOENT' || error.code === 'EISDIR') {
+            throw new NotFoundError(
+                spaceTrim(`
+                    Agent book \`${agentPath}\` was not found or is not a file.
+
+                    Pass a path to a \`.book\` file in \`--agent\`.
+                `),
+            );
+        }
+        throw error;
+    });
+
+    return {
+        agentSource: agentSource as string_book,
+        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+    };
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git log --all --oneline --grep="specify.*agent" --regexp-ignore-case; git branch --all --contains HEAD; git remote -v; git log --all --oneline -- agents/coding/unslopper.book | Select-Object -First 30; git log --all --oneline -- .vscode/terminals.json | Select-Object -First 30' in C:\Users\me\work\ai\promptbook
 succeeded in 2066ms:
33d53494d [✨🚝] Make the `TEAM` commitment work.
1ceeb9542 [✨🥥] Remove commitments `COMPONENT`, `WALLET`, `MEMORY`, `USE BROWSER`, `USE DEEPSEARCH`, `USE SEARCH ENGINE`, `USE SPAWN`, `USE TIMEOUT`, `USE TIME`, `USE EMAIL` and `META`
718824921 [✨😇] Allow to specify an agent for `ptbk coder run` - manual enhance of the prompt
4c5db6c29 [✨😇] Allow to specify an agent for `ptbk coder run`
64a55cccf [✨🗜] Add `--ignore <pattern>` flag to `ptbk agent run-multiple`
01622ffca [✨🗜] Add `--ignore <pattern>` flag to `ptbk agent run-multiple`
f26e251ac [🎞️📄] Promptbook agents can read the files are cropped
752afe6f0 [✨🔳] Allow to specify website in `KNOWLEDGE` commitment.
1a43798be [✨🔳] Allow to specify website in `KNOWLEDGE` commitment.
db57c1627 [✨🔳] Allow to specify knowledge freely in the text of the `KNOWLEDGE` commitment.
b4d8dd8af [✨🦮] OpenAI compatibility mode of the agent server should respect the `response_format`
f37bd22d4 [✨🦮] OpenAI compatibility mode of the agent server should respect the `response_format`
07e259add [✨🦮] OpenAI compatibility mode of the agent server should respect the `response_format`
76e06e804 [✨🦮] OpenAI compatibility mode of the agent server should respect the `response_format`
a5430e791 [✨🎮] Allow to specify model in coding agent when using `--agent openai-codex`
14bbdfd25 [✨👺] To be able to specify the teammates of the agent.
c6eeed348 [✨😋] Create agentic behaviour on Facebook
47da5f178 [✨🏐] Allow to add MCP server to agent via `USE MCP` commitment
d7d2ef2fc [✨🪓] Create federated servers logic to Agents Server
* main
  remotes/origin/main
origin	https://github.com/webgptorg/promptbook.git (fetch)
origin	https://github.com/webgptorg/promptbook.git (push)
0d73e070c Prompts
7ed61fbac ✨ Add new coding agent commands for `qwen3.8-max`, `qwen3.8-flash`, `deepseek-v4-pro`, `deepseek-v4-flash`, and `glm-5.2`
23bdf0daa New models
cfb986a7a [✨🤛] Implement `ptbk coder list`
b1e3e4cad Terminals
0fe778db9 Terminals
ea72edb55 Terminals
9567c243e [✨🛕] Add flag `--no-harness-update` which will turn off automatic check if the harness is up to date
0acb33212 Update ping command to use `claude-sonnet-5` model
500a4ea94 `--min-priority` and `--max-priority`
40e226ba0 `--auto-pull --auto-push`
3286feaa7 Cleanup `agents/examples`
aa19fe797 Mark old terminals
7b0e910e1 Terminals
7f49cf011 Terminals
c0f509f9d Terminals
6cfd53e49 Terminals
86f4d63e4 [✨🌫] Add `--commit` option to `ptbk coder` commands
0d7ea27a2 Revert "[✨🏷] Create command `ptbk coder ping`"
403cc6edf Terminals
c43548eaa Terminals
de5b61bb7 Terminals
a648851ea [✨🏷] Create command `ptbk coder ping`
4b554dba2 Terminals
372145924 Autofail
f536aaba9 Terminals - Add `gpt-5.6-sol`
be698ce08 Cleanup terminals
6ba990ba5 Terminals
5b35967f7 Terminals
e6557faab Terminals
f0adea127 [✨🎋] Add `--max-priority` and `--min-priority` options to `ptbk coder`

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git show --stat --oneline 4c5db6c29; git show --format=fuller --find-renames --find-copies 4c5db6c29 --; git show --stat --oneline 718824921; git show --format=fuller --find-renames --find-copies 718824921 --' in C:\Users\me\work\ai\promptbook
 succeeded in 474ms:
4c5db6c29 [✨😇] Allow to specify an agent for `ptbk coder run`
 changelog/_current-preversion.md                   |  6 ++++
 prompts/2026-06-0390-ptbk-coder-agent.md           |  3 +-
 scripts/run-codex-prompts/cli/RunOptions.ts        |  5 +++
 .../common/resolveAgentSystemMessage.ts            | 36 ++++++++++++++++++++++
 scripts/run-codex-prompts/main/runCodexPrompts.ts  |  3 ++
 scripts/run-codex-prompts/main/runPromptRound.ts   |  9 +++++-
 src/cli/cli-commands/coder/run.ts                  |  8 ++++-
 src/cli/cli-commands/coder/server.ts               |  8 ++++-
 8 files changed, 74 insertions(+), 4 deletions(-)
commit 4c5db6c29a99eaf69cb151260167de8b81d52485
Author:     Pavol Hejny <me@pavolhejny.com>
AuthorDate: Sat Jun 20 17:22:23 2026 +0200
Commit:     Pavol Hejny <me@pavolhejny.com>
CommitDate: Sat Jun 20 17:22:23 2026 +0200

    [✨😇] Allow to specify an agent for `ptbk coder run`
    
    ```bash
    ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md --agent agents/coding-expert.book
    ```
    
    -   It is possible to specify all of the `--context` and `--agent`, both will be used for creating the prompt for the agent
    -   Keep in mind the DRY _(don't repeat yourself)_ principle, reuse same logic from `ptbk agent chat --agent ./agents/default/generic-chatter.book`
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 0d30b9e4d..68c20d159 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,9 @@
+-   Added `--agent <agent-book-path>` option to `ptbk coder run` and `ptbk coder server`:
+
+    -   Accepts a path to a `.book` file (relative to the current working directory) whose compiled system message is prepended to each coding prompt before the task text.
+    -   Can be combined with `--context`: the agent system message is inserted first, followed by the task prompt, and finally the context — so the harness sees agent instructions → task → additional context.
+    -   Example: `ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh --context AGENTS.md --agent agents/coding-expert.book`
+
 -   `ptbk coder init` now automatically generates 5 prompt boilerplate files after setting up the project configuration.
 
 -   Added `ptbk coder server` command that runs the same prompt-processing logic as `ptbk coder run` but keeps the process alive and serves a kanban web UI on a configurable port (default `4441`):
diff --git a/prompts/2026-06-0390-ptbk-coder-agent.md b/prompts/2026-06-0390-ptbk-coder-agent.md
index 1f79a4a61..d4cfb7e3e 100644
--- a/prompts/2026-06-0390-ptbk-coder-agent.md
+++ b/prompts/2026-06-0390-ptbk-coder-agent.md
@@ -1,4 +1,4 @@
-[ ]
+[x] $1.22 an hour by Claude Code
 
 [✨😇] Allow to specify an agent for `ptbk coder run`
 
@@ -11,3 +11,4 @@ ptbk coder run --harness github-copilot --model gpt-5.4 --thinking-level xhigh -
 -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
 -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
 -   Add the changes into the [changelog](changelog/_current-preversion.md)
+
diff --git a/scripts/run-codex-prompts/cli/RunOptions.ts b/scripts/run-codex-prompts/cli/RunOptions.ts
index bbcc37fd4..cd80c33b4 100644
--- a/scripts/run-codex-prompts/cli/RunOptions.ts
+++ b/scripts/run-codex-prompts/cli/RunOptions.ts
@@ -74,6 +74,11 @@ export type RunOptions = {
     autoPull: boolean;
     agentName?: PromptRunnerHarnessName;
     model?: string;
+    /**
+     * Optional path to an agent `.book` file whose compiled system message is prepended to each codex prompt.
+     * Supports the same path resolution as `--context`: relative to the current working directory.
+     */
+    agent?: string;
     /**
      * Minimum prompt priority required for processing.
      */
diff --git a/scripts/run-codex-prompts/common/resolveAgentSystemMessage.ts b/scripts/run-codex-prompts/common/resolveAgentSystemMessage.ts
new file mode 100644
index 000000000..3301a655f
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveAgentSystemMessage.ts
@@ -0,0 +1,36 @@
+import { readFile } from 'fs/promises';
+import { resolve } from 'path';
+import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
+import { NotFoundError } from '../../../src/errors/NotFoundError';
+import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
+import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveAgentSystemMessage(
+    agentPath: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<string | undefined> {
+    if (!agentPath) {
+        return undefined;
+    }
+
+    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
+    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+        if (error.code === 'ENOENT' || error.code === 'EISDIR') {
+            throw new NotFoundError(
+                spaceTrim(`
+                    Agent book \`${agentPath}\` was not found or is not a file.
+
+                    Pass a path to a \`.book\` file in \`--agent\`.
+                `),
+            );
+        }
+        throw error;
+    });
+
+    return createAgentRunnerSystemMessage(agentSource as string_book);
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index 5c2aad10e..97b502c48 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -13,6 +13,7 @@ import type {
 } from '../common/CoderRunPauseCheckpoint';
 import { CliProgressDisplay } from '../common/cliProgressDisplay';
 import { formatDurationMs } from '../common/parseDuration';
+import { resolveAgentSystemMessage } from '../common/resolveAgentSystemMessage';
 import { resolveCoderContext } from '../common/resolveCoderContext';
 import {
     announcePauseTargetLabel,
@@ -73,6 +74,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
 
     try {
         const resolvedCoderContext = await resolveCoderContext(options.context, process.cwd());
+        const resolvedAgentSystemMessage = await resolveAgentSystemMessage(options.agent, process.cwd());
 
         if (await runDryRunIfRequested(options)) {
             return;
@@ -170,6 +172,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
                 nextPrompt,
                 promptLabel,
                 resolvedCoderContext,
+                resolvedAgentSystemMessage,
                 isRichUiEnabled,
                 progressDisplay,
                 uiHandle,
diff --git a/scripts/run-codex-prompts/main/runPromptRound.ts b/scripts/run-codex-prompts/main/runPromptRound.ts
index a325b87c3..21ce95c4c 100644
--- a/scripts/run-codex-prompts/main/runPromptRound.ts
+++ b/scripts/run-codex-prompts/main/runPromptRound.ts
@@ -40,6 +40,7 @@ type RunPromptRoundOptions = {
     nextPrompt: PromptSelection;
     promptLabel: string;
     resolvedCoderContext?: string;
+    resolvedAgentSystemMessage?: string;
     isRichUiEnabled: boolean;
     progressDisplay?: CliProgressDisplay;
     uiHandle?: CoderRunUiHandle;
@@ -58,13 +59,19 @@ export async function runPromptRound({
     nextPrompt,
     promptLabel,
     resolvedCoderContext,
+    resolvedAgentSystemMessage,
     isRichUiEnabled,
     progressDisplay,
     uiHandle,
     waitForRequestedPause,
 }: RunPromptRoundOptions): Promise<void> {
     const commitMessage = buildCommitMessage(nextPrompt.file, nextPrompt.section);
-    const codexPrompt = appendCoderContext(buildCodexPrompt(nextPrompt.file, nextPrompt.section), resolvedCoderContext);
+    const taskPrompt = buildCodexPrompt(nextPrompt.file, nextPrompt.section);
+    // Prepend agent system message before the task so the harness sees agent instructions first
+    const promptWithAgent = resolvedAgentSystemMessage
+        ? `${resolvedAgentSystemMessage.trim()}\n\n${taskPrompt}`
+        : taskPrompt;
+    const codexPrompt = appendCoderContext(promptWithAgent, resolvedCoderContext);
     const scriptPath = buildScriptPath(nextPrompt.file, nextPrompt.section);
 
     setPromptRoundRunningState({ isRichUiEnabled, promptLabel, scriptPath, uiHandle });
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index e8a19f01d..dbfebbae1 100644
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -43,6 +43,10 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
 
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
+    command.option(
+        '--agent <agent-book-path>',
+        'Path to a .book file whose compiled system message is prepended to each coding prompt',
+    );
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -80,9 +84,10 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
 
     command.action(
         handleActionErrors(async (cliOptions) => {
-            const { dryRun, context, test, preserveLogs, priority, wait, autoMigrate, allowDestructiveAutoMigrate } =
+            const { dryRun, agent, context, test, preserveLogs, priority, wait, autoMigrate, allowDestructiveAutoMigrate } =
                 cliOptions as {
                     readonly dryRun: boolean;
+                    readonly agent?: string;
                     readonly context?: string;
                     readonly test?: string | string[];
                     readonly preserveLogs: boolean;
@@ -119,6 +124,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
                 ignoreGitChanges: runnerOptions.ignoreGitChanges,
                 agentName: runnerOptions.agentName,
                 model: runnerOptions.model,
+                agent,
                 context,
                 testCommand,
                 preserveLogs,
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index 6f163a2f8..2805b8ab9 100644
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -59,6 +59,10 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
     );
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
+    command.option(
+        '--agent <agent-book-path>',
+        'Path to a .book file whose compiled system message is prepended to each coding prompt',
+    );
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -95,10 +99,11 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
 
     command.action(
         handleActionErrors(async (cliOptions) => {
-            const { port: rawPort, dryRun, context, test, preserveLogs, priority, wait, autoMigrate, allowDestructiveAutoMigrate } =
+            const { port: rawPort, dryRun, agent, context, test, preserveLogs, priority, wait, autoMigrate, allowDestructiveAutoMigrate } =
                 cliOptions as {
                     readonly port: string;
                     readonly dryRun: boolean;
+                    readonly agent?: string;
                     readonly context?: string;
                     readonly test?: string | string[];
                     readonly preserveLogs: boolean;
@@ -133,6 +138,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
                 ignoreGitChanges: runnerOptions.ignoreGitChanges,
                 agentName: runnerOptions.agentName,
                 model: runnerOptions.model,
+                agent,
                 context,
                 testCommand,
                 preserveLogs,
718824921 [✨😇] Allow to specify an agent for `ptbk coder run` - manual enhance of the prompt
 scripts/run-codex-prompts/main/runPromptRound.ts | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)
commit 71882492169351f86d885f74c803c2fee5c994cb
Author:     Pavol Hejny <me@pavolhejny.com>
AuthorDate: Mon Jun 22 17:13:07 2026 +0200
Commit:     Pavol Hejny <me@pavolhejny.com>
CommitDate: Mon Jun 22 17:13:07 2026 +0200

    [✨😇] Allow to specify an agent for `ptbk coder run` - manual enhance of the prompt

diff --git a/scripts/run-codex-prompts/main/runPromptRound.ts b/scripts/run-codex-prompts/main/runPromptRound.ts
index 21ce95c4c..647da0d42 100644
--- a/scripts/run-codex-prompts/main/runPromptRound.ts
+++ b/scripts/run-codex-prompts/main/runPromptRound.ts
@@ -1,18 +1,18 @@
 import colors from 'colors';
 import moment from 'moment';
+import type { RunOptions } from '../cli/RunOptions';
+import { appendCoderContext } from '../common/appendCoderContext';
+import type { CliProgressDisplay } from '../common/cliProgressDisplay';
+import type { WaitForCoderRunPauseCheckpoint } from '../common/CoderRunPauseCheckpoint';
+import { formatCommitMessageForDisplay } from '../common/formatCommitMessageForDisplay';
 import {
     captureChangedFilesSnapshot,
     normalizeLineEndingsInFilesChangedSinceSnapshot,
     type ChangedFilesSnapshot,
 } from '../common/normalizeLineEndingsInChangedFiles';
-import { formatCommitMessageForDisplay } from '../common/formatCommitMessageForDisplay';
 import { printCommitMessage } from '../common/printCommitMessage';
-import { appendCoderContext } from '../common/appendCoderContext';
-import type { WaitForCoderRunPauseCheckpoint } from '../common/CoderRunPauseCheckpoint';
 import { withPromptRuntimeLog } from '../common/runGoScript/withPromptRuntimeLog';
 import { waitForEnter } from '../common/waitForEnter';
-import type { CliProgressDisplay } from '../common/cliProgressDisplay';
-import type { RunOptions } from '../cli/RunOptions';
 import { commitChanges } from '../git/commitChanges';
 import { runAutoMigrateTestingServers } from '../migrations/runAutoMigrateTestingServers';
 import { buildCodexPrompt } from '../prompts/buildCodexPrompt';
@@ -69,7 +69,7 @@ export async function runPromptRound({
     const taskPrompt = buildCodexPrompt(nextPrompt.file, nextPrompt.section);
     // Prepend agent system message before the task so the harness sees agent instructions first
     const promptWithAgent = resolvedAgentSystemMessage
-        ? `${resolvedAgentSystemMessage.trim()}\n\n${taskPrompt}`
+        ? `${resolvedAgentSystemMessage.trim()}\n\n## Your Task\n\n${taskPrompt}`
         : taskPrompt;
     const codexPrompt = appendCoderContext(promptWithAgent, resolvedCoderContext);
     const scriptPath = buildScriptPath(nextPrompt.file, nextPrompt.section);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!node_modules'"' --glob '"'!prompts/2026-09-0210-ptbk-coder-by-agent.md'"' \"Both "'`agents/coding/developer'"\\.book"'`|Developer Foo bar|Alongside the `developer` add there `unslopper`|Allowto combine" prompts changelog .' in C:\Users\me\work\ai\promptbook
 exited 1 in 576ms:
ParserError: 
Line |
   2 |  … ook`|Developer Foo bar|Alongside the `developer` add there `unslopper …
     |                                                               ~~
     | The Unicode escape sequence is not valid. A valid sequence is `u{ followed by one to six hex digits and a
     | closing '}'.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts; Get-Content scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.test.ts; Get-Content scripts/run-codex-prompts/prompts/parsePromptRunnerFilters.ts; rg -n --hidden --glob '"'!node_modules'"' \"PromptRunnerIdentity|agentName.*modelName|harnessName.*modelName|runner.*filter|model.*harness\" scripts/run-codex-prompts/prompts src/cli/cli-commands/coder | Select-Object -First 400" in C:\Users\me\work\ai\promptbook
 succeeded in 564ms:
import { normalizeToKebabCase } from '../../../src/utils/normalization/normalize-to-kebab-case';
import type { PromptFile } from './types/PromptFile';
import type { PromptSection } from './types/PromptSection';

/**
 * Names of the harness and model currently selected for a coder run.
 */
export type PromptRunnerIdentity = {
    /**
     * Stable harness identifier, for example `github-copilot`.
     */
    readonly harnessName?: string;
    /**
     * Effective model identifier, for example `gpt-5.5`.
     */
    readonly modelName?: string;
};

/**
 * Checks whether a prompt is unrestricted or matches the selected harness/model.
 *
 * A prompt status line can contain one or more backtick-delimited model or harness
 * names. Matching is intentionally based on normalized substrings so a token such as
 * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
 */
export function isPromptCompatibleWithRunner(
    file: PromptFile,
    section: PromptSection,
    promptRunnerIdentity?: PromptRunnerIdentity,
): boolean {
    if (promptRunnerIdentity === undefined) {
        return true;
    }

    const statusLine = section.statusLineIndex === undefined ? undefined : file.lines[section.statusLineIndex];
    const requiredRunnerTokens = statusLine === undefined ? [] : extractPromptRunnerTokens(statusLine);

    if (requiredRunnerTokens.length === 0) {
        return true;
    }

    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
        .filter((name): name is string => name !== undefined && name.trim() !== '')
        .map((name) => normalizeToKebabCase(name))
        .filter((name) => name !== '');

    return requiredRunnerTokens.some((requiredRunnerToken) => {
        const normalizedRequiredRunnerToken = normalizeToKebabCase(requiredRunnerToken);

        return (
            normalizedRequiredRunnerToken !== '' &&
            normalizedRunnerNames.some((normalizedRunnerName) =>
                normalizedRunnerName.includes(normalizedRequiredRunnerToken),
            )
        );
    });
}

/**
 * Extracts model and harness tokens from a prompt status line.
 */
export function extractPromptRunnerTokens(statusLine: string): string[] {
    return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
        .map((match) => match[1]?.trim() ?? '')
        .filter((token) => token !== '');
}
Get-Content: 
Line |
   2 |  … hRunner.ts; Get-Content scripts/run-codex-prompts/prompts/isPromptCom …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\prompts\isPromptCompatibleWithRunner.test.ts' because it does not exist.
Get-Content: 
Line |
   2 |  … er.test.ts; Get-Content scripts/run-codex-prompts/prompts/parsePrompt …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\prompts\parsePromptRunnerFilters.ts' because it does not exist.
scripts/run-codex-prompts/prompts\findNextTodoPrompt.ts:3:import type { PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
scripts/run-codex-prompts/prompts\findNextTodoPrompt.ts:13:    promptRunnerIdentity?: PromptRunnerIdentity,
scripts/run-codex-prompts/prompts\isPromptCompatibleWithRunner.ts:8:export type PromptRunnerIdentity = {
scripts/run-codex-prompts/prompts\isPromptCompatibleWithRunner.ts:22: * A prompt status line can contain one or more backtick-delimited model or harness
scripts/run-codex-prompts/prompts\isPromptCompatibleWithRunner.ts:29:    promptRunnerIdentity?: PromptRunnerIdentity,
scripts/run-codex-prompts/prompts\isPromptCompatibleWithRunner.ts:42:    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
scripts/run-codex-prompts/prompts\isPromptCompatibleWithRunner.ts:60: * Extracts model and harness tokens from a prompt status line.
scripts/run-codex-prompts/prompts\listRunnablePrompts.ts:4:import { isPromptCompatibleWithRunner, type PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
scripts/run-codex-prompts/prompts\listRunnablePrompts.ts:15:    promptRunnerIdentity?: PromptRunnerIdentity,
scripts/run-codex-prompts/prompts\listUpcomingTasks.ts:5:import type { PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
scripts/run-codex-prompts/prompts\listUpcomingTasks.ts:15:    promptRunnerIdentity?: PromptRunnerIdentity,
scripts/run-codex-prompts/prompts\parsePromptFile.ts:102:    // Todo [ ] may contain backtick-delimited model/harness tokens and priority markers
scripts/run-codex-prompts/prompts\writePromptStatusLine.ts:11: * The complete line is replaced because a todo status can contain a required model/harness token
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:6:describe('prompt runner filtering', () => {
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:7:    it('matches model, model family, and harness tokens while preserving priority markers', () => {
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:9:            'prompts/runner-filtering.md',
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:46:            findNextTodoPrompt([file], {}, { harnessName: 'github-copilot', modelName: 'gpt-5.5' })?.section.index,
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:52:            'prompts/runner-filtering.md',
scripts/run-codex-prompts/prompts\promptRunnerFiltering.test.ts:85:            'prompts/runner-filtering.md',

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts; Get-Content scripts/run-codex-prompts/prompts/parsePromptFile.ts | Select-Object -First 145; Get-Content scripts/run-codex-prompts/prompts/writePromptStatusLine.ts; Get-Content scripts/run-codex-prompts/prompts/listRunnablePrompts.ts; Get-Content scripts/run-codex-prompts/prompts/findNextTodoPrompt.ts; Get-Content scripts/run-codex-prompts/prompts/listUpcomingTasks.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 377ms:
import { spaceTrim } from 'spacetrim';
import { findNextTodoPrompt } from './findNextTodoPrompt';
import { listRunnablePrompts } from './listRunnablePrompts';
import { parsePromptFile } from './parsePromptFile';

describe('prompt runner filtering', () => {
    it('matches model, model family, and harness tokens while preserving priority markers', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`gpt-5.5\`
                Exact model
                ---
                [ ] use model \`gpt-5.5\`
                Exact model with wording
                ---
                [ ] !!!! \`gpt\`
                Model family
                ---
                [ ] use \`github-copilot\` !!!!!
                Harness
                ---
                [ ] use \`claude-opus\` or \`github-copilot\`
                One of multiple tokens
                ---
                [ ] use \`claude-opus\`
                Different model family
                ---
                [ ]
                Unrestricted
            `),
        );

        const runnable = listRunnablePrompts(
            [file],
            {},
            {
                harnessName: 'github-copilot',
                modelName: 'gpt-5.5',
            },
        );

        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 6]);
        expect(runnable.map(({ section }) => section.priority)).toEqual([0, 0, 4, 5, 0, 0]);
        expect(
            findNextTodoPrompt([file], {}, { harnessName: 'github-copilot', modelName: 'gpt-5.5' })?.section.index,
        ).toBe(3);
    });

    it('matches normalized model families and aliases dynamically', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`CLAUDE\`
                Claude family
                ---
                [ ] \`claude-opus\`
                Claude Opus family
                ---
                [ ] \`opus\`
                Opus alias
                ---
                [ ] \`claude-opus-4.8\`
                Exact normalized model
                ---
                [ ] \`gpt\`
                Different family
            `),
        );

        const runnable = listRunnablePrompts(
            [file],
            {},
            {
                harnessName: 'claude-code',
                modelName: 'claude-opus-4.8',
            },
        );

        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
    });

    it('does not constrain prompts when no runner identity is provided', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`gpt\`
                Model-specific task
                ---
                [ ]
                General task
            `),
        );

        expect(listRunnablePrompts([file])).toHaveLength(2);
    });
});
import { basename } from 'path';
import { findFirstNonEmptyLineIndex } from './findFirstNonEmptyLineIndex';
import { extractPromptRunnerTokens } from './isPromptCompatibleWithRunner';
import type { PromptFile } from './types/PromptFile';
import type { PromptSection } from './types/PromptSection';
import type { PromptStatus } from './types/PromptStatus';

/**
 * Matches the supported prompt status markers at the start of a line.
 *
 * A different bracketed prefix can be ordinary prompt content, for example an emoji tag.
 *
 * @private internal constant of `parsePromptFile`
 */
const PROMPT_STATUS_LINE_PATTERN = /^\[(?<marker> |-|\.|[xX]|\^|!)\](?<details>.*)$/u;

/**
 * Parses a prompt markdown file into sections and metadata.
 */
export function parsePromptFile(filePath: string, content: string): PromptFile {
    const eol = content.includes('\r\n') ? '\r\n' : '\n';
    const hasFinalEol = content.endsWith('\n');
    const lines = content.split(/\r?\n/);
    const sections: PromptSection[] = [];

    let startLine = 0;
    let index = 0;

    for (let i = 0; i <= lines.length; i++) {
        const line = lines[i];
        const isSeparator = i < lines.length && line !== undefined && line.trim() === '---';
        const isEnd = i === lines.length;
        if (!isSeparator && !isEnd) {
            continue;
        }

        const endLine = i - 1;
        const firstNonEmptyLine = findFirstNonEmptyLineIndex(lines, startLine, endLine);
        if (firstNonEmptyLine !== undefined) {
            const statusLine = (lines[firstNonEmptyLine] || '').trim();
            const parsedStatus = parseStatusLine(statusLine);
            const status = parsedStatus?.status ?? (PROMPT_STATUS_LINE_PATTERN.test(statusLine) ? 'not-ready' : 'todo');
            const priority = parsedStatus?.priority ?? 0;

            sections.push({
                index,
                startLine,
                endLine,
                status,
                priority,
                statusLineIndex: parsedStatus ? firstNonEmptyLine : undefined,
            });
            index += 1;
        }

        startLine = i + 1;
    }

    return {
        path: filePath,
        name: basename(filePath),
        lines,
        eol,
        hasFinalEol,
        sections,
    };
}

/**
 * Parses a status line like "[ ] !!", "[ ] use `gpt` !!!!!" or "[-]" into status and priority.
 * For [x] done, [!] failed and [^] in-progress prompts, allow metadata after the status marker.
 */
function parseStatusLine(line: string): { status: PromptStatus; priority: number } | undefined {
    const statusLineMatch = line.match(PROMPT_STATUS_LINE_PATTERN);
    const marker = statusLineMatch?.groups?.marker;
    const details = statusLineMatch?.groups?.details ?? '';

    if (marker === 'x' || marker === 'X') {
        // For done prompts [x], allow any content after (for cost/time metadata).
        return { status: 'done', priority: 0 };
    }

    if (marker === '!') {
        // For failed prompts [!], allow any content after (for failure metadata).
        return { status: 'failed', priority: 0 };
    }

    if (marker === '^') {
        // For in-progress prompts [^], allow any content after (for the steps recorded so far).
        return { status: 'in-progress', priority: 0 };
    }

    // Treat [.] as the alternative spelling of not-ready [-], preserving the historical clean-line syntax.
    if ((marker === '-' || marker === '.') && /^[!\s]*$/u.test(details)) {
        return { status: 'not-ready', priority: 0 };
    }

    if (marker !== ' ') {
        return undefined;
    }

    // Todo [ ] may contain backtick-delimited model/harness tokens and priority markers
    // before or after those tokens. Other trailing text remains an invalid status line.
    const isPriorityOnly = /^[!\s]*$/u.test(details);
    const hasPromptRunnerTokens = extractPromptRunnerTokens(line).length > 0;

    if (details.trim() !== '' && !isPriorityOnly && !hasPromptRunnerTokens) {
        return undefined;
    }

    return { status: 'todo', priority: details.match(/!/gu)?.length ?? 0 };
}
import { spaceTrim } from 'spacetrim';
import { UnexpectedError } from '../../../src/errors/UnexpectedError';
import { findFirstNonEmptyLineIndex } from './findFirstNonEmptyLineIndex';
import { resolvePromptStatusLine } from './resolvePromptStatusLine';
import type { PromptFile } from './types/PromptFile';
import type { PromptSection } from './types/PromptSection';

/**
 * Matches a complete todo `[ ]` or in-progress `[^]` status line, capturing its indentation.
 *
 * The complete line is replaced because a todo status can contain a required model/harness token
 * in addition to its priority markers, and an in-progress status already carries the metadata of
 * the steps recorded so far.
 */
const REWRITABLE_PROMPT_STATUS_LINE_PATTERN = /^(?<indentation>\s*)\[(?:\s*|\^)\].*$/u;

/**
 * Rewrites the status line of one prompt section while preserving its indentation.
 *
 * An unmarked prompt is implicitly a todo, so its first live status line is inserted immediately before its content.
 * Only a todo `[ ]` or an in-progress `[^]` status line is otherwise rewritten, so an already finalized `[x]`, `[!]`
 * or `[-]` status is never overwritten by accident.
 */
export function writePromptStatusLine(file: PromptFile, section: PromptSection, replacementStatusLine: string): void {
    if (section.statusLineIndex === undefined) {
        insertPromptStatusLine(file, section, replacementStatusLine);
        return;
    }

    const { statusLineIndex, line } = resolvePromptStatusLine(file, section);

    file.lines[statusLineIndex] = line.replace(
        REWRITABLE_PROMPT_STATUS_LINE_PATTERN,
        `$<indentation>${replacementStatusLine}`,
    );
}

/**
 * Inserts the first persisted status line for an unmarked prompt and keeps parsed section positions in sync.
 *
 * @private internal utility of `writePromptStatusLine`
 */
function insertPromptStatusLine(file: PromptFile, section: PromptSection, replacementStatusLine: string): void {
    const statusLineIndex = findFirstNonEmptyLineIndex(file.lines, section.startLine, section.endLine);

    if (statusLineIndex === undefined) {
        const promptReference = `Prompt ${section.index + 1} in \`${file.name}\``;

        throw new UnexpectedError(
            spaceTrim(`
                ${promptReference} does not have content where its status line can be added.
            `),
        );
    }

    file.lines.splice(statusLineIndex, 0, replacementStatusLine);
    updatePromptSectionPositionsAfterStatusInsertion(file, section, statusLineIndex);
}

/**
 * Updates the parsed indexes after adding a status line to an unmarked prompt.
 *
 * @private internal utility of `writePromptStatusLine`
 */
function updatePromptSectionPositionsAfterStatusInsertion(
    file: PromptFile,
    section: PromptSection,
    statusLineIndex: number,
): void {
    for (const promptSection of file.sections) {
        if (promptSection === section) {
            promptSection.statusLineIndex = statusLineIndex;
            promptSection.endLine += 1;
            continue;
        }

        if (promptSection.startLine >= statusLineIndex) {
            promptSection.startLine += 1;
            promptSection.endLine += 1;

            if (promptSection.statusLineIndex !== undefined) {
                promptSection.statusLineIndex += 1;
            }
        }
    }

    if (!file.sections.includes(section)) {
        section.statusLineIndex = statusLineIndex;
        section.endLine += 1;
    }
}
import type { PromptFile } from './types/PromptFile';
import type { PromptSelection } from './types/PromptSelection';
import { isPromptInPriorityFilter } from './isPromptInPriorityFilter';
import { isPromptCompatibleWithRunner, type PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
import { isPromptToBeWritten } from './isPromptToBeWritten';
import { listTodoPrompts } from './listTodoPrompts';
import type { PriorityFilter } from './priorityFilter';

/**
 * Lists todo prompts that are ready to run (no authoring placeholders).
 */
export function listRunnablePrompts(
    files: PromptFile[],
    priorityFilter: PriorityFilter = {},
    promptRunnerIdentity?: PromptRunnerIdentity,
): PromptSelection[] {
    return listTodoPrompts(files).filter(
        (prompt) =>
            !isPromptToBeWritten(prompt.file, prompt.section) &&
            isPromptInPriorityFilter(prompt.section, priorityFilter) &&
            isPromptCompatibleWithRunner(prompt.file, prompt.section, promptRunnerIdentity),
    );
}
import type { PromptFile } from './types/PromptFile';
import type { PromptSelection } from './types/PromptSelection';
import type { PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
import { listRunnablePrompts } from './listRunnablePrompts';
import type { PriorityFilter } from './priorityFilter';

/**
 * Selects the next runnable prompt based on priority.
 */
export function findNextTodoPrompt(
    files: PromptFile[],
    priorityFilter: PriorityFilter = {},
    promptRunnerIdentity?: PromptRunnerIdentity,
): PromptSelection | undefined {
    let nextPrompt: PromptSelection | undefined;

    for (const prompt of listRunnablePrompts(files, priorityFilter, promptRunnerIdentity)) {
        if (!nextPrompt || prompt.section.priority > nextPrompt.section.priority) {
            nextPrompt = prompt;
        }
    }

    return nextPrompt;
}
import type { PromptFile } from './types/PromptFile';
import type { UpcomingTask } from './types/UpcomingTask';
import { buildPromptLabelForDisplay } from './buildPromptLabelForDisplay';
import { buildPromptSummary } from './buildPromptSummary';
import type { PromptRunnerIdentity } from './isPromptCompatibleWithRunner';
import { listRunnablePrompts } from './listRunnablePrompts';
import type { PriorityFilter } from './priorityFilter';

/**
 * Lists upcoming tasks that are ready to run (no authoring placeholders).
 */
export function listUpcomingTasks(
    files: PromptFile[],
    priorityFilter: PriorityFilter = {},
    promptRunnerIdentity?: PromptRunnerIdentity,
): UpcomingTask[] {
    return listRunnablePrompts(files, priorityFilter, promptRunnerIdentity).map(({ file, section }) => ({
        label: buildPromptLabelForDisplay(file, section),
        summary: buildPromptSummary(file, section),
        priority: section.priority,
    }));
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/cli/cli-commands/coder/list.ts; Get-Content src/cli/cli-commands/coder/list.test.ts; Get-Content src/cli/cli-commands/common/promptRunnerCliOptions.ts | Select-Object -First 400; Get-Content src/cli/cli-commands/coder.ts | Select-Object -First 220' in C:\Users\me\work\ai\promptbook
 succeeded in 721ms:
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { handleActionErrors } from '../common/handleActionErrors';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import type { PromptRunnerSelectionCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerSelectionCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';

/**
 * Initializes `coder list` command for Promptbook CLI utilities.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderListCommand(program: Program): $side_effect {
    const command = program.command('list');
    command.description(
        spaceTrim(`
            List ready coding prompts by priority without executing them

            ${PROMPT_RUNNER_DESCRIPTION}

            Features:
            - Lists only ready, fully authored prompts
            - Groups prompts from highest to lowest priority
            - Optional --harness and --model filters show only prompts compatible with that runner
            - Does not start a coding harness or modify prompt files
        `),
    );

    addPromptRunnerSelectionOptions(command);
    addPromptPriorityOptions(command);

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
            } = cliOptions as {
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
            } & PromptRunnerSelectionCliOptions;
            const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                cliOptions as PromptRunnerSelectionCliOptions,
                { isAgentRequired: false },
            );
            const promptRunnerIdentity =
                runnerOptions.agentName === undefined && runnerOptions.model === undefined
                    ? undefined
                    : {
                          harnessName: runnerOptions.agentName,
                          modelName: runnerOptions.model,
                      };

            // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
            const { listCoderPrompts } = await import('../../../../scripts/run-codex-prompts/main/listCoderPrompts');
            await listCoderPrompts({
                priority,
                minimumPriority,
                maximumPriority,
                promptRunnerIdentity,
            });
        }),
    );
}

// Note: [🟡] Code for CLI command [list](src/cli/cli-commands/coder/list.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
import { Command } from 'commander';
import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
import { $initializeCoderListCommand } from './list';

jest.mock('../../../../scripts/run-codex-prompts/main/listCoderPrompts', () => ({
    listCoderPrompts: jest.fn(),
}));

/**
 * Typed Jest mock for the read-only coder prompt listing entrypoint.
 */
function getListCoderPromptsMock(): jest.MockedFunction<typeof listCoderPrompts> {
    return listCoderPrompts as jest.MockedFunction<typeof listCoderPrompts>;
}

/**
 * Creates a Commander program with the `coder list` subcommand registered.
 */
function createProgramWithListCommand(): Command {
    const program = new Command();
    $initializeCoderListCommand(program);
    return program;
}

describe('$initializeCoderListCommand', () => {
    let processExitSpy: jest.SpyInstance<never, [code?: string | number | null | undefined]>;
    let originalHarnessEnvironmentValue: string | undefined;
    let originalModelEnvironmentValue: string | undefined;

    beforeAll(() => {
        originalHarnessEnvironmentValue = process.env.PTBK_HARNESS;
        originalModelEnvironmentValue = process.env.PTBK_MODEL;
        delete process.env.PTBK_HARNESS;
        delete process.env.PTBK_MODEL;
    });

    beforeEach(() => {
        getListCoderPromptsMock().mockResolvedValue(0);
        processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
    });

    afterEach(() => {
        processExitSpy.mockRestore();
        jest.clearAllMocks();
    });

    afterAll(() => {
        if (originalHarnessEnvironmentValue === undefined) {
            delete process.env.PTBK_HARNESS;
        } else {
            process.env.PTBK_HARNESS = originalHarnessEnvironmentValue;
        }

        if (originalModelEnvironmentValue === undefined) {
            delete process.env.PTBK_MODEL;
        } else {
            process.env.PTBK_MODEL = originalModelEnvironmentValue;
        }
    });

    it('lists prompts without requiring a harness', async () => {
        const program = createProgramWithListCommand();

        await program.parseAsync(['node', 'test', 'list'], { from: 'node' });

        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                promptRunnerIdentity: undefined,
            }),
        );
    });

    it('passes harness, model, and inclusive priority filters to the prompt listing', async () => {
        const program = createProgramWithListCommand();

        await program.parseAsync(
            [
                'node',
                'test',
                'list',
                '--harness',
                'github-copilot',
                '--model',
                'gpt-5.5',
                '--min-priority',
                '2',
                '--max-priority',
                '4',
            ],
            { from: 'node' },
        );

        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                minimumPriority: 2,
                maximumPriority: 4,
                promptRunnerIdentity: {
                    harnessName: 'github-copilot',
                    modelName: 'gpt-5.5',
                },
            }),
        );
    });

    it('passes --priority as the legacy minimum-priority alias', async () => {
        const program = createProgramWithListCommand();

        await program.parseAsync(['node', 'test', 'list', '--priority', '3'], { from: 'node' });

        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
            expect.objectContaining({
                priority: 3,
            }),
        );
    });
});
import colors from 'colors';
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import {
    CLI_AGENT_HARNESS_NAMES,
    PTBK_HARNESS_ENV,
    PTBK_MODEL_ENV,
    PTBK_THINKING_LEVEL_ENV,
} from '../../../book-3.0/cliAgentEnv';
import type { GitChangesMode } from '../coder/GitChangesMode';
import { DEFAULT_GIT_CHANGES_MODE, GIT_CHANGES_MODE_VALUES } from '../coder/GitChangesMode';
import type { ThinkingLevel } from '../coder/ThinkingLevel';
import { THINKING_LEVEL_VALUES } from '../coder/ThinkingLevel';

export { PTBK_HARNESS_ENV, PTBK_MODEL_ENV, PTBK_THINKING_LEVEL_ENV };

/**
 * Runner identifiers supported by Promptbook CLI agent orchestration commands.
 *
 * @private internal utility of `promptbookCli`
 */
export const PROMPT_RUNNER_HARNESS_NAMES = CLI_AGENT_HARNESS_NAMES;

/**
 * Runner identifier supported by Promptbook CLI agent orchestration commands.
 *
 * @private internal utility of `promptbookCli`
 */
export type PromptRunnerHarnessName = (typeof PROMPT_RUNNER_HARNESS_NAMES)[number];

/**
 * Commander option bag for shared runner flags.
 *
 * @private internal utility of `promptbookCli`
 */
export type PromptRunnerCliOptions = {
    readonly harness?: string;
    readonly model?: string;
    readonly ui: boolean;
    readonly thinkingLevel?: ThinkingLevel;
    readonly commit: boolean;
    readonly gitChanges: GitChangesMode;
    readonly allowCredits: boolean;
    readonly normalizeLineEndings: boolean;
    readonly autoPush: boolean;
    readonly autoPull: boolean;
};

/**
 * Commander option bag for runner selection plus terminal/runtime switches shared with server orchestration.
 *
 * @private internal utility of `promptbookCli`
 */
export type PromptRunnerSelectionCliOptions = Pick<
    PromptRunnerCliOptions,
    'harness' | 'model' | 'ui' | 'thinkingLevel' | 'allowCredits'
>;

/**
 * Normalized runner options used by runner-backed CLI commands.
 *
 * @private internal utility of `promptbookCli`
 */
export type NormalizedPromptRunnerCliOptions = {
    readonly agentName?: PromptRunnerHarnessName;
    readonly model?: string;
    readonly noUi: boolean;
    readonly thinkingLevel?: ThinkingLevel;
    readonly noCommit: boolean;
    readonly gitChanges: GitChangesMode;
    readonly allowCredits: boolean;
    readonly normalizeLineEndings: boolean;
    readonly autoPush: boolean;
    readonly autoPull: boolean;
};

/**
 * Normalized runner selection plus terminal/runtime switches shared with server orchestration.
 *
 * @private internal utility of `promptbookCli`
 */
export type NormalizedPromptRunnerSelectionCliOptions = Pick<
    NormalizedPromptRunnerCliOptions,
    'agentName' | 'model' | 'noUi' | 'thinkingLevel' | 'allowCredits'
>;

/**
 * Description block shared by runner-backed CLI commands.
 *
 * @private internal utility of `promptbookCli`
 */
export const PROMPT_RUNNER_DESCRIPTION = spaceTrim(`
    Runners:
    - openai-codex: OpenAI Codex integration (requires --model when executing)
    - github-copilot: GitHub Copilot CLI integration
    - cline: Cline CLI integration
    - claude-code: Claude Code integration
    - opencode: Opencode integration
    - gemini: Google Gemini CLI integration (requires --model when executing)
    - qwen-code: Qwen Code CLI integration (requires --model when executing)
`);

/**
 * Commander description for the `--harness` option.
 *
 * @private internal utility of `promptbookCli`
 */
export const PROMPT_RUNNER_HARNESS_OPTION_DESCRIPTION = `Select runner: ${PROMPT_RUNNER_HARNESS_NAMES.join(
    ', ',
)} (required when executing prompts)`;

/**
 * Runner harness names listed as alternatives of the `--harness` option in error messages.
 *
 * @private internal utility of `promptbookCli`
 */
export const PROMPT_RUNNER_HARNESS_OPTION_HINT = `--harness <${PROMPT_RUNNER_HARNESS_NAMES.join('|')}>`;

/**
 * Commander description for the `--model` option.
 *
 * @private internal utility of `promptbookCli`
 */
export const PROMPT_RUNNER_MODEL_OPTION_DESCRIPTION = spaceTrim(`
    Model to use or filter by (required when executing with openai-codex, gemini and qwen-code)

    OpenAI examples: gpt-5.2-codex, default
    Gemini examples: gemini-3-flash-preview, default
    Qwen examples: qwen3.8-max, default

    For openai-codex, "default" overrides no model at all and keeps the one configured in Codex itself, which is what a ChatGPT-account login accepts
`);

/**
 * Commander description for the `--git-changes` option.
 *
 * @private internal utility of `promptbookCli`
 */
export const GIT_CHANGES_OPTION_DESCRIPTION = spaceTrim(`
    Decide what happens when the working tree has uncommitted changes before a prompt starts:
    - fail: refuse to start and ask for a commit or a stash (the default)
    - ignore: start anyway and leave the uncommitted changes where they are
    - continue: resume the single prompt which was left in the middle of its implementation
`);

/**
 * Registers runner selection flags on a command.
 *
 * @private internal utility of `promptbookCli`
 */
export function addPromptRunnerSelectionOptions(command: Program): void {
    command.addOption(
        new Option('--harness <harness-name>', PROMPT_RUNNER_HARNESS_OPTION_DESCRIPTION).env(PTBK_HARNESS_ENV),
    );
    command.addOption(new Option('--model <model>', PROMPT_RUNNER_MODEL_OPTION_DESCRIPTION).env(PTBK_MODEL_ENV));
}

/**
 * Registers shared runner terminal/runtime flags on a command.
 *
 * @private internal utility of `promptbookCli`
 */
export function addPromptRunnerRuntimeOptions(command: Program): void {
    command.option(
        '--no-ui',
        'Disable the rich terminal UI and keep plain streaming console output for logging and debugging',
    );
    command.addOption(
        new Option(
            '--thinking-level <thinking-level>',
            `Set reasoning effort for supported runners (${THINKING_LEVEL_VALUES.join(', ')})`,
        )
            .choices([...THINKING_LEVEL_VALUES])
            .env(PTBK_THINKING_LEVEL_ENV),
    );
    command.option(
        '--allow-credits',
        'Allow OpenAI Codex runner to spend credits when rate limits are exhausted',
        false,
    );
}

/**
 * Registers shared runner execution flags on a command.
 *
 * @private internal utility of `promptbookCli`
 */
export function addPromptRunnerExecutionOptions(command: Program): void {
    addPromptRunnerRuntimeOptions(command);
    command.option('--no-commit', 'Leave successful changes in the working directory instead of creating git commits');
    command.addOption(
        new Option('--git-changes <mode>', GIT_CHANGES_OPTION_DESCRIPTION)
            .choices([...GIT_CHANGES_MODE_VALUES])
            .default(DEFAULT_GIT_CHANGES_MODE),
    );
    command.option(
        '--no-normalize-line-endings',
        'Disable automatic LF normalization for files changed in each coding round',
    );
    command.option('--auto-push', 'Automatically git push after each commit', false);
    command.option('--auto-pull', 'Automatically git pull before the first and each subsequent prompt', false);
}

/**
 * Converts Commander runner flags into normalized runner options.
 *
 * @private internal utility of `promptbookCli`
 */
export function normalizePromptRunnerCliOptions(
    cliOptions: PromptRunnerCliOptions,
    options: {
        readonly isAgentRequired: boolean;
    },
): NormalizedPromptRunnerCliOptions {
    const selectionOptions = normalizePromptRunnerSelectionCliOptions(cliOptions, options);

    return {
        ...selectionOptions,
        noCommit: !cliOptions.commit,
        gitChanges: cliOptions.gitChanges ?? DEFAULT_GIT_CHANGES_MODE,
        normalizeLineEndings: cliOptions.normalizeLineEndings,
        autoPush: cliOptions.autoPush,
        autoPull: cliOptions.autoPull,
    };
}

/**
 * Converts Commander runner selection/runtime flags into the normalized runner shape.
 *
 * @private internal utility of `promptbookCli`
 */
export function normalizePromptRunnerSelectionCliOptions(
    cliOptions: PromptRunnerSelectionCliOptions,
    options: {
        readonly isAgentRequired: boolean;
    },
): NormalizedPromptRunnerSelectionCliOptions {
    return {
        agentName: resolvePromptRunnerHarnessName(cliOptions.harness, options),
        model: cliOptions.model,
        noUi: !cliOptions.ui,
        thinkingLevel: cliOptions.thinkingLevel,
        allowCredits: cliOptions.allowCredits,
    };
}

/**
 * Parses and validates one runner harness name.
 */
function resolvePromptRunnerHarnessName(
    harness: string | undefined,
    options: {
        readonly isAgentRequired: boolean;
    },
): PromptRunnerHarnessName | undefined {
    if (!harness) {
        if (!options.isAgentRequired) {
            return undefined;
        }

        throw new Error(colors.red(`You must choose a harness using ${PROMPT_RUNNER_HARNESS_OPTION_HINT}`));
    }

    if (isPromptRunnerHarnessName(harness)) {
        return harness;
    }

    throw new Error(
        colors.red(`Invalid harness "${harness}". Must be one of: ${PROMPT_RUNNER_HARNESS_NAMES.join(', ')}`),
    );
}

/**
 * Checks whether a string is one supported runner harness name.
 */
function isPromptRunnerHarnessName(harness: string): harness is PromptRunnerHarnessName {
    return PROMPT_RUNNER_HARNESS_NAMES.includes(harness as PromptRunnerHarnessName);
}

// Note: [🟡] Code for CLI runner options [promptRunnerCliOptions](src/cli/cli-commands/common/promptRunnerCliOptions.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../utils/organization/$side_effect';
import { $requireCliSubcommand } from '../common/$requireCliSubcommand';
import { $initializeCoderAddCommand } from './coder/add';
import { $initializeCoderFindFreshEmojiTagCommand } from './coder/find-fresh-emoji-tags';
import { $initializeCoderFindRefactorCandidatesCommand } from './coder/find-refactor-candidates';
import { $initializeCoderFindUnwrittenCommand } from './coder/find-unwritten';
import { $initializeCoderGenerateBoilerplatesCommand } from './coder/generate-boilerplates';
import { $initializeCoderInitCommand } from './coder/init';
import { $initializeCoderListCommand } from './coder/list';
import { $initializeCoderPingCommand } from './coder/ping';
import { $initializeCoderRunCommand } from './coder/run';
import { $initializeCoderServerCommand } from './coder/server';
import { $initializeCoderVerifyCommand } from './coder/verify';

/**
 * Initializes `coder` command with subcommands for Promptbook CLI utilities
 *
 * The coder command provides utilities for automated coding:
 * - init: Initialize coder configuration in current project
 * - add: Add one ready-to-run prompt file to the queue
 * - generate-boilerplates: Generate prompt boilerplate files
 * - find-refactor-candidates: Find files that need refactoring
 * - list: List ready prompts in priority order without running them
 * - run: Run coding prompts with AI agents
 * - ping: Test one harness and model with a tiny dummy prompt
 * - verify: Verify completed prompts
 * - find-fresh-emoji-tags: Find unused emoji tags
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderCommand(program: Program): $side_effect {
    const coderCommand = program.command('coder');
    coderCommand.description(
        spaceTrim(`
            Coding utilities for automated development workflows

            Subcommands:
            - init: Initialize coder configuration in current project
            - add: Add one ready-to-run prompt file to the queue
            - generate-boilerplates: Generate prompt boilerplate files
            - find-refactor-candidates: Find files that need refactoring
            - find-unwritten: List prompt sections that still need to be authored
            - list: List ready prompts in priority order without running them
            - run: Run coding prompts with AI agents
            - ping: Test the connection, response time and quota of one harness and model
            - server: Start a long-running coder server with a kanban web UI
            - verify: Verify completed prompts
            - find-fresh-emoji-tags: Find unused emoji tags
        `),
    );

    // Register all subcommands
    $initializeCoderInitCommand(coderCommand);
    $initializeCoderAddCommand(coderCommand);
    $initializeCoderGenerateBoilerplatesCommand(coderCommand);
    $initializeCoderFindRefactorCandidatesCommand(coderCommand);
    $initializeCoderFindUnwrittenCommand(coderCommand);
    $initializeCoderListCommand(coderCommand);
    $initializeCoderRunCommand(coderCommand);
    $initializeCoderPingCommand(coderCommand);
    $initializeCoderServerCommand(coderCommand);
    $initializeCoderVerifyCommand(coderCommand);
    $initializeCoderFindFreshEmojiTagCommand(coderCommand);

    // If no subcommand is provided, show help
    $requireCliSubcommand(coderCommand);
}

// Note: [🟡] Code for CLI command [coder](src/cli/cli-commands/coder.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git log --oneline --all -- scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts; git log -p --max-count=5 -- scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts; git show --format=fuller --stat cfb986a7a; git show --format=fuller cfb986a7a -- scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts src/cli/cli-commands/coder/list.ts changelog/_current-preversion.md' in C:\Users\me\work\ai\promptbook
 succeeded in 1037ms:
240272096 [✨🚭] Allow to specify required model for each prompt
commit 240272096038e03141502a7c8ee00a7b7f90dacd
Author: Promptbook Coding Agent <me+agent@pavolhejny.com>
Date:   Mon Aug 3 02:11:14 2026 +0200

    [✨🚭] Allow to specify required model for each prompt
    
    **For example when you run:**
    
    ```bash
    ptbk coder run --harness github-copilot --model gpt-5.5 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md
    ```
    
    **This prompt will run:**
    
    ```markdown
    [ ] use `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use model `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] !!!! `gpt`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use `github-copilot` !!!!!
    
    [✨𓀍] Do some work
    ```
    
    **But this will be ignored:**
    
    ```markdown
    [ ] use `claude-opus`
    
    [✨𓀍] Do some work
    ```
    
    -   In the `[ ]` section of the prompt to be done allowed to specify the required model or harness for this prompt
    -   Allow to combine the model / harness name in the `[ ]` section with the priority !!!!!
    -   you can specify either model or harness or just family of the models. For example:
        -   `gpt` - any model from the gpt family
        -   `github-copilot` - the harness
        -   `claude` - any model from the claude family
        -   `claude-opus` - any model from the claude-opus family
        -   `opus` - any model from the claude-opus family
        -   this shouldn't be hardcoded. The matching from the prompt and the harness or model name should be done dynamically via normalized names
        -   the normalized name of the model or harness is computed from the name of the model or harness by removing all non-alphanumeric characters and lowercasing it. For example:
            -   `gpt-5.5` -> `gpt-5-5`
            -   `claude-opus-4.8` -> `claude-opus-4-8`
            -   `github-copilot` -> `github-copilot`
            -   `claude-opus` -> `claude-opus`
            -   `claude` -> `claude`
            -   Use the normalizing functions which are here in the project, do not implement your own normalizing function
    -   the strategy should work as follows: it should firstly take all the tokens in these `parentheses` in the prompt line
        -   For example: "[ ] use `gpt-5.5`" -> it should take the token `gpt-5.5`
    -   Then it should normalize the name of the model or harness which is used to run this prompt. For example: `gpt-5.5` -> `gpt-5-5`
    -   Then it should normalize the name of the model and harness which is used to run the ptbk coder
    -   Then match all the tokens from the prompt with the normalized name of the model or harness which is used to run this prompt. If any of the tokens matches, then this prompt can be run. If none of the tokens matches, then this prompt should be skipped and not run.
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Update the [`ptbk coder` landing website](apps/coder-landing)
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
new file mode 100644
index 000000000..7cb024b84
--- /dev/null
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -0,0 +1,66 @@
+import { normalizeToKebabCase } from '../../../src/utils/normalization/normalize-to-kebab-case';
+import type { PromptFile } from './types/PromptFile';
+import type { PromptSection } from './types/PromptSection';
+
+/**
+ * Names of the harness and model currently selected for a coder run.
+ */
+export type PromptRunnerIdentity = {
+    /**
+     * Stable harness identifier, for example `github-copilot`.
+     */
+    readonly harnessName?: string;
+    /**
+     * Effective model identifier, for example `gpt-5.5`.
+     */
+    readonly modelName?: string;
+};
+
+/**
+ * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ *
+ * A prompt status line can contain one or more backtick-delimited model or harness
+ * names. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ */
+export function isPromptCompatibleWithRunner(
+    file: PromptFile,
+    section: PromptSection,
+    promptRunnerIdentity?: PromptRunnerIdentity,
+): boolean {
+    if (promptRunnerIdentity === undefined) {
+        return true;
+    }
+
+    const statusLine = section.statusLineIndex === undefined ? undefined : file.lines[section.statusLineIndex];
+    const requiredRunnerTokens = statusLine === undefined ? [] : extractPromptRunnerTokens(statusLine);
+
+    if (requiredRunnerTokens.length === 0) {
+        return true;
+    }
+
+    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+        .filter((name): name is string => name !== undefined && name.trim() !== '')
+        .map((name) => normalizeToKebabCase(name))
+        .filter((name) => name !== '');
+
+    return requiredRunnerTokens.some((requiredRunnerToken) => {
+        const normalizedRequiredRunnerToken = normalizeToKebabCase(requiredRunnerToken);
+
+        return (
+            normalizedRequiredRunnerToken !== '' &&
+            normalizedRunnerNames.some((normalizedRunnerName) =>
+                normalizedRunnerName.includes(normalizedRequiredRunnerToken),
+            )
+        );
+    });
+}
+
+/**
+ * Extracts model and harness tokens from a prompt status line.
+ */
+export function extractPromptRunnerTokens(statusLine: string): string[] {
+    return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
+        .map((match) => match[1]?.trim() ?? '')
+        .filter((token) => token !== '');
+}
commit cfb986a7aca642b37cf4079bf755af99dbfd59e2
Author:     Promptbook Coding Agent <me+agent@pavolhejny.com>
AuthorDate: Wed Sep 9 20:11:36 2026 +0200
Commit:     Promptbook Coding Agent <me+agent@pavolhejny.com>
CommitDate: Wed Sep 9 20:11:36 2026 +0200

    [✨🤛] Implement `ptbk coder list`
    
    ```bash
    ptbk coder list
    ```
    
    -   This command will list all the prompts which should be run by their priority, but do not run them.
    -   Allow specifying harness and model for prompts which are filtered out.
    -   Allow specifying priorities
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   Add new command to [the dev scripts in `terminals.json`](.vscode/terminals.json)
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Update the [`ptbk coder` landing website](apps/coder-landing) to a reflect the changes
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

 .vscode/terminals.json                             |   7 ++
 apps/coder-landing/specs/content/commands.md       |   8 +-
 .../specs/sections/advanced-features.md            |   4 +-
 apps/coder-landing/specs/sections/quickstart.md    |   2 +-
 .../src/components/sections/Quickstart.tsx         |   6 +-
 apps/coder-landing/src/data/advancedFeatures.ts    |   9 +-
 apps/coder-landing/src/data/commands.ts            |   5 +
 changelog/_current-preversion.md                   |   4 +
 prompts/2026-09-0090-ptbk-coder-list.md            |   3 +-
 .../main/listCoderPrompts.test.ts                  | 110 +++++++++++++++++++
 scripts/run-codex-prompts/main/listCoderPrompts.ts |  40 +++++++
 src/cli/cli-commands/coder.ts                      |   4 +
 src/cli/cli-commands/coder/list.test.ts            | 116 +++++++++++++++++++++
 src/cli/cli-commands/coder/list.ts                 |  77 ++++++++++++++
 src/cli/cli-commands/coder/run.ts                  |  18 +---
 src/cli/cli-commands/coder/server.ts               |  18 +---
 .../common/promptPriorityCliOptions.ts             |  30 ++++++
 .../cli-commands/common/promptRunnerCliOptions.ts  |  10 +-
 src/cli/test/ptbk.test.ts                          |   5 +
 19 files changed, 425 insertions(+), 51 deletions(-)
commit cfb986a7aca642b37cf4079bf755af99dbfd59e2
Author:     Promptbook Coding Agent <me+agent@pavolhejny.com>
AuthorDate: Wed Sep 9 20:11:36 2026 +0200
Commit:     Promptbook Coding Agent <me+agent@pavolhejny.com>
CommitDate: Wed Sep 9 20:11:36 2026 +0200

    [✨🤛] Implement `ptbk coder list`
    
    ```bash
    ptbk coder list
    ```
    
    -   This command will list all the prompts which should be run by their priority, but do not run them.
    -   Allow specifying harness and model for prompts which are filtered out.
    -   Allow specifying priorities
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   Add new command to [the dev scripts in `terminals.json`](.vscode/terminals.json)
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Update the [`ptbk coder` landing website](apps/coder-landing) to a reflect the changes
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index def7c15cf..2038dc355 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,7 @@
+-   Added `ptbk coder list`, a read-only queue preview that lists ready, fully authored prompts in descending priority
+    groups without starting a harness. Its optional `--harness`, `--model`, `--priority`, `--min-priority`, and
+    `--max-priority` filters let you preview exactly the queue a selected runner would receive.
+
 -   Added a live GitHub star counter and **Star on GitHub** call to action to the **ptbk coder** landing page.
 
 -   Added support for plain markdown prompts without a status checkbox in **ptbk coder**. `ptbk coder run` and
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
new file mode 100644
index 000000000..b0ca04b05
--- /dev/null
+++ b/src/cli/cli-commands/coder/list.ts
@@ -0,0 +1,77 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+import { spaceTrim } from 'spacetrim';
+import type { $side_effect } from '../../../utils/organization/$side_effect';
+import { handleActionErrors } from '../common/handleActionErrors';
+import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
+import type { PromptRunnerSelectionCliOptions } from '../common/promptRunnerCliOptions';
+import {
+    addPromptRunnerSelectionOptions,
+    normalizePromptRunnerSelectionCliOptions,
+    PROMPT_RUNNER_DESCRIPTION,
+} from '../common/promptRunnerCliOptions';
+
+/**
+ * Initializes `coder list` command for Promptbook CLI utilities.
+ *
+ * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
+ *
+ * @private internal function of `promptbookCli`
+ */
+export function $initializeCoderListCommand(program: Program): $side_effect {
+    const command = program.command('list');
+    command.description(
+        spaceTrim(`
+            List ready coding prompts by priority without executing them
+
+            ${PROMPT_RUNNER_DESCRIPTION}
+
+            Features:
+            - Lists only ready, fully authored prompts
+            - Groups prompts from highest to lowest priority
+            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Does not start a coding harness or modify prompt files
+        `),
+    );
+
+    addPromptRunnerSelectionOptions(command);
+    addPromptPriorityOptions(command);
+
+    command.action(
+        handleActionErrors(async (cliOptions) => {
+            const {
+                priority,
+                minPriority: minimumPriority,
+                maxPriority: maximumPriority,
+            } = cliOptions as {
+                readonly priority?: number;
+                readonly minPriority?: number;
+                readonly maxPriority?: number;
+            } & PromptRunnerSelectionCliOptions;
+            const runnerOptions = normalizePromptRunnerSelectionCliOptions(
+                cliOptions as PromptRunnerSelectionCliOptions,
+                { isAgentRequired: false },
+            );
+            const promptRunnerIdentity =
+                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                    ? undefined
+                    : {
+                          harnessName: runnerOptions.agentName,
+                          modelName: runnerOptions.model,
+                      };
+
+            // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
+            const { listCoderPrompts } = await import('../../../../scripts/run-codex-prompts/main/listCoderPrompts');
+            await listCoderPrompts({
+                priority,
+                minimumPriority,
+                maximumPriority,
+                promptRunnerIdentity,
+            });
+        }),
+    );
+}
+
+// Note: [🟡] Code for CLI command [list](src/cli/cli-commands/coder/list.ts) should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and entity name

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n 'coder (run|list).*agent|Run ptbk coder|unslopper|developer' .vscode/terminals.json; Get-Content agents/coding/unslopper.book | Select-Object -First 30; Get-Content changelog/_current-preversion.md | Select-Object -First 120; git show 240272096 -- .vscode/terminals.json apps/coder-landing/src/data/advancedFeatures.ts apps/coder-landing/specs/sections/advanced-features.md apps/coder-landing/specs/content/commands.md changelog/_current-preversion.md | Select-Object -First 600" in C:\Users\me\work\ai\promptbook
 succeeded in 573ms:
26:            "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
30:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
37:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-sol` Coding Agent (💤)(💸)(⏳)",
41:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
48:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-terra` Coding Agent (💤)(💸)(⏳)",
52:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
59:            "name": "🐙🚀 Run ptbk coder `gpt-5.6-luna` Coding Agent (💤)(💸)(⏳)",
63:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
70:            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Coding Agent (💤)(💸)(⏳)",
74:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
81:            "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
82:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
88:            "name": "🐙🚀 Run ptbk coder `claude-fable` Coding Agent (💤)(💸)(⏳)",
89:            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
95:            "name": "🐙🚀 Run ptbk coder `qwen3.8-max` Coding Agent (💤)(💸)(⏳)",
99:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
106:            "name": "🐙🚀 Run ptbk coder `qwen3.8-flash` Coding Agent (💤)(💸)(⏳)",
110:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
117:            "name": "🐙🚀 Run ptbk coder `deepseek-v4-pro` Coding Agent (💤)(💸)(⏳)",
121:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
128:            "name": "🐙🚀 Run ptbk coder `deepseek-v4-flash` Coding Agent (💤)(💸)(⏳)",
132:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
139:            "name": "🐙🚀 Run ptbk coder `glm-5.2` Coding Agent (💤)(💸)(⏳)",
143:                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
455:            "name": "🧸🔠 Run ptbk coder landing page",
Unslopper

GOAL Edit text so it sounds human, direct, and alive. Remove AI patterns, cut filler, and keep the original meaning and tone.

RULE You are un-slopping text, not for summarizing it.
RULE Work only on text editing and rewriting. Do not add new facts unless the user asks for them.


WRITING RULE Preserve meaning, intent, and audience. If the text is unclear, ask a short clarifying question instead of guessing.
WRITING RULE Prefer plain language. Use short words, concrete nouns, and active voice.
WRITING RULE Remove puffery, hype, vague claims, and empty polish. Say what happened.
WRITING RULE Avoid generic AI phrasing like "Additionally", "crucial", "delve", "underscore", "showcase", and similar filler words unless the user clearly wants that style.
WRITING RULE Avoid "not just X, but Y" framing, forced three-part lists, and synonym cycling.
WRITING RULE Avoid em dashes, curly quotes, decorative emojis, and title case headings in rewritten text unless the source text requires them.
WRITING RULE Keep punctuation simple. Use periods and commas first. Use colons only when they earn their place.
WRITING RULE If the text needs more soul, add a human voice. That can mean a clear opinion, a sharper turn of phrase, or a small amount of mess.
WRITING RULE Do not make the writing sterile. Vary sentence length and rhythm.
WRITING RULE Do not sound sycophantic, promotional, or like a chatbot.
WRITING RULE When revising, scan for obvious AI tells first, then rewrite, then self-audit the result.
WRITING RULE If the user gives a style target, match it. If not, aim for natural, sharp, and readable.
WRITING RULE Keep the output focused on the rewritten text unless the user asks for notes or an explanation. 
WRITING RULE When a sentence feels generic enough to fit any project, rewrite it or cut it.
WRITING RULE Use straight quotes only.
WRITING RULE Prefer one idea per sentence when possible.
WRITING RULE Use first person when it sounds natural and helps the voice.
WRITING RULE "The update improves the workflow" becomes "The update cuts three steps from the workflow."
WRITING RULE "This is a pivotal moment" becomes "This changes how the team works."


CLOSED
-   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
    `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
    pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
    own `Deprecated:` paragraph, so the listing which is supposed to introduce the CLI was mostly made of commands
    nobody should start using. `ptbk --help`, and the same listing which plain `ptbk` prints when it asks for a
    subcommand, now offers only the commands which are meant to be used - `coder`, `agent`, `agent-folder`,
    `agents-server`, `about` and `hello`. Nothing was removed: every deprecated command still runs, still warns that it
    is deprecated when it is used, still explains the deprecation in its own help - `ptbk run --help` - and is still
    reachable through `ptbk help run`. Only the "did you mean" hint for a mistyped command stops pointing at them.
    Being left out of the help is now simply a part of what deprecating a command means, written once in
    `$deprecateCliCommand`, so any command deprecated later disappears from the help on its own.

-   `ptbk` without a subcommand no longer falls back to the deprecated `ptbk run`. Until now `run` was registered as
    the default command of the CLI, so everything which did not name a command - `ptbk ./write-cv.book`, a mistyped
    `ptbk codr` or a stray option - was quietly handed over to the pipeline runner of the old system, which was the
    last thing a deprecated command should be doing. Plain `ptbk` now asks to specify a subcommand and prints the
    top-level help, exactly like `ptbk coder`, `ptbk agent`, `ptbk agent-folder` and `ptbk agents-server` already did
    when they were called without one of their own subcommands, and that one shared behavior is now written once
    instead of five times. Anything which is not a command is refused with `unknown command` and a "did you mean"
    hint instead of being executed as a pipeline, so a script which still passes a book path to `ptbk` fails loudly
    rather than appearing to succeed. `ptbk run` itself is untouched and keeps working for everyone who asks for it
    explicitly. The command list in the help now starts with `coder`, followed by `agent`, `agent-folder` and
    `agents-server`, and the deprecated commands of the old pipeline system are listed after them.

-   `ptbk coder` now saves the run trace of every prompt round, so a finished run can still be analyzed afterwards.
    Until now the only durable record of a round was the one status line written into the prompt file: the live
    runtime log holding everything the harness and the verification command had written was a temporary artifact,
    deleted as soon as a successful round was over unless `--preserve-logs` was passed, and lost together with the
    reasoning, the tool calls and the diffs of the run. Each round now writes `prompts/traces/<prompt-file-name>.md`
    - a trace named exactly like the prompt file it belongs to, with the same `-2` section suffix the temporary
    scripts of a multi-section prompt file already use. The trace pairs the metadata of the round - which prompt and
    section it implemented, whether it succeeded or failed, the harness, model, thinking level and login method which
    ran it, how many coding attempts it took, what each step cost and how long each one ran, the verification
    command, the start, end and duration, and the error which ended a failed round - with the untouched runtime log
    of the harness. Because the trace is written before the round is committed, it lands in the very same commit as
    the prompt it describes, and a task whose isolated worktree cannot be merged back carries its trace into the
    failure commit instead. A failed round is traced too, next to the `.error.log` it already wrote. Re-running the
    same prompt overwrites its trace and leaves the earlier ones in the git history.

-   `ptbk coder run` and `ptbk coder server` no longer walk into a full disk. A coding round writes prompt and log
    artifacts, lets the harness rewrite the project, may install npm packages, may create a git worktree for
    `--isolate` and usually runs a build, so a run started without enough space used to die somewhere in the middle,
    after the coding agent had already rewritten a part of the project. The free disk space is now measured before
    anything is installed or written: with less than 2 GB left the command refuses to start and says how much space
    is left, how much is needed and how to get it back, instead of failing later with a raw filesystem error. The
    same measurement is repeated while the run is in progress - at the checkpoints where `ptbk coder` can stop
    anyway, so it is checked before every round, every verification and every commit - and once less than 1 GB is
    left the run reports the same warning and pauses there, waiting for the disk space to be freed and for `P` to
    resume it. The limit to keep running is lower than the limit to start, because interrupting a round which is
    already in progress costs more than not starting one. A run started with `--no-questions` never pauses, because
    there is nobody who could free the disk space and resume it - it reports the warning and keeps going. A platform
    which cannot report its free disk space never stops a run.

-   Added `--no-questions` to every `ptbk coder` command which can stop and wait for an answer it does not strictly
    need. `init`, `add`, `run`, `ping` and `server` still do all their work, but no longer ask anything: installing
    a missing coding harness, updating an outdated one, updating an outdated Promptbook CLI and adding the missing
    harness ignore rules to `.gitignore` are all skipped and reported together with the command which does them
    manually, and `ptbk coder add` refuses a missing description instead of asking for it. Because an outdated
    harness or CLI can only be updated after a confirmation, `--no-questions` also skips the npm latest-version
    lookup which would only lead to a question, which is what the never released `--no-harness-update` used to do -
    that flag is replaced by `--no-questions` and is gone. The one combination which can not work,
    `--no-auto --no-questions`, is refused right away instead of waiting forever for a confirmation nobody may be
    asked for. `ptbk coder verify` does not take the flag, because walking through finished prompts and asking about
    each of them is the only thing it does.

-   `ptbk coder` no longer answers a harness which is not logged in with a wall of raw CLI output. A harness whose
    login is missing or whose session has expired now fails with a short branded `AuthenticationError` which names
    the harness, quotes the one sentence the harness itself reported - such as `Failed to authenticate: OAuth session
    expired and could not be refreshed` - and says exactly how to sign in again, for example by running `claude` and
    then the `/login` command inside it. How each harness is signed in is described once, next to how it is installed
    and detected, and the failure is recognized once for every harness and for every command
    which runs prompts, so `coder run`, `coder server` and `coder ping` all report it the same way. The terminal and
    the `.error.log` file written next to the prompt both show those instructions on their own now, instead of
    following them with a stack trace of a failure nobody can debug. Because no retry of a harness which is not
    logged in can ever succeed, such a prompt is also no longer retried three times with the `--wait-after-error`
    delay in between - the instructions are shown right away.

-   Fixed the wall of `npm warn ERESOLVE overriding peer dependency` warnings printed when installing `ptbk`:

    -   The Vercel AI SDK providers `@ai-sdk/openai`, `@ai-sdk/google` and `@ai-sdk/deepseek` were still on the
        generation which accepts only Zod 3, while the rest of the ecosystem - the Agents Server, `@openai/agents`,
        `@modelcontextprotocol/sdk` - needs Zod 4. No single Zod could satisfy both sides, so npm reported a conflict
        for every one of those packages. They are upgraded to the newest generation which accepts both Zod 3 and
        Zod 4 and still runs on Node.js 18, so the supported Node.js versions of Promptbook do not change, and
        `createExecutionToolsFromVercelProvider` is migrated to the model interface of that generation. Settings
        given as `additionalChatSettings` are now passed to the model call instead of to the model constructor,
        which is where the Vercel AI SDK expects them, and the `userId` option is no longer sent as the `user`
        setting, because it has become specific to each provider.
    -   Generated packages no longer publish dependencies which only look like imports. Package generation infers
        dependencies by reading the generated bundle, and it used to count module specifiers written inside the
        source-code templates of the transpilers, so `@promptbook/core` published `zod`, `dotenv` and
        `@anthropic-ai/sdk` as runtime dependencies although it never imports any of them. The pinned `zod` in
        particular forced one exact Zod version onto every project which installs Promptbook.
    -   Generated packages no longer carry the `overrides` field of the repository. npm honors overrides only in the
        root project of an installation, so publishing them only left a dangling reference to a dependency which the
        published package does not declare.
    -   `swagger-ui-react` is upgraded, because the newer version asks for a version of `react-copy-to-clipboard`
        which no longer refuses React 19.

    What remains after this are two warnings about React 19 coming from `react-debounce-input` and `react-inspector`
    inside `swagger-ui-react`. `swagger-ui-react` asks for those two by an exact and by an outdated version, so the
    only project which can stop those two warnings is `swagger-ui-react` itself.

-   The hero headline of the **ptbk coder** landing page now rotates through seven claims instead of saying one
    thing forever. Every claim is written once in a single configuration the rotating headline, the page tagline
    and the sharing image all read, and the claims crossfade with a slight vertical slide - the claim which leaves
    rises out of the headline while the next one rises into it. All claims share one grid cell, so the headline
    keeps the height of the tallest of them and nothing around it moves while they rotate. The same reserved
    height finally balances the hero: together with a terminal text size tuned one step down, the copy column now
    reaches almost as high as the live terminal next to it, instead of floating in the middle of a section the
    terminal alone defines. Visitors whose system prefers reduced motion keep the first claim without rotation.

-   Fixed `ptbk coder init` overwriting configuration the project already owns. Existing `package.json` scripts and
    `.vscode/settings.json` settings are now kept verbatim and only genuinely missing ones are added. Files a default
    script points at, like `agents/developer.book`, `AGENTS.md` and `prompts/templates/common.md`, are created only
    together with the script which references them, so a project with its own `coder:run` no longer receives an unused
    default agent. `ptbk coder init` also initializes the `test-for-ptbk-coder` npm script which the default
    `coder:run` uses as its verification command.

-   Rebalanced the hero of the **ptbk coder** landing page, whose live terminal used to be much taller than the copy
commit 240272096038e03141502a7c8ee00a7b7f90dacd
Author: Promptbook Coding Agent <me+agent@pavolhejny.com>
Date:   Mon Aug 3 02:11:14 2026 +0200

    [✨🚭] Allow to specify required model for each prompt
    
    **For example when you run:**
    
    ```bash
    ptbk coder run --harness github-copilot --model gpt-5.5 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md
    ```
    
    **This prompt will run:**
    
    ```markdown
    [ ] use `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use model `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] !!!! `gpt`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use `github-copilot` !!!!!
    
    [✨𓀍] Do some work
    ```
    
    **But this will be ignored:**
    
    ```markdown
    [ ] use `claude-opus`
    
    [✨𓀍] Do some work
    ```
    
    -   In the `[ ]` section of the prompt to be done allowed to specify the required model or harness for this prompt
    -   Allow to combine the model / harness name in the `[ ]` section with the priority !!!!!
    -   you can specify either model or harness or just family of the models. For example:
        -   `gpt` - any model from the gpt family
        -   `github-copilot` - the harness
        -   `claude` - any model from the claude family
        -   `claude-opus` - any model from the claude-opus family
        -   `opus` - any model from the claude-opus family
        -   this shouldn't be hardcoded. The matching from the prompt and the harness or model name should be done dynamically via normalized names
        -   the normalized name of the model or harness is computed from the name of the model or harness by removing all non-alphanumeric characters and lowercasing it. For example:
            -   `gpt-5.5` -> `gpt-5-5`
            -   `claude-opus-4.8` -> `claude-opus-4-8`
            -   `github-copilot` -> `github-copilot`
            -   `claude-opus` -> `claude-opus`
            -   `claude` -> `claude`
            -   Use the normalizing functions which are here in the project, do not implement your own normalizing function
    -   the strategy should work as follows: it should firstly take all the tokens in these `parentheses` in the prompt line
        -   For example: "[ ] use `gpt-5.5`" -> it should take the token `gpt-5.5`
    -   Then it should normalize the name of the model or harness which is used to run this prompt. For example: `gpt-5.5` -> `gpt-5-5`
    -   Then it should normalize the name of the model and harness which is used to run the ptbk coder
    -   Then match all the tokens from the prompt with the normalized name of the model or harness which is used to run this prompt. If any of the tokens matches, then this prompt can be run. If none of the tokens matches, then this prompt should be skipped and not run.
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Update the [`ptbk coder` landing website](apps/coder-landing)
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index 2191d9729..017d2032d 100644
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -9,6 +9,7 @@ The single source of truth for every shell command shown on the page. Any sectio
 | `INIT_COMMAND`            | `ptbk coder init`                                                                                                                                                                                      | [Quickstart step 2](../sections/quickstart.md)                              |
 | `ADD_COMMAND`             | `ptbk coder add "Add a dark mode toggle to the settings page"`                                                                                                                                         | [Quickstart step 3](../sections/quickstart.md)                              |
 | `RUN_COMMAND`             | `ptbk coder run --harness claude-code`                                                                                                                                                                 | [Quickstart step 4](../sections/quickstart.md)                              |
+| `MODEL_FILTER_COMMAND`    | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND` | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`         | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
 | `PING_COMMAND`            | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`                                                                                                                    | [Advanced features](../sections/advanced-features.md)                       |
@@ -25,6 +26,7 @@ These `ptbk coder` options may be referenced in page prose and must be described
 
 -   `--harness <name>` — selects the coding agent; one of the six harnesses in [`harness-catalog.md`](./harness-catalog.md). Required for non-dry runs.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex` and `gemini`.
+-   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example ``[ ] use model `gpt-5.5` ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
 -   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 375018eba..bca6b2707 100644
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twelve cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these thirteen cards, in this order:
 
 | #   | Title                         | Snippet                                                                                  |
 | --- | ----------------------------- | ---------------------------------------------------------------------------------------- |
@@ -20,11 +20,12 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 | 5   | Isolated worktrees            | `ptbk coder run --harness claude-code --isolate`                                         |
 | 6   | Kanban web UI                 | `ptbk coder server --port 4441 --harness claude-code`                                    |
 | 7   | Prompt priorities             | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                 |
-| 8   | Pacing and retries            | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m` |
-| 9   | Dry run first                 | `ptbk coder run --dry-run`                                                               |
-| 10  | Ping before you code          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`       |
-| 11  | Human in the loop             | `ptbk coder run --harness claude-code --no-auto`                                         |
-| 12  | Verify and archive            | `ptbk coder verify`                                                                      |
+| 8   | Model-specific prompts        | `ptbk coder run --harness github-copilot --model gpt-5.5`                                |
+| 9   | Pacing and retries            | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m` |
+| 10  | Dry run first                 | `ptbk coder run --dry-run`                                                               |
+| 11  | Ping before you code          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`       |
+| 12  | Human in the loop             | `ptbk coder run --harness claude-code --no-auto`                                         |
+| 13  | Verify and archive            | `ptbk coder verify`                                                                      |
 
 ## Descriptions (verbatim card copy)
 
@@ -35,10 +36,11 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 5. **Isolated worktrees** — "Implement every prompt in its own temporary git worktree with its own environment. Verified work is merged back into your branch as one commit; an unmergeable task is marked as failed and its worktree is kept for you. Deeply nested repositories are handled on Windows too."
 6. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 7. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
-8. **Pacing and retries** — "Pace the queue with wall-clock wait durations that keep elapsing through pause and sleep, skip the active wait with S, and retry errors after a cool-down."
-9. **Dry run first** — "Preview which prompts would run — without touching your code or spending a single token."
-10. **Ping before you code** — "Make one small disposable harness/model call, see the returned result and response time, and optionally start consuming an applicable quota before a longer coding run."
-11. **Human in the loop** — "Confirm each prompt manually with --no-auto, press P to pause a running queue, or press X to end after the current prompt."
-12. **Verify and archive** — "Successful coding rounds record the harness, model and selected thinking level in the prompt status line. Walk through completed prompts interactively, archive the finished ones to prompts/done/ and auto-append repair prompts for incomplete work."
+8. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it automatically."
+9. **Pacing and retries** — "Pace the queue with wall-clock wait durations that keep elapsing through pause and sleep, skip the active wait with S, and retry errors after a cool-down."
+10. **Dry run first** — "Preview which prompts would run — without touching your code or spending a single token."
+11. **Ping before you code** — "Make one small disposable harness/model call, see the returned result and response time, and optionally start consuming an applicable quota before a longer coding run."
+12. **Human in the loop** — "Confirm each prompt manually with --no-auto, press P to pause a running queue, or press X to end after the current prompt."
+13. **Verify and archive** — "Successful coding rounds record the harness, model and selected thinking level in the prompt status line. Walk through completed prompts interactively, archive the finished ones to prompts/done/ and auto-append repair prompts for incomplete work."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c9e7b1e61..29b032481 100644
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,10 @@
-import { DRY_RUN_COMMAND, PING_COMMAND, TEST_BEFORE_FIX_COMMAND, VERIFY_COMMAND } from './commands';
+import {
+    DRY_RUN_COMMAND,
+    MODEL_FILTER_COMMAND,
+    PING_COMMAND,
+    TEST_BEFORE_FIX_COMMAND,
+    VERIFY_COMMAND,
+} from './commands';
 
 /**
  * One advanced feature of `ptbk coder` presented as a card with a terminal snippet.
@@ -67,6 +73,12 @@ export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
         description: 'Give prompts a priority and process only the range you want in the current run.',
         sampleCommand: 'ptbk coder run --harness claude-code --min-priority 1 --max-priority 5',
     },
+    {
+        title: 'Model-specific prompts',
+        description:
+            'Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it automatically.',
+        sampleCommand: MODEL_FILTER_COMMAND,
+    },
     {
         title: 'Pacing and retries',
         description:
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index 3a9a3349f..a3d903564 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,5 @@
+-   Added required model and harness matching to `ptbk coder` prompts. A ready `[ ]` status line can now contain one or more backtick-delimited normalized model, model-family, or harness names, combine them with `!` priority markers, and run only when the selected runner matches. The `ptbk coder` landing page now documents model-specific prompts.
+
 -   Simplified the Agents Server manGo onboarding Book step so the assignment now generates one editable Book directly. The intermediate markdown draft and separate conversion panel were removed, the real Book editor is shown immediately, and section buttons insert valid Book-language commitments before the final `OPEN` or `CLOSED` marker.
 
 -   Fixed rich `ptbk coder run` terminal controls so `S Skip current waiting` is shown only while the coder is in a waiting phase (between prompts, after a prompt, or after an error), and hidden while a prompt is active or the run has finished. The Coder landing demo now matches its completed `DONE` frame and omits the unavailable skip action.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/book-2.0/agent-source/normalizeAgentName.ts; Get-Content src/utils/normalization/normalize-to-kebab-case.ts; Get-Content src/utils/normalization/normalize-to-kebab-case.test.ts; Get-Content src/book-2.0/agent-source/AgentBasicInformation.ts | Select-Object -First 130; Get-Content src/types/string_agent_name.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 440ms:
import { spaceTrim } from 'spacetrim';
import type { string_agent_name } from '../../types/string_agent_name';
import { titleToName } from '../../utils/normalization/titleToName';

/**
 * Normalizes agent name from arbitrary string to valid agent name
 *
 * Note: [🔂] This function is idempotent.
 *
 * @public exported from `@promptbook/core`
 */
export function normalizeAgentName(rawAgentName: string): string_agent_name {
    return titleToName(spaceTrim(rawAgentName));
}
import { removeDiacritics } from './removeDiacritics';

/**
 * Semantic helper for kebab-case strings
 *
 * @example 'hello-world'
 * @example 'i-love-promptbook'
 *
 * @public exported from `@promptbook/utils`
 */
export type string_kebab_case = string;

/**
 * Type describing char type.
 *
 * @private type of `normalizeToKebabCase`
 */
type char_type = 'LOWERCASE' | 'UPPERCASE' | 'NUMBER' | 'SLASH' | 'OTHER';

/**
 * Converts a given text to kebab-case format.
 *
 * Note: [🔂] This function is idempotent.
 *
 * @param text The text to be converted.
 * @returns The kebab-case formatted string.
 * @example 'hello-world'
 * @example 'i-love-promptbook'
 *
 * @public exported from `@promptbook/utils`
 */
export function normalizeToKebabCase(text: string): string_kebab_case {
    text = removeDiacritics(text);

    let charType: char_type;
    let lastCharType: char_type = 'OTHER';

    let normalizedName = '';

    for (const char of text) {
        let normalizedChar: string;

        if (/^[a-z]$/.test(char)) {
            charType = 'LOWERCASE';
            normalizedChar = char;
        } else if (/^[A-Z]$/.test(char)) {
            charType = 'UPPERCASE';
            normalizedChar = char.toLowerCase();
        } else if (/^[0-9]$/.test(char)) {
            charType = 'NUMBER';
            normalizedChar = char;
        } else {
            charType = 'OTHER';
            normalizedChar = '-';
        }

        if (
            charType !== lastCharType &&
            !(lastCharType === 'UPPERCASE' && charType === 'LOWERCASE') &&
            !(lastCharType === 'NUMBER') &&
            !(charType === 'NUMBER')
        ) {
            normalizedName += '-';
        }

        normalizedName += normalizedChar;

        lastCharType = charType;
    }

    normalizedName = normalizedName.split(/-+/g).join('-');
    normalizedName = normalizedName.split(/-?\/-?/g).join('/');
    normalizedName = normalizedName.replace(/^-/, '');
    normalizedName = normalizedName.replace(/-$/, '');

    return normalizedName;
}

// Note: [💞] Ignore a discrepancy between file name and entity name
import { spaceTrim } from 'spacetrim';
import { describe, expect, it } from '@jest/globals';
import { normalizeToKebabCase } from './normalize-to-kebab-case';

describe('how normalizing to kebab-case works', () => {
    it('will normalize one word', () => {
        expect(normalizeToKebabCase('hello')).toEqual('hello');
        expect(normalizeToKebabCase(' hello ')).toEqual('hello');
        expect(normalizeToKebabCase('HELLO')).toEqual('hello');
    });

    it('will normalize sentence', () => {
        expect(normalizeToKebabCase('hello world')).toEqual('hello-world');
        expect(normalizeToKebabCase('   hello world')).toEqual('hello-world');
        expect(normalizeToKebabCase('helloWorld')).toEqual('hello-world');
        expect(normalizeToKebabCase('hello___world')).toEqual('hello-world');
        expect(normalizeToKebabCase('hello_/_world')).toEqual('hello-world');
        expect(normalizeToKebabCase('hello_\\_world')).toEqual('hello-world');
        expect(normalizeToKebabCase('hello.world')).toEqual('hello-world');
        expect(
            normalizeToKebabCase(
                spaceTrim(`
            hello
            world
        `),
            ),
        ).toEqual('hello-world');
    });

    it('will normalize chars with diacritics', () => {
        expect(normalizeToKebabCase('hělló wórld')).toEqual('hello-world');
    });

    it('can normalize word with numbers', () => {
        expect(normalizeToKebabCase('1FooBar2')).toEqual('1foo-bar2');
    });
});
import type { AvatarVisualId } from '../../avatars/types/AvatarVisualDefinition';
import type { string_agent_hash, string_agent_name, string_agent_permanent_id } from '../../types/string_agent_name';
import type { string_agent_url } from '../../types/string_agent_url';
import type { string_fonts } from '../../types/string_markdown';
import type { string_color } from '../../types/string_person_fullname';
import type { string_url_image } from '../../types/string_url_image';
import type { AgentVisibility } from './agentSourceVisibility';

/**
 * Unified parameter representation that supports two different notations:
 * 1. @Parameter - single word parameter starting with @
 * 2. {parameterName} or {parameter with multiple words} or {parameterName: description text}
 * Both notations represent the same syntax feature - parameters
 */
export type BookParameter = {
    /**
     * The raw text of the parameter as it appears in the source
     */
    text: string;

    /**
     * The notation used for this parameter
     */
    notation: 'at' | 'brace';

    /**
     * The parameter name (without @ or {})
     */
    name: string;

    /**
     * Optional description for {parameterName: description} notation
     */
    description?: string;
};

/**
 * Capability of the agent
 * This is parsed from commitments like USE CALENDAR, USE PROJECT, KNOWLEDGE, etc.
 */
export type AgentCapability = {
    /**
     * The type of the capability
     */
    type:
        | 'knowledge'
        | 'user-location'
        | 'inheritance'
        | 'import'
        | 'image-generator'
        | 'team'
        | 'popup'
        | 'privacy'
        | 'project'
        | 'calendar';

    /**
     * The label to display for this capability
     */
    label: string;

    /**
     * The name of the icon to display for this capability
     */
    iconName: string;

    /**
     * Optional link to another agent
     * This is used for 'inheritance' and 'import' types
     */
    agentUrl?: string_agent_url;

    // <- Note: When creating new capabilities, look for [🪀] to add new icons
};

/**
 * Information about agent basic.
 */
export type AgentBasicInformation = {
    /**
     * Name of the agent
     * This is the first line of the agent source
     */
    agentName: string_agent_name;

    /**
     * Hash of the agent source for integrity verification
     */
    agentHash: string_agent_hash;

    /**
     * Unique identifier of the agent
     * This is a random base58 string assigned by the server
     */
    permanentId?: string_agent_permanent_id;

    /**
     * Optional description of the agent
     * This is derived from the last `GOAL` / `GOALS` commitment,
     * falling back to deprecated `PERSONA` / `PERSONAE` for backward compatibility.
     */
    personaDescription: string | null;
    // <- TODO: [🕛][🧠]  Maybe rename to just `description`

    /**
     * The initial message shown to the user when the chat starts
     * This is the line starting with "INITIAL MESSAGE"
     */
    initialMessage: string | null;

    /**
     * Optional custom thinking placeholder variants shown while this agent is composing a reply.
     *
     * This is derived from `META THINKING MESSAGE` commitments.
     * Multiple commitments are preserved as multiple variants.
     */
    thinkingMessages?: Array<string>;

    /**
     * Metadata commitments parsed from META lines
     * Each META commitment has the format "META TYPE content"
     * When there are multiple meta commitments of the same type, later overrides earlier
     */
    meta: {
        fullname?: string;
        description?: string;
        disclaimer?: string;
        inputPlaceholder?: string;
        messageSuffix?: string;
        image?: string_url_image;
import type { string_agent_hash_private } from './string_agent_hash_private';
import type { string_agent_name_in_book_private } from './string_agent_name_in_book_private';
import type { string_agent_name_private } from './string_agent_name_private';
import type { string_agent_permanent_id_private } from './string_agent_permanent_id_private';

/**
 * Semantic helper
 *
 * For example `"My AI Assistant"`
 *
 * TODO: !!!! Brand the type
 */
export type string_agent_name = string_agent_name_private;

/**
 * Semantic helper
 *
 * For example `"My AI Assistant"`
 */
export type string_agent_name_in_book = string_agent_name_in_book_private;

/**
 * Semantic helper
 *
 * For example `"b126926439c5fcb83609888a11283723c1ef137c0ad599a77a1be81812bd221d"`
 */
export type string_agent_hash = string_agent_hash_private;

/**
 * Semantic helper
 *
 * For example `"3mJr7AoUXx2Wqd"`
 *
 * TODO: !!!! Brand the type
 */
export type string_agent_permanent_id = string_agent_permanent_id_private;

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/utils/normalization/titleToName.ts; Get-Content src/utils/normalization/titleToName.test.ts | Select-Object -First 240; Get-Content src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts | Select-Object -First 110' in C:\Users\me\work\ai\promptbook
 succeeded in 440ms:
import { basename } from 'path';
import { isValidFilePath } from '../validators/filePath/isValidFilePath';
import { isValidUrl } from '../validators/url/isValidUrl';
import { normalizeToKebabCase } from './normalize-to-kebab-case';
import { removeEmojis } from './removeEmojis';

/**
 * Converts a title string into a normalized name.
 *
 * Note: [🔂] This function is idempotent.
 *
 * @param value The title string to be converted to a name.
 * @returns A normalized name derived from the input title.
 * @example 'Hello World!' -> 'hello-world'
 *
 * @public exported from `@promptbook/utils`
 */
export function titleToName(value: string): string {
    if (isValidUrl(value)) {
        value = value.replace(/^https?:\/\//, '');
        value = value.replace(/\.html$/, '');
    } else if (isValidFilePath(value)) {
        value = basename(value);
        // Note: Keeping extension in the name
    }

    value = value.split('/').join('-');

    value = removeEmojis(value);
    value = normalizeToKebabCase(value);

    // TODO: [🧠] Maybe warn or add some padding to short name which are not good identifiers
    return value;
}
import { spaceTrim } from 'spacetrim';
import { describe, expect, it } from '@jest/globals';
import { titleToName } from './titleToName';

describe('how titleToName works', () => {
    it('should make name from title', () => {
        expect(titleToName(`hello`)).toBe(`hello`);
        expect(titleToName(`hello 🤘`)).toBe(`hello`);
        expect(titleToName(`hello world`)).toBe(`hello-world`);
        expect(titleToName(`hello_world`)).toBe(`hello-world`);
        expect(titleToName(`Hello, how are ýóů?`)).toBe(`hello-how-are-you`);
        expect(
            titleToName(
                spaceTrim(`  Hello, how
                           are ýóů?   `),
            ),
        ).toBe(`hello-how-are-you`);
    });

    it('should make name url', () => {
        expect(titleToName(`https://promptbook.studio/webgpt/write-website-content.book`)).toBe(
            `promptbook-studio-webgpt-write-website-content-book`,
        );

        expect(titleToName(`https://promptbook.studio/webgpt/write-website-content.html`)).toBe(
            `promptbook-studio-webgpt-write-website-content`,
        );
    });

    it('should make relative file path', () => {
        expect(titleToName(`../webgpt/write-website-content.book`)).toBe(`write-website-content-book`);
        expect(titleToName(`../webgpt/write-website-content.book.html`)).toBe(`write-website-content-book-html`);
        expect(titleToName(`./webgpt/write-website-content.book`)).toBe(`write-website-content-book`);
        expect(titleToName(`./webgpt/write-website-content.book.html`)).toBe(`write-website-content-book-html`);
    });

    it('should make absolute file path', () => {
        expect(titleToName(`C://Users/pavol/projects/webgpt/write-website-content.book`)).toBe(
            `write-website-content-book`,
        );
        expect(titleToName(`/home/pavol/projects/webgpt/write-website-content.book`)).toBe(
            `write-website-content-book`,
        );
    });
});
import { spaceTrim } from 'spacetrim';
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { ParsedCommitment } from '../../commitments/_base/ParsedCommitment';
import { COMMITMENT_REGISTRY } from '../../commitments/index';
import type { AgentSourceParseResult } from './AgentSourceParseResult';
import { getBookCommitmentLineType } from './getBookCommitmentLineType';
import { parseAgentSourcePrelude } from './parseAgentSourcePrelude';
import type { ParsedUnknownCommitment } from './ParsedUnknownCommitment';
import type { string_book } from './string_book';

/**
 * Regex pattern to match horizontal lines (markdown thematic breaks)
 * Matches 3 or more hyphens, underscores, or asterisks (with optional spaces between)
 */
const HORIZONTAL_LINE_PATTERN = /^[\s]*[-_*][\s]*[-_*][\s]*[-_*][\s]*[-_*]*[\s]*$/;

/**
 * Registered commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentKnownCommitment = {
    readonly kind: 'known';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly contentLines: string[];
};

/**
 * Unknown commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentUnknownCommitment = {
    readonly kind: 'unknown';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly sourceLines: string[];
};

/**
 * Commitment-like source block currently being collected.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentCommitment = CurrentKnownCommitment | CurrentUnknownCommitment;

/**
 * Parses agent source using the new commitment system with multiline support
 * This function replaces the hardcoded commitment parsing in the original parseAgentSource
 *
 * The first non-empty line is always consumed as plain-text agent name. Commitment parsing
 * starts only after that title line has been fixed.
 *
 * @private internal utility of `parseAgentSource`
 */
export function parseAgentSourceWithCommitments(agentSource: string_book): Omit<AgentSourceParseResult, 'agentHash'> {
    if (!agentSource || !agentSource.trim()) {
        return {
            agentName: null,
            commitments: [],
            unknownCommitments: [],
            nonCommitmentLines: [],
        };
    }

    const { lines, agentName, agentNameLineIndex, agentNameLineNumber } = parseAgentSourcePrelude(agentSource);

    const commitments: ParsedCommitment[] = [];
    const unknownCommitments: ParsedUnknownCommitment[] = [];
    const nonCommitmentLines: string[] = agentNameLineIndex >= 0 ? [lines[agentNameLineIndex]!] : [];

    // Parse commitments with multiline support.
    let currentCommitment: CurrentCommitment | null = null;

    // Process lines starting from after the agent name line
    const startIndex = agentNameLineIndex >= 0 ? agentNameLineIndex + 1 : 0;
    let isInsideCodeBlock = false;
    for (let i = startIndex; i < lines.length; i++) {
        const line = lines[i];
        if (line === undefined) {
            continue;
        }

        const trimmedLine = line.trim();

        // Check if this line starts or ends a code block
        if (trimmedLine.startsWith('```')) {
            isInsideCodeBlock = !isInsideCodeBlock;

            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        if (isInsideCodeBlock) {
            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        // Check if this line starts a new commitment
        let foundNewCommitment = false;
        for (const definition of COMMITMENT_REGISTRY) {
            const typeRegex = definition.createTypeRegex();
            const match = typeRegex.exec(line.trim());

            if (match && match.groups?.type) {
                appendCurrentCommitment(currentCommitment, commitments, unknownCommitments);


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "promptRunnerIdentity|resolvedCoderAgent|resolveCoderAgent" scripts/run-codex-prompts/main/runCodexPrompts.ts src/cli/cli-commands/coder; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 90 -First 90; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 700 -First 100; Get-Content scripts/run-codex-prompts/main/listCoderPrompts.ts; Get-Content scripts/run-codex-prompts/main/listCoderPrompts.test.ts | Select-Object -First 200' in C:\Users\me\work\ai\promptbook
 succeeded in 485ms:
scripts/run-codex-prompts/main/runCodexPrompts.ts:16:import { resolveCoderAgent } from '../common/resolveCoderAgent';
scripts/run-codex-prompts/main/runCodexPrompts.ts:110:        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
scripts/run-codex-prompts/main/runCodexPrompts.ts:111:        const resolvedAgentSystemMessage = resolvedCoderAgent?.systemMessage;
scripts/run-codex-prompts/main/runCodexPrompts.ts:118:        const promptRunnerIdentity: PromptRunnerIdentity = {
scripts/run-codex-prompts/main/runCodexPrompts.ts:125:        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
scripts/run-codex-prompts/main/runCodexPrompts.ts:170:                    promptRunnerIdentity,
scripts/run-codex-prompts/main/runCodexPrompts.ts:202:                promptRunnerIdentity,
scripts/run-codex-prompts/main/runCodexPrompts.ts:212:                promptRunnerIdentity,
scripts/run-codex-prompts/main/runCodexPrompts.ts:783:    promptRunnerIdentity: PromptRunnerIdentity;
scripts/run-codex-prompts/main/runCodexPrompts.ts:791:        promptRunnerIdentity,
scripts/run-codex-prompts/main/runCodexPrompts.ts:813:            : findNextTodoPrompt(promptFiles, runOptions.priorityFilter, promptRunnerIdentity),
scripts/run-codex-prompts/main/runCodexPrompts.ts:826:    promptRunnerIdentity: PromptRunnerIdentity;
scripts/run-codex-prompts/main/runCodexPrompts.ts:828:    const { hasShownUpcomingTasks, promptFiles, stats, priorityFilter, isRichUiEnabled, promptRunnerIdentity } =
scripts/run-codex-prompts/main/runCodexPrompts.ts:841:    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter, promptRunnerIdentity));
src/cli/cli-commands/coder\list.test.ts:68:                promptRunnerIdentity: undefined,
src/cli/cli-commands/coder\list.test.ts:97:                promptRunnerIdentity: {
src/cli/cli-commands/coder\list.ts:56:            const promptRunnerIdentity =
src/cli/cli-commands/coder\list.ts:70:                promptRunnerIdentity,

    const runStartDate = moment();
    const { isRichUiEnabled, progressDisplay, uiHandle } = createRunDisplays(options, runStartDate);
    const waitForRequestedPause = createPauseWaiter({
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        // Note: Every pause checkpoint of the whole run goes through this one waiter, so watching the free disk
        //       space here covers each round, each verification and each runner without repeating the check
        guardFreeDiskSpace: createFreeDiskSpaceGuard({
            inspectedPath: process.cwd(),
            isAskingQuestionsEnabled: options.isAskingQuestionsEnabled ?? true,
        }),
    });

    startPauseListenerIfNeeded(isRichUiEnabled);

    try {
        const resolvedCoderContext = await resolveCoderContext(options.context, process.cwd());
        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
        const resolvedAgentSystemMessage = resolvedCoderAgent?.systemMessage;

        if (await runDryRunIfRequested(options)) {
            return;
        }

        const { runner, actualRunnerModel, runnerMetadata } = resolvePromptRunner(options);
        const promptRunnerIdentity: PromptRunnerIdentity = {
            harnessName: options.agentName,
            modelName: actualRunnerModel,
        };
        console.info(colors.green(`Running prompts with ${runner.name}`));

        initializeRunUi(uiHandle, runner.name, actualRunnerModel, options);
        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
        await refreshCoderRunUiSubscriptionUsage({
            runner,
            uiState: uiHandle?.state,
        });
        await seedCachedAveragePromptDuration({
            options,
            actualRunnerModel,
            progressDisplay,
            uiHandle,
        });

        let hasShownUpcomingTasks = false;
        let hasWaitedForStart = false;
        let previousRoundStartTime: number | undefined;
        let previousRoundEndTime: number | undefined;
        let completedRunCount = 0;
        let hasRunTestBefore = false;
        // Note: Only the very first round resumes the interrupted prompt, every later round starts from a clean tree
        let isContinuingInterruptedPrompt = options.gitChanges === 'continue';

        while (just(true)) {
            if (options.autoPull && !options.dryRun) {
                await waitForRequestedPause({
                    checkpointLabel: 'pulling the latest repository changes',
                    phase: 'loading',
                    statusMessage: 'Pulling latest changes...',
                });
            }
            await pullLatestChangesIfEnabled({
                options,
                isRichUiEnabled,
            });

            if (!hasRunTestBefore && options.testBefore !== 'no') {
                await waitForRequestedPause({
                    checkpointLabel: 'loading prompts before running initial tests',
                    phase: 'loading',
                    statusMessage: 'Loading prompts before running initial tests...',
                });
                await loadPromptQueueSnapshot({
                    options,
                    isRichUiEnabled,
                    progressDisplay,
                    uiHandle,
                    promptRunnerIdentity,
                    isContinuingInterruptedPrompt,
                });
            }

            if (!hasRunTestBefore) {
                hasWaitedForStart = await runTestBeforeIfNeeded({
                    options,
                    runner,
                    runnerMetadata,
                    resolvedCoderContext,
        });

        resetPauseTargetLabel();
    };
}

/**
 * Starts the pause listener only when the rich TTY UI is not consuming keyboard input.
 */
function startPauseListenerIfNeeded(isRichUiEnabled: boolean): void {
    if (!isRichUiEnabled) {
        listenForCoderRunControls();
    }
}

/**
 * Runs the dry-run reporting mode and returns whether the main execution should stop.
 */
async function runDryRunIfRequested(options: RunOptions): Promise<boolean> {
    if (!options.dryRun) {
        return false;
    }

    const promptFiles = await loadPromptFiles(PROMPTS_DIR);
    const stats = summarizePrompts(promptFiles, options.priorityFilter);
    printStats(stats, options.priorityFilter);
    console.info(colors.yellow('Following prompts need to be written:'));
    printPromptsToBeWritten(promptFiles, options.priorityFilter);
    return true;
}

/**
 * Seeds the rich UI with the selected runner configuration.
 */
function initializeRunUi(
    uiHandle: CoderRunUiHandle | undefined,
    runnerName: string,
    actualRunnerModel: string | undefined,
    options: RunOptions,
): void {
    uiHandle?.state.setConfig({
        agentName: runnerName,
        modelName: actualRunnerModel,
        thinkingLevel: options.thinkingLevel,
        context: options.context,
        serverUrl: options.serverUrl,
        priorityFilter: options.priorityFilter,
        limit: options.limit,
        testCommand: options.testCommand,
    });
    uiHandle?.state.setPhase('loading');
    uiHandle?.state.setStatusMessage(`Running prompts with ${runnerName}`);
}

/**
 * Prepares the `--agent` book avatar ASCII-art renderer and shows it above the dashboard boxes.
 *
 * Leaves the header empty when no agent is selected, the UI is disabled, or the visual cannot be rendered.
 */
async function initializeRunUiAgentVisual(
    uiHandle: CoderRunUiHandle | undefined,
    agentSource: string_book | undefined,
): Promise<void> {
    if (!uiHandle || !agentSource) {
        return;
    }

    const agentVisual = await buildCoderRunAgentVisual(agentSource);

    if (agentVisual) {
        uiHandle.state.setAgentVisual(agentVisual);
    }
}

/**
 * Loads prompt files, updates progress displays, and selects the next runnable prompt.
 */
async function loadPromptQueueSnapshot(options: {
    options: RunOptions;
    isRichUiEnabled: boolean;
    progressDisplay?: CliProgressDisplay;
    uiHandle?: CoderRunUiHandle;
    promptRunnerIdentity: PromptRunnerIdentity;
    isContinuingInterruptedPrompt: boolean;
}): Promise<PromptQueueSnapshot> {
    const {
        options: runOptions,
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        promptRunnerIdentity,
        isContinuingInterruptedPrompt,
    } = options;
    uiHandle?.state.setCurrentScriptPath(undefined);

    const promptFiles = await loadPromptFiles(PROMPTS_DIR);
    const stats = summarizePrompts(promptFiles, runOptions.priorityFilter);

    progressDisplay?.update(stats);
    uiHandle?.state.updateProgress(stats);
import { join } from 'path';
import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { normalizePriorityFilter, type PriorityFilterInput } from '../prompts/priorityFilter';
import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';

/**
 * Options which select the ready prompts listed by `ptbk coder list`.
 *
 * @public exported from `@promptbook/cli`
 */
export type ListCoderPromptsOptions = PriorityFilterInput & {
    /**
     * Optional harness and model selection used to omit prompts routed to other runners.
     */
    readonly promptRunnerIdentity?: PromptRunnerIdentity;
};

/**
 * Directory containing the prompt queue of the current project.
 *
 * @private internal constant of `listCoderPrompts`
 */
const PROMPTS_DIRECTORY_PATH = join(process.cwd(), 'prompts');

/**
 * Lists ready, fully authored coding prompts in descending priority groups without starting a coding harness.
 *
 * @returns The number of prompts printed.
 * @public exported from `@promptbook/cli`
 */
export async function listCoderPrompts(options: ListCoderPromptsOptions = {}): Promise<number> {
    const priorityFilter = normalizePriorityFilter(options);
    const promptFiles = await loadPromptFiles(PROMPTS_DIRECTORY_PATH);
    const upcomingTasks = listUpcomingTasks(promptFiles, priorityFilter, options.promptRunnerIdentity);

    printUpcomingTasks(upcomingTasks);
    return upcomingTasks.length;
}
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { parsePromptFile } from '../prompts/parsePromptFile';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { listCoderPrompts } from './listCoderPrompts';

jest.mock('../prompts/loadPromptFiles', () => ({
    loadPromptFiles: jest.fn(),
}));

jest.mock('../prompts/printUpcomingTasks', () => ({
    printUpcomingTasks: jest.fn(),
}));

/**
 * Typed Jest mock for loading the current project's prompt files.
 */
function getLoadPromptFilesMock(): jest.MockedFunction<typeof loadPromptFiles> {
    return loadPromptFiles as jest.MockedFunction<typeof loadPromptFiles>;
}

/**
 * Typed Jest mock for the priority-grouped task renderer.
 */
function getPrintUpcomingTasksMock(): jest.MockedFunction<typeof printUpcomingTasks> {
    return printUpcomingTasks as jest.MockedFunction<typeof printUpcomingTasks>;
}

describe('listCoderPrompts', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    it('lists only ready prompts matching the selected runner and priority range', async () => {
        const promptFile = parsePromptFile(
            join(process.cwd(), 'prompts', 'list-test.md'),
            spaceTrim(`
                [ ] !!!! use \`github-copilot\`
                Compatible harness
                ---
                [ ] !!! use \`gpt-5.5\`
                Compatible model
                ---
                [ ] !!!!! use \`claude\`
                Different model family
                ---
                [ ] !!
                @@@ Still needs authoring
                ---
                [x] Done
                Finished prompt
            `),
        );
        getLoadPromptFilesMock().mockResolvedValue([promptFile]);

        const listedPromptCount = await listCoderPrompts({
            minimumPriority: 3,
            maximumPriority: 4,
            promptRunnerIdentity: {
                harnessName: 'github-copilot',
                modelName: 'gpt-5.5',
            },
        });

        expect(listedPromptCount).toBe(2);
        expect(getPrintUpcomingTasksMock()).toHaveBeenCalledWith([
            {
                label: 'prompts/list-test.md#1',
                summary: 'Compatible harness',
                priority: 4,
            },
            {
                label: 'prompts/list-test.md#4',
                summary: 'Compatible model',
                priority: 3,
            },
        ]);
    });

    it('does not filter runner-specific prompts when no harness or model is selected', async () => {
        const promptFile = parsePromptFile(
            join(process.cwd(), 'prompts', 'list-test.md'),
            spaceTrim(`
                [ ] use \`gpt\`
                GPT task
                ---
                [ ] use \`claude\`
                Claude task
            `),
        );
        getLoadPromptFilesMock().mockResolvedValue([promptFile]);

        const listedPromptCount = await listCoderPrompts();

        expect(listedPromptCount).toBe(2);
        expect(getPrintUpcomingTasksMock()).toHaveBeenCalledWith([
            {
                label: 'prompts/list-test.md#1',
                summary: 'GPT task',
                priority: 0,
            },
            {
                label: 'prompts/list-test.md#4',
                summary: 'Claude task',
                priority: 0,
            },
        ]);
    });
});

codex
The existing `--agent` already injects a Book into the harness; the missing piece is prompt routing. Ready `[ ]` lines currently match only selected harness and model, so agent-tagged tasks are not recognized. I’ll extend that shared routing identity with stable agent aliases (path, filename, stem, and Book title), and make `coder list` preview the same agent-filtered queue.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts; rg -n "agentName:|agentName.*parseAgentSource" src/book-2.0/agent-source/parseAgentSource*.test.ts src/book-2.0/agent-source/*.test.ts | Select-Object -First 100' in C:\Users\me\work\ai\promptbook
 exited 1 in 462ms:
import type { AgentBasicInformation } from '../AgentBasicInformation';

/**
 * Ensures the parsed profile always exposes a fullname value.
 *
 * @private internal utility of `parseAgentSource`
 */
export function ensureMetaFullname(meta: AgentBasicInformation['meta'], fallbackFullname: string): void {
    if (!meta.fullname) {
        meta.fullname = fallbackFullname;
    }
}
rg: src/book-2.0/agent-source/parseAgentSource*.test.ts: The filename, directory name, or volume label syntax is incorrect. (os error 123)
rg: src/book-2.0/agent-source/*.test.ts: The filename, directory name, or volume label syntax is incorrect. (os error 123)

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content apps/coder-landing/src/data/advancedFeatures.ts; Get-Content apps/coder-landing/specs/sections/advanced-features.md; Get-Content apps/coder-landing/specs/product.md | Select-Object -First 100; Get-Content apps/coder-landing/package.json; rg -n --hidden --glob '"'!node_modules'"' \"MODEL_FILTER_COMMAND|Model-specific prompts|Prompt routing|model, model-family, or harness\" apps/coder-landing" in C:\Users\me\work\ai\promptbook
 succeeded in 382ms:
import {
    GENERATE_BOILERPLATES_COMMAND,
    GIT_CHANGES_CONTINUE_COMMAND,
    INIT_GIT_SYNC_COMMAND,
    LIST_COMMAND,
    MODEL_FILTER_COMMAND,
    PING_COMMAND,
    PING_PERIOD_COMMAND,
    TEST_BEFORE_FIX_COMMAND,
    VERIFY_COMMAND,
} from './commands';

/**
 * One advanced feature of `ptbk coder` presented as a card with a terminal snippet.
 */
export type AdvancedFeatureDefinition = {
    /**
     * Short title of the feature
     */
    readonly title: string;

    /**
     * One- or two-sentence description of the feature
     */
    readonly description: string;

    /**
     * Terminal snippet demonstrating the feature
     */
    readonly sampleCommand: string;
};

/**
 * Advanced features of `ptbk coder` shown in the features grid, from everyday to power-user.
 *
 * Note: Specified in [`specs/sections/advanced-features.md`](../../specs/sections/advanced-features.md)
 */
export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
    {
        title: 'Verified by your tests',
        description:
            'Run any test command after each prompt. When it fails, ptbk coder hands the output back to the agent, which retries until the tests pass.',
        sampleCommand: 'ptbk coder run --harness claude-code --test npm test',
    },
    {
        title: 'Test before coding',
        description:
            'Run the tests before the first coding prompt. Stop on failures that were already there, or let one repair prompt fix them before the backlog starts.',
        sampleCommand: TEST_BEFORE_FIX_COMMAND,
    },
    {
        title: 'Commits with its own identity',
        description:
            'Every successful round lands under a git identity that belongs to the agent, GPG-signed if you set that up. You can always tell which commits it wrote.',
        sampleCommand: 'CODING_AGENT_GIT_NAME="Promptbook Coding Agent"',
    },
    {
        title: 'Autopilot git',
        description:
            'Pull before prompts and push after commits, so a long-running queue stays in sync with your remote.',
        sampleCommand: 'ptbk coder run --harness claude-code --auto-pull --auto-push',
    },
    {
        title: 'Git-synced housekeeping',
        description:
            'ptbk coder init, add, generate-boilerplates and verify all take the same --commit, --auto-push and --auto-pull switches. Setting up a project, queueing prompts and archiving finished ones leave no uncommitted work behind. Verify pulls and pushes around every single verification.',
        sampleCommand: INIT_GIT_SYNC_COMMAND,
    },
    {
        title: 'Isolated worktrees',
        description:
            'Implement every prompt in its own temporary git worktree with its own environment. Verified work lands back on your branch as one commit. If a task will not merge, ptbk coder marks it failed and keeps its worktree so you can look at it. Deeply nested repositories work on Windows too.',
        sampleCommand: 'ptbk coder run --harness claude-code --isolate',
    },
    {
        title: 'Kanban web UI',
        description:
            'ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser.',
        sampleCommand: 'ptbk coder server --port 4441 --harness claude-code',
    },
    {
        title: 'Prompt priorities',
        description: 'Give prompts a priority and process only the range you want in the current run.',
        sampleCommand: 'ptbk coder run --harness claude-code --min-priority 1 --max-priority 5',
    },
    {
        title: 'Model-specific prompts',
        description:
            'Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it.',
        sampleCommand: MODEL_FILTER_COMMAND,
    },
    {
        title: 'Pacing and retries',
        description:
            'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
        sampleCommand: 'ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m',
    },
    {
        title: 'List before you run',
        description:
            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
        sampleCommand: LIST_COMMAND,
    },
    {
        title: 'Ping before you queue',
        description:
            'ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore.',
        sampleCommand: PING_COMMAND,
    },
    {
        title: 'Keep the 5-hour window rolling',
        description:
            'Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit.',
        sampleCommand: PING_PERIOD_COMMAND,
    },
    {
        title: 'Human in the loop',
        description:
            'Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing.',
        sampleCommand: 'ptbk coder run --harness claude-code --no-auto',
    },
    {
        title: 'Nothing to answer',
        description:
            'The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it.',
        sampleCommand: 'ptbk coder run --harness claude-code --no-questions',
    },
    {
        title: 'Live status in the prompt file',
        description:
            'A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add <!--ptbk-coder-ignore--> anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done.',
        sampleCommand: '[^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress',
    },
    {
        title: 'Pick up where the run stopped',
        description:
            'A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it.',
        sampleCommand: GIT_CHANGES_CONTINUE_COMMAND,
    },
    {
        title: 'Run traces you can read later',
        description:
            'Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone.',
        sampleCommand: 'prompts/traces/2026-09-0180-add-dark-mode.md',
    },
    {
        title: 'Verify and archive',
        description:
            'Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random.',
        sampleCommand: VERIFY_COMMAND,
    },
    {
        title: 'Many prompts per file',
        description:
            'ptbk coder generate-boilerplates writes one prompt per file by default (--count 5*1). Use --count N*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task.',
        sampleCommand: GENERATE_BOILERPLATES_COMMAND,
    },
];
# Section: Advanced features

Anchor `#features`. A grid of feature cards, each pairing a short explanation with a terminal snippet — covering the "everything around the agent" machinery from [`../product.md`](../product.md).

## Copy

-   **Heading**: `Built for unattended coding` ("unattended" in Promptbook Blue).
-   **Lead paragraph**: The agent writes the code. ptbk coder does the rest: it runs your tests, commits, pulls and pushes, paces the queue against your quota window, and gives you back control the moment you press P or X. That is what keeps a run going for hours without you.

## Cards

3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:

| #   | Title                          | Snippet                                                                                          |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
| 1   | Verified by your tests         | `ptbk coder run --harness claude-code --test npm test`                                           |
| 2   | Test before coding             | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                 |
| 3   | Commits with its own identity  | `CODING_AGENT_GIT_NAME="Promptbook Coding Agent"`                                                |
| 4   | Autopilot git                  | `ptbk coder run --harness claude-code --auto-pull --auto-push`                                   |
| 5   | Git-synced housekeeping        | `ptbk coder init --auto-pull --commit --auto-push`                                               |
| 6   | Isolated worktrees             | `ptbk coder run --harness claude-code --isolate`                                                 |
| 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
| 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
| 11  | List before you run            | `ptbk coder list`                                                                                |
| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |

## Descriptions (verbatim card copy)

1. **Verified by your tests** — "Run any test command after each prompt. When it fails, ptbk coder hands the output back to the agent, which retries until the tests pass."
2. **Test before coding** — "Run the tests before the first coding prompt. Stop on failures that were already there, or let one repair prompt fix them before the backlog starts."
3. **Commits with its own identity** — "Every successful round lands under a git identity that belongs to the agent, GPG-signed if you set that up. You can always tell which commits it wrote."
4. **Autopilot git** — "Pull before prompts and push after commits, so a long-running queue stays in sync with your remote."
5. **Git-synced housekeeping** — "ptbk coder init, add, generate-boilerplates and verify all take the same --commit, --auto-push and --auto-pull switches. Setting up a project, queueing prompts and archiving finished ones leave no uncommitted work behind. Verify pulls and pushes around every single verification."
6. **Isolated worktrees** — "Implement every prompt in its own temporary git worktree with its own environment. Verified work lands back on your branch as one commit. If a task will not merge, ptbk coder marks it failed and keeps its worktree so you can look at it. Deeply nested repositories work on Windows too."
7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."

Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
# Product: what `ptbk coder` is

This spec defines the product story every section of the page must be consistent with.

## One-sentence definition

**`ptbk coder` is an orchestrator that drives existing AI coding agents (Claude Code, OpenAI Codex, GitHub Copilot, Gemini CLI, Qwen Code, opencode, Cline) through a queue of plain-markdown prompt files — implementing, testing, committing and pushing each task automatically.**

`ptbk coder` is a CLI tool shipped inside the [`ptbk` npm package](https://www.npmjs.com/package/ptbk). It is a **subproduct of Promptbook** (see [`sections/footer.md`](./sections/footer.md) for how this relationship is presented).

## Target visitor

A software developer who:

-   has **never heard of `ptbk coder`** or Promptbook,
-   **may know** Claude Code and/or OpenAI Codex,
-   wants to use AI agents to code and develop software.

Consequence: the page may reference Claude Code / Codex as familiar anchors, but must explain everything specific to `ptbk coder` from zero. No Promptbook internals may be assumed.

## Positioning

-   `ptbk coder` **does not replace** coding agents — it sits **one level above** them ("harnesses") and keeps them working through a whole backlog unattended.
-   The mental shift to communicate: _from interactive chat sessions_ (one task at a time) _to a versioned queue of prompt files_ (`prompts/` folder) processed autonomously.
-   The agent's behavior lives in a versioned `.book` file, not in one vendor's prompt box. The same queue and behavior can move between supported harnesses with `--harness`; opencode can use the provider, including a local model, configured in that harness.
-   A successful task records its `[x]` status in the PRD and commits that status with the implementation. Reverting the commit therefore returns both the code and the PRD state together.
-   Around the agent, `ptbk coder` adds the unattended-operation machinery: test verification with retry feedback, git commits under a dedicated agent identity (optionally GPG-signed), auto pull/push, isolated worktrees, pacing, priorities, a kanban web UI, and personas defined in the Book language.

## Core workflow (the loop the page must explain)

1. `ptbk coder init` scaffolds the project (see [`content/commands.md`](./content/commands.md)).
2. The developer writes each task as one markdown file in `prompts/`. A status checkbox is optional: a prompt without one starts ready at priority `0` and gains its status line when processing starts.
3. `ptbk coder run` (or `ptbk coder server`) feeds prompts one by one to the selected harness, with:
    - an optional agent persona from a `.book` file (`--agent`, see [`content/developer-agent.md`](./content/developer-agent.md)),
    - optional project context (`--context`, e.g. `AGENTS.md`).
4. When enabled, tests run before the first coding prompt (`--test-before`); existing failures either stop the run or create one repair prompt. After each prompt, the test command (`--test`) runs and failures are fed back to the agent, which retries until green.
5. The changes are committed under the agent git identity; optionally pushed (`--auto-push`). With `--isolate` the whole round happens in a temporary git worktree that is merged back into the current branch once the task is verified.
6. Finished prompts are verified and archived to `prompts/done/` (`ptbk coder verify`).
7. `ptbk coder server` additionally keeps running forever, watches `prompts/` for new files, and serves a Trello-style kanban board (default port 4441).

## Key subcommands

| Command             | Purpose                                                                                                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ptbk coder init`   | Scaffold `prompts/`, `prompts/done/`, `agents/developer.book`, `AGENTS.md`, `.env` agent-identity entries, `.gitignore`, `package.json` scripts, `.vscode/settings.json` |
| `ptbk coder run`    | Process the prompt queue once, then exit                                                                                                                                 |
| `ptbk coder server` | Same processing, but never exits; watches for new prompts; serves kanban UI                                                                                              |
| `ptbk coder verify` | Interactive verification of completed prompts; archives to `prompts/done/`                                                                                               |

## Tone

Practical, developer-to-developer, no hype. Terminal-first: whenever something can be shown as a command, show it as a command (see [`components/terminal-block.md`](./components/terminal-block.md)).
{
    "name": "coder-landing",
    "scripts": {
        "predev": "npx kill-port 4025",
        "dev": "next dev -p 4025",
        "test-build": "npm run build",
        "prebuild": "npx kill-port 4025",
        "build": "next build",
        "start": "next start -p 4025",
        "lint": "next lint --max-warnings=0",
        "postinstall": "cd ../../ && npm ci"
    }
}
apps/coder-landing\specs\sections\advanced-features.md:24:| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
apps/coder-landing\specs\sections\advanced-features.md:47:9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
apps/coder-landing\specs\content\commands.md:15:| `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
apps/coder-landing\specs\content\commands.md:34:-   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
apps/coder-landing\src\data\advancedFeatures.ts:6:    MODEL_FILTER_COMMAND,
apps/coder-landing\src\data\advancedFeatures.ts:87:        title: 'Model-specific prompts',
apps/coder-landing\src\data\advancedFeatures.ts:90:        sampleCommand: MODEL_FILTER_COMMAND,
apps/coder-landing\src\data\commands.ts:51:export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n --hidden --glob '"'!node_modules'"' 'Allow to specify the agent in the prompt|Developer Foo bar|Alongside the developer add there unslopper' prompts changelog .vscode" in C:\Users\me\work\ai\promptbook
 succeeded in 9280ms:
prompts\2026-09-0210-ptbk-coder-by-agent.md:3:[✨🎻] Allow to specify the agent in the prompt
prompts\2026-09-0210-ptbk-coder-by-agent.md:11:    -   Also `Developer Foo bar` should work - The name of the agent from the 1st line of the book file.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content prompts/2026-09-0210-ptbk-coder-by-agent.md' in C:\Users\me\work\ai\promptbook
 succeeded in 314ms:
[^] by OpenAI Codex `gpt-5.6-terra` thinking `max` - Implementation in progress

[✨🎻] Allow to specify the agent in the prompt

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md
```

-   You can already specify the harness or model, allow also to specify the agent.
-   Both `agents/coding/developer.book`, `developer.book` and `developer` should work
    -   Also `Developer Foo bar` should work - The name of the agent from the 1st line of the book file.
-   Allowto combine, the specifying, the harness, model, or agent,
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json), Alongside the `developer` add there `unslopper`
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing)
-   Add the changes into the [changelog](changelog/_current-preversion.md)


apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\cli\parseRunOptions.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\main\runCodexPrompts.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\agentCliOptions.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\list.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\run.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\server.ts
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --check; git diff -- scripts/run-codex-prompts/common/resolveCoderAgent.ts src/cli/cli-commands/coder/agentCliOptions.ts src/cli/cli-commands/coder/run.ts src/cli/cli-commands/coder/server.ts src/cli/cli-commands/coder/list.ts scripts/run-codex-prompts/main/runCodexPrompts.ts scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/cli/parseRunOptions.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 473ms:
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9..7c8d81c0c 100644
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@ const DEFAULT_WAIT_AFTER_ERROR_MS = 10 * 60 * 1000;
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@ const KNOWN_OPTION_FLAGS = new Set([
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@ export function parseRunOptions(args: string[]): RunOptions {
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@ export function parseRunOptions(args: string[]): RunOptions {
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43..7c1ec7d13 100644
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,19 +1,33 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
+    /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
     /**
      * Compiled system message injected into each coding prompt.
      */
@@ -21,34 +35,94 @@ export type ResolvedCoderAgent = {
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
     };
 }
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
+
+    return {
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+    };
+}
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd7..33d3dc116 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84..33858246b 100644
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@ import type { PromptFile } from './types/PromptFile';
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@ export type PromptRunnerIdentity = {
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@ export function isPromptCompatibleWithRunner(
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05..3d064ae9e 100644
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@ import {
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@ export function $initializeCoderListCommand(program: Program): $side_effect {
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fa..41382d38f 100644
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@ import {
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b..3a48ebbfb 100644
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@ import {
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\cli\parseRunOptions.test.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\resolveCoderAgent.test.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\prompts\promptRunnerFiltering.test.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\list.test.ts
C:\Users\me\work\ai\promptbook\src\cli\cli-commands\coder\run.test.ts
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx jest --runInBand scripts/run-codex-prompts/common/resolveCoderAgent.test.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts scripts/run-codex-prompts/cli/parseRunOptions.test.ts src/cli/cli-commands/coder/run.test.ts src/cli/cli-commands/coder/list.test.ts' in C:\Users\me\work\ai\promptbook
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 32885ms:
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts (7.453 s)
PASS src/cli/cli-commands/coder/run.test.ts
PASS scripts/run-codex-prompts/cli/parseRunOptions.test.ts
PASS src/cli/cli-commands/coder/list.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts

Test Suites: 5 passed, 5 total
Tests:       86 passed, 86 total
Snapshots:   0 total
Time:        10.542 s
Ran all test suites matching /scripts\\run-codex-prompts\\common\\resolveCoderAgent.test.ts|scripts\\run-codex-prompts\\prompts\\promptRunnerFiltering.test.ts|scripts\\run-codex-prompts\\cli\\parseRunOptions.test.ts|src\\cli\\cli-commands\\coder\\run.test.ts|src\\cli\\cli-commands\\coder\\list.test.ts/i.

codex
The routing layer now carries agent references from the selected Book, including the title parsed from its first line, while retaining the existing normalized substring matching used for models and harnesses. I’m updating the public examples next so the feature is discoverable rather than hidden behind status-line syntax.
2026-09-12T09:33:09.313187Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\advanced-features.md:
| 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
| 11  | List before you run            | `ptbk coder list`                                                                                |
| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
| 15  | Nothing to answer              | `ptbk coder run --no-questions`                                            |
| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- apps/coder-landing/src/data/commands.ts apps/coder-landing/src/data/advancedFeatures.ts apps/coder-landing/src/components/sections/AgentsSection.tsx apps/coder-landing/specs .vscode/terminals.json changelog/_current-preversion.md; Get-Content apps/coder-landing/specs/sections/advanced-features.md | Select-Object -First 100' in C:\Users\me\work\ai\promptbook
 succeeded in 475ms:
# Section: Advanced features

Anchor `#features`. A grid of feature cards, each pairing a short explanation with a terminal snippet — covering the "everything around the agent" machinery from [`../product.md`](../product.md).

## Copy

-   **Heading**: `Built for unattended coding` ("unattended" in Promptbook Blue).
-   **Lead paragraph**: The agent writes the code. ptbk coder does the rest: it runs your tests, commits, pulls and pushes, paces the queue against your quota window, and gives you back control the moment you press P or X. That is what keeps a run going for hours without you.

## Cards

3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:

| #   | Title                          | Snippet                                                                                          |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
| 1   | Verified by your tests         | `ptbk coder run --harness claude-code --test npm test`                                           |
| 2   | Test before coding             | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                 |
| 3   | Commits with its own identity  | `CODING_AGENT_GIT_NAME="Promptbook Coding Agent"`                                                |
| 4   | Autopilot git                  | `ptbk coder run --harness claude-code --auto-pull --auto-push`                                   |
| 5   | Git-synced housekeeping        | `ptbk coder init --auto-pull --commit --auto-push`                                               |
| 6   | Isolated worktrees             | `ptbk coder run --harness claude-code --isolate`                                                 |
| 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
| 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
| 11  | List before you run            | `ptbk coder list`                                                                                |
| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |

## Descriptions (verbatim card copy)

1. **Verified by your tests** — "Run any test command after each prompt. When it fails, ptbk coder hands the output back to the agent, which retries until the tests pass."
2. **Test before coding** — "Run the tests before the first coding prompt. Stop on failures that were already there, or let one repair prompt fix them before the backlog starts."
3. **Commits with its own identity** — "Every successful round lands under a git identity that belongs to the agent, GPG-signed if you set that up. You can always tell which commits it wrote."
4. **Autopilot git** — "Pull before prompts and push after commits, so a long-running queue stays in sync with your remote."
5. **Git-synced housekeeping** — "ptbk coder init, add, generate-boilerplates and verify all take the same --commit, --auto-push and --auto-pull switches. Setting up a project, queueing prompts and archiving finished ones leave no uncommitted work behind. Verify pulls and pushes around every single verification."
6. **Isolated worktrees** — "Implement every prompt in its own temporary git worktree with its own environment. Verified work lands back on your branch as one commit. If a task will not merge, ptbk coder marks it failed and keeps its worktree so you can look at it. Deeply nested repositories work on Windows too."
7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."

Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).

diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\src\components\sections\AgentsSection.tsx
C:\Users\me\work\ai\promptbook\apps\coder-landing\src\data\advancedFeatures.ts
C:\Users\me\work\ai\promptbook\apps\coder-landing\src\data\commands.ts
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\content\commands.md
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\agent-book.md
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\advanced-features.md
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..09b9287dfd34f9745b6e35ba5f1e41e92fe32153
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..09b9287dfd34f9745b6e35ba5f1e41e92fe32153
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\advanced-features.md
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\.vscode\terminals.json
C:\Users\me\work\ai\promptbook\changelog\_current-preversion.md
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --check; git status --short; git diff --stat; Get-Content src/cli/cli-commands/coder/list.ts; Get-Content apps/coder-landing/specs/sections/agent-book.md; Get-Content apps/coder-landing/specs/sections/advanced-features.md | Select-Object -First 100; Get-Content changelog/_current-preversion.md | Select-Object -First 35' in C:\Users\me\work\ai\promptbook
 succeeded in 767ms:
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
 M .vscode/terminals.json
 M apps/coder-landing/specs/content/commands.md
 M apps/coder-landing/specs/sections/advanced-features.md
 M apps/coder-landing/specs/sections/agent-book.md
 M apps/coder-landing/src/components/sections/AgentsSection.tsx
 M apps/coder-landing/src/data/advancedFeatures.ts
 M apps/coder-landing/src/data/commands.ts
 M changelog/_current-preversion.md
 M prompts/2026-09-0210-ptbk-coder-by-agent.md
 M scripts/run-codex-prompts/cli/parseRunOptions.test.ts
 M scripts/run-codex-prompts/cli/parseRunOptions.ts
 M scripts/run-codex-prompts/common/resolveCoderAgent.ts
 M scripts/run-codex-prompts/main/runCodexPrompts.ts
 M scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
 M scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
 M src/cli/cli-commands/coder/list.test.ts
 M src/cli/cli-commands/coder/list.ts
 M src/cli/cli-commands/coder/run.test.ts
 M src/cli/cli-commands/coder/run.ts
 M src/cli/cli-commands/coder/server.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
?? src/cli/cli-commands/coder/agentCliOptions.ts
 .vscode/terminals.json                             |  11 +++
 apps/coder-landing/specs/content/commands.md       |   5 +-
 .../specs/sections/advanced-features.md            |  48 +++++-----
 apps/coder-landing/specs/sections/agent-book.md    |   3 +-
 .../src/components/sections/AgentsSection.tsx      |   6 ++
 apps/coder-landing/src/data/advancedFeatures.ts    |   9 +-
 apps/coder-landing/src/data/commands.ts            |   6 ++
 changelog/_current-preversion.md                   |  11 +++
 prompts/2026-09-0210-ptbk-coder-by-agent.md        |   3 +-
 .../run-codex-prompts/cli/parseRunOptions.test.ts  |  17 ++++
 scripts/run-codex-prompts/cli/parseRunOptions.ts   |   5 +-
 .../run-codex-prompts/common/resolveCoderAgent.ts  | 102 ++++++++++++++++++---
 scripts/run-codex-prompts/main/runCodexPrompts.ts  |   1 +
 .../prompts/isPromptCompatibleWithRunner.ts        |  21 +++--
 .../prompts/promptRunnerFiltering.test.ts          |  45 +++++++++
 src/cli/cli-commands/coder/list.test.ts            |  46 ++++++++++
 src/cli/cli-commands/coder/list.ts                 |  18 +++-
 src/cli/cli-commands/coder/run.test.ts             |  29 ++++++
 src/cli/cli-commands/coder/run.ts                  |   8 +-
 src/cli/cli-commands/coder/server.ts               |   8 +-
 20 files changed, 340 insertions(+), 62 deletions(-)
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { handleActionErrors } from '../common/handleActionErrors';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import type { PromptRunnerSelectionCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerSelectionCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';

/**
 * Initializes `coder list` command for Promptbook CLI utilities.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderListCommand(program: Program): $side_effect {
    const command = program.command('list');
    command.description(
        spaceTrim(`
            List ready coding prompts by priority without executing them

            ${PROMPT_RUNNER_DESCRIPTION}

            Features:
            - Lists only ready, fully authored prompts
            - Groups prompts from highest to lowest priority
            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
            - Does not start a coding harness or modify prompt files
        `),
    );

    addPromptRunnerSelectionOptions(command);
    addCoderAgentOption(command);
    addPromptPriorityOptions(command);

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                agent,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
            } = cliOptions as {
                readonly agent?: string;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
            const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                cliOptions as PromptRunnerSelectionCliOptions,
                { isAgentRequired: false },
            );
            const resolvedCoderAgentBook =
                agent === undefined
                    ? undefined
                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
                          .resolveCoderAgentBook(agent, process.cwd());
            const promptRunnerIdentity =
                runnerOptions.agentName === undefined &&
                runnerOptions.model === undefined &&
                resolvedCoderAgentBook === undefined
                    ? undefined
                    : {
                          harnessName: runnerOptions.agentName,
                          modelName: runnerOptions.model,
                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                      };

            // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
            const { listCoderPrompts } = await import('../../../../scripts/run-codex-prompts/main/listCoderPrompts');
            await listCoderPrompts({
                priority,
                minimumPriority,
                maximumPriority,
                promptRunnerIdentity,
            });
        }),
    );
}

// Note: [🟡] Code for CLI command [list](src/cli/cli-commands/coder/list.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
# Section: Agent personas (Book)

Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.

## Copy

-   **Heading**: `Give your agent a soul, in plain text` ("soul" in Promptbook Green).
-   **Lead paragraph**: every run can carry an agent written in the **Book language** (link the words "Book language" to https://github.com/webgptorg/book), Promptbook's human-readable language for defining AI agents. Its rules, its persona and what it knows live in one `.book` file, and ptbk coder compiles that file into the system message of every coding prompt.

## Layout

Two columns on desktop (stacked on mobile):

### Left — the option

1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.

### Right — the agent itself

1. Caption above (small monospace, gray): `readonly preview of agents/developer.book`
2. The [readonly Book editor embed](../components/book-editor-embed.md) showing the verbatim source from [`../content/developer-agent.md`](../content/developer-agent.md).
# Section: Advanced features

Anchor `#features`. A grid of feature cards, each pairing a short explanation with a terminal snippet — covering the "everything around the agent" machinery from [`../product.md`](../product.md).

## Copy

-   **Heading**: `Built for unattended coding` ("unattended" in Promptbook Blue).
-   **Lead paragraph**: The agent writes the code. ptbk coder does the rest: it runs your tests, commits, pulls and pushes, paces the queue against your quota window, and gives you back control the moment you press P or X. That is what keeps a run going for hours without you.

## Cards

3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:

| #   | Title                          | Snippet                                                                                          |
| --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
| 1   | Verified by your tests         | `ptbk coder run --harness claude-code --test npm test`                                           |
| 2   | Test before coding             | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                 |
| 3   | Commits with its own identity  | `CODING_AGENT_GIT_NAME="Promptbook Coding Agent"`                                                |
| 4   | Autopilot git                  | `ptbk coder run --harness claude-code --auto-pull --auto-push`                                   |
| 5   | Git-synced housekeeping        | `ptbk coder init --auto-pull --commit --auto-push`                                               |
| 6   | Isolated worktrees             | `ptbk coder run --harness claude-code --isolate`                                                 |
| 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
| 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
| 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
| 12  | List before you run            | `ptbk coder list`                                                                                |
| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |

## Descriptions (verbatim card copy)

1. **Verified by your tests** — "Run any test command after each prompt. When it fails, ptbk coder hands the output back to the agent, which retries until the tests pass."
2. **Test before coding** — "Run the tests before the first coding prompt. Stop on failures that were already there, or let one repair prompt fix them before the backlog starts."
3. **Commits with its own identity** — "Every successful round lands under a git identity that belongs to the agent, GPG-signed if you set that up. You can always tell which commits it wrote."
4. **Autopilot git** — "Pull before prompts and push after commits, so a long-running queue stays in sync with your remote."
5. **Git-synced housekeeping** — "ptbk coder init, add, generate-boilerplates and verify all take the same --commit, --auto-push and --auto-pull switches. Setting up a project, queueing prompts and archiving finished ones leave no uncommitted work behind. Verify pulls and pushes around every single verification."
6. **Isolated worktrees** — "Implement every prompt in its own temporary git worktree with its own environment. Verified work lands back on your branch as one commit. If a task will not merge, ptbk coder marks it failed and keeps its worktree so you can look at it. Deeply nested repositories work on Windows too."
7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."

Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
-   Added agent-specific prompt routing to `ptbk coder`:

    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
        filename without `.book`, or the Book title from its first line. For example, a run using
        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
        `developer`, and `Promptbook Developer` routing tokens.
    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
        <path.book>` previews the same agent-specific queue without starting a harness.
    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
        documented agent-specific routing on the `ptbk coder` landing page.

-   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
    `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
    pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
    own `Deprecated:` paragraph, so the listing which is supposed to introduce the CLI was mostly made of commands
    nobody should start using. `ptbk --help`, and the same listing which plain `ptbk` prints when it asks for a
    subcommand, now offers only the commands which are meant to be used - `coder`, `agent`, `agent-folder`,
    `agents-server`, `about` and `hello`. Nothing was removed: every deprecated command still runs, still warns that it
    is deprecated when it is used, still explains the deprecation in its own help - `ptbk run --help` - and is still
    reachable through `ptbk help run`. Only the "did you mean" hint for a mistyped command stops pointing at them.
    Being left out of the help is now simply a part of what deprecating a command means, written once in
    `$deprecateCliCommand`, so any command deprecated later disappears from the help on its own.

-   `ptbk` without a subcommand no longer falls back to the deprecated `ptbk run`. Until now `run` was registered as
    the default command of the CLI, so everything which did not name a command - `ptbk ./write-cv.book`, a mistyped
    `ptbk codr` or a stray option - was quietly handed over to the pipeline runner of the old system, which was the
    last thing a deprecated command should be doing. Plain `ptbk` now asks to specify a subcommand and prints the
    top-level help, exactly like `ptbk coder`, `ptbk agent`, `ptbk agent-folder` and `ptbk agents-server` already did
    when they were called without one of their own subcommands, and that one shared behavior is now written once
    instead of five times. Anything which is not a command is refused with `unknown command` and a "did you mean"
    hint instead of being executed as a pipeline, so a script which still passes a book path to `ptbk` fails loudly
    rather than appearing to succeed. `ptbk run` itself is untouched and keeps working for everyone who asks for it
    explicitly. The command list in the help now starts with `coder`, followed by `agent`, `agent-folder` and
    `agents-server`, and the deprecated commands of the old pipeline system are listed after them.


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --runInBand scripts/run-codex-prompts/common/resolveCoderAgent.test.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts scripts/run-codex-prompts/cli/parseRunOptions.test.ts src/cli/cli-commands/coder/run.test.ts src/cli/cli-commands/coder/list.test.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 4642ms:
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
PASS src/cli/cli-commands/coder/run.test.ts
PASS scripts/run-codex-prompts/cli/parseRunOptions.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
PASS src/cli/cli-commands/coder/list.test.ts

Test Suites: 5 passed, 5 total
Tests:       86 passed, 86 total
Snapshots:   0 total
Time:        3.015 s, estimated 9 s
Ran all test suites matching /scripts\\run-codex-prompts\\common\\resolveCoderAgent.test.ts|scripts\\run-codex-prompts\\prompts\\promptRunnerFiltering.test.ts|scripts\\run-codex-prompts\\cli\\parseRunOptions.test.ts|src\\cli\\cli-commands\\coder\\run.test.ts|src\\cli\\cli-commands\\coder\\list.test.ts/i.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..211afeb77e1c9560792005683faa81c36ec3825e
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with `[ ] use agent `developer``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Focused coverage is passing: agent aliases route correctly, Book-title matching works after normalization, legacy parsing keeps `--agent`, and `coder list --agent` forwards the same identity. I’ve also added the adjacent Unslopper VS Code preset and landing-page guidance.
apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\sections\agent-book.md
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-types' in C:\Users\me\work\ai\promptbook
 succeeded in 25127ms:

> promptbook@0.114.0-33 test-types
> tsc


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-build' in C:\Users\me\work\ai\promptbook\apps\coder-landing
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-lint; npm run test-spellcheck' in C:\Users\me\work\ai\promptbook
 succeeded in 64977ms:

> test-build
> npm run build


> prebuild
> npx kill-port 4025

Process on port 4025 killed

> build
> next build

   ▲ Next.js 15.4.11
   - Experiments (use with caution):
     ✓ externalDir

   Creating an optimized production build ...
 ✓ Compiled successfully in 14.0s
   Linting and checking validity of types ...
   Collecting page data ...
   Generating static pages (0/8) ...
   Generating static pages (2/8) 
   Generating static pages (4/8) 
   Generating static pages (6/8) 
 ✓ Generating static pages (8/8)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                 Size  First Load JS  Revalidate  Expire
┌ ○ /                                     169 kB         269 kB          1h      1y
├ ○ /_not-found                            988 B         101 kB
├ ○ /opengraph-image                       134 B         100 kB
├ ○ /robots.txt                            134 B         100 kB
├ ○ /sitemap.xml                           134 B         100 kB
└ ○ /twitter-image                         134 B         100 kB
+ First Load JS shared by all             100 kB
  ├ chunks/87c73c54-1f4741035a95c140.js  54.1 kB
  ├ chunks/902-fac49f818fe98178.js         44 kB
  └ other shared chunks (total)          2.08 kB


○  (Static)  prerendered as static content


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-lint' in C:\Users\me\work\ai\promptbook
 succeeded in 26638ms:

> promptbook@0.114.0-33 test-lint
> eslint src


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 63827ms:

> promptbook@0.114.0-33 test-lint
> eslint src


> promptbook@0.114.0-33 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/2072 src/_packages/anthropic-claude.index.ts 1347.59ms
   2/2072 src/_packages/anthropic-claude.readme.md 112.99ms
   3/2072 src/_packages/azure-openai.index.ts 5.36ms
   4/2072 src/_packages/azure-openai.readme.md 15.76ms
   5/2072 src/_packages/browser.index.ts 6.31ms
   6/2072 src/_packages/browser.readme.md 32.50ms
   7/2072 src/_packages/cli.index.ts 23.07ms
   8/2072 src/_packages/cli.readme.md 32.30ms
   9/2072 src/_packages/color.index.ts 7.75ms
  10/2072 src/_packages/color.readme.md 67.23ms
  11/2072 src/_packages/components.index.ts 40.51ms
  12/2072 src/_packages/components.readme.md 13.80ms
  13/2072 src/_packages/content/$provideExecutionToolsForNode.md 7.00ms
  14/2072 src/_packages/content/providers.md 2.15ms
  15/2072 src/_packages/core.index.ts 49.00ms
  16/2072 src/_packages/core.readme.md 59.00ms
  17/2072 src/_packages/deepseek.index.ts 2.99ms
  18/2072 src/_packages/deepseek.readme.md 1.67ms
  19/2072 src/_packages/documents.index.ts 2.43ms
  20/2072 src/_packages/documents.readme.md 1.85ms
  21/2072 src/_packages/editable.index.ts 8.64ms
  22/2072 src/_packages/editable.readme.md 3.32ms
  23/2072 src/_packages/fake-llm.index.ts 2.40ms
  24/2072 src/_packages/fake-llm.readme.md 2.87ms
  25/2072 src/_packages/google.index.ts 3.34ms
  26/2072 src/_packages/google.readme.md 10.06ms
  27/2072 src/_packages/javascript.index.ts 3.45ms
  28/2072 src/_packages/javascript.readme.md 5.31ms
  29/2072 src/_packages/legacy-documents.index.ts 2.60ms
  30/2072 src/_packages/legacy-documents.readme.md 1.85ms
  31/2072 src/_packages/markdown-utils.index.ts 7.15ms
  32/2072 src/_packages/markdown-utils.readme.md 15.89ms
  33/2072 src/_packages/markitdown.index.ts 4.17ms
  34/2072 src/_packages/markitdown.readme.md 1.61ms
  35/2072 src/_packages/node.index.ts 5.82ms
  36/2072 src/_packages/node.readme.md 7.00ms
  37/2072 src/_packages/ollama.index.ts 3.02ms
  38/2072 src/_packages/ollama.readme.md 2.48ms
  39/2072 src/_packages/openai.index.ts 9.82ms
  40/2072 src/_packages/openai.readme.md 23.56ms
  41/2072 src/_packages/pdf.index.ts 3.16ms
  42/2072 src/_packages/pdf.readme.md 1.61ms
  43/2072 src/_packages/promptbook.readme.md 1.64ms
  44/2072 src/_packages/ptbk.readme.md 1.44ms
  45/2072 src/_packages/README.md 2.84ms
  46/2072 src/_packages/remote-client.index.ts 3.88ms
  47/2072 src/_packages/remote-client.readme.md 1.50ms
  48/2072 src/_packages/remote-server.index.ts 2.56ms
  49/2072 src/_packages/remote-server.readme.md 5.62ms
  50/2072 src/_packages/templates.index.ts 1.90ms
  51/2072 src/_packages/templates.readme.md 1.91ms
  52/2072 src/_packages/types.index.ts 75.23ms
  53/2072 src/_packages/types.readme.md 25.98ms
  54/2072 src/_packages/utils.index.ts 17.27ms
  55/2072 src/_packages/utils.readme.md 18.21ms
  56/2072 src/_packages/vercel.index.ts 5.56ms
  57/2072 src/_packages/vercel.readme.md 6.65ms
  58/2072 src/_packages/website-crawler.index.ts 2.44ms
  59/2072 src/_packages/website-crawler.readme.md 1.46ms
  60/2072 src/_packages/wizard.index.ts 7.84ms
  61/2072 src/_packages/wizard.readme.md 1.85ms
  62/2072 src/avatars/Avatar.tsx 52.13ms
  63/2072 src/avatars/avatarAnimationScheduler.ts 13.11ms
  64/2072 src/avatars/avatarInteractionUtils.test.ts 6.20ms
  65/2072 src/avatars/avatarInteractionUtils.ts 28.23ms
  66/2072 src/avatars/AvatarOrImage.tsx 8.35ms
  67/2072 src/avatars/avatarPointerTracking.ts 8.19ms
  68/2072 src/avatars/avatarRenderingUtils.test.ts 6.79ms
  69/2072 src/avatars/avatarRenderingUtils.ts 22.05ms
  70/2072 src/avatars/avatarVisibilityTracking.ts 4.66ms
  71/2072 src/avatars/index.ts 3.89ms
  72/2072 src/avatars/renderAvatarVisual.ts 7.89ms
  73/2072 src/avatars/renderAvatarVisualAsciiArt.ts 23.29ms
  74/2072 src/avatars/renderAvatarVisualTerminalText.test.ts 33.51ms
  75/2072 src/avatars/renderAvatarVisualTerminalText.ts 28.09ms
  76/2072 src/avatars/types/AvatarDefinition.ts 2.86ms
  77/2072 src/avatars/types/AvatarVisualDefinition.ts 8.43ms
  78/2072 src/avatars/visuals/asciiOctopusAvatarVisual.ts 41.86ms
  79/2072 src/avatars/visuals/avatar3dProjectionShared.ts 9.97ms
  80/2072 src/avatars/visuals/avatarVisualRegistry.test.ts 4.25ms
  81/2072 src/avatars/visuals/avatarVisualRegistry.ts 5.09ms
  82/2072 src/avatars/visuals/fractalAvatarVisual.ts 14.78ms
  83/2072 src/avatars/visuals/minecraft2AvatarVisual.ts 16.26ms
  84/2072 src/avatars/visuals/minecraftAvatarVisual.ts 10.61ms
  85/2072 src/avatars/visuals/minecraftAvatarVisualShared.ts 12.97ms
  86/2072 src/avatars/visuals/octopus2AvatarVisual.ts 37.94ms
  87/2072 src/avatars/visuals/octopus3AvatarVisual.test.ts 5.40ms
  88/2072 src/avatars/visuals/octopus3AvatarVisual.ts 26.68ms
  89/2072 src/avatars/visuals/octopus3d2AvatarVisual.ts 43.73ms
  90/2072 src/avatars/visuals/octopus3d3AvatarVisual.ts 41.27ms
  91/2072 src/avatars/visuals/octopus3d4AvatarVisual.ts 51.58ms
  92/2072 src/avatars/visuals/octopus3dAvatarVisual.ts 36.50ms
  93/2072 src/avatars/visuals/octopus3dAvatarVisualShared.ts 26.11ms
  94/2072 src/avatars/visuals/octopusAvatarVisual.ts 11.79ms
  95/2072 src/avatars/visuals/octopusAvatarVisualShared.test.ts 50.59ms
  96/2072 src/avatars/visuals/octopusAvatarVisualShared.ts 27.38ms
  97/2072 src/avatars/visuals/orbAvatarVisual.test.ts 8.24ms
  98/2072 src/avatars/visuals/orbAvatarVisual.ts 41.89ms
  99/2072 src/avatars/visuals/pixelArtAvatarVisual.ts 8.54ms
 100/2072 src/book-2.0/agent-source/AgentBasicInformation.ts 7.22ms
 101/2072 src/book-2.0/agent-source/AgentModelRequirements.ts 4.97ms
 102/2072 src/book-2.0/agent-source/AgentReferenceResolver.ts 3.87ms
 103/2072 src/book-2.0/agent-source/AgentSourceParseResult.ts 8.37ms
 104/2072 src/book-2.0/agent-source/agentSourceVisibility.test.ts 3.15ms
 105/2072 src/book-2.0/agent-source/agentSourceVisibility.ts 13.27ms
 106/2072 src/book-2.0/agent-source/BookEditable.ts 3.66ms
 107/2072 src/book-2.0/agent-source/communication-samples.test.ts 4.78ms
 108/2072 src/book-2.0/agent-source/computeAgentHash.test.ts 1.99ms
 109/2072 src/book-2.0/agent-source/computeAgentHash.ts 3.68ms
 110/2072 src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts 4.12ms
 111/2072 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 8.71ms
 112/2072 src/book-2.0/agent-source/createAgentModelRequirements.ts 8.44ms
 113/2072 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 5.61ms
 114/2072 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 3.72ms
 115/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 3.22ms
 116/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 3.05ms
 117/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 3.13ms
 118/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 4.55ms
 119/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 3.16ms
 120/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 4.46ms
 121/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 6.32ms
 122/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts 4.14ms
 123/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts 10.02ms
 124/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts 13.78ms
 125/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements.ts 6.10ms
 126/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources.ts 4.94ms
 127/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/ParsedAgentSourceWithCommitments.ts 1.95ms
 128/2072 src/book-2.0/agent-source/createCommitmentRegex.ts 3.18ms
 129/2072 src/book-2.0/agent-source/createDefaultAgentName.ts 2.31ms
 130/2072 src/book-2.0/agent-source/createTeamToolName.ts 2.99ms
 131/2072 src/book-2.0/agent-source/extractMetaLinks.ts 2.27ms
 132/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 2.34ms
 133/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 3.38ms
 134/2072 src/book-2.0/agent-source/getBookCommitmentLineType.ts 3.57ms
 135/2072 src/book-2.0/agent-source/normalizeAgentName.test.ts 3.24ms
 136/2072 src/book-2.0/agent-source/normalizeAgentName.ts 3.05ms
 137/2072 src/book-2.0/agent-source/padBook.ts 2.63ms
 138/2072 src/book-2.0/agent-source/parseAgentSource.import.test.ts 3.39ms
 139/2072 src/book-2.0/agent-source/parseAgentSource.test.ts 21.42ms
 140/2072 src/book-2.0/agent-source/parseAgentSource.ts 5.68ms
 141/2072 src/book-2.0/agent-source/parseAgentSource/applyMetaCommitment.ts 15.53ms
 142/2072 src/book-2.0/agent-source/parseAgentSource/consumeConversationSampleCommitment.ts 2.94ms
 143/2072 src/book-2.0/agent-source/parseAgentSource/createCapabilitiesFromCommitment.ts 9.91ms
 144/2072 src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts 2.06ms
 145/2072 src/book-2.0/agent-source/parseAgentSource/extractAgentProfileText.ts 2.94ms
 146/2072 src/book-2.0/agent-source/parseAgentSource/extractInitialMessage.ts 2.12ms
 147/2072 src/book-2.0/agent-source/parseAgentSource/extractParsedAgentProfile.ts 3.85ms
 148/2072 src/book-2.0/agent-source/parseAgentSource/ParseAgentSourceState.ts 2.26ms
 149/2072 src/book-2.0/agent-source/parseAgentSource/ParsedAgentProfile.ts 2.16ms
 150/2072 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 3.09ms
 151/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 3.85ms
 152/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 4.78ms
 153/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 7.86ms
 154/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts 3.41ms
 155/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts 2.77ms
 156/2072 src/book-2.0/agent-source/ParsedUnknownCommitment.ts 4.14ms
 157/2072 src/book-2.0/agent-source/parseParameters.ts 4.66ms
 158/2072 src/book-2.0/agent-source/parseTeamCommitment.ts 10.27ms
 159/2072 src/book-2.0/agent-source/pseudoAgentReferences.ts 9.26ms
 160/2072 src/book-2.0/agent-source/README.md 2.40ms
 161/2072 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 2.66ms
 162/2072 src/book-2.0/agent-source/string_book.ts 3.39ms
 163/2072 src/book-2.0/agent-source/TeammateProfileResolver.ts 2.73ms
 164/2072 src/book-2.0/book-language-documentation/BookLanguageDocumentationExample.ts 2.92ms
 165/2072 src/book-2.0/book-language-documentation/bookLanguageDocumentationExamples.ts 4.61ms
 166/2072 src/book-2.0/book-language-documentation/BookLanguageManualDictionary.ts 13.30ms
 167/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts 6.86ms
 168/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.ts 21.75ms
 169/2072 src/book-2.0/book-language-documentation/englishBookLanguageManualDictionary.ts 13.92ms
 170/2072 src/book-2.0/book-language-documentation/getBookLanguageManualCommitmentGroups.ts 4.23ms
 171/2072 src/book-2.0/book-language-documentation/getBookLanguageManualDictionary.ts 3.32ms
 172/2072 src/book-2.0/book-language-documentation/getSafeCodeBlock.ts 5.30ms
 173/2072 src/book-2.0/book-language-documentation/renderCommitmentCatalogSection.ts 4.49ms
 174/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts 6.02ms
 175/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.ts 14.46ms
 176/2072 src/book-2.0/book-language-documentation/toStableAnchorId.ts 57.36ms
 177/2072 src/book-2.0/README.md 1.47ms
 178/2072 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 2.80ms
 179/2072 src/book-3.0/agentFolderPaths.ts 114.75ms
 180/2072 src/book-3.0/AgentMessageRunReport.test.ts 4.75ms
 181/2072 src/book-3.0/AgentMessageRunReport.ts 41.79ms
 182/2072 src/book-3.0/AgentPlannedMessagesSidecar.test.ts 5.66ms
 183/2072 src/book-3.0/AgentPlannedMessagesSidecar.ts 10.66ms
 184/2072 src/book-3.0/AgentTeamConversationWorkspace.ts 6.45ms
 185/2072 src/book-3.0/Book.test.ts 25.99ms
 186/2072 src/book-3.0/Book.ts 9.02ms
 187/2072 src/book-3.0/BookNodeAgentSource.ts 5.26ms
 188/2072 src/book-3.0/CliAgent.test.ts 5.73ms
 189/2072 src/book-3.0/CliAgent.ts 11.83ms
 190/2072 src/book-3.0/cliAgentEnv.ts 3.33ms
 191/2072 src/book-3.0/codexLoginMethod.ts 3.70ms
 192/2072 src/book-3.0/createAgentMessageSidecarBaseName.ts 3.12ms
 193/2072 src/book-3.0/describeAgentPlannedMessageSchedule.ts 5.42ms
 194/2072 src/book-3.0/LiteAgent.test.ts 19.39ms
 195/2072 src/book-3.0/LiteAgent.ts 37.57ms
 196/2072 src/book-components/_common/Dropdown/Dropdown.tsx 27.74ms
 197/2072 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 3.17ms
 198/2072 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 3.49ms
 199/2072 src/book-components/_common/Modal/Modal.tsx 4.62ms
 200/2072 src/book-components/_common/MonacoEditorWithShadowDom.tsx 2.62ms
 201/2072 src/book-components/_common/react-utils/classNames.ts 1.74ms
 202/2072 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 3.16ms
 203/2072 src/book-components/_common/react-utils/escapeRegex.tsx 2.33ms
 204/2072 src/book-components/_common/Tooltip/Tooltip.tsx 7.48ms
 205/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 16.65ms
 206/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 2.96ms
 207/2072 src/book-components/AvatarProfile/AvatarChip/index.ts 1.72ms
 208/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 5.50ms
 209/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 3.71ms
 210/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 2.90ms
 211/2072 src/book-components/AvatarProfile/README.md 1.17ms
 212/2072 src/book-components/BookEditor/BookEditor.tsx 26.86ms
 213/2072 src/book-components/BookEditor/BookEditorAboutPromptbookInformation.tsx 8.11ms
 214/2072 src/book-components/BookEditor/BookEditorActionbar.tsx 48.39ms
 215/2072 src/book-components/BookEditor/BookEditorBrowserConfig.ts 2.07ms
 216/2072 src/book-components/BookEditor/BookEditorForClient.tsx 2.70ms
 217/2072 src/book-components/BookEditor/BookEditorMonaco.tsx 21.46ms
 218/2072 src/book-components/BookEditor/BookEditorMonacoConstants.ts 2.08ms
 219/2072 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 3.55ms
 220/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 3.13ms
 221/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 12.43ms
 222/2072 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 8.75ms
 223/2072 src/book-components/BookEditor/BookEditorSyntaxTokenStyles.ts 5.50ms
 224/2072 src/book-components/BookEditor/BookEditorTheme.ts 6.54ms
 225/2072 src/book-components/BookEditor/createBookSourceHighlightStyles.ts 5.29ms
 226/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.browser.ts 17.88ms
 227/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 7.63ms
 228/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 5.06ms
 229/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts 4.35ms
 230/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.ts 5.28ms
 231/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts 9.29ms
 232/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.ts 4.48ms
 233/2072 src/book-components/BookEditor/highlightBookSourceToHtml.ts 9.18ms
 234/2072 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 5.72ms
 235/2072 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 8.62ms
 236/2072 src/book-components/BookEditor/useBookEditorMonacoInteractions.ts 11.91ms
 237/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 15.12ms
 238/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 33.91ms
 239/2072 src/book-components/BookEditor/useBookEditorMonacoLifecycle.ts 9.75ms
 240/2072 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 6.97ms
 241/2072 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 6.41ms
 242/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/bookEditorMonacoUploadTypes.ts 6.85ms
 243/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/clearScheduledTimer.ts 2.63ms
 244/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/enqueueFilesForUpload.ts 12.30ms
 245/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadEditorSync.ts 8.39ms
 246/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadItemsState.ts 6.57ms
 247/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadProgressQueue.ts 7.33ms
 248/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadQueue.ts 12.47ms
 249/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useCompletedUploadsAutoClear.ts 4.07ms
 250/2072 src/book-components/Chat/AgentChat/AgentChat.test.tsx 11.06ms
 251/2072 src/book-components/Chat/AgentChat/AgentChat.tsx 8.12ms
 252/2072 src/book-components/Chat/AgentChat/AgentChatProps.tsx 3.91ms
 253/2072 src/book-components/Chat/AgentChip/AgentChip.tsx 5.29ms
 254/2072 src/book-components/Chat/AgentChip/index.ts 1.71ms
 255/2072 src/book-components/Chat/Chat/Chat.tsx 27.02ms
 256/2072 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 8.22ms
 257/2072 src/book-components/Chat/Chat/ChatActionsBar.tsx 21.97ms
 258/2072 src/book-components/Chat/Chat/ChatCitationModal.test.tsx 4.77ms
 259/2072 src/book-components/Chat/Chat/ChatCitationModal.tsx 14.20ms
 260/2072 src/book-components/Chat/Chat/chatCssClassNames.ts 5.37ms
 261/2072 src/book-components/Chat/Chat/ChatFeedbackStar.tsx 11.30ms
 262/2072 src/book-components/Chat/Chat/ChatImageAttachmentModal.tsx 6.13ms
 263/2072 src/book-components/Chat/Chat/ChatInputArea.test.tsx 10.75ms
 264/2072 src/book-components/Chat/Chat/ChatInputArea.tsx 30.25ms
 265/2072 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 12.81ms
 266/2072 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 1.83ms
 267/2072 src/book-components/Chat/Chat/ChatMessageAttachments.tsx 7.87ms
 268/2072 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 24.71ms
 269/2072 src/book-components/Chat/Chat/ChatMessageItem.tsx 54.86ms
 270/2072 src/book-components/Chat/Chat/ChatMessageList.tsx 13.35ms
 271/2072 src/book-components/Chat/Chat/ChatMessageMap.tsx 47.13ms
 272/2072 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 6.63ms
 273/2072 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 8.11ms
 274/2072 src/book-components/Chat/Chat/ChatProps.tsx 60.68ms
 275/2072 src/book-components/Chat/Chat/ChatRatingModal.test.tsx 9.71ms
 276/2072 src/book-components/Chat/Chat/ChatRatingModal.tsx 17.69ms
 277/2072 src/book-components/Chat/Chat/ChatReplyPreview.tsx 4.17ms
 278/2072 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 6.45ms
 279/2072 src/book-components/Chat/Chat/ChatSoundToggle.tsx 14.79ms
 280/2072 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 15.42ms
 281/2072 src/book-components/Chat/Chat/ChatToolCallModal.tsx 13.62ms
 282/2072 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 23.33ms
 283/2072 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 9.20ms
 284/2072 src/book-components/Chat/Chat/CitationIframePreview.test.tsx 5.33ms
 285/2072 src/book-components/Chat/Chat/CitationIframePreview.tsx 7.25ms
 286/2072 src/book-components/Chat/Chat/ClockIcon.tsx 3.18ms
 287/2072 src/book-components/Chat/Chat/constants.tsx 1.94ms
 288/2072 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 17.54ms
 289/2072 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 6.50ms
 290/2072 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 6.65ms
 291/2072 src/book-components/Chat/Chat/insertDictationChunk.test.ts 2.91ms
 292/2072 src/book-components/Chat/Chat/insertDictationChunk.ts 5.09ms
 293/2072 src/book-components/Chat/Chat/learnDictationDictionary.test.ts 2.05ms
 294/2072 src/book-components/Chat/Chat/learnDictationDictionary.ts 3.81ms
 295/2072 src/book-components/Chat/Chat/pagePreview/createPagePreviewSessionId.ts 3.42ms
 296/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx 10.38ms
 297/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.tsx 25.62ms
 298/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreviewToolbar.tsx 14.77ms
 299/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts 5.22ms
 300/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.ts 19.82ms
 301/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputEvent.ts 9.21ms
 302/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts 4.80ms
 303/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.ts 11.33ms
 304/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewSessionState.ts 3.10ms
 305/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewViewport.ts 3.71ms
 306/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts 14.99ms
 307/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 6.38ms
 308/2072 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 25.03ms
 309/2072 src/book-components/Chat/Chat/renderAgentProjectDiff.tsx 5.47ms
 310/2072 src/book-components/Chat/Chat/renderAgentProjectToolCallDetails.tsx 29.81ms
 311/2072 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 10.12ms
 312/2072 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 18.38ms
 313/2072 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 9.95ms
 314/2072 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 32.55ms
 315/2072 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 7.30ms
 316/2072 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 9.79ms
 317/2072 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 20.07ms
 318/2072 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 11.02ms
 319/2072 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 4.15ms
 320/2072 src/book-components/Chat/Chat/renderToolCallDetails.tsx 31.40ms
 321/2072 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 3.98ms
 322/2072 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 9.94ms
 323/2072 src/book-components/Chat/Chat/resolveChatRatingMessageKey.ts 1.88ms
 324/2072 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 15.16ms
 325/2072 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 4.98ms
 326/2072 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 2.79ms
 327/2072 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 7.72ms
 328/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx 6.00ms
 329/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 22.45ms
 330/2072 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 7.39ms
 331/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx 4.53ms
 332/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 23.67ms
 333/2072 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 25.10ms
 334/2072 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 20.46ms
 335/2072 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 8.42ms
 336/2072 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 5.47ms
 337/2072 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 7.90ms
 338/2072 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 7.74ms
 339/2072 src/book-components/Chat/Chat/useChatScrollState.ts 11.68ms
 340/2072 src/book-components/Chat/Chat/useChatToolCallModalState.ts 29.71ms
 341/2072 src/book-components/Chat/Chat/useChatToolCallState.ts 7.01ms
 342/2072 src/book-components/Chat/CodeBlock/CodeBlock.tsx 14.51ms
 343/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 3.32ms
 344/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 3.25ms
 345/2072 src/book-components/Chat/effects/ChatEffectsSystem.tsx 5.24ms
 346/2072 src/book-components/Chat/effects/components/ConfettiEffect.tsx 4.19ms
 347/2072 src/book-components/Chat/effects/components/HeartsEffect.tsx 4.57ms
 348/2072 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 2.36ms
 349/2072 src/book-components/Chat/effects/index.ts 19.08ms
 350/2072 src/book-components/Chat/effects/types/ChatEffect.ts 2.11ms
 351/2072 src/book-components/Chat/effects/types/ChatEffectConfig.ts 14.21ms
 352/2072 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 6.82ms
 353/2072 src/book-components/Chat/effects/types/ChatEffectType.ts 2.44ms
 354/2072 src/book-components/Chat/effects/utils/detectEffects.ts 3.04ms
 355/2072 src/book-components/Chat/hooks/index.ts 4.07ms
 356/2072 src/book-components/Chat/hooks/useChatActionsOverlap.ts 10.63ms
 357/2072 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 8.45ms
 358/2072 src/book-components/Chat/hooks/useChatAutoScroll.ts 27.23ms
 359/2072 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 9.78ms
 360/2072 src/book-components/Chat/hooks/useChatCompleteNotification.ts 7.47ms
 361/2072 src/book-components/Chat/hooks/useChatRatings.ts 19.46ms
 362/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx 4.64ms
 363/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.ts 12.62ms
 364/2072 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 7.14ms
 365/2072 src/book-components/Chat/LlmChat/defaults.ts 2.46ms
 366/2072 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 7.92ms
 367/2072 src/book-components/Chat/LlmChat/LlmChat.test.tsx 18.59ms
 368/2072 src/book-components/Chat/LlmChat/LlmChat.tsx 11.78ms
 369/2072 src/book-components/Chat/LlmChat/LlmChatProps.tsx 9.60ms
 370/2072 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 34.76ms
 371/2072 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 4.95ms
 372/2072 src/book-components/Chat/LlmChat/useLlmChatState.ts 22.53ms
 373/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 9.91ms
 374/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 17.19ms
 375/2072 src/book-components/Chat/MockedChat/constants.ts 3.88ms
 376/2072 src/book-components/Chat/MockedChat/MockedChat.tsx 30.15ms
 377/2072 src/book-components/Chat/README.md 11.46ms
 378/2072 src/book-components/Chat/save/_common/chatExportRendering.ts 17.33ms
 379/2072 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 4.56ms
 380/2072 src/book-components/Chat/save/_common/ChatSaveFormatHandler.ts 3.37ms
 381/2072 src/book-components/Chat/save/_common/createChatExportFilename.ts 5.03ms
 382/2072 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 12.64ms
 383/2072 src/book-components/Chat/save/_common/getPromptbookExportBranding.ts 4.54ms
 384/2072 src/book-components/Chat/save/_common/string_chat_format_name.ts 1.81ms
 385/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts 7.71ms
 386/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 52.76ms
 387/2072 src/book-components/Chat/save/index.ts 3.76ms
 388/2072 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 2.41ms
 389/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts 4.50ms
 390/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 7.75ms
 391/2072 src/book-components/Chat/save/pdf/buildChatPdf.ts 80.55ms
 392/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts 12.96ms
 393/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 2.44ms
 394/2072 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 5.55ms
 395/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts 3.35ms
 396/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 5.69ms
 397/2072 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.38ms
 398/2072 src/book-components/Chat/SourceChip/index.ts 1.98ms
 399/2072 src/book-components/Chat/SourceChip/SourceChip.test.tsx 4.40ms
 400/2072 src/book-components/Chat/SourceChip/SourceChip.tsx 17.75ms
 401/2072 src/book-components/Chat/types/ChatMessage.ts 23.51ms
 402/2072 src/book-components/Chat/types/ChatParticipant.ts 4.21ms
 403/2072 src/book-components/Chat/types/CitationLabelResolver.ts 2.53ms
 404/2072 src/book-components/Chat/utils/$provideServerDomWindow.ts 7.17ms
 405/2072 src/book-components/Chat/utils/addUtmParamsToUrl.ts 16.75ms
 406/2072 src/book-components/Chat/utils/agentProjectToolCall.test.ts 5.10ms
 407/2072 src/book-components/Chat/utils/agentProjectToolCall.ts 7.87ms
 408/2072 src/book-components/Chat/utils/ChatPersistence.test.ts 7.77ms
 409/2072 src/book-components/Chat/utils/ChatPersistence.ts 20.03ms
 410/2072 src/book-components/Chat/utils/citationHelpers.test.ts 6.04ms
 411/2072 src/book-components/Chat/utils/citationHelpers.ts 9.04ms
 412/2072 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 9.59ms
 413/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 3.26ms
 414/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 13.69ms
 415/2072 src/book-components/Chat/utils/createShortLinkForChat.ts 2.03ms
 416/2072 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 2.45ms
 417/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts 2.98ms
 418/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.ts 24.69ms
 419/2072 src/book-components/Chat/utils/downloadFile.ts 2.98ms
 420/2072 src/book-components/Chat/utils/exportChatHistory.ts 11.83ms
 421/2072 src/book-components/Chat/utils/ExportFormat.ts 1.80ms
 422/2072 src/book-components/Chat/utils/externalSourceToolCall.ts 4.37ms
 423/2072 src/book-components/Chat/utils/formatToolCallDateTime.ts 7.68ms
 424/2072 src/book-components/Chat/utils/formatToolCallLocalTime.ts 3.78ms
 425/2072 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 4.24ms
 426/2072 src/book-components/Chat/utils/generatePdfContent.ts 14.57ms
 427/2072 src/book-components/Chat/utils/generateQrDataUrl.ts 3.82ms
 428/2072 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 14.11ms
 429/2072 src/book-components/Chat/utils/getPromptbookBranding.ts 1.93ms
 430/2072 src/book-components/Chat/utils/getToolCallChipletInfo.test.ts 3.23ms
 431/2072 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 4.52ms
 432/2072 src/book-components/Chat/utils/getToolCallChipletInfo.ts 27.18ms
 433/2072 src/book-components/Chat/utils/isVisibleChatToolCall.ts 6.95ms
 434/2072 src/book-components/Chat/utils/loadAgentProfile.ts 13.85ms
 435/2072 src/book-components/Chat/utils/messagesToHtml.ts 18.44ms
 436/2072 src/book-components/Chat/utils/messagesToJson.ts 3.24ms
 437/2072 src/book-components/Chat/utils/messagesToMarkdown.ts 6.48ms
 438/2072 src/book-components/Chat/utils/messagesToText.ts 3.01ms
 439/2072 src/book-components/Chat/utils/parseCitationMarker.ts 8.53ms
 440/2072 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 3.16ms
 441/2072 src/book-components/Chat/utils/parseCitationsFromContent.ts 6.89ms
 442/2072 src/book-components/Chat/utils/parseImagePrompts.test.ts 3.42ms
 443/2072 src/book-components/Chat/utils/parseImagePrompts.ts 4.33ms
 444/2072 src/book-components/Chat/utils/parseMessageButtons.test.ts 3.78ms
 445/2072 src/book-components/Chat/utils/parseMessageButtons.ts 3.98ms
 446/2072 src/book-components/Chat/utils/renderMarkdown.test.ts 5.63ms
 447/2072 src/book-components/Chat/utils/renderMarkdown.ts 55.73ms
 448/2072 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 9.54ms
 449/2072 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 4.24ms
 450/2072 src/book-components/Chat/utils/resolveCitationSourceDisplay.ts 42.80ms
 451/2072 src/book-components/Chat/utils/resolveCitationUrl.test.ts 6.30ms
 452/2072 src/book-components/Chat/utils/resolveCitationUrl.ts 21.18ms
 453/2072 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 3.43ms
 454/2072 src/book-components/Chat/utils/resolveToolCallState.ts 5.54ms
 455/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 6.55ms
 456/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 6.57ms
 457/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 4.26ms
 458/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 42.58ms
 459/2072 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 4.26ms
 460/2072 src/book-components/Chat/utils/thinkingMessageVariants.ts 4.20ms
 461/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 5.13ms
 462/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 36.74ms
 463/2072 src/book-components/Chat/utils/toolCallParsing.test.ts 6.21ms
 464/2072 src/book-components/Chat/utils/toolCallParsing.ts 3.49ms
 465/2072 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 14.17ms
 466/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 2.51ms
 467/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 2.58ms
 468/2072 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 21.95ms
 469/2072 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 13.51ms
 470/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 3.03ms
 471/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 2.28ms
 472/2072 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 3.04ms
 473/2072 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 3.45ms
 474/2072 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 3.68ms
 475/2072 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 6.85ms
 476/2072 src/book-components/Chat/utils/walletCredentialToolCall.ts 15.80ms
 477/2072 src/book-components/fonts/README.md 1.37ms
 478/2072 src/book-components/icons/AboutIcon.tsx 4.27ms
 479/2072 src/book-components/icons/ArrowIcon.tsx 6.83ms
 480/2072 src/book-components/icons/AttachmentIcon.tsx 3.03ms
 481/2072 src/book-components/icons/CameraIcon.tsx 5.82ms
 482/2072 src/book-components/icons/CloseIcon.tsx 8.42ms
 483/2072 src/book-components/icons/DownloadIcon.tsx 3.09ms
 484/2072 src/book-components/icons/EmailIcon.tsx 3.12ms
 485/2072 src/book-components/icons/ExitFullscreenIcon.tsx 3.26ms
 486/2072 src/book-components/icons/FullscreenIcon.tsx 2.64ms
 487/2072 src/book-components/icons/MenuIcon.tsx 5.68ms
 488/2072 src/book-components/icons/MicIcon.tsx 2.27ms
 489/2072 src/book-components/icons/PauseIcon.tsx 2.47ms
 490/2072 src/book-components/icons/PlayIcon.tsx 3.05ms
 491/2072 src/book-components/icons/ResetIcon.tsx 2.06ms
 492/2072 src/book-components/icons/SaveIcon.tsx 3.37ms
 493/2072 src/book-components/icons/SendIcon.tsx 2.10ms
 494/2072 src/book-components/icons/SolidArrowButton.tsx 3.61ms
 495/2072 src/book-components/icons/StopIcon.tsx 3.58ms
 496/2072 src/book-components/icons/TeacherIcon.tsx 2.88ms
 497/2072 src/book-components/icons/TemplateIcon.tsx 2.42ms
 498/2072 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 5.38ms
 499/2072 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 26.83ms
 500/2072 src/book-components/Qr/BrandedQrCode.tsx 2.57ms
 501/2072 src/book-components/Qr/GenericQrCode.tsx 2.47ms
 502/2072 src/book-components/Qr/PromptbookQrCode.tsx 13.77ms
 503/2072 src/book-components/Qr/useQrCode.ts 3.63ms
 504/2072 src/cli/$runPromptbookCli.ts 3.10ms
 505/2072 src/cli/cli-commands/_boilerplate.ts 4.82ms
 506/2072 src/cli/cli-commands/about.ts 19.29ms
 507/2072 src/cli/cli-commands/agent-folder.ts 4.71ms
 508/2072 src/cli/cli-commands/agent-folder/agentProjectPaths.ts 5.40ms
 509/2072 src/cli/cli-commands/agent-folder/agentRunCliOptions.ts 4.20ms
 510/2072 src/cli/cli-commands/agent-folder/init.ts 3.78ms
 511/2072 src/cli/cli-commands/agent-folder/initializeAgentProjectConfiguration.ts 5.70ms
 512/2072 src/cli/cli-commands/agent-folder/initializeAgentRunnerCommand.ts 6.41ms
 513/2072 src/cli/cli-commands/agent-folder/printAgentInitializationSummary.ts 5.30ms
 514/2072 src/cli/cli-commands/agent-folder/run.test.ts 7.88ms
 515/2072 src/cli/cli-commands/agent-folder/run.ts 3.85ms
 516/2072 src/cli/cli-commands/agent-folder/runMultiple.ts 4.14ms
 517/2072 src/cli/cli-commands/agent-folder/tick.ts 3.48ms
 518/2072 src/cli/cli-commands/agent.ts 3.19ms
 519/2072 src/cli/cli-commands/agent/agentCliOptions.ts 5.30ms
 520/2072 src/cli/cli-commands/agent/chat.ts 6.51ms
 521/2072 src/cli/cli-commands/agent/exec.ts 5.03ms
 522/2072 src/cli/cli-commands/agent/run.test.ts 10.83ms
 523/2072 src/cli/cli-commands/agents-server.ts 4.38ms
 524/2072 src/cli/cli-commands/agents-server/buildAgentsServer.test.ts 13.39ms
 525/2072 src/cli/cli-commands/agents-server/buildAgentsServer.ts 5.19ms
 526/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_FILENAME.ts 2.17ms
 527/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_VERSION.ts 1.74ms
 528/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts 1.67ms
 529/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCache.ts 1.97ms
 530/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCacheOptions.ts 2.14ms
 531/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerBuildSourceFingerprint.ts 5.00ms
 532/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerRuntimeEnvironment.ts 6.53ms
 533/2072 src/cli/cli-commands/agents-server/buildAgentsServer/ensureAgentsServerBuild.ts 4.67ms
 534/2072 src/cli/cli-commands/agents-server/buildAgentsServer/getAgentsServerBuildInputRelativePaths.ts 2.43ms
 535/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPath.ts 2.47ms
 536/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPathMaterialized.ts 2.78ms
 537/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildCacheCurrent.ts 3.21ms
 538/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildInputTestFile.ts 4.29ms
 539/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputDirectoryName.ts 2.54ms
 540/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputPath.ts 3.14ms
 541/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerRuntimeSourcePath.ts 3.13ms
 542/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isFile.ts 1.76ms
 543/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isPathInsideNodeModules.ts 2.01ms
 544/2072 src/cli/cli-commands/agents-server/buildAgentsServer/NODE_MODULES_DIRECTORY_NAME.ts 1.69ms
 545/2072 src/cli/cli-commands/agents-server/buildAgentsServer/normalizeAgentsServerBuildInputPath.ts 15.41ms
 546/2072 src/cli/cli-commands/agents-server/buildAgentsServer/prepareAgentsServerRuntime.ts 3.51ms
 547/2072 src/cli/cli-commands/agents-server/buildAgentsServer/PreparedAgentsServerRuntime.ts 2.35ms
 548/2072 src/cli/cli-commands/agents-server/buildAgentsServer/readAgentsServerBuildCache.ts 2.87ms
 549/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerAppPath.ts 2.52ms
 550/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildAppPath.ts 4.02ms
 551/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildOutputPath.ts 2.73ms
 552/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNextCliPath.ts 2.38ms
 553/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNodeModulesPath.ts 2.55ms
 554/2072 src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts 7.69ms
 555/2072 src/cli/cli-commands/agents-server/buildAgentsServer/shouldCopyAgentsServerRuntimePath.ts 15.73ms
 556/2072 src/cli/cli-commands/agents-server/buildAgentsServer/synchronizeMaterializedAgentsServerRuntime.ts 7.98ms
 557/2072 src/cli/cli-commands/agents-server/buildAgentsServer/writeAgentsServerBuildCache.ts 3.26ms
 558/2072 src/cli/cli-commands/agents-server/ensureAgentsServerEnvFile.ts 5.54ms
 559/2072 src/cli/cli-commands/agents-server/ensureAgentsServerGitignoreFile.ts 3.28ms
 560/2072 src/cli/cli-commands/agents-server/init.test.ts 8.04ms
 561/2072 src/cli/cli-commands/agents-server/init.ts 3.33ms
 562/2072 src/cli/cli-commands/agents-server/initializeAgentsServerProjectConfiguration.ts 4.44ms
 563/2072 src/cli/cli-commands/agents-server/printAgentsServerInitializationSummary.ts 5.62ms
 564/2072 src/cli/cli-commands/agents-server/run.test.ts 8.53ms
 565/2072 src/cli/cli-commands/agents-server/run.ts 9.80ms
 566/2072 src/cli/cli-commands/agents-server/startAgentsServer.ts 11.75ms
 567/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts 9.24ms
 568/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.ts 5.20ms
 569/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerLogStreams.ts 3.45ms
 570/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerRuntimePaths.ts 3.56ms
 571/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerSupervisorState.ts 3.94ms
 572/2072 src/cli/cli-commands/agents-server/startAgentsServer/createInternalRouteErrorMessage.ts 2.13ms
 573/2072 src/cli/cli-commands/agents-server/startAgentsServer/createLocalAgentRunOptions.ts 4.46ms
 574/2072 src/cli/cli-commands/agents-server/startAgentsServer/forwardChildOutput.ts 2.79ms
 575/2072 src/cli/cli-commands/agents-server/startAgentsServer/loadAgentsServerProjectEnvironment.ts 2.32ms
 576/2072 src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts 2.09ms
 577/2072 src/cli/cli-commands/agents-server/startAgentsServer/prepareAgentsServerLaunch.ts 5.75ms
 578/2072 src/cli/cli-commands/agents-server/startAgentsServer/PreparedAgentsServerLaunch.ts 2.96ms
 579/2072 src/cli/cli-commands/agents-server/startAgentsServer/readInternalRouteErrorDetails.ts 6.34ms
 580/2072 src/cli/cli-commands/agents-server/startAgentsServer/resolveAgentsServerChildHostname.ts 2.20ms
 581/2072 src/cli/cli-commands/agents-server/startAgentsServer/StartAgentsServerOptions.ts 13.74ms
 582/2072 src/cli/cli-commands/agents-server/startAgentsServer/startNextServer.ts 8.97ms
 583/2072 src/cli/cli-commands/agents-server/startAgentsServer/startUserChatJobWorkerPump.ts 7.77ms
 584/2072 src/cli/cli-commands/agents-server/startAgentsServer/stopChildProcess.ts 1.99ms
 585/2072 src/cli/cli-commands/agents-server/startAgentsServer/waitForLocalAgentRunnerLimits.ts 6.99ms
 586/2072 src/cli/cli-commands/coder.ts 12.92ms
 587/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts 5.64ms
 588/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.ts 6.49ms
 589/2072 src/cli/cli-commands/coder/add.test.ts 8.79ms
 590/2072 src/cli/cli-commands/coder/add.ts 21.00ms
 591/2072 src/cli/cli-commands/coder/agentCliOptions.ts 3.14ms
 592/2072 src/cli/cli-commands/coder/agentsFile.ts 3.28ms
 593/2072 src/cli/cli-commands/coder/boilerplateCount.test.ts 3.67ms
 594/2072 src/cli/cli-commands/coder/boilerplateCount.ts 5.65ms
 595/2072 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 16.16ms
 596/2072 src/cli/cli-commands/coder/boilerplateTemplates.ts 14.85ms
 597/2072 src/cli/cli-commands/coder/coderReferencedArtifacts.ts 7.06ms
 598/2072 src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts 6.72ms
 599/2072 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 5.42ms
 600/2072 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 6.56ms
 601/2072 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 3.70ms
 602/2072 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 3.62ms
 603/2072 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 5.05ms
 604/2072 src/cli/cli-commands/coder/ensureDirectory.ts 5.58ms
 605/2072 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 6.43ms
 606/2072 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 8.12ms
 607/2072 src/cli/cli-commands/coder/find-refactor-candidates.ts 8.24ms
 608/2072 src/cli/cli-commands/coder/find-unwritten.ts 6.24ms
 609/2072 src/cli/cli-commands/coder/formatDisplayPath.ts 8.43ms
 610/2072 src/cli/cli-commands/coder/generate-boilerplates.test.ts 6.51ms
 611/2072 src/cli/cli-commands/coder/generate-boilerplates.ts 15.07ms
 612/2072 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 7.37ms
 613/2072 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 3.11ms
 614/2072 src/cli/cli-commands/coder/getTypescriptModule.test.ts 2.71ms
 615/2072 src/cli/cli-commands/coder/getTypescriptModule.ts 3.00ms
 616/2072 src/cli/cli-commands/coder/GitChangesMode.ts 2.96ms
 617/2072 src/cli/cli-commands/coder/init.test.ts 7.26ms
 618/2072 src/cli/cli-commands/coder/init.ts 10.68ms
 619/2072 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 8.30ms
 620/2072 src/cli/cli-commands/coder/list.test.ts 7.95ms
 621/2072 src/cli/cli-commands/coder/list.ts 6.33ms
 622/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 4.94ms
 623/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 11.24ms
 624/2072 src/cli/cli-commands/coder/ping.test.ts 10.60ms
 625/2072 src/cli/cli-commands/coder/ping.ts 7.54ms
 626/2072 src/cli/cli-commands/coder/printCoderRunFailure.ts 2.02ms
 627/2072 src/cli/cli-commands/coder/printInitializationSummary.ts 5.81ms
 628/2072 src/cli/cli-commands/coder/run.test.ts 10.79ms
 629/2072 src/cli/cli-commands/coder/run.ts 23.60ms
 630/2072 src/cli/cli-commands/coder/server.test.ts 6.77ms
 631/2072 src/cli/cli-commands/coder/server.ts 17.68ms
 632/2072 src/cli/cli-commands/coder/ThinkingLevel.ts 19.88ms
 633/2072 src/cli/cli-commands/coder/verify.test.ts 13.42ms
 634/2072 src/cli/cli-commands/coder/verify.ts 7.20ms
 635/2072 src/cli/cli-commands/coder/waitOptions.ts 4.80ms
 636/2072 src/cli/cli-commands/common/$askForConfirmation.test.ts 2.94ms
 637/2072 src/cli/cli-commands/common/$askForConfirmation.ts 16.04ms
 638/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts 24.19ms
 639/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.ts 10.81ms
 640/2072 src/cli/cli-commands/common/createNonNegativeIntegerOptionParser.ts 24.88ms
 641/2072 src/cli/cli-commands/common/createPositiveIntegerOptionParser.ts 2.85ms
 642/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts 5.82ms
 643/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.ts 11.70ms
 644/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts 8.62ms
 645/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.ts 3.82ms
 646/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts 2.15ms
 647/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.ts 3.04ms
 648/2072 src/cli/cli-commands/common/disk-space/formatLowFreeDiskSpaceWarning.ts 11.17ms
 649/2072 src/cli/cli-commands/common/disk-space/freeDiskSpaceConstants.ts 3.91ms
 650/2072 src/cli/cli-commands/common/disk-space/FreeDiskSpaceStatus.ts 3.39ms
 651/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts 2.62ms
 652/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.ts 5.70ms
 653/2072 src/cli/cli-commands/common/handleActionErrors.ts 7.30ms
 654/2072 src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts 9.69ms
 655/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts 6.59ms
 656/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.ts 5.89ms
 657/2072 src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts 5.88ms
 658/2072 src/cli/cli-commands/common/harness/$resolveHarnessCommandPath.ts 28.36ms
 659/2072 src/cli/cli-commands/common/harness/$resolveHarnessInstallationOrigin.ts 3.89ms
 660/2072 src/cli/cli-commands/common/harness/$resolveInstalledHarnessVersion.ts 3.36ms
 661/2072 src/cli/cli-commands/common/harness/$runHarnessInstallationCommand.ts 6.25ms
 662/2072 src/cli/cli-commands/common/harness/buildHarnessInstallCommand.ts 3.81ms
 663/2072 src/cli/cli-commands/common/harness/formatHarnessInstallationWarning.ts 19.21ms
 664/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts 9.73ms
 665/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.ts 7.53ms
 666/2072 src/cli/cli-commands/common/harness/HarnessDefinition.test.ts 10.01ms
 667/2072 src/cli/cli-commands/common/harness/HarnessDefinition.ts 18.79ms
 668/2072 src/cli/cli-commands/common/harness/HarnessInstallationOrigin.ts 3.65ms
 669/2072 src/cli/cli-commands/common/harness/HarnessInstallationStatus.ts 3.67ms
 670/2072 src/cli/cli-commands/common/harness/HarnessUpdatePlan.ts 2.76ms
 671/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts 6.63ms
 672/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.ts 12.45ms
 673/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts 3.54ms
 674/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.ts 4.70ms
 675/2072 src/cli/cli-commands/common/npm/$askForNpmPackageInstallationApproval.ts 5.98ms
 676/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts 3.81ms
 677/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.ts 4.49ms
 678/2072 src/cli/cli-commands/common/npm/$resolveNpmGlobalPrefixPath.ts 3.99ms
 679/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts 2.39ms
 680/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.ts 2.78ms
 681/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts 3.48ms
 682/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.ts 3.45ms
 683/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts 2.96ms
 684/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.ts 9.29ms
 685/2072 src/cli/cli-commands/common/projectInitialization.ts 12.89ms
 686/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts 7.86ms
 687/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.ts 9.10ms
 688/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts 8.10ms
 689/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.ts 7.84ms
 690/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts 7.90ms
 691/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.ts 14.74ms
 692/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts 5.84ms
 693/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.ts 16.09ms
 694/2072 src/cli/cli-commands/common/promptbook-cli/buildPromptbookCliInstallCommand.ts 3.62ms
 695/2072 src/cli/cli-commands/common/promptbook-cli/formatPromptbookCliInstallationWarning.ts 6.04ms
 696/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallation.ts 5.21ms
 697/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallationStatus.ts 8.76ms
 698/2072 src/cli/cli-commands/common/promptPriorityCliOptions.ts 5.42ms
 699/2072 src/cli/cli-commands/common/promptRunnerCliOptions.ts 14.24ms
 700/2072 src/cli/cli-commands/common/questionsCliOptions.test.ts 7.24ms
 701/2072 src/cli/cli-commands/common/questionsCliOptions.ts 8.81ms
 702/2072 src/cli/cli-commands/hello.ts 4.24ms
 703/2072 src/cli/cli-commands/list-models.ts 4.88ms
 704/2072 src/cli/cli-commands/list-scrapers.ts 4.50ms
 705/2072 src/cli/cli-commands/login.ts 6.23ms
 706/2072 src/cli/cli-commands/make.ts 24.94ms
 707/2072 src/cli/cli-commands/prettify.ts 13.10ms
 708/2072 src/cli/cli-commands/run.ts 4.81ms
 709/2072 src/cli/cli-commands/run/prepareRunCommandResources.ts 11.71ms
 710/2072 src/cli/cli-commands/run/resolveRunInputParameters.ts 5.92ms
 711/2072 src/cli/cli-commands/run/runCommandAction.ts 8.00ms
 712/2072 src/cli/cli-commands/run/runPipelineExecution.ts 7.21ms
 713/2072 src/cli/cli-commands/runInteractiveChatbot.ts 8.56ms
 714/2072 src/cli/cli-commands/start-agents-server.ts 7.91ms
 715/2072 src/cli/cli-commands/start-pipelines-server.ts 40.07ms
 716/2072 src/cli/cli-commands/test-command.ts 16.38ms
 717/2072 src/cli/common/$addGlobalOptionsToCommand.ts 2.50ms
 718/2072 src/cli/common/$deprecateCliCommand.test.ts 4.02ms
 719/2072 src/cli/common/$deprecateCliCommand.ts 4.18ms
 720/2072 src/cli/common/$hideCliCommandFromHelp.test.ts 4.09ms
 721/2072 src/cli/common/$hideCliCommandFromHelp.ts 3.38ms
 722/2072 src/cli/common/$provideLlmToolsForCli.ts 8.12ms
 723/2072 src/cli/common/$requireCliSubcommand.test.ts 4.09ms
 724/2072 src/cli/common/$requireCliSubcommand.ts 3.06ms
 725/2072 src/cli/common/loadPromptsModule.ts 2.37ms
 726/2072 src/cli/main.ts 1.90ms
 727/2072 src/cli/other/install.test.ts 3.68ms
 728/2072 src/cli/other/vpsInstall.test.ts 30.25ms
 729/2072 src/cli/promptbookCli.ts 14.71ms
 730/2072 src/cli/test/ptbk.test.ts 10.98ms
 731/2072 src/cli/test/ptbk.ts 1.65ms
 732/2072 src/cli/test/tsconfig.json 4.74ms
 733/2072 src/collection/agent-collection/AgentCollection.ts 6.68ms
 734/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 23.77ms
 735/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 2.20ms
 736/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 13.60ms
 737/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts 4.80ms
 738/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.ts 5.01ms
 739/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 4.88ms
 740/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 6.16ms
 741/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 6.32ms
 742/2072 src/collection/agent-collection/CreateAgentInput.test.ts 3.17ms
 743/2072 src/collection/agent-collection/CreateAgentInput.ts 10.67ms
 744/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 5.29ms
 745/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 22.77ms
 746/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 4.39ms
 747/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 2.95ms
 748/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 8.10ms
 749/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 11.05ms
 750/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 7.33ms
 751/2072 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 4.16ms
 752/2072 src/collection/pipeline-collection/PipelineCollection.ts 3.35ms
 753/2072 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 5.33ms
 754/2072 src/collection/pipeline-collection/pipelineCollectionToJson.ts 3.53ms
 755/2072 src/collection/pipeline-collection/SimplePipelineCollection.ts 8.71ms
 756/2072 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 3.54ms
 757/2072 src/commands/_BOILERPLATE/BoilerplateCommand.ts 1.89ms
 758/2072 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 6.60ms
 759/2072 src/commands/_common/getParserForCommand.test.ts 5.98ms
 760/2072 src/commands/_common/getParserForCommand.ts 2.78ms
 761/2072 src/commands/_common/parseCommand.ts 13.42ms
 762/2072 src/commands/_common/stringifyCommand.test.ts 3.46ms
 763/2072 src/commands/_common/stringifyCommand.ts 2.70ms
 764/2072 src/commands/_common/types/Command.ts 3.57ms
 765/2072 src/commands/_common/types/CommandParser.ts 30.56ms
 766/2072 src/commands/_common/types/CommandType.ts 8.09ms
 767/2072 src/commands/_common/types/CommandUsagePlaces.ts 12.37ms
 768/2072 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 6.53ms
 769/2072 src/commands/BOOK_VERSION/BookVersionCommand.ts 2.15ms
 770/2072 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 5.88ms
 771/2072 src/commands/EXPECT/expectCommand.test.ts 4.03ms
 772/2072 src/commands/EXPECT/ExpectCommand.ts 4.07ms
 773/2072 src/commands/EXPECT/expectCommandParser.ts 9.78ms
 774/2072 src/commands/FOREACH/foreachCommand.test.ts 5.56ms
 775/2072 src/commands/FOREACH/ForeachCommand.ts 2.00ms
 776/2072 src/commands/FOREACH/foreachCommandParser.ts 9.07ms
 777/2072 src/commands/FOREACH/ForeachJson.ts 3.03ms
 778/2072 src/commands/FORMAT/formatCommand.test.ts 2.85ms
 779/2072 src/commands/FORMAT/FormatCommand.ts 1.95ms
 780/2072 src/commands/FORMAT/formatCommandParser.ts 5.73ms
 781/2072 src/commands/FORMFACTOR/formfactorCommand.test.ts 5.23ms
 782/2072 src/commands/FORMFACTOR/FormfactorCommand.ts 3.99ms
 783/2072 src/commands/FORMFACTOR/formfactorCommandParser.ts 5.56ms
 784/2072 src/commands/index.ts 13.21ms
 785/2072 src/commands/JOKER/jokerCommand.test.ts 2.98ms
 786/2072 src/commands/JOKER/JokerCommand.ts 2.55ms
 787/2072 src/commands/JOKER/jokerCommandParser.ts 6.19ms
 788/2072 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 3.90ms
 789/2072 src/commands/KNOWLEDGE/KnowledgeCommand.ts 2.04ms
 790/2072 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 8.66ms
 791/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 2.57ms
 792/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 3.09ms
 793/2072 src/commands/MODEL/modelCommand.test.ts 3.01ms
 794/2072 src/commands/MODEL/ModelCommand.ts 2.34ms
 795/2072 src/commands/MODEL/modelCommandParser.ts 10.00ms
 796/2072 src/commands/PARAMETER/parameterCommand.test.ts 8.72ms
 797/2072 src/commands/PARAMETER/ParameterCommand.ts 2.39ms
 798/2072 src/commands/PARAMETER/parameterCommandParser.ts 7.59ms
 799/2072 src/commands/PERSONA/personaCommand.test.ts 4.01ms
 800/2072 src/commands/PERSONA/PersonaCommand.ts 2.07ms
 801/2072 src/commands/PERSONA/personaCommandParser.ts 7.64ms
 802/2072 src/commands/POSTPROCESS/postprocessCommand.test.ts 6.16ms
 803/2072 src/commands/POSTPROCESS/PostprocessCommand.ts 2.28ms
 804/2072 src/commands/POSTPROCESS/postprocessCommandParser.ts 5.70ms
 805/2072 src/commands/README.md 1.88ms
 806/2072 src/commands/SECTION/sectionCommand.test.ts 3.49ms
 807/2072 src/commands/SECTION/SectionCommand.ts 1.88ms
 808/2072 src/commands/SECTION/sectionCommandParser.ts 11.02ms
 809/2072 src/commands/URL/urlCommand.test.ts 4.06ms
 810/2072 src/commands/URL/UrlCommand.ts 1.98ms
 811/2072 src/commands/URL/urlCommandParser.ts 6.29ms
 812/2072 src/commands/X_ACTION/actionCommand.test.ts 2.14ms
 813/2072 src/commands/X_ACTION/ActionCommand.ts 1.99ms
 814/2072 src/commands/X_ACTION/actionCommandParser.ts 4.66ms
 815/2072 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 2.37ms
 816/2072 src/commands/X_INSTRUMENT/InstrumentCommand.ts 2.66ms
 817/2072 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 5.65ms
 818/2072 src/commitments/_base/BaseCommitmentDefinition.ts 15.82ms
 819/2072 src/commitments/_base/BookCommitment.ts 1.54ms
 820/2072 src/commitments/_base/CommitmentDefinition.ts 6.25ms
 821/2072 src/commitments/_base/createEmptyAgentModelRequirements.ts 11.41ms
 822/2072 src/commitments/_base/formatOptionalInstructionBlock.ts 2.01ms
 823/2072 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 10.33ms
 824/2072 src/commitments/_base/ParsedCommitment.ts 2.71ms
 825/2072 src/commitments/_common/commitmentToolFunctions.ts 7.67ms
 826/2072 src/commitments/_common/createWritingCommitmentSections.ts 3.41ms
 827/2072 src/commitments/_common/getAllCommitmentDefinitions.test.ts 4.57ms
 828/2072 src/commitments/_common/getAllCommitmentDefinitions.ts 2.34ms
 829/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 3.47ms
 830/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 3.98ms
 831/2072 src/commitments/_common/getAllCommitmentsToolTitles.ts 3.21ms
 832/2072 src/commitments/_common/getAllCommitmentTypes.ts 4.78ms
 833/2072 src/commitments/_common/getCommitmentDefinition.ts 2.39ms
 834/2072 src/commitments/_common/getCommitmentNoticeMetadata.test.ts 33.79ms
 835/2072 src/commitments/_common/getCommitmentNoticeMetadata.ts 6.67ms
 836/2072 src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts 2.53ms
 837/2072 src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts 3.21ms
 838/2072 src/commitments/_common/getGroupedCommitmentDefinitions.ts 4.65ms
 839/2072 src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts 3.09ms
 840/2072 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 12.70ms
 841/2072 src/commitments/_common/isCommitmentSupported.ts 2.50ms
 842/2072 src/commitments/_common/sortCommitmentDefinitions.ts 5.26ms
 843/2072 src/commitments/_common/teamInternalAgentAccess.ts 5.99ms
 844/2072 src/commitments/_common/toolExecutionEnvelope.ts 5.08ms
 845/2072 src/commitments/_common/toolRuntimeContext.ts 11.76ms
 846/2072 src/commitments/ACTION/ACTION.test.ts 8.41ms
 847/2072 src/commitments/ACTION/ACTION.ts 9.22ms
 848/2072 src/commitments/CLOSED/CLOSED.test.ts 3.99ms
 849/2072 src/commitments/CLOSED/CLOSED.ts 3.91ms
 850/2072 src/commitments/DELETE/DELETE.test.ts 7.67ms
 851/2072 src/commitments/DELETE/DELETE.ts 11.50ms
 852/2072 src/commitments/DICTIONARY/DICTIONARY.ts 5.64ms
 853/2072 src/commitments/FORMAT/FORMAT.test.ts 3.60ms
 854/2072 src/commitments/FORMAT/FORMAT.ts 13.00ms
 855/2072 src/commitments/FROM/FROM.ts 9.10ms
 856/2072 src/commitments/GOAL/GOAL.ts 5.59ms
 857/2072 src/commitments/IMPORT/IMPORT.ts 6.95ms
 858/2072 src/commitments/index.test.ts 5.03ms
 859/2072 src/commitments/index.ts 16.89ms
 860/2072 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 7.76ms
 861/2072 src/commitments/LANGUAGE/LANGUAGE.ts 3.80ms
 862/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 3.04ms
 863/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 3.91ms
 864/2072 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 4.63ms
 865/2072 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 5.49ms
 866/2072 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 4.04ms
 867/2072 src/commitments/MESSAGE/MESSAGE.ts 8.95ms
 868/2072 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 8.90ms
 869/2072 src/commitments/META_AVATAR/META_AVATAR.ts 5.07ms
 870/2072 src/commitments/META_COLOR/META_COLOR.ts 4.98ms
 871/2072 src/commitments/META_DESCRIPTION/META_DESCRIPTION.ts 4.12ms
 872/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 4.56ms
 873/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 4.09ms
 874/2072 src/commitments/META_DOMAIN/META_DOMAIN.ts 4.45ms
 875/2072 src/commitments/META_FONT/META_FONT.ts 4.64ms
 876/2072 src/commitments/META_FULLNAME/META_FULLNAME.ts 4.01ms
 877/2072 src/commitments/META_ID/META_ID.ts 4.18ms
 878/2072 src/commitments/META_IMAGE/META_IMAGE.ts 4.63ms
 879/2072 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 4.00ms
 880/2072 src/commitments/META_LINK/META_LINK.ts 6.88ms
 881/2072 src/commitments/META_THINKING_MESSAGE/META_THINKING_MESSAGE.ts 3.68ms
 882/2072 src/commitments/META_VISIBILITY/META_VISIBILITY.ts 4.01ms
 883/2072 src/commitments/META_VOICE/META_VOICE.ts 3.89ms
 884/2072 src/commitments/MODEL/MODEL.test.ts 3.96ms
 885/2072 src/commitments/MODEL/MODEL.ts 7.86ms
 886/2072 src/commitments/NOTE/NOTE.ts 4.81ms
 887/2072 src/commitments/OPEN/OPEN.ts 5.70ms
 888/2072 src/commitments/PERSONA/persona-verification-example.md 4.88ms
 889/2072 src/commitments/PERSONA/PERSONA.ts 8.35ms
 890/2072 src/commitments/README.md 2.32ms
 891/2072 src/commitments/RULE/RULE.ts 7.67ms
 892/2072 src/commitments/SAMPLE/SAMPLE.ts 4.87ms
 893/2072 src/commitments/SCENARIO/SCENARIO.ts 12.35ms
 894/2072 src/commitments/STYLE/STYLE.ts 6.72ms
 895/2072 src/commitments/TEAM/TEAM.ts 21.04ms
 896/2072 src/commitments/TEMPLATE/README.md 12.46ms
 897/2072 src/commitments/TEMPLATE/TEMPLATE.test.ts 5.60ms
 898/2072 src/commitments/TEMPLATE/TEMPLATE.ts 9.51ms
 899/2072 src/commitments/USE_CALENDAR/calendarReference.ts 9.31ms
 900/2072 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 6.10ms
 901/2072 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 15.46ms
 902/2072 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 6.99ms
 903/2072 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 2.88ms
 904/2072 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 3.52ms
 905/2072 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 8.87ms
 906/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 7.31ms
 907/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 14.32ms
 908/2072 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.80ms
 909/2072 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 1.79ms
 910/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 2.87ms
 911/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 5.15ms
 912/2072 src/commitments/USE_MCP/USE_MCP.ts 4.31ms
 913/2072 src/commitments/USE_POPUP/USE_POPUP.ts 7.83ms
 914/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 5.31ms
 915/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 10.21ms
 916/2072 src/commitments/USE_PROJECT/callGitHubApi.ts 7.05ms
 917/2072 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 17.46ms
 918/2072 src/commitments/USE_PROJECT/createUseProjectTools.ts 5.83ms
 919/2072 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 2.95ms
 920/2072 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 2.65ms
 921/2072 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 2.36ms
 922/2072 src/commitments/USE_PROJECT/projectReference.test.ts 6.31ms
 923/2072 src/commitments/USE_PROJECT/projectReference.ts 8.70ms
 924/2072 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 8.83ms
 925/2072 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 11.13ms
 926/2072 src/commitments/USE_PROJECT/USE_PROJECT.ts 12.16ms
 927/2072 src/commitments/USE_PROJECT/UseProjectToolNames.ts 2.88ms
 928/2072 src/commitments/USE_PROJECT/UseProjectWallet.ts 2.26ms
 929/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 5.16ms
 930/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 15.87ms
 931/2072 src/commitments/WRITING_RULES/WRITING_RULES.ts 6.56ms
 932/2072 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 4.73ms
 933/2072 src/config.test.ts 1.68ms
 934/2072 src/config.ts 13.93ms
 935/2072 src/constants.ts 10.20ms
 936/2072 src/constants/streaming.ts 2.14ms
 937/2072 src/conversion/archive/loadArchive.ts 4.26ms
 938/2072 src/conversion/archive/loadJsZipModule.ts 2.16ms
 939/2072 src/conversion/archive/saveArchive.ts 4.39ms
 940/2072 src/conversion/compilePipeline.test.ts 3.56ms
 941/2072 src/conversion/compilePipeline.ts 3.92ms
 942/2072 src/conversion/compilePipelineOnRemoteServer.ts 3.36ms
 943/2072 src/conversion/parsePipeline.test.ts 4.12ms
 944/2072 src/conversion/parsePipeline.ts 4.84ms
 945/2072 src/conversion/parsePipeline/applyPipelineHead.ts 5.24ms
 946/2072 src/conversion/parsePipeline/createInitialPipelineJson.ts 2.97ms
 947/2072 src/conversion/parsePipeline/createUniqueSectionNameResolver.ts 3.66ms
 948/2072 src/conversion/parsePipeline/defineParameter.ts 7.41ms
 949/2072 src/conversion/parsePipeline/extractPipelineDescription.ts 2.66ms
 950/2072 src/conversion/parsePipeline/finalizeParsedPipeline.ts 8.48ms
 951/2072 src/conversion/parsePipeline/getPipelineIdentification.ts 2.67ms
 952/2072 src/conversion/parsePipeline/parsePreparedPipelineSections.ts 4.76ms
 953/2072 src/conversion/parsePipeline/preparePipelineString.ts 5.58ms
 954/2072 src/conversion/parsePipeline/processPipelineSection.ts 13.57ms
 955/2072 src/conversion/pipelineJsonToString.test.ts 6.97ms
 956/2072 src/conversion/pipelineJsonToString.ts 4.02ms
 957/2072 src/conversion/pipelineJsonToString/appendMarkdownBlock.ts 2.33ms
 958/2072 src/conversion/pipelineJsonToString/createPipelineCommands.ts 3.68ms
 959/2072 src/conversion/pipelineJsonToString/createPipelineIntroduction.ts 2.73ms
 960/2072 src/conversion/pipelineJsonToString/createTaskSerialization.ts 7.06ms
 961/2072 src/conversion/pipelineJsonToString/stringifyCommands.ts 2.32ms
 962/2072 src/conversion/pipelineJsonToString/stringifyTask.ts 2.99ms
 963/2072 src/conversion/prettify/PrettifyOptions.ts 2.40ms
 964/2072 src/conversion/prettify/prettifyPipelineString.ts 5.98ms
 965/2072 src/conversion/prettify/renderPipelineMermaidOptions.ts 7.51ms
 966/2072 src/conversion/utils/extractParameterNamesFromTask.test.ts 3.66ms
 967/2072 src/conversion/utils/extractParameterNamesFromTask.ts 5.66ms
 968/2072 src/conversion/validation/_importPipeline.test.ts 6.43ms
 969/2072 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 3.40ms
 970/2072 src/conversion/validation/validatePipeline-logicErrors.test.ts 3.31ms
 971/2072 src/conversion/validation/validatePipeline.test.ts 14.14ms
 972/2072 src/conversion/validation/validatePipeline.ts 6.92ms
 973/2072 src/conversion/validation/validatePipeline/createPipelineValidationContext.ts 3.12ms
 974/2072 src/conversion/validation/validatePipeline/validatePipelineCollectionsStructure.ts 4.39ms
 975/2072 src/conversion/validation/validatePipeline/validatePipelineDependencyResolution.ts 11.87ms
 976/2072 src/conversion/validation/validatePipeline/validatePipelineMetadata.ts 13.17ms
 977/2072 src/conversion/validation/validatePipeline/validatePipelineParameters.ts 7.21ms
 978/2072 src/conversion/validation/validatePipeline/validatePipelineTasks.ts 7.97ms
 979/2072 src/dialogs/callback/CallbackInterfaceTools.ts 23.54ms
 980/2072 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 9.59ms
 981/2072 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 3.54ms
 982/2072 src/dialogs/user-interface-execution-tools.test.ts 7.88ms
 983/2072 src/errors/0-BoilerplateError.ts 2.54ms
 984/2072 src/errors/0-index.ts 5.22ms
 985/2072 src/errors/AbstractFormatError.ts 1.90ms
 986/2072 src/errors/assertsError.ts 2.99ms
 987/2072 src/errors/AuthenticationError.ts 3.64ms
 988/2072 src/errors/CollectionError.ts 1.89ms
 989/2072 src/errors/ConflictError.ts 1.76ms
 990/2072 src/errors/DatabaseError.ts 1.78ms
 991/2072 src/errors/EnvironmentMismatchError.ts 20.16ms
 992/2072 src/errors/ExpectError.ts 2.48ms
 993/2072 src/errors/KnowledgeScrapeError.ts 3.52ms
 994/2072 src/errors/LimitReachedError.ts 1.86ms
 995/2072 src/errors/MissingToolsError.ts 2.22ms
 996/2072 src/errors/NotAllowed.ts 8.26ms
 997/2072 src/errors/NotFoundError.ts 1.70ms
 998/2072 src/errors/NotYetImplementedError.ts 11.75ms
 999/2072 src/errors/ParseError.ts 1.90ms
1000/2072 src/errors/PipelineExecutionError.ts 3.03ms
1001/2072 src/errors/PipelineLogicError.ts 2.05ms
1002/2072 src/errors/PipelineUrlError.ts 1.74ms
1003/2072 src/errors/PromptbookFetchError.ts 1.97ms
1004/2072 src/errors/UnexpectedError.ts 14.22ms
1005/2072 src/errors/utils/deserializeError.test.ts 6.40ms
1006/2072 src/errors/utils/deserializeError.ts 3.04ms
1007/2072 src/errors/utils/ErrorJson.ts 9.17ms
1008/2072 src/errors/utils/getErrorReportUrl.ts 5.56ms
1009/2072 src/errors/utils/serializeError.test.ts 2.21ms
1010/2072 src/errors/utils/serializeError.ts 2.32ms
1011/2072 src/errors/WrappedError.ts 3.80ms
1012/2072 src/executables/$provideExecutablesForNode.ts 9.97ms
1013/2072 src/executables/apps/locateLibreoffice.test.ts 4.13ms
1014/2072 src/executables/apps/locateLibreoffice.ts 3.22ms
1015/2072 src/executables/apps/locatePandoc.test.ts 2.54ms
1016/2072 src/executables/apps/locatePandoc.ts 2.65ms
1017/2072 src/executables/apps/locateVscode.test.ts 2.21ms
1018/2072 src/executables/apps/locateVscode.ts 2.13ms
1019/2072 src/executables/browsers/locateBrowser.test.ts 3.15ms
1020/2072 src/executables/browsers/locateBrowser.ts 3.55ms
1021/2072 src/executables/browsers/locateChrome.test.ts 15.61ms
1022/2072 src/executables/browsers/locateChrome.ts 2.68ms
1023/2072 src/executables/browsers/locateDefaultSystemBrowser.test.ts 1.99ms
1024/2072 src/executables/browsers/locateDefaultSystemBrowser.ts 2.11ms
1025/2072 src/executables/browsers/locateEdge.test.ts 1.95ms
1026/2072 src/executables/browsers/locateEdge.ts 2.42ms
1027/2072 src/executables/browsers/locateFirefox.test.ts 2.03ms
1028/2072 src/executables/browsers/locateFirefox.ts 2.12ms
1029/2072 src/executables/browsers/locateInternetExplorer.ts 1.89ms
1030/2072 src/executables/browsers/locateSafari.ts 1.80ms
1031/2072 src/executables/locateApp.test.ts 3.42ms
1032/2072 src/executables/locateApp.ts 3.76ms
1033/2072 src/executables/platforms/locateAppOnLinux.ts 7.83ms
1034/2072 src/executables/platforms/locateAppOnMacOs.ts 4.21ms
1035/2072 src/executables/platforms/locateAppOnWindows.test.ts 3.60ms
1036/2072 src/executables/platforms/locateAppOnWindows.ts 4.10ms
1037/2072 src/executables/README.md 1.49ms
1038/2072 src/execution/AbstractTaskResult.ts 2.98ms
1039/2072 src/execution/assertsTaskSuccessful.ts 5.27ms
1040/2072 src/execution/AvailableModel.ts 3.56ms
1041/2072 src/execution/CommonToolsOptions.ts 2.83ms
1042/2072 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 4.12ms
1043/2072 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 23.54ms
1044/2072 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 3.82ms
1045/2072 src/execution/createPipelineExecutor/10-executePipeline.ts 18.98ms
1046/2072 src/execution/createPipelineExecutor/20-executeTask.ts 11.34ms
1047/2072 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 19.29ms
1048/2072 src/execution/createPipelineExecutor/40-executeAttempts.ts 10.87ms
1049/2072 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 3.13ms
1050/2072 src/execution/createPipelineExecutor/executeSingleAttempt.ts 20.61ms
1051/2072 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 5.73ms
1052/2072 src/execution/createPipelineExecutor/getContextForTask.ts 5.95ms
1053/2072 src/execution/createPipelineExecutor/getExamplesForTask.ts 3.26ms
1054/2072 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 6.81ms
1055/2072 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 9.62ms
1056/2072 src/execution/createPipelineExecutor/handleAttemptFailure.ts 5.72ms
1057/2072 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 2.81ms
1058/2072 src/execution/createPipelineExecutor/reportPromptExecution.ts 5.40ms
1059/2072 src/execution/EmbeddingVector.ts 1.85ms
1060/2072 src/execution/embeddingVectorToString.ts 2.01ms
1061/2072 src/execution/Executables.ts 2.23ms
1062/2072 src/execution/execution-report/countWorkingDuration.test.ts 2.72ms
1063/2072 src/execution/execution-report/countWorkingDuration.ts 2.27ms
1064/2072 src/execution/execution-report/ExecutionPromptReportJson.ts 2.36ms
1065/2072 src/execution/execution-report/ExecutionReportJson.ts 3.85ms
1066/2072 src/execution/execution-report/executionReportJsonToString.ts 11.39ms
1067/2072 src/execution/execution-report/ExecutionReportString.ts 2.21ms
1068/2072 src/execution/execution-report/ExecutionReportStringOptions.ts 6.93ms
1069/2072 src/execution/ExecutionTask.ts 16.18ms
1070/2072 src/execution/ExecutionTools.ts 4.55ms
1071/2072 src/execution/FilesystemTools.ts 1.79ms
1072/2072 src/execution/LlmExecutionTools.ts 8.95ms
1073/2072 src/execution/LlmExecutionToolsConstructor.ts 1.96ms
1074/2072 src/execution/PipelineExecutor.ts 2.36ms
1075/2072 src/execution/PipelineExecutorResult.ts 3.49ms
1076/2072 src/execution/PromptbookFetch.test-type.ts 2.12ms
1077/2072 src/execution/PromptbookFetch.ts 2.95ms
1078/2072 src/execution/PromptResult.ts 5.34ms
1079/2072 src/execution/resolveTaskTldr.test.ts 3.08ms
1080/2072 src/execution/resolveTaskTldr.ts 9.27ms
1081/2072 src/execution/ScriptExecutionTools.ts 3.99ms
1082/2072 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 1.78ms
1083/2072 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 6.34ms
1084/2072 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 8.02ms
1085/2072 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 3.71ms
1086/2072 src/execution/translation/automatic-translate/automatic-translators/README.md 1.41ms
1087/2072 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 2.88ms
1088/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 1.90ms
1089/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 2.24ms
1090/2072 src/execution/translation/automatic-translate/translateMessages.ts 4.11ms
1091/2072 src/execution/UncertainNumber.ts 2.00ms
1092/2072 src/execution/Usage.ts 2.74ms
1093/2072 src/execution/UserInterfaceTools.ts 3.38ms
1094/2072 src/execution/utils/$provideExecutionToolsForNode.ts 4.69ms
1095/2072 src/execution/utils/addUsage.test.ts 3.70ms
1096/2072 src/execution/utils/addUsage.ts 3.67ms
1097/2072 src/execution/utils/checkExpectations.test.ts 2.44ms
1098/2072 src/execution/utils/checkExpectations.ts 6.31ms
1099/2072 src/execution/utils/computeUsageCounts.ts 3.71ms
1100/2072 src/execution/utils/forEachAsync.ts 3.13ms
1101/2072 src/execution/utils/formatUsagePrice.test.ts 2.06ms
1102/2072 src/execution/utils/formatUsagePrice.ts 2.24ms
1103/2072 src/execution/utils/uncertainNumber.ts 2.39ms
1104/2072 src/execution/utils/usage-constants.ts 2.82ms
1105/2072 src/execution/utils/usageToHuman.ts 4.56ms
1106/2072 src/execution/utils/usageToWorktime.test.ts 7.82ms
1107/2072 src/execution/utils/usageToWorktime.ts 3.18ms
1108/2072 src/execution/utils/validatePromptResult.ts 6.59ms
1109/2072 src/expectations/README.md 1.42ms
1110/2072 src/file-security-checkers/FileSecurityChecker.ts 2.70ms
1111/2072 src/file-security-checkers/FileSecurityCheckResult.ts 2.32ms
1112/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 4.03ms
1113/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 6.28ms
1114/2072 src/formats/_common/FormatParser.ts 5.02ms
1115/2072 src/formats/_common/FormatSubvalueParser.ts 7.09ms
1116/2072 src/formats/csv/CsvFormatError.ts 2.19ms
1117/2072 src/formats/csv/CsvFormatParser.ts 6.17ms
1118/2072 src/formats/csv/CsvSettings.ts 3.86ms
1119/2072 src/formats/csv/utils/csvParse.ts 4.05ms
1120/2072 src/formats/csv/utils/isValidCsvString.test.ts 2.57ms
1121/2072 src/formats/csv/utils/isValidCsvString.ts 2.23ms
1122/2072 src/formats/index.ts 2.30ms
1123/2072 src/formats/json/JsonFormatParser.ts 3.31ms
1124/2072 src/formats/json/utils/isValidJsonString.test.ts 2.22ms
1125/2072 src/formats/json/utils/isValidJsonString.ts 2.54ms
1126/2072 src/formats/json/utils/jsonParse.ts 2.47ms
1127/2072 src/formats/README.md 1.67ms
1128/2072 src/formats/text/TextFormatParser.ts 4.25ms
1129/2072 src/formats/xml/utils/isValidXmlString.test.ts 2.20ms
1130/2072 src/formats/xml/utils/isValidXmlString.ts 2.37ms
1131/2072 src/formats/xml/XmlFormatParser.ts 3.04ms
1132/2072 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 2.73ms
1133/2072 src/formfactors/_common/AbstractFormfactorDefinition.ts 4.30ms
1134/2072 src/formfactors/_common/FormfactorDefinition.ts 2.10ms
1135/2072 src/formfactors/_common/string_formfactor_name.ts 1.58ms
1136/2072 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 3.72ms
1137/2072 src/formfactors/completion/CompletionFormfactorDefinition.ts 3.23ms
1138/2072 src/formfactors/generator/GeneratorFormfactorDefinition.ts 5.66ms
1139/2072 src/formfactors/generic/GenericFormfactorDefinition.ts 2.27ms
1140/2072 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 2.88ms
1141/2072 src/formfactors/index.ts 3.10ms
1142/2072 src/formfactors/matcher/MatcherFormfactorDefinition.ts 3.30ms
1143/2072 src/formfactors/README.md 1.38ms
1144/2072 src/formfactors/sheets/SheetsFormfactorDefinition.ts 3.15ms
1145/2072 src/formfactors/translator/TranslatorFormfactorDefinition.ts 5.29ms
1146/2072 src/globals.d.ts 1.72ms
1147/2072 src/high-level-abstractions/_common/HighLevelAbstraction.ts 3.50ms
1148/2072 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 4.50ms
1149/2072 src/high-level-abstractions/index.ts 3.00ms
1150/2072 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 7.33ms
1151/2072 src/high-level-abstractions/README.md 1.62ms
1152/2072 src/import-plugins/$fileImportPlugins.ts 2.33ms
1153/2072 src/import-plugins/AgentFileImportPlugin.ts 3.99ms
1154/2072 src/import-plugins/FileImportPlugin.ts 2.12ms
1155/2072 src/import-plugins/JsonFileImportPlugin.ts 3.05ms
1156/2072 src/import-plugins/TextFileImportPlugin.ts 2.82ms
1157/2072 src/llm-providers/_common/filterModels.ts 5.60ms
1158/2072 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 2.34ms
1159/2072 src/llm-providers/_common/register/$llmToolsRegister.ts 2.25ms
1160/2072 src/llm-providers/_common/register/$provideEnvFilename.ts 7.19ms
1161/2072 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 7.82ms
1162/2072 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 5.95ms
1163/2072 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 9.14ms
1164/2072 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 5.81ms
1165/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 4.40ms
1166/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolConfigurationStatusMessage.ts 3.64ms
1167/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolEntryKey.ts 2.03ms
1168/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolInstallationStatusMessage.ts 3.47ms
1169/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolsMessageContext.ts 5.53ms
1170/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolStatus.ts 4.80ms
1171/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getAvailableRegisteredLlmToolsEnvironmentVariables.ts 2.49ms
1172/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getRegisteredLlmToolRegisters.ts 2.45ms
1173/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/hasRegisteredLlmToolEntry.ts 2.49ms
1174/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolEntries.ts 3.30ms
1175/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolStatuses.ts 2.78ms
1176/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/RegisteredLlmToolsMessageContext.ts 3.98ms
1177/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderAvailableRegisteredLlmTools.ts 5.35ms
1178/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderNoRegisteredLlmToolsMessage.ts 2.35ms
1179/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRegisteredLlmToolsMessage.ts 3.07ms
1180/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRelevantRegisteredLlmToolsEnvironmentVariables.ts 3.50ms
1181/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/USED_ENV_FILENAME.ts 2.68ms
1182/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 2.93ms
1183/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 7.21ms
1184/2072 src/llm-providers/_common/register/LlmToolsConfiguration.ts 3.43ms
1185/2072 src/llm-providers/_common/register/LlmToolsMetadata.ts 4.73ms
1186/2072 src/llm-providers/_common/register/LlmToolsOptions.ts 2.66ms
1187/2072 src/llm-providers/_common/utils/assertUniqueModels.ts 2.60ms
1188/2072 src/llm-providers/_common/utils/cache/CacheItem.ts 2.54ms
1189/2072 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 12.90ms
1190/2072 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 3.27ms
1191/2072 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 7.46ms
1192/2072 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 6.13ms
1193/2072 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 4.05ms
1194/2072 src/llm-providers/_common/utils/pricing.test.ts 1.80ms
1195/2072 src/llm-providers/_common/utils/pricing.ts 2.14ms
1196/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 5.90ms
1197/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 3.96ms
1198/2072 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 11.31ms
1199/2072 src/llm-providers/_multiple/joinLlmExecutionTools.ts 5.05ms
1200/2072 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 17.82ms
1201/2072 src/llm-providers/_multiple/playground/playground.ts 7.17ms
1202/2072 src/llm-providers/_multiple/playground/tsconfig.json 17.40ms
1203/2072 src/llm-providers/agent/Agent.test.ts 6.60ms
1204/2072 src/llm-providers/agent/Agent.ts 17.75ms
1205/2072 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 41.10ms
1206/2072 src/llm-providers/agent/AgentLlmExecutionTools.ts 16.66ms
1207/2072 src/llm-providers/agent/AgentLlmExecutionToolsAgentKitRunner.ts 17.62ms
1208/2072 src/llm-providers/agent/AgentLlmExecutionToolsOpenAiAssistantRunner.ts 11.40ms
1209/2072 src/llm-providers/agent/AgentLlmExecutionToolsPromptPreparer.ts 22.90ms
1210/2072 src/llm-providers/agent/AgentOptions.ts 3.89ms
1211/2072 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.49ms
1212/2072 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 4.05ms
1213/2072 src/llm-providers/agent/emitAgentLlmExecutionToolsAssistantPreparationProgress.ts 3.96ms
1214/2072 src/llm-providers/agent/playground/playground.ts 9.38ms
1215/2072 src/llm-providers/agent/playground/tsconfig.json 2.66ms
1216/2072 src/llm-providers/agent/register-configuration.ts 4.03ms
1217/2072 src/llm-providers/agent/register-constructor.ts 2.49ms
1218/2072 src/llm-providers/agent/RemoteAgent.test.ts 11.93ms
1219/2072 src/llm-providers/agent/RemoteAgent.ts 90.12ms
1220/2072 src/llm-providers/agent/RemoteAgentOptions.ts 2.65ms
1221/2072 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 13.34ms
1222/2072 src/llm-providers/agent/self-learning/SelfLearningManager.ts 24.94ms
1223/2072 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 27.34ms
1224/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 15.41ms
1225/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 15.31ms
1226/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 4.35ms
1227/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 24.27ms
1228/2072 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 12.07ms
1229/2072 src/llm-providers/anthropic-claude/playground/playground.ts 6.05ms
1230/2072 src/llm-providers/anthropic-claude/playground/tsconfig.json 3.79ms
1231/2072 src/llm-providers/anthropic-claude/register-configuration.ts 4.53ms
1232/2072 src/llm-providers/anthropic-claude/register-constructor.ts 5.06ms
1233/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 16.06ms
1234/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 3.38ms
1235/2072 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.66ms
1236/2072 src/llm-providers/azure-openai/playground/playground.ts 5.34ms
1237/2072 src/llm-providers/azure-openai/playground/tsconfig.json 3.20ms
1238/2072 src/llm-providers/azure-openai/register-configuration.ts 5.80ms
1239/2072 src/llm-providers/azure-openai/register-constructor.ts 3.05ms
1240/2072 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 4.90ms
1241/2072 src/llm-providers/deepseek/deepseek-models.ts 13.05ms
1242/2072 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 2.29ms
1243/2072 src/llm-providers/deepseek/other/compatibility-issue.md 2.39ms
1244/2072 src/llm-providers/deepseek/register-configuration.ts 5.43ms
1245/2072 src/llm-providers/deepseek/register-constructor.ts 2.92ms
1246/2072 src/llm-providers/google/createGoogleExecutionTools.ts 21.86ms
1247/2072 src/llm-providers/google/google-models.ts 17.79ms
1248/2072 src/llm-providers/google/GoogleExecutionToolsOptions.ts 2.39ms
1249/2072 src/llm-providers/google/other/compatibility-issue.md 2.27ms
1250/2072 src/llm-providers/google/register-configuration.ts 4.50ms
1251/2072 src/llm-providers/google/register-constructor.ts 2.83ms
1252/2072 src/llm-providers/mocked/$fakeTextToExpectations.ts 8.97ms
1253/2072 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 7.70ms
1254/2072 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 10.69ms
1255/2072 src/llm-providers/mocked/test/faked-completion.test.ts 8.09ms
1256/2072 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 2.95ms
1257/2072 src/llm-providers/mocked/test/joker.test.ts 4.65ms
1258/2072 src/llm-providers/mocked/test/mocked-chat.test.ts 6.83ms
1259/2072 src/llm-providers/mocked/test/mocked-completion.test.ts 8.91ms
1260/2072 src/llm-providers/ollama/createOllamaExecutionTools.ts 3.18ms
1261/2072 src/llm-providers/ollama/ollama-models.ts 13.82ms
1262/2072 src/llm-providers/ollama/OllamaExecutionTools.ts 10.39ms
1263/2072 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 2.76ms
1264/2072 src/llm-providers/ollama/playground/playground.ts 6.53ms
1265/2072 src/llm-providers/ollama/playground/tsconfig.json 2.78ms
1266/2072 src/llm-providers/ollama/register-configuration.ts 4.28ms
1267/2072 src/llm-providers/ollama/register-constructor.ts 2.69ms
1268/2072 src/llm-providers/openai/computeOpenAiUsage.test.ts 3.14ms
1269/2072 src/llm-providers/openai/computeOpenAiUsage.ts 6.22ms
1270/2072 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 3.65ms
1271/2072 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 7.94ms
1272/2072 src/llm-providers/openai/createOpenAiExecutionTools.ts 3.91ms
1273/2072 src/llm-providers/openai/openai-models.test.ts 2.34ms
1274/2072 src/llm-providers/openai/openai-models.ts 22.50ms
1275/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts 3.61ms
1276/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 22.44ms
1277/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsInputBuilder.ts 7.40ms
1278/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 2.44ms
1279/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOutputTypeMapper.ts 6.43ms
1280/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsToolBuilder.ts 25.95ms
1281/2072 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 23.63ms
1282/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 5.51ms
1283/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsProgressReporter.ts 6.46ms
1284/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsPromptBuilder.ts 6.74ms
1285/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsStreamRunner.ts 14.55ms
1286/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsToolRunner.ts 33.12ms
1287/2072 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 14.72ms
1288/2072 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 21.05ms
1289/2072 src/llm-providers/openai/OpenAiCompatibleModelCatalog.ts 9.42ms
1290/2072 src/llm-providers/openai/OpenAiCompatibleNonChatPromptCaller.ts 15.03ms
1291/2072 src/llm-providers/openai/OpenAiCompatibleRequestManager.ts 11.53ms
1292/2072 src/llm-providers/openai/OpenAiExecutionTools.ts 4.41ms
1293/2072 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 2.21ms
1294/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchHandler.ts 13.97ms
1295/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchPoller.ts 27.04ms
1296/2072 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 10.37ms
1297/2072 src/llm-providers/openai/OpenAiVectorStoreKnowledgeSourcePreparer.ts 8.00ms
1298/2072 src/llm-providers/openai/playground/playground.ts 19.01ms
1299/2072 src/llm-providers/openai/playground/tsconfig.json 3.20ms
1300/2072 src/llm-providers/openai/register-configuration.ts 7.63ms
1301/2072 src/llm-providers/openai/register-constructor.ts 4.45ms
1302/2072 src/llm-providers/openai/utils/buildToolInvocationScript.ts 2.89ms
1303/2072 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 15.07ms
1304/2072 src/llm-providers/openai/utils/loadOpenAiAgentsModule.ts 3.39ms
1305/2072 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 2.82ms
1306/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatProgressReporter.ts 11.77ms
1307/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatPromptBuilder.ts 7.85ms
1308/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatToolCaller.ts 14.48ms
1309/2072 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 6.19ms
1310/2072 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 2.55ms
1311/2072 src/llm-providers/README.md 1.28ms
1312/2072 src/llm-providers/remote/playground/playground.ts 6.59ms
1313/2072 src/llm-providers/remote/playground/tsconfig.json 2.64ms
1314/2072 src/llm-providers/remote/RemoteLlmExecutionTools.ts 8.14ms
1315/2072 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 11.70ms
1316/2072 src/llm-providers/vercel/playground/playground.ts 10.48ms
1317/2072 src/llm-providers/vercel/playground/tsconfig.json 4.39ms
1318/2072 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 3.65ms
1319/2072 src/llm-providers/vercel/VercelProvider.ts 2.61ms
1320/2072 src/migrations/migratePipeline.ts 4.33ms
1321/2072 src/other/README.md 1.22ms
1322/2072 src/other/templates/getBookTemplates.ts 4.96ms
1323/2072 src/personas/preparePersona.ts 11.44ms
1324/2072 src/personas/README.md 1.26ms
1325/2072 src/pipeline/book-notation.ts 3.37ms
1326/2072 src/pipeline/isValidPipelineString.test.ts 1.95ms
1327/2072 src/pipeline/isValidPipelineString.ts 2.67ms
1328/2072 src/pipeline/PipelineInterface/constants.ts 1.97ms
1329/2072 src/pipeline/PipelineInterface/getPipelineInterface.ts 3.29ms
1330/2072 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 4.54ms
1331/2072 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 6.77ms
1332/2072 src/pipeline/PipelineInterface/PipelineInterface.ts 2.54ms
1333/2072 src/pipeline/PipelineInterface/README.md 1.55ms
1334/2072 src/pipeline/PipelineJson/CommonTaskJson.ts 5.50ms
1335/2072 src/pipeline/PipelineJson/DialogTaskJson.ts 1.92ms
1336/2072 src/pipeline/PipelineJson/Expectations.ts 2.84ms
1337/2072 src/pipeline/PipelineJson/KnowledgePieceJson.ts 7.08ms
1338/2072 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 3.18ms
1339/2072 src/pipeline/PipelineJson/ParameterJson.ts 4.36ms
1340/2072 src/pipeline/PipelineJson/PersonaJson.ts 15.30ms
1341/2072 src/pipeline/PipelineJson/PipelineJson.ts 9.15ms
1342/2072 src/pipeline/PipelineJson/PreparationJson.ts 5.72ms
1343/2072 src/pipeline/PipelineJson/PromptTaskJson.ts 3.81ms
1344/2072 src/pipeline/PipelineJson/ScriptTaskJson.ts 2.78ms
1345/2072 src/pipeline/PipelineJson/SimpleTaskJson.ts 2.34ms
1346/2072 src/pipeline/PipelineJson/TaskJson.ts 2.30ms
1347/2072 src/pipeline/PipelineString.ts 2.05ms
1348/2072 src/pipeline/prompt-notation.test.ts 6.63ms
1349/2072 src/pipeline/prompt-notation.ts 9.90ms
1350/2072 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 9.80ms
1351/2072 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 7.25ms
1352/2072 src/pipeline/prompt-notation/helpers/ParameterSection.ts 6.10ms
1353/2072 src/pipeline/validatePipelineString.ts 3.36ms
1354/2072 src/playground/permanent/_boilerplate.ts 1.84ms
1355/2072 src/playground/permanent/agent-with-browser-playground.ts 2.32ms
1356/2072 src/playground/permanent/error-handling-playground.ts 2.34ms
1357/2072 src/playground/playground.ts 2.29ms
1358/2072 src/playground/tsconfig.json 3.03ms
1359/2072 src/postprocessing/README.md 1.28ms
1360/2072 src/postprocessing/utils/extractBlock.ts 15.04ms
1361/2072 src/postprocessing/utils/extractJsonBlock.test.ts 3.03ms
1362/2072 src/postprocessing/utils/extractJsonBlock.ts 15.58ms
1363/2072 src/prepare/isPipelinePrepared.test.ts 14.03ms
1364/2072 src/prepare/isPipelinePrepared.ts 7.93ms
1365/2072 src/prepare/PrepareAndScrapeOptions.ts 4.08ms
1366/2072 src/prepare/preparePipeline.ts 15.42ms
1367/2072 src/prepare/preparePipelineOnRemoteServer.ts 4.00ms
1368/2072 src/prepare/prepareTasks.ts 13.29ms
1369/2072 src/prepare/unpreparePipeline.ts 4.17ms
1370/2072 src/remote-server/createRemoteClient.ts 6.59ms
1371/2072 src/remote-server/http-sample/README.md 1.72ms
1372/2072 src/remote-server/openapi-types.ts 16.32ms
1373/2072 src/remote-server/openapi.ts 8.27ms
1374/2072 src/remote-server/README.md 2.15ms
1375/2072 src/remote-server/RemoteServer.ts 2.35ms
1376/2072 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 1.95ms
1377/2072 src/remote-server/socket-types/_subtypes/Identification.ts 6.56ms
1378/2072 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 2.47ms
1379/2072 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 4.45ms
1380/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 2.24ms
1381/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 2.48ms
1382/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 2.25ms
1383/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 2.29ms
1384/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 2.38ms
1385/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 1.95ms
1386/2072 src/remote-server/startAgentServer.ts 4.49ms
1387/2072 src/remote-server/startRemoteServer.ts 14.07ms
1388/2072 src/remote-server/startRemoteServer/createRemoteServerExpressApp.ts 2.40ms
1389/2072 src/remote-server/startRemoteServer/createRemoteServerHandle.ts 6.37ms
1390/2072 src/remote-server/startRemoteServer/createSocketServer.ts 2.38ms
1391/2072 src/remote-server/startRemoteServer/getExecutionToolsFromIdentification.ts 7.49ms
1392/2072 src/remote-server/startRemoteServer/registerBookRoutes.ts 6.35ms
1393/2072 src/remote-server/startRemoteServer/registerExecutionRoutes.ts 7.83ms
1394/2072 src/remote-server/startRemoteServer/registerListModelsSocketHandler.ts 4.19ms
1395/2072 src/remote-server/startRemoteServer/registerLoginRoute.ts 5.07ms
1396/2072 src/remote-server/startRemoteServer/registerNotFoundRoute.ts 2.14ms
1397/2072 src/remote-server/startRemoteServer/registerOpenAiCompatibleChatCompletionsRoute.ts 6.61ms
1398/2072 src/remote-server/startRemoteServer/registerOpenApiRoutes.ts 4.12ms
1399/2072 src/remote-server/startRemoteServer/registerPreparePipelineSocketHandler.ts 21.42ms
1400/2072 src/remote-server/startRemoteServer/registerPromptSocketHandler.ts 7.49ms
1401/2072 src/remote-server/startRemoteServer/registerRemoteServerHttpRoutes.ts 3.85ms
1402/2072 src/remote-server/startRemoteServer/registerRemoteServerSocketHandlers.ts 28.84ms
1403/2072 src/remote-server/startRemoteServer/registerServerIndexRoute.ts 7.04ms
1404/2072 src/remote-server/startRemoteServer/RemoteServerRuntime.ts 2.78ms
1405/2072 src/remote-server/startRemoteServer/resolveStartRemoteServerConfiguration.ts 3.08ms
1406/2072 src/remote-server/startRemoteServer/respondToSocketRequest.ts 2.74ms
1407/2072 src/remote-server/startRemoteServer/SocketResponse.ts 1.76ms
1408/2072 src/remote-server/startRemoteServer/startListening.ts 4.00ms
1409/2072 src/remote-server/startRemoteServer/StartRemoteServerConfiguration.ts 2.92ms
1410/2072 src/remote-server/types/RemoteClientOptions.ts 3.90ms
1411/2072 src/remote-server/types/RemoteServerOptions.ts 14.10ms
1412/2072 src/remote-server/ui/renderServerIndexHtml.ts 5.90ms
1413/2072 src/remote-server/ui/ServerApp.tsx 9.86ms
1414/2072 src/remote-server/ui/types.ts 3.64ms
1415/2072 src/scrapers/_boilerplate/BoilerplateScraper.ts 11.72ms
1416/2072 src/scrapers/_boilerplate/createBoilerplateScraper.ts 2.48ms
1417/2072 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 7.56ms
1418/2072 src/scrapers/_boilerplate/playground/tsconfig.json 2.94ms
1419/2072 src/scrapers/_boilerplate/README.md 1.23ms
1420/2072 src/scrapers/_boilerplate/register-constructor.ts 2.80ms
1421/2072 src/scrapers/_boilerplate/register-metadata.ts 5.97ms
1422/2072 src/scrapers/_common/Converter.ts 3.01ms
1423/2072 src/scrapers/_common/prepareKnowledgePieces.test.ts 3.34ms
1424/2072 src/scrapers/_common/prepareKnowledgePieces.ts 8.37ms
1425/2072 src/scrapers/_common/register/$provideFilesystemForNode.ts 3.29ms
1426/2072 src/scrapers/_common/register/$provideScrapersForBrowser.ts 4.29ms
1427/2072 src/scrapers/_common/register/$provideScrapersForNode.ts 6.58ms
1428/2072 src/scrapers/_common/register/$provideScriptingForNode.ts 3.85ms
1429/2072 src/scrapers/_common/register/$registeredScrapersMessage.ts 6.45ms
1430/2072 src/scrapers/_common/register/$scrapersMetadataRegister.ts 2.26ms
1431/2072 src/scrapers/_common/register/$scrapersRegister.ts 2.14ms
1432/2072 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 6.18ms
1433/2072 src/scrapers/_common/register/ScraperConstructor.ts 2.77ms
1434/2072 src/scrapers/_common/Scraper.ts 4.09ms
1435/2072 src/scrapers/_common/ScraperIntermediateSource.ts 2.31ms
1436/2072 src/scrapers/_common/utils/fetchUrlContent.ts 8.13ms
1437/2072 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 2.94ms
1438/2072 src/scrapers/_common/utils/getScraperIntermediateSource.ts 8.46ms
1439/2072 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 17.04ms
1440/2072 src/scrapers/_common/utils/promptbookFetch.test.ts 3.69ms
1441/2072 src/scrapers/_common/utils/promptbookFetch.ts 3.17ms
1442/2072 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 3.33ms
1443/2072 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 35.89ms
1444/2072 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 25.19ms
1445/2072 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 7.14ms
1446/2072 src/scrapers/document-legacy/LegacyDocumentScraper.ts 10.52ms
1447/2072 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 4.66ms
1448/2072 src/scrapers/document-legacy/playground/tsconfig.json 2.72ms
1449/2072 src/scrapers/document-legacy/README.md 1.58ms
1450/2072 src/scrapers/document-legacy/register-constructor.ts 2.23ms
1451/2072 src/scrapers/document-legacy/register-metadata.ts 4.50ms
1452/2072 src/scrapers/document/createDocumentScraper.ts 2.72ms
1453/2072 src/scrapers/document/DocumentScraper.test.ts 8.81ms
1454/2072 src/scrapers/document/DocumentScraper.ts 10.80ms
1455/2072 src/scrapers/document/examples/10-simple.docx.knowledge.json 24.92ms
1456/2072 src/scrapers/document/examples/10-simple.odt.knowledge.json 36.28ms
1457/2072 src/scrapers/document/playground/document-scraper-playground.ts 4.74ms
1458/2072 src/scrapers/document/playground/tsconfig.json 6.24ms
1459/2072 src/scrapers/document/README.md 1.87ms
1460/2072 src/scrapers/document/register-constructor.ts 2.41ms
1461/2072 src/scrapers/document/register-metadata.ts 4.52ms
1462/2072 src/scrapers/markdown/createMarkdownScraper.ts 3.07ms
1463/2072 src/scrapers/markdown/MarkdownScraper.test.ts 3.66ms
1464/2072 src/scrapers/markdown/MarkdownScraper.ts 13.18ms
1465/2072 src/scrapers/markdown/playground/markdown-scraper-playground.ts 4.46ms
1466/2072 src/scrapers/markdown/playground/tsconfig.json 3.31ms
1467/2072 src/scrapers/markdown/README.md 2.00ms
1468/2072 src/scrapers/markdown/register-constructor.ts 2.31ms
1469/2072 src/scrapers/markdown/register-metadata.ts 3.69ms
1470/2072 src/scrapers/markitdown/createMarkitdownScraper.ts 3.55ms
1471/2072 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 14.95ms
1472/2072 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 22.12ms
1473/2072 src/scrapers/markitdown/MarkitdownScraper.ts 11.69ms
1474/2072 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 5.25ms
1475/2072 src/scrapers/markitdown/playground/tsconfig.json 3.46ms
1476/2072 src/scrapers/markitdown/README.md 1.40ms
1477/2072 src/scrapers/markitdown/register-constructor.ts 2.81ms
1478/2072 src/scrapers/markitdown/register-metadata.ts 25.13ms
1479/2072 src/scrapers/pdf/createPdfScraper.ts 3.44ms
1480/2072 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 1.66ms
1481/2072 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 2.90ms
1482/2072 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 8.04ms
1483/2072 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 28.75ms
1484/2072 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 2.36ms
1485/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 13.13ms
1486/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 1.83ms
1487/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 2.19ms
1488/2072 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 1.72ms
1489/2072 src/scrapers/pdf/lib/pdf2md/models/Page.js 1.70ms
1490/2072 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 1.88ms
1491/2072 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 1.81ms
1492/2072 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 1.69ms
1493/2072 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 5.23ms
1494/2072 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 3.20ms
1495/2072 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 2.83ms
1496/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 3.28ms
1497/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 3.40ms
1498/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 20.56ms
1499/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 6.54ms
1500/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 9.67ms
1501/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 4.47ms
1502/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 18.90ms
1503/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 5.22ms
1504/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 4.87ms
1505/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 5.31ms
1506/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 4.39ms
1507/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 5.24ms
1508/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 3.15ms
1509/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 2.55ms
1510/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 2.66ms
1511/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 2.56ms
1512/2072 src/scrapers/pdf/lib/pdf2md/models/Word.js 1.51ms
1513/2072 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 3.87ms
1514/2072 src/scrapers/pdf/lib/pdf2md/pdf2md.js 3.59ms
1515/2072 src/scrapers/pdf/lib/pdf2md/README.md 1.32ms
1516/2072 src/scrapers/pdf/lib/pdf2md/util/cli.js 2.84ms
1517/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 2.24ms
1518/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/createDocumentStub.js 2.72ms
1519/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElement.js 5.09ms
1520/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElementSerializer.js 6.97ms
1521/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/Image.js 2.23ms
1522/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/xmlEncode.js 2.34ms
1523/2072 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 2.93ms
1524/2072 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 4.60ms
1525/2072 src/scrapers/pdf/lib/pdf2md/util/pdf.js 13.08ms
1526/2072 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 6.67ms
1527/2072 src/scrapers/pdf/lib/pdf2md/util/transformations.js 10.14ms
1528/2072 src/scrapers/pdf/PdfScraper.ts 5.97ms
1529/2072 src/scrapers/pdf/playground/pdf-scraper-playground.ts 4.28ms
1530/2072 src/scrapers/pdf/playground/tsconfig.json 2.54ms
1531/2072 src/scrapers/pdf/README.md 1.67ms
1532/2072 src/scrapers/pdf/register-constructor.ts 2.16ms
1533/2072 src/scrapers/pdf/register-metadata.ts 3.76ms
1534/2072 src/scrapers/README.md 1.39ms
1535/2072 src/scrapers/website/createWebsiteScraper.ts 2.92ms
1536/2072 src/scrapers/website/playground/tsconfig.json 2.67ms
1537/2072 src/scrapers/website/playground/website-scraper-playground.ts 5.07ms
1538/2072 src/scrapers/website/README.md 1.46ms
1539/2072 src/scrapers/website/register-constructor.ts 9.91ms
1540/2072 src/scrapers/website/register-metadata.ts 4.11ms
1541/2072 src/scrapers/website/utils/createShowdownConverter.test.ts 4.27ms
1542/2072 src/scrapers/website/utils/createShowdownConverter.ts 2.20ms
1543/2072 src/scrapers/website/WebsiteScraper.ts 11.29ms
1544/2072 src/scripting/_test/custom-function-async.test.ts.test.ts 5.68ms
1545/2072 src/scripting/_test/custom-function-missing.test.ts 4.75ms
1546/2072 src/scripting/_test/custom-function-with-dependencies.test.ts 6.91ms
1547/2072 src/scripting/_test/custom-function.test.ts 8.79ms
1548/2072 src/scripting/_test/postprocessing.test.ts 11.42ms
1549/2072 src/scripting/_test/script-execution-errors.test.ts 6.22ms
1550/2072 src/scripting/_test/script-execution-tools.test.ts 7.15ms
1551/2072 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 6.24ms
1552/2072 src/scripting/javascript/JavascriptEvalExecutionTools.ts 10.94ms
1553/2072 src/scripting/javascript/JavascriptExecutionTools.ts 1.81ms
1554/2072 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 4.98ms
1555/2072 src/scripting/javascript/postprocessing-functions.ts 8.08ms
1556/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 2.82ms
1557/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 4.19ms
1558/2072 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 2.19ms
1559/2072 src/scripting/python/PythonExecutionTools.ts 3.65ms
1560/2072 src/scripting/typescript/TypescriptExecutionTools.ts 3.24ms
1561/2072 src/search-engines/_index.ts 2.28ms
1562/2072 src/search-engines/bing/BingSearchEngine.ts 3.64ms
1563/2072 src/search-engines/dummy/DummySearchEngine.ts 3.60ms
1564/2072 src/search-engines/google/GoogleSearchEngine.ts 4.75ms
1565/2072 src/search-engines/SearchEngine.ts 1.95ms
1566/2072 src/search-engines/SearchResult.ts 1.63ms
1567/2072 src/search-engines/serp/SerpSearchEngine.ts 3.70ms
1568/2072 src/speech-recognition/BrowserSpeechRecognition.ts 7.02ms
1569/2072 src/speech-recognition/OpenAiSpeechRecognition.test.ts 4.49ms
1570/2072 src/speech-recognition/OpenAiSpeechRecognition.ts 26.15ms
1571/2072 src/storage/_common/PromptbookStorage.test-type.ts 2.12ms
1572/2072 src/storage/_common/PromptbookStorage.ts 3.11ms
1573/2072 src/storage/blackhole/BlackholeStorage.ts 3.04ms
1574/2072 src/storage/env-storage/$EnvStorage.ts 5.61ms
1575/2072 src/storage/file-cache-storage/FileCacheStorage.ts 8.54ms
1576/2072 src/storage/file-cache-storage/FileCacheStorageOptions.ts 2.04ms
1577/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 1.67ms
1578/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 2.21ms
1579/2072 src/storage/local-storage/getIndexedDbStorage.ts 4.00ms
1580/2072 src/storage/local-storage/getLocalStorage.ts 2.93ms
1581/2072 src/storage/local-storage/getSessionStorage.ts 2.99ms
1582/2072 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 2.05ms
1583/2072 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 3.65ms
1584/2072 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 4.15ms
1585/2072 src/storage/memory/MemoryStorage.ts 3.29ms
1586/2072 src/storage/utils/PrefixStorage.ts 2.92ms
1587/2072 src/transpilers/_common/BookTranspiler.ts 2.77ms
1588/2072 src/transpilers/_common/BookTranspilerOptions.ts 5.30ms
1589/2072 src/transpilers/_common/createTranspiledTeamRuntimeSection.ts 12.98ms
1590/2072 src/transpilers/_common/createZodSchemaSource.ts 6.19ms
1591/2072 src/transpilers/_common/formatUsedToolFunctions.test.ts 2.51ms
1592/2072 src/transpilers/_common/formatUsedToolFunctions.ts 4.83ms
1593/2072 src/transpilers/_common/prepareSdkTranspilerContext.ts 7.47ms
1594/2072 src/transpilers/_common/register/$bookTranspilersRegister.ts 2.47ms
1595/2072 src/transpilers/_common/resolveClaudeModelName.ts 2.43ms
1596/2072 src/transpilers/_common/TranspiledTeamExport.ts 4.55ms
1597/2072 src/transpilers/_common/transpiledTeamTranspilers.test.ts 5.28ms
1598/2072 src/transpilers/agent-os/AgentOsTranspiler.test.ts 12.10ms
1599/2072 src/transpilers/agent-os/AgentOsTranspiler.ts 14.14ms
1600/2072 src/transpilers/agent-os/register.ts 3.57ms
1601/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts 5.34ms
1602/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.ts 17.03ms
1603/2072 src/transpilers/anthropic-claude-managed/register.ts 5.39ms
1604/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts 10.93ms
1605/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.ts 24.97ms
1606/2072 src/transpilers/anthropic-claude-sdk/register.ts 2.28ms
1607/2072 src/transpilers/e2b/E2BTranspiler.test.ts 3.91ms
1608/2072 src/transpilers/e2b/E2BTranspiler.ts 10.60ms
1609/2072 src/transpilers/e2b/register.ts 3.01ms
1610/2072 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 5.23ms
1611/2072 src/transpilers/formatted-book-in-markdown/register.ts 2.80ms
1612/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts 7.17ms
1613/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.ts 19.52ms
1614/2072 src/transpilers/openai-agents/register.ts 2.84ms
1615/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 5.49ms
1616/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 12.57ms
1617/2072 src/transpilers/openai-sdk/playground/playground.ts 2.32ms
1618/2072 src/transpilers/openai-sdk/playground/tsconfig.json 2.49ms
1619/2072 src/transpilers/openai-sdk/register.ts 3.35ms
1620/2072 src/types/Arrayable.ts 1.71ms
1621/2072 src/types/InputParameters_private.ts 1.83ms
1622/2072 src/types/IntermediateFilesStrategy.ts 2.24ms
1623/2072 src/types/LlmCall.ts 1.89ms
1624/2072 src/types/LlmToolDefinition.ts 2.58ms
1625/2072 src/types/Message.ts 3.63ms
1626/2072 src/types/ModelRequirements.ts 6.53ms
1627/2072 src/types/ModelVariant.ts 2.43ms
1628/2072 src/types/NonEmptyArray.ts 1.82ms
1629/2072 src/types/number_bytes.ts 1.50ms
1630/2072 src/types/number_id.ts 1.42ms
1631/2072 src/types/number_likeness.ts 1.86ms
1632/2072 src/types/number_milliseconds.ts 1.86ms
1633/2072 src/types/number_percent.ts 1.55ms
1634/2072 src/types/number_positive.ts 1.29ms
1635/2072 src/types/number_usd.ts 1.78ms
1636/2072 src/types/Parameters_private.ts 2.39ms
1637/2072 src/types/Parameters.ts 2.24ms
1638/2072 src/types/Prompt.ts 5.75ms
1639/2072 src/types/ReservedParameters_private.ts 2.07ms
1640/2072 src/types/ScriptLanguage.ts 2.14ms
1641/2072 src/types/SectionType.ts 2.32ms
1642/2072 src/types/SpeechRecognition.ts 4.25ms
1643/2072 src/types/string_agent_hash_private.ts 7.74ms
1644/2072 src/types/string_agent_name_in_book_private.ts 1.90ms
1645/2072 src/types/string_agent_name_private.ts 1.70ms
1646/2072 src/types/string_agent_name.ts 1.69ms
1647/2072 src/types/string_agent_permanent_id_private.ts 1.69ms
1648/2072 src/types/string_agent_url_private.ts 1.45ms
1649/2072 src/types/string_agent_url.ts 1.53ms
1650/2072 src/types/string_base_url_private.ts 1.54ms
1651/2072 src/types/string_base_url.ts 1.96ms
1652/2072 src/types/string_base64_private.ts 2.09ms
1653/2072 src/types/string_base64.ts 1.78ms
1654/2072 src/types/string_business_category_name_private.ts 1.62ms
1655/2072 src/types/string_business_category_name.ts 1.50ms
1656/2072 src/types/string_char_private.ts 1.40ms
1657/2072 src/types/string_chat_prompt_private.ts 1.54ms
1658/2072 src/types/string_completion_prompt_private.ts 1.87ms
1659/2072 src/types/string_email_private.ts 1.45ms
1660/2072 src/types/string_email.ts 1.45ms
1661/2072 src/types/string_filename.ts 2.32ms
1662/2072 src/types/string_host_private.ts 2.34ms
1663/2072 src/types/string_host.ts 1.90ms
1664/2072 src/types/string_href_private.ts 2.08ms
1665/2072 src/types/string_href.ts 1.61ms
1666/2072 src/types/string_knowledge_source_content.ts 2.12ms
1667/2072 src/types/string_markdown.ts 3.67ms
1668/2072 src/types/string_mime_type_private.ts 5.84ms
1669/2072 src/types/string_mime_type.ts 1.51ms
1670/2072 src/types/string_model_description_private.ts 1.57ms
1671/2072 src/types/string_model_name_private.ts 2.68ms
1672/2072 src/types/string_model_name.ts 1.75ms
1673/2072 src/types/string_name_private.ts 1.47ms
1674/2072 src/types/string_name.ts 2.05ms
1675/2072 src/types/string_page_private.ts 1.48ms
1676/2072 src/types/string_page.ts 1.59ms
1677/2072 src/types/string_parameter_name.ts 2.26ms
1678/2072 src/types/string_parameter_value_private.ts 1.53ms
1679/2072 src/types/string_person_fullname.ts 3.20ms
1680/2072 src/types/string_persona_description_private.ts 1.47ms
1681/2072 src/types/string_persona_description.ts 1.49ms
1682/2072 src/types/string_pipeline_root_url_private.ts 1.32ms
1683/2072 src/types/string_pipeline_root_url.ts 1.27ms
1684/2072 src/types/string_pipeline_url_private.ts 1.39ms
1685/2072 src/types/string_pipeline_url.ts 3.86ms
1686/2072 src/types/string_prompt_image_private.ts 1.58ms
1687/2072 src/types/string_prompt_private.ts 1.67ms
1688/2072 src/types/string_prompt.ts 2.19ms
1689/2072 src/types/string_promptbook_server_url_private.ts 2.17ms
1690/2072 src/types/string_promptbook_server_url.ts 1.80ms
1691/2072 src/types/string_reserved_parameter_name_private.ts 3.33ms
1692/2072 src/types/string_sha256.ts 3.65ms
1693/2072 src/types/string_system_message_private.ts 1.55ms
1694/2072 src/types/string_template_private.ts 1.45ms
1695/2072 src/types/string_text_prompt_private.ts 8.73ms
1696/2072 src/types/string_title_private.ts 2.04ms
1697/2072 src/types/string_title.ts 1.42ms
1698/2072 src/types/string_token.ts 2.92ms
1699/2072 src/types/string_url_image_private.ts 12.90ms
1700/2072 src/types/string_url_image.ts 1.54ms
1701/2072 src/types/string_url_private.ts 1.48ms
1702/2072 src/types/string_url.ts 2.18ms
1703/2072 src/types/TaskType.ts 2.17ms
1704/2072 src/types/ToolCall.ts 6.79ms
1705/2072 src/types/typeAliasEmoji.ts 4.55ms
1706/2072 src/types/typeAliases.ts 6.91ms
1707/2072 src/types/Updatable.ts 2.97ms
1708/2072 src/utils/agent-message-runtime/AgentMessageProjectChange.ts 6.21ms
1709/2072 src/utils/agent-message-runtime/agentMessageRuntimePaths.ts 3.69ms
1710/2072 src/utils/agent-message-runtime/AgentMessageTouchedExternalSource.ts 5.60ms
1711/2072 src/utils/agent-message-runtime/parseAgentMessageRuntimeLogEvents.ts 6.35ms
1712/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts 5.73ms
1713/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.ts 11.54ms
1714/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts 5.68ms
1715/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.ts 12.29ms
1716/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts 3.76ms
1717/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.ts 16.09ms
1718/2072 src/utils/agents/resolveAgentAvatarImageUrl.test.ts 4.35ms
1719/2072 src/utils/agents/resolveAgentAvatarImageUrl.ts 8.53ms
1720/2072 src/utils/agents/terminalAgentAvatarVisual.test.ts 5.40ms
1721/2072 src/utils/agents/terminalAgentAvatarVisual.ts 15.94ms
1722/2072 src/utils/ascii-art/$detectTerminalAnsiColorDepth.ts 3.67ms
1723/2072 src/utils/ascii-art/convertImageDataToAsciiArt.ts 9.66ms
1724/2072 src/utils/ascii-art/createAnsiColorCode.ts 4.67ms
1725/2072 src/utils/chat/chatAttachments.test.ts 16.69ms
1726/2072 src/utils/chat/chatAttachments.ts 4.39ms
1727/2072 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 4.32ms
1728/2072 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 3.98ms
1729/2072 src/utils/chat/chatAttachments/appendChatContextSections.ts 2.50ms
1730/2072 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 4.28ms
1731/2072 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 2.83ms
1732/2072 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 4.76ms
1733/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 12.51ms
1734/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 7.47ms
1735/2072 src/utils/chat/constants.ts 2.08ms
1736/2072 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 2.35ms
1737/2072 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 2.46ms
1738/2072 src/utils/chat/escapeRegExp.ts 1.70ms
1739/2072 src/utils/clientVersion.ts 5.39ms
1740/2072 src/utils/color/$randomColor.ts 2.10ms
1741/2072 src/utils/color/Color.test.ts 2.22ms
1742/2072 src/utils/color/Color.ts 7.40ms
1743/2072 src/utils/color/ColorValue.ts 3.25ms
1744/2072 src/utils/color/css-colors.ts 8.44ms
1745/2072 src/utils/color/internal-utils/checkChannelValue.ts 2.49ms
1746/2072 src/utils/color/internal-utils/hslToRgb.ts 2.03ms
1747/2072 src/utils/color/internal-utils/rgbToHsl.ts 2.11ms
1748/2072 src/utils/color/isHexColorString.ts 2.29ms
1749/2072 src/utils/color/operators/ColorTransformer.ts 1.37ms
1750/2072 src/utils/color/operators/darken.ts 2.15ms
1751/2072 src/utils/color/operators/furthest.ts 2.68ms
1752/2072 src/utils/color/operators/grayscale.ts 2.00ms
1753/2072 src/utils/color/operators/lighten.ts 2.57ms
1754/2072 src/utils/color/operators/mixWithColor.ts 2.38ms
1755/2072 src/utils/color/operators/nearest.ts 2.19ms
1756/2072 src/utils/color/operators/negative.ts 1.70ms
1757/2072 src/utils/color/operators/negativeLightness.ts 2.33ms
1758/2072 src/utils/color/operators/README.md 1.21ms
1759/2072 src/utils/color/operators/saturate.ts 2.27ms
1760/2072 src/utils/color/operators/withAlpha.ts 1.79ms
1761/2072 src/utils/color/parseColorString.ts 2.84ms
1762/2072 src/utils/color/parsers/ColorChannelSet.ts 1.84ms
1763/2072 src/utils/color/parsers/parseHexColor.ts 3.38ms
1764/2072 src/utils/color/parsers/parseHslColor.ts 2.61ms
1765/2072 src/utils/color/parsers/parseRgbColor.ts 2.90ms
1766/2072 src/utils/color/utils/areColorsEqual.ts 1.91ms
1767/2072 src/utils/color/utils/colorDistance.ts 2.23ms
1768/2072 src/utils/color/utils/colorHue.ts 1.57ms
1769/2072 src/utils/color/utils/colorHueDistance.test.ts 2.46ms
1770/2072 src/utils/color/utils/colorHueDistance.ts 1.69ms
1771/2072 src/utils/color/utils/colorLuminance.ts 1.74ms
1772/2072 src/utils/color/utils/colorSatulightion.ts 3.25ms
1773/2072 src/utils/color/utils/colorSaturation.ts 1.79ms
1774/2072 src/utils/color/utils/colorToDataUrl.ts 3.39ms
1775/2072 src/utils/color/utils/mixColors.ts 1.96ms
1776/2072 src/utils/database/uniqueConstraint.ts 4.04ms
1777/2072 src/utils/DEFAULT_THINKING_MESSAGES.ts 2.30ms
1778/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 3.08ms
1779/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 3.56ms
1780/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 2.36ms
1781/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 3.24ms
1782/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 2.79ms
1783/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 3.08ms
1784/2072 src/utils/editable/types/PipelineEditableSerialized.ts 3.24ms
1785/2072 src/utils/editable/utils/isFlatPipeline.test.ts 2.41ms
1786/2072 src/utils/editable/utils/isFlatPipeline.ts 3.17ms
1787/2072 src/utils/editable/utils/renamePipelineParameter.test.ts 5.95ms
1788/2072 src/utils/editable/utils/renamePipelineParameter.ts 5.12ms
1789/2072 src/utils/editable/utils/stringifyPipelineJson.ts 3.52ms
1790/2072 src/utils/environment/$detectRuntimeEnvironment.ts 2.65ms
1791/2072 src/utils/environment/$getGlobalScope.ts 1.77ms
1792/2072 src/utils/environment/$isRunningInBrowser.ts 1.93ms
1793/2072 src/utils/environment/$isRunningInJest.ts 1.71ms
1794/2072 src/utils/environment/$isRunningInNode.ts 1.78ms
1795/2072 src/utils/environment/$isRunningInWebWorker.ts 2.50ms
1796/2072 src/utils/execCommand/$execCommand.ts 5.43ms
1797/2072 src/utils/execCommand/$execCommandNormalizeOptions.ts 3.80ms
1798/2072 src/utils/execCommand/$execCommands.ts 2.60ms
1799/2072 src/utils/execCommand/execCommand.test.ts 2.25ms
1800/2072 src/utils/execCommand/execCommandNormalizeOptions.test.ts 9.89ms
1801/2072 src/utils/execCommand/ExecCommandOptions.ts 3.30ms
1802/2072 src/utils/expectation-counters/constants.ts 2.88ms
1803/2072 src/utils/expectation-counters/countCharacters.test.ts 2.45ms
1804/2072 src/utils/expectation-counters/countCharacters.ts 2.35ms
1805/2072 src/utils/expectation-counters/countLines.test.ts 1.86ms
1806/2072 src/utils/expectation-counters/countLines.ts 2.73ms
1807/2072 src/utils/expectation-counters/countPages.test.ts 8.67ms
1808/2072 src/utils/expectation-counters/countPages.ts 15.14ms
1809/2072 src/utils/expectation-counters/countParagraphs.test.ts 2.84ms
1810/2072 src/utils/expectation-counters/countParagraphs.ts 2.04ms
1811/2072 src/utils/expectation-counters/countSentences.test.ts 2.51ms
1812/2072 src/utils/expectation-counters/countSentences.ts 4.96ms
1813/2072 src/utils/expectation-counters/countWords.test.ts 2.77ms
1814/2072 src/utils/expectation-counters/countWords.ts 3.36ms
1815/2072 src/utils/expectation-counters/index.ts 4.26ms
1816/2072 src/utils/expectation-counters/other/countPages.md 2.94ms
1817/2072 src/utils/files/$induceBookDownload.ts 2.77ms
1818/2072 src/utils/files/$induceFileDownload.ts 3.23ms
1819/2072 src/utils/files/decodeAttachmentAsText.test.ts 4.37ms
1820/2072 src/utils/files/decodeAttachmentAsText.ts 22.42ms
1821/2072 src/utils/files/extensionToMimeType.test.ts 2.73ms
1822/2072 src/utils/files/extensionToMimeType.ts 1.55ms
1823/2072 src/utils/files/getFileExtension.test.ts 2.53ms
1824/2072 src/utils/files/getFileExtension.ts 1.75ms
1825/2072 src/utils/files/isDirectoryExisting.test.ts 1.93ms
1826/2072 src/utils/files/isDirectoryExisting.ts 2.65ms
1827/2072 src/utils/files/isExecutable.ts 2.20ms
1828/2072 src/utils/files/isFileExisting.test.ts 1.96ms
1829/2072 src/utils/files/isFileExisting.ts 2.98ms
1830/2072 src/utils/files/listAllFiles.test.ts 3.41ms
1831/2072 src/utils/files/listAllFiles.ts 3.32ms
1832/2072 src/utils/files/mimeTypeToExtension.test.ts 2.41ms
1833/2072 src/utils/files/mimeTypeToExtension.ts 1.87ms
1834/2072 src/utils/files/ObjectUrl.ts 5.52ms
1835/2072 src/utils/files/readResponseBytes.ts 6.58ms
1836/2072 src/utils/filesystem/promptbookTemporaryPath.test.ts 2.48ms
1837/2072 src/utils/filesystem/promptbookTemporaryPath.ts 5.02ms
1838/2072 src/utils/html/escapeHtml.ts 2.00ms
1839/2072 src/utils/isTimingSafeEqualString.ts 3.19ms
1840/2072 src/utils/knowledge/inlineKnowledgeSource.test.ts 5.73ms
1841/2072 src/utils/knowledge/inlineKnowledgeSource.ts 19.70ms
1842/2072 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 4.10ms
1843/2072 src/utils/knowledge/simplifyKnowledgeLabel.ts 8.10ms
1844/2072 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 4.48ms
1845/2072 src/utils/linguistic-hash/linguisticHash.test.ts 4.30ms
1846/2072 src/utils/linguistic-hash/linguisticHash.ts 3.29ms
1847/2072 src/utils/linguistic-hash/LinguisticHashLanguage.ts 3.72ms
1848/2072 src/utils/linguistic-hash/linguisticHashTypes.ts 1.79ms
1849/2072 src/utils/linguistic-hash/linguisticHashWordCount.ts 2.45ms
1850/2072 src/utils/linguistic-hash/linguisticHashWords.cs.ts 3.36ms
1851/2072 src/utils/linguistic-hash/linguisticHashWords.en.ts 12.73ms
1852/2072 src/utils/linguistic-hash/linguisticHashWordSelection.ts 4.10ms
1853/2072 src/utils/markdown/addAutoGeneratedSection.test.ts 3.80ms
1854/2072 src/utils/markdown/addAutoGeneratedSection.ts 3.67ms
1855/2072 src/utils/markdown/createMarkdownChart.test.ts 2.95ms
1856/2072 src/utils/markdown/createMarkdownChart.ts 7.03ms
1857/2072 src/utils/markdown/createMarkdownTable.test.ts 2.44ms
1858/2072 src/utils/markdown/createMarkdownTable.ts 2.45ms
1859/2072 src/utils/markdown/escapeMarkdownBlock.test.ts 2.43ms
1860/2072 src/utils/markdown/escapeMarkdownBlock.ts 2.10ms
1861/2072 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 2.16ms
1862/2072 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 6.11ms
1863/2072 src/utils/markdown/extractAllBlocksFromMarkdown.ts 5.27ms
1864/2072 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 5.32ms
1865/2072 src/utils/markdown/extractAllListItemsFromMarkdown.ts 3.17ms
1866/2072 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 5.39ms
1867/2072 src/utils/markdown/extractOneBlockFromMarkdown.ts 3.00ms
1868/2072 src/utils/markdown/flattenMarkdown.test.ts 4.90ms
1869/2072 src/utils/markdown/flattenMarkdown.ts 3.29ms
1870/2072 src/utils/markdown/humanizeAiText.test.ts 4.55ms
1871/2072 src/utils/markdown/humanizeAiText.ts 3.67ms
1872/2072 src/utils/markdown/humanizeAiTextEllipsis.ts 2.03ms
1873/2072 src/utils/markdown/humanizeAiTextEmdashed.ts 1.92ms
1874/2072 src/utils/markdown/humanizeAiTextQuotes.ts 1.88ms
1875/2072 src/utils/markdown/humanizeAiTextSources.ts 1.79ms
1876/2072 src/utils/markdown/humanizeAiTextWhitespace.ts 2.34ms
1877/2072 src/utils/markdown/parseMarkdownSection.test.ts 2.52ms
1878/2072 src/utils/markdown/parseMarkdownSection.ts 2.94ms
1879/2072 src/utils/markdown/prettifyMarkdown.ts 2.20ms
1880/2072 src/utils/markdown/promptbookifyAiText.test.ts 2.59ms
1881/2072 src/utils/markdown/promptbookifyAiText.ts 3.34ms
1882/2072 src/utils/markdown/README.md 1.23ms
1883/2072 src/utils/markdown/removeMarkdownComments.test.ts 3.12ms
1884/2072 src/utils/markdown/removeMarkdownComments.ts 2.37ms
1885/2072 src/utils/markdown/removeMarkdownFormatting.test.ts 3.77ms
1886/2072 src/utils/markdown/removeMarkdownFormatting.ts 1.81ms
1887/2072 src/utils/markdown/removeMarkdownLinks.test.ts 1.69ms
1888/2072 src/utils/markdown/removeMarkdownLinks.ts 2.01ms
1889/2072 src/utils/markdown/shiftMarkdownHeadingLevels.ts 2.77ms
1890/2072 src/utils/markdown/splitMarkdownIntoSections.test.ts 3.21ms
1891/2072 src/utils/markdown/splitMarkdownIntoSections.ts 3.13ms
1892/2072 src/utils/markdown/trimCodeBlock.test.ts 3.61ms
1893/2072 src/utils/markdown/trimCodeBlock.ts 1.77ms
1894/2072 src/utils/markdown/trimEndOfCodeBlock.test.ts 2.03ms
1895/2072 src/utils/markdown/trimEndOfCodeBlock.ts 2.35ms
1896/2072 src/utils/misc/$getCurrentDate.ts 1.68ms
1897/2072 src/utils/misc/$Register.ts 4.08ms
1898/2072 src/utils/misc/aboutPromptbookInformation.ts 4.48ms
1899/2072 src/utils/misc/arrayableToArray.test.ts 1.55ms
1900/2072 src/utils/misc/arrayableToArray.ts 1.82ms
1901/2072 src/utils/misc/computeHash.test.ts 1.68ms
1902/2072 src/utils/misc/computeHash.ts 1.82ms
1903/2072 src/utils/misc/createLazyModuleLoader.ts 2.67ms
1904/2072 src/utils/misc/debounce.test.ts 2.68ms
1905/2072 src/utils/misc/debounce.ts 1.89ms
1906/2072 src/utils/misc/emojis.ts 4.86ms
1907/2072 src/utils/misc/FromtoItems.ts 1.77ms
1908/2072 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 3.53ms
1909/2072 src/utils/misc/parseNumber.test.ts 2.67ms
1910/2072 src/utils/misc/parseNumber.ts 3.64ms
1911/2072 src/utils/misc/xAboutPromptbookInformation.tsx 2.66ms
1912/2072 src/utils/normalization/capitalize.test.ts 1.88ms
1913/2072 src/utils/normalization/capitalize.ts 1.69ms
1914/2072 src/utils/normalization/constructImageFilename.test.ts 2.51ms
1915/2072 src/utils/normalization/constructImageFilename.ts 2.42ms
1916/2072 src/utils/normalization/decapitalize.test.ts 2.17ms
1917/2072 src/utils/normalization/decapitalize.ts 1.50ms
1918/2072 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 6.52ms
1919/2072 src/utils/normalization/IKeywords.ts 1.52ms
1920/2072 src/utils/normalization/index.ts 0.83ms
1921/2072 src/utils/normalization/isValidKeyword.test.ts 2.09ms
1922/2072 src/utils/normalization/isValidKeyword.ts 2.15ms
1923/2072 src/utils/normalization/nameToUriPart.test.ts 2.19ms
1924/2072 src/utils/normalization/nameToUriPart.ts 1.87ms
1925/2072 src/utils/normalization/nameToUriParts.test.ts 2.06ms
1926/2072 src/utils/normalization/nameToUriParts.ts 1.85ms
1927/2072 src/utils/normalization/normalize-to-kebab-case.test.ts 2.39ms
1928/2072 src/utils/normalization/normalize-to-kebab-case.ts 2.97ms
1929/2072 src/utils/normalization/normalizeMessageText.test.ts 1.87ms
1930/2072 src/utils/normalization/normalizeMessageText.ts 1.48ms
1931/2072 src/utils/normalization/normalizeTo_camelCase.test.ts 1.88ms
1932/2072 src/utils/normalization/normalizeTo_camelCase.ts 3.17ms
1933/2072 src/utils/normalization/normalizeTo_PascalCase.test.ts 1.93ms
1934/2072 src/utils/normalization/normalizeTo_PascalCase.ts 1.77ms
1935/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 1.76ms
1936/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 3.59ms
1937/2072 src/utils/normalization/normalizeTo_snake_case.test.ts 1.89ms
1938/2072 src/utils/normalization/normalizeTo_snake_case.ts 1.59ms
1939/2072 src/utils/normalization/normalizeWhitespaces.test.ts 1.53ms
1940/2072 src/utils/normalization/normalizeWhitespaces.ts 1.40ms
1941/2072 src/utils/normalization/orderJson.test.ts 1.58ms
1942/2072 src/utils/normalization/orderJson.ts 2.18ms
1943/2072 src/utils/normalization/parseKeywords.test.ts 2.15ms
1944/2072 src/utils/normalization/parseKeywords.ts 2.09ms
1945/2072 src/utils/normalization/parseKeywordsFromString.test.ts 3.48ms
1946/2072 src/utils/normalization/parseKeywordsFromString.ts 1.87ms
1947/2072 src/utils/normalization/removeDiacritics.test.ts 1.95ms
1948/2072 src/utils/normalization/removeDiacritics.ts 1.81ms
1949/2072 src/utils/normalization/removeEmojis.test.ts 2.15ms
1950/2072 src/utils/normalization/removeEmojis.ts 1.65ms
1951/2072 src/utils/normalization/removeQuotes.test.ts 1.74ms
1952/2072 src/utils/normalization/removeQuotes.ts 1.95ms
1953/2072 src/utils/normalization/searchKeywords.test.ts 1.84ms
1954/2072 src/utils/normalization/searchKeywords.ts 1.73ms
1955/2072 src/utils/normalization/suffixUrl.test.ts 1.40ms
1956/2072 src/utils/normalization/suffixUrl.ts 1.44ms
1957/2072 src/utils/normalization/titleToName.test.ts 2.60ms
1958/2072 src/utils/normalization/titleToName.ts 3.16ms
1959/2072 src/utils/normalization/unwrapResult.test.ts 4.88ms
1960/2072 src/utils/normalization/unwrapResult.ts 4.94ms
1961/2072 src/utils/organization/___and___.ts 1.30ms
1962/2072 src/utils/organization/___or___.ts 1.21ms
1963/2072 src/utils/organization/$side_effect.ts 1.20ms
1964/2072 src/utils/organization/$sideEffect.ts 2.14ms
1965/2072 src/utils/organization/empty_object.ts 1.39ms
1966/2072 src/utils/organization/just_empty_object.ts 1.48ms
1967/2072 src/utils/organization/just.ts 1.71ms
1968/2072 src/utils/organization/keepImported.ts 1.67ms
1969/2072 src/utils/organization/keepTypeImported.ts 1.50ms
1970/2072 src/utils/organization/keepUnused.ts 2.39ms
1971/2072 src/utils/organization/preserve.ts 1.87ms
1972/2072 src/utils/organization/README.md 2.60ms
1973/2072 src/utils/organization/really_any.ts 1.35ms
1974/2072 src/utils/organization/really_unknown.ts 1.19ms
1975/2072 src/utils/organization/spaceTrim.ts 1.48ms
1976/2072 src/utils/organization/TODO_any.ts 1.23ms
1977/2072 src/utils/organization/TODO_narrow.ts 1.05ms
1978/2072 src/utils/organization/TODO_object.ts 1.04ms
1979/2072 src/utils/organization/TODO_remove_as.ts 1.15ms
1980/2072 src/utils/organization/TODO_string.ts 1.10ms
1981/2072 src/utils/organization/TODO_unknown.ts 1.07ms
1982/2072 src/utils/organization/TODO_USE.ts 1.49ms
1983/2072 src/utils/parameters/extractParameterNames.test.ts 2.70ms
1984/2072 src/utils/parameters/extractParameterNames.ts 2.09ms
1985/2072 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 2.93ms
1986/2072 src/utils/parameters/mapAvailableToExpectedParameters.ts 4.11ms
1987/2072 src/utils/parameters/numberToString.test.ts 1.48ms
1988/2072 src/utils/parameters/numberToString.ts 2.11ms
1989/2072 src/utils/parameters/templateParameters.test.ts 4.37ms
1990/2072 src/utils/parameters/templateParameters.ts 4.26ms
1991/2072 src/utils/parameters/valueToString.test.ts 1.90ms
1992/2072 src/utils/parameters/valueToString.ts 2.89ms
1993/2072 src/utils/random/$generateBookBoilerplate.test.ts 1.97ms
1994/2072 src/utils/random/$generateBookBoilerplate.ts 4.26ms
1995/2072 src/utils/random/$randomAgentPersona.ts 2.91ms
1996/2072 src/utils/random/$randomAgentRule.ts 2.54ms
1997/2072 src/utils/random/$randomBase58.ts 2.06ms
1998/2072 src/utils/random/$randomFullnameWithColor.ts 2.85ms
1999/2072 src/utils/random/$randomItem.ts 1.44ms
2000/2072 src/utils/random/$randomSeed.ts 1.52ms
2001/2072 src/utils/random/$randomToken.ts 1.95ms
2002/2072 src/utils/random/CzechNamePool.ts 6.54ms
2003/2072 src/utils/random/EnglishNamePool.ts 5.95ms
2004/2072 src/utils/random/getNamePool.ts 2.43ms
2005/2072 src/utils/random/NamePool.ts 1.68ms
2006/2072 src/utils/serialization/$deepFreeze.ts 3.10ms
2007/2072 src/utils/serialization/asSerializable.test.ts 1.48ms
2008/2072 src/utils/serialization/asSerializable.ts 2.09ms
2009/2072 src/utils/serialization/checkSerializableAsJson.ts 7.68ms
2010/2072 src/utils/serialization/clonePipeline.ts 3.32ms
2011/2072 src/utils/serialization/deepClone.test.ts 2.19ms
2012/2072 src/utils/serialization/deepClone.ts 2.84ms
2013/2072 src/utils/serialization/exportJson.ts 3.69ms
2014/2072 src/utils/serialization/isSerializableAsJson.test.ts 2.96ms
2015/2072 src/utils/serialization/isSerializableAsJson.ts 2.11ms
2016/2072 src/utils/serialization/jsonStringsToJsons.test.ts 2.95ms
2017/2072 src/utils/serialization/jsonStringsToJsons.ts 1.88ms
2018/2072 src/utils/serialization/serializeToPromptbookJavascript.test.ts 2.08ms
2019/2072 src/utils/serialization/serializeToPromptbookJavascript.ts 4.10ms
2020/2072 src/utils/sets/difference.test.ts 1.93ms
2021/2072 src/utils/sets/difference.ts 1.77ms
2022/2072 src/utils/sets/intersection.test.ts 1.82ms
2023/2072 src/utils/sets/intersection.ts 2.27ms
2024/2072 src/utils/sets/union.test.ts 1.86ms
2025/2072 src/utils/sets/union.ts 1.56ms
2026/2072 src/utils/take/classes/TakeChain.ts 1.86ms
2027/2072 src/utils/take/interfaces/ITakeChain.ts 1.93ms
2028/2072 src/utils/take/interfaces/Takeable.ts 1.56ms
2029/2072 src/utils/take/take.test.ts 3.43ms
2030/2072 src/utils/take/take.ts 1.97ms
2031/2072 src/utils/toolCalls/getToolCallIdentity.ts 1.79ms
2032/2072 src/utils/toolCalls/mergeToolCalls.ts 6.12ms
2033/2072 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 2.82ms
2034/2072 src/utils/validators/email/isValidEmail.test.ts 1.53ms
2035/2072 src/utils/validators/email/isValidEmail.ts 1.46ms
2036/2072 src/utils/validators/filePath/isRootPath.test.ts 1.89ms
2037/2072 src/utils/validators/filePath/isRootPath.ts 1.71ms
2038/2072 src/utils/validators/filePath/isValidFilePath.test.ts 4.41ms
2039/2072 src/utils/validators/filePath/isValidFilePath.ts 3.11ms
2040/2072 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 1.72ms
2041/2072 src/utils/validators/javascriptName/isValidJavascriptName.ts 1.86ms
2042/2072 src/utils/validators/parameterName/validateParameterName.test.ts 2.68ms
2043/2072 src/utils/validators/parameterName/validateParameterName.ts 3.42ms
2044/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 1.73ms
2045/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 2.10ms
2046/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 1.69ms
2047/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 1.80ms
2048/2072 src/utils/validators/url/extractUrlsFromText.test.ts 1.96ms
2049/2072 src/utils/validators/url/extractUrlsFromText.ts 3.59ms
2050/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 1.80ms
2051/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 3.40ms
2052/2072 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 1.56ms
2053/2072 src/utils/validators/url/isUrlOnPrivateNetwork.ts 2.08ms
2054/2072 src/utils/validators/url/isValidAgentUrl.test.ts 2.46ms
2055/2072 src/utils/validators/url/isValidAgentUrl.ts 2.52ms
2056/2072 src/utils/validators/url/isValidPipelineUrl.test.ts 1.92ms
2057/2072 src/utils/validators/url/isValidPipelineUrl.ts 2.13ms
2058/2072 src/utils/validators/url/isValidUrl.test.ts 2.11ms
2059/2072 src/utils/validators/url/isValidUrl.ts 2.57ms
2060/2072 src/utils/validators/url/normalizeDomainForMatching.test.ts 1.59ms
2061/2072 src/utils/validators/url/normalizeDomainForMatching.ts 2.55ms
2062/2072 src/utils/validators/uuid/isValidUuid.test.ts 1.97ms
2063/2072 src/utils/validators/uuid/isValidUuid.ts 2.15ms
2064/2072 src/version.ts 2.04ms
2065/2072 src/wizard/$getCompiledBook.ts 8.72ms
2066/2072 src/wizard/test/books/test.book.md 1.32ms
2067/2072 src/wizard/test/README.md 1.23ms
2068/2072 src/wizard/test/sub/books/test.book.md 1.28ms
2069/2072 src/wizard/test/sub/subsub/books/test.book.md 1.27ms
2070/2072 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.23ms
2071/2072 src/wizard/test/sub/subsub/subsubsub/README.md 1.09ms
2072/2072 src/wizard/wizard.ts 7.08ms
CSpell: Files checked: 2072, Issues found: 0 in 0 files.

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm run test-spellcheck' in C:\Users\me\work\ai\promptbook
 succeeded in 17837ms:

> promptbook@0.114.0-33 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/2072 src/_packages/anthropic-claude.index.ts 867.83ms
   2/2072 src/_packages/anthropic-claude.readme.md 75.46ms
   3/2072 src/_packages/azure-openai.index.ts 5.51ms
   4/2072 src/_packages/azure-openai.readme.md 15.99ms
   5/2072 src/_packages/browser.index.ts 6.51ms
   6/2072 src/_packages/browser.readme.md 7.69ms
   7/2072 src/_packages/cli.index.ts 13.24ms
   8/2072 src/_packages/cli.readme.md 19.55ms
   9/2072 src/_packages/color.index.ts 8.40ms
  10/2072 src/_packages/color.readme.md 11.26ms
  11/2072 src/_packages/components.index.ts 17.73ms
  12/2072 src/_packages/components.readme.md 14.02ms
  13/2072 src/_packages/content/$provideExecutionToolsForNode.md 4.35ms
  14/2072 src/_packages/content/providers.md 1.72ms
  15/2072 src/_packages/core.index.ts 35.45ms
  16/2072 src/_packages/core.readme.md 27.80ms
  17/2072 src/_packages/deepseek.index.ts 3.84ms
  18/2072 src/_packages/deepseek.readme.md 1.41ms
  19/2072 src/_packages/documents.index.ts 5.32ms
  20/2072 src/_packages/documents.readme.md 1.98ms
  21/2072 src/_packages/editable.index.ts 9.71ms
  22/2072 src/_packages/editable.readme.md 7.02ms
  23/2072 src/_packages/fake-llm.index.ts 2.51ms
  24/2072 src/_packages/fake-llm.readme.md 1.49ms
  25/2072 src/_packages/google.index.ts 2.73ms
  26/2072 src/_packages/google.readme.md 15.27ms
  27/2072 src/_packages/javascript.index.ts 5.01ms
  28/2072 src/_packages/javascript.readme.md 3.97ms
  29/2072 src/_packages/legacy-documents.index.ts 2.58ms
  30/2072 src/_packages/legacy-documents.readme.md 2.54ms
  31/2072 src/_packages/markdown-utils.index.ts 10.89ms
  32/2072 src/_packages/markdown-utils.readme.md 8.61ms
  33/2072 src/_packages/markitdown.index.ts 3.80ms
  34/2072 src/_packages/markitdown.readme.md 1.54ms
  35/2072 src/_packages/node.index.ts 6.32ms
  36/2072 src/_packages/node.readme.md 7.54ms
  37/2072 src/_packages/ollama.index.ts 2.87ms
  38/2072 src/_packages/ollama.readme.md 5.20ms
  39/2072 src/_packages/openai.index.ts 7.54ms
  40/2072 src/_packages/openai.readme.md 17.64ms
  41/2072 src/_packages/pdf.index.ts 5.58ms
  42/2072 src/_packages/pdf.readme.md 1.65ms
  43/2072 src/_packages/promptbook.readme.md 1.30ms
  44/2072 src/_packages/ptbk.readme.md 1.72ms
  45/2072 src/_packages/README.md 2.05ms
  46/2072 src/_packages/remote-client.index.ts 3.16ms
  47/2072 src/_packages/remote-client.readme.md 1.56ms
  48/2072 src/_packages/remote-server.index.ts 3.42ms
  49/2072 src/_packages/remote-server.readme.md 4.67ms
  50/2072 src/_packages/templates.index.ts 2.86ms
  51/2072 src/_packages/templates.readme.md 6.66ms
  52/2072 src/_packages/types.index.ts 64.51ms
  53/2072 src/_packages/types.readme.md 21.69ms
  54/2072 src/_packages/utils.index.ts 13.30ms
  55/2072 src/_packages/utils.readme.md 18.32ms
  56/2072 src/_packages/vercel.index.ts 3.18ms
  57/2072 src/_packages/vercel.readme.md 6.54ms
  58/2072 src/_packages/website-crawler.index.ts 2.30ms
  59/2072 src/_packages/website-crawler.readme.md 1.27ms
  60/2072 src/_packages/wizard.index.ts 7.33ms
  61/2072 src/_packages/wizard.readme.md 2.43ms
  62/2072 src/avatars/Avatar.tsx 24.12ms
  63/2072 src/avatars/avatarAnimationScheduler.ts 7.04ms
  64/2072 src/avatars/avatarInteractionUtils.test.ts 6.58ms
  65/2072 src/avatars/avatarInteractionUtils.ts 10.24ms
  66/2072 src/avatars/AvatarOrImage.tsx 5.07ms
  67/2072 src/avatars/avatarPointerTracking.ts 9.44ms
  68/2072 src/avatars/avatarRenderingUtils.test.ts 9.64ms
  69/2072 src/avatars/avatarRenderingUtils.ts 19.38ms
  70/2072 src/avatars/avatarVisibilityTracking.ts 4.74ms
  71/2072 src/avatars/index.ts 3.52ms
  72/2072 src/avatars/renderAvatarVisual.ts 4.91ms
  73/2072 src/avatars/renderAvatarVisualAsciiArt.ts 10.04ms
  74/2072 src/avatars/renderAvatarVisualTerminalText.test.ts 5.35ms
  75/2072 src/avatars/renderAvatarVisualTerminalText.ts 12.23ms
  76/2072 src/avatars/types/AvatarDefinition.ts 2.55ms
  77/2072 src/avatars/types/AvatarVisualDefinition.ts 8.70ms
  78/2072 src/avatars/visuals/asciiOctopusAvatarVisual.ts 28.76ms
  79/2072 src/avatars/visuals/avatar3dProjectionShared.ts 10.47ms
  80/2072 src/avatars/visuals/avatarVisualRegistry.test.ts 3.51ms
  81/2072 src/avatars/visuals/avatarVisualRegistry.ts 5.32ms
  82/2072 src/avatars/visuals/fractalAvatarVisual.ts 13.43ms
  83/2072 src/avatars/visuals/minecraft2AvatarVisual.ts 17.26ms
  84/2072 src/avatars/visuals/minecraftAvatarVisual.ts 7.53ms
  85/2072 src/avatars/visuals/minecraftAvatarVisualShared.ts 6.87ms
  86/2072 src/avatars/visuals/octopus2AvatarVisual.ts 11.61ms
  87/2072 src/avatars/visuals/octopus3AvatarVisual.test.ts 4.16ms
  88/2072 src/avatars/visuals/octopus3AvatarVisual.ts 24.29ms
  89/2072 src/avatars/visuals/octopus3d2AvatarVisual.ts 20.98ms
  90/2072 src/avatars/visuals/octopus3d3AvatarVisual.ts 33.97ms
  91/2072 src/avatars/visuals/octopus3d4AvatarVisual.ts 36.17ms
  92/2072 src/avatars/visuals/octopus3dAvatarVisual.ts 22.34ms
  93/2072 src/avatars/visuals/octopus3dAvatarVisualShared.ts 10.21ms
  94/2072 src/avatars/visuals/octopusAvatarVisual.ts 8.66ms
  95/2072 src/avatars/visuals/octopusAvatarVisualShared.test.ts 8.33ms
  96/2072 src/avatars/visuals/octopusAvatarVisualShared.ts 17.66ms
  97/2072 src/avatars/visuals/orbAvatarVisual.test.ts 3.89ms
  98/2072 src/avatars/visuals/orbAvatarVisual.ts 18.24ms
  99/2072 src/avatars/visuals/pixelArtAvatarVisual.ts 6.18ms
 100/2072 src/book-2.0/agent-source/AgentBasicInformation.ts 5.88ms
 101/2072 src/book-2.0/agent-source/AgentModelRequirements.ts 4.70ms
 102/2072 src/book-2.0/agent-source/AgentReferenceResolver.ts 2.76ms
 103/2072 src/book-2.0/agent-source/AgentSourceParseResult.ts 2.64ms
 104/2072 src/book-2.0/agent-source/agentSourceVisibility.test.ts 2.81ms
 105/2072 src/book-2.0/agent-source/agentSourceVisibility.ts 6.54ms
 106/2072 src/book-2.0/agent-source/BookEditable.ts 3.14ms
 107/2072 src/book-2.0/agent-source/communication-samples.test.ts 4.22ms
 108/2072 src/book-2.0/agent-source/computeAgentHash.test.ts 1.82ms
 109/2072 src/book-2.0/agent-source/computeAgentHash.ts 1.81ms
 110/2072 src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts 3.68ms
 111/2072 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 7.41ms
 112/2072 src/book-2.0/agent-source/createAgentModelRequirements.ts 8.22ms
 113/2072 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 3.74ms
 114/2072 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 4.07ms
 115/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 9.93ms
 116/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 4.20ms
 117/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 3.48ms
 118/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 4.48ms
 119/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 2.87ms
 120/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 3.51ms
 121/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 5.87ms
 122/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts 3.16ms
 123/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts 9.51ms
 124/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts 16.88ms
 125/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements.ts 5.53ms
 126/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources.ts 6.18ms
 127/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/ParsedAgentSourceWithCommitments.ts 2.03ms
 128/2072 src/book-2.0/agent-source/createCommitmentRegex.ts 3.11ms
 129/2072 src/book-2.0/agent-source/createDefaultAgentName.ts 2.46ms
 130/2072 src/book-2.0/agent-source/createTeamToolName.ts 2.70ms
 131/2072 src/book-2.0/agent-source/extractMetaLinks.ts 2.55ms
 132/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 3.43ms
 133/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 2.46ms
 134/2072 src/book-2.0/agent-source/getBookCommitmentLineType.ts 2.88ms
 135/2072 src/book-2.0/agent-source/normalizeAgentName.test.ts 2.93ms
 136/2072 src/book-2.0/agent-source/normalizeAgentName.ts 2.01ms
 137/2072 src/book-2.0/agent-source/padBook.ts 2.67ms
 138/2072 src/book-2.0/agent-source/parseAgentSource.import.test.ts 3.81ms
 139/2072 src/book-2.0/agent-source/parseAgentSource.test.ts 14.35ms
 140/2072 src/book-2.0/agent-source/parseAgentSource.ts 4.67ms
 141/2072 src/book-2.0/agent-source/parseAgentSource/applyMetaCommitment.ts 6.86ms
 142/2072 src/book-2.0/agent-source/parseAgentSource/consumeConversationSampleCommitment.ts 2.41ms
 143/2072 src/book-2.0/agent-source/parseAgentSource/createCapabilitiesFromCommitment.ts 9.49ms
 144/2072 src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts 1.99ms
 145/2072 src/book-2.0/agent-source/parseAgentSource/extractAgentProfileText.ts 3.57ms
 146/2072 src/book-2.0/agent-source/parseAgentSource/extractInitialMessage.ts 2.38ms
 147/2072 src/book-2.0/agent-source/parseAgentSource/extractParsedAgentProfile.ts 4.03ms
 148/2072 src/book-2.0/agent-source/parseAgentSource/ParseAgentSourceState.ts 1.93ms
 149/2072 src/book-2.0/agent-source/parseAgentSource/ParsedAgentProfile.ts 1.86ms
 150/2072 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 3.00ms
 151/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 2.90ms
 152/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 3.94ms
 153/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 7.71ms
 154/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts 6.65ms
 155/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts 3.63ms
 156/2072 src/book-2.0/agent-source/ParsedUnknownCommitment.ts 2.90ms
 157/2072 src/book-2.0/agent-source/parseParameters.ts 5.55ms
 158/2072 src/book-2.0/agent-source/parseTeamCommitment.ts 14.19ms
 159/2072 src/book-2.0/agent-source/pseudoAgentReferences.ts 19.34ms
 160/2072 src/book-2.0/agent-source/README.md 8.87ms
 161/2072 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 3.24ms
 162/2072 src/book-2.0/agent-source/string_book.ts 3.61ms
 163/2072 src/book-2.0/agent-source/TeammateProfileResolver.ts 3.18ms
 164/2072 src/book-2.0/book-language-documentation/BookLanguageDocumentationExample.ts 2.28ms
 165/2072 src/book-2.0/book-language-documentation/bookLanguageDocumentationExamples.ts 5.41ms
 166/2072 src/book-2.0/book-language-documentation/BookLanguageManualDictionary.ts 8.95ms
 167/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts 8.42ms
 168/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.ts 24.60ms
 169/2072 src/book-2.0/book-language-documentation/englishBookLanguageManualDictionary.ts 19.24ms
 170/2072 src/book-2.0/book-language-documentation/getBookLanguageManualCommitmentGroups.ts 6.30ms
 171/2072 src/book-2.0/book-language-documentation/getBookLanguageManualDictionary.ts 6.48ms
 172/2072 src/book-2.0/book-language-documentation/getSafeCodeBlock.ts 2.24ms
 173/2072 src/book-2.0/book-language-documentation/renderCommitmentCatalogSection.ts 4.74ms
 174/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts 4.45ms
 175/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.ts 12.80ms
 176/2072 src/book-2.0/book-language-documentation/toStableAnchorId.ts 2.99ms
 177/2072 src/book-2.0/README.md 1.19ms
 178/2072 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 2.10ms
 179/2072 src/book-3.0/agentFolderPaths.ts 2.28ms
 180/2072 src/book-3.0/AgentMessageRunReport.test.ts 4.16ms
 181/2072 src/book-3.0/AgentMessageRunReport.ts 10.44ms
 182/2072 src/book-3.0/AgentPlannedMessagesSidecar.test.ts 5.49ms
 183/2072 src/book-3.0/AgentPlannedMessagesSidecar.ts 11.74ms
 184/2072 src/book-3.0/AgentTeamConversationWorkspace.ts 6.85ms
 185/2072 src/book-3.0/Book.test.ts 3.85ms
 186/2072 src/book-3.0/Book.ts 7.42ms
 187/2072 src/book-3.0/BookNodeAgentSource.ts 6.34ms
 188/2072 src/book-3.0/CliAgent.test.ts 5.65ms
 189/2072 src/book-3.0/CliAgent.ts 9.16ms
 190/2072 src/book-3.0/cliAgentEnv.ts 3.19ms
 191/2072 src/book-3.0/codexLoginMethod.ts 3.36ms
 192/2072 src/book-3.0/createAgentMessageSidecarBaseName.ts 2.42ms
 193/2072 src/book-3.0/describeAgentPlannedMessageSchedule.ts 5.02ms
 194/2072 src/book-3.0/LiteAgent.test.ts 5.05ms
 195/2072 src/book-3.0/LiteAgent.ts 15.04ms
 196/2072 src/book-components/_common/Dropdown/Dropdown.tsx 4.02ms
 197/2072 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 3.09ms
 198/2072 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 3.12ms
 199/2072 src/book-components/_common/Modal/Modal.tsx 2.57ms
 200/2072 src/book-components/_common/MonacoEditorWithShadowDom.tsx 3.40ms
 201/2072 src/book-components/_common/react-utils/classNames.ts 1.65ms
 202/2072 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 2.94ms
 203/2072 src/book-components/_common/react-utils/escapeRegex.tsx 1.64ms
 204/2072 src/book-components/_common/Tooltip/Tooltip.tsx 7.11ms
 205/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 3.90ms
 206/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 3.06ms
 207/2072 src/book-components/AvatarProfile/AvatarChip/index.ts 1.49ms
 208/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 4.70ms
 209/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 2.53ms
 210/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 3.24ms
 211/2072 src/book-components/AvatarProfile/README.md 1.45ms
 212/2072 src/book-components/BookEditor/BookEditor.tsx 14.87ms
 213/2072 src/book-components/BookEditor/BookEditorAboutPromptbookInformation.tsx 4.02ms
 214/2072 src/book-components/BookEditor/BookEditorActionbar.tsx 10.53ms
 215/2072 src/book-components/BookEditor/BookEditorBrowserConfig.ts 1.65ms
 216/2072 src/book-components/BookEditor/BookEditorForClient.tsx 3.44ms
 217/2072 src/book-components/BookEditor/BookEditorMonaco.tsx 18.30ms
 218/2072 src/book-components/BookEditor/BookEditorMonacoConstants.ts 1.88ms
 219/2072 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 2.78ms
 220/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 2.78ms
 221/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 10.59ms
 222/2072 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 7.22ms
 223/2072 src/book-components/BookEditor/BookEditorSyntaxTokenStyles.ts 3.67ms
 224/2072 src/book-components/BookEditor/BookEditorTheme.ts 2.24ms
 225/2072 src/book-components/BookEditor/createBookSourceHighlightStyles.ts 3.15ms
 226/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.browser.ts 1.66ms
 227/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 4.98ms
 228/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 4.79ms
 229/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts 3.89ms
 230/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.ts 4.63ms
 231/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts 3.44ms
 232/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.ts 3.34ms
 233/2072 src/book-components/BookEditor/highlightBookSourceToHtml.ts 7.07ms
 234/2072 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 5.78ms
 235/2072 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 3.68ms
 236/2072 src/book-components/BookEditor/useBookEditorMonacoInteractions.ts 11.33ms
 237/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 13.91ms
 238/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 18.10ms
 239/2072 src/book-components/BookEditor/useBookEditorMonacoLifecycle.ts 7.92ms
 240/2072 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 5.33ms
 241/2072 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 6.45ms
 242/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/bookEditorMonacoUploadTypes.ts 6.26ms
 243/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/clearScheduledTimer.ts 2.55ms
 244/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/enqueueFilesForUpload.ts 8.93ms
 245/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadEditorSync.ts 6.58ms
 246/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadItemsState.ts 5.64ms
 247/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadProgressQueue.ts 4.40ms
 248/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadQueue.ts 11.57ms
 249/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useCompletedUploadsAutoClear.ts 3.30ms
 250/2072 src/book-components/Chat/AgentChat/AgentChat.test.tsx 9.35ms
 251/2072 src/book-components/Chat/AgentChat/AgentChat.tsx 9.77ms
 252/2072 src/book-components/Chat/AgentChat/AgentChatProps.tsx 3.54ms
 253/2072 src/book-components/Chat/AgentChip/AgentChip.tsx 7.26ms
 254/2072 src/book-components/Chat/AgentChip/index.ts 1.31ms
 255/2072 src/book-components/Chat/Chat/Chat.tsx 21.89ms
 256/2072 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 5.87ms
 257/2072 src/book-components/Chat/Chat/ChatActionsBar.tsx 10.40ms
 258/2072 src/book-components/Chat/Chat/ChatCitationModal.test.tsx 4.22ms
 259/2072 src/book-components/Chat/Chat/ChatCitationModal.tsx 9.55ms
 260/2072 src/book-components/Chat/Chat/chatCssClassNames.ts 5.41ms
 261/2072 src/book-components/Chat/Chat/ChatFeedbackStar.tsx 4.50ms
 262/2072 src/book-components/Chat/Chat/ChatImageAttachmentModal.tsx 4.79ms
 263/2072 src/book-components/Chat/Chat/ChatInputArea.test.tsx 9.32ms
 264/2072 src/book-components/Chat/Chat/ChatInputArea.tsx 22.85ms
 265/2072 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 7.93ms
 266/2072 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 1.48ms
 267/2072 src/book-components/Chat/Chat/ChatMessageAttachments.tsx 7.02ms
 268/2072 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 18.86ms
 269/2072 src/book-components/Chat/Chat/ChatMessageItem.tsx 47.44ms
 270/2072 src/book-components/Chat/Chat/ChatMessageList.tsx 9.38ms
 271/2072 src/book-components/Chat/Chat/ChatMessageMap.tsx 14.91ms
 272/2072 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 7.21ms
 273/2072 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 5.96ms
 274/2072 src/book-components/Chat/Chat/ChatProps.tsx 32.63ms
 275/2072 src/book-components/Chat/Chat/ChatRatingModal.test.tsx 6.03ms
 276/2072 src/book-components/Chat/Chat/ChatRatingModal.tsx 14.77ms
 277/2072 src/book-components/Chat/Chat/ChatReplyPreview.tsx 3.68ms
 278/2072 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 5.37ms
 279/2072 src/book-components/Chat/Chat/ChatSoundToggle.tsx 10.37ms
 280/2072 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 11.53ms
 281/2072 src/book-components/Chat/Chat/ChatToolCallModal.tsx 11.30ms
 282/2072 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 5.55ms
 283/2072 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 5.93ms
 284/2072 src/book-components/Chat/Chat/CitationIframePreview.test.tsx 4.26ms
 285/2072 src/book-components/Chat/Chat/CitationIframePreview.tsx 5.11ms
 286/2072 src/book-components/Chat/Chat/ClockIcon.tsx 2.79ms
 287/2072 src/book-components/Chat/Chat/constants.tsx 1.61ms
 288/2072 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 11.49ms
 289/2072 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 4.68ms
 290/2072 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 5.39ms
 291/2072 src/book-components/Chat/Chat/insertDictationChunk.test.ts 2.43ms
 292/2072 src/book-components/Chat/Chat/insertDictationChunk.ts 2.88ms
 293/2072 src/book-components/Chat/Chat/learnDictationDictionary.test.ts 1.94ms
 294/2072 src/book-components/Chat/Chat/learnDictationDictionary.ts 2.95ms
 295/2072 src/book-components/Chat/Chat/pagePreview/createPagePreviewSessionId.ts 2.99ms
 296/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx 8.65ms
 297/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.tsx 20.43ms
 298/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreviewToolbar.tsx 6.26ms
 299/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts 3.46ms
 300/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.ts 6.52ms
 301/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputEvent.ts 4.25ms
 302/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts 4.42ms
 303/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.ts 5.71ms
 304/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewSessionState.ts 2.00ms
 305/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewViewport.ts 2.75ms
 306/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts 2.57ms
 307/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 20.21ms
 308/2072 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 21.65ms
 309/2072 src/book-components/Chat/Chat/renderAgentProjectDiff.tsx 4.88ms
 310/2072 src/book-components/Chat/Chat/renderAgentProjectToolCallDetails.tsx 12.32ms
 311/2072 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 4.91ms
 312/2072 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 14.77ms
 313/2072 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 6.47ms
 314/2072 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 29.99ms
 315/2072 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 7.33ms
 316/2072 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 9.53ms
 317/2072 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 22.48ms
 318/2072 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 7.17ms
 319/2072 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 3.76ms
 320/2072 src/book-components/Chat/Chat/renderToolCallDetails.tsx 19.09ms
 321/2072 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 4.16ms
 322/2072 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 7.48ms
 323/2072 src/book-components/Chat/Chat/resolveChatRatingMessageKey.ts 3.48ms
 324/2072 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 22.07ms
 325/2072 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 3.65ms
 326/2072 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 3.32ms
 327/2072 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 4.80ms
 328/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx 6.92ms
 329/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 19.47ms
 330/2072 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 7.08ms
 331/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx 4.60ms
 332/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 19.39ms
 333/2072 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 27.66ms
 334/2072 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 5.29ms
 335/2072 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 5.61ms
 336/2072 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 4.53ms
 337/2072 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 6.60ms
 338/2072 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 3.82ms
 339/2072 src/book-components/Chat/Chat/useChatScrollState.ts 9.98ms
 340/2072 src/book-components/Chat/Chat/useChatToolCallModalState.ts 16.13ms
 341/2072 src/book-components/Chat/Chat/useChatToolCallState.ts 7.27ms
 342/2072 src/book-components/Chat/CodeBlock/CodeBlock.tsx 8.40ms
 343/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 1.99ms
 344/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 2.68ms
 345/2072 src/book-components/Chat/effects/ChatEffectsSystem.tsx 4.59ms
 346/2072 src/book-components/Chat/effects/components/ConfettiEffect.tsx 4.41ms
 347/2072 src/book-components/Chat/effects/components/HeartsEffect.tsx 3.56ms
 348/2072 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 1.95ms
 349/2072 src/book-components/Chat/effects/index.ts 2.62ms
 350/2072 src/book-components/Chat/effects/types/ChatEffect.ts 2.10ms
 351/2072 src/book-components/Chat/effects/types/ChatEffectConfig.ts 2.46ms
 352/2072 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 2.76ms
 353/2072 src/book-components/Chat/effects/types/ChatEffectType.ts 1.42ms
 354/2072 src/book-components/Chat/effects/utils/detectEffects.ts 2.86ms
 355/2072 src/book-components/Chat/hooks/index.ts 1.34ms
 356/2072 src/book-components/Chat/hooks/useChatActionsOverlap.ts 6.78ms
 357/2072 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 6.59ms
 358/2072 src/book-components/Chat/hooks/useChatAutoScroll.ts 17.60ms
 359/2072 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 8.57ms
 360/2072 src/book-components/Chat/hooks/useChatCompleteNotification.ts 6.68ms
 361/2072 src/book-components/Chat/hooks/useChatRatings.ts 14.34ms
 362/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx 4.22ms
 363/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.ts 6.02ms
 364/2072 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 3.08ms
 365/2072 src/book-components/Chat/LlmChat/defaults.ts 1.72ms
 366/2072 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 2.15ms
 367/2072 src/book-components/Chat/LlmChat/LlmChat.test.tsx 11.93ms
 368/2072 src/book-components/Chat/LlmChat/LlmChat.tsx 4.85ms
 369/2072 src/book-components/Chat/LlmChat/LlmChatProps.tsx 7.86ms
 370/2072 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 19.16ms
 371/2072 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 4.31ms
 372/2072 src/book-components/Chat/LlmChat/useLlmChatState.ts 11.89ms
 373/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 9.68ms
 374/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 14.11ms
 375/2072 src/book-components/Chat/MockedChat/constants.ts 3.48ms
 376/2072 src/book-components/Chat/MockedChat/MockedChat.tsx 20.90ms
 377/2072 src/book-components/Chat/README.md 5.62ms
 378/2072 src/book-components/Chat/save/_common/chatExportRendering.ts 8.60ms
 379/2072 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 6.31ms
 380/2072 src/book-components/Chat/save/_common/ChatSaveFormatHandler.ts 2.47ms
 381/2072 src/book-components/Chat/save/_common/createChatExportFilename.ts 4.48ms
 382/2072 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 2.10ms
 383/2072 src/book-components/Chat/save/_common/getPromptbookExportBranding.ts 3.44ms
 384/2072 src/book-components/Chat/save/_common/string_chat_format_name.ts 1.46ms
 385/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts 5.81ms
 386/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 20.22ms
 387/2072 src/book-components/Chat/save/index.ts 3.19ms
 388/2072 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 1.94ms
 389/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts 3.54ms
 390/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 6.26ms
 391/2072 src/book-components/Chat/save/pdf/buildChatPdf.ts 11.48ms
 392/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts 9.76ms
 393/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 2.48ms
 394/2072 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 4.28ms
 395/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts 2.66ms
 396/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 4.16ms
 397/2072 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.32ms
 398/2072 src/book-components/Chat/SourceChip/index.ts 1.40ms
 399/2072 src/book-components/Chat/SourceChip/SourceChip.test.tsx 2.73ms
 400/2072 src/book-components/Chat/SourceChip/SourceChip.tsx 7.40ms
 401/2072 src/book-components/Chat/types/ChatMessage.ts 9.92ms
 402/2072 src/book-components/Chat/types/ChatParticipant.ts 3.70ms
 403/2072 src/book-components/Chat/types/CitationLabelResolver.ts 2.40ms
 404/2072 src/book-components/Chat/utils/$provideServerDomWindow.ts 5.10ms
 405/2072 src/book-components/Chat/utils/addUtmParamsToUrl.ts 1.85ms
 406/2072 src/book-components/Chat/utils/agentProjectToolCall.test.ts 3.56ms
 407/2072 src/book-components/Chat/utils/agentProjectToolCall.ts 6.00ms
 408/2072 src/book-components/Chat/utils/ChatPersistence.test.ts 4.00ms
 409/2072 src/book-components/Chat/utils/ChatPersistence.ts 5.38ms
 410/2072 src/book-components/Chat/utils/citationHelpers.test.ts 5.41ms
 411/2072 src/book-components/Chat/utils/citationHelpers.ts 4.42ms
 412/2072 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 6.25ms
 413/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 2.65ms
 414/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 8.29ms
 415/2072 src/book-components/Chat/utils/createShortLinkForChat.ts 1.73ms
 416/2072 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 1.86ms
 417/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts 2.23ms
 418/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.ts 6.78ms
 419/2072 src/book-components/Chat/utils/downloadFile.ts 2.01ms
 420/2072 src/book-components/Chat/utils/exportChatHistory.ts 4.07ms
 421/2072 src/book-components/Chat/utils/ExportFormat.ts 1.32ms
 422/2072 src/book-components/Chat/utils/externalSourceToolCall.ts 4.24ms
 423/2072 src/book-components/Chat/utils/formatToolCallDateTime.ts 4.26ms
 424/2072 src/book-components/Chat/utils/formatToolCallLocalTime.ts 1.75ms
 425/2072 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 2.13ms
 426/2072 src/book-components/Chat/utils/generatePdfContent.ts 2.82ms
 427/2072 src/book-components/Chat/utils/generateQrDataUrl.ts 1.80ms
 428/2072 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 4.03ms
 429/2072 src/book-components/Chat/utils/getPromptbookBranding.ts 2.27ms
 430/2072 src/book-components/Chat/utils/getToolCallChipletInfo.test.ts 3.35ms
 431/2072 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 3.63ms
 432/2072 src/book-components/Chat/utils/getToolCallChipletInfo.ts 19.56ms
 433/2072 src/book-components/Chat/utils/isVisibleChatToolCall.ts 2.20ms
 434/2072 src/book-components/Chat/utils/loadAgentProfile.ts 7.22ms
 435/2072 src/book-components/Chat/utils/messagesToHtml.ts 5.92ms
 436/2072 src/book-components/Chat/utils/messagesToJson.ts 2.73ms
 437/2072 src/book-components/Chat/utils/messagesToMarkdown.ts 3.01ms
 438/2072 src/book-components/Chat/utils/messagesToText.ts 2.64ms
 439/2072 src/book-components/Chat/utils/parseCitationMarker.ts 4.88ms
 440/2072 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 2.84ms
 441/2072 src/book-components/Chat/utils/parseCitationsFromContent.ts 5.77ms
 442/2072 src/book-components/Chat/utils/parseImagePrompts.test.ts 2.49ms
 443/2072 src/book-components/Chat/utils/parseImagePrompts.ts 3.62ms
 444/2072 src/book-components/Chat/utils/parseMessageButtons.test.ts 2.67ms
 445/2072 src/book-components/Chat/utils/parseMessageButtons.ts 3.61ms
 446/2072 src/book-components/Chat/utils/renderMarkdown.test.ts 4.60ms
 447/2072 src/book-components/Chat/utils/renderMarkdown.ts 39.69ms
 448/2072 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 4.82ms
 449/2072 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 2.43ms
 450/2072 src/book-components/Chat/utils/resolveCitationSourceDisplay.ts 21.81ms
 451/2072 src/book-components/Chat/utils/resolveCitationUrl.test.ts 2.45ms
 452/2072 src/book-components/Chat/utils/resolveCitationUrl.ts 6.57ms
 453/2072 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 6.08ms
 454/2072 src/book-components/Chat/utils/resolveToolCallState.ts 3.53ms
 455/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 4.37ms
 456/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 8.27ms
 457/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 8.72ms
 458/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 10.09ms
 459/2072 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 3.20ms
 460/2072 src/book-components/Chat/utils/thinkingMessageVariants.ts 4.87ms
 461/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 5.51ms
 462/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 35.16ms
 463/2072 src/book-components/Chat/utils/toolCallParsing.test.ts 5.67ms
 464/2072 src/book-components/Chat/utils/toolCallParsing.ts 4.06ms
 465/2072 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 6.46ms
 466/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 3.52ms
 467/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 2.61ms
 468/2072 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 14.61ms
 469/2072 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 2.46ms
 470/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 2.37ms
 471/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 2.41ms
 472/2072 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 2.77ms
 473/2072 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 3.73ms
 474/2072 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 2.44ms
 475/2072 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 9.35ms
 476/2072 src/book-components/Chat/utils/walletCredentialToolCall.ts 22.83ms
 477/2072 src/book-components/fonts/README.md 1.40ms
 478/2072 src/book-components/icons/AboutIcon.tsx 2.58ms
 479/2072 src/book-components/icons/ArrowIcon.tsx 4.58ms
 480/2072 src/book-components/icons/AttachmentIcon.tsx 2.56ms
 481/2072 src/book-components/icons/CameraIcon.tsx 2.78ms
 482/2072 src/book-components/icons/CloseIcon.tsx 2.48ms
 483/2072 src/book-components/icons/DownloadIcon.tsx 2.81ms
 484/2072 src/book-components/icons/EmailIcon.tsx 6.49ms
 485/2072 src/book-components/icons/ExitFullscreenIcon.tsx 3.24ms
 486/2072 src/book-components/icons/FullscreenIcon.tsx 2.93ms
 487/2072 src/book-components/icons/MenuIcon.tsx 2.87ms
 488/2072 src/book-components/icons/MicIcon.tsx 2.04ms
 489/2072 src/book-components/icons/PauseIcon.tsx 1.91ms
 490/2072 src/book-components/icons/PlayIcon.tsx 1.74ms
 491/2072 src/book-components/icons/ResetIcon.tsx 2.34ms
 492/2072 src/book-components/icons/SaveIcon.tsx 2.94ms
 493/2072 src/book-components/icons/SendIcon.tsx 2.06ms
 494/2072 src/book-components/icons/SolidArrowButton.tsx 3.35ms
 495/2072 src/book-components/icons/StopIcon.tsx 2.08ms
 496/2072 src/book-components/icons/TeacherIcon.tsx 2.72ms
 497/2072 src/book-components/icons/TemplateIcon.tsx 2.15ms
 498/2072 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 5.98ms
 499/2072 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 31.29ms
 500/2072 src/book-components/Qr/BrandedQrCode.tsx 2.43ms
 501/2072 src/book-components/Qr/GenericQrCode.tsx 1.91ms
 502/2072 src/book-components/Qr/PromptbookQrCode.tsx 13.39ms
 503/2072 src/book-components/Qr/useQrCode.ts 3.19ms
 504/2072 src/cli/$runPromptbookCli.ts 2.31ms
 505/2072 src/cli/cli-commands/_boilerplate.ts 2.84ms
 506/2072 src/cli/cli-commands/about.ts 4.74ms
 507/2072 src/cli/cli-commands/agent-folder.ts 3.80ms
 508/2072 src/cli/cli-commands/agent-folder/agentProjectPaths.ts 4.82ms
 509/2072 src/cli/cli-commands/agent-folder/agentRunCliOptions.ts 4.29ms
 510/2072 src/cli/cli-commands/agent-folder/init.ts 5.07ms
 511/2072 src/cli/cli-commands/agent-folder/initializeAgentProjectConfiguration.ts 5.10ms
 512/2072 src/cli/cli-commands/agent-folder/initializeAgentRunnerCommand.ts 6.08ms
 513/2072 src/cli/cli-commands/agent-folder/printAgentInitializationSummary.ts 4.91ms
 514/2072 src/cli/cli-commands/agent-folder/run.test.ts 9.79ms
 515/2072 src/cli/cli-commands/agent-folder/run.ts 4.71ms
 516/2072 src/cli/cli-commands/agent-folder/runMultiple.ts 4.29ms
 517/2072 src/cli/cli-commands/agent-folder/tick.ts 6.04ms
 518/2072 src/cli/cli-commands/agent.ts 3.51ms
 519/2072 src/cli/cli-commands/agent/agentCliOptions.ts 5.10ms
 520/2072 src/cli/cli-commands/agent/chat.ts 5.25ms
 521/2072 src/cli/cli-commands/agent/exec.ts 5.57ms
 522/2072 src/cli/cli-commands/agent/run.test.ts 7.44ms
 523/2072 src/cli/cli-commands/agents-server.ts 3.81ms
 524/2072 src/cli/cli-commands/agents-server/buildAgentsServer.test.ts 22.49ms
 525/2072 src/cli/cli-commands/agents-server/buildAgentsServer.ts 5.44ms
 526/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_FILENAME.ts 1.82ms
 527/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_VERSION.ts 1.55ms
 528/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts 1.99ms
 529/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCache.ts 2.00ms
 530/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCacheOptions.ts 2.16ms
 531/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerBuildSourceFingerprint.ts 5.55ms
 532/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerRuntimeEnvironment.ts 4.54ms
 533/2072 src/cli/cli-commands/agents-server/buildAgentsServer/ensureAgentsServerBuild.ts 4.52ms
 534/2072 src/cli/cli-commands/agents-server/buildAgentsServer/getAgentsServerBuildInputRelativePaths.ts 3.23ms
 535/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPath.ts 4.94ms
 536/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPathMaterialized.ts 3.10ms
 537/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildCacheCurrent.ts 5.06ms
 538/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildInputTestFile.ts 2.93ms
 539/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputDirectoryName.ts 2.20ms
 540/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputPath.ts 2.83ms
 541/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerRuntimeSourcePath.ts 2.87ms
 542/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isFile.ts 2.04ms
 543/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isPathInsideNodeModules.ts 2.09ms
 544/2072 src/cli/cli-commands/agents-server/buildAgentsServer/NODE_MODULES_DIRECTORY_NAME.ts 1.31ms
 545/2072 src/cli/cli-commands/agents-server/buildAgentsServer/normalizeAgentsServerBuildInputPath.ts 1.66ms
 546/2072 src/cli/cli-commands/agents-server/buildAgentsServer/prepareAgentsServerRuntime.ts 3.02ms
 547/2072 src/cli/cli-commands/agents-server/buildAgentsServer/PreparedAgentsServerRuntime.ts 2.07ms
 548/2072 src/cli/cli-commands/agents-server/buildAgentsServer/readAgentsServerBuildCache.ts 2.44ms
 549/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerAppPath.ts 2.08ms
 550/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildAppPath.ts 5.20ms
 551/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildOutputPath.ts 2.35ms
 552/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNextCliPath.ts 2.10ms
 553/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNodeModulesPath.ts 2.58ms
 554/2072 src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts 7.78ms
 555/2072 src/cli/cli-commands/agents-server/buildAgentsServer/shouldCopyAgentsServerRuntimePath.ts 3.35ms
 556/2072 src/cli/cli-commands/agents-server/buildAgentsServer/synchronizeMaterializedAgentsServerRuntime.ts 7.03ms
 557/2072 src/cli/cli-commands/agents-server/buildAgentsServer/writeAgentsServerBuildCache.ts 2.69ms
 558/2072 src/cli/cli-commands/agents-server/ensureAgentsServerEnvFile.ts 6.68ms
 559/2072 src/cli/cli-commands/agents-server/ensureAgentsServerGitignoreFile.ts 3.18ms
 560/2072 src/cli/cli-commands/agents-server/init.test.ts 8.31ms
 561/2072 src/cli/cli-commands/agents-server/init.ts 3.56ms
 562/2072 src/cli/cli-commands/agents-server/initializeAgentsServerProjectConfiguration.ts 3.33ms
 563/2072 src/cli/cli-commands/agents-server/printAgentsServerInitializationSummary.ts 3.64ms
 564/2072 src/cli/cli-commands/agents-server/run.test.ts 8.45ms
 565/2072 src/cli/cli-commands/agents-server/run.ts 9.54ms
 566/2072 src/cli/cli-commands/agents-server/startAgentsServer.ts 8.54ms
 567/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts 3.06ms
 568/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.ts 4.01ms
 569/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerLogStreams.ts 3.67ms
 570/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerRuntimePaths.ts 4.22ms
 571/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerSupervisorState.ts 3.57ms
 572/2072 src/cli/cli-commands/agents-server/startAgentsServer/createInternalRouteErrorMessage.ts 2.82ms
 573/2072 src/cli/cli-commands/agents-server/startAgentsServer/createLocalAgentRunOptions.ts 3.34ms
 574/2072 src/cli/cli-commands/agents-server/startAgentsServer/forwardChildOutput.ts 2.62ms
 575/2072 src/cli/cli-commands/agents-server/startAgentsServer/loadAgentsServerProjectEnvironment.ts 1.94ms
 576/2072 src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts 1.71ms
 577/2072 src/cli/cli-commands/agents-server/startAgentsServer/prepareAgentsServerLaunch.ts 5.98ms
 578/2072 src/cli/cli-commands/agents-server/startAgentsServer/PreparedAgentsServerLaunch.ts 2.46ms
 579/2072 src/cli/cli-commands/agents-server/startAgentsServer/readInternalRouteErrorDetails.ts 2.80ms
 580/2072 src/cli/cli-commands/agents-server/startAgentsServer/resolveAgentsServerChildHostname.ts 2.25ms
 581/2072 src/cli/cli-commands/agents-server/startAgentsServer/StartAgentsServerOptions.ts 3.28ms
 582/2072 src/cli/cli-commands/agents-server/startAgentsServer/startNextServer.ts 5.03ms
 583/2072 src/cli/cli-commands/agents-server/startAgentsServer/startUserChatJobWorkerPump.ts 6.29ms
 584/2072 src/cli/cli-commands/agents-server/startAgentsServer/stopChildProcess.ts 2.22ms
 585/2072 src/cli/cli-commands/agents-server/startAgentsServer/waitForLocalAgentRunnerLimits.ts 5.46ms
 586/2072 src/cli/cli-commands/coder.ts 4.75ms
 587/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts 4.44ms
 588/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.ts 4.11ms
 589/2072 src/cli/cli-commands/coder/add.test.ts 7.21ms
 590/2072 src/cli/cli-commands/coder/add.ts 14.45ms
 591/2072 src/cli/cli-commands/coder/agentCliOptions.ts 2.33ms
 592/2072 src/cli/cli-commands/coder/agentsFile.ts 2.40ms
 593/2072 src/cli/cli-commands/coder/boilerplateCount.test.ts 2.64ms
 594/2072 src/cli/cli-commands/coder/boilerplateCount.ts 4.88ms
 595/2072 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 13.07ms
 596/2072 src/cli/cli-commands/coder/boilerplateTemplates.ts 10.16ms
 597/2072 src/cli/cli-commands/coder/coderReferencedArtifacts.ts 6.10ms
 598/2072 src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts 3.85ms
 599/2072 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 4.26ms
 600/2072 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 4.89ms
 601/2072 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 2.96ms
 602/2072 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 3.45ms
 603/2072 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 3.86ms
 604/2072 src/cli/cli-commands/coder/ensureDirectory.ts 3.76ms
 605/2072 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 3.06ms
 606/2072 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 5.07ms
 607/2072 src/cli/cli-commands/coder/find-refactor-candidates.ts 4.62ms
 608/2072 src/cli/cli-commands/coder/find-unwritten.ts 4.06ms
 609/2072 src/cli/cli-commands/coder/formatDisplayPath.ts 1.93ms
 610/2072 src/cli/cli-commands/coder/generate-boilerplates.test.ts 4.87ms
 611/2072 src/cli/cli-commands/coder/generate-boilerplates.ts 10.59ms
 612/2072 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 5.90ms
 613/2072 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 2.77ms
 614/2072 src/cli/cli-commands/coder/getTypescriptModule.test.ts 2.50ms
 615/2072 src/cli/cli-commands/coder/getTypescriptModule.ts 2.79ms
 616/2072 src/cli/cli-commands/coder/GitChangesMode.ts 2.70ms
 617/2072 src/cli/cli-commands/coder/init.test.ts 6.20ms
 618/2072 src/cli/cli-commands/coder/init.ts 10.21ms
 619/2072 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 5.75ms
 620/2072 src/cli/cli-commands/coder/list.test.ts 6.73ms
 621/2072 src/cli/cli-commands/coder/list.ts 8.15ms
 622/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 5.75ms
 623/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 8.05ms
 624/2072 src/cli/cli-commands/coder/ping.test.ts 7.99ms
 625/2072 src/cli/cli-commands/coder/ping.ts 7.28ms
 626/2072 src/cli/cli-commands/coder/printCoderRunFailure.ts 3.54ms
 627/2072 src/cli/cli-commands/coder/printInitializationSummary.ts 5.18ms
 628/2072 src/cli/cli-commands/coder/run.test.ts 10.27ms
 629/2072 src/cli/cli-commands/coder/run.ts 14.00ms
 630/2072 src/cli/cli-commands/coder/server.test.ts 5.57ms
 631/2072 src/cli/cli-commands/coder/server.ts 13.60ms
 632/2072 src/cli/cli-commands/coder/ThinkingLevel.ts 3.15ms
 633/2072 src/cli/cli-commands/coder/verify.test.ts 5.84ms
 634/2072 src/cli/cli-commands/coder/verify.ts 5.28ms
 635/2072 src/cli/cli-commands/coder/waitOptions.ts 3.30ms
 636/2072 src/cli/cli-commands/common/$askForConfirmation.test.ts 2.55ms
 637/2072 src/cli/cli-commands/common/$askForConfirmation.ts 3.61ms
 638/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts 3.72ms
 639/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.ts 4.88ms
 640/2072 src/cli/cli-commands/common/createNonNegativeIntegerOptionParser.ts 3.11ms
 641/2072 src/cli/cli-commands/common/createPositiveIntegerOptionParser.ts 2.82ms
 642/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts 3.65ms
 643/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.ts 3.26ms
 644/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts 3.49ms
 645/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.ts 3.16ms
 646/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts 1.79ms
 647/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.ts 2.69ms
 648/2072 src/cli/cli-commands/common/disk-space/formatLowFreeDiskSpaceWarning.ts 4.41ms
 649/2072 src/cli/cli-commands/common/disk-space/freeDiskSpaceConstants.ts 2.92ms
 650/2072 src/cli/cli-commands/common/disk-space/FreeDiskSpaceStatus.ts 2.92ms
 651/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts 2.19ms
 652/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.ts 3.23ms
 653/2072 src/cli/cli-commands/common/handleActionErrors.ts 2.78ms
 654/2072 src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts 6.80ms
 655/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts 5.15ms
 656/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.ts 5.36ms
 657/2072 src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts 8.03ms
 658/2072 src/cli/cli-commands/common/harness/$resolveHarnessCommandPath.ts 3.73ms
 659/2072 src/cli/cli-commands/common/harness/$resolveHarnessInstallationOrigin.ts 4.03ms
 660/2072 src/cli/cli-commands/common/harness/$resolveInstalledHarnessVersion.ts 3.36ms
 661/2072 src/cli/cli-commands/common/harness/$runHarnessInstallationCommand.ts 5.07ms
 662/2072 src/cli/cli-commands/common/harness/buildHarnessInstallCommand.ts 3.05ms
 663/2072 src/cli/cli-commands/common/harness/formatHarnessInstallationWarning.ts 4.85ms
 664/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts 4.91ms
 665/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.ts 4.30ms
 666/2072 src/cli/cli-commands/common/harness/HarnessDefinition.test.ts 8.96ms
 667/2072 src/cli/cli-commands/common/harness/HarnessDefinition.ts 9.75ms
 668/2072 src/cli/cli-commands/common/harness/HarnessInstallationOrigin.ts 5.15ms
 669/2072 src/cli/cli-commands/common/harness/HarnessInstallationStatus.ts 3.61ms
 670/2072 src/cli/cli-commands/common/harness/HarnessUpdatePlan.ts 15.56ms
 671/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts 11.28ms
 672/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.ts 23.40ms
 673/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts 6.74ms
 674/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.ts 6.05ms
 675/2072 src/cli/cli-commands/common/npm/$askForNpmPackageInstallationApproval.ts 3.33ms
 676/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts 4.03ms
 677/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.ts 4.73ms
 678/2072 src/cli/cli-commands/common/npm/$resolveNpmGlobalPrefixPath.ts 3.67ms
 679/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts 1.82ms
 680/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.ts 2.63ms
 681/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts 3.11ms
 682/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.ts 3.17ms
 683/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts 2.59ms
 684/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.ts 8.45ms
 685/2072 src/cli/cli-commands/common/projectInitialization.ts 11.21ms
 686/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts 8.41ms
 687/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.ts 5.35ms
 688/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts 7.42ms
 689/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.ts 9.29ms
 690/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts 8.10ms
 691/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.ts 13.53ms
 692/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts 6.22ms
 693/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.ts 5.63ms
 694/2072 src/cli/cli-commands/common/promptbook-cli/buildPromptbookCliInstallCommand.ts 2.62ms
 695/2072 src/cli/cli-commands/common/promptbook-cli/formatPromptbookCliInstallationWarning.ts 4.30ms
 696/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallation.ts 5.21ms
 697/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallationStatus.ts 3.15ms
 698/2072 src/cli/cli-commands/common/promptPriorityCliOptions.ts 3.33ms
 699/2072 src/cli/cli-commands/common/promptRunnerCliOptions.ts 10.74ms
 700/2072 src/cli/cli-commands/common/questionsCliOptions.test.ts 4.28ms
 701/2072 src/cli/cli-commands/common/questionsCliOptions.ts 4.95ms
 702/2072 src/cli/cli-commands/hello.ts 3.43ms
 703/2072 src/cli/cli-commands/list-models.ts 3.86ms
 704/2072 src/cli/cli-commands/list-scrapers.ts 8.58ms
 705/2072 src/cli/cli-commands/login.ts 5.15ms
 706/2072 src/cli/cli-commands/make.ts 21.36ms
 707/2072 src/cli/cli-commands/prettify.ts 5.13ms
 708/2072 src/cli/cli-commands/run.ts 7.81ms
 709/2072 src/cli/cli-commands/run/prepareRunCommandResources.ts 11.49ms
 710/2072 src/cli/cli-commands/run/resolveRunInputParameters.ts 5.57ms
 711/2072 src/cli/cli-commands/run/runCommandAction.ts 8.57ms
 712/2072 src/cli/cli-commands/run/runPipelineExecution.ts 6.64ms
 713/2072 src/cli/cli-commands/runInteractiveChatbot.ts 6.15ms
 714/2072 src/cli/cli-commands/start-agents-server.ts 6.66ms
 715/2072 src/cli/cli-commands/start-pipelines-server.ts 10.36ms
 716/2072 src/cli/cli-commands/test-command.ts 8.26ms
 717/2072 src/cli/common/$addGlobalOptionsToCommand.ts 2.44ms
 718/2072 src/cli/common/$deprecateCliCommand.test.ts 3.83ms
 719/2072 src/cli/common/$deprecateCliCommand.ts 2.85ms
 720/2072 src/cli/common/$hideCliCommandFromHelp.test.ts 2.68ms
 721/2072 src/cli/common/$hideCliCommandFromHelp.ts 2.60ms
 722/2072 src/cli/common/$provideLlmToolsForCli.ts 7.40ms
 723/2072 src/cli/common/$requireCliSubcommand.test.ts 4.35ms
 724/2072 src/cli/common/$requireCliSubcommand.ts 2.56ms
 725/2072 src/cli/common/loadPromptsModule.ts 3.34ms
 726/2072 src/cli/main.ts 2.58ms
 727/2072 src/cli/other/install.test.ts 2.19ms
 728/2072 src/cli/other/vpsInstall.test.ts 23.71ms
 729/2072 src/cli/promptbookCli.ts 8.44ms
 730/2072 src/cli/test/ptbk.test.ts 9.04ms
 731/2072 src/cli/test/ptbk.ts 1.87ms
 732/2072 src/cli/test/tsconfig.json 2.99ms
 733/2072 src/collection/agent-collection/AgentCollection.ts 4.82ms
 734/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 22.29ms
 735/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 2.24ms
 736/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 9.93ms
 737/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts 5.28ms
 738/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.ts 4.30ms
 739/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 4.19ms
 740/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 6.17ms
 741/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 6.01ms
 742/2072 src/collection/agent-collection/CreateAgentInput.test.ts 2.86ms
 743/2072 src/collection/agent-collection/CreateAgentInput.ts 12.76ms
 744/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 6.27ms
 745/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 20.68ms
 746/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 3.55ms
 747/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 4.33ms
 748/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 3.45ms
 749/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 4.91ms
 750/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 3.89ms
 751/2072 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 4.34ms
 752/2072 src/collection/pipeline-collection/PipelineCollection.ts 4.24ms
 753/2072 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 4.48ms
 754/2072 src/collection/pipeline-collection/pipelineCollectionToJson.ts 4.34ms
 755/2072 src/collection/pipeline-collection/SimplePipelineCollection.ts 6.21ms
 756/2072 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 2.99ms
 757/2072 src/commands/_BOILERPLATE/BoilerplateCommand.ts 1.93ms
 758/2072 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 5.31ms
 759/2072 src/commands/_common/getParserForCommand.test.ts 2.47ms
 760/2072 src/commands/_common/getParserForCommand.ts 2.91ms
 761/2072 src/commands/_common/parseCommand.ts 7.40ms
 762/2072 src/commands/_common/stringifyCommand.test.ts 3.13ms
 763/2072 src/commands/_common/stringifyCommand.ts 2.28ms
 764/2072 src/commands/_common/types/Command.ts 1.72ms
 765/2072 src/commands/_common/types/CommandParser.ts 9.23ms
 766/2072 src/commands/_common/types/CommandType.ts 2.31ms
 767/2072 src/commands/_common/types/CommandUsagePlaces.ts 1.93ms
 768/2072 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 3.56ms
 769/2072 src/commands/BOOK_VERSION/BookVersionCommand.ts 1.91ms
 770/2072 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 5.48ms
 771/2072 src/commands/EXPECT/expectCommand.test.ts 4.32ms
 772/2072 src/commands/EXPECT/ExpectCommand.ts 2.73ms
 773/2072 src/commands/EXPECT/expectCommandParser.ts 9.71ms
 774/2072 src/commands/FOREACH/foreachCommand.test.ts 4.98ms
 775/2072 src/commands/FOREACH/ForeachCommand.ts 2.17ms
 776/2072 src/commands/FOREACH/foreachCommandParser.ts 9.86ms
 777/2072 src/commands/FOREACH/ForeachJson.ts 3.71ms
 778/2072 src/commands/FORMAT/formatCommand.test.ts 2.59ms
 779/2072 src/commands/FORMAT/FormatCommand.ts 1.72ms
 780/2072 src/commands/FORMAT/formatCommandParser.ts 5.04ms
 781/2072 src/commands/FORMFACTOR/formfactorCommand.test.ts 2.92ms
 782/2072 src/commands/FORMFACTOR/FormfactorCommand.ts 2.16ms
 783/2072 src/commands/FORMFACTOR/formfactorCommandParser.ts 5.30ms
 784/2072 src/commands/index.ts 4.07ms
 785/2072 src/commands/JOKER/jokerCommand.test.ts 2.62ms
 786/2072 src/commands/JOKER/JokerCommand.ts 1.88ms
 787/2072 src/commands/JOKER/jokerCommandParser.ts 5.61ms
 788/2072 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 4.11ms
 789/2072 src/commands/KNOWLEDGE/KnowledgeCommand.ts 1.82ms
 790/2072 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 6.26ms
 791/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 2.50ms
 792/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 7.82ms
 793/2072 src/commands/MODEL/modelCommand.test.ts 3.05ms
 794/2072 src/commands/MODEL/ModelCommand.ts 3.56ms
 795/2072 src/commands/MODEL/modelCommandParser.ts 8.65ms
 796/2072 src/commands/PARAMETER/parameterCommand.test.ts 6.73ms
 797/2072 src/commands/PARAMETER/ParameterCommand.ts 2.79ms
 798/2072 src/commands/PARAMETER/parameterCommandParser.ts 7.43ms
 799/2072 src/commands/PERSONA/personaCommand.test.ts 4.95ms
 800/2072 src/commands/PERSONA/PersonaCommand.ts 2.19ms
 801/2072 src/commands/PERSONA/personaCommandParser.ts 8.58ms
 802/2072 src/commands/POSTPROCESS/postprocessCommand.test.ts 3.04ms
 803/2072 src/commands/POSTPROCESS/PostprocessCommand.ts 2.15ms
 804/2072 src/commands/POSTPROCESS/postprocessCommandParser.ts 5.70ms
 805/2072 src/commands/README.md 3.24ms
 806/2072 src/commands/SECTION/sectionCommand.test.ts 2.69ms
 807/2072 src/commands/SECTION/SectionCommand.ts 2.74ms
 808/2072 src/commands/SECTION/sectionCommandParser.ts 11.12ms
 809/2072 src/commands/URL/urlCommand.test.ts 4.06ms
 810/2072 src/commands/URL/UrlCommand.ts 1.56ms
 811/2072 src/commands/URL/urlCommandParser.ts 5.28ms
 812/2072 src/commands/X_ACTION/actionCommand.test.ts 2.26ms
 813/2072 src/commands/X_ACTION/ActionCommand.ts 1.82ms
 814/2072 src/commands/X_ACTION/actionCommandParser.ts 4.87ms
 815/2072 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 2.90ms
 816/2072 src/commands/X_INSTRUMENT/InstrumentCommand.ts 1.67ms
 817/2072 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 4.99ms
 818/2072 src/commitments/_base/BaseCommitmentDefinition.ts 11.63ms
 819/2072 src/commitments/_base/BookCommitment.ts 1.55ms
 820/2072 src/commitments/_base/CommitmentDefinition.ts 5.63ms
 821/2072 src/commitments/_base/createEmptyAgentModelRequirements.ts 3.71ms
 822/2072 src/commitments/_base/formatOptionalInstructionBlock.ts 2.36ms
 823/2072 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 4.03ms
 824/2072 src/commitments/_base/ParsedCommitment.ts 2.00ms
 825/2072 src/commitments/_common/commitmentToolFunctions.ts 7.89ms
 826/2072 src/commitments/_common/createWritingCommitmentSections.ts 3.28ms
 827/2072 src/commitments/_common/getAllCommitmentDefinitions.test.ts 3.58ms
 828/2072 src/commitments/_common/getAllCommitmentDefinitions.ts 2.83ms
 829/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 2.73ms
 830/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 4.56ms
 831/2072 src/commitments/_common/getAllCommitmentsToolTitles.ts 7.32ms
 832/2072 src/commitments/_common/getAllCommitmentTypes.ts 4.29ms
 833/2072 src/commitments/_common/getCommitmentDefinition.ts 2.36ms
 834/2072 src/commitments/_common/getCommitmentNoticeMetadata.test.ts 3.16ms
 835/2072 src/commitments/_common/getCommitmentNoticeMetadata.ts 5.24ms
 836/2072 src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts 2.18ms
 837/2072 src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts 3.30ms
 838/2072 src/commitments/_common/getGroupedCommitmentDefinitions.ts 4.83ms
 839/2072 src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts 2.66ms
 840/2072 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 3.20ms
 841/2072 src/commitments/_common/isCommitmentSupported.ts 2.00ms
 842/2072 src/commitments/_common/sortCommitmentDefinitions.ts 10.94ms
 843/2072 src/commitments/_common/teamInternalAgentAccess.ts 7.55ms
 844/2072 src/commitments/_common/toolExecutionEnvelope.ts 3.37ms
 845/2072 src/commitments/_common/toolRuntimeContext.ts 14.95ms
 846/2072 src/commitments/ACTION/ACTION.test.ts 5.64ms
 847/2072 src/commitments/ACTION/ACTION.ts 7.43ms
 848/2072 src/commitments/CLOSED/CLOSED.test.ts 8.28ms
 849/2072 src/commitments/CLOSED/CLOSED.ts 10.90ms
 850/2072 src/commitments/DELETE/DELETE.test.ts 6.67ms
 851/2072 src/commitments/DELETE/DELETE.ts 12.61ms
 852/2072 src/commitments/DICTIONARY/DICTIONARY.ts 5.93ms
 853/2072 src/commitments/FORMAT/FORMAT.test.ts 8.41ms
 854/2072 src/commitments/FORMAT/FORMAT.ts 11.26ms
 855/2072 src/commitments/FROM/FROM.ts 5.02ms
 856/2072 src/commitments/GOAL/GOAL.ts 8.08ms
 857/2072 src/commitments/IMPORT/IMPORT.ts 21.12ms
 858/2072 src/commitments/index.test.ts 4.44ms
 859/2072 src/commitments/index.ts 25.15ms
 860/2072 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 9.69ms
 861/2072 src/commitments/LANGUAGE/LANGUAGE.ts 35.72ms
 862/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 4.60ms
 863/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 5.59ms
 864/2072 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 19.68ms
 865/2072 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 6.50ms
 866/2072 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 4.96ms
 867/2072 src/commitments/MESSAGE/MESSAGE.ts 7.44ms
 868/2072 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 6.24ms
 869/2072 src/commitments/META_AVATAR/META_AVATAR.ts 5.02ms
 870/2072 src/commitments/META_COLOR/META_COLOR.ts 6.88ms
 871/2072 src/commitments/META_DESCRIPTION/META_DESCRIPTION.ts 4.45ms
 872/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 2.89ms
 873/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 3.82ms
 874/2072 src/commitments/META_DOMAIN/META_DOMAIN.ts 3.89ms
 875/2072 src/commitments/META_FONT/META_FONT.ts 4.43ms
 876/2072 src/commitments/META_FULLNAME/META_FULLNAME.ts 4.11ms
 877/2072 src/commitments/META_ID/META_ID.ts 4.22ms
 878/2072 src/commitments/META_IMAGE/META_IMAGE.ts 6.42ms
 879/2072 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 3.98ms
 880/2072 src/commitments/META_LINK/META_LINK.ts 5.13ms
 881/2072 src/commitments/META_THINKING_MESSAGE/META_THINKING_MESSAGE.ts 3.86ms
 882/2072 src/commitments/META_VISIBILITY/META_VISIBILITY.ts 3.91ms
 883/2072 src/commitments/META_VOICE/META_VOICE.ts 4.44ms
 884/2072 src/commitments/MODEL/MODEL.test.ts 4.33ms
 885/2072 src/commitments/MODEL/MODEL.ts 10.21ms
 886/2072 src/commitments/NOTE/NOTE.ts 4.70ms
 887/2072 src/commitments/OPEN/OPEN.ts 3.68ms
 888/2072 src/commitments/PERSONA/persona-verification-example.md 5.00ms
 889/2072 src/commitments/PERSONA/PERSONA.ts 9.56ms
 890/2072 src/commitments/README.md 1.87ms
 891/2072 src/commitments/RULE/RULE.ts 5.49ms
 892/2072 src/commitments/SAMPLE/SAMPLE.ts 9.84ms
 893/2072 src/commitments/SCENARIO/SCENARIO.ts 7.74ms
 894/2072 src/commitments/STYLE/STYLE.ts 5.63ms
 895/2072 src/commitments/TEAM/TEAM.ts 27.54ms
 896/2072 src/commitments/TEMPLATE/README.md 4.52ms
 897/2072 src/commitments/TEMPLATE/TEMPLATE.test.ts 5.06ms
 898/2072 src/commitments/TEMPLATE/TEMPLATE.ts 6.92ms
 899/2072 src/commitments/USE_CALENDAR/calendarReference.ts 9.86ms
 900/2072 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 5.49ms
 901/2072 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 17.28ms
 902/2072 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 10.08ms
 903/2072 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 6.22ms
 904/2072 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 7.80ms
 905/2072 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 10.47ms
 906/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 8.10ms
 907/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 7.36ms
 908/2072 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.43ms
 909/2072 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 1.98ms
 910/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 4.70ms
 911/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 7.96ms
 912/2072 src/commitments/USE_MCP/USE_MCP.ts 4.70ms
 913/2072 src/commitments/USE_POPUP/USE_POPUP.ts 6.99ms
 914/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 3.96ms
 915/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 9.67ms
 916/2072 src/commitments/USE_PROJECT/callGitHubApi.ts 6.89ms
 917/2072 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 15.64ms
 918/2072 src/commitments/USE_PROJECT/createUseProjectTools.ts 5.27ms
 919/2072 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 2.29ms
 920/2072 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 2.46ms
 921/2072 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 1.91ms
 922/2072 src/commitments/USE_PROJECT/projectReference.test.ts 2.80ms
 923/2072 src/commitments/USE_PROJECT/projectReference.ts 8.66ms
 924/2072 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 7.23ms
 925/2072 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 8.47ms
 926/2072 src/commitments/USE_PROJECT/USE_PROJECT.ts 7.13ms
 927/2072 src/commitments/USE_PROJECT/UseProjectToolNames.ts 2.41ms
 928/2072 src/commitments/USE_PROJECT/UseProjectWallet.ts 1.69ms
 929/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 4.66ms
 930/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 9.27ms
 931/2072 src/commitments/WRITING_RULES/WRITING_RULES.ts 5.19ms
 932/2072 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 4.45ms
 933/2072 src/config.test.ts 1.60ms
 934/2072 src/config.ts 13.25ms
 935/2072 src/constants.ts 8.65ms
 936/2072 src/constants/streaming.ts 4.18ms
 937/2072 src/conversion/archive/loadArchive.ts 10.48ms
 938/2072 src/conversion/archive/loadJsZipModule.ts 2.45ms
 939/2072 src/conversion/archive/saveArchive.ts 4.40ms
 940/2072 src/conversion/compilePipeline.test.ts 6.34ms
 941/2072 src/conversion/compilePipeline.ts 6.44ms
 942/2072 src/conversion/compilePipelineOnRemoteServer.ts 3.85ms
 943/2072 src/conversion/parsePipeline.test.ts 3.46ms
 944/2072 src/conversion/parsePipeline.ts 4.80ms
 945/2072 src/conversion/parsePipeline/applyPipelineHead.ts 6.31ms
 946/2072 src/conversion/parsePipeline/createInitialPipelineJson.ts 3.07ms
 947/2072 src/conversion/parsePipeline/createUniqueSectionNameResolver.ts 6.68ms
 948/2072 src/conversion/parsePipeline/defineParameter.ts 4.64ms
 949/2072 src/conversion/parsePipeline/extractPipelineDescription.ts 8.58ms
 950/2072 src/conversion/parsePipeline/finalizeParsedPipeline.ts 6.92ms
 951/2072 src/conversion/parsePipeline/getPipelineIdentification.ts 3.14ms
 952/2072 src/conversion/parsePipeline/parsePreparedPipelineSections.ts 6.17ms
 953/2072 src/conversion/parsePipeline/preparePipelineString.ts 6.28ms
 954/2072 src/conversion/parsePipeline/processPipelineSection.ts 14.03ms
 955/2072 src/conversion/pipelineJsonToString.test.ts 8.88ms
 956/2072 src/conversion/pipelineJsonToString.ts 4.76ms
 957/2072 src/conversion/pipelineJsonToString/appendMarkdownBlock.ts 2.14ms
 958/2072 src/conversion/pipelineJsonToString/createPipelineCommands.ts 4.14ms
 959/2072 src/conversion/pipelineJsonToString/createPipelineIntroduction.ts 2.34ms
 960/2072 src/conversion/pipelineJsonToString/createTaskSerialization.ts 6.40ms
 961/2072 src/conversion/pipelineJsonToString/stringifyCommands.ts 1.92ms
 962/2072 src/conversion/pipelineJsonToString/stringifyTask.ts 3.06ms
 963/2072 src/conversion/prettify/PrettifyOptions.ts 2.13ms
 964/2072 src/conversion/prettify/prettifyPipelineString.ts 3.98ms
 965/2072 src/conversion/prettify/renderPipelineMermaidOptions.ts 7.21ms
 966/2072 src/conversion/utils/extractParameterNamesFromTask.test.ts 2.32ms
 967/2072 src/conversion/utils/extractParameterNamesFromTask.ts 4.57ms
 968/2072 src/conversion/validation/_importPipeline.test.ts 4.66ms
 969/2072 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 3.65ms
 970/2072 src/conversion/validation/validatePipeline-logicErrors.test.ts 3.20ms
 971/2072 src/conversion/validation/validatePipeline.test.ts 3.64ms
 972/2072 src/conversion/validation/validatePipeline.ts 5.37ms
 973/2072 src/conversion/validation/validatePipeline/createPipelineValidationContext.ts 3.06ms
 974/2072 src/conversion/validation/validatePipeline/validatePipelineCollectionsStructure.ts 4.15ms
 975/2072 src/conversion/validation/validatePipeline/validatePipelineDependencyResolution.ts 8.28ms
 976/2072 src/conversion/validation/validatePipeline/validatePipelineMetadata.ts 4.09ms
 977/2072 src/conversion/validation/validatePipeline/validatePipelineParameters.ts 6.10ms
 978/2072 src/conversion/validation/validatePipeline/validatePipelineTasks.ts 7.71ms
 979/2072 src/dialogs/callback/CallbackInterfaceTools.ts 3.05ms
 980/2072 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 2.36ms
 981/2072 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 4.19ms
 982/2072 src/dialogs/user-interface-execution-tools.test.ts 5.42ms
 983/2072 src/errors/0-BoilerplateError.ts 1.70ms
 984/2072 src/errors/0-index.ts 6.97ms
 985/2072 src/errors/AbstractFormatError.ts 2.27ms
 986/2072 src/errors/assertsError.ts 3.44ms
 987/2072 src/errors/AuthenticationError.ts 1.77ms
 988/2072 src/errors/CollectionError.ts 1.66ms
 989/2072 src/errors/ConflictError.ts 1.96ms
 990/2072 src/errors/DatabaseError.ts 2.39ms
 991/2072 src/errors/EnvironmentMismatchError.ts 1.97ms
 992/2072 src/errors/ExpectError.ts 2.30ms
 993/2072 src/errors/KnowledgeScrapeError.ts 1.86ms
 994/2072 src/errors/LimitReachedError.ts 1.93ms
 995/2072 src/errors/MissingToolsError.ts 1.80ms
 996/2072 src/errors/NotAllowed.ts 1.63ms
 997/2072 src/errors/NotFoundError.ts 1.55ms
 998/2072 src/errors/NotYetImplementedError.ts 2.28ms
 999/2072 src/errors/ParseError.ts 2.27ms
1000/2072 src/errors/PipelineExecutionError.ts 2.68ms
1001/2072 src/errors/PipelineLogicError.ts 1.74ms
1002/2072 src/errors/PipelineUrlError.ts 1.86ms
1003/2072 src/errors/PromptbookFetchError.ts 1.61ms
1004/2072 src/errors/UnexpectedError.ts 2.42ms
1005/2072 src/errors/utils/deserializeError.test.ts 2.10ms
1006/2072 src/errors/utils/deserializeError.ts 2.61ms
1007/2072 src/errors/utils/ErrorJson.ts 2.01ms
1008/2072 src/errors/utils/getErrorReportUrl.ts 2.72ms
1009/2072 src/errors/utils/serializeError.test.ts 4.65ms
1010/2072 src/errors/utils/serializeError.ts 2.25ms
1011/2072 src/errors/WrappedError.ts 2.34ms
1012/2072 src/executables/$provideExecutablesForNode.ts 4.09ms
1013/2072 src/executables/apps/locateLibreoffice.test.ts 2.91ms
1014/2072 src/executables/apps/locateLibreoffice.ts 2.51ms
1015/2072 src/executables/apps/locatePandoc.test.ts 1.68ms
1016/2072 src/executables/apps/locatePandoc.ts 2.50ms
1017/2072 src/executables/apps/locateVscode.test.ts 1.73ms
1018/2072 src/executables/apps/locateVscode.ts 2.63ms
1019/2072 src/executables/browsers/locateBrowser.test.ts 3.83ms
1020/2072 src/executables/browsers/locateBrowser.ts 3.70ms
1021/2072 src/executables/browsers/locateChrome.test.ts 1.94ms
1022/2072 src/executables/browsers/locateChrome.ts 2.64ms
1023/2072 src/executables/browsers/locateDefaultSystemBrowser.test.ts 5.12ms
1024/2072 src/executables/browsers/locateDefaultSystemBrowser.ts 3.30ms
1025/2072 src/executables/browsers/locateEdge.test.ts 2.09ms
1026/2072 src/executables/browsers/locateEdge.ts 3.25ms
1027/2072 src/executables/browsers/locateFirefox.test.ts 2.21ms
1028/2072 src/executables/browsers/locateFirefox.ts 2.78ms
1029/2072 src/executables/browsers/locateInternetExplorer.ts 3.60ms
1030/2072 src/executables/browsers/locateSafari.ts 2.13ms
1031/2072 src/executables/locateApp.test.ts 3.04ms
1032/2072 src/executables/locateApp.ts 3.89ms
1033/2072 src/executables/platforms/locateAppOnLinux.ts 3.17ms
1034/2072 src/executables/platforms/locateAppOnMacOs.ts 4.57ms
1035/2072 src/executables/platforms/locateAppOnWindows.test.ts 3.10ms
1036/2072 src/executables/platforms/locateAppOnWindows.ts 4.40ms
1037/2072 src/executables/README.md 1.78ms
1038/2072 src/execution/AbstractTaskResult.ts 2.75ms
1039/2072 src/execution/assertsTaskSuccessful.ts 3.63ms
1040/2072 src/execution/AvailableModel.ts 3.74ms
1041/2072 src/execution/CommonToolsOptions.ts 2.79ms
1042/2072 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 4.90ms
1043/2072 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 11.72ms
1044/2072 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 4.34ms
1045/2072 src/execution/createPipelineExecutor/10-executePipeline.ts 16.76ms
1046/2072 src/execution/createPipelineExecutor/20-executeTask.ts 12.13ms
1047/2072 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 10.17ms
1048/2072 src/execution/createPipelineExecutor/40-executeAttempts.ts 10.91ms
1049/2072 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 4.81ms
1050/2072 src/execution/createPipelineExecutor/executeSingleAttempt.ts 18.07ms
1051/2072 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 8.25ms
1052/2072 src/execution/createPipelineExecutor/getContextForTask.ts 2.81ms
1053/2072 src/execution/createPipelineExecutor/getExamplesForTask.ts 2.38ms
1054/2072 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 6.97ms
1055/2072 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 5.79ms
1056/2072 src/execution/createPipelineExecutor/handleAttemptFailure.ts 5.43ms
1057/2072 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 2.83ms
1058/2072 src/execution/createPipelineExecutor/reportPromptExecution.ts 4.78ms
1059/2072 src/execution/EmbeddingVector.ts 1.74ms
1060/2072 src/execution/embeddingVectorToString.ts 1.74ms
1061/2072 src/execution/Executables.ts 2.07ms
1062/2072 src/execution/execution-report/countWorkingDuration.test.ts 2.11ms
1063/2072 src/execution/execution-report/countWorkingDuration.ts 2.05ms
1064/2072 src/execution/execution-report/ExecutionPromptReportJson.ts 2.75ms
1065/2072 src/execution/execution-report/ExecutionReportJson.ts 3.72ms
1066/2072 src/execution/execution-report/executionReportJsonToString.ts 9.45ms
1067/2072 src/execution/execution-report/ExecutionReportString.ts 2.41ms
1068/2072 src/execution/execution-report/ExecutionReportStringOptions.ts 2.53ms
1069/2072 src/execution/ExecutionTask.ts 13.54ms
1070/2072 src/execution/ExecutionTools.ts 4.36ms
1071/2072 src/execution/FilesystemTools.ts 2.07ms
1072/2072 src/execution/LlmExecutionTools.ts 7.02ms
1073/2072 src/execution/LlmExecutionToolsConstructor.ts 1.64ms
1074/2072 src/execution/PipelineExecutor.ts 2.19ms
1075/2072 src/execution/PipelineExecutorResult.ts 2.81ms
1076/2072 src/execution/PromptbookFetch.test-type.ts 1.67ms
1077/2072 src/execution/PromptbookFetch.ts 1.63ms
1078/2072 src/execution/PromptResult.ts 5.69ms
1079/2072 src/execution/resolveTaskTldr.test.ts 2.61ms
1080/2072 src/execution/resolveTaskTldr.ts 5.93ms
1081/2072 src/execution/ScriptExecutionTools.ts 2.83ms
1082/2072 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 1.33ms
1083/2072 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 1.94ms
1084/2072 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 1.39ms
1085/2072 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 2.88ms
1086/2072 src/execution/translation/automatic-translate/automatic-translators/README.md 1.11ms
1087/2072 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 1.20ms
1088/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 1.66ms
1089/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 1.92ms
1090/2072 src/execution/translation/automatic-translate/translateMessages.ts 3.00ms
1091/2072 src/execution/UncertainNumber.ts 1.72ms
1092/2072 src/execution/Usage.ts 2.65ms
1093/2072 src/execution/UserInterfaceTools.ts 2.41ms
1094/2072 src/execution/utils/$provideExecutionToolsForNode.ts 3.71ms
1095/2072 src/execution/utils/addUsage.test.ts 3.65ms
1096/2072 src/execution/utils/addUsage.ts 2.38ms
1097/2072 src/execution/utils/checkExpectations.test.ts 1.87ms
1098/2072 src/execution/utils/checkExpectations.ts 2.74ms
1099/2072 src/execution/utils/computeUsageCounts.ts 4.24ms
1100/2072 src/execution/utils/forEachAsync.ts 2.53ms
1101/2072 src/execution/utils/formatUsagePrice.test.ts 1.59ms
1102/2072 src/execution/utils/formatUsagePrice.ts 1.71ms
1103/2072 src/execution/utils/uncertainNumber.ts 1.84ms
1104/2072 src/execution/utils/usage-constants.ts 2.76ms
1105/2072 src/execution/utils/usageToHuman.ts 4.21ms
1106/2072 src/execution/utils/usageToWorktime.test.ts 2.57ms
1107/2072 src/execution/utils/usageToWorktime.ts 4.26ms
1108/2072 src/execution/utils/validatePromptResult.ts 6.39ms
1109/2072 src/expectations/README.md 1.25ms
1110/2072 src/file-security-checkers/FileSecurityChecker.ts 2.48ms
1111/2072 src/file-security-checkers/FileSecurityCheckResult.ts 2.47ms
1112/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 3.92ms
1113/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 5.84ms
1114/2072 src/formats/_common/FormatParser.ts 4.53ms
1115/2072 src/formats/_common/FormatSubvalueParser.ts 4.53ms
1116/2072 src/formats/csv/CsvFormatError.ts 1.57ms
1117/2072 src/formats/csv/CsvFormatParser.ts 6.48ms
1118/2072 src/formats/csv/CsvSettings.ts 2.05ms
1119/2072 src/formats/csv/utils/csvParse.ts 2.22ms
1120/2072 src/formats/csv/utils/isValidCsvString.test.ts 1.98ms
1121/2072 src/formats/csv/utils/isValidCsvString.ts 1.59ms
1122/2072 src/formats/index.ts 1.69ms
1123/2072 src/formats/json/JsonFormatParser.ts 2.59ms
1124/2072 src/formats/json/utils/isValidJsonString.test.ts 1.71ms
1125/2072 src/formats/json/utils/isValidJsonString.ts 1.66ms
1126/2072 src/formats/json/utils/jsonParse.ts 1.77ms
1127/2072 src/formats/README.md 1.31ms
1128/2072 src/formats/text/TextFormatParser.ts 3.58ms
1129/2072 src/formats/xml/utils/isValidXmlString.test.ts 1.84ms
1130/2072 src/formats/xml/utils/isValidXmlString.ts 1.94ms
1131/2072 src/formats/xml/XmlFormatParser.ts 2.70ms
1132/2072 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 2.45ms
1133/2072 src/formfactors/_common/AbstractFormfactorDefinition.ts 3.43ms
1134/2072 src/formfactors/_common/FormfactorDefinition.ts 1.76ms
1135/2072 src/formfactors/_common/string_formfactor_name.ts 1.49ms
1136/2072 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 3.53ms
1137/2072 src/formfactors/completion/CompletionFormfactorDefinition.ts 3.48ms
1138/2072 src/formfactors/generator/GeneratorFormfactorDefinition.ts 2.70ms
1139/2072 src/formfactors/generic/GenericFormfactorDefinition.ts 2.21ms
1140/2072 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 2.56ms
1141/2072 src/formfactors/index.ts 2.53ms
1142/2072 src/formfactors/matcher/MatcherFormfactorDefinition.ts 2.78ms
1143/2072 src/formfactors/README.md 1.18ms
1144/2072 src/formfactors/sheets/SheetsFormfactorDefinition.ts 2.98ms
1145/2072 src/formfactors/translator/TranslatorFormfactorDefinition.ts 2.88ms
1146/2072 src/globals.d.ts 1.39ms
1147/2072 src/high-level-abstractions/_common/HighLevelAbstraction.ts 2.57ms
1148/2072 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 3.74ms
1149/2072 src/high-level-abstractions/index.ts 1.90ms
1150/2072 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 4.89ms
1151/2072 src/high-level-abstractions/README.md 1.20ms
1152/2072 src/import-plugins/$fileImportPlugins.ts 2.48ms
1153/2072 src/import-plugins/AgentFileImportPlugin.ts 2.93ms
1154/2072 src/import-plugins/FileImportPlugin.ts 1.85ms
1155/2072 src/import-plugins/JsonFileImportPlugin.ts 2.16ms
1156/2072 src/import-plugins/TextFileImportPlugin.ts 2.14ms
1157/2072 src/llm-providers/_common/filterModels.ts 4.38ms
1158/2072 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 2.34ms
1159/2072 src/llm-providers/_common/register/$llmToolsRegister.ts 1.92ms
1160/2072 src/llm-providers/_common/register/$provideEnvFilename.ts 4.12ms
1161/2072 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 4.59ms
1162/2072 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 5.26ms
1163/2072 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 7.56ms
1164/2072 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 5.34ms
1165/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 4.10ms
1166/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolConfigurationStatusMessage.ts 3.75ms
1167/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolEntryKey.ts 1.90ms
1168/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolInstallationStatusMessage.ts 3.90ms
1169/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolsMessageContext.ts 3.06ms
1170/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolStatus.ts 5.60ms
1171/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getAvailableRegisteredLlmToolsEnvironmentVariables.ts 2.71ms
1172/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getRegisteredLlmToolRegisters.ts 2.33ms
1173/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/hasRegisteredLlmToolEntry.ts 2.58ms
1174/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolEntries.ts 4.34ms
1175/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolStatuses.ts 2.91ms
1176/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/RegisteredLlmToolsMessageContext.ts 3.69ms
1177/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderAvailableRegisteredLlmTools.ts 5.49ms
1178/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderNoRegisteredLlmToolsMessage.ts 2.45ms
1179/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRegisteredLlmToolsMessage.ts 2.99ms
1180/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRelevantRegisteredLlmToolsEnvironmentVariables.ts 3.13ms
1181/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/USED_ENV_FILENAME.ts 2.42ms
1182/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 2.94ms
1183/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 6.54ms
1184/2072 src/llm-providers/_common/register/LlmToolsConfiguration.ts 3.14ms
1185/2072 src/llm-providers/_common/register/LlmToolsMetadata.ts 4.43ms
1186/2072 src/llm-providers/_common/register/LlmToolsOptions.ts 2.08ms
1187/2072 src/llm-providers/_common/utils/assertUniqueModels.ts 2.18ms
1188/2072 src/llm-providers/_common/utils/cache/CacheItem.ts 2.53ms
1189/2072 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 9.93ms
1190/2072 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 2.56ms
1191/2072 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 5.60ms
1192/2072 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 5.32ms
1193/2072 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 2.11ms
1194/2072 src/llm-providers/_common/utils/pricing.test.ts 1.37ms
1195/2072 src/llm-providers/_common/utils/pricing.ts 1.56ms
1196/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 3.21ms
1197/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 3.57ms
1198/2072 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 2.49ms
1199/2072 src/llm-providers/_multiple/joinLlmExecutionTools.ts 4.55ms
1200/2072 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 7.62ms
1201/2072 src/llm-providers/_multiple/playground/playground.ts 5.96ms
1202/2072 src/llm-providers/_multiple/playground/tsconfig.json 2.77ms
1203/2072 src/llm-providers/agent/Agent.test.ts 5.79ms
1204/2072 src/llm-providers/agent/Agent.ts 13.15ms
1205/2072 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 8.30ms
1206/2072 src/llm-providers/agent/AgentLlmExecutionTools.ts 14.94ms
1207/2072 src/llm-providers/agent/AgentLlmExecutionToolsAgentKitRunner.ts 11.99ms
1208/2072 src/llm-providers/agent/AgentLlmExecutionToolsOpenAiAssistantRunner.ts 8.40ms
1209/2072 src/llm-providers/agent/AgentLlmExecutionToolsPromptPreparer.ts 9.26ms
1210/2072 src/llm-providers/agent/AgentOptions.ts 3.14ms
1211/2072 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.04ms
1212/2072 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 3.39ms
1213/2072 src/llm-providers/agent/emitAgentLlmExecutionToolsAssistantPreparationProgress.ts 3.40ms
1214/2072 src/llm-providers/agent/playground/playground.ts 7.24ms
1215/2072 src/llm-providers/agent/playground/tsconfig.json 2.22ms
1216/2072 src/llm-providers/agent/register-configuration.ts 2.96ms
1217/2072 src/llm-providers/agent/register-constructor.ts 1.87ms
1218/2072 src/llm-providers/agent/RemoteAgent.test.ts 5.96ms
1219/2072 src/llm-providers/agent/RemoteAgent.ts 23.35ms
1220/2072 src/llm-providers/agent/RemoteAgentOptions.ts 1.82ms
1221/2072 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 5.47ms
1222/2072 src/llm-providers/agent/self-learning/SelfLearningManager.ts 18.34ms
1223/2072 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 8.24ms
1224/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 11.60ms
1225/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 3.71ms
1226/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 3.50ms
1227/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 4.51ms
1228/2072 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 3.01ms
1229/2072 src/llm-providers/anthropic-claude/playground/playground.ts 4.89ms
1230/2072 src/llm-providers/anthropic-claude/playground/tsconfig.json 2.42ms
1231/2072 src/llm-providers/anthropic-claude/register-configuration.ts 4.03ms
1232/2072 src/llm-providers/anthropic-claude/register-constructor.ts 2.52ms
1233/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 13.85ms
1234/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 2.83ms
1235/2072 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.29ms
1236/2072 src/llm-providers/azure-openai/playground/playground.ts 4.51ms
1237/2072 src/llm-providers/azure-openai/playground/tsconfig.json 2.11ms
1238/2072 src/llm-providers/azure-openai/register-configuration.ts 5.01ms
1239/2072 src/llm-providers/azure-openai/register-constructor.ts 2.76ms
1240/2072 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 3.91ms
1241/2072 src/llm-providers/deepseek/deepseek-models.ts 7.93ms
1242/2072 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 2.08ms
1243/2072 src/llm-providers/deepseek/other/compatibility-issue.md 1.92ms
1244/2072 src/llm-providers/deepseek/register-configuration.ts 4.48ms
1245/2072 src/llm-providers/deepseek/register-constructor.ts 2.41ms
1246/2072 src/llm-providers/google/createGoogleExecutionTools.ts 10.62ms
1247/2072 src/llm-providers/google/google-models.ts 9.68ms
1248/2072 src/llm-providers/google/GoogleExecutionToolsOptions.ts 2.00ms
1249/2072 src/llm-providers/google/other/compatibility-issue.md 2.04ms
1250/2072 src/llm-providers/google/register-configuration.ts 4.13ms
1251/2072 src/llm-providers/google/register-constructor.ts 2.72ms
1252/2072 src/llm-providers/mocked/$fakeTextToExpectations.ts 5.51ms
1253/2072 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 10.39ms
1254/2072 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 16.75ms
1255/2072 src/llm-providers/mocked/test/faked-completion.test.ts 6.13ms
1256/2072 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 2.79ms
1257/2072 src/llm-providers/mocked/test/joker.test.ts 5.10ms
1258/2072 src/llm-providers/mocked/test/mocked-chat.test.ts 6.84ms
1259/2072 src/llm-providers/mocked/test/mocked-completion.test.ts 6.67ms
1260/2072 src/llm-providers/ollama/createOllamaExecutionTools.ts 2.90ms
1261/2072 src/llm-providers/ollama/ollama-models.ts 10.95ms
1262/2072 src/llm-providers/ollama/OllamaExecutionTools.ts 5.35ms
1263/2072 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 2.83ms
1264/2072 src/llm-providers/ollama/playground/playground.ts 5.72ms
1265/2072 src/llm-providers/ollama/playground/tsconfig.json 2.97ms
1266/2072 src/llm-providers/ollama/register-configuration.ts 4.29ms
1267/2072 src/llm-providers/ollama/register-constructor.ts 2.20ms
1268/2072 src/llm-providers/openai/computeOpenAiUsage.test.ts 3.10ms
1269/2072 src/llm-providers/openai/computeOpenAiUsage.ts 5.27ms
1270/2072 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 3.26ms
1271/2072 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 7.94ms
1272/2072 src/llm-providers/openai/createOpenAiExecutionTools.ts 6.08ms
1273/2072 src/llm-providers/openai/openai-models.test.ts 2.55ms
1274/2072 src/llm-providers/openai/openai-models.ts 21.10ms
1275/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts 3.24ms
1276/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 18.02ms
1277/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsInputBuilder.ts 11.07ms
1278/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 2.49ms
1279/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOutputTypeMapper.ts 6.85ms
1280/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsToolBuilder.ts 18.95ms
1281/2072 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 17.75ms
1282/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 2.84ms
1283/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsProgressReporter.ts 5.37ms
1284/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsPromptBuilder.ts 4.13ms
1285/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsStreamRunner.ts 11.78ms
1286/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsToolRunner.ts 18.68ms
1287/2072 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 10.20ms
1288/2072 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 3.64ms
1289/2072 src/llm-providers/openai/OpenAiCompatibleModelCatalog.ts 4.48ms
1290/2072 src/llm-providers/openai/OpenAiCompatibleNonChatPromptCaller.ts 11.59ms
1291/2072 src/llm-providers/openai/OpenAiCompatibleRequestManager.ts 6.25ms
1292/2072 src/llm-providers/openai/OpenAiExecutionTools.ts 4.84ms
1293/2072 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 1.68ms
1294/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchHandler.ts 11.31ms
1295/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchPoller.ts 16.60ms
1296/2072 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 8.71ms
1297/2072 src/llm-providers/openai/OpenAiVectorStoreKnowledgeSourcePreparer.ts 7.25ms
1298/2072 src/llm-providers/openai/playground/playground.ts 13.76ms
1299/2072 src/llm-providers/openai/playground/tsconfig.json 2.26ms
1300/2072 src/llm-providers/openai/register-configuration.ts 6.68ms
1301/2072 src/llm-providers/openai/register-constructor.ts 4.05ms
1302/2072 src/llm-providers/openai/utils/buildToolInvocationScript.ts 2.55ms
1303/2072 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 12.15ms
1304/2072 src/llm-providers/openai/utils/loadOpenAiAgentsModule.ts 1.93ms
1305/2072 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 2.12ms
1306/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatProgressReporter.ts 10.35ms
1307/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatPromptBuilder.ts 6.85ms
1308/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatToolCaller.ts 11.39ms
1309/2072 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 5.81ms
1310/2072 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 2.27ms
1311/2072 src/llm-providers/README.md 1.38ms
1312/2072 src/llm-providers/remote/playground/playground.ts 6.22ms
1313/2072 src/llm-providers/remote/playground/tsconfig.json 2.84ms
1314/2072 src/llm-providers/remote/RemoteLlmExecutionTools.ts 7.45ms
1315/2072 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 9.84ms
1316/2072 src/llm-providers/vercel/playground/playground.ts 6.18ms
1317/2072 src/llm-providers/vercel/playground/tsconfig.json 2.39ms
1318/2072 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 3.31ms
1319/2072 src/llm-providers/vercel/VercelProvider.ts 2.27ms
1320/2072 src/migrations/migratePipeline.ts 3.84ms
1321/2072 src/other/README.md 1.14ms
1322/2072 src/other/templates/getBookTemplates.ts 4.16ms
1323/2072 src/personas/preparePersona.ts 6.19ms
1324/2072 src/personas/README.md 1.19ms
1325/2072 src/pipeline/book-notation.ts 3.14ms
1326/2072 src/pipeline/isValidPipelineString.test.ts 1.61ms
1327/2072 src/pipeline/isValidPipelineString.ts 2.18ms
1328/2072 src/pipeline/PipelineInterface/constants.ts 1.82ms
1329/2072 src/pipeline/PipelineInterface/getPipelineInterface.ts 3.34ms
1330/2072 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 2.54ms
1331/2072 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 3.03ms
1332/2072 src/pipeline/PipelineInterface/PipelineInterface.ts 2.84ms
1333/2072 src/pipeline/PipelineInterface/README.md 1.58ms
1334/2072 src/pipeline/PipelineJson/CommonTaskJson.ts 5.00ms
1335/2072 src/pipeline/PipelineJson/DialogTaskJson.ts 2.15ms
1336/2072 src/pipeline/PipelineJson/Expectations.ts 3.49ms
1337/2072 src/pipeline/PipelineJson/KnowledgePieceJson.ts 5.07ms
1338/2072 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 3.75ms
1339/2072 src/pipeline/PipelineJson/ParameterJson.ts 4.49ms
1340/2072 src/pipeline/PipelineJson/PersonaJson.ts 7.03ms
1341/2072 src/pipeline/PipelineJson/PipelineJson.ts 8.92ms
1342/2072 src/pipeline/PipelineJson/PreparationJson.ts 5.47ms
1343/2072 src/pipeline/PipelineJson/PromptTaskJson.ts 3.87ms
1344/2072 src/pipeline/PipelineJson/ScriptTaskJson.ts 2.41ms
1345/2072 src/pipeline/PipelineJson/SimpleTaskJson.ts 2.28ms
1346/2072 src/pipeline/PipelineJson/TaskJson.ts 5.03ms
1347/2072 src/pipeline/PipelineString.ts 4.79ms
1348/2072 src/pipeline/prompt-notation.test.ts 7.81ms
1349/2072 src/pipeline/prompt-notation.ts 8.65ms
1350/2072 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 4.62ms
1351/2072 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 5.61ms
1352/2072 src/pipeline/prompt-notation/helpers/ParameterSection.ts 4.83ms
1353/2072 src/pipeline/validatePipelineString.ts 8.63ms
1354/2072 src/playground/permanent/_boilerplate.ts 1.89ms
1355/2072 src/playground/permanent/agent-with-browser-playground.ts 2.13ms
1356/2072 src/playground/permanent/error-handling-playground.ts 2.14ms
1357/2072 src/playground/playground.ts 2.14ms
1358/2072 src/playground/tsconfig.json 2.59ms
1359/2072 src/postprocessing/README.md 1.34ms
1360/2072 src/postprocessing/utils/extractBlock.ts 2.39ms
1361/2072 src/postprocessing/utils/extractJsonBlock.test.ts 3.55ms
1362/2072 src/postprocessing/utils/extractJsonBlock.ts 3.88ms
1363/2072 src/prepare/isPipelinePrepared.test.ts 2.53ms
1364/2072 src/prepare/isPipelinePrepared.ts 4.42ms
1365/2072 src/prepare/PrepareAndScrapeOptions.ts 3.49ms
1366/2072 src/prepare/preparePipeline.ts 10.68ms
1367/2072 src/prepare/preparePipelineOnRemoteServer.ts 3.55ms
1368/2072 src/prepare/prepareTasks.ts 5.40ms
1369/2072 src/prepare/unpreparePipeline.ts 4.03ms
1370/2072 src/remote-server/createRemoteClient.ts 4.05ms
1371/2072 src/remote-server/http-sample/README.md 1.44ms
1372/2072 src/remote-server/openapi-types.ts 9.51ms
1373/2072 src/remote-server/openapi.ts 8.50ms
1374/2072 src/remote-server/README.md 2.10ms
1375/2072 src/remote-server/RemoteServer.ts 2.26ms
1376/2072 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 1.67ms
1377/2072 src/remote-server/socket-types/_subtypes/Identification.ts 4.67ms
1378/2072 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 2.18ms
1379/2072 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 3.37ms
1380/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 2.22ms
1381/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 2.02ms
1382/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 2.10ms
1383/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 1.95ms
1384/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 1.71ms
1385/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 1.99ms
1386/2072 src/remote-server/startAgentServer.ts 3.68ms
1387/2072 src/remote-server/startRemoteServer.ts 5.91ms
1388/2072 src/remote-server/startRemoteServer/createRemoteServerExpressApp.ts 2.44ms
1389/2072 src/remote-server/startRemoteServer/createRemoteServerHandle.ts 2.47ms
1390/2072 src/remote-server/startRemoteServer/createSocketServer.ts 2.03ms
1391/2072 src/remote-server/startRemoteServer/getExecutionToolsFromIdentification.ts 6.07ms
1392/2072 src/remote-server/startRemoteServer/registerBookRoutes.ts 4.32ms
1393/2072 src/remote-server/startRemoteServer/registerExecutionRoutes.ts 7.18ms
1394/2072 src/remote-server/startRemoteServer/registerListModelsSocketHandler.ts 3.81ms
1395/2072 src/remote-server/startRemoteServer/registerLoginRoute.ts 3.48ms
1396/2072 src/remote-server/startRemoteServer/registerNotFoundRoute.ts 1.91ms
1397/2072 src/remote-server/startRemoteServer/registerOpenAiCompatibleChatCompletionsRoute.ts 5.95ms
1398/2072 src/remote-server/startRemoteServer/registerOpenApiRoutes.ts 4.97ms
1399/2072 src/remote-server/startRemoteServer/registerPreparePipelineSocketHandler.ts 4.13ms
1400/2072 src/remote-server/startRemoteServer/registerPromptSocketHandler.ts 5.77ms
1401/2072 src/remote-server/startRemoteServer/registerRemoteServerHttpRoutes.ts 3.15ms
1402/2072 src/remote-server/startRemoteServer/registerRemoteServerSocketHandlers.ts 3.14ms
1403/2072 src/remote-server/startRemoteServer/registerServerIndexRoute.ts 8.00ms
1404/2072 src/remote-server/startRemoteServer/RemoteServerRuntime.ts 2.30ms
1405/2072 src/remote-server/startRemoteServer/resolveStartRemoteServerConfiguration.ts 2.95ms
1406/2072 src/remote-server/startRemoteServer/respondToSocketRequest.ts 3.45ms
1407/2072 src/remote-server/startRemoteServer/SocketResponse.ts 1.62ms
1408/2072 src/remote-server/startRemoteServer/startListening.ts 2.22ms
1409/2072 src/remote-server/startRemoteServer/StartRemoteServerConfiguration.ts 3.19ms
1410/2072 src/remote-server/types/RemoteClientOptions.ts 2.64ms
1411/2072 src/remote-server/types/RemoteServerOptions.ts 6.87ms
1412/2072 src/remote-server/ui/renderServerIndexHtml.ts 5.73ms
1413/2072 src/remote-server/ui/ServerApp.tsx 5.21ms
1414/2072 src/remote-server/ui/types.ts 2.35ms
1415/2072 src/scrapers/_boilerplate/BoilerplateScraper.ts 7.63ms
1416/2072 src/scrapers/_boilerplate/createBoilerplateScraper.ts 2.34ms
1417/2072 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 4.42ms
1418/2072 src/scrapers/_boilerplate/playground/tsconfig.json 2.35ms
1419/2072 src/scrapers/_boilerplate/README.md 1.12ms
1420/2072 src/scrapers/_boilerplate/register-constructor.ts 1.97ms
1421/2072 src/scrapers/_boilerplate/register-metadata.ts 4.16ms
1422/2072 src/scrapers/_common/Converter.ts 2.39ms
1423/2072 src/scrapers/_common/prepareKnowledgePieces.test.ts 2.68ms
1424/2072 src/scrapers/_common/prepareKnowledgePieces.ts 8.99ms
1425/2072 src/scrapers/_common/register/$provideFilesystemForNode.ts 3.09ms
1426/2072 src/scrapers/_common/register/$provideScrapersForBrowser.ts 3.30ms
1427/2072 src/scrapers/_common/register/$provideScrapersForNode.ts 4.54ms
1428/2072 src/scrapers/_common/register/$provideScriptingForNode.ts 3.35ms
1429/2072 src/scrapers/_common/register/$registeredScrapersMessage.ts 4.85ms
1430/2072 src/scrapers/_common/register/$scrapersMetadataRegister.ts 2.01ms
1431/2072 src/scrapers/_common/register/$scrapersRegister.ts 1.76ms
1432/2072 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 3.53ms
1433/2072 src/scrapers/_common/register/ScraperConstructor.ts 3.03ms
1434/2072 src/scrapers/_common/Scraper.ts 3.46ms
1435/2072 src/scrapers/_common/ScraperIntermediateSource.ts 1.85ms
1436/2072 src/scrapers/_common/utils/fetchUrlContent.ts 5.14ms
1437/2072 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 2.65ms
1438/2072 src/scrapers/_common/utils/getScraperIntermediateSource.ts 6.04ms
1439/2072 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 13.07ms
1440/2072 src/scrapers/_common/utils/promptbookFetch.test.ts 2.04ms
1441/2072 src/scrapers/_common/utils/promptbookFetch.ts 2.46ms
1442/2072 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 2.74ms
1443/2072 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 26.76ms
1444/2072 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 12.69ms
1445/2072 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 6.75ms
1446/2072 src/scrapers/document-legacy/LegacyDocumentScraper.ts 12.90ms
1447/2072 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 5.45ms
1448/2072 src/scrapers/document-legacy/playground/tsconfig.json 3.18ms
1449/2072 src/scrapers/document-legacy/README.md 2.07ms
1450/2072 src/scrapers/document-legacy/register-constructor.ts 5.21ms
1451/2072 src/scrapers/document-legacy/register-metadata.ts 6.31ms
1452/2072 src/scrapers/document/createDocumentScraper.ts 3.09ms
1453/2072 src/scrapers/document/DocumentScraper.test.ts 7.85ms
1454/2072 src/scrapers/document/DocumentScraper.ts 8.44ms
1455/2072 src/scrapers/document/examples/10-simple.docx.knowledge.json 23.71ms
1456/2072 src/scrapers/document/examples/10-simple.odt.knowledge.json 25.24ms
1457/2072 src/scrapers/document/playground/document-scraper-playground.ts 4.72ms
1458/2072 src/scrapers/document/playground/tsconfig.json 2.48ms
1459/2072 src/scrapers/document/README.md 1.65ms
1460/2072 src/scrapers/document/register-constructor.ts 2.72ms
1461/2072 src/scrapers/document/register-metadata.ts 3.94ms
1462/2072 src/scrapers/markdown/createMarkdownScraper.ts 3.74ms
1463/2072 src/scrapers/markdown/MarkdownScraper.test.ts 4.28ms
1464/2072 src/scrapers/markdown/MarkdownScraper.ts 10.06ms
1465/2072 src/scrapers/markdown/playground/markdown-scraper-playground.ts 4.29ms
1466/2072 src/scrapers/markdown/playground/tsconfig.json 2.60ms
1467/2072 src/scrapers/markdown/README.md 1.91ms
1468/2072 src/scrapers/markdown/register-constructor.ts 2.01ms
1469/2072 src/scrapers/markdown/register-metadata.ts 3.14ms
1470/2072 src/scrapers/markitdown/createMarkitdownScraper.ts 2.48ms
1471/2072 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 16.64ms
1472/2072 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 14.58ms
1473/2072 src/scrapers/markitdown/MarkitdownScraper.ts 9.75ms
1474/2072 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 4.76ms
1475/2072 src/scrapers/markitdown/playground/tsconfig.json 2.19ms
1476/2072 src/scrapers/markitdown/README.md 1.03ms
1477/2072 src/scrapers/markitdown/register-constructor.ts 2.15ms
1478/2072 src/scrapers/markitdown/register-metadata.ts 3.96ms
1479/2072 src/scrapers/pdf/createPdfScraper.ts 2.61ms
1480/2072 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 1.49ms
1481/2072 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 2.50ms
1482/2072 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 7.62ms
1483/2072 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 2.32ms
1484/2072 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 1.86ms
1485/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 4.87ms
1486/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 2.21ms
1487/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 1.80ms
1488/2072 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 1.42ms
1489/2072 src/scrapers/pdf/lib/pdf2md/models/Page.js 1.33ms
1490/2072 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 1.88ms
1491/2072 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 1.64ms
1492/2072 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 1.40ms
1493/2072 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 2.82ms
1494/2072 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 1.99ms
1495/2072 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 2.46ms
1496/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 2.80ms
1497/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 2.92ms
1498/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 4.24ms
1499/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 4.14ms
1500/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 8.57ms
1501/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 3.83ms
1502/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 10.76ms
1503/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 4.92ms
1504/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 4.27ms
1505/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 4.61ms
1506/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 3.45ms
1507/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 3.15ms
1508/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 8.24ms
1509/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 2.29ms
1510/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 4.30ms
1511/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 2.81ms
1512/2072 src/scrapers/pdf/lib/pdf2md/models/Word.js 1.76ms
1513/2072 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 4.89ms
1514/2072 src/scrapers/pdf/lib/pdf2md/pdf2md.js 2.87ms
1515/2072 src/scrapers/pdf/lib/pdf2md/README.md 1.14ms
1516/2072 src/scrapers/pdf/lib/pdf2md/util/cli.js 2.80ms
1517/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 2.15ms
1518/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/createDocumentStub.js 2.41ms
1519/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElement.js 4.05ms
1520/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElementSerializer.js 3.77ms
1521/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/Image.js 1.41ms
1522/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/xmlEncode.js 2.24ms
1523/2072 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 2.04ms
1524/2072 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 4.47ms
1525/2072 src/scrapers/pdf/lib/pdf2md/util/pdf.js 6.05ms
1526/2072 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 4.16ms
1527/2072 src/scrapers/pdf/lib/pdf2md/util/transformations.js 4.00ms
1528/2072 src/scrapers/pdf/PdfScraper.ts 4.72ms
1529/2072 src/scrapers/pdf/playground/pdf-scraper-playground.ts 3.92ms
1530/2072 src/scrapers/pdf/playground/tsconfig.json 2.26ms
1531/2072 src/scrapers/pdf/README.md 1.76ms
1532/2072 src/scrapers/pdf/register-constructor.ts 2.06ms
1533/2072 src/scrapers/pdf/register-metadata.ts 3.70ms
1534/2072 src/scrapers/README.md 1.19ms
1535/2072 src/scrapers/website/createWebsiteScraper.ts 2.52ms
1536/2072 src/scrapers/website/playground/tsconfig.json 2.24ms
1537/2072 src/scrapers/website/playground/website-scraper-playground.ts 4.03ms
1538/2072 src/scrapers/website/README.md 1.19ms
1539/2072 src/scrapers/website/register-constructor.ts 2.01ms
1540/2072 src/scrapers/website/register-metadata.ts 3.31ms
1541/2072 src/scrapers/website/utils/createShowdownConverter.test.ts 2.77ms
1542/2072 src/scrapers/website/utils/createShowdownConverter.ts 2.25ms
1543/2072 src/scrapers/website/WebsiteScraper.ts 10.56ms
1544/2072 src/scripting/_test/custom-function-async.test.ts.test.ts 4.04ms
1545/2072 src/scripting/_test/custom-function-missing.test.ts 4.16ms
1546/2072 src/scripting/_test/custom-function-with-dependencies.test.ts 5.09ms
1547/2072 src/scripting/_test/custom-function.test.ts 7.89ms
1548/2072 src/scripting/_test/postprocessing.test.ts 7.04ms
1549/2072 src/scripting/_test/script-execution-errors.test.ts 7.05ms
1550/2072 src/scripting/_test/script-execution-tools.test.ts 4.90ms
1551/2072 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 5.27ms
1552/2072 src/scripting/javascript/JavascriptEvalExecutionTools.ts 10.33ms
1553/2072 src/scripting/javascript/JavascriptExecutionTools.ts 2.31ms
1554/2072 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 5.31ms
1555/2072 src/scripting/javascript/postprocessing-functions.ts 5.16ms
1556/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 4.17ms
1557/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 4.30ms
1558/2072 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 2.08ms
1559/2072 src/scripting/python/PythonExecutionTools.ts 3.22ms
1560/2072 src/scripting/typescript/TypescriptExecutionTools.ts 4.49ms
1561/2072 src/search-engines/_index.ts 2.26ms
1562/2072 src/search-engines/bing/BingSearchEngine.ts 5.26ms
1563/2072 src/search-engines/dummy/DummySearchEngine.ts 3.98ms
1564/2072 src/search-engines/google/GoogleSearchEngine.ts 7.82ms
1565/2072 src/search-engines/SearchEngine.ts 11.08ms
1566/2072 src/search-engines/SearchResult.ts 2.21ms
1567/2072 src/search-engines/serp/SerpSearchEngine.ts 3.73ms
1568/2072 src/speech-recognition/BrowserSpeechRecognition.ts 5.89ms
1569/2072 src/speech-recognition/OpenAiSpeechRecognition.test.ts 4.14ms
1570/2072 src/speech-recognition/OpenAiSpeechRecognition.ts 32.85ms
1571/2072 src/storage/_common/PromptbookStorage.test-type.ts 3.34ms
1572/2072 src/storage/_common/PromptbookStorage.ts 3.69ms
1573/2072 src/storage/blackhole/BlackholeStorage.ts 2.91ms
1574/2072 src/storage/env-storage/$EnvStorage.ts 6.59ms
1575/2072 src/storage/file-cache-storage/FileCacheStorage.ts 13.02ms
1576/2072 src/storage/file-cache-storage/FileCacheStorageOptions.ts 3.02ms
1577/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 2.47ms
1578/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 3.12ms
1579/2072 src/storage/local-storage/getIndexedDbStorage.ts 5.73ms
1580/2072 src/storage/local-storage/getLocalStorage.ts 8.94ms
1581/2072 src/storage/local-storage/getSessionStorage.ts 3.14ms
1582/2072 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 3.37ms
1583/2072 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 5.11ms
1584/2072 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 6.10ms
1585/2072 src/storage/memory/MemoryStorage.ts 2.62ms
1586/2072 src/storage/utils/PrefixStorage.ts 2.34ms
1587/2072 src/transpilers/_common/BookTranspiler.ts 2.23ms
1588/2072 src/transpilers/_common/BookTranspilerOptions.ts 3.72ms
1589/2072 src/transpilers/_common/createTranspiledTeamRuntimeSection.ts 11.57ms
1590/2072 src/transpilers/_common/createZodSchemaSource.ts 5.17ms
1591/2072 src/transpilers/_common/formatUsedToolFunctions.test.ts 2.31ms
1592/2072 src/transpilers/_common/formatUsedToolFunctions.ts 4.82ms
1593/2072 src/transpilers/_common/prepareSdkTranspilerContext.ts 6.97ms
1594/2072 src/transpilers/_common/register/$bookTranspilersRegister.ts 1.90ms
1595/2072 src/transpilers/_common/resolveClaudeModelName.ts 2.05ms
1596/2072 src/transpilers/_common/TranspiledTeamExport.ts 3.91ms
1597/2072 src/transpilers/_common/transpiledTeamTranspilers.test.ts 4.59ms
1598/2072 src/transpilers/agent-os/AgentOsTranspiler.test.ts 3.94ms
1599/2072 src/transpilers/agent-os/AgentOsTranspiler.ts 11.53ms
1600/2072 src/transpilers/agent-os/register.ts 3.40ms
1601/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts 4.46ms
1602/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.ts 13.16ms
1603/2072 src/transpilers/anthropic-claude-managed/register.ts 2.01ms
1604/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts 4.54ms
1605/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.ts 10.66ms
1606/2072 src/transpilers/anthropic-claude-sdk/register.ts 1.96ms
1607/2072 src/transpilers/e2b/E2BTranspiler.test.ts 3.45ms
1608/2072 src/transpilers/e2b/E2BTranspiler.ts 7.66ms
1609/2072 src/transpilers/e2b/register.ts 2.24ms
1610/2072 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 2.90ms
1611/2072 src/transpilers/formatted-book-in-markdown/register.ts 2.81ms
1612/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts 4.57ms
1613/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.ts 14.45ms
1614/2072 src/transpilers/openai-agents/register.ts 1.97ms
1615/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 4.30ms
1616/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 9.45ms
1617/2072 src/transpilers/openai-sdk/playground/playground.ts 1.95ms
1618/2072 src/transpilers/openai-sdk/playground/tsconfig.json 2.09ms
1619/2072 src/transpilers/openai-sdk/register.ts 3.10ms
1620/2072 src/types/Arrayable.ts 1.44ms
1621/2072 src/types/InputParameters_private.ts 1.60ms
1622/2072 src/types/IntermediateFilesStrategy.ts 1.70ms
1623/2072 src/types/LlmCall.ts 1.55ms
1624/2072 src/types/LlmToolDefinition.ts 2.32ms
1625/2072 src/types/Message.ts 2.43ms
1626/2072 src/types/ModelRequirements.ts 6.45ms
1627/2072 src/types/ModelVariant.ts 2.19ms
1628/2072 src/types/NonEmptyArray.ts 1.41ms
1629/2072 src/types/number_bytes.ts 1.22ms
1630/2072 src/types/number_id.ts 1.21ms
1631/2072 src/types/number_likeness.ts 1.07ms
1632/2072 src/types/number_milliseconds.ts 1.76ms
1633/2072 src/types/number_percent.ts 1.24ms
1634/2072 src/types/number_positive.ts 1.41ms
1635/2072 src/types/number_usd.ts 1.43ms
1636/2072 src/types/Parameters_private.ts 1.88ms
1637/2072 src/types/Parameters.ts 1.77ms
1638/2072 src/types/Prompt.ts 4.83ms
1639/2072 src/types/ReservedParameters_private.ts 2.27ms
1640/2072 src/types/ScriptLanguage.ts 1.58ms
1641/2072 src/types/SectionType.ts 2.14ms
1642/2072 src/types/SpeechRecognition.ts 3.41ms
1643/2072 src/types/string_agent_hash_private.ts 1.40ms
1644/2072 src/types/string_agent_name_in_book_private.ts 1.56ms
1645/2072 src/types/string_agent_name_private.ts 1.29ms
1646/2072 src/types/string_agent_name.ts 1.61ms
1647/2072 src/types/string_agent_permanent_id_private.ts 1.37ms
1648/2072 src/types/string_agent_url_private.ts 1.16ms
1649/2072 src/types/string_agent_url.ts 1.15ms
1650/2072 src/types/string_base_url_private.ts 1.33ms
1651/2072 src/types/string_base_url.ts 1.13ms
1652/2072 src/types/string_base64_private.ts 1.48ms
1653/2072 src/types/string_base64.ts 1.34ms
1654/2072 src/types/string_business_category_name_private.ts 1.12ms
1655/2072 src/types/string_business_category_name.ts 1.06ms
1656/2072 src/types/string_char_private.ts 1.55ms
1657/2072 src/types/string_chat_prompt_private.ts 1.36ms
1658/2072 src/types/string_completion_prompt_private.ts 1.22ms
1659/2072 src/types/string_email_private.ts 1.14ms
1660/2072 src/types/string_email.ts 1.30ms
1661/2072 src/types/string_filename.ts 1.81ms
1662/2072 src/types/string_host_private.ts 1.47ms
1663/2072 src/types/string_host.ts 1.77ms
1664/2072 src/types/string_href_private.ts 1.35ms
1665/2072 src/types/string_href.ts 1.29ms
1666/2072 src/types/string_knowledge_source_content.ts 1.85ms
1667/2072 src/types/string_markdown.ts 2.99ms
1668/2072 src/types/string_mime_type_private.ts 1.23ms
1669/2072 src/types/string_mime_type.ts 1.80ms
1670/2072 src/types/string_model_description_private.ts 1.32ms
1671/2072 src/types/string_model_name_private.ts 1.68ms
1672/2072 src/types/string_model_name.ts 1.41ms
1673/2072 src/types/string_name_private.ts 1.34ms
1674/2072 src/types/string_name.ts 1.86ms
1675/2072 src/types/string_page_private.ts 1.25ms
1676/2072 src/types/string_page.ts 1.66ms
1677/2072 src/types/string_parameter_name.ts 2.47ms
1678/2072 src/types/string_parameter_value_private.ts 4.13ms
1679/2072 src/types/string_person_fullname.ts 4.24ms
1680/2072 src/types/string_persona_description_private.ts 1.49ms
1681/2072 src/types/string_persona_description.ts 2.65ms
1682/2072 src/types/string_pipeline_root_url_private.ts 1.89ms
1683/2072 src/types/string_pipeline_root_url.ts 1.77ms
1684/2072 src/types/string_pipeline_url_private.ts 2.19ms
1685/2072 src/types/string_pipeline_url.ts 1.89ms
1686/2072 src/types/string_prompt_image_private.ts 1.52ms
1687/2072 src/types/string_prompt_private.ts 1.36ms
1688/2072 src/types/string_prompt.ts 1.97ms
1689/2072 src/types/string_promptbook_server_url_private.ts 1.42ms
1690/2072 src/types/string_promptbook_server_url.ts 1.55ms
1691/2072 src/types/string_reserved_parameter_name_private.ts 1.55ms
1692/2072 src/types/string_sha256.ts 2.47ms
1693/2072 src/types/string_system_message_private.ts 1.38ms
1694/2072 src/types/string_template_private.ts 1.13ms
1695/2072 src/types/string_text_prompt_private.ts 1.68ms
1696/2072 src/types/string_title_private.ts 1.47ms
1697/2072 src/types/string_title.ts 1.42ms
1698/2072 src/types/string_token.ts 2.30ms
1699/2072 src/types/string_url_image_private.ts 1.34ms
1700/2072 src/types/string_url_image.ts 1.19ms
1701/2072 src/types/string_url_private.ts 2.86ms
1702/2072 src/types/string_url.ts 2.07ms
1703/2072 src/types/TaskType.ts 7.21ms
1704/2072 src/types/ToolCall.ts 8.10ms
1705/2072 src/types/typeAliasEmoji.ts 4.08ms
1706/2072 src/types/typeAliases.ts 4.68ms
1707/2072 src/types/Updatable.ts 2.80ms
1708/2072 src/utils/agent-message-runtime/AgentMessageProjectChange.ts 6.39ms
1709/2072 src/utils/agent-message-runtime/agentMessageRuntimePaths.ts 3.62ms
1710/2072 src/utils/agent-message-runtime/AgentMessageTouchedExternalSource.ts 3.68ms
1711/2072 src/utils/agent-message-runtime/parseAgentMessageRuntimeLogEvents.ts 6.85ms
1712/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts 6.75ms
1713/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.ts 9.43ms
1714/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts 4.42ms
1715/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.ts 11.54ms
1716/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts 3.68ms
1717/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.ts 8.78ms
1718/2072 src/utils/agents/resolveAgentAvatarImageUrl.test.ts 10.93ms
1719/2072 src/utils/agents/resolveAgentAvatarImageUrl.ts 10.25ms
1720/2072 src/utils/agents/terminalAgentAvatarVisual.test.ts 10.12ms
1721/2072 src/utils/agents/terminalAgentAvatarVisual.ts 31.46ms
1722/2072 src/utils/ascii-art/$detectTerminalAnsiColorDepth.ts 4.14ms
1723/2072 src/utils/ascii-art/convertImageDataToAsciiArt.ts 9.80ms
1724/2072 src/utils/ascii-art/createAnsiColorCode.ts 5.50ms
1725/2072 src/utils/chat/chatAttachments.test.ts 9.24ms
1726/2072 src/utils/chat/chatAttachments.ts 3.81ms
1727/2072 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 2.92ms
1728/2072 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 3.15ms
1729/2072 src/utils/chat/chatAttachments/appendChatContextSections.ts 2.17ms
1730/2072 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 4.32ms
1731/2072 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 2.66ms
1732/2072 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 5.60ms
1733/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 10.07ms
1734/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 3.92ms
1735/2072 src/utils/chat/constants.ts 2.24ms
1736/2072 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 2.37ms
1737/2072 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 2.18ms
1738/2072 src/utils/chat/escapeRegExp.ts 1.80ms
1739/2072 src/utils/clientVersion.ts 5.02ms
1740/2072 src/utils/color/$randomColor.ts 2.52ms
1741/2072 src/utils/color/Color.test.ts 1.65ms
1742/2072 src/utils/color/Color.ts 7.59ms
1743/2072 src/utils/color/ColorValue.ts 3.19ms
1744/2072 src/utils/color/css-colors.ts 6.46ms
1745/2072 src/utils/color/internal-utils/checkChannelValue.ts 1.89ms
1746/2072 src/utils/color/internal-utils/hslToRgb.ts 2.24ms
1747/2072 src/utils/color/internal-utils/rgbToHsl.ts 1.85ms
1748/2072 src/utils/color/isHexColorString.ts 1.51ms
1749/2072 src/utils/color/operators/ColorTransformer.ts 1.42ms
1750/2072 src/utils/color/operators/darken.ts 1.83ms
1751/2072 src/utils/color/operators/furthest.ts 1.63ms
1752/2072 src/utils/color/operators/grayscale.ts 1.94ms
1753/2072 src/utils/color/operators/lighten.ts 1.85ms
1754/2072 src/utils/color/operators/mixWithColor.ts 2.47ms
1755/2072 src/utils/color/operators/nearest.ts 2.04ms
1756/2072 src/utils/color/operators/negative.ts 1.39ms
1757/2072 src/utils/color/operators/negativeLightness.ts 1.62ms
1758/2072 src/utils/color/operators/README.md 2.82ms
1759/2072 src/utils/color/operators/saturate.ts 2.32ms
1760/2072 src/utils/color/operators/withAlpha.ts 1.47ms
1761/2072 src/utils/color/parseColorString.ts 2.53ms
1762/2072 src/utils/color/parsers/ColorChannelSet.ts 1.23ms
1763/2072 src/utils/color/parsers/parseHexColor.ts 2.83ms
1764/2072 src/utils/color/parsers/parseHslColor.ts 2.12ms
1765/2072 src/utils/color/parsers/parseRgbColor.ts 2.43ms
1766/2072 src/utils/color/utils/areColorsEqual.ts 1.55ms
1767/2072 src/utils/color/utils/colorDistance.ts 2.18ms
1768/2072 src/utils/color/utils/colorHue.ts 1.94ms
1769/2072 src/utils/color/utils/colorHueDistance.test.ts 2.19ms
1770/2072 src/utils/color/utils/colorHueDistance.ts 1.50ms
1771/2072 src/utils/color/utils/colorLuminance.ts 1.30ms
1772/2072 src/utils/color/utils/colorSatulightion.ts 1.98ms
1773/2072 src/utils/color/utils/colorSaturation.ts 1.40ms
1774/2072 src/utils/color/utils/colorToDataUrl.ts 3.12ms
1775/2072 src/utils/color/utils/mixColors.ts 1.72ms
1776/2072 src/utils/database/uniqueConstraint.ts 3.68ms
1777/2072 src/utils/DEFAULT_THINKING_MESSAGES.ts 2.22ms
1778/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 2.33ms
1779/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 3.05ms
1780/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 2.03ms
1781/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 3.44ms
1782/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 2.93ms
1783/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 2.65ms
1784/2072 src/utils/editable/types/PipelineEditableSerialized.ts 2.52ms
1785/2072 src/utils/editable/utils/isFlatPipeline.test.ts 3.06ms
1786/2072 src/utils/editable/utils/isFlatPipeline.ts 3.60ms
1787/2072 src/utils/editable/utils/renamePipelineParameter.test.ts 5.43ms
1788/2072 src/utils/editable/utils/renamePipelineParameter.ts 4.12ms
1789/2072 src/utils/editable/utils/stringifyPipelineJson.ts 3.18ms
1790/2072 src/utils/environment/$detectRuntimeEnvironment.ts 2.07ms
1791/2072 src/utils/environment/$getGlobalScope.ts 1.45ms
1792/2072 src/utils/environment/$isRunningInBrowser.ts 1.52ms
1793/2072 src/utils/environment/$isRunningInJest.ts 1.69ms
1794/2072 src/utils/environment/$isRunningInNode.ts 2.02ms
1795/2072 src/utils/environment/$isRunningInWebWorker.ts 1.71ms
1796/2072 src/utils/execCommand/$execCommand.ts 4.56ms
1797/2072 src/utils/execCommand/$execCommandNormalizeOptions.ts 3.42ms
1798/2072 src/utils/execCommand/$execCommands.ts 2.08ms
1799/2072 src/utils/execCommand/execCommand.test.ts 1.69ms
1800/2072 src/utils/execCommand/execCommandNormalizeOptions.test.ts 2.80ms
1801/2072 src/utils/execCommand/ExecCommandOptions.ts 3.17ms
1802/2072 src/utils/expectation-counters/constants.ts 1.71ms
1803/2072 src/utils/expectation-counters/countCharacters.test.ts 2.36ms
1804/2072 src/utils/expectation-counters/countCharacters.ts 2.63ms
1805/2072 src/utils/expectation-counters/countLines.test.ts 1.65ms
1806/2072 src/utils/expectation-counters/countLines.ts 2.13ms
1807/2072 src/utils/expectation-counters/countPages.test.ts 6.60ms
1808/2072 src/utils/expectation-counters/countPages.ts 2.01ms
1809/2072 src/utils/expectation-counters/countParagraphs.test.ts 1.69ms
1810/2072 src/utils/expectation-counters/countParagraphs.ts 1.89ms
1811/2072 src/utils/expectation-counters/countSentences.test.ts 1.83ms
1812/2072 src/utils/expectation-counters/countSentences.ts 1.92ms
1813/2072 src/utils/expectation-counters/countWords.test.ts 2.09ms
1814/2072 src/utils/expectation-counters/countWords.ts 2.75ms
1815/2072 src/utils/expectation-counters/index.ts 2.88ms
1816/2072 src/utils/expectation-counters/other/countPages.md 2.74ms
1817/2072 src/utils/files/$induceBookDownload.ts 2.51ms
1818/2072 src/utils/files/$induceFileDownload.ts 2.32ms
1819/2072 src/utils/files/decodeAttachmentAsText.test.ts 5.75ms
1820/2072 src/utils/files/decodeAttachmentAsText.ts 16.05ms
1821/2072 src/utils/files/extensionToMimeType.test.ts 2.68ms
1822/2072 src/utils/files/extensionToMimeType.ts 1.41ms
1823/2072 src/utils/files/getFileExtension.test.ts 2.13ms
1824/2072 src/utils/files/getFileExtension.ts 1.51ms
1825/2072 src/utils/files/isDirectoryExisting.test.ts 1.78ms
1826/2072 src/utils/files/isDirectoryExisting.ts 2.36ms
1827/2072 src/utils/files/isExecutable.ts 1.88ms
1828/2072 src/utils/files/isFileExisting.test.ts 1.93ms
1829/2072 src/utils/files/isFileExisting.ts 2.47ms
1830/2072 src/utils/files/listAllFiles.test.ts 1.95ms
1831/2072 src/utils/files/listAllFiles.ts 3.74ms
1832/2072 src/utils/files/mimeTypeToExtension.test.ts 2.53ms
1833/2072 src/utils/files/mimeTypeToExtension.ts 1.39ms
1834/2072 src/utils/files/ObjectUrl.ts 3.33ms
1835/2072 src/utils/files/readResponseBytes.ts 2.64ms
1836/2072 src/utils/filesystem/promptbookTemporaryPath.test.ts 2.48ms
1837/2072 src/utils/filesystem/promptbookTemporaryPath.ts 3.41ms
1838/2072 src/utils/html/escapeHtml.ts 1.69ms
1839/2072 src/utils/isTimingSafeEqualString.ts 2.56ms
1840/2072 src/utils/knowledge/inlineKnowledgeSource.test.ts 2.86ms
1841/2072 src/utils/knowledge/inlineKnowledgeSource.ts 4.98ms
1842/2072 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 2.24ms
1843/2072 src/utils/knowledge/simplifyKnowledgeLabel.ts 7.07ms
1844/2072 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 3.69ms
1845/2072 src/utils/linguistic-hash/linguisticHash.test.ts 3.52ms
1846/2072 src/utils/linguistic-hash/linguisticHash.ts 3.20ms
1847/2072 src/utils/linguistic-hash/LinguisticHashLanguage.ts 2.76ms
1848/2072 src/utils/linguistic-hash/linguisticHashTypes.ts 1.65ms
1849/2072 src/utils/linguistic-hash/linguisticHashWordCount.ts 2.02ms
1850/2072 src/utils/linguistic-hash/linguisticHashWords.cs.ts 3.16ms
1851/2072 src/utils/linguistic-hash/linguisticHashWords.en.ts 11.59ms
1852/2072 src/utils/linguistic-hash/linguisticHashWordSelection.ts 3.58ms
1853/2072 src/utils/markdown/addAutoGeneratedSection.test.ts 2.63ms
1854/2072 src/utils/markdown/addAutoGeneratedSection.ts 3.02ms
1855/2072 src/utils/markdown/createMarkdownChart.test.ts 2.20ms
1856/2072 src/utils/markdown/createMarkdownChart.ts 3.27ms
1857/2072 src/utils/markdown/createMarkdownTable.test.ts 2.80ms
1858/2072 src/utils/markdown/createMarkdownTable.ts 2.25ms
1859/2072 src/utils/markdown/escapeMarkdownBlock.test.ts 1.70ms
1860/2072 src/utils/markdown/escapeMarkdownBlock.ts 1.45ms
1861/2072 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 2.04ms
1862/2072 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 5.60ms
1863/2072 src/utils/markdown/extractAllBlocksFromMarkdown.ts 4.13ms
1864/2072 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 3.35ms
1865/2072 src/utils/markdown/extractAllListItemsFromMarkdown.ts 2.28ms
1866/2072 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 3.57ms
1867/2072 src/utils/markdown/extractOneBlockFromMarkdown.ts 3.18ms
1868/2072 src/utils/markdown/flattenMarkdown.test.ts 3.60ms
1869/2072 src/utils/markdown/flattenMarkdown.ts 3.34ms
1870/2072 src/utils/markdown/humanizeAiText.test.ts 3.98ms
1871/2072 src/utils/markdown/humanizeAiText.ts 2.75ms
1872/2072 src/utils/markdown/humanizeAiTextEllipsis.ts 1.98ms
1873/2072 src/utils/markdown/humanizeAiTextEmdashed.ts 1.84ms
1874/2072 src/utils/markdown/humanizeAiTextQuotes.ts 2.21ms
1875/2072 src/utils/markdown/humanizeAiTextSources.ts 1.65ms
1876/2072 src/utils/markdown/humanizeAiTextWhitespace.ts 2.16ms
1877/2072 src/utils/markdown/parseMarkdownSection.test.ts 1.92ms
1878/2072 src/utils/markdown/parseMarkdownSection.ts 2.50ms
1879/2072 src/utils/markdown/prettifyMarkdown.ts 1.75ms
1880/2072 src/utils/markdown/promptbookifyAiText.test.ts 2.21ms
1881/2072 src/utils/markdown/promptbookifyAiText.ts 2.70ms
1882/2072 src/utils/markdown/README.md 1.24ms
1883/2072 src/utils/markdown/removeMarkdownComments.test.ts 2.33ms
1884/2072 src/utils/markdown/removeMarkdownComments.ts 1.50ms
1885/2072 src/utils/markdown/removeMarkdownFormatting.test.ts 3.48ms
1886/2072 src/utils/markdown/removeMarkdownFormatting.ts 1.51ms
1887/2072 src/utils/markdown/removeMarkdownLinks.test.ts 1.90ms
1888/2072 src/utils/markdown/removeMarkdownLinks.ts 1.87ms
1889/2072 src/utils/markdown/shiftMarkdownHeadingLevels.ts 2.53ms
1890/2072 src/utils/markdown/splitMarkdownIntoSections.test.ts 2.74ms
1891/2072 src/utils/markdown/splitMarkdownIntoSections.ts 2.56ms
1892/2072 src/utils/markdown/trimCodeBlock.test.ts 3.35ms
1893/2072 src/utils/markdown/trimCodeBlock.ts 1.81ms
1894/2072 src/utils/markdown/trimEndOfCodeBlock.test.ts 1.73ms
1895/2072 src/utils/markdown/trimEndOfCodeBlock.ts 1.51ms
1896/2072 src/utils/misc/$getCurrentDate.ts 1.49ms
1897/2072 src/utils/misc/$Register.ts 5.08ms
1898/2072 src/utils/misc/aboutPromptbookInformation.ts 3.79ms
1899/2072 src/utils/misc/arrayableToArray.test.ts 1.48ms
1900/2072 src/utils/misc/arrayableToArray.ts 1.64ms
1901/2072 src/utils/misc/computeHash.test.ts 1.51ms
1902/2072 src/utils/misc/computeHash.ts 1.94ms
1903/2072 src/utils/misc/createLazyModuleLoader.ts 2.32ms
1904/2072 src/utils/misc/debounce.test.ts 1.98ms
1905/2072 src/utils/misc/debounce.ts 1.95ms
1906/2072 src/utils/misc/emojis.ts 5.88ms
1907/2072 src/utils/misc/FromtoItems.ts 1.55ms
1908/2072 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 7.38ms
1909/2072 src/utils/misc/parseNumber.test.ts 4.56ms
1910/2072 src/utils/misc/parseNumber.ts 3.38ms
1911/2072 src/utils/misc/xAboutPromptbookInformation.tsx 2.73ms
1912/2072 src/utils/normalization/capitalize.test.ts 1.74ms
1913/2072 src/utils/normalization/capitalize.ts 1.62ms
1914/2072 src/utils/normalization/constructImageFilename.test.ts 4.17ms
1915/2072 src/utils/normalization/constructImageFilename.ts 2.53ms
1916/2072 src/utils/normalization/decapitalize.test.ts 2.09ms
1917/2072 src/utils/normalization/decapitalize.ts 1.66ms
1918/2072 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 5.63ms
1919/2072 src/utils/normalization/IKeywords.ts 1.31ms
1920/2072 src/utils/normalization/index.ts 0.68ms
1921/2072 src/utils/normalization/isValidKeyword.test.ts 1.54ms
1922/2072 src/utils/normalization/isValidKeyword.ts 1.71ms
1923/2072 src/utils/normalization/nameToUriPart.test.ts 1.84ms
1924/2072 src/utils/normalization/nameToUriPart.ts 1.75ms
1925/2072 src/utils/normalization/nameToUriParts.test.ts 2.12ms
1926/2072 src/utils/normalization/nameToUriParts.ts 1.92ms
1927/2072 src/utils/normalization/normalize-to-kebab-case.test.ts 1.82ms
1928/2072 src/utils/normalization/normalize-to-kebab-case.ts 2.44ms
1929/2072 src/utils/normalization/normalizeMessageText.test.ts 1.85ms
1930/2072 src/utils/normalization/normalizeMessageText.ts 1.30ms
1931/2072 src/utils/normalization/normalizeTo_camelCase.test.ts 1.70ms
1932/2072 src/utils/normalization/normalizeTo_camelCase.ts 2.58ms
1933/2072 src/utils/normalization/normalizeTo_PascalCase.test.ts 2.12ms
1934/2072 src/utils/normalization/normalizeTo_PascalCase.ts 2.29ms
1935/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 1.74ms
1936/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 3.50ms
1937/2072 src/utils/normalization/normalizeTo_snake_case.test.ts 1.72ms
1938/2072 src/utils/normalization/normalizeTo_snake_case.ts 2.01ms
1939/2072 src/utils/normalization/normalizeWhitespaces.test.ts 1.51ms
1940/2072 src/utils/normalization/normalizeWhitespaces.ts 1.72ms
1941/2072 src/utils/normalization/orderJson.test.ts 1.70ms
1942/2072 src/utils/normalization/orderJson.ts 1.94ms
1943/2072 src/utils/normalization/parseKeywords.test.ts 2.05ms
1944/2072 src/utils/normalization/parseKeywords.ts 2.03ms
1945/2072 src/utils/normalization/parseKeywordsFromString.test.ts 3.43ms
1946/2072 src/utils/normalization/parseKeywordsFromString.ts 2.14ms
1947/2072 src/utils/normalization/removeDiacritics.test.ts 1.94ms
1948/2072 src/utils/normalization/removeDiacritics.ts 2.08ms
1949/2072 src/utils/normalization/removeEmojis.test.ts 2.41ms
1950/2072 src/utils/normalization/removeEmojis.ts 1.51ms
1951/2072 src/utils/normalization/removeQuotes.test.ts 1.68ms
1952/2072 src/utils/normalization/removeQuotes.ts 2.15ms
1953/2072 src/utils/normalization/searchKeywords.test.ts 1.75ms
1954/2072 src/utils/normalization/searchKeywords.ts 1.70ms
1955/2072 src/utils/normalization/suffixUrl.test.ts 1.38ms
1956/2072 src/utils/normalization/suffixUrl.ts 1.40ms
1957/2072 src/utils/normalization/titleToName.test.ts 2.04ms
1958/2072 src/utils/normalization/titleToName.ts 2.20ms
1959/2072 src/utils/normalization/unwrapResult.test.ts 5.66ms
1960/2072 src/utils/normalization/unwrapResult.ts 5.16ms
1961/2072 src/utils/organization/___and___.ts 1.17ms
1962/2072 src/utils/organization/___or___.ts 1.03ms
1963/2072 src/utils/organization/$side_effect.ts 1.19ms
1964/2072 src/utils/organization/$sideEffect.ts 1.76ms
1965/2072 src/utils/organization/empty_object.ts 1.30ms
1966/2072 src/utils/organization/just_empty_object.ts 2.02ms
1967/2072 src/utils/organization/just.ts 1.51ms
1968/2072 src/utils/organization/keepImported.ts 1.65ms
1969/2072 src/utils/organization/keepTypeImported.ts 1.46ms
1970/2072 src/utils/organization/keepUnused.ts 1.78ms
1971/2072 src/utils/organization/preserve.ts 1.89ms
1972/2072 src/utils/organization/README.md 1.32ms
1973/2072 src/utils/organization/really_any.ts 1.26ms
1974/2072 src/utils/organization/really_unknown.ts 1.42ms
1975/2072 src/utils/organization/spaceTrim.ts 1.36ms
1976/2072 src/utils/organization/TODO_any.ts 1.17ms
1977/2072 src/utils/organization/TODO_narrow.ts 1.02ms
1978/2072 src/utils/organization/TODO_object.ts 1.04ms
1979/2072 src/utils/organization/TODO_remove_as.ts 1.56ms
1980/2072 src/utils/organization/TODO_string.ts 1.63ms
1981/2072 src/utils/organization/TODO_unknown.ts 1.04ms
1982/2072 src/utils/organization/TODO_USE.ts 1.50ms
1983/2072 src/utils/parameters/extractParameterNames.test.ts 2.56ms
1984/2072 src/utils/parameters/extractParameterNames.ts 1.77ms
1985/2072 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 2.89ms
1986/2072 src/utils/parameters/mapAvailableToExpectedParameters.ts 4.83ms
1987/2072 src/utils/parameters/numberToString.test.ts 1.55ms
1988/2072 src/utils/parameters/numberToString.ts 1.74ms
1989/2072 src/utils/parameters/templateParameters.test.ts 4.56ms
1990/2072 src/utils/parameters/templateParameters.ts 4.00ms
1991/2072 src/utils/parameters/valueToString.test.ts 1.91ms
1992/2072 src/utils/parameters/valueToString.ts 2.50ms
1993/2072 src/utils/random/$generateBookBoilerplate.test.ts 1.91ms
1994/2072 src/utils/random/$generateBookBoilerplate.ts 4.86ms
1995/2072 src/utils/random/$randomAgentPersona.ts 3.49ms
1996/2072 src/utils/random/$randomAgentRule.ts 2.70ms
1997/2072 src/utils/random/$randomBase58.ts 2.91ms
1998/2072 src/utils/random/$randomFullnameWithColor.ts 10.51ms
1999/2072 src/utils/random/$randomItem.ts 2.23ms
2000/2072 src/utils/random/$randomSeed.ts 2.45ms
2001/2072 src/utils/random/$randomToken.ts 2.80ms
2002/2072 src/utils/random/CzechNamePool.ts 14.38ms
2003/2072 src/utils/random/EnglishNamePool.ts 7.30ms
2004/2072 src/utils/random/getNamePool.ts 4.26ms
2005/2072 src/utils/random/NamePool.ts 1.97ms
2006/2072 src/utils/serialization/$deepFreeze.ts 4.10ms
2007/2072 src/utils/serialization/asSerializable.test.ts 1.56ms
2008/2072 src/utils/serialization/asSerializable.ts 2.81ms
2009/2072 src/utils/serialization/checkSerializableAsJson.ts 8.65ms
2010/2072 src/utils/serialization/clonePipeline.ts 3.03ms
2011/2072 src/utils/serialization/deepClone.test.ts 2.44ms
2012/2072 src/utils/serialization/deepClone.ts 3.04ms
2013/2072 src/utils/serialization/exportJson.ts 3.46ms
2014/2072 src/utils/serialization/isSerializableAsJson.test.ts 3.24ms
2015/2072 src/utils/serialization/isSerializableAsJson.ts 2.45ms
2016/2072 src/utils/serialization/jsonStringsToJsons.test.ts 3.34ms
2017/2072 src/utils/serialization/jsonStringsToJsons.ts 2.40ms
2018/2072 src/utils/serialization/serializeToPromptbookJavascript.test.ts 2.87ms
2019/2072 src/utils/serialization/serializeToPromptbookJavascript.ts 4.37ms
2020/2072 src/utils/sets/difference.test.ts 2.55ms
2021/2072 src/utils/sets/difference.ts 2.49ms
2022/2072 src/utils/sets/intersection.test.ts 1.80ms
2023/2072 src/utils/sets/intersection.ts 2.13ms
2024/2072 src/utils/sets/union.test.ts 1.66ms
2025/2072 src/utils/sets/union.ts 1.99ms
2026/2072 src/utils/take/classes/TakeChain.ts 2.10ms
2027/2072 src/utils/take/interfaces/ITakeChain.ts 2.22ms
2028/2072 src/utils/take/interfaces/Takeable.ts 1.79ms
2029/2072 src/utils/take/take.test.ts 4.78ms
2030/2072 src/utils/take/take.ts 2.44ms
2031/2072 src/utils/toolCalls/getToolCallIdentity.ts 2.20ms
2032/2072 src/utils/toolCalls/mergeToolCalls.ts 8.43ms
2033/2072 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 2.86ms
2034/2072 src/utils/validators/email/isValidEmail.test.ts 1.67ms
2035/2072 src/utils/validators/email/isValidEmail.ts 2.45ms
2036/2072 src/utils/validators/filePath/isRootPath.test.ts 2.59ms
2037/2072 src/utils/validators/filePath/isRootPath.ts 2.14ms
2038/2072 src/utils/validators/filePath/isValidFilePath.test.ts 5.63ms
2039/2072 src/utils/validators/filePath/isValidFilePath.ts 3.32ms
2040/2072 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 1.86ms
2041/2072 src/utils/validators/javascriptName/isValidJavascriptName.ts 2.09ms
2042/2072 src/utils/validators/parameterName/validateParameterName.test.ts 2.99ms
2043/2072 src/utils/validators/parameterName/validateParameterName.ts 3.50ms
2044/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 1.66ms
2045/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 1.89ms
2046/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 1.54ms
2047/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 1.95ms
2048/2072 src/utils/validators/url/extractUrlsFromText.test.ts 1.96ms
2049/2072 src/utils/validators/url/extractUrlsFromText.ts 3.50ms
2050/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 2.26ms
2051/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 3.68ms
2052/2072 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 1.58ms
2053/2072 src/utils/validators/url/isUrlOnPrivateNetwork.ts 2.01ms
2054/2072 src/utils/validators/url/isValidAgentUrl.test.ts 1.73ms
2055/2072 src/utils/validators/url/isValidAgentUrl.ts 2.35ms
2056/2072 src/utils/validators/url/isValidPipelineUrl.test.ts 1.95ms
2057/2072 src/utils/validators/url/isValidPipelineUrl.ts 2.12ms
2058/2072 src/utils/validators/url/isValidUrl.test.ts 2.26ms
2059/2072 src/utils/validators/url/isValidUrl.ts 2.37ms
2060/2072 src/utils/validators/url/normalizeDomainForMatching.test.ts 1.74ms
2061/2072 src/utils/validators/url/normalizeDomainForMatching.ts 2.82ms
2062/2072 src/utils/validators/uuid/isValidUuid.test.ts 1.96ms
2063/2072 src/utils/validators/uuid/isValidUuid.ts 1.55ms
2064/2072 src/version.ts 1.84ms
2065/2072 src/wizard/$getCompiledBook.ts 9.33ms
2066/2072 src/wizard/test/books/test.book.md 1.43ms
2067/2072 src/wizard/test/README.md 1.42ms
2068/2072 src/wizard/test/sub/books/test.book.md 1.31ms
2069/2072 src/wizard/test/sub/subsub/books/test.book.md 1.18ms
2070/2072 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.19ms
2071/2072 src/wizard/test/sub/subsub/subsubsub/README.md 1.05ms
2072/2072 src/wizard/wizard.ts 6.93ms
CSpell: Files checked: 2072, Issues found: 0 in 0 files.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Type-checking, linting, spellchecking, and the focused Jest set pass. I’m running the required full `npm test` suite now; I’ll report any unrelated repository failure separately if one appears.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npm test' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' } | Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List" in C:\Users\me\work\ai\promptbook
 succeeded in 825ms:

ProcessId       : 24232
ParentProcessId : 39716
Name            : cmd.exe
CommandLine     : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && 
                  npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run 
                  test-types && npm run test-books && npm run test-book-components-build && npm run 
                  test-package-generation && npm run test-unit && npm run test-app-agents-server && echo "🎉 All tests 
                  passed!"

ProcessId       : 22968
ParentProcessId : 50108
Name            : cmd.exe
CommandLine     : C:\WINDOWS\system32\cmd.exe /d /s /c eslint src

ProcessId       : 43396
ParentProcessId : 22968
Name            : node.exe
CommandLine     : "node"   "C:\Users\me\work\ai\promptbook\node_modules\.bin\\..\eslint\bin\eslint.js" src


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31011ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 23672
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-package-generation


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The full suite is still progressing normally: name checks, spellcheck, and lint have completed, and it has moved on to package generation.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31049ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31613ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Package generation completed and the suite is now in the repository-wide Jest phase. No failures have surfaced so far.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31758ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31539ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The full suite remains active in Jest; it has not reported an error. I’m letting it finish rather than substituting the focused run for the required project check.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31022ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31762ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The test run is still in the unit-suite stage and continues to hold active Jest workers. The earlier targeted tests already cover this change specifically; I’m keeping the full suite running to completion.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 52210ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 3424
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node -r ./scripts/ignore-kill-eperm.js 
              ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 35140
Name        : node.exe
CommandLine : node  -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

ProcessId   : 4328
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js

ProcessId   : 38168
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r ./scripts/ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\jest-worker\build\workers\processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process -Id 35140,4328,38168 | Select-Object Id, ProcessName, CPU, StartTime, Responding | Format-Table -AutoSize' in C:\Users\me\work\ai\promptbook
 succeeded in 1296ms:

   Id ProcessName    CPU StartTime             Responding
   -- -----------    --- ---------             ----------
 4328 node          2.67 9/12/2026 11:40:23 AM       True
35140 node          5.88 9/12/2026 11:40:21 AM       True
38168 node        115.53 9/12/2026 11:40:24 AM       True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51916ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Repository-wide Jest has completed; the final Agents Server test stage is running now. No failure has stopped the command, so the suite remains on track.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 31130ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 30839ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 30975ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The final application tests are still running. The suite has already passed the parts that compile, lint, type-check, and exercise the new routing logic.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51442ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process -Id 44392 | Select-Object Id, ProcessName, CPU, StartTime, Responding | Format-Table -AutoSize' in C:\Users\me\work\ai\promptbook
 succeeded in 756ms:

   Id ProcessName    CPU StartTime             Responding
   -- -----------    --- ---------             ----------
44392 node        231.17 9/12/2026 11:46:29 AM       True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51551ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/package.json | Select-Object -First 100; Get-Process -Id 44392 | Select-Object Id, CPU, StartTime | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 605ms:
{
    "name": "promptbook-agents-server",
    "scripts": {
        "generate-reserved-paths": "ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts",
        "prebuild": "npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440",
        "predev": "npm run generate-reserved-paths && node ./scripts/kill-port.js 4440",
        "dev": "next dev -p 4440",
        "test": "npm run test-e2e",
        "test-e2e": "node ./scripts/run-e2e-tests.js",
        "pretest-build": "node ./scripts/kill-port.js 4021 4440",
        "test-build": "npm run build",
        "pretest-e2e": "node ./scripts/kill-port.js 4021 4440",
        "build": "node ./scripts/build-agents-server.js",
        "build-e2e": "node ./scripts/build-e2e.js",
        "start": "next start -p 4440",
        "lint": "eslint src/ --max-warnings=0 --cache --cache-location ./node_modules/.cache/eslint/agents-server-src.json",
        "postinstall": "node ./scripts/ensure-root-dependencies.js"
    }
}

Id        : 44392
CPU       : 310.921875
StartTime : 9/12/2026 11:46:29 AM


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content apps/agents-server/scripts/run-e2e-tests.js | Select-Object -First 300' in C:\Users\me\work\ai\promptbook
 succeeded in 610ms:
'use strict';

const path = require('node:path');
const { runNpm, startNpm, startNpmWithCollectedOutput } = require('./run-npm.js');

/**
 * Root directory of the Agents Server app.
 */
const APP_ROOT = path.resolve(__dirname, '..');

/**
 * Runs the complete Agents Server E2E verification flow.
 *
 * App linting remains an explicit test phase, but it reads only the sources and therefore does not
 * depend on the production build. Both run at the same time and the flow waits for the slower one,
 * which is always the build. The build then skips its duplicate lint pass, and Playwright starts
 * that same build instead of compiling a second `.next-e2e` output through `webServer`.
 */
async function runE2eTests() {
    const commandOptions = {
        cwd: APP_ROOT,
    };

    const lintCompletion = startNpmWithCollectedOutput(['run', 'lint'], commandOptions);
    const buildStatus = await startNpm(['run', 'build-e2e', '--', '--no-lint'], commandOptions);
    const lintResult = await lintCompletion;

    console.log('\n> lint (ran next to the production build)\n');
    process.stdout.write(lintResult.output);

    if (lintResult.status !== 0) {
        process.exit(lintResult.status);
    }

    if (buildStatus !== 0) {
        process.exit(buildStatus);
    }

    runNpm(['exec', '--', 'playwright', 'test', ...process.argv.slice(2)], {
        ...commandOptions,
        environmentOverrides: {
            PTBK_E2E_BUILD_READY: 'true',
        },
    });
}

void runE2eTests();

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The final stage is the Agents Server end-to-end flow, which builds the app and then runs Playwright. It is CPU-active and has not failed; this is the expected slower part of `npm test`.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 50839ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 44392
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process -Id 44392 | Select-Object Id, CPU, StartTime | Format-List; Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 44392 } | Select-Object ProcessId, Name, CommandLine | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 9375ms:

Id        : 44392
CPU       : 425.015625
StartTime : 9/12/2026 11:46:29 AM


ProcessId   : 21892
Name        : sentry-cli.exe
CommandLine : C:\Users\me\work\ai\promptbook\node_modules\@sentry\cli-win32-x64\bin\sentry-cli.exe --header 
              sentry-trace:5595a06c429342bf986ac4f2610298e2-bc04012085382876 releases new 
              c4ff44007f2682383381b92cc546ff70b25e9734


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 55374ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 48648
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The build phase completed and the final test stage has moved into its Playwright workers. It is still clean so far.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51670ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 48648
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51662ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 26240
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Playwright is still cycling through its test workers; the complete suite has not failed or exited early.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 51448ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 50781ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 39108
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The full run is continuing through the Agents Server end-to-end tests. I’ll keep monitoring it and then do the final diff review once it exits.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 53189ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server

ProcessId   : 39360
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 24468
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 50804
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 27980
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 46152
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 22244
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 32432
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 45768
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 50460
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 16588
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js

ProcessId   : 45520
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe -r 
              C:\Users\me\work\ai\promptbook\scripts\ignore-kill-eperm.js 
              C:\Users\me\work\ai\promptbook\node_modules\next\dist\compiled\jest-worker/processChild.js


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 53156ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 20; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
 succeeded in 23062ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 49280 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 49280 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 6645ms:

ProcessId   : 43928
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c (cd ./apps/agents-server/ && npm run test)


Id         : 49280
CPU        : 0.546875
StartTime  : 9/12/2026 11:46:17 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 43928 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 43928 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 3612ms:

ProcessId   : 40048
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test


Id         : 43928
CPU        : 0.046875
StartTime  : 9/12/2026 11:46:18 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 40048 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 40048 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 3354ms:

ProcessId   : 49056
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c npm run test-e2e


Id         : 40048
CPU        : 0.40625
StartTime  : 9/12/2026 11:46:18 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 49056 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 49056 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 1451ms:

ProcessId   : 29156
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-e2e


Id         : 49056
CPU        : 0.015625
StartTime  : 9/12/2026 11:46:18 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 29156 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 29156 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 4858ms:

ProcessId   : 42068
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c node ./scripts/run-e2e-tests.js


Id         : 29156
CPU        : 0.46875
StartTime  : 9/12/2026 11:46:18 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 42068 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 42068 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 3491ms:

ProcessId   : 25484
Name        : node.exe
CommandLine : node  ./scripts/run-e2e-tests.js


Id         : 42068
CPU        : 0.015625
StartTime  : 9/12/2026 11:46:19 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 25484 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 25484 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 2246ms:

ProcessId   : 36456
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c "npm.cmd exec -- playwright test"


Id         : 25484
CPU        : 0.09375
StartTime  : 9/12/2026 11:46:19 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 36456 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 36456 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 5014ms:

ProcessId   : 50552
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" exec -- playwright test


Id         : 36456
CPU        : 0.03125
StartTime  : 9/12/2026 11:57:56 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 50552 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 50552 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 8112ms:

ProcessId   : 25604
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c playwright test


Id         : 50552
CPU        : 1.484375
StartTime  : 9/12/2026 11:57:58 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 53758ms:

ProcessId   : 24232
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c echo "Note: Running from most lightweight to heaviest tests" && npm 
              run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm 
              run test-books && npm run test-book-components-build && npm run test-package-generation && npm run 
              test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

ProcessId   : 49280
Name        : node.exe
CommandLine : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\\node.exe"  
              "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node_modules\npm\bin\npm-cli.js" run test-app-agents-server


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process -Id 50552 -ErrorAction SilentlyContinue | Select-Object Id, CPU, StartTime, Responding | Format-List; Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 25604 } | Select-Object ProcessId, Name, CommandLine | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 5477ms:

Id         : 50552
CPU        : 1.484375
StartTime  : 9/12/2026 11:57:58 AM
Responding : True


ProcessId   : 47296
Name        : node.exe
CommandLine : "node"   "C:\Users\me\work\ai\promptbook\node_modules\.bin\\..\@playwright\test\cli.js" test


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.ParentProcessId -eq 47296 } | Select-Object ProcessId, Name, CommandLine | Format-List; Get-Process -Id 47296 | Select-Object Id, CPU, StartTime, Responding | Format-List' in C:\Users\me\work\ai\promptbook
 succeeded in 3863ms:

ProcessId   : 14088
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c "node ./tests/e2e/mockSupabaseServer.cjs"

ProcessId   : 25200
Name        : cmd.exe
CommandLine : C:\WINDOWS\system32\cmd.exe /d /s /c "npm run start"

ProcessId   : 18888
Name        : node.exe
CommandLine : C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe 
              C:\Users\me\work\ai\promptbook\node_modules\playwright\lib\worker\workerProcessEntry.js


Id         : 47296
CPU        : 1.671875
StartTime  : 9/12/2026 11:58:13 AM
Responding : True


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The end-to-end server and mock backend are now running under Playwright. This is the expected live verification phase, and it is still progressing without an error.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 50; $activeTestProcess = Get-CimInstance Win32_Process | Where-Object { $_.Name -match '"'node|cmd' -and "'$_.CommandLine -match '"'npm test|jest|eslint|tsc|test-package-generation|test-app-agents-server' }; if ("'$null -eq $activeTestProcess) { '"'npm test process has finished' } else { "'$activeTestProcess | Select-Object ProcessId, Name, CommandLine | Format-List }' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 exited 1 in 1445704ms:

> promptbook@0.114.0-33 test
> echo "Note: Running from most lightweight to heaviest tests" && npm run test-name-discrepancies && npm run test-spellcheck && npm run test-lint && npm run test-types && npm run test-books && npm run test-book-components-build && npm run test-package-generation && npm run test-unit && npm run test-app-agents-server && echo "🎉 All tests passed!"

"Note: Running from most lightweight to heaviest tests" 

> promptbook@0.114.0-33 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
[32mNo name discrepancies found[39m

> promptbook@0.114.0-33 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/2072 src/_packages/anthropic-claude.index.ts 835.08ms
   2/2072 src/_packages/anthropic-claude.readme.md 77.53ms
   3/2072 src/_packages/azure-openai.index.ts 4.83ms
   4/2072 src/_packages/azure-openai.readme.md 15.62ms
   5/2072 src/_packages/browser.index.ts 5.57ms
   6/2072 src/_packages/browser.readme.md 12.90ms
   7/2072 src/_packages/cli.index.ts 15.23ms
   8/2072 src/_packages/cli.readme.md 19.82ms
   9/2072 src/_packages/color.index.ts 6.40ms
  10/2072 src/_packages/color.readme.md 10.93ms
  11/2072 src/_packages/components.index.ts 20.72ms
  12/2072 src/_packages/components.readme.md 11.57ms
  13/2072 src/_packages/content/$provideExecutionToolsForNode.md 4.22ms
  14/2072 src/_packages/content/providers.md 1.99ms
  15/2072 src/_packages/core.index.ts 34.94ms
  16/2072 src/_packages/core.readme.md 27.83ms
  17/2072 src/_packages/deepseek.index.ts 3.64ms
  18/2072 src/_packages/deepseek.readme.md 2.38ms
  19/2072 src/_packages/documents.index.ts 2.55ms
  20/2072 src/_packages/documents.readme.md 1.62ms
  21/2072 src/_packages/editable.index.ts 7.58ms
  22/2072 src/_packages/editable.readme.md 3.16ms
  23/2072 src/_packages/fake-llm.index.ts 2.12ms
  24/2072 src/_packages/fake-llm.readme.md 1.36ms
  25/2072 src/_packages/google.index.ts 2.79ms
  26/2072 src/_packages/google.readme.md 7.39ms
  27/2072 src/_packages/javascript.index.ts 2.20ms
  28/2072 src/_packages/javascript.readme.md 3.45ms
  29/2072 src/_packages/legacy-documents.index.ts 2.24ms
  30/2072 src/_packages/legacy-documents.readme.md 1.44ms
  31/2072 src/_packages/markdown-utils.index.ts 5.67ms
  32/2072 src/_packages/markdown-utils.readme.md 9.31ms
  33/2072 src/_packages/markitdown.index.ts 3.52ms
  34/2072 src/_packages/markitdown.readme.md 1.38ms
  35/2072 src/_packages/node.index.ts 5.95ms
  36/2072 src/_packages/node.readme.md 6.57ms
  37/2072 src/_packages/ollama.index.ts 2.82ms
  38/2072 src/_packages/ollama.readme.md 3.11ms
  39/2072 src/_packages/openai.index.ts 5.32ms
  40/2072 src/_packages/openai.readme.md 13.54ms
  41/2072 src/_packages/pdf.index.ts 5.14ms
  42/2072 src/_packages/pdf.readme.md 1.35ms
  43/2072 src/_packages/promptbook.readme.md 1.80ms
  44/2072 src/_packages/ptbk.readme.md 1.27ms
  45/2072 src/_packages/README.md 1.72ms
  46/2072 src/_packages/remote-client.index.ts 2.84ms
  47/2072 src/_packages/remote-client.readme.md 1.24ms
  48/2072 src/_packages/remote-server.index.ts 2.29ms
  49/2072 src/_packages/remote-server.readme.md 3.61ms
  50/2072 src/_packages/templates.index.ts 1.79ms
  51/2072 src/_packages/templates.readme.md 1.53ms
  52/2072 src/_packages/types.index.ts 56.11ms
  53/2072 src/_packages/types.readme.md 23.88ms
  54/2072 src/_packages/utils.index.ts 12.24ms
  55/2072 src/_packages/utils.readme.md 16.45ms
  56/2072 src/_packages/vercel.index.ts 2.63ms
  57/2072 src/_packages/vercel.readme.md 6.17ms
  58/2072 src/_packages/website-crawler.index.ts 2.70ms
  59/2072 src/_packages/website-crawler.readme.md 1.29ms
  60/2072 src/_packages/wizard.index.ts 6.98ms
  61/2072 src/_packages/wizard.readme.md 1.71ms
  62/2072 src/avatars/Avatar.tsx 24.71ms
  63/2072 src/avatars/avatarAnimationScheduler.ts 6.88ms
  64/2072 src/avatars/avatarInteractionUtils.test.ts 6.73ms
  65/2072 src/avatars/avatarInteractionUtils.ts 9.62ms
  66/2072 src/avatars/AvatarOrImage.tsx 4.36ms
  67/2072 src/avatars/avatarPointerTracking.ts 7.25ms
  68/2072 src/avatars/avatarRenderingUtils.test.ts 5.23ms
  69/2072 src/avatars/avatarRenderingUtils.ts 12.67ms
  70/2072 src/avatars/avatarVisibilityTracking.ts 4.76ms
  71/2072 src/avatars/index.ts 2.62ms
  72/2072 src/avatars/renderAvatarVisual.ts 4.49ms
  73/2072 src/avatars/renderAvatarVisualAsciiArt.ts 9.45ms
  74/2072 src/avatars/renderAvatarVisualTerminalText.test.ts 5.31ms
  75/2072 src/avatars/renderAvatarVisualTerminalText.ts 12.79ms
  76/2072 src/avatars/types/AvatarDefinition.ts 2.73ms
  77/2072 src/avatars/types/AvatarVisualDefinition.ts 9.24ms
  78/2072 src/avatars/visuals/asciiOctopusAvatarVisual.ts 38.54ms
  79/2072 src/avatars/visuals/avatar3dProjectionShared.ts 8.61ms
  80/2072 src/avatars/visuals/avatarVisualRegistry.test.ts 3.10ms
  81/2072 src/avatars/visuals/avatarVisualRegistry.ts 4.92ms
  82/2072 src/avatars/visuals/fractalAvatarVisual.ts 12.76ms
  83/2072 src/avatars/visuals/minecraft2AvatarVisual.ts 16.30ms
  84/2072 src/avatars/visuals/minecraftAvatarVisual.ts 8.24ms
  85/2072 src/avatars/visuals/minecraftAvatarVisualShared.ts 7.81ms
  86/2072 src/avatars/visuals/octopus2AvatarVisual.ts 11.68ms
  87/2072 src/avatars/visuals/octopus3AvatarVisual.test.ts 4.38ms
  88/2072 src/avatars/visuals/octopus3AvatarVisual.ts 24.96ms
  89/2072 src/avatars/visuals/octopus3d2AvatarVisual.ts 53.91ms
  90/2072 src/avatars/visuals/octopus3d3AvatarVisual.ts 42.58ms
  91/2072 src/avatars/visuals/octopus3d4AvatarVisual.ts 32.23ms
  92/2072 src/avatars/visuals/octopus3dAvatarVisual.ts 20.89ms
  93/2072 src/avatars/visuals/octopus3dAvatarVisualShared.ts 8.46ms
  94/2072 src/avatars/visuals/octopusAvatarVisual.ts 7.76ms
  95/2072 src/avatars/visuals/octopusAvatarVisualShared.test.ts 9.15ms
  96/2072 src/avatars/visuals/octopusAvatarVisualShared.ts 16.20ms
  97/2072 src/avatars/visuals/orbAvatarVisual.test.ts 3.93ms
  98/2072 src/avatars/visuals/orbAvatarVisual.ts 16.81ms
  99/2072 src/avatars/visuals/pixelArtAvatarVisual.ts 5.67ms
 100/2072 src/book-2.0/agent-source/AgentBasicInformation.ts 5.89ms
 101/2072 src/book-2.0/agent-source/AgentModelRequirements.ts 4.88ms
 102/2072 src/book-2.0/agent-source/AgentReferenceResolver.ts 2.53ms
 103/2072 src/book-2.0/agent-source/AgentSourceParseResult.ts 2.29ms
 104/2072 src/book-2.0/agent-source/agentSourceVisibility.test.ts 2.56ms
 105/2072 src/book-2.0/agent-source/agentSourceVisibility.ts 6.70ms
 106/2072 src/book-2.0/agent-source/BookEditable.ts 4.18ms
 107/2072 src/book-2.0/agent-source/communication-samples.test.ts 4.41ms
 108/2072 src/book-2.0/agent-source/computeAgentHash.test.ts 1.71ms
 109/2072 src/book-2.0/agent-source/computeAgentHash.ts 1.56ms
 110/2072 src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts 3.62ms
 111/2072 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 7.14ms
 112/2072 src/book-2.0/agent-source/createAgentModelRequirements.ts 7.44ms
 113/2072 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 4.77ms
 114/2072 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 3.08ms
 115/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 3.04ms
 116/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 2.98ms
 117/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 2.55ms
 118/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 5.44ms
 119/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 2.64ms
 120/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 3.32ms
 121/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 5.55ms
 122/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts 3.31ms
 123/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts 7.99ms
 124/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts 14.89ms
 125/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements.ts 6.28ms
 126/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources.ts 6.95ms
 127/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/ParsedAgentSourceWithCommitments.ts 2.44ms
 128/2072 src/book-2.0/agent-source/createCommitmentRegex.ts 3.70ms
 129/2072 src/book-2.0/agent-source/createDefaultAgentName.ts 2.11ms
 130/2072 src/book-2.0/agent-source/createTeamToolName.ts 3.16ms
 131/2072 src/book-2.0/agent-source/extractMetaLinks.ts 2.35ms
 132/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 2.90ms
 133/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 2.90ms
 134/2072 src/book-2.0/agent-source/getBookCommitmentLineType.ts 3.05ms
 135/2072 src/book-2.0/agent-source/normalizeAgentName.test.ts 2.83ms
 136/2072 src/book-2.0/agent-source/normalizeAgentName.ts 2.01ms
 137/2072 src/book-2.0/agent-source/padBook.ts 2.40ms
 138/2072 src/book-2.0/agent-source/parseAgentSource.import.test.ts 2.85ms
 139/2072 src/book-2.0/agent-source/parseAgentSource.test.ts 12.20ms
 140/2072 src/book-2.0/agent-source/parseAgentSource.ts 4.63ms
 141/2072 src/book-2.0/agent-source/parseAgentSource/applyMetaCommitment.ts 7.00ms
 142/2072 src/book-2.0/agent-source/parseAgentSource/consumeConversationSampleCommitment.ts 3.14ms
 143/2072 src/book-2.0/agent-source/parseAgentSource/createCapabilitiesFromCommitment.ts 9.17ms
 144/2072 src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts 1.72ms
 145/2072 src/book-2.0/agent-source/parseAgentSource/extractAgentProfileText.ts 2.51ms
 146/2072 src/book-2.0/agent-source/parseAgentSource/extractInitialMessage.ts 1.94ms
 147/2072 src/book-2.0/agent-source/parseAgentSource/extractParsedAgentProfile.ts 3.60ms
 148/2072 src/book-2.0/agent-source/parseAgentSource/ParseAgentSourceState.ts 1.87ms
 149/2072 src/book-2.0/agent-source/parseAgentSource/ParsedAgentProfile.ts 1.74ms
 150/2072 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 2.84ms
 151/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 3.26ms
 152/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 3.97ms
 153/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 7.05ms
 154/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts 2.91ms
 155/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts 2.50ms
 156/2072 src/book-2.0/agent-source/ParsedUnknownCommitment.ts 2.30ms
 157/2072 src/book-2.0/agent-source/parseParameters.ts 3.84ms
 158/2072 src/book-2.0/agent-source/parseTeamCommitment.ts 6.76ms
 159/2072 src/book-2.0/agent-source/pseudoAgentReferences.ts 8.53ms
 160/2072 src/book-2.0/agent-source/README.md 6.78ms
 161/2072 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 3.19ms
 162/2072 src/book-2.0/agent-source/string_book.ts 4.63ms
 163/2072 src/book-2.0/agent-source/TeammateProfileResolver.ts 3.22ms
 164/2072 src/book-2.0/book-language-documentation/BookLanguageDocumentationExample.ts 1.99ms
 165/2072 src/book-2.0/book-language-documentation/bookLanguageDocumentationExamples.ts 4.61ms
 166/2072 src/book-2.0/book-language-documentation/BookLanguageManualDictionary.ts 6.70ms
 167/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts 5.53ms
 168/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.ts 20.33ms
 169/2072 src/book-2.0/book-language-documentation/englishBookLanguageManualDictionary.ts 13.25ms
 170/2072 src/book-2.0/book-language-documentation/getBookLanguageManualCommitmentGroups.ts 3.75ms
 171/2072 src/book-2.0/book-language-documentation/getBookLanguageManualDictionary.ts 4.33ms
 172/2072 src/book-2.0/book-language-documentation/getSafeCodeBlock.ts 2.15ms
 173/2072 src/book-2.0/book-language-documentation/renderCommitmentCatalogSection.ts 3.31ms
 174/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts 4.07ms
 175/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.ts 11.41ms
 176/2072 src/book-2.0/book-language-documentation/toStableAnchorId.ts 2.15ms
 177/2072 src/book-2.0/README.md 1.26ms
 178/2072 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 2.56ms
 179/2072 src/book-3.0/agentFolderPaths.ts 2.56ms
 180/2072 src/book-3.0/AgentMessageRunReport.test.ts 5.04ms
 181/2072 src/book-3.0/AgentMessageRunReport.ts 9.57ms
 182/2072 src/book-3.0/AgentPlannedMessagesSidecar.test.ts 4.81ms
 183/2072 src/book-3.0/AgentPlannedMessagesSidecar.ts 10.06ms
 184/2072 src/book-3.0/AgentTeamConversationWorkspace.ts 5.88ms
 185/2072 src/book-3.0/Book.test.ts 3.63ms
 186/2072 src/book-3.0/Book.ts 6.67ms
 187/2072 src/book-3.0/BookNodeAgentSource.ts 4.61ms
 188/2072 src/book-3.0/CliAgent.test.ts 4.78ms
 189/2072 src/book-3.0/CliAgent.ts 7.96ms
 190/2072 src/book-3.0/cliAgentEnv.ts 2.49ms
 191/2072 src/book-3.0/codexLoginMethod.ts 3.89ms
 192/2072 src/book-3.0/createAgentMessageSidecarBaseName.ts 2.51ms
 193/2072 src/book-3.0/describeAgentPlannedMessageSchedule.ts 4.31ms
 194/2072 src/book-3.0/LiteAgent.test.ts 5.08ms
 195/2072 src/book-3.0/LiteAgent.ts 13.79ms
 196/2072 src/book-components/_common/Dropdown/Dropdown.tsx 3.36ms
 197/2072 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 3.03ms
 198/2072 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 2.90ms
 199/2072 src/book-components/_common/Modal/Modal.tsx 2.36ms
 200/2072 src/book-components/_common/MonacoEditorWithShadowDom.tsx 2.93ms
 201/2072 src/book-components/_common/react-utils/classNames.ts 1.45ms
 202/2072 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 2.64ms
 203/2072 src/book-components/_common/react-utils/escapeRegex.tsx 1.56ms
 204/2072 src/book-components/_common/Tooltip/Tooltip.tsx 6.32ms
 205/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 3.32ms
 206/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 2.47ms
 207/2072 src/book-components/AvatarProfile/AvatarChip/index.ts 1.32ms
 208/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 4.56ms
 209/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 2.52ms
 210/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 2.43ms
 211/2072 src/book-components/AvatarProfile/README.md 0.99ms
 212/2072 src/book-components/BookEditor/BookEditor.tsx 10.04ms
 213/2072 src/book-components/BookEditor/BookEditorAboutPromptbookInformation.tsx 3.33ms
 214/2072 src/book-components/BookEditor/BookEditorActionbar.tsx 7.77ms
 215/2072 src/book-components/BookEditor/BookEditorBrowserConfig.ts 1.48ms
 216/2072 src/book-components/BookEditor/BookEditorForClient.tsx 3.58ms
 217/2072 src/book-components/BookEditor/BookEditorMonaco.tsx 16.23ms
 218/2072 src/book-components/BookEditor/BookEditorMonacoConstants.ts 1.80ms
 219/2072 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 2.90ms
 220/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 2.52ms
 221/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 12.27ms
 222/2072 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 6.52ms
 223/2072 src/book-components/BookEditor/BookEditorSyntaxTokenStyles.ts 3.48ms
 224/2072 src/book-components/BookEditor/BookEditorTheme.ts 2.22ms
 225/2072 src/book-components/BookEditor/createBookSourceHighlightStyles.ts 2.94ms
 226/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.browser.ts 1.54ms
 227/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 4.98ms
 228/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 4.50ms
 229/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts 3.09ms
 230/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.ts 6.28ms
 231/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts 3.34ms
 232/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.ts 3.42ms
 233/2072 src/book-components/BookEditor/highlightBookSourceToHtml.ts 8.64ms
 234/2072 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 6.50ms
 235/2072 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 5.12ms
 236/2072 src/book-components/BookEditor/useBookEditorMonacoInteractions.ts 10.91ms
 237/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 9.59ms
 238/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 16.67ms
 239/2072 src/book-components/BookEditor/useBookEditorMonacoLifecycle.ts 8.24ms
 240/2072 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 5.34ms
 241/2072 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 6.98ms
 242/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/bookEditorMonacoUploadTypes.ts 6.57ms
 243/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/clearScheduledTimer.ts 8.11ms
 244/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/enqueueFilesForUpload.ts 9.22ms
 245/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadEditorSync.ts 7.63ms
 246/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadItemsState.ts 7.98ms
 247/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadProgressQueue.ts 5.48ms
 248/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadQueue.ts 13.02ms
 249/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useCompletedUploadsAutoClear.ts 3.37ms
 250/2072 src/book-components/Chat/AgentChat/AgentChat.test.tsx 9.26ms
 251/2072 src/book-components/Chat/AgentChat/AgentChat.tsx 7.02ms
 252/2072 src/book-components/Chat/AgentChat/AgentChatProps.tsx 3.20ms
 253/2072 src/book-components/Chat/AgentChip/AgentChip.tsx 5.56ms
 254/2072 src/book-components/Chat/AgentChip/index.ts 1.52ms
 255/2072 src/book-components/Chat/Chat/Chat.tsx 21.68ms
 256/2072 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 6.49ms
 257/2072 src/book-components/Chat/Chat/ChatActionsBar.tsx 9.05ms
 258/2072 src/book-components/Chat/Chat/ChatCitationModal.test.tsx 4.61ms
 259/2072 src/book-components/Chat/Chat/ChatCitationModal.tsx 9.87ms
 260/2072 src/book-components/Chat/Chat/chatCssClassNames.ts 4.91ms
 261/2072 src/book-components/Chat/Chat/ChatFeedbackStar.tsx 4.85ms
 262/2072 src/book-components/Chat/Chat/ChatImageAttachmentModal.tsx 4.87ms
 263/2072 src/book-components/Chat/Chat/ChatInputArea.test.tsx 9.23ms
 264/2072 src/book-components/Chat/Chat/ChatInputArea.tsx 30.55ms
 265/2072 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 8.25ms
 266/2072 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 1.58ms
 267/2072 src/book-components/Chat/Chat/ChatMessageAttachments.tsx 7.35ms
 268/2072 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 19.91ms
 269/2072 src/book-components/Chat/Chat/ChatMessageItem.tsx 46.17ms
 270/2072 src/book-components/Chat/Chat/ChatMessageList.tsx 11.22ms
 271/2072 src/book-components/Chat/Chat/ChatMessageMap.tsx 16.23ms
 272/2072 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 5.62ms
 273/2072 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 8.42ms
 274/2072 src/book-components/Chat/Chat/ChatProps.tsx 34.32ms
 275/2072 src/book-components/Chat/Chat/ChatRatingModal.test.tsx 5.38ms
 276/2072 src/book-components/Chat/Chat/ChatRatingModal.tsx 11.09ms
 277/2072 src/book-components/Chat/Chat/ChatReplyPreview.tsx 3.17ms
 278/2072 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 5.85ms
 279/2072 src/book-components/Chat/Chat/ChatSoundToggle.tsx 9.15ms
 280/2072 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 10.58ms
 281/2072 src/book-components/Chat/Chat/ChatToolCallModal.tsx 9.88ms
 282/2072 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 4.89ms
 283/2072 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 5.44ms
 284/2072 src/book-components/Chat/Chat/CitationIframePreview.test.tsx 4.02ms
 285/2072 src/book-components/Chat/Chat/CitationIframePreview.tsx 5.53ms
 286/2072 src/book-components/Chat/Chat/ClockIcon.tsx 3.45ms
 287/2072 src/book-components/Chat/Chat/constants.tsx 2.15ms
 288/2072 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 12.50ms
 289/2072 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 4.42ms
 290/2072 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 5.91ms
 291/2072 src/book-components/Chat/Chat/insertDictationChunk.test.ts 3.17ms
 292/2072 src/book-components/Chat/Chat/insertDictationChunk.ts 2.90ms
 293/2072 src/book-components/Chat/Chat/learnDictationDictionary.test.ts 1.59ms
 294/2072 src/book-components/Chat/Chat/learnDictationDictionary.ts 3.40ms
 295/2072 src/book-components/Chat/Chat/pagePreview/createPagePreviewSessionId.ts 2.69ms
 296/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx 8.44ms
 297/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.tsx 21.26ms
 298/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreviewToolbar.tsx 6.88ms
 299/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts 3.28ms
 300/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.ts 6.46ms
 301/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputEvent.ts 3.20ms
 302/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts 3.92ms
 303/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.ts 6.45ms
 304/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewSessionState.ts 1.87ms
 305/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewViewport.ts 2.42ms
 306/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts 2.27ms
 307/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 4.47ms
 308/2072 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 17.61ms
 309/2072 src/book-components/Chat/Chat/renderAgentProjectDiff.tsx 4.21ms
 310/2072 src/book-components/Chat/Chat/renderAgentProjectToolCallDetails.tsx 9.99ms
 311/2072 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 4.54ms
 312/2072 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 13.17ms
 313/2072 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 4.67ms
 314/2072 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 14.88ms
 315/2072 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 5.81ms
 316/2072 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 7.28ms
 317/2072 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 13.51ms
 318/2072 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 6.36ms
 319/2072 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 3.43ms
 320/2072 src/book-components/Chat/Chat/renderToolCallDetails.tsx 18.73ms
 321/2072 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 3.07ms
 322/2072 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 7.23ms
 323/2072 src/book-components/Chat/Chat/resolveChatRatingMessageKey.ts 1.51ms
 324/2072 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 10.17ms
 325/2072 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 2.99ms
 326/2072 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 2.29ms
 327/2072 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 3.97ms
 328/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx 5.35ms
 329/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 16.72ms
 330/2072 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 6.68ms
 331/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx 4.06ms
 332/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 16.47ms
 333/2072 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 21.60ms
 334/2072 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 4.72ms
 335/2072 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 4.58ms
 336/2072 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 5.78ms
 337/2072 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 6.62ms
 338/2072 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 4.23ms
 339/2072 src/book-components/Chat/Chat/useChatScrollState.ts 8.81ms
 340/2072 src/book-components/Chat/Chat/useChatToolCallModalState.ts 13.00ms
 341/2072 src/book-components/Chat/Chat/useChatToolCallState.ts 6.11ms
 342/2072 src/book-components/Chat/CodeBlock/CodeBlock.tsx 7.30ms
 343/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 2.04ms
 344/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 2.51ms
 345/2072 src/book-components/Chat/effects/ChatEffectsSystem.tsx 4.41ms
 346/2072 src/book-components/Chat/effects/components/ConfettiEffect.tsx 3.76ms
 347/2072 src/book-components/Chat/effects/components/HeartsEffect.tsx 3.29ms
 348/2072 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 1.78ms
 349/2072 src/book-components/Chat/effects/index.ts 2.45ms
 350/2072 src/book-components/Chat/effects/types/ChatEffect.ts 1.93ms
 351/2072 src/book-components/Chat/effects/types/ChatEffectConfig.ts 1.86ms
 352/2072 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 2.48ms
 353/2072 src/book-components/Chat/effects/types/ChatEffectType.ts 1.56ms
 354/2072 src/book-components/Chat/effects/utils/detectEffects.ts 3.37ms
 355/2072 src/book-components/Chat/hooks/index.ts 1.29ms
 356/2072 src/book-components/Chat/hooks/useChatActionsOverlap.ts 6.63ms
 357/2072 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 6.20ms
 358/2072 src/book-components/Chat/hooks/useChatAutoScroll.ts 15.48ms
 359/2072 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 7.08ms
 360/2072 src/book-components/Chat/hooks/useChatCompleteNotification.ts 5.75ms
 361/2072 src/book-components/Chat/hooks/useChatRatings.ts 12.85ms
 362/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx 3.47ms
 363/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.ts 4.84ms
 364/2072 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 3.15ms
 365/2072 src/book-components/Chat/LlmChat/defaults.ts 1.59ms
 366/2072 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 1.46ms
 367/2072 src/book-components/Chat/LlmChat/LlmChat.test.tsx 10.77ms
 368/2072 src/book-components/Chat/LlmChat/LlmChat.tsx 4.01ms
 369/2072 src/book-components/Chat/LlmChat/LlmChatProps.tsx 7.57ms
 370/2072 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 18.50ms
 371/2072 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 3.98ms
 372/2072 src/book-components/Chat/LlmChat/useLlmChatState.ts 11.15ms
 373/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 9.05ms
 374/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 14.15ms
 375/2072 src/book-components/Chat/MockedChat/constants.ts 3.37ms
 376/2072 src/book-components/Chat/MockedChat/MockedChat.tsx 19.06ms
 377/2072 src/book-components/Chat/README.md 6.20ms
 378/2072 src/book-components/Chat/save/_common/chatExportRendering.ts 8.42ms
 379/2072 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 3.58ms
 380/2072 src/book-components/Chat/save/_common/ChatSaveFormatHandler.ts 2.63ms
 381/2072 src/book-components/Chat/save/_common/createChatExportFilename.ts 2.42ms
 382/2072 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 2.61ms
 383/2072 src/book-components/Chat/save/_common/getPromptbookExportBranding.ts 4.42ms
 384/2072 src/book-components/Chat/save/_common/string_chat_format_name.ts 2.28ms
 385/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts 13.49ms
 386/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 21.47ms
 387/2072 src/book-components/Chat/save/index.ts 3.27ms
 388/2072 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 2.46ms
 389/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts 4.80ms
 390/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 6.98ms
 391/2072 src/book-components/Chat/save/pdf/buildChatPdf.ts 12.41ms
 392/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts 9.34ms
 393/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 2.25ms
 394/2072 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 5.30ms
 395/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts 2.81ms
 396/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 4.26ms
 397/2072 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.05ms
 398/2072 src/book-components/Chat/SourceChip/index.ts 1.62ms
 399/2072 src/book-components/Chat/SourceChip/SourceChip.test.tsx 2.92ms
 400/2072 src/book-components/Chat/SourceChip/SourceChip.tsx 8.57ms
 401/2072 src/book-components/Chat/types/ChatMessage.ts 11.35ms
 402/2072 src/book-components/Chat/types/ChatParticipant.ts 3.81ms
 403/2072 src/book-components/Chat/types/CitationLabelResolver.ts 1.98ms
 404/2072 src/book-components/Chat/utils/$provideServerDomWindow.ts 10.65ms
 405/2072 src/book-components/Chat/utils/addUtmParamsToUrl.ts 2.69ms
 406/2072 src/book-components/Chat/utils/agentProjectToolCall.test.ts 10.26ms
 407/2072 src/book-components/Chat/utils/agentProjectToolCall.ts 15.83ms
 408/2072 src/book-components/Chat/utils/ChatPersistence.test.ts 4.60ms
 409/2072 src/book-components/Chat/utils/ChatPersistence.ts 6.53ms
 410/2072 src/book-components/Chat/utils/citationHelpers.test.ts 6.09ms
 411/2072 src/book-components/Chat/utils/citationHelpers.ts 5.33ms
 412/2072 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 6.88ms
 413/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 3.90ms
 414/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 10.51ms
 415/2072 src/book-components/Chat/utils/createShortLinkForChat.ts 1.76ms
 416/2072 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 2.15ms
 417/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts 2.29ms
 418/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.ts 6.63ms
 419/2072 src/book-components/Chat/utils/downloadFile.ts 2.10ms
 420/2072 src/book-components/Chat/utils/exportChatHistory.ts 4.25ms
 421/2072 src/book-components/Chat/utils/ExportFormat.ts 1.37ms
 422/2072 src/book-components/Chat/utils/externalSourceToolCall.ts 3.90ms
 423/2072 src/book-components/Chat/utils/formatToolCallDateTime.ts 4.75ms
 424/2072 src/book-components/Chat/utils/formatToolCallLocalTime.ts 2.51ms
 425/2072 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 2.02ms
 426/2072 src/book-components/Chat/utils/generatePdfContent.ts 2.66ms
 427/2072 src/book-components/Chat/utils/generateQrDataUrl.ts 1.79ms
 428/2072 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 4.05ms
 429/2072 src/book-components/Chat/utils/getPromptbookBranding.ts 1.84ms
 430/2072 src/book-components/Chat/utils/getToolCallChipletInfo.test.ts 3.11ms
 431/2072 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 2.82ms
 432/2072 src/book-components/Chat/utils/getToolCallChipletInfo.ts 16.87ms
 433/2072 src/book-components/Chat/utils/isVisibleChatToolCall.ts 2.09ms
 434/2072 src/book-components/Chat/utils/loadAgentProfile.ts 7.61ms
 435/2072 src/book-components/Chat/utils/messagesToHtml.ts 5.69ms
 436/2072 src/book-components/Chat/utils/messagesToJson.ts 2.61ms
 437/2072 src/book-components/Chat/utils/messagesToMarkdown.ts 3.25ms
 438/2072 src/book-components/Chat/utils/messagesToText.ts 2.49ms
 439/2072 src/book-components/Chat/utils/parseCitationMarker.ts 4.71ms
 440/2072 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 2.67ms
 441/2072 src/book-components/Chat/utils/parseCitationsFromContent.ts 5.46ms
 442/2072 src/book-components/Chat/utils/parseImagePrompts.test.ts 2.35ms
 443/2072 src/book-components/Chat/utils/parseImagePrompts.ts 3.77ms
 444/2072 src/book-components/Chat/utils/parseMessageButtons.test.ts 2.74ms
 445/2072 src/book-components/Chat/utils/parseMessageButtons.ts 3.55ms
 446/2072 src/book-components/Chat/utils/renderMarkdown.test.ts 4.44ms
 447/2072 src/book-components/Chat/utils/renderMarkdown.ts 39.91ms
 448/2072 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 4.64ms
 449/2072 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 2.64ms
 450/2072 src/book-components/Chat/utils/resolveCitationSourceDisplay.ts 21.03ms
 451/2072 src/book-components/Chat/utils/resolveCitationUrl.test.ts 2.08ms
 452/2072 src/book-components/Chat/utils/resolveCitationUrl.ts 4.36ms
 453/2072 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 3.01ms
 454/2072 src/book-components/Chat/utils/resolveToolCallState.ts 2.21ms
 455/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 4.27ms
 456/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 6.85ms
 457/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 3.62ms
 458/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 9.14ms
 459/2072 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 2.78ms
 460/2072 src/book-components/Chat/utils/thinkingMessageVariants.ts 3.49ms
 461/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 4.77ms
 462/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 17.26ms
 463/2072 src/book-components/Chat/utils/toolCallParsing.test.ts 4.58ms
 464/2072 src/book-components/Chat/utils/toolCallParsing.ts 2.63ms
 465/2072 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 4.35ms
 466/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 2.04ms
 467/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 1.90ms
 468/2072 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 10.82ms
 469/2072 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 2.32ms
 470/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 1.81ms
 471/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 1.91ms
 472/2072 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 2.28ms
 473/2072 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 2.97ms
 474/2072 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 1.94ms
 475/2072 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 5.75ms
 476/2072 src/book-components/Chat/utils/walletCredentialToolCall.ts 10.57ms
 477/2072 src/book-components/fonts/README.md 1.21ms
 478/2072 src/book-components/icons/AboutIcon.tsx 2.10ms
 479/2072 src/book-components/icons/ArrowIcon.tsx 3.42ms
 480/2072 src/book-components/icons/AttachmentIcon.tsx 2.14ms
 481/2072 src/book-components/icons/CameraIcon.tsx 2.53ms
 482/2072 src/book-components/icons/CloseIcon.tsx 2.20ms
 483/2072 src/book-components/icons/DownloadIcon.tsx 1.98ms
 484/2072 src/book-components/icons/EmailIcon.tsx 2.53ms
 485/2072 src/book-components/icons/ExitFullscreenIcon.tsx 2.74ms
 486/2072 src/book-components/icons/FullscreenIcon.tsx 2.82ms
 487/2072 src/book-components/icons/MenuIcon.tsx 2.18ms
 488/2072 src/book-components/icons/MicIcon.tsx 2.37ms
 489/2072 src/book-components/icons/PauseIcon.tsx 1.51ms
 490/2072 src/book-components/icons/PlayIcon.tsx 1.43ms
 491/2072 src/book-components/icons/ResetIcon.tsx 2.07ms
 492/2072 src/book-components/icons/SaveIcon.tsx 2.47ms
 493/2072 src/book-components/icons/SendIcon.tsx 1.59ms
 494/2072 src/book-components/icons/SolidArrowButton.tsx 2.87ms
 495/2072 src/book-components/icons/StopIcon.tsx 1.53ms
 496/2072 src/book-components/icons/TeacherIcon.tsx 2.05ms
 497/2072 src/book-components/icons/TemplateIcon.tsx 1.57ms
 498/2072 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 4.50ms
 499/2072 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 19.95ms
 500/2072 src/book-components/Qr/BrandedQrCode.tsx 2.23ms
 501/2072 src/book-components/Qr/GenericQrCode.tsx 1.81ms
 502/2072 src/book-components/Qr/PromptbookQrCode.tsx 11.31ms
 503/2072 src/book-components/Qr/useQrCode.ts 2.93ms
 504/2072 src/cli/$runPromptbookCli.ts 1.98ms
 505/2072 src/cli/cli-commands/_boilerplate.ts 2.76ms
 506/2072 src/cli/cli-commands/about.ts 3.92ms
 507/2072 src/cli/cli-commands/agent-folder.ts 3.33ms
 508/2072 src/cli/cli-commands/agent-folder/agentProjectPaths.ts 4.58ms
 509/2072 src/cli/cli-commands/agent-folder/agentRunCliOptions.ts 3.53ms
 510/2072 src/cli/cli-commands/agent-folder/init.ts 4.14ms
 511/2072 src/cli/cli-commands/agent-folder/initializeAgentProjectConfiguration.ts 4.13ms
 512/2072 src/cli/cli-commands/agent-folder/initializeAgentRunnerCommand.ts 5.29ms
 513/2072 src/cli/cli-commands/agent-folder/printAgentInitializationSummary.ts 4.03ms
 514/2072 src/cli/cli-commands/agent-folder/run.test.ts 7.39ms
 515/2072 src/cli/cli-commands/agent-folder/run.ts 3.51ms
 516/2072 src/cli/cli-commands/agent-folder/runMultiple.ts 4.12ms
 517/2072 src/cli/cli-commands/agent-folder/tick.ts 4.43ms
 518/2072 src/cli/cli-commands/agent.ts 2.71ms
 519/2072 src/cli/cli-commands/agent/agentCliOptions.ts 4.52ms
 520/2072 src/cli/cli-commands/agent/chat.ts 4.69ms
 521/2072 src/cli/cli-commands/agent/exec.ts 4.16ms
 522/2072 src/cli/cli-commands/agent/run.test.ts 5.64ms
 523/2072 src/cli/cli-commands/agents-server.ts 3.56ms
 524/2072 src/cli/cli-commands/agents-server/buildAgentsServer.test.ts 9.43ms
 525/2072 src/cli/cli-commands/agents-server/buildAgentsServer.ts 3.36ms
 526/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_FILENAME.ts 1.47ms
 527/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_VERSION.ts 1.70ms
 528/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts 1.44ms
 529/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCache.ts 1.60ms
 530/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCacheOptions.ts 1.69ms
 531/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerBuildSourceFingerprint.ts 3.69ms
 532/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerRuntimeEnvironment.ts 4.05ms
 533/2072 src/cli/cli-commands/agents-server/buildAgentsServer/ensureAgentsServerBuild.ts 4.06ms
 534/2072 src/cli/cli-commands/agents-server/buildAgentsServer/getAgentsServerBuildInputRelativePaths.ts 1.83ms
 535/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPath.ts 1.76ms
 536/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPathMaterialized.ts 2.28ms
 537/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildCacheCurrent.ts 2.44ms
 538/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildInputTestFile.ts 1.84ms
 539/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputDirectoryName.ts 2.35ms
 540/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputPath.ts 2.52ms
 541/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerRuntimeSourcePath.ts 3.34ms
 542/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isFile.ts 1.92ms
 543/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isPathInsideNodeModules.ts 1.54ms
 544/2072 src/cli/cli-commands/agents-server/buildAgentsServer/NODE_MODULES_DIRECTORY_NAME.ts 1.34ms
 545/2072 src/cli/cli-commands/agents-server/buildAgentsServer/normalizeAgentsServerBuildInputPath.ts 1.59ms
 546/2072 src/cli/cli-commands/agents-server/buildAgentsServer/prepareAgentsServerRuntime.ts 2.87ms
 547/2072 src/cli/cli-commands/agents-server/buildAgentsServer/PreparedAgentsServerRuntime.ts 2.04ms
 548/2072 src/cli/cli-commands/agents-server/buildAgentsServer/readAgentsServerBuildCache.ts 2.50ms
 549/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerAppPath.ts 1.98ms
 550/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildAppPath.ts 2.86ms
 551/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildOutputPath.ts 1.97ms
 552/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNextCliPath.ts 1.83ms
 553/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNodeModulesPath.ts 1.83ms
 554/2072 src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts 6.93ms
 555/2072 src/cli/cli-commands/agents-server/buildAgentsServer/shouldCopyAgentsServerRuntimePath.ts 3.15ms
 556/2072 src/cli/cli-commands/agents-server/buildAgentsServer/synchronizeMaterializedAgentsServerRuntime.ts 7.32ms
 557/2072 src/cli/cli-commands/agents-server/buildAgentsServer/writeAgentsServerBuildCache.ts 2.66ms
 558/2072 src/cli/cli-commands/agents-server/ensureAgentsServerEnvFile.ts 4.56ms
 559/2072 src/cli/cli-commands/agents-server/ensureAgentsServerGitignoreFile.ts 2.82ms
 560/2072 src/cli/cli-commands/agents-server/init.test.ts 6.83ms
 561/2072 src/cli/cli-commands/agents-server/init.ts 2.98ms
 562/2072 src/cli/cli-commands/agents-server/initializeAgentsServerProjectConfiguration.ts 3.15ms
 563/2072 src/cli/cli-commands/agents-server/printAgentsServerInitializationSummary.ts 3.45ms
 564/2072 src/cli/cli-commands/agents-server/run.test.ts 8.77ms
 565/2072 src/cli/cli-commands/agents-server/run.ts 8.12ms
 566/2072 src/cli/cli-commands/agents-server/startAgentsServer.ts 8.47ms
 567/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts 2.89ms
 568/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.ts 3.52ms
 569/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerLogStreams.ts 3.22ms
 570/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerRuntimePaths.ts 3.09ms
 571/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerSupervisorState.ts 3.20ms
 572/2072 src/cli/cli-commands/agents-server/startAgentsServer/createInternalRouteErrorMessage.ts 1.68ms
 573/2072 src/cli/cli-commands/agents-server/startAgentsServer/createLocalAgentRunOptions.ts 3.16ms
 574/2072 src/cli/cli-commands/agents-server/startAgentsServer/forwardChildOutput.ts 2.79ms
 575/2072 src/cli/cli-commands/agents-server/startAgentsServer/loadAgentsServerProjectEnvironment.ts 1.76ms
 576/2072 src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts 1.53ms
 577/2072 src/cli/cli-commands/agents-server/startAgentsServer/prepareAgentsServerLaunch.ts 4.76ms
 578/2072 src/cli/cli-commands/agents-server/startAgentsServer/PreparedAgentsServerLaunch.ts 2.45ms
 579/2072 src/cli/cli-commands/agents-server/startAgentsServer/readInternalRouteErrorDetails.ts 3.51ms
 580/2072 src/cli/cli-commands/agents-server/startAgentsServer/resolveAgentsServerChildHostname.ts 1.63ms
 581/2072 src/cli/cli-commands/agents-server/startAgentsServer/StartAgentsServerOptions.ts 2.51ms
 582/2072 src/cli/cli-commands/agents-server/startAgentsServer/startNextServer.ts 5.84ms
 583/2072 src/cli/cli-commands/agents-server/startAgentsServer/startUserChatJobWorkerPump.ts 6.16ms
 584/2072 src/cli/cli-commands/agents-server/startAgentsServer/stopChildProcess.ts 1.80ms
 585/2072 src/cli/cli-commands/agents-server/startAgentsServer/waitForLocalAgentRunnerLimits.ts 5.68ms
 586/2072 src/cli/cli-commands/coder.ts 4.63ms
 587/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts 4.28ms
 588/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.ts 4.41ms
 589/2072 src/cli/cli-commands/coder/add.test.ts 6.54ms
 590/2072 src/cli/cli-commands/coder/add.ts 14.92ms
 591/2072 src/cli/cli-commands/coder/agentCliOptions.ts 2.20ms
 592/2072 src/cli/cli-commands/coder/agentsFile.ts 2.55ms
 593/2072 src/cli/cli-commands/coder/boilerplateCount.test.ts 2.41ms
 594/2072 src/cli/cli-commands/coder/boilerplateCount.ts 4.62ms
 595/2072 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 11.46ms
 596/2072 src/cli/cli-commands/coder/boilerplateTemplates.ts 11.85ms
 597/2072 src/cli/cli-commands/coder/coderReferencedArtifacts.ts 6.62ms
 598/2072 src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts 6.76ms
 599/2072 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 11.92ms
 600/2072 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 8.05ms
 601/2072 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 3.83ms
 602/2072 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 5.16ms
 603/2072 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 4.16ms
 604/2072 src/cli/cli-commands/coder/ensureDirectory.ts 3.83ms
 605/2072 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 4.74ms
 606/2072 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 4.62ms
 607/2072 src/cli/cli-commands/coder/find-refactor-candidates.ts 4.75ms
 608/2072 src/cli/cli-commands/coder/find-unwritten.ts 4.14ms
 609/2072 src/cli/cli-commands/coder/formatDisplayPath.ts 1.93ms
 610/2072 src/cli/cli-commands/coder/generate-boilerplates.test.ts 5.53ms
 611/2072 src/cli/cli-commands/coder/generate-boilerplates.ts 11.80ms
 612/2072 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 8.53ms
 613/2072 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 3.00ms
 614/2072 src/cli/cli-commands/coder/getTypescriptModule.test.ts 2.75ms
 615/2072 src/cli/cli-commands/coder/getTypescriptModule.ts 2.87ms
 616/2072 src/cli/cli-commands/coder/GitChangesMode.ts 2.78ms
 617/2072 src/cli/cli-commands/coder/init.test.ts 7.34ms
 618/2072 src/cli/cli-commands/coder/init.ts 10.48ms
 619/2072 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 8.54ms
 620/2072 src/cli/cli-commands/coder/list.test.ts 6.11ms
 621/2072 src/cli/cli-commands/coder/list.ts 5.81ms
 622/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 5.00ms
 623/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 9.26ms
 624/2072 src/cli/cli-commands/coder/ping.test.ts 9.28ms
 625/2072 src/cli/cli-commands/coder/ping.ts 7.60ms
 626/2072 src/cli/cli-commands/coder/printCoderRunFailure.ts 2.24ms
 627/2072 src/cli/cli-commands/coder/printInitializationSummary.ts 6.30ms
 628/2072 src/cli/cli-commands/coder/run.test.ts 12.31ms
 629/2072 src/cli/cli-commands/coder/run.ts 15.46ms
 630/2072 src/cli/cli-commands/coder/server.test.ts 6.07ms
 631/2072 src/cli/cli-commands/coder/server.ts 11.82ms
 632/2072 src/cli/cli-commands/coder/ThinkingLevel.ts 3.67ms
 633/2072 src/cli/cli-commands/coder/verify.test.ts 4.96ms
 634/2072 src/cli/cli-commands/coder/verify.ts 4.79ms
 635/2072 src/cli/cli-commands/coder/waitOptions.ts 3.11ms
 636/2072 src/cli/cli-commands/common/$askForConfirmation.test.ts 2.42ms
 637/2072 src/cli/cli-commands/common/$askForConfirmation.ts 2.73ms
 638/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts 3.48ms
 639/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.ts 4.26ms
 640/2072 src/cli/cli-commands/common/createNonNegativeIntegerOptionParser.ts 3.57ms
 641/2072 src/cli/cli-commands/common/createPositiveIntegerOptionParser.ts 2.55ms
 642/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts 3.54ms
 643/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.ts 2.90ms
 644/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts 3.46ms
 645/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.ts 3.55ms
 646/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts 1.74ms
 647/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.ts 2.65ms
 648/2072 src/cli/cli-commands/common/disk-space/formatLowFreeDiskSpaceWarning.ts 3.63ms
 649/2072 src/cli/cli-commands/common/disk-space/freeDiskSpaceConstants.ts 3.11ms
 650/2072 src/cli/cli-commands/common/disk-space/FreeDiskSpaceStatus.ts 2.73ms
 651/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts 1.85ms
 652/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.ts 2.16ms
 653/2072 src/cli/cli-commands/common/handleActionErrors.ts 3.34ms
 654/2072 src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts 5.32ms
 655/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts 4.77ms
 656/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.ts 4.12ms
 657/2072 src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts 4.29ms
 658/2072 src/cli/cli-commands/common/harness/$resolveHarnessCommandPath.ts 3.08ms
 659/2072 src/cli/cli-commands/common/harness/$resolveHarnessInstallationOrigin.ts 5.02ms
 660/2072 src/cli/cli-commands/common/harness/$resolveInstalledHarnessVersion.ts 3.08ms
 661/2072 src/cli/cli-commands/common/harness/$runHarnessInstallationCommand.ts 4.14ms
 662/2072 src/cli/cli-commands/common/harness/buildHarnessInstallCommand.ts 3.36ms
 663/2072 src/cli/cli-commands/common/harness/formatHarnessInstallationWarning.ts 5.17ms
 664/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts 4.23ms
 665/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.ts 3.73ms
 666/2072 src/cli/cli-commands/common/harness/HarnessDefinition.test.ts 5.44ms
 667/2072 src/cli/cli-commands/common/harness/HarnessDefinition.ts 6.94ms
 668/2072 src/cli/cli-commands/common/harness/HarnessInstallationOrigin.ts 3.00ms
 669/2072 src/cli/cli-commands/common/harness/HarnessInstallationStatus.ts 3.23ms
 670/2072 src/cli/cli-commands/common/harness/HarnessUpdatePlan.ts 2.54ms
 671/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts 3.86ms
 672/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.ts 5.55ms
 673/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts 4.35ms
 674/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.ts 3.40ms
 675/2072 src/cli/cli-commands/common/npm/$askForNpmPackageInstallationApproval.ts 2.42ms
 676/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts 3.51ms
 677/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.ts 4.05ms
 678/2072 src/cli/cli-commands/common/npm/$resolveNpmGlobalPrefixPath.ts 3.24ms
 679/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts 2.03ms
 680/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.ts 2.75ms
 681/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts 3.07ms
 682/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.ts 4.08ms
 683/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts 3.24ms
 684/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.ts 6.92ms
 685/2072 src/cli/cli-commands/common/projectInitialization.ts 9.46ms
 686/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts 6.39ms
 687/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.ts 4.44ms
 688/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts 7.32ms
 689/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.ts 7.51ms
 690/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts 5.40ms
 691/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.ts 9.42ms
 692/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts 5.01ms
 693/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.ts 4.07ms
 694/2072 src/cli/cli-commands/common/promptbook-cli/buildPromptbookCliInstallCommand.ts 2.10ms
 695/2072 src/cli/cli-commands/common/promptbook-cli/formatPromptbookCliInstallationWarning.ts 4.41ms
 696/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallation.ts 3.74ms
 697/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallationStatus.ts 2.22ms
 698/2072 src/cli/cli-commands/common/promptPriorityCliOptions.ts 2.33ms
 699/2072 src/cli/cli-commands/common/promptRunnerCliOptions.ts 8.92ms
 700/2072 src/cli/cli-commands/common/questionsCliOptions.test.ts 3.59ms
 701/2072 src/cli/cli-commands/common/questionsCliOptions.ts 4.01ms
 702/2072 src/cli/cli-commands/hello.ts 3.00ms
 703/2072 src/cli/cli-commands/list-models.ts 3.14ms
 704/2072 src/cli/cli-commands/list-scrapers.ts 3.25ms
 705/2072 src/cli/cli-commands/login.ts 3.08ms
 706/2072 src/cli/cli-commands/make.ts 18.68ms
 707/2072 src/cli/cli-commands/prettify.ts 5.56ms
 708/2072 src/cli/cli-commands/run.ts 3.91ms
 709/2072 src/cli/cli-commands/run/prepareRunCommandResources.ts 9.73ms
 710/2072 src/cli/cli-commands/run/resolveRunInputParameters.ts 5.19ms
 711/2072 src/cli/cli-commands/run/runCommandAction.ts 6.36ms
 712/2072 src/cli/cli-commands/run/runPipelineExecution.ts 6.34ms
 713/2072 src/cli/cli-commands/runInteractiveChatbot.ts 36.89ms
 714/2072 src/cli/cli-commands/start-agents-server.ts 10.55ms
 715/2072 src/cli/cli-commands/start-pipelines-server.ts 9.87ms
 716/2072 src/cli/cli-commands/test-command.ts 7.14ms
 717/2072 src/cli/common/$addGlobalOptionsToCommand.ts 2.56ms
 718/2072 src/cli/common/$deprecateCliCommand.test.ts 3.35ms
 719/2072 src/cli/common/$deprecateCliCommand.ts 2.46ms
 720/2072 src/cli/common/$hideCliCommandFromHelp.test.ts 2.21ms
 721/2072 src/cli/common/$hideCliCommandFromHelp.ts 3.14ms
 722/2072 src/cli/common/$provideLlmToolsForCli.ts 6.71ms
 723/2072 src/cli/common/$requireCliSubcommand.test.ts 4.80ms
 724/2072 src/cli/common/$requireCliSubcommand.ts 3.13ms
 725/2072 src/cli/common/loadPromptsModule.ts 1.84ms
 726/2072 src/cli/main.ts 1.61ms
 727/2072 src/cli/other/install.test.ts 1.70ms
 728/2072 src/cli/other/vpsInstall.test.ts 19.95ms
 729/2072 src/cli/promptbookCli.ts 8.94ms
 730/2072 src/cli/test/ptbk.test.ts 8.33ms
 731/2072 src/cli/test/ptbk.ts 1.48ms
 732/2072 src/cli/test/tsconfig.json 2.36ms
 733/2072 src/collection/agent-collection/AgentCollection.ts 3.72ms
 734/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 18.52ms
 735/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 1.79ms
 736/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 6.88ms
 737/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts 3.98ms
 738/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.ts 3.59ms
 739/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 4.05ms
 740/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 6.26ms
 741/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 6.46ms
 742/2072 src/collection/agent-collection/CreateAgentInput.test.ts 2.90ms
 743/2072 src/collection/agent-collection/CreateAgentInput.ts 6.65ms
 744/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 4.49ms
 745/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 18.65ms
 746/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 4.55ms
 747/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 2.54ms
 748/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 3.12ms
 749/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 8.43ms
 750/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 3.70ms
 751/2072 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 4.20ms
 752/2072 src/collection/pipeline-collection/PipelineCollection.ts 3.03ms
 753/2072 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 4.55ms
 754/2072 src/collection/pipeline-collection/pipelineCollectionToJson.ts 3.29ms
 755/2072 src/collection/pipeline-collection/SimplePipelineCollection.ts 5.63ms
 756/2072 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 2.67ms
 757/2072 src/commands/_BOILERPLATE/BoilerplateCommand.ts 1.46ms
 758/2072 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 6.16ms
 759/2072 src/commands/_common/getParserForCommand.test.ts 2.63ms
 760/2072 src/commands/_common/getParserForCommand.ts 2.32ms
 761/2072 src/commands/_common/parseCommand.ts 6.84ms
 762/2072 src/commands/_common/stringifyCommand.test.ts 2.93ms
 763/2072 src/commands/_common/stringifyCommand.ts 2.20ms
 764/2072 src/commands/_common/types/Command.ts 1.75ms
 765/2072 src/commands/_common/types/CommandParser.ts 9.02ms
 766/2072 src/commands/_common/types/CommandType.ts 2.27ms
 767/2072 src/commands/_common/types/CommandUsagePlaces.ts 1.71ms
 768/2072 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 3.50ms
 769/2072 src/commands/BOOK_VERSION/BookVersionCommand.ts 1.54ms
 770/2072 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 5.18ms
 771/2072 src/commands/EXPECT/expectCommand.test.ts 3.31ms
 772/2072 src/commands/EXPECT/ExpectCommand.ts 3.72ms
 773/2072 src/commands/EXPECT/expectCommandParser.ts 7.89ms
 774/2072 src/commands/FOREACH/foreachCommand.test.ts 4.38ms
 775/2072 src/commands/FOREACH/ForeachCommand.ts 1.77ms
 776/2072 src/commands/FOREACH/foreachCommandParser.ts 9.39ms
 777/2072 src/commands/FOREACH/ForeachJson.ts 2.51ms
 778/2072 src/commands/FORMAT/formatCommand.test.ts 2.27ms
 779/2072 src/commands/FORMAT/FormatCommand.ts 1.55ms
 780/2072 src/commands/FORMAT/formatCommandParser.ts 4.69ms
 781/2072 src/commands/FORMFACTOR/formfactorCommand.test.ts 2.85ms
 782/2072 src/commands/FORMFACTOR/FormfactorCommand.ts 1.83ms
 783/2072 src/commands/FORMFACTOR/formfactorCommandParser.ts 5.05ms
 784/2072 src/commands/index.ts 4.79ms
 785/2072 src/commands/JOKER/jokerCommand.test.ts 2.49ms
 786/2072 src/commands/JOKER/JokerCommand.ts 1.62ms
 787/2072 src/commands/JOKER/jokerCommandParser.ts 5.66ms
 788/2072 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 2.75ms
 789/2072 src/commands/KNOWLEDGE/KnowledgeCommand.ts 1.88ms
 790/2072 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 5.93ms
 791/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 2.45ms
 792/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 2.91ms
 793/2072 src/commands/MODEL/modelCommand.test.ts 3.04ms
 794/2072 src/commands/MODEL/ModelCommand.ts 2.00ms
 795/2072 src/commands/MODEL/modelCommandParser.ts 7.53ms
 796/2072 src/commands/PARAMETER/parameterCommand.test.ts 5.68ms
 797/2072 src/commands/PARAMETER/ParameterCommand.ts 2.65ms
 798/2072 src/commands/PARAMETER/parameterCommandParser.ts 8.14ms
 799/2072 src/commands/PERSONA/personaCommand.test.ts 5.05ms
 800/2072 src/commands/PERSONA/PersonaCommand.ts 3.13ms
 801/2072 src/commands/PERSONA/personaCommandParser.ts 11.40ms
 802/2072 src/commands/POSTPROCESS/postprocessCommand.test.ts 4.21ms
 803/2072 src/commands/POSTPROCESS/PostprocessCommand.ts 2.22ms
 804/2072 src/commands/POSTPROCESS/postprocessCommandParser.ts 6.03ms
 805/2072 src/commands/README.md 1.92ms
 806/2072 src/commands/SECTION/sectionCommand.test.ts 2.81ms
 807/2072 src/commands/SECTION/SectionCommand.ts 1.66ms
 808/2072 src/commands/SECTION/sectionCommandParser.ts 11.03ms
 809/2072 src/commands/URL/urlCommand.test.ts 3.77ms
 810/2072 src/commands/URL/UrlCommand.ts 1.65ms
 811/2072 src/commands/URL/urlCommandParser.ts 5.51ms
 812/2072 src/commands/X_ACTION/actionCommand.test.ts 2.21ms
 813/2072 src/commands/X_ACTION/ActionCommand.ts 1.59ms
 814/2072 src/commands/X_ACTION/actionCommandParser.ts 4.32ms
 815/2072 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 2.19ms
 816/2072 src/commands/X_INSTRUMENT/InstrumentCommand.ts 2.20ms
 817/2072 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 4.70ms
 818/2072 src/commitments/_base/BaseCommitmentDefinition.ts 9.65ms
 819/2072 src/commitments/_base/BookCommitment.ts 1.75ms
 820/2072 src/commitments/_base/CommitmentDefinition.ts 5.38ms
 821/2072 src/commitments/_base/createEmptyAgentModelRequirements.ts 3.30ms
 822/2072 src/commitments/_base/formatOptionalInstructionBlock.ts 3.35ms
 823/2072 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 5.47ms
 824/2072 src/commitments/_base/ParsedCommitment.ts 2.79ms
 825/2072 src/commitments/_common/commitmentToolFunctions.ts 4.41ms
 826/2072 src/commitments/_common/createWritingCommitmentSections.ts 2.89ms
 827/2072 src/commitments/_common/getAllCommitmentDefinitions.test.ts 2.78ms
 828/2072 src/commitments/_common/getAllCommitmentDefinitions.ts 2.35ms
 829/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 2.20ms
 830/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 3.12ms
 831/2072 src/commitments/_common/getAllCommitmentsToolTitles.ts 2.52ms
 832/2072 src/commitments/_common/getAllCommitmentTypes.ts 2.47ms
 833/2072 src/commitments/_common/getCommitmentDefinition.ts 1.93ms
 834/2072 src/commitments/_common/getCommitmentNoticeMetadata.test.ts 3.49ms
 835/2072 src/commitments/_common/getCommitmentNoticeMetadata.ts 7.94ms
 836/2072 src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts 2.16ms
 837/2072 src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts 4.00ms
 838/2072 src/commitments/_common/getGroupedCommitmentDefinitions.ts 4.95ms
 839/2072 src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts 2.39ms
 840/2072 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 2.82ms
 841/2072 src/commitments/_common/isCommitmentSupported.ts 1.59ms
 842/2072 src/commitments/_common/sortCommitmentDefinitions.ts 4.89ms
 843/2072 src/commitments/_common/teamInternalAgentAccess.ts 5.39ms
 844/2072 src/commitments/_common/toolExecutionEnvelope.ts 3.17ms
 845/2072 src/commitments/_common/toolRuntimeContext.ts 8.93ms
 846/2072 src/commitments/ACTION/ACTION.test.ts 3.08ms
 847/2072 src/commitments/ACTION/ACTION.ts 4.17ms
 848/2072 src/commitments/CLOSED/CLOSED.test.ts 2.19ms
 849/2072 src/commitments/CLOSED/CLOSED.ts 2.95ms
 850/2072 src/commitments/DELETE/DELETE.test.ts 2.85ms
 851/2072 src/commitments/DELETE/DELETE.ts 6.63ms
 852/2072 src/commitments/DICTIONARY/DICTIONARY.ts 4.69ms
 853/2072 src/commitments/FORMAT/FORMAT.test.ts 2.92ms
 854/2072 src/commitments/FORMAT/FORMAT.ts 4.10ms
 855/2072 src/commitments/FROM/FROM.ts 3.87ms
 856/2072 src/commitments/GOAL/GOAL.ts 4.90ms
 857/2072 src/commitments/IMPORT/IMPORT.ts 3.59ms
 858/2072 src/commitments/index.test.ts 2.46ms
 859/2072 src/commitments/index.ts 11.81ms
 860/2072 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 6.40ms
 861/2072 src/commitments/LANGUAGE/LANGUAGE.ts 3.29ms
 862/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 2.39ms
 863/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 3.24ms
 864/2072 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 3.53ms
 865/2072 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 4.69ms
 866/2072 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 3.63ms
 867/2072 src/commitments/MESSAGE/MESSAGE.ts 6.87ms
 868/2072 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 3.82ms
 869/2072 src/commitments/META_AVATAR/META_AVATAR.ts 3.86ms
 870/2072 src/commitments/META_COLOR/META_COLOR.ts 3.75ms
 871/2072 src/commitments/META_DESCRIPTION/META_DESCRIPTION.ts 3.80ms
 872/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 2.51ms
 873/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 4.53ms
 874/2072 src/commitments/META_DOMAIN/META_DOMAIN.ts 3.91ms
 875/2072 src/commitments/META_FONT/META_FONT.ts 3.73ms
 876/2072 src/commitments/META_FULLNAME/META_FULLNAME.ts 3.27ms
 877/2072 src/commitments/META_ID/META_ID.ts 3.85ms
 878/2072 src/commitments/META_IMAGE/META_IMAGE.ts 4.20ms
 879/2072 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 3.59ms
 880/2072 src/commitments/META_LINK/META_LINK.ts 6.03ms
 881/2072 src/commitments/META_THINKING_MESSAGE/META_THINKING_MESSAGE.ts 3.75ms
 882/2072 src/commitments/META_VISIBILITY/META_VISIBILITY.ts 3.72ms
 883/2072 src/commitments/META_VOICE/META_VOICE.ts 3.66ms
 884/2072 src/commitments/MODEL/MODEL.test.ts 2.88ms
 885/2072 src/commitments/MODEL/MODEL.ts 7.28ms
 886/2072 src/commitments/NOTE/NOTE.ts 4.14ms
 887/2072 src/commitments/OPEN/OPEN.ts 4.11ms
 888/2072 src/commitments/PERSONA/persona-verification-example.md 3.99ms
 889/2072 src/commitments/PERSONA/PERSONA.ts 6.61ms
 890/2072 src/commitments/README.md 1.69ms
 891/2072 src/commitments/RULE/RULE.ts 4.41ms
 892/2072 src/commitments/SAMPLE/SAMPLE.ts 3.93ms
 893/2072 src/commitments/SCENARIO/SCENARIO.ts 5.77ms
 894/2072 src/commitments/STYLE/STYLE.ts 5.59ms
 895/2072 src/commitments/TEAM/TEAM.ts 16.97ms
 896/2072 src/commitments/TEMPLATE/README.md 3.69ms
 897/2072 src/commitments/TEMPLATE/TEMPLATE.test.ts 5.06ms
 898/2072 src/commitments/TEMPLATE/TEMPLATE.ts 5.71ms
 899/2072 src/commitments/USE_CALENDAR/calendarReference.ts 8.61ms
 900/2072 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 4.71ms
 901/2072 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 12.20ms
 902/2072 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 5.70ms
 903/2072 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 2.30ms
 904/2072 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 3.28ms
 905/2072 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 7.35ms
 906/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 6.08ms
 907/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 7.82ms
 908/2072 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.11ms
 909/2072 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 1.64ms
 910/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 2.38ms
 911/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 4.16ms
 912/2072 src/commitments/USE_MCP/USE_MCP.ts 3.14ms
 913/2072 src/commitments/USE_POPUP/USE_POPUP.ts 6.46ms
 914/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 3.40ms
 915/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 8.63ms
 916/2072 src/commitments/USE_PROJECT/callGitHubApi.ts 4.91ms
 917/2072 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 13.00ms
 918/2072 src/commitments/USE_PROJECT/createUseProjectTools.ts 5.91ms
 919/2072 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 2.35ms
 920/2072 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 2.19ms
 921/2072 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 1.85ms
 922/2072 src/commitments/USE_PROJECT/projectReference.test.ts 2.81ms
 923/2072 src/commitments/USE_PROJECT/projectReference.ts 6.82ms
 924/2072 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 6.65ms
 925/2072 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 8.48ms
 926/2072 src/commitments/USE_PROJECT/USE_PROJECT.ts 7.99ms
 927/2072 src/commitments/USE_PROJECT/UseProjectToolNames.ts 2.52ms
 928/2072 src/commitments/USE_PROJECT/UseProjectWallet.ts 1.67ms
 929/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 4.89ms
 930/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 8.50ms
 931/2072 src/commitments/WRITING_RULES/WRITING_RULES.ts 4.83ms
 932/2072 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 4.45ms
 933/2072 src/config.test.ts 1.42ms
 934/2072 src/config.ts 11.17ms
 935/2072 src/constants.ts 8.19ms
 936/2072 src/constants/streaming.ts 1.81ms
 937/2072 src/conversion/archive/loadArchive.ts 3.47ms
 938/2072 src/conversion/archive/loadJsZipModule.ts 1.73ms
 939/2072 src/conversion/archive/saveArchive.ts 3.44ms
 940/2072 src/conversion/compilePipeline.test.ts 2.65ms
 941/2072 src/conversion/compilePipeline.ts 3.08ms
 942/2072 src/conversion/compilePipelineOnRemoteServer.ts 3.44ms
 943/2072 src/conversion/parsePipeline.test.ts 2.80ms
 944/2072 src/conversion/parsePipeline.ts 3.86ms
 945/2072 src/conversion/parsePipeline/applyPipelineHead.ts 4.39ms
 946/2072 src/conversion/parsePipeline/createInitialPipelineJson.ts 2.17ms
 947/2072 src/conversion/parsePipeline/createUniqueSectionNameResolver.ts 3.57ms
 948/2072 src/conversion/parsePipeline/defineParameter.ts 3.61ms
 949/2072 src/conversion/parsePipeline/extractPipelineDescription.ts 2.11ms
 950/2072 src/conversion/parsePipeline/finalizeParsedPipeline.ts 5.63ms
 951/2072 src/conversion/parsePipeline/getPipelineIdentification.ts 2.12ms
 952/2072 src/conversion/parsePipeline/parsePreparedPipelineSections.ts 4.14ms
 953/2072 src/conversion/parsePipeline/preparePipelineString.ts 5.49ms
 954/2072 src/conversion/parsePipeline/processPipelineSection.ts 10.85ms
 955/2072 src/conversion/pipelineJsonToString.test.ts 5.10ms
 956/2072 src/conversion/pipelineJsonToString.ts 3.22ms
 957/2072 src/conversion/pipelineJsonToString/appendMarkdownBlock.ts 1.77ms
 958/2072 src/conversion/pipelineJsonToString/createPipelineCommands.ts 2.83ms
 959/2072 src/conversion/pipelineJsonToString/createPipelineIntroduction.ts 2.02ms
 960/2072 src/conversion/pipelineJsonToString/createTaskSerialization.ts 6.72ms
 961/2072 src/conversion/pipelineJsonToString/stringifyCommands.ts 1.62ms
 962/2072 src/conversion/pipelineJsonToString/stringifyTask.ts 2.54ms
 963/2072 src/conversion/prettify/PrettifyOptions.ts 1.97ms
 964/2072 src/conversion/prettify/prettifyPipelineString.ts 3.94ms
 965/2072 src/conversion/prettify/renderPipelineMermaidOptions.ts 5.94ms
 966/2072 src/conversion/utils/extractParameterNamesFromTask.test.ts 1.72ms
 967/2072 src/conversion/utils/extractParameterNamesFromTask.ts 4.17ms
 968/2072 src/conversion/validation/_importPipeline.test.ts 4.07ms
 969/2072 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 4.02ms
 970/2072 src/conversion/validation/validatePipeline-logicErrors.test.ts 2.94ms
 971/2072 src/conversion/validation/validatePipeline.test.ts 2.95ms
 972/2072 src/conversion/validation/validatePipeline.ts 4.75ms
 973/2072 src/conversion/validation/validatePipeline/createPipelineValidationContext.ts 2.50ms
 974/2072 src/conversion/validation/validatePipeline/validatePipelineCollectionsStructure.ts 3.43ms
 975/2072 src/conversion/validation/validatePipeline/validatePipelineDependencyResolution.ts 7.04ms
 976/2072 src/conversion/validation/validatePipeline/validatePipelineMetadata.ts 3.95ms
 977/2072 src/conversion/validation/validatePipeline/validatePipelineParameters.ts 4.88ms
 978/2072 src/conversion/validation/validatePipeline/validatePipelineTasks.ts 6.52ms
 979/2072 src/dialogs/callback/CallbackInterfaceTools.ts 2.77ms
 980/2072 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 2.08ms
 981/2072 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 3.45ms
 982/2072 src/dialogs/user-interface-execution-tools.test.ts 5.53ms
 983/2072 src/errors/0-BoilerplateError.ts 1.76ms
 984/2072 src/errors/0-index.ts 6.68ms
 985/2072 src/errors/AbstractFormatError.ts 3.88ms
 986/2072 src/errors/assertsError.ts 3.08ms
 987/2072 src/errors/AuthenticationError.ts 1.70ms
 988/2072 src/errors/CollectionError.ts 2.17ms
 989/2072 src/errors/ConflictError.ts 1.65ms
 990/2072 src/errors/DatabaseError.ts 1.81ms
 991/2072 src/errors/EnvironmentMismatchError.ts 1.88ms
 992/2072 src/errors/ExpectError.ts 2.27ms
 993/2072 src/errors/KnowledgeScrapeError.ts 1.79ms
 994/2072 src/errors/LimitReachedError.ts 2.26ms
 995/2072 src/errors/MissingToolsError.ts 1.94ms
 996/2072 src/errors/NotAllowed.ts 1.53ms
 997/2072 src/errors/NotFoundError.ts 1.57ms
 998/2072 src/errors/NotYetImplementedError.ts 1.88ms
 999/2072 src/errors/ParseError.ts 1.54ms
1000/2072 src/errors/PipelineExecutionError.ts 2.27ms
1001/2072 src/errors/PipelineLogicError.ts 2.03ms
1002/2072 src/errors/PipelineUrlError.ts 1.65ms
1003/2072 src/errors/PromptbookFetchError.ts 1.68ms
1004/2072 src/errors/UnexpectedError.ts 2.24ms
1005/2072 src/errors/utils/deserializeError.test.ts 2.36ms
1006/2072 src/errors/utils/deserializeError.ts 2.60ms
1007/2072 src/errors/utils/ErrorJson.ts 1.94ms
1008/2072 src/errors/utils/getErrorReportUrl.ts 2.70ms
1009/2072 src/errors/utils/serializeError.test.ts 2.34ms
1010/2072 src/errors/utils/serializeError.ts 2.20ms
1011/2072 src/errors/WrappedError.ts 2.21ms
1012/2072 src/executables/$provideExecutablesForNode.ts 3.62ms
1013/2072 src/executables/apps/locateLibreoffice.test.ts 3.14ms
1014/2072 src/executables/apps/locateLibreoffice.ts 2.51ms
1015/2072 src/executables/apps/locatePandoc.test.ts 1.58ms
1016/2072 src/executables/apps/locatePandoc.ts 2.39ms
1017/2072 src/executables/apps/locateVscode.test.ts 1.58ms
1018/2072 src/executables/apps/locateVscode.ts 2.89ms
1019/2072 src/executables/browsers/locateBrowser.test.ts 2.53ms
1020/2072 src/executables/browsers/locateBrowser.ts 2.76ms
1021/2072 src/executables/browsers/locateChrome.test.ts 1.45ms
1022/2072 src/executables/browsers/locateChrome.ts 1.66ms
1023/2072 src/executables/browsers/locateDefaultSystemBrowser.test.ts 1.84ms
1024/2072 src/executables/browsers/locateDefaultSystemBrowser.ts 1.55ms
1025/2072 src/executables/browsers/locateEdge.test.ts 1.56ms
1026/2072 src/executables/browsers/locateEdge.ts 1.76ms
1027/2072 src/executables/browsers/locateFirefox.test.ts 1.32ms
1028/2072 src/executables/browsers/locateFirefox.ts 1.62ms
1029/2072 src/executables/browsers/locateInternetExplorer.ts 1.83ms
1030/2072 src/executables/browsers/locateSafari.ts 1.75ms
1031/2072 src/executables/locateApp.test.ts 2.46ms
1032/2072 src/executables/locateApp.ts 3.90ms
1033/2072 src/executables/platforms/locateAppOnLinux.ts 2.85ms
1034/2072 src/executables/platforms/locateAppOnMacOs.ts 4.08ms
1035/2072 src/executables/platforms/locateAppOnWindows.test.ts 2.58ms
1036/2072 src/executables/platforms/locateAppOnWindows.ts 6.35ms
1037/2072 src/executables/README.md 1.53ms
1038/2072 src/execution/AbstractTaskResult.ts 4.80ms
1039/2072 src/execution/assertsTaskSuccessful.ts 6.74ms
1040/2072 src/execution/AvailableModel.ts 3.68ms
1041/2072 src/execution/CommonToolsOptions.ts 3.82ms
1042/2072 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 3.97ms
1043/2072 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 11.64ms
1044/2072 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 3.48ms
1045/2072 src/execution/createPipelineExecutor/10-executePipeline.ts 13.45ms
1046/2072 src/execution/createPipelineExecutor/20-executeTask.ts 11.49ms
1047/2072 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 9.43ms
1048/2072 src/execution/createPipelineExecutor/40-executeAttempts.ts 10.21ms
1049/2072 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 2.51ms
1050/2072 src/execution/createPipelineExecutor/executeSingleAttempt.ts 16.19ms
1051/2072 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 5.26ms
1052/2072 src/execution/createPipelineExecutor/getContextForTask.ts 2.44ms
1053/2072 src/execution/createPipelineExecutor/getExamplesForTask.ts 2.81ms
1054/2072 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 51.99ms
1055/2072 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 12.45ms
1056/2072 src/execution/createPipelineExecutor/handleAttemptFailure.ts 7.15ms
1057/2072 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 22.38ms
1058/2072 src/execution/createPipelineExecutor/reportPromptExecution.ts 6.53ms
1059/2072 src/execution/EmbeddingVector.ts 1.85ms
1060/2072 src/execution/embeddingVectorToString.ts 29.41ms
1061/2072 src/execution/Executables.ts 2.26ms
1062/2072 src/execution/execution-report/countWorkingDuration.test.ts 2.59ms
1063/2072 src/execution/execution-report/countWorkingDuration.ts 2.66ms
1064/2072 src/execution/execution-report/ExecutionPromptReportJson.ts 3.09ms
1065/2072 src/execution/execution-report/ExecutionReportJson.ts 3.62ms
1066/2072 src/execution/execution-report/executionReportJsonToString.ts 12.46ms
1067/2072 src/execution/execution-report/ExecutionReportString.ts 1.94ms
1068/2072 src/execution/execution-report/ExecutionReportStringOptions.ts 2.26ms
1069/2072 src/execution/ExecutionTask.ts 12.97ms
1070/2072 src/execution/ExecutionTools.ts 4.26ms
1071/2072 src/execution/FilesystemTools.ts 1.57ms
1072/2072 src/execution/LlmExecutionTools.ts 8.26ms
1073/2072 src/execution/LlmExecutionToolsConstructor.ts 1.66ms
1074/2072 src/execution/PipelineExecutor.ts 1.93ms
1075/2072 src/execution/PipelineExecutorResult.ts 2.87ms
1076/2072 src/execution/PromptbookFetch.test-type.ts 1.57ms
1077/2072 src/execution/PromptbookFetch.ts 1.57ms
1078/2072 src/execution/PromptResult.ts 4.21ms
1079/2072 src/execution/resolveTaskTldr.test.ts 2.47ms
1080/2072 src/execution/resolveTaskTldr.ts 6.12ms
1081/2072 src/execution/ScriptExecutionTools.ts 2.60ms
1082/2072 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 2.69ms
1083/2072 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 2.26ms
1084/2072 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 1.45ms
1085/2072 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 2.81ms
1086/2072 src/execution/translation/automatic-translate/automatic-translators/README.md 1.03ms
1087/2072 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 1.46ms
1088/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 1.70ms
1089/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 1.85ms
1090/2072 src/execution/translation/automatic-translate/translateMessages.ts 3.47ms
1091/2072 src/execution/UncertainNumber.ts 1.64ms
1092/2072 src/execution/Usage.ts 2.67ms
1093/2072 src/execution/UserInterfaceTools.ts 2.84ms
1094/2072 src/execution/utils/$provideExecutionToolsForNode.ts 3.97ms
1095/2072 src/execution/utils/addUsage.test.ts 5.72ms
1096/2072 src/execution/utils/addUsage.ts 2.71ms
1097/2072 src/execution/utils/checkExpectations.test.ts 2.07ms
1098/2072 src/execution/utils/checkExpectations.ts 3.58ms
1099/2072 src/execution/utils/computeUsageCounts.ts 2.57ms
1100/2072 src/execution/utils/forEachAsync.ts 2.61ms
1101/2072 src/execution/utils/formatUsagePrice.test.ts 2.23ms
1102/2072 src/execution/utils/formatUsagePrice.ts 1.83ms
1103/2072 src/execution/utils/uncertainNumber.ts 1.85ms
1104/2072 src/execution/utils/usage-constants.ts 2.67ms
1105/2072 src/execution/utils/usageToHuman.ts 5.65ms
1106/2072 src/execution/utils/usageToWorktime.test.ts 2.57ms
1107/2072 src/execution/utils/usageToWorktime.ts 2.26ms
1108/2072 src/execution/utils/validatePromptResult.ts 5.22ms
1109/2072 src/expectations/README.md 1.07ms
1110/2072 src/file-security-checkers/FileSecurityChecker.ts 2.20ms
1111/2072 src/file-security-checkers/FileSecurityCheckResult.ts 2.62ms
1112/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 3.82ms
1113/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 5.27ms
1114/2072 src/formats/_common/FormatParser.ts 4.18ms
1115/2072 src/formats/_common/FormatSubvalueParser.ts 4.08ms
1116/2072 src/formats/csv/CsvFormatError.ts 1.49ms
1117/2072 src/formats/csv/CsvFormatParser.ts 5.24ms
1118/2072 src/formats/csv/CsvSettings.ts 2.02ms
1119/2072 src/formats/csv/utils/csvParse.ts 2.26ms
1120/2072 src/formats/csv/utils/isValidCsvString.test.ts 1.93ms
1121/2072 src/formats/csv/utils/isValidCsvString.ts 1.91ms
1122/2072 src/formats/index.ts 1.71ms
1123/2072 src/formats/json/JsonFormatParser.ts 2.63ms
1124/2072 src/formats/json/utils/isValidJsonString.test.ts 2.66ms
1125/2072 src/formats/json/utils/isValidJsonString.ts 1.68ms
1126/2072 src/formats/json/utils/jsonParse.ts 1.71ms
1127/2072 src/formats/README.md 1.62ms
1128/2072 src/formats/text/TextFormatParser.ts 3.69ms
1129/2072 src/formats/xml/utils/isValidXmlString.test.ts 1.88ms
1130/2072 src/formats/xml/utils/isValidXmlString.ts 1.86ms
1131/2072 src/formats/xml/XmlFormatParser.ts 2.66ms
1132/2072 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 2.34ms
1133/2072 src/formfactors/_common/AbstractFormfactorDefinition.ts 3.34ms
1134/2072 src/formfactors/_common/FormfactorDefinition.ts 1.70ms
1135/2072 src/formfactors/_common/string_formfactor_name.ts 1.43ms
1136/2072 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 2.95ms
1137/2072 src/formfactors/completion/CompletionFormfactorDefinition.ts 2.52ms
1138/2072 src/formfactors/generator/GeneratorFormfactorDefinition.ts 2.59ms
1139/2072 src/formfactors/generic/GenericFormfactorDefinition.ts 1.88ms
1140/2072 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 2.41ms
1141/2072 src/formfactors/index.ts 2.56ms
1142/2072 src/formfactors/matcher/MatcherFormfactorDefinition.ts 3.19ms
1143/2072 src/formfactors/README.md 1.14ms
1144/2072 src/formfactors/sheets/SheetsFormfactorDefinition.ts 2.62ms
1145/2072 src/formfactors/translator/TranslatorFormfactorDefinition.ts 3.09ms
1146/2072 src/globals.d.ts 1.45ms
1147/2072 src/high-level-abstractions/_common/HighLevelAbstraction.ts 2.78ms
1148/2072 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 3.40ms
1149/2072 src/high-level-abstractions/index.ts 1.95ms
1150/2072 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 6.62ms
1151/2072 src/high-level-abstractions/README.md 1.43ms
1152/2072 src/import-plugins/$fileImportPlugins.ts 1.92ms
1153/2072 src/import-plugins/AgentFileImportPlugin.ts 3.06ms
1154/2072 src/import-plugins/FileImportPlugin.ts 2.22ms
1155/2072 src/import-plugins/JsonFileImportPlugin.ts 2.55ms
1156/2072 src/import-plugins/TextFileImportPlugin.ts 2.72ms
1157/2072 src/llm-providers/_common/filterModels.ts 4.60ms
1158/2072 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 1.75ms
1159/2072 src/llm-providers/_common/register/$llmToolsRegister.ts 2.21ms
1160/2072 src/llm-providers/_common/register/$provideEnvFilename.ts 3.31ms
1161/2072 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 3.41ms
1162/2072 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 4.89ms
1163/2072 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 7.00ms
1164/2072 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 5.10ms
1165/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 3.40ms
1166/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolConfigurationStatusMessage.ts 3.57ms
1167/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolEntryKey.ts 1.87ms
1168/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolInstallationStatusMessage.ts 3.78ms
1169/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolsMessageContext.ts 3.04ms
1170/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolStatus.ts 5.14ms
1171/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getAvailableRegisteredLlmToolsEnvironmentVariables.ts 3.44ms
1172/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getRegisteredLlmToolRegisters.ts 2.20ms
1173/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/hasRegisteredLlmToolEntry.ts 2.52ms
1174/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolEntries.ts 4.28ms
1175/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolStatuses.ts 2.72ms
1176/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/RegisteredLlmToolsMessageContext.ts 3.42ms
1177/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderAvailableRegisteredLlmTools.ts 3.95ms
1178/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderNoRegisteredLlmToolsMessage.ts 1.99ms
1179/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRegisteredLlmToolsMessage.ts 2.86ms
1180/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRelevantRegisteredLlmToolsEnvironmentVariables.ts 3.25ms
1181/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/USED_ENV_FILENAME.ts 2.23ms
1182/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 2.68ms
1183/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 5.93ms
1184/2072 src/llm-providers/_common/register/LlmToolsConfiguration.ts 2.92ms
1185/2072 src/llm-providers/_common/register/LlmToolsMetadata.ts 3.81ms
1186/2072 src/llm-providers/_common/register/LlmToolsOptions.ts 1.82ms
1187/2072 src/llm-providers/_common/utils/assertUniqueModels.ts 2.12ms
1188/2072 src/llm-providers/_common/utils/cache/CacheItem.ts 2.06ms
1189/2072 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 10.12ms
1190/2072 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 2.62ms
1191/2072 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 5.91ms
1192/2072 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 5.57ms
1193/2072 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 2.12ms
1194/2072 src/llm-providers/_common/utils/pricing.test.ts 1.44ms
1195/2072 src/llm-providers/_common/utils/pricing.ts 1.54ms
1196/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 3.05ms
1197/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 3.30ms
1198/2072 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 2.54ms
1199/2072 src/llm-providers/_multiple/joinLlmExecutionTools.ts 5.10ms
1200/2072 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 7.74ms
1201/2072 src/llm-providers/_multiple/playground/playground.ts 5.82ms
1202/2072 src/llm-providers/_multiple/playground/tsconfig.json 2.37ms
1203/2072 src/llm-providers/agent/Agent.test.ts 5.16ms
1204/2072 src/llm-providers/agent/Agent.ts 14.35ms
1205/2072 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 7.93ms
1206/2072 src/llm-providers/agent/AgentLlmExecutionTools.ts 14.88ms
1207/2072 src/llm-providers/agent/AgentLlmExecutionToolsAgentKitRunner.ts 12.65ms
1208/2072 src/llm-providers/agent/AgentLlmExecutionToolsOpenAiAssistantRunner.ts 8.49ms
1209/2072 src/llm-providers/agent/AgentLlmExecutionToolsPromptPreparer.ts 10.22ms
1210/2072 src/llm-providers/agent/AgentOptions.ts 3.51ms
1211/2072 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.48ms
1212/2072 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 4.66ms
1213/2072 src/llm-providers/agent/emitAgentLlmExecutionToolsAssistantPreparationProgress.ts 3.53ms
1214/2072 src/llm-providers/agent/playground/playground.ts 7.43ms
1215/2072 src/llm-providers/agent/playground/tsconfig.json 2.23ms
1216/2072 src/llm-providers/agent/register-configuration.ts 3.12ms
1217/2072 src/llm-providers/agent/register-constructor.ts 1.88ms
1218/2072 src/llm-providers/agent/RemoteAgent.test.ts 6.42ms
1219/2072 src/llm-providers/agent/RemoteAgent.ts 24.26ms
1220/2072 src/llm-providers/agent/RemoteAgentOptions.ts 1.85ms
1221/2072 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 5.56ms
1222/2072 src/llm-providers/agent/self-learning/SelfLearningManager.ts 19.02ms
1223/2072 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 8.41ms
1224/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 12.89ms
1225/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 2.90ms
1226/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 2.41ms
1227/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 4.14ms
1228/2072 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 2.90ms
1229/2072 src/llm-providers/anthropic-claude/playground/playground.ts 4.88ms
1230/2072 src/llm-providers/anthropic-claude/playground/tsconfig.json 2.59ms
1231/2072 src/llm-providers/anthropic-claude/register-configuration.ts 3.85ms
1232/2072 src/llm-providers/anthropic-claude/register-constructor.ts 2.46ms
1233/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 11.52ms
1234/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 2.71ms
1235/2072 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.13ms
1236/2072 src/llm-providers/azure-openai/playground/playground.ts 4.76ms
1237/2072 src/llm-providers/azure-openai/playground/tsconfig.json 2.20ms
1238/2072 src/llm-providers/azure-openai/register-configuration.ts 4.63ms
1239/2072 src/llm-providers/azure-openai/register-constructor.ts 2.76ms
1240/2072 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 3.86ms
1241/2072 src/llm-providers/deepseek/deepseek-models.ts 6.64ms
1242/2072 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 2.22ms
1243/2072 src/llm-providers/deepseek/other/compatibility-issue.md 2.07ms
1244/2072 src/llm-providers/deepseek/register-configuration.ts 4.36ms
1245/2072 src/llm-providers/deepseek/register-constructor.ts 2.78ms
1246/2072 src/llm-providers/google/createGoogleExecutionTools.ts 11.58ms
1247/2072 src/llm-providers/google/google-models.ts 9.08ms
1248/2072 src/llm-providers/google/GoogleExecutionToolsOptions.ts 1.97ms
1249/2072 src/llm-providers/google/other/compatibility-issue.md 1.86ms
1250/2072 src/llm-providers/google/register-configuration.ts 3.41ms
1251/2072 src/llm-providers/google/register-constructor.ts 1.89ms
1252/2072 src/llm-providers/mocked/$fakeTextToExpectations.ts 4.99ms
1253/2072 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 8.19ms
1254/2072 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 6.77ms
1255/2072 src/llm-providers/mocked/test/faked-completion.test.ts 5.77ms
1256/2072 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 2.48ms
1257/2072 src/llm-providers/mocked/test/joker.test.ts 4.36ms
1258/2072 src/llm-providers/mocked/test/mocked-chat.test.ts 5.71ms
1259/2072 src/llm-providers/mocked/test/mocked-completion.test.ts 5.72ms
1260/2072 src/llm-providers/ollama/createOllamaExecutionTools.ts 2.80ms
1261/2072 src/llm-providers/ollama/ollama-models.ts 10.92ms
1262/2072 src/llm-providers/ollama/OllamaExecutionTools.ts 5.38ms
1263/2072 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 2.43ms
1264/2072 src/llm-providers/ollama/playground/playground.ts 11.68ms
1265/2072 src/llm-providers/ollama/playground/tsconfig.json 4.94ms
1266/2072 src/llm-providers/ollama/register-configuration.ts 5.69ms
1267/2072 src/llm-providers/ollama/register-constructor.ts 5.37ms
1268/2072 src/llm-providers/openai/computeOpenAiUsage.test.ts 3.79ms
1269/2072 src/llm-providers/openai/computeOpenAiUsage.ts 7.25ms
1270/2072 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 11.53ms
1271/2072 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 8.44ms
1272/2072 src/llm-providers/openai/createOpenAiExecutionTools.ts 6.94ms
1273/2072 src/llm-providers/openai/openai-models.test.ts 2.22ms
1274/2072 src/llm-providers/openai/openai-models.ts 36.06ms
1275/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts 4.02ms
1276/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 24.65ms
1277/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsInputBuilder.ts 7.93ms
1278/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 2.74ms
1279/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOutputTypeMapper.ts 7.04ms
1280/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsToolBuilder.ts 23.73ms
1281/2072 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 26.98ms
1282/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 2.90ms
1283/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsProgressReporter.ts 7.99ms
1284/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsPromptBuilder.ts 4.54ms
1285/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsStreamRunner.ts 10.80ms
1286/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsToolRunner.ts 19.84ms
1287/2072 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 8.76ms
1288/2072 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 3.96ms
1289/2072 src/llm-providers/openai/OpenAiCompatibleModelCatalog.ts 4.66ms
1290/2072 src/llm-providers/openai/OpenAiCompatibleNonChatPromptCaller.ts 13.07ms
1291/2072 src/llm-providers/openai/OpenAiCompatibleRequestManager.ts 6.27ms
1292/2072 src/llm-providers/openai/OpenAiExecutionTools.ts 4.25ms
1293/2072 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 1.76ms
1294/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchHandler.ts 12.42ms
1295/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchPoller.ts 16.17ms
1296/2072 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 9.35ms
1297/2072 src/llm-providers/openai/OpenAiVectorStoreKnowledgeSourcePreparer.ts 7.55ms
1298/2072 src/llm-providers/openai/playground/playground.ts 13.47ms
1299/2072 src/llm-providers/openai/playground/tsconfig.json 2.62ms
1300/2072 src/llm-providers/openai/register-configuration.ts 7.66ms
1301/2072 src/llm-providers/openai/register-constructor.ts 4.55ms
1302/2072 src/llm-providers/openai/utils/buildToolInvocationScript.ts 3.73ms
1303/2072 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 11.35ms
1304/2072 src/llm-providers/openai/utils/loadOpenAiAgentsModule.ts 1.71ms
1305/2072 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 1.97ms
1306/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatProgressReporter.ts 8.94ms
1307/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatPromptBuilder.ts 7.38ms
1308/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatToolCaller.ts 10.73ms
1309/2072 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 5.24ms
1310/2072 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 2.58ms
1311/2072 src/llm-providers/README.md 1.11ms
1312/2072 src/llm-providers/remote/playground/playground.ts 5.70ms
1313/2072 src/llm-providers/remote/playground/tsconfig.json 2.84ms
1314/2072 src/llm-providers/remote/RemoteLlmExecutionTools.ts 6.91ms
1315/2072 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 8.66ms
1316/2072 src/llm-providers/vercel/playground/playground.ts 5.86ms
1317/2072 src/llm-providers/vercel/playground/tsconfig.json 2.26ms
1318/2072 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 4.69ms
1319/2072 src/llm-providers/vercel/VercelProvider.ts 2.04ms
1320/2072 src/migrations/migratePipeline.ts 3.46ms
1321/2072 src/other/README.md 1.20ms
1322/2072 src/other/templates/getBookTemplates.ts 3.58ms
1323/2072 src/personas/preparePersona.ts 5.96ms
1324/2072 src/personas/README.md 1.03ms
1325/2072 src/pipeline/book-notation.ts 2.84ms
1326/2072 src/pipeline/isValidPipelineString.test.ts 1.65ms
1327/2072 src/pipeline/isValidPipelineString.ts 2.15ms
1328/2072 src/pipeline/PipelineInterface/constants.ts 1.62ms
1329/2072 src/pipeline/PipelineInterface/getPipelineInterface.ts 3.02ms
1330/2072 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 2.18ms
1331/2072 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 2.94ms
1332/2072 src/pipeline/PipelineInterface/PipelineInterface.ts 2.08ms
1333/2072 src/pipeline/PipelineInterface/README.md 1.56ms
1334/2072 src/pipeline/PipelineJson/CommonTaskJson.ts 4.27ms
1335/2072 src/pipeline/PipelineJson/DialogTaskJson.ts 1.59ms
1336/2072 src/pipeline/PipelineJson/Expectations.ts 2.78ms
1337/2072 src/pipeline/PipelineJson/KnowledgePieceJson.ts 2.89ms
1338/2072 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 3.14ms
1339/2072 src/pipeline/PipelineJson/ParameterJson.ts 3.50ms
1340/2072 src/pipeline/PipelineJson/PersonaJson.ts 2.67ms
1341/2072 src/pipeline/PipelineJson/PipelineJson.ts 5.94ms
1342/2072 src/pipeline/PipelineJson/PreparationJson.ts 2.95ms
1343/2072 src/pipeline/PipelineJson/PromptTaskJson.ts 2.21ms
1344/2072 src/pipeline/PipelineJson/ScriptTaskJson.ts 1.85ms
1345/2072 src/pipeline/PipelineJson/SimpleTaskJson.ts 1.53ms
1346/2072 src/pipeline/PipelineJson/TaskJson.ts 1.67ms
1347/2072 src/pipeline/PipelineString.ts 2.53ms
1348/2072 src/pipeline/prompt-notation.test.ts 5.58ms
1349/2072 src/pipeline/prompt-notation.ts 7.52ms
1350/2072 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 4.28ms
1351/2072 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 4.67ms
1352/2072 src/pipeline/prompt-notation/helpers/ParameterSection.ts 3.97ms
1353/2072 src/pipeline/validatePipelineString.ts 3.18ms
1354/2072 src/playground/permanent/_boilerplate.ts 1.76ms
1355/2072 src/playground/permanent/agent-with-browser-playground.ts 2.39ms
1356/2072 src/playground/permanent/error-handling-playground.ts 2.06ms
1357/2072 src/playground/playground.ts 2.22ms
1358/2072 src/playground/tsconfig.json 3.03ms
1359/2072 src/postprocessing/README.md 1.23ms
1360/2072 src/postprocessing/utils/extractBlock.ts 2.93ms
1361/2072 src/postprocessing/utils/extractJsonBlock.test.ts 2.81ms
1362/2072 src/postprocessing/utils/extractJsonBlock.ts 4.03ms
1363/2072 src/prepare/isPipelinePrepared.test.ts 2.47ms
1364/2072 src/prepare/isPipelinePrepared.ts 3.69ms
1365/2072 src/prepare/PrepareAndScrapeOptions.ts 3.46ms
1366/2072 src/prepare/preparePipeline.ts 12.41ms
1367/2072 src/prepare/preparePipelineOnRemoteServer.ts 3.70ms
1368/2072 src/prepare/prepareTasks.ts 5.91ms
1369/2072 src/prepare/unpreparePipeline.ts 3.94ms
1370/2072 src/remote-server/createRemoteClient.ts 4.39ms
1371/2072 src/remote-server/http-sample/README.md 1.37ms
1372/2072 src/remote-server/openapi-types.ts 8.95ms
1373/2072 src/remote-server/openapi.ts 8.03ms
1374/2072 src/remote-server/README.md 1.72ms
1375/2072 src/remote-server/RemoteServer.ts 2.07ms
1376/2072 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 1.63ms
1377/2072 src/remote-server/socket-types/_subtypes/Identification.ts 3.58ms
1378/2072 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 1.91ms
1379/2072 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 1.86ms
1380/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 1.93ms
1381/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 1.81ms
1382/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 1.72ms
1383/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 1.72ms
1384/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 1.82ms
1385/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 1.55ms
1386/2072 src/remote-server/startAgentServer.ts 3.40ms
1387/2072 src/remote-server/startRemoteServer.ts 5.80ms
1388/2072 src/remote-server/startRemoteServer/createRemoteServerExpressApp.ts 2.50ms
1389/2072 src/remote-server/startRemoteServer/createRemoteServerHandle.ts 2.10ms
1390/2072 src/remote-server/startRemoteServer/createSocketServer.ts 1.83ms
1391/2072 src/remote-server/startRemoteServer/getExecutionToolsFromIdentification.ts 5.21ms
1392/2072 src/remote-server/startRemoteServer/registerBookRoutes.ts 3.98ms
1393/2072 src/remote-server/startRemoteServer/registerExecutionRoutes.ts 6.57ms
1394/2072 src/remote-server/startRemoteServer/registerListModelsSocketHandler.ts 3.60ms
1395/2072 src/remote-server/startRemoteServer/registerLoginRoute.ts 3.18ms
1396/2072 src/remote-server/startRemoteServer/registerNotFoundRoute.ts 2.42ms
1397/2072 src/remote-server/startRemoteServer/registerOpenAiCompatibleChatCompletionsRoute.ts 5.31ms
1398/2072 src/remote-server/startRemoteServer/registerOpenApiRoutes.ts 2.83ms
1399/2072 src/remote-server/startRemoteServer/registerPreparePipelineSocketHandler.ts 3.96ms
1400/2072 src/remote-server/startRemoteServer/registerPromptSocketHandler.ts 6.50ms
1401/2072 src/remote-server/startRemoteServer/registerRemoteServerHttpRoutes.ts 2.85ms
1402/2072 src/remote-server/startRemoteServer/registerRemoteServerSocketHandlers.ts 2.60ms
1403/2072 src/remote-server/startRemoteServer/registerServerIndexRoute.ts 5.84ms
1404/2072 src/remote-server/startRemoteServer/RemoteServerRuntime.ts 2.14ms
1405/2072 src/remote-server/startRemoteServer/resolveStartRemoteServerConfiguration.ts 2.65ms
1406/2072 src/remote-server/startRemoteServer/respondToSocketRequest.ts 2.40ms
1407/2072 src/remote-server/startRemoteServer/SocketResponse.ts 1.35ms
1408/2072 src/remote-server/startRemoteServer/startListening.ts 2.00ms
1409/2072 src/remote-server/startRemoteServer/StartRemoteServerConfiguration.ts 2.65ms
1410/2072 src/remote-server/types/RemoteClientOptions.ts 2.23ms
1411/2072 src/remote-server/types/RemoteServerOptions.ts 7.83ms
1412/2072 src/remote-server/ui/renderServerIndexHtml.ts 5.05ms
1413/2072 src/remote-server/ui/ServerApp.tsx 6.09ms
1414/2072 src/remote-server/ui/types.ts 2.70ms
1415/2072 src/scrapers/_boilerplate/BoilerplateScraper.ts 8.04ms
1416/2072 src/scrapers/_boilerplate/createBoilerplateScraper.ts 2.41ms
1417/2072 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 4.06ms
1418/2072 src/scrapers/_boilerplate/playground/tsconfig.json 2.14ms
1419/2072 src/scrapers/_boilerplate/README.md 0.93ms
1420/2072 src/scrapers/_boilerplate/register-constructor.ts 2.28ms
1421/2072 src/scrapers/_boilerplate/register-metadata.ts 3.20ms
1422/2072 src/scrapers/_common/Converter.ts 2.29ms
1423/2072 src/scrapers/_common/prepareKnowledgePieces.test.ts 2.76ms
1424/2072 src/scrapers/_common/prepareKnowledgePieces.ts 6.77ms
1425/2072 src/scrapers/_common/register/$provideFilesystemForNode.ts 2.93ms
1426/2072 src/scrapers/_common/register/$provideScrapersForBrowser.ts 4.14ms
1427/2072 src/scrapers/_common/register/$provideScrapersForNode.ts 4.43ms
1428/2072 src/scrapers/_common/register/$provideScriptingForNode.ts 3.12ms
1429/2072 src/scrapers/_common/register/$registeredScrapersMessage.ts 6.52ms
1430/2072 src/scrapers/_common/register/$scrapersMetadataRegister.ts 1.81ms
1431/2072 src/scrapers/_common/register/$scrapersRegister.ts 1.97ms
1432/2072 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 3.46ms
1433/2072 src/scrapers/_common/register/ScraperConstructor.ts 1.98ms
1434/2072 src/scrapers/_common/Scraper.ts 3.36ms
1435/2072 src/scrapers/_common/ScraperIntermediateSource.ts 2.24ms
1436/2072 src/scrapers/_common/utils/fetchUrlContent.ts 5.35ms
1437/2072 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 2.45ms
1438/2072 src/scrapers/_common/utils/getScraperIntermediateSource.ts 6.12ms
1439/2072 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 12.53ms
1440/2072 src/scrapers/_common/utils/promptbookFetch.test.ts 1.48ms
1441/2072 src/scrapers/_common/utils/promptbookFetch.ts 2.54ms
1442/2072 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 2.41ms
1443/2072 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 25.69ms
1444/2072 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 12.27ms
1445/2072 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 6.06ms
1446/2072 src/scrapers/document-legacy/LegacyDocumentScraper.ts 8.48ms
1447/2072 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 4.39ms
1448/2072 src/scrapers/document-legacy/playground/tsconfig.json 2.28ms
1449/2072 src/scrapers/document-legacy/README.md 1.47ms
1450/2072 src/scrapers/document-legacy/register-constructor.ts 2.33ms
1451/2072 src/scrapers/document-legacy/register-metadata.ts 3.42ms
1452/2072 src/scrapers/document/createDocumentScraper.ts 2.18ms
1453/2072 src/scrapers/document/DocumentScraper.test.ts 5.69ms
1454/2072 src/scrapers/document/DocumentScraper.ts 7.64ms
1455/2072 src/scrapers/document/examples/10-simple.docx.knowledge.json 20.46ms
1456/2072 src/scrapers/document/examples/10-simple.odt.knowledge.json 21.64ms
1457/2072 src/scrapers/document/playground/document-scraper-playground.ts 4.41ms
1458/2072 src/scrapers/document/playground/tsconfig.json 5.73ms
1459/2072 src/scrapers/document/README.md 1.81ms
1460/2072 src/scrapers/document/register-constructor.ts 2.36ms
1461/2072 src/scrapers/document/register-metadata.ts 4.50ms
1462/2072 src/scrapers/markdown/createMarkdownScraper.ts 3.36ms
1463/2072 src/scrapers/markdown/MarkdownScraper.test.ts 5.56ms
1464/2072 src/scrapers/markdown/MarkdownScraper.ts 15.87ms
1465/2072 src/scrapers/markdown/playground/markdown-scraper-playground.ts 4.88ms
1466/2072 src/scrapers/markdown/playground/tsconfig.json 2.80ms
1467/2072 src/scrapers/markdown/README.md 1.25ms
1468/2072 src/scrapers/markdown/register-constructor.ts 2.33ms
1469/2072 src/scrapers/markdown/register-metadata.ts 3.70ms
1470/2072 src/scrapers/markitdown/createMarkitdownScraper.ts 2.60ms
1471/2072 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 12.52ms
1472/2072 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 13.19ms
1473/2072 src/scrapers/markitdown/MarkitdownScraper.ts 9.02ms
1474/2072 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 5.03ms
1475/2072 src/scrapers/markitdown/playground/tsconfig.json 2.65ms
1476/2072 src/scrapers/markitdown/README.md 1.36ms
1477/2072 src/scrapers/markitdown/register-constructor.ts 2.39ms
1478/2072 src/scrapers/markitdown/register-metadata.ts 4.35ms
1479/2072 src/scrapers/pdf/createPdfScraper.ts 2.54ms
1480/2072 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 1.69ms
1481/2072 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 2.67ms
1482/2072 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 6.47ms
1483/2072 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 2.70ms
1484/2072 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 3.64ms
1485/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 5.54ms
1486/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 2.00ms
1487/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 2.23ms
1488/2072 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 2.80ms
1489/2072 src/scrapers/pdf/lib/pdf2md/models/Page.js 2.12ms
1490/2072 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 2.06ms
1491/2072 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 1.93ms
1492/2072 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 1.85ms
1493/2072 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 3.44ms
1494/2072 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 2.78ms
1495/2072 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 3.59ms
1496/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 2.93ms
1497/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 3.21ms
1498/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 4.48ms
1499/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 5.47ms
1500/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 7.88ms
1501/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 3.51ms
1502/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 11.30ms
1503/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 4.74ms
1504/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 3.89ms
1505/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 4.61ms
1506/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 3.58ms
1507/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 3.95ms
1508/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 2.78ms
1509/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 2.63ms
1510/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 2.45ms
1511/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 2.44ms
1512/2072 src/scrapers/pdf/lib/pdf2md/models/Word.js 1.56ms
1513/2072 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 6.50ms
1514/2072 src/scrapers/pdf/lib/pdf2md/pdf2md.js 3.07ms
1515/2072 src/scrapers/pdf/lib/pdf2md/README.md 1.10ms
1516/2072 src/scrapers/pdf/lib/pdf2md/util/cli.js 2.69ms
1517/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 1.93ms
1518/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/createDocumentStub.js 2.40ms
1519/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElement.js 3.37ms
1520/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElementSerializer.js 3.70ms
1521/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/Image.js 1.82ms
1522/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/xmlEncode.js 2.08ms
1523/2072 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 1.87ms
1524/2072 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 4.59ms
1525/2072 src/scrapers/pdf/lib/pdf2md/util/pdf.js 5.69ms
1526/2072 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 4.20ms
1527/2072 src/scrapers/pdf/lib/pdf2md/util/transformations.js 3.48ms
1528/2072 src/scrapers/pdf/PdfScraper.ts 4.71ms
1529/2072 src/scrapers/pdf/playground/pdf-scraper-playground.ts 3.86ms
1530/2072 src/scrapers/pdf/playground/tsconfig.json 2.32ms
1531/2072 src/scrapers/pdf/README.md 1.46ms
1532/2072 src/scrapers/pdf/register-constructor.ts 1.85ms
1533/2072 src/scrapers/pdf/register-metadata.ts 3.33ms
1534/2072 src/scrapers/README.md 1.17ms
1535/2072 src/scrapers/website/createWebsiteScraper.ts 2.32ms
1536/2072 src/scrapers/website/playground/tsconfig.json 2.28ms
1537/2072 src/scrapers/website/playground/website-scraper-playground.ts 4.73ms
1538/2072 src/scrapers/website/README.md 1.20ms
1539/2072 src/scrapers/website/register-constructor.ts 1.88ms
1540/2072 src/scrapers/website/register-metadata.ts 3.17ms
1541/2072 src/scrapers/website/utils/createShowdownConverter.test.ts 2.82ms
1542/2072 src/scrapers/website/utils/createShowdownConverter.ts 2.18ms
1543/2072 src/scrapers/website/WebsiteScraper.ts 8.96ms
1544/2072 src/scripting/_test/custom-function-async.test.ts.test.ts 4.16ms
1545/2072 src/scripting/_test/custom-function-missing.test.ts 3.69ms
1546/2072 src/scripting/_test/custom-function-with-dependencies.test.ts 4.33ms
1547/2072 src/scripting/_test/custom-function.test.ts 5.42ms
1548/2072 src/scripting/_test/postprocessing.test.ts 4.35ms
1549/2072 src/scripting/_test/script-execution-errors.test.ts 4.30ms
1550/2072 src/scripting/_test/script-execution-tools.test.ts 4.20ms
1551/2072 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 4.00ms
1552/2072 src/scripting/javascript/JavascriptEvalExecutionTools.ts 8.69ms
1553/2072 src/scripting/javascript/JavascriptExecutionTools.ts 1.59ms
1554/2072 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 2.86ms
1555/2072 src/scripting/javascript/postprocessing-functions.ts 4.25ms
1556/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 1.89ms
1557/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 3.35ms
1558/2072 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 1.99ms
1559/2072 src/scripting/python/PythonExecutionTools.ts 2.80ms
1560/2072 src/scripting/typescript/TypescriptExecutionTools.ts 2.56ms
1561/2072 src/search-engines/_index.ts 1.80ms
1562/2072 src/search-engines/bing/BingSearchEngine.ts 3.12ms
1563/2072 src/search-engines/dummy/DummySearchEngine.ts 2.51ms
1564/2072 src/search-engines/google/GoogleSearchEngine.ts 4.52ms
1565/2072 src/search-engines/SearchEngine.ts 1.82ms
1566/2072 src/search-engines/SearchResult.ts 1.52ms
1567/2072 src/search-engines/serp/SerpSearchEngine.ts 3.37ms
1568/2072 src/speech-recognition/BrowserSpeechRecognition.ts 4.58ms
1569/2072 src/speech-recognition/OpenAiSpeechRecognition.test.ts 3.55ms
1570/2072 src/speech-recognition/OpenAiSpeechRecognition.ts 19.06ms
1571/2072 src/storage/_common/PromptbookStorage.test-type.ts 1.82ms
1572/2072 src/storage/_common/PromptbookStorage.ts 2.25ms
1573/2072 src/storage/blackhole/BlackholeStorage.ts 2.68ms
1574/2072 src/storage/env-storage/$EnvStorage.ts 5.02ms
1575/2072 src/storage/file-cache-storage/FileCacheStorage.ts 8.40ms
1576/2072 src/storage/file-cache-storage/FileCacheStorageOptions.ts 2.24ms
1577/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 3.01ms
1578/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 1.94ms
1579/2072 src/storage/local-storage/getIndexedDbStorage.ts 3.12ms
1580/2072 src/storage/local-storage/getLocalStorage.ts 2.56ms
1581/2072 src/storage/local-storage/getSessionStorage.ts 3.10ms
1582/2072 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 1.65ms
1583/2072 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 3.04ms
1584/2072 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 3.09ms
1585/2072 src/storage/memory/MemoryStorage.ts 2.50ms
1586/2072 src/storage/utils/PrefixStorage.ts 2.24ms
1587/2072 src/transpilers/_common/BookTranspiler.ts 2.52ms
1588/2072 src/transpilers/_common/BookTranspilerOptions.ts 3.38ms
1589/2072 src/transpilers/_common/createTranspiledTeamRuntimeSection.ts 12.78ms
1590/2072 src/transpilers/_common/createZodSchemaSource.ts 5.09ms
1591/2072 src/transpilers/_common/formatUsedToolFunctions.test.ts 2.08ms
1592/2072 src/transpilers/_common/formatUsedToolFunctions.ts 4.25ms
1593/2072 src/transpilers/_common/prepareSdkTranspilerContext.ts 6.74ms
1594/2072 src/transpilers/_common/register/$bookTranspilersRegister.ts 1.89ms
1595/2072 src/transpilers/_common/resolveClaudeModelName.ts 1.79ms
1596/2072 src/transpilers/_common/TranspiledTeamExport.ts 3.81ms
1597/2072 src/transpilers/_common/transpiledTeamTranspilers.test.ts 4.88ms
1598/2072 src/transpilers/agent-os/AgentOsTranspiler.test.ts 3.91ms
1599/2072 src/transpilers/agent-os/AgentOsTranspiler.ts 12.35ms
1600/2072 src/transpilers/agent-os/register.ts 1.97ms
1601/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts 4.64ms
1602/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.ts 13.33ms
1603/2072 src/transpilers/anthropic-claude-managed/register.ts 2.04ms
1604/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts 4.07ms
1605/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.ts 10.40ms
1606/2072 src/transpilers/anthropic-claude-sdk/register.ts 1.94ms
1607/2072 src/transpilers/e2b/E2BTranspiler.test.ts 3.11ms
1608/2072 src/transpilers/e2b/E2BTranspiler.ts 7.01ms
1609/2072 src/transpilers/e2b/register.ts 1.92ms
1610/2072 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 2.78ms
1611/2072 src/transpilers/formatted-book-in-markdown/register.ts 2.15ms
1612/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts 5.15ms
1613/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.ts 13.84ms
1614/2072 src/transpilers/openai-agents/register.ts 2.61ms
1615/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 5.80ms
1616/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 14.16ms
1617/2072 src/transpilers/openai-sdk/playground/playground.ts 2.54ms
1618/2072 src/transpilers/openai-sdk/playground/tsconfig.json 3.29ms
1619/2072 src/transpilers/openai-sdk/register.ts 3.78ms
1620/2072 src/types/Arrayable.ts 2.31ms
1621/2072 src/types/InputParameters_private.ts 1.73ms
1622/2072 src/types/IntermediateFilesStrategy.ts 1.98ms
1623/2072 src/types/LlmCall.ts 1.63ms
1624/2072 src/types/LlmToolDefinition.ts 2.33ms
1625/2072 src/types/Message.ts 3.20ms
1626/2072 src/types/ModelRequirements.ts 6.53ms
1627/2072 src/types/ModelVariant.ts 1.86ms
1628/2072 src/types/NonEmptyArray.ts 1.60ms
1629/2072 src/types/number_bytes.ts 1.35ms
1630/2072 src/types/number_id.ts 1.32ms
1631/2072 src/types/number_likeness.ts 1.96ms
1632/2072 src/types/number_milliseconds.ts 1.76ms
1633/2072 src/types/number_percent.ts 2.98ms
1634/2072 src/types/number_positive.ts 1.28ms
1635/2072 src/types/number_usd.ts 1.84ms
1636/2072 src/types/Parameters_private.ts 1.76ms
1637/2072 src/types/Parameters.ts 1.75ms
1638/2072 src/types/Prompt.ts 5.61ms
1639/2072 src/types/ReservedParameters_private.ts 1.56ms
1640/2072 src/types/ScriptLanguage.ts 1.56ms
1641/2072 src/types/SectionType.ts 2.00ms
1642/2072 src/types/SpeechRecognition.ts 3.73ms
1643/2072 src/types/string_agent_hash_private.ts 1.47ms
1644/2072 src/types/string_agent_name_in_book_private.ts 1.83ms
1645/2072 src/types/string_agent_name_private.ts 1.65ms
1646/2072 src/types/string_agent_name.ts 1.46ms
1647/2072 src/types/string_agent_permanent_id_private.ts 1.27ms
1648/2072 src/types/string_agent_url_private.ts 1.34ms
1649/2072 src/types/string_agent_url.ts 1.19ms
1650/2072 src/types/string_base_url_private.ts 1.14ms
1651/2072 src/types/string_base_url.ts 1.06ms
1652/2072 src/types/string_base64_private.ts 1.36ms
1653/2072 src/types/string_base64.ts 1.26ms
1654/2072 src/types/string_business_category_name_private.ts 1.53ms
1655/2072 src/types/string_business_category_name.ts 1.16ms
1656/2072 src/types/string_char_private.ts 1.11ms
1657/2072 src/types/string_chat_prompt_private.ts 1.17ms
1658/2072 src/types/string_completion_prompt_private.ts 1.22ms
1659/2072 src/types/string_email_private.ts 1.06ms
1660/2072 src/types/string_email.ts 1.04ms
1661/2072 src/types/string_filename.ts 1.81ms
1662/2072 src/types/string_host_private.ts 1.66ms
1663/2072 src/types/string_host.ts 1.50ms
1664/2072 src/types/string_href_private.ts 1.34ms
1665/2072 src/types/string_href.ts 1.32ms
1666/2072 src/types/string_knowledge_source_content.ts 1.83ms
1667/2072 src/types/string_markdown.ts 4.66ms
1668/2072 src/types/string_mime_type_private.ts 1.21ms
1669/2072 src/types/string_mime_type.ts 1.23ms
1670/2072 src/types/string_model_description_private.ts 1.17ms
1671/2072 src/types/string_model_name_private.ts 1.27ms
1672/2072 src/types/string_model_name.ts 1.12ms
1673/2072 src/types/string_name_private.ts 1.05ms
1674/2072 src/types/string_name.ts 1.59ms
1675/2072 src/types/string_page_private.ts 1.51ms
1676/2072 src/types/string_page.ts 1.14ms
1677/2072 src/types/string_parameter_name.ts 1.73ms
1678/2072 src/types/string_parameter_value_private.ts 1.16ms
1679/2072 src/types/string_person_fullname.ts 2.68ms
1680/2072 src/types/string_persona_description_private.ts 1.65ms
1681/2072 src/types/string_persona_description.ts 1.30ms
1682/2072 src/types/string_pipeline_root_url_private.ts 1.15ms
1683/2072 src/types/string_pipeline_root_url.ts 1.37ms
1684/2072 src/types/string_pipeline_url_private.ts 1.21ms
1685/2072 src/types/string_pipeline_url.ts 1.23ms
1686/2072 src/types/string_prompt_image_private.ts 1.12ms
1687/2072 src/types/string_prompt_private.ts 1.17ms
1688/2072 src/types/string_prompt.ts 1.71ms
1689/2072 src/types/string_promptbook_server_url_private.ts 1.19ms
1690/2072 src/types/string_promptbook_server_url.ts 1.43ms
1691/2072 src/types/string_reserved_parameter_name_private.ts 1.46ms
1692/2072 src/types/string_sha256.ts 2.09ms
1693/2072 src/types/string_system_message_private.ts 1.41ms
1694/2072 src/types/string_template_private.ts 1.03ms
1695/2072 src/types/string_text_prompt_private.ts 1.06ms
1696/2072 src/types/string_title_private.ts 1.09ms
1697/2072 src/types/string_title.ts 1.18ms
1698/2072 src/types/string_token.ts 2.07ms
1699/2072 src/types/string_url_image_private.ts 1.02ms
1700/2072 src/types/string_url_image.ts 1.18ms
1701/2072 src/types/string_url_private.ts 1.00ms
1702/2072 src/types/string_url.ts 1.67ms
1703/2072 src/types/TaskType.ts 1.61ms
1704/2072 src/types/ToolCall.ts 4.83ms
1705/2072 src/types/typeAliasEmoji.ts 3.50ms
1706/2072 src/types/typeAliases.ts 4.22ms
1707/2072 src/types/Updatable.ts 2.80ms
1708/2072 src/utils/agent-message-runtime/AgentMessageProjectChange.ts 5.17ms
1709/2072 src/utils/agent-message-runtime/agentMessageRuntimePaths.ts 3.08ms
1710/2072 src/utils/agent-message-runtime/AgentMessageTouchedExternalSource.ts 4.23ms
1711/2072 src/utils/agent-message-runtime/parseAgentMessageRuntimeLogEvents.ts 4.15ms
1712/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts 4.46ms
1713/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.ts 7.88ms
1714/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts 4.15ms
1715/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.ts 10.39ms
1716/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts 2.99ms
1717/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.ts 6.07ms
1718/2072 src/utils/agents/resolveAgentAvatarImageUrl.test.ts 3.52ms
1719/2072 src/utils/agents/resolveAgentAvatarImageUrl.ts 7.33ms
1720/2072 src/utils/agents/terminalAgentAvatarVisual.test.ts 3.39ms
1721/2072 src/utils/agents/terminalAgentAvatarVisual.ts 10.52ms
1722/2072 src/utils/ascii-art/$detectTerminalAnsiColorDepth.ts 3.42ms
1723/2072 src/utils/ascii-art/convertImageDataToAsciiArt.ts 15.51ms
1724/2072 src/utils/ascii-art/createAnsiColorCode.ts 9.09ms
1725/2072 src/utils/chat/chatAttachments.test.ts 14.74ms
1726/2072 src/utils/chat/chatAttachments.ts 6.94ms
1727/2072 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 3.16ms
1728/2072 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 3.46ms
1729/2072 src/utils/chat/chatAttachments/appendChatContextSections.ts 2.13ms
1730/2072 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 3.89ms
1731/2072 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 4.46ms
1732/2072 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 4.35ms
1733/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 5.68ms
1734/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 3.19ms
1735/2072 src/utils/chat/constants.ts 1.97ms
1736/2072 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 1.96ms
1737/2072 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 2.06ms
1738/2072 src/utils/chat/escapeRegExp.ts 1.61ms
1739/2072 src/utils/clientVersion.ts 4.90ms
1740/2072 src/utils/color/$randomColor.ts 2.00ms
1741/2072 src/utils/color/Color.test.ts 3.05ms
1742/2072 src/utils/color/Color.ts 6.41ms
1743/2072 src/utils/color/ColorValue.ts 3.14ms
1744/2072 src/utils/color/css-colors.ts 6.09ms
1745/2072 src/utils/color/internal-utils/checkChannelValue.ts 2.12ms
1746/2072 src/utils/color/internal-utils/hslToRgb.ts 1.87ms
1747/2072 src/utils/color/internal-utils/rgbToHsl.ts 2.27ms
1748/2072 src/utils/color/isHexColorString.ts 2.23ms
1749/2072 src/utils/color/operators/ColorTransformer.ts 1.40ms
1750/2072 src/utils/color/operators/darken.ts 1.94ms
1751/2072 src/utils/color/operators/furthest.ts 2.25ms
1752/2072 src/utils/color/operators/grayscale.ts 1.65ms
1753/2072 src/utils/color/operators/lighten.ts 2.02ms
1754/2072 src/utils/color/operators/mixWithColor.ts 2.48ms
1755/2072 src/utils/color/operators/nearest.ts 1.89ms
1756/2072 src/utils/color/operators/negative.ts 1.56ms
1757/2072 src/utils/color/operators/negativeLightness.ts 1.90ms
1758/2072 src/utils/color/operators/README.md 2.85ms
1759/2072 src/utils/color/operators/saturate.ts 2.34ms
1760/2072 src/utils/color/operators/withAlpha.ts 1.50ms
1761/2072 src/utils/color/parseColorString.ts 2.47ms
1762/2072 src/utils/color/parsers/ColorChannelSet.ts 1.22ms
1763/2072 src/utils/color/parsers/parseHexColor.ts 3.08ms
1764/2072 src/utils/color/parsers/parseHslColor.ts 3.68ms
1765/2072 src/utils/color/parsers/parseRgbColor.ts 2.74ms
1766/2072 src/utils/color/utils/areColorsEqual.ts 1.82ms
1767/2072 src/utils/color/utils/colorDistance.ts 2.24ms
1768/2072 src/utils/color/utils/colorHue.ts 1.58ms
1769/2072 src/utils/color/utils/colorHueDistance.test.ts 2.29ms
1770/2072 src/utils/color/utils/colorHueDistance.ts 1.61ms
1771/2072 src/utils/color/utils/colorLuminance.ts 2.15ms
1772/2072 src/utils/color/utils/colorSatulightion.ts 1.93ms
1773/2072 src/utils/color/utils/colorSaturation.ts 1.44ms
1774/2072 src/utils/color/utils/colorToDataUrl.ts 3.24ms
1775/2072 src/utils/color/utils/mixColors.ts 2.85ms
1776/2072 src/utils/database/uniqueConstraint.ts 3.54ms
1777/2072 src/utils/DEFAULT_THINKING_MESSAGES.ts 1.89ms
1778/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 2.33ms
1779/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 3.09ms
1780/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 2.13ms
1781/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 3.01ms
1782/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 2.39ms
1783/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 3.07ms
1784/2072 src/utils/editable/types/PipelineEditableSerialized.ts 2.92ms
1785/2072 src/utils/editable/utils/isFlatPipeline.test.ts 2.02ms
1786/2072 src/utils/editable/utils/isFlatPipeline.ts 3.71ms
1787/2072 src/utils/editable/utils/renamePipelineParameter.test.ts 5.64ms
1788/2072 src/utils/editable/utils/renamePipelineParameter.ts 3.39ms
1789/2072 src/utils/editable/utils/stringifyPipelineJson.ts 3.49ms
1790/2072 src/utils/environment/$detectRuntimeEnvironment.ts 2.97ms
1791/2072 src/utils/environment/$getGlobalScope.ts 1.54ms
1792/2072 src/utils/environment/$isRunningInBrowser.ts 1.94ms
1793/2072 src/utils/environment/$isRunningInJest.ts 1.62ms
1794/2072 src/utils/environment/$isRunningInNode.ts 1.88ms
1795/2072 src/utils/environment/$isRunningInWebWorker.ts 2.18ms
1796/2072 src/utils/execCommand/$execCommand.ts 5.30ms
1797/2072 src/utils/execCommand/$execCommandNormalizeOptions.ts 3.07ms
1798/2072 src/utils/execCommand/$execCommands.ts 2.27ms
1799/2072 src/utils/execCommand/execCommand.test.ts 1.74ms
1800/2072 src/utils/execCommand/execCommandNormalizeOptions.test.ts 3.19ms
1801/2072 src/utils/execCommand/ExecCommandOptions.ts 3.03ms
1802/2072 src/utils/expectation-counters/constants.ts 1.53ms
1803/2072 src/utils/expectation-counters/countCharacters.test.ts 2.35ms
1804/2072 src/utils/expectation-counters/countCharacters.ts 1.89ms
1805/2072 src/utils/expectation-counters/countLines.test.ts 1.67ms
1806/2072 src/utils/expectation-counters/countLines.ts 2.66ms
1807/2072 src/utils/expectation-counters/countPages.test.ts 6.40ms
1808/2072 src/utils/expectation-counters/countPages.ts 1.98ms
1809/2072 src/utils/expectation-counters/countParagraphs.test.ts 1.62ms
1810/2072 src/utils/expectation-counters/countParagraphs.ts 1.65ms
1811/2072 src/utils/expectation-counters/countSentences.test.ts 1.69ms
1812/2072 src/utils/expectation-counters/countSentences.ts 1.88ms
1813/2072 src/utils/expectation-counters/countWords.test.ts 2.33ms
1814/2072 src/utils/expectation-counters/countWords.ts 2.41ms
1815/2072 src/utils/expectation-counters/index.ts 2.49ms
1816/2072 src/utils/expectation-counters/other/countPages.md 3.38ms
1817/2072 src/utils/files/$induceBookDownload.ts 3.51ms
1818/2072 src/utils/files/$induceFileDownload.ts 3.21ms
1819/2072 src/utils/files/decodeAttachmentAsText.test.ts 5.78ms
1820/2072 src/utils/files/decodeAttachmentAsText.ts 16.66ms
1821/2072 src/utils/files/extensionToMimeType.test.ts 2.18ms
1822/2072 src/utils/files/extensionToMimeType.ts 2.02ms
1823/2072 src/utils/files/getFileExtension.test.ts 1.96ms
1824/2072 src/utils/files/getFileExtension.ts 2.08ms
1825/2072 src/utils/files/isDirectoryExisting.test.ts 2.08ms
1826/2072 src/utils/files/isDirectoryExisting.ts 3.18ms
1827/2072 src/utils/files/isExecutable.ts 1.85ms
1828/2072 src/utils/files/isFileExisting.test.ts 1.80ms
1829/2072 src/utils/files/isFileExisting.ts 2.86ms
1830/2072 src/utils/files/listAllFiles.test.ts 2.16ms
1831/2072 src/utils/files/listAllFiles.ts 3.09ms
1832/2072 src/utils/files/mimeTypeToExtension.test.ts 2.41ms
1833/2072 src/utils/files/mimeTypeToExtension.ts 1.48ms
1834/2072 src/utils/files/ObjectUrl.ts 4.28ms
1835/2072 src/utils/files/readResponseBytes.ts 3.04ms
1836/2072 src/utils/filesystem/promptbookTemporaryPath.test.ts 2.40ms
1837/2072 src/utils/filesystem/promptbookTemporaryPath.ts 4.04ms
1838/2072 src/utils/html/escapeHtml.ts 1.75ms
1839/2072 src/utils/isTimingSafeEqualString.ts 2.73ms
1840/2072 src/utils/knowledge/inlineKnowledgeSource.test.ts 2.59ms
1841/2072 src/utils/knowledge/inlineKnowledgeSource.ts 5.67ms
1842/2072 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 2.22ms
1843/2072 src/utils/knowledge/simplifyKnowledgeLabel.ts 8.25ms
1844/2072 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 3.62ms
1845/2072 src/utils/linguistic-hash/linguisticHash.test.ts 3.20ms
1846/2072 src/utils/linguistic-hash/linguisticHash.ts 3.01ms
1847/2072 src/utils/linguistic-hash/LinguisticHashLanguage.ts 2.80ms
1848/2072 src/utils/linguistic-hash/linguisticHashTypes.ts 1.63ms
1849/2072 src/utils/linguistic-hash/linguisticHashWordCount.ts 1.87ms
1850/2072 src/utils/linguistic-hash/linguisticHashWords.cs.ts 2.95ms
1851/2072 src/utils/linguistic-hash/linguisticHashWords.en.ts 11.98ms
1852/2072 src/utils/linguistic-hash/linguisticHashWordSelection.ts 3.70ms
1853/2072 src/utils/markdown/addAutoGeneratedSection.test.ts 2.84ms
1854/2072 src/utils/markdown/addAutoGeneratedSection.ts 3.25ms
1855/2072 src/utils/markdown/createMarkdownChart.test.ts 2.33ms
1856/2072 src/utils/markdown/createMarkdownChart.ts 3.67ms
1857/2072 src/utils/markdown/createMarkdownTable.test.ts 2.35ms
1858/2072 src/utils/markdown/createMarkdownTable.ts 2.46ms
1859/2072 src/utils/markdown/escapeMarkdownBlock.test.ts 1.79ms
1860/2072 src/utils/markdown/escapeMarkdownBlock.ts 1.49ms
1861/2072 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 2.10ms
1862/2072 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 5.86ms
1863/2072 src/utils/markdown/extractAllBlocksFromMarkdown.ts 3.85ms
1864/2072 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 3.65ms
1865/2072 src/utils/markdown/extractAllListItemsFromMarkdown.ts 2.70ms
1866/2072 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 6.78ms
1867/2072 src/utils/markdown/extractOneBlockFromMarkdown.ts 3.61ms
1868/2072 src/utils/markdown/flattenMarkdown.test.ts 4.38ms
1869/2072 src/utils/markdown/flattenMarkdown.ts 4.01ms
1870/2072 src/utils/markdown/humanizeAiText.test.ts 4.04ms
1871/2072 src/utils/markdown/humanizeAiText.ts 2.31ms
1872/2072 src/utils/markdown/humanizeAiTextEllipsis.ts 2.21ms
1873/2072 src/utils/markdown/humanizeAiTextEmdashed.ts 1.87ms
1874/2072 src/utils/markdown/humanizeAiTextQuotes.ts 1.79ms
1875/2072 src/utils/markdown/humanizeAiTextSources.ts 1.90ms
1876/2072 src/utils/markdown/humanizeAiTextWhitespace.ts 1.84ms
1877/2072 src/utils/markdown/parseMarkdownSection.test.ts 1.80ms
1878/2072 src/utils/markdown/parseMarkdownSection.ts 3.20ms
1879/2072 src/utils/markdown/prettifyMarkdown.ts 1.90ms
1880/2072 src/utils/markdown/promptbookifyAiText.test.ts 2.46ms
1881/2072 src/utils/markdown/promptbookifyAiText.ts 2.85ms
1882/2072 src/utils/markdown/README.md 1.08ms
1883/2072 src/utils/markdown/removeMarkdownComments.test.ts 2.70ms
1884/2072 src/utils/markdown/removeMarkdownComments.ts 2.07ms
1885/2072 src/utils/markdown/removeMarkdownFormatting.test.ts 3.35ms
1886/2072 src/utils/markdown/removeMarkdownFormatting.ts 1.52ms
1887/2072 src/utils/markdown/removeMarkdownLinks.test.ts 1.55ms
1888/2072 src/utils/markdown/removeMarkdownLinks.ts 1.40ms
1889/2072 src/utils/markdown/shiftMarkdownHeadingLevels.ts 2.80ms
1890/2072 src/utils/markdown/splitMarkdownIntoSections.test.ts 2.80ms
1891/2072 src/utils/markdown/splitMarkdownIntoSections.ts 2.58ms
1892/2072 src/utils/markdown/trimCodeBlock.test.ts 3.17ms
1893/2072 src/utils/markdown/trimCodeBlock.ts 1.90ms
1894/2072 src/utils/markdown/trimEndOfCodeBlock.test.ts 1.83ms
1895/2072 src/utils/markdown/trimEndOfCodeBlock.ts 1.72ms
1896/2072 src/utils/misc/$getCurrentDate.ts 1.56ms
1897/2072 src/utils/misc/$Register.ts 4.13ms
1898/2072 src/utils/misc/aboutPromptbookInformation.ts 3.74ms
1899/2072 src/utils/misc/arrayableToArray.test.ts 1.44ms
1900/2072 src/utils/misc/arrayableToArray.ts 1.58ms
1901/2072 src/utils/misc/computeHash.test.ts 1.58ms
1902/2072 src/utils/misc/computeHash.ts 2.10ms
1903/2072 src/utils/misc/createLazyModuleLoader.ts 2.29ms
1904/2072 src/utils/misc/debounce.test.ts 2.56ms
1905/2072 src/utils/misc/debounce.ts 1.54ms
1906/2072 src/utils/misc/emojis.ts 4.18ms
1907/2072 src/utils/misc/FromtoItems.ts 1.82ms
1908/2072 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 4.56ms
1909/2072 src/utils/misc/parseNumber.test.ts 3.03ms
1910/2072 src/utils/misc/parseNumber.ts 3.43ms
1911/2072 src/utils/misc/xAboutPromptbookInformation.tsx 2.69ms
1912/2072 src/utils/normalization/capitalize.test.ts 1.74ms
1913/2072 src/utils/normalization/capitalize.ts 1.68ms
1914/2072 src/utils/normalization/constructImageFilename.test.ts 2.56ms
1915/2072 src/utils/normalization/constructImageFilename.ts 2.37ms
1916/2072 src/utils/normalization/decapitalize.test.ts 1.74ms
1917/2072 src/utils/normalization/decapitalize.ts 1.69ms
1918/2072 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 5.45ms
1919/2072 src/utils/normalization/IKeywords.ts 1.80ms
1920/2072 src/utils/normalization/index.ts 0.72ms
1921/2072 src/utils/normalization/isValidKeyword.test.ts 1.79ms
1922/2072 src/utils/normalization/isValidKeyword.ts 1.80ms
1923/2072 src/utils/normalization/nameToUriPart.test.ts 1.83ms
1924/2072 src/utils/normalization/nameToUriPart.ts 1.66ms
1925/2072 src/utils/normalization/nameToUriParts.test.ts 1.82ms
1926/2072 src/utils/normalization/nameToUriParts.ts 1.42ms
1927/2072 src/utils/normalization/normalize-to-kebab-case.test.ts 2.16ms
1928/2072 src/utils/normalization/normalize-to-kebab-case.ts 2.75ms
1929/2072 src/utils/normalization/normalizeMessageText.test.ts 1.56ms
1930/2072 src/utils/normalization/normalizeMessageText.ts 2.05ms
1931/2072 src/utils/normalization/normalizeTo_camelCase.test.ts 1.67ms
1932/2072 src/utils/normalization/normalizeTo_camelCase.ts 3.10ms
1933/2072 src/utils/normalization/normalizeTo_PascalCase.test.ts 3.11ms
1934/2072 src/utils/normalization/normalizeTo_PascalCase.ts 2.11ms
1935/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 1.64ms
1936/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 3.29ms
1937/2072 src/utils/normalization/normalizeTo_snake_case.test.ts 1.79ms
1938/2072 src/utils/normalization/normalizeTo_snake_case.ts 1.55ms
1939/2072 src/utils/normalization/normalizeWhitespaces.test.ts 1.56ms
1940/2072 src/utils/normalization/normalizeWhitespaces.ts 1.83ms
1941/2072 src/utils/normalization/orderJson.test.ts 2.20ms
1942/2072 src/utils/normalization/orderJson.ts 2.04ms
1943/2072 src/utils/normalization/parseKeywords.test.ts 2.49ms
1944/2072 src/utils/normalization/parseKeywords.ts 2.86ms
1945/2072 src/utils/normalization/parseKeywordsFromString.test.ts 4.15ms
1946/2072 src/utils/normalization/parseKeywordsFromString.ts 3.08ms
1947/2072 src/utils/normalization/removeDiacritics.test.ts 2.12ms
1948/2072 src/utils/normalization/removeDiacritics.ts 1.70ms
1949/2072 src/utils/normalization/removeEmojis.test.ts 2.04ms
1950/2072 src/utils/normalization/removeEmojis.ts 1.52ms
1951/2072 src/utils/normalization/removeQuotes.test.ts 1.58ms
1952/2072 src/utils/normalization/removeQuotes.ts 1.76ms
1953/2072 src/utils/normalization/searchKeywords.test.ts 1.74ms
1954/2072 src/utils/normalization/searchKeywords.ts 1.79ms
1955/2072 src/utils/normalization/suffixUrl.test.ts 1.56ms
1956/2072 src/utils/normalization/suffixUrl.ts 1.50ms
1957/2072 src/utils/normalization/titleToName.test.ts 1.96ms
1958/2072 src/utils/normalization/titleToName.ts 3.28ms
1959/2072 src/utils/normalization/unwrapResult.test.ts 5.74ms
1960/2072 src/utils/normalization/unwrapResult.ts 5.19ms
1961/2072 src/utils/organization/___and___.ts 1.73ms
1962/2072 src/utils/organization/___or___.ts 1.59ms
1963/2072 src/utils/organization/$side_effect.ts 1.19ms
1964/2072 src/utils/organization/$sideEffect.ts 1.72ms
1965/2072 src/utils/organization/empty_object.ts 1.29ms
1966/2072 src/utils/organization/just_empty_object.ts 1.25ms
1967/2072 src/utils/organization/just.ts 1.40ms
1968/2072 src/utils/organization/keepImported.ts 1.88ms
1969/2072 src/utils/organization/keepTypeImported.ts 1.57ms
1970/2072 src/utils/organization/keepUnused.ts 1.84ms
1971/2072 src/utils/organization/preserve.ts 1.85ms
1972/2072 src/utils/organization/README.md 1.22ms
1973/2072 src/utils/organization/really_any.ts 1.26ms
1974/2072 src/utils/organization/really_unknown.ts 1.24ms
1975/2072 src/utils/organization/spaceTrim.ts 1.38ms
1976/2072 src/utils/organization/TODO_any.ts 1.16ms
1977/2072 src/utils/organization/TODO_narrow.ts 1.06ms
1978/2072 src/utils/organization/TODO_object.ts 1.10ms
1979/2072 src/utils/organization/TODO_remove_as.ts 1.04ms
1980/2072 src/utils/organization/TODO_string.ts 1.32ms
1981/2072 src/utils/organization/TODO_unknown.ts 1.48ms
1982/2072 src/utils/organization/TODO_USE.ts 1.54ms
1983/2072 src/utils/parameters/extractParameterNames.test.ts 2.64ms
1984/2072 src/utils/parameters/extractParameterNames.ts 1.79ms
1985/2072 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 3.00ms
1986/2072 src/utils/parameters/mapAvailableToExpectedParameters.ts 4.06ms
1987/2072 src/utils/parameters/numberToString.test.ts 1.53ms
1988/2072 src/utils/parameters/numberToString.ts 1.70ms
1989/2072 src/utils/parameters/templateParameters.test.ts 5.36ms
1990/2072 src/utils/parameters/templateParameters.ts 4.17ms
1991/2072 src/utils/parameters/valueToString.test.ts 1.93ms
1992/2072 src/utils/parameters/valueToString.ts 2.57ms
1993/2072 src/utils/random/$generateBookBoilerplate.test.ts 1.92ms
1994/2072 src/utils/random/$generateBookBoilerplate.ts 3.99ms
1995/2072 src/utils/random/$randomAgentPersona.ts 3.19ms
1996/2072 src/utils/random/$randomAgentRule.ts 2.29ms
1997/2072 src/utils/random/$randomBase58.ts 2.30ms
1998/2072 src/utils/random/$randomFullnameWithColor.ts 4.00ms
1999/2072 src/utils/random/$randomItem.ts 1.47ms
2000/2072 src/utils/random/$randomSeed.ts 1.50ms
2001/2072 src/utils/random/$randomToken.ts 1.75ms
2002/2072 src/utils/random/CzechNamePool.ts 6.09ms
2003/2072 src/utils/random/EnglishNamePool.ts 4.23ms
2004/2072 src/utils/random/getNamePool.ts 1.86ms
2005/2072 src/utils/random/NamePool.ts 1.79ms
2006/2072 src/utils/serialization/$deepFreeze.ts 2.47ms
2007/2072 src/utils/serialization/asSerializable.test.ts 1.50ms
2008/2072 src/utils/serialization/asSerializable.ts 2.73ms
2009/2072 src/utils/serialization/checkSerializableAsJson.ts 7.01ms
2010/2072 src/utils/serialization/clonePipeline.ts 3.30ms
2011/2072 src/utils/serialization/deepClone.test.ts 2.64ms
2012/2072 src/utils/serialization/deepClone.ts 2.88ms
2013/2072 src/utils/serialization/exportJson.ts 3.20ms
2014/2072 src/utils/serialization/isSerializableAsJson.test.ts 2.61ms
2015/2072 src/utils/serialization/isSerializableAsJson.ts 2.11ms
2016/2072 src/utils/serialization/jsonStringsToJsons.test.ts 2.73ms
2017/2072 src/utils/serialization/jsonStringsToJsons.ts 2.10ms
2018/2072 src/utils/serialization/serializeToPromptbookJavascript.test.ts 1.69ms
2019/2072 src/utils/serialization/serializeToPromptbookJavascript.ts 3.64ms
2020/2072 src/utils/sets/difference.test.ts 1.75ms
2021/2072 src/utils/sets/difference.ts 1.68ms
2022/2072 src/utils/sets/intersection.test.ts 1.98ms
2023/2072 src/utils/sets/intersection.ts 2.08ms
2024/2072 src/utils/sets/union.test.ts 1.47ms
2025/2072 src/utils/sets/union.ts 1.41ms
2026/2072 src/utils/take/classes/TakeChain.ts 1.75ms
2027/2072 src/utils/take/interfaces/ITakeChain.ts 1.83ms
2028/2072 src/utils/take/interfaces/Takeable.ts 1.24ms
2029/2072 src/utils/take/take.test.ts 3.43ms
2030/2072 src/utils/take/take.ts 2.04ms
2031/2072 src/utils/toolCalls/getToolCallIdentity.ts 1.77ms
2032/2072 src/utils/toolCalls/mergeToolCalls.ts 6.44ms
2033/2072 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 2.55ms
2034/2072 src/utils/validators/email/isValidEmail.test.ts 1.46ms
2035/2072 src/utils/validators/email/isValidEmail.ts 2.21ms
2036/2072 src/utils/validators/filePath/isRootPath.test.ts 1.42ms
2037/2072 src/utils/validators/filePath/isRootPath.ts 1.46ms
2038/2072 src/utils/validators/filePath/isValidFilePath.test.ts 4.11ms
2039/2072 src/utils/validators/filePath/isValidFilePath.ts 2.50ms
2040/2072 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 1.60ms
2041/2072 src/utils/validators/javascriptName/isValidJavascriptName.ts 2.01ms
2042/2072 src/utils/validators/parameterName/validateParameterName.test.ts 2.40ms
2043/2072 src/utils/validators/parameterName/validateParameterName.ts 3.23ms
2044/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 1.60ms
2045/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 2.32ms
2046/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 1.62ms
2047/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 1.61ms
2048/2072 src/utils/validators/url/extractUrlsFromText.test.ts 1.85ms
2049/2072 src/utils/validators/url/extractUrlsFromText.ts 4.65ms
2050/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 2.35ms
2051/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 4.04ms
2052/2072 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 1.81ms
2053/2072 src/utils/validators/url/isUrlOnPrivateNetwork.ts 2.19ms
2054/2072 src/utils/validators/url/isValidAgentUrl.test.ts 2.44ms
2055/2072 src/utils/validators/url/isValidAgentUrl.ts 2.77ms
2056/2072 src/utils/validators/url/isValidPipelineUrl.test.ts 1.97ms
2057/2072 src/utils/validators/url/isValidPipelineUrl.ts 2.19ms
2058/2072 src/utils/validators/url/isValidUrl.test.ts 2.88ms
2059/2072 src/utils/validators/url/isValidUrl.ts 2.30ms
2060/2072 src/utils/validators/url/normalizeDomainForMatching.test.ts 2.64ms
2061/2072 src/utils/validators/url/normalizeDomainForMatching.ts 3.12ms
2062/2072 src/utils/validators/uuid/isValidUuid.test.ts 2.33ms
2063/2072 src/utils/validators/uuid/isValidUuid.ts 6.38ms
2064/2072 src/version.ts 2.20ms
2065/2072 src/wizard/$getCompiledBook.ts 9.55ms
2066/2072 src/wizard/test/books/test.book.md 1.48ms
2067/2072 src/wizard/test/README.md 2.06ms
2068/2072 src/wizard/test/sub/books/test.book.md 1.36ms
2069/2072 src/wizard/test/sub/subsub/books/test.book.md 1.32ms
2070/2072 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 1.53ms
2071/2072 src/wizard/test/sub/subsub/subsubsub/README.md 1.18ms
2072/2072 src/wizard/wizard.ts 7.87ms
CSpell: Files checked: 2072, Issues found: 0 in 0 files.

> promptbook@0.114.0-33 test-lint
> eslint src


> promptbook@0.114.0-33 test-types
> tsc


> promptbook@0.114.0-33 test-books
> echo "Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

"Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

> promptbook@0.114.0-33 test-book-components-build
> echo "Note: Skipping book components build tests because of constants problems with this test"

"Note: Skipping book components build tests because of constants problems with this test"

> promptbook@0.114.0-33 test-package-generation
> ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler

📦  Generating packages
[36m0️⃣  Prepare the needed information about the packages[39m
Promptbook version 0.114.0-33
[36m1️⃣  Generate entry file for each package[39m
[32mGenerated index file ./src/_packages/anthropic-claude.index.ts[39m
[32mGenerated index file ./src/_packages/azure-openai.index.ts[39m
[32mGenerated index file ./src/_packages/browser.index.ts[39m
[32mGenerated index file ./src/_packages/cli.index.ts[39m
[32mGenerated index file ./src/_packages/color.index.ts[39m
[32mGenerated index file ./src/_packages/components.index.ts[39m
[32mGenerated index file ./src/_packages/core.index.ts[39m
[32mGenerated index file ./src/_packages/deepseek.index.ts[39m
[32mGenerated index file ./src/_packages/documents.index.ts[39m
[32mGenerated index file ./src/_packages/editable.index.ts[39m
[32mGenerated index file ./src/_packages/fake-llm.index.ts[39m
[32mGenerated index file ./src/_packages/google.index.ts[39m
[32mGenerated index file ./src/_packages/javascript.index.ts[39m
[32mGenerated index file ./src/_packages/legacy-documents.index.ts[39m
[32mGenerated index file ./src/_packages/markdown-utils.index.ts[39m
[32mGenerated index file ./src/_packages/markitdown.index.ts[39m
[32mGenerated index file ./src/_packages/node.index.ts[39m
[32mGenerated index file ./src/_packages/ollama.index.ts[39m
[32mGenerated index file ./src/_packages/openai.index.ts[39m
[32mGenerated index file ./src/_packages/pdf.index.ts[39m
[32mGenerated index file ./src/_packages/remote-client.index.ts[39m
[32mGenerated index file ./src/_packages/remote-server.index.ts[39m
[32mGenerated index file ./src/_packages/templates.index.ts[39m
[32mGenerated index file ./src/_packages/types.index.ts[39m
[32mGenerated index file ./src/_packages/utils.index.ts[39m
[32mGenerated index file ./src/_packages/vercel.index.ts[39m
[32mGenerated index file ./src/_packages/website-crawler.index.ts[39m
[32mGenerated index file ./src/_packages/wizard.index.ts[39m
[36m2️⃣  Generate package.json, README and other crucial files for each package[39m
[36m3️⃣  Cleanup build directories for each package[39m
[33mSkipping the cleanup for bundler[39m
[36m4️⃣  Generate bundle for each package[39m
[33mSkipping the bundler[39m
[36m5️⃣  Postprocess the generated bundle[39m
[33mSkipping postprocessing[39m
[36m6️⃣  Add dependencies for each package[39m
[33mBundle file ./packages/editable/esm/index.es.js does not exist[39m
[33mBundle file ./packages/fake-llm/esm/index.es.js does not exist[39m
[33mBundle file ./packages/google/esm/index.es.js does not exist[39m
[33mBundle file ./packages/javascript/esm/index.es.js does not exist[39m
[33mBundle file ./packages/legacy-documents/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markdown-utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markitdown/esm/index.es.js does not exist[39m
[33mBundle file ./packages/node/esm/index.es.js does not exist[39m
[33mBundle file ./packages/ollama/esm/index.es.js does not exist[39m
[33mBundle file ./packages/openai/esm/index.es.js does not exist[39m
[33mBundle file ./packages/pdf/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-client/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-server/esm/index.es.js does not exist[39m
[33mBundle file ./packages/templates/esm/index.es.js does not exist[39m
[33mBundle file ./packages/types/esm/index.es.js does not exist[39m
[33mBundle file ./packages/utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/vercel/esm/index.es.js does not exist[39m
[33mBundle file ./packages/website-crawler/esm/index.es.js does not exist[39m
[33mBundle file ./packages/wizard/esm/index.es.js does not exist[39m
[36m7️⃣  Copy agents-server app to CLI package[39m
Copying ./apps/agents-server to ./packages/cli/apps/agents-server
Copying ./apps/_common to ./packages/cli/apps/_common
Copying ./src to ./packages/cli/src
Copying ./books to ./packages/cli/books
Copying ./servers.ts to ./packages/cli/servers.ts
Copying ./security.config.ts to ./packages/cli/security.config.ts
[32mAgents-server app copied successfully[39m
Copying ./agents/default/developer.book to ./packages/cli/agents/default/developer.book
[36m8️⃣  Test that nothing what should not be published is published[39m
[33mSkipping the bundler, skipping the tests for bundle content[39m
[36m9️⃣  Make publishing instructions for Github Actions[39m

> promptbook@0.114.0-33 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

PASS scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.test.ts (49.631 s)
  ● Console

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      runner stderr

      13 |     if (source === 'stderr') {
      14 |         if (chunk.trim()) {
    > 15 |             console.warn(chunk);
         |                     ^
      16 |         }
      17 |
      18 |         return;

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      test failure

      13 |     if (source === 'stderr') {
      14 |         if (chunk.trim()) {
    > 15 |             console.warn(chunk);
         |                     ^
      16 |         }
      17 |
      18 |         return;

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

PASS src/scripting/_test/postprocessing.test.ts (17.314 s)
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Paůl!“\n\n[1️⃣ Say that again!](?message=Hello%20Pa%C5%AFl!)\n[2️⃣ Say that twice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)\n[3️⃣ Say that thrice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!lůaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!lůaP olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!luaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!luaP olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_L_FA_5C_A_P02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_LUA_P_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Adam!“\n\n[1️⃣ Say that again!](?message=Hello%20Adam!)\n[2️⃣ Say that twice!](?message=Hello%20Adam!Hello%20Adam!)\n[3️⃣ Say that thrice!](?message=Hello%20Adam!Hello%20Adam!Hello%20Adam!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      MAD_A02_OLLE_H_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_MAD_A02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_MAD_A_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello John!“\n\n[1️⃣ Say that again!](?message=Hello%20John!)\n[2️⃣ Say that twice!](?message=Hello%20John!Hello%20John!)\n[3️⃣ Say that thrice!](?message=Hello%20John!Hello%20John!Hello%20John!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      NHO_J02_OLLE_H_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_NHO_J02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_NHO_J_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello DAVID!“\n\n[1️⃣ Say that again!](?message=Hello%20DAVID!)\n[2️⃣ Say that twice!](?message=Hello%20DAVID!Hello%20DAVID!)\n[3️⃣ Say that thrice!](?message=Hello%20DAVID!Hello%20DAVID!Hello%20DAVID!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      DIVAD02_OLLE_H_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_DIVAD02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_DIVAD_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS scripts/run-codex-prompts/common/runGoScript/$spawnLoggedBashScript.test.ts (18.802 s)
PASS src/scrapers/markdown/MarkdownScraper.test.ts (13.832 s)
PASS scripts/run-codex-prompts/git/coderCommitScope.test.ts (13.59 s)
PASS src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts (6.981 s)
PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.optimisticNavigation.test.tsx (10.002 s)
PASS src/llm-providers/mocked/test/mocked-completion.test.ts
  ● Console

    console.info
      🖋 Mocked callCompletionModel call

      at MockedEchoLlmExecutionTools.callCompletionModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:141:21)
          at async Promise.all (index 0)

PASS scripts/utils/findAllProjectEntities.test.ts
  ● Console

    console.error
      Duplicate entity name "listAllFiles" found in files:
       - C:/Users/me/work/ai/promptbook/src/utils/files/listAllFiles.ts
       - C:/Users/me/work/ai/promptbook/scripts/delete-openai-resources/listAllOpenAiResources.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "BOOK_LANGUAGE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "PROMPTBOOK_ENGINE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "string_promptbook_version" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "value$" found in files:
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/analyzeSourceFileForRefactorCandidate.test.ts
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "MonacoEditorWithShadowDom" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/_common/MonacoEditorWithShadowDom.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/_utils/MonacoEditorWithShadowDom.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadProgressCallback" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadOptions" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackMode.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatVisualMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/chatVisualMode.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "FriendlyErrorMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/errorMessages.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/types/ChatMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/types.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "RunBrowserToolError" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/tools/runBrowserErrors.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "LocalAgentRunnerLimits" found in files:
       - C:/Users/me/work/ai/promptbook/src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverLimits.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Json" found in files:
       - C:/Users/me/work/ai/promptbook/src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/schema.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "TIME_INTERVALS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "HTTP_STATUS_CODES" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_TOKEN" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_INTERVAL_MS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "nameToSubfolderPath" found in files:
       - C:/Users/me/work/ai/promptbook/src/storage/file-cache-storage/utils/nameToSubfolderPath.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/cdn/utils/nameToSubfolderPath.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "id" found in files:
       - C:/Users/me/work/ai/promptbook/src/types/string_token.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "async" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/[...rest]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/about/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/api-tokens/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/backup/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/browser-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-feedback/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-css/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-js/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/error-simulation/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/files/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/image-generator-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/login-methods/shibboleth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/send-email/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/metadata/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/models/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/planned-messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/search-engine-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager-vps/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/[taskId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/tool-limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/transcriptions/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/usage/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/[userId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/voice-input-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/goal/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/iframe/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/[docId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/cli-access/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/database/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/environment/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/harness-auth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/logs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/servers/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/update/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/profile/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-memory/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-wallet/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/view/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "function" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/experiments/story/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/global-error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/not-found.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/restricted/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/search/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/story/[[...story]]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/settings/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/VercelDeploymentCard/VercelDeploymentCard.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_app.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_document.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/500.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "dynamic" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "parseBooleanFlag" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/_utils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/analyticsMetadata.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "AgentProfile" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/AgentProfileWrapper.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/AgentProfile/AgentProfile.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "runtime" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "GET" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/system-message/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/default-avatar.png/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/icon-256.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-fullhd.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-phone.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/files/[...filePath]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/error-simulation/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/logs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-artifacts/[artifactName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/threads/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-streaming/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/embed.js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/images/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/long-running-task/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/check/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/scrape/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/story/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/team-agent-profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/usage/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/s3/[first]/[second]/[hash]/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "POST" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/missing-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/reference-diagnostics/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/[connectionId]/disconnect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openrouter/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/share-target/[shareTargetId]/consume/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/share-target/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/retry/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/default-agents/reinstate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/dns-records/cloudflare/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/migrate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/clone/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/change-password/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/logout/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/acs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/refresh/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/revoke/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/citation-label/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/stalwart/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/error-reports/application/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/evaluate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/test/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/audio/transcriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/input/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/send-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/spawn-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/upload/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PUT" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "maxDuration" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "OPTIONS" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "DELETE" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PATCH" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/draft/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/visibility/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "as" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/CanonicalAgentChatSurface.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatExport/downloadChatPdfFromServer.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "generateMetadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatsSnapshot" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/useAgentChatHistorySyncState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "header" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/AgentCodePageHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/useAgentCodeExportState.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "banner" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeErrorBanner.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeExportWarningBanner.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "YourComponent" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "alt" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "size" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "contentType" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "revalidate" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "metadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ControlPanelOptionAvailability" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Header/ControlPanel/ControlPanelContentState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/getControlPanelOptionAvailability.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "collectDescendantFolderIds" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOrganization/folderTree.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "findAgentByIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOwnership.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "are" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/AgentsListHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Card" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/Card.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Card.tsx

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "normalizeServerUrl" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/normalizeServerUrl.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/search/createDefaultServerSearchProviders/normalizeServerUrl.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Button" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Button.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "quoteIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/sqlite/localSqliteSupabase/localSqliteSql.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverManagement/createManagedServer/createSqlRecorder.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/quoteIdentifier.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRow.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackAdmin.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CalendarProvider" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/calendarConnections/CalendarConnectionRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/providers/CalendarProvider.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "MetaDisclaimerStatus" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimer.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimerClient.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "listRegisteredServersUsingServiceRole" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistry.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistryNode.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UsageAnalyticsQuery" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAdmin.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAnalytics/UsageAnalyticsQuery.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "createUserChat" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/createUserChat.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/createUserChat.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatSummary" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "cancelUserChatTimeout" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/cancelUserChatTimeout.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/cancelUserChatTimeout.ts

      160 |             reportedEntityNames.add(entity.name);
      161 |
    > 162 |             console.error(
          |                     ^
      163 |                 `Duplicate entity name "${entity.name}" found in files:\n${[
      164 |                     entity.filename,
      165 |                     ...duplicates.map((existingEntity) => existingEntity.filename),

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

PASS src/scripting/_test/custom-function-async.test.ts.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS apps/agents-server/src/tools/$provideCdnForServer.test.ts
PASS scripts/run-agent-messages/git/commitAgentProjectChanges.test.ts (6.324 s)
PASS src/utils/markdown/humanizeAiText.test.ts
PASS apps/agents-server/src/message-providers/email/stalwart/parseInboundStalwartEmail.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts
  ● Console

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/model-variant-mismatch.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/sample-dont-pass-expectations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/void-knowledge.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

PASS apps/agents-server/src/utils/bookLanguageDocumentation/createBookLanguageDocumentationPdfResponse.test.ts
PASS scripts/run-agent-messages/main/runMultipleAgentMessages.test.ts
  ● Console

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/first.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/second.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-active/messages/queued/question.book with Active Agent.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mCloned 1 new agent repository: agent-new[39m

      at MultipleAgentGithubSynchronizer.synchronizeIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentGithubSynchronizer.ts:71:21)

    console.info
      [34mProcessing agent-new/messages/queued/question.book with Agent new.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mPulling latest changes for 2 watched agent repositories...[39m

      at MultipleAgentAutoPuller.pullIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentAutoPuller.ts:56:21)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

PASS apps/agents-server/src/tools/agentGoalChatTimeoutTools.test.ts
PASS scripts/run-agent-messages/main/runAgentMessages.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeRegistry.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
PASS src/book-components/Chat/LlmChat/LlmChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

      68 |         document.body.appendChild(container);
      69 |
    > 70 |         await act(async () => {
         |                  ^
      71 |             const root = createRoot(container);
      72 |             root.render(
      73 |                 <LlmChat title="Test" llmTools={customLlmTools as TODO_any} thread={thread} layout="STANDALONE" />,

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

      70 |         await act(async () => {
      71 |             const root = createRoot(container);
    > 72 |             root.render(
         |                  ^
      73 |                 <LlmChat title="Test" llmTools={customLlmTools as TODO_any} thread={thread} layout="STANDALONE" />,
      74 |             );
      75 |         });

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:72:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

      611 |             });
      612 |
    > 613 |             setMessages((previousMessages) => [...previousMessages, userMessage, loadingMessage]);
          |             ^
      614 |
      615 |             const taskId = `llm_call_${Date.now()}`;
      616 |             setTasksProgress([{ id: taskId, name: 'Generating response...', progress: 0 }]);

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:613:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

      614 |
      615 |             const taskId = `llm_call_${Date.now()}`;
    > 616 |             setTasksProgress([{ id: taskId, name: 'Generating response...', progress: 0 }]);
          |             ^
      617 |
      618 |             const generationStartedAtMs = Date.now();
      619 |             let streamingAbortController: AbortController | null = null;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:616:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

      629 |                 });
      630 |
    > 631 |                 setTasksProgress([{ id: taskId, name: 'Generating response...', progress: 50 }]);
          |                 ^
      632 |
      633 |                 const execution = await executeLlmChatPrompt({
      634 |                     assistantMessageStartedAt,

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:631:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

      644 |
      645 |                 streamingAbortController = execution.streamingAbortController;
    > 646 |                 setTasksProgress([{ id: taskId, name: 'Response generated', progress: 100 }]);
          |                 ^
      647 |
      648 |                 if (!execution.result.content.trim()) {
      649 |                     throw new Error('The agent did not respond.');

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:646:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

      659 |                 });
      660 |
    > 661 |                 setMessages((previousMessages) =>
          |                 ^
      662 |                     updateMessageById(previousMessages, loadingMessage.id, () => assistantMessage),
      663 |                 );
      664 |

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:661:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

      702 |                 }
      703 |
    > 704 |                 setIsStreaming(false);
          |                 ^
      705 |                 requestInFlightRef.current = false;
      706 |             }
      707 |         },

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:704:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

      262 |         await act(async () => {
      263 |             const root = createRoot(container);
    > 264 |             root.render(
          |                  ^
      265 |                 <LlmChat
      266 |                     title="Delegated reset"
      267 |                     llmTools={mockLlmTools}

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:264:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:262:18)

    console.error
      The current testing environment is not configured to support act(...)

      304 |         await act(async () => {
      305 |             const root = createRoot(container);
    > 306 |             root.render(
          |                  ^
      307 |                 <LlmChat title="Test" llmTools={mockLlmTools} initialMessages={initialMessages} layout="STANDALONE" />,
      308 |             );
      309 |         });

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:306:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:304:18)

    console.error
      The current testing environment is not configured to support act(...)

      310 |         }
      311 |
    > 312 |         setMessages(buildInitialMessages());
          |         ^
      313 |         setTasksProgress([]);
      314 |         hasUserInteractedRef.current = false;
      315 |         clearLastFailedMessage();

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:312:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

      311 |
      312 |         setMessages(buildInitialMessages());
    > 313 |         setTasksProgress([]);
          |         ^
      314 |         hasUserInteractedRef.current = false;
      315 |         clearLastFailedMessage();
      316 |

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:313:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

      723 |
      724 |     const clearLastFailedMessage = useCallback(() => {
    > 725 |         setLastFailedMessage(null);
          |         ^
      726 |     }, []);
      727 |
      728 |     const handleRetry = useCallback(() => {

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:725:9
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:315:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

PASS src/book-components/Chat/Chat/ChatMessageItem.test.tsx
PASS src/book-3.0/LiteAgent.test.ts
PASS src/llm-providers/mocked/test/mocked-chat.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

PASS scripts/find-fresh-emoji-tags/utils/$shuffleItems.test.ts
PASS apps/agents-server/src/utils/errorReporting/sendApplicationErrorReportToSentry.test.ts
PASS src/scripting/javascript/utils/extractVariablesFromScript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils
... 897730 bytes omitted ...
Run #6

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #6

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #7

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #9

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #11

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #13

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

PASS apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts
  ● Console

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_cancel {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'cancelled'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:167:17)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.error
      [local-chat-runner] planned_message_command_failed {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        action: 'set',
        error: {
          name: 'Error',
          message: 'Invalid planned-message delay.',
          stack: 'Error: Invalid planned-message delay.\n' +
            '    at Object.<anonymous> (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\utils\\localChatRunner\\applyLocalAgentPlannedMessageCommands.test.ts:96:36)',
          id: undefined
        }
      }

      174 |         return cancelledPlannedMessage.status === 'cancelled' ? cancelledPlannedMessage : null;
      175 |     } catch (error) {
    > 176 |         console.error('[local-chat-runner]', 'planned_message_command_failed', {
          |                 ^
      177 |             chatId: options.job.chatId,
      178 |             jobId: options.job.id,
      179 |             action: options.command.action,

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:176:17)
      at applyLocalAgentPlannedMessageCommands (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:78:24)
      at Object.<anonymous> (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts:99:33)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'updated',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'not_found',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

PASS apps/agents-server/src/tools/getAllToolFunctionsForServer.test.ts
PASS apps/agents-server/src/utils/agentsTransfer/importAgentsFromFiles.test.ts
PASS src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx
PASS src/scrapers/_common/prepareKnowledgePieces.test.ts
PASS apps/agents-server/src/utils/chat/resolveCitationSourceLabel.test.ts
PASS src/conversion/validation/validatePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

PASS apps/agents-server/src/database/sqlite/standaloneServerRegistryStore.test.ts
PASS apps/agents-server/src/instrumentation.test.ts
PASS src/book-components/Chat/AgentChat/AgentChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

      67 |         document.body.appendChild(container);
      68 |
    > 69 |         await act(async () => {
         |                  ^
      70 |             const root = createRoot(container);
      71 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
      72 |         });

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.error
      The current testing environment is not configured to support act(...)

      69 |         await act(async () => {
      70 |             const root = createRoot(container);
    > 71 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
         |                  ^
      72 |         });
      73 |
      74 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:71:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      86 |         await act(async () => {
      87 |             const root = createRoot(container);
    > 88 |             root.render(<AgentChat agent={agent} title="Custom Chat Title" layout="STANDALONE" />);
         |                  ^
      89 |         });
      90 |
      91 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:88:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:86:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      100 |         await act(async () => {
      101 |             const root = createRoot(container);
    > 102 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
          |                  ^
      103 |         });
      104 |
      105 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:102:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:100:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      131 |         await act(async () => {
      132 |             const root = createRoot(container);
    > 133 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
          |                  ^
      134 |         });
      135 |
      136 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:133:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:131:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      155 |         await act(async () => {
      156 |             const root = createRoot(container);
    > 157 |             root.render(
          |                  ^
      158 |                 <AgentChat
      159 |                     agent={agent}
      160 |                     onChange={mockOnChange}

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:157:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:155:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      182 |         await act(async () => {
      183 |             const root = createRoot(container);
    > 184 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
          |                  ^
      185 |         });
      186 |
      187 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:184:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:182:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      208 |         await act(async () => {
      209 |             const root = createRoot(container);
    > 210 |             root.render(<AgentChat agent={minimalAgent} layout="STANDALONE" />);
          |                  ^
      211 |         });
      212 |
      213 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:210:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:208:18)

    console.error
      The current testing environment is not configured to support act(...)

      240 |         await act(async () => {
      241 |             const root = createRoot(container);
    > 242 |             root.render(<AgentChat agent={agent} persistenceKey="test-persistence-key" layout="STANDALONE" />);
          |                  ^
      243 |         });
      244 |
      245 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:242:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:240:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

      254 |         await act(async () => {
      255 |             const root = createRoot(container);
    > 256 |             root.render(<AgentChat agent={agent} layout="STANDALONE" />);
          |                  ^
      257 |         });
      258 |
      259 |         const capturedProps = (globalThis as { __lastLlmChatProps?: CapturedLlmChatProps }).__lastLlmChatProps;

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:256:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:254:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

      74 |             // <- Note: Can not use `assertsError(error)` here because it causes circular dependency
      75 |
    > 76 |             console.warn(
         |                     ^
      77 |                 spaceTrim(
      78 |                     (block) => `
      79 |                         Color.fromSafe error:

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

PASS apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/remote-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://core-test.ptbk.io/agents/adam" after 3 attempts: NotFoundError: Failed to import agent from "https://core-test.ptbk.io/agents/adam"
      
      Agent not found
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:486:10)
          at resolveParentAgentContext (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:526:11)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:838:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:409:37) {
        id: undefined
      }

      124 |             return result.value;
      125 |         } catch (error) {
    > 126 |             console.warn(
          |                     ^
      127 |                 `[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`,
      128 |                 error,
      129 |             );

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:486:10)
      at resolveParentAgentContext (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:526:11)
      at resolveInheritedAgentSource (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:838:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:409:37)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://federated.example/agents/missing-source" after 3 attempts: NotFoundError: Failed to import agent from "https://federated.example/agents/missing-source"
      
      Remote import unavailable
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:486:10)
          at resolveImportCommitmentLine (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:597:39)
          at resolveAgentSourceBuild (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:701:48)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:839:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:499:37) {
        id: undefined
      }

      124 |             return result.value;
      125 |         } catch (error) {
    > 126 |             console.warn(
          |                     ^
      127 |                 `[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`,
      128 |                 error,
      129 |             );

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:486:10)
      at resolveImportCommitmentLine (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:597:39)
      at resolveAgentSourceBuild (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:701:48)
      at resolveInheritedAgentSource (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:839:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:499:37)

PASS apps/agents-server/src/utils/userChat/persistUserChatJobTerminalState.test.ts
  ● Console

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:208:9)
      }

       97 |         });
       98 |     } catch (error) {
    >  99 |         console.error('[ChatHistory] Failed to record durable user-chat message.', {
          |                 ^
      100 |             agentPermanentId,
      101 |             chatId,
      102 |             taskId,

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:242:9)
      }

       97 |         });
       98 |     } catch (error) {
    >  99 |         console.error('[ChatHistory] Failed to record durable user-chat message.', {
          |                 ^
      100 |             agentPermanentId,
      101 |             chatId,
      102 |             taskId,

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

PASS apps/agents-server/src/utils/agentProjects/startDefaultAgentProjectRuntimes.test.ts
PASS scripts/generate-packages/getPackagesMetadataForRollup.test.ts
PASS src/book-components/Chat/Chat/ChatInputArea.test.tsx
PASS apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.test.ts
PASS apps/agents-server/src/utils/agentsTransfer/createAgentsExportZipStream.test.ts
PASS scripts/run-agent-messages/main/tickAgentMessages.test.ts
PASS src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts
PASS src/conversion/compilePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

PASS src/conversion/parsePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

      172 |         if ($taskJson.modelRequirements[command.key] !== undefined) {
      173 |             if ($taskJson.modelRequirements[command.key] === command.value) {
    > 174 |                 console.warn(
          |                         ^
      175 |                     `Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${
      176 |                         $taskJson.title || $taskJson.name
      177 |                     }"`,

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

PASS apps/agents-server/src/components/Header/buildDocumentationDropdownItems.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/book/download/route.test.ts
PASS src/scrapers/_common/utils/promptbookFetch.test.ts
PASS apps/agents-server/src/utils/customDomainRouting.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/parent-agent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS apps/agents-server/src/database/seedDefaultAgents.test.ts
PASS apps/agents-server/src/search/createDefaultServerSearchProviders/createDocumentationSearchProvider.test.ts
PASS apps/agents-server/src/app/admin/backup/BackupClient.test.tsx
PASS apps/agents-server/src/utils/resolveAppendOnlySelfLearningAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS src/scripting/_test/script-execution-tools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "apple";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      bpple

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "a cup of coffee";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      b cup of coffee

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS apps/agents-server/src/components/AgentNaming/translateLegacyAgentText.test.ts
PASS src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx
  ● Console

    console.error
      Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.

      377 |             pendingToggleFallbackTimeoutIds.forEach((timeoutId) => window.clearTimeout(timeoutId));
      378 |             pendingToggleFallbackTimeoutIds.clear();
    > 379 |             rootsRef.current.forEach((root) => root.unmount());
          |                                                     ^
      380 |             rootsRef.current = [];
      381 |         };
      382 |     }, [htmlContent, inlineReferences, resolvedTheme]);

      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24773:21)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:53
          at Array.forEach (<anonymous>)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:30
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23965:13)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at commitHookEffectListUnmount (node_modules/react-dom/cjs/react-dom-client.development.js:11996:17)
      at commitHookPassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:12043:11)
      at commitPassiveUnmountEffectsInsideOfDeletedTree_begin (node_modules/react-dom/cjs/react-dom-client.development.js:14269:13)
      at recursivelyTraversePassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:14167:13)
      at commitPassiveUnmountOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14194:11)
      at flushPassiveEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15736:9)
      at flushPendingEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15702:14)
      at flushSpawnedWork (node_modules/react-dom/cjs/react-dom-client.development.js:15668:44)
      at commitRoot (node_modules/react-dom/cjs/react-dom-client.development.js:15403:9)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14526:15)
      at performSyncWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:16231:7)
      at flushSyncWorkAcrossRoots_impl (node_modules/react-dom/cjs/react-dom-client.development.js:16079:21)
      at flushSyncWork$1 (node_modules/react-dom/cjs/react-dom-client.development.js:14721:12)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24777:11)
      at Object.unmount (node_modules/@testing-library/react/dist/pure.js:163:12)
      at node_modules/@testing-library/react/dist/pure.js:307:12
      at node_modules/@testing-library/react/dist/act-compat.js:47:24
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at node_modules/@testing-library/react/dist/act-compat.js:46:25
      at node_modules/@testing-library/react/dist/pure.js:306:28
          at Array.forEach (<anonymous>)
      at cleanup (node_modules/@testing-library/react/dist/pure.js:302:22)
      at Object.<anonymous> (node_modules/@testing-library/react/dist/index.js:28:25)

PASS apps/agents-server/src/utils/transpilers/getTranspiledAgentExportWarnings.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/externalChatMessageBook.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/resolveTeamCapabilitiesFromAgentSource.test.ts
PASS apps/agents-server/src/utils/importAgent.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/foobarhululu"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/scheduleAgentGoalChatModifiedNote.test.ts
PASS apps/agents-server/src/utils/errorReporting/registerServerErrorSentryLogging.test.ts
PASS apps/agents-server/src/app/superadmin/servers/ServersRegistryTable.test.tsx
PASS src/llm-providers/agent/RemoteAgent.test.ts
  ● Console

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

      37 |
      38 |         // TODO: [🟥] Detect browser / node and make it colorful
    > 39 |         console.warn(warningMessage);
         |                 ^
      40 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      41 |
      42 |         /*

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:128:33)

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

      37 |
      38 |         // TODO: [🟥] Detect browser / node and make it colorful
    > 39 |         console.warn(warningMessage);
         |                 ^
      40 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      41 |
      42 |         /*

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:173:33)

PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectInfo.test.ts
PASS apps/agents-server/src/utils/agentRouting/resolveAgentRouteTarget.test.ts
PASS apps/agents-server/src/database/reinstateDefaultAgents.test.ts
PASS src/scripting/javascript/JavascriptEvalExecutionTools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal.split('').reverse().join('-') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      t-a-c

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "\"cat\"";
      (async ()=>{ return removeQuotes(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "The animal is: \"dog\"";
      (async ()=>{ return unwrapResult(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      dog

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      
      (async ()=>{ return open_popup({ url: "https://ptbk.io" }) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.log
      !!!! [Tool] open_popup called { args: { url: 'https://ptbk.io' } }

      at open_popup (src/commitments/USE_POPUP/USE_POPUP.ts:143:25)

    console.info
      🚀 Script evaluated successfully, result:   
      Popup window with URL "https://ptbk.io" was requested.
      
      Note: The agent is currently running on the server, so the popup cannot be opened automatically.
      The user can open it manually from the tool call details in the chat.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      const get_user_location = customFunctions.get_user_location;
                      const calendar_list_events = customFunctions.calendar_list_events;
                      const calendar_get_event = customFunctions.calendar_get_event;
                      const calendar_create_event = customFunctions.calendar_create_event;
                      const calendar_update_event = customFunctions.calendar_update_event;
                      const calendar_delete_event = customFunctions.calendar_delete_event;
                      const calendar_invite_guests = customFunctions.calendar_invite_guests;
                      const open_popup = customFunctions.open_popup;
                      const turn_privacy_on = customFunctions.turn_privacy_on;
                      const project_list_files = customFunctions.project_list_files;
                      const project_read_file = customFunctions.project_read_file;
                      const project_upsert_file = customFunctions.project_upsert_file;
                      const project_delete_file = customFunctions.project_delete_file;
                      const project_create_branch = customFunctions.project_create_branch;
                      const project_create_pull_request = customFunctions.project_create_pull_request;
      
                      // The script:
                      const animal = "cat";
                      const sound = "meow";
                      (async ()=>{ function makeSentence(animal, sound) {
          return animal + ' makes ' + sound + '.';
      }
      return makeSentence(animal, sound); })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      cat makes meow.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS src/commands/SECTION/sectionCommand.test.ts
PASS apps/agents-server/src/utils/chatExport/sanitizeChatPdfExportHtml.test.ts
PASS src/conversion/validation/pipelineStringToJson-parseErrors.test.ts
PASS src/scripting/_test/custom-function.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS apps/agents-server/src/instrumentation-node.test.ts
  ● Console

    console.info
      🗂️ VPS server registry ready with 0 registered server(s) in standalone SQLite mode.

      at registerNodeRuntimeInstrumentation (apps/agents-server/src/instrumentation-node.ts:29:21)

PASS apps/agents-server/src/utils/chatMessageChips/createAnsweredMessageChipToolCalls.test.ts
PASS src/cli/cli-commands/agents-server/buildAgentsServer.test.ts
PASS src/cli/cli-commands/coder/init.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAllAgentProjectSummaries.test.ts
PASS scripts/run-agent-chat/executeAgentChatTurn.test.ts
PASS src/llm-providers/agent/AgentLlmExecutionTools.test.ts
PASS src/llm-providers/agent/Agent.test.ts
PASS apps/agents-server/src/components/WalletRecordDialog/WalletRecordDialog.test.tsx
PASS apps/agents-server/src/components/AgentProfile/AgentProfile.test.tsx
PASS src/utils/expectation-counters/countPages.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts
  ● Console

    console.warn
      Failed to import file ./local/path/data.json: Error: Invalid file URL or path: ./local/path/data.json
          at readImportedFile (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:135:11)
          at createImportedFileSystemMessage (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:91:41)
          at importReferencedFiles (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:70:37)
          at augmentAgentModelRequirementsFromSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:46:20)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:34:20)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

      71 |             requirements = appendSystemMessageSection(requirements, importedContent);
      72 |         } catch (error) {
    > 73 |             console.warn(`Failed to import file ${fileUrl}:`, error);
         |                     ^
      74 |         }
      75 |     }
      76 |

      at importReferencedFiles (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:73:21)
      at augmentAgentModelRequirementsFromSource (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:46:20)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:34:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

PASS apps/agents-server/src/components/CloudflareDnsWizard/CloudflareDnsWizard.test.tsx
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDomains.test.ts
PASS src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx
PASS src/llm-providers/mocked/test/faked-completion.test.ts
  ● Console

    console.info
      💬 Mocked faked prompt {
        title: 'Prompt',
        pipelineUrl: 'https://promptbook.studio/examples/pipeline.book#prompt',
        parameters: { thing: 'a cup of coffee' },
        content: 'One day I went to the shop and bought {thing}.\nNow I have {thing}.',
        modelRequirements: { modelVariant: 'COMPLETION', modelName: 'gpt-3.5-turbo-instruct' },
        expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } },
        format: undefined,
        postprocessingFunctionNames: undefined
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:87:21)
          at async Promise.all (index 0)

    console.info
      { expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)
          at async Promise.all (index 0)

    console.info
      💬 Mocked faked result {
        content: 'Ut duis elit est eu Lorem esse anim adipisicing. Et ea nostrud fugiat',
        modelName: 'mocked-facked',
        timing: {
          start: '2026-09-12T09:44:48.532Z',
          complete: '2026-09-12T09:44:48.532Z'
        },
        usage: {
          price: { value: 0 },
          duration: { value: 0 },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'One day I went to the shop and bought a cup of coffee.\n' +
          'Now I have a cup of coffee.',
        rawRequest: null,
        rawResponse: { note: 'This is mocked echo' }
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:122:21)

PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSource.test.ts
PASS src/scrapers/website/utils/createShowdownConverter.test.ts
PASS src/scripting/_test/custom-function-with-dependencies.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Matthew the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist (28 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Mark the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Luke the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello John the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/cli/cli-commands/coder/boilerplateTemplates.test.ts
PASS apps/agents-server/src/components/DnsRecordsInstructions/DnsRecordsInstructions.test.tsx
PASS scripts/run-agent-chat/runAgentChat.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts
PASS src/scrapers/document-legacy/LegacyDocumentScraper.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts
PASS scripts/run-agent-messages/messages/resolveAnsweredMessageTouches.test.ts
PASS src/scripting/_test/custom-function-missing.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHelloWithTypo = customFunctions.addHelloWithTypo;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

PASS src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx

  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)


  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

      20 |
      21 |     for (const warning of warnings) {
    > 22 |         console.warn(warning.message);
         |                 ^
      23 |         // <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks
      24 |     }
      25 |

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

PASS apps/agents-server/src/components/UsersList/PasswordGeneratorDialog.test.tsx
PASS scripts/run-codex-prompts/prompts/writePromptRunTrace.test.ts
PASS apps/agents-server/src/components/ApplicationErrorPage/ApplicationErrorPage.test.tsx
PASS apps/agents-server/src/app/api/auth/change-password/route.test.ts
PASS apps/agents-server/src/utils/serverManagement/standaloneVpsServerMetadata.test.ts
PASS src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts
PASS src/dialogs/user-interface-execution-tools.test.ts
  ● Console

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite apple to buy?" is not apple but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite a cup of coffee to buy?" is not a cup of coffee but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

PASS apps/agents-server/src/utils/agentProjects/humanizeAgentProjectName.test.ts
PASS scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts
PASS scripts/run-codex-prompts/common/waitForPause.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts
PASS apps/agents-server/src/utils/metadataConfigurationTransfer.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
  ● Console

    console.warn
      Failed to resolve commitment references for FROM, falling back to safe defaults: Error: Unable to resolve agent reference "foo"
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:32:23)
          at C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:397:39
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:404:13)
          at Object.mockConstructor [as resolveCommitmentContent] (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:148:19)
          at resolveCommitmentContent (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:68:45)
          at applyCommitmentsToAgentModelRequirements (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:37:41)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:33:66)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:84)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      68 |         return await agentReferenceResolver.resolveCommitmentContent(commitment.type, commitment.content);
      69 |     } catch (error) {
    > 70 |         console.warn(
         |                 ^
      71 |             `Failed to resolve commitment references for ${commitment.type}, falling back to safe defaults:`,
      72 |             error,
      73 |         );

      at resolveCommitmentContent (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:70:17)
      at applyCommitmentsToAgentModelRequirements (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:37:35)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:33:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:35)

PASS src/llm-providers/mocked/test/joker.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)

PASS apps/agents-server/src/utils/agentProjects/createAgentProjectMarkdownReferences.test.ts
PASS src/llm-providers/agent/self-learning/SelfLearningManager.test.ts
  ● Console

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nINITIAL MESSAGE Hello! I am your helpful assistant.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '- The agent source does not have an INITIAL MESSAGE defined, generate one.\n' +
          '- The INITIAL MESSAGE should be welcoming, informative about the agent capabilities and also should give some quick options to start the conversation with the agent.\n' +
          '- The quick option looks like `[👋 Hello](?message=Hello, how are you?)`\n' +
          '- A browser action option looks like `[🖨️ Print](?action=window.print%28%29)` and runs only after the user clicks it.\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nKNOWLEDGE New info.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          'INITIAL MESSAGE Welcome!\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts
PASS apps/agents-server/src/components/AgentProjects/useAgentProjectMarkdownReferences.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/normalizeCreateServerInput.test.ts
PASS src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts
PASS src/utils/execCommand/execCommand.test.ts
PASS apps/agents-server/src/utils/defaultAgents/resolveDefaultAgentsStatus.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectFaviconRelativePath.test.ts
PASS src/cli/cli-commands/coder/add.test.ts
  ● Console

    console.info
      [32m✓ Added prompt [✨💯] in prompts\2026-09-0000-add-a-dark-mode-toggle.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🥢] in prompts\2026-09-0000-task-with-priority.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨💽] in prompts\2026-09-0000-first-line-summary.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🟥] in prompts\2026-09-0000-first-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🈶] in prompts\2026-09-0010-second-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

PASS apps/agents-server/src/utils/userChat/createUserChatJobHeartbeatController.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts
PASS src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts
PASS src/book-2.0/agent-source/communication-samples.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ensureExternalAgentRepository.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectAccess.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts
PASS src/book-components/Chat/Chat/CitationIframePreview.test.tsx
PASS apps/agents-server/src/utils/knowledge/resolveWebsiteKnowledgeSourcesForServer.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts
PASS apps/agents-server/src/utils/projects/extractProjectRepositoriesFromAgentSource.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/ExternalUserChatAdminActions.test.tsx
PASS src/book-components/Chat/Chat/ChatActionsBar.test.tsx
PASS src/avatars/renderAvatarVisualTerminalText.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts
PASS scripts/run-codex-prompts/common/createFreeDiskSpaceGuard.test.ts
PASS apps/agents-server/src/utils/userChat/listUserChats.test.ts
PASS apps/agents-server/src/components/ChatEnterBehavior/ChatEnterBehaviorPreferencesProvider.test.tsx
PASS apps/agents-server/src/app/admin/messages/MessageTableCells.test.tsx
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
PASS apps/agents-server/src/utils/agentProjects/hasAgentProjects.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts
PASS apps/agents-server/src/utils/thinkingMessages.test.ts
PASS src/conversion/validation/validatePipeline-logicErrors.test.ts
PASS src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatSummary.agentGoalChat.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts
PASS apps/agents-server/src/tools/run_browser.test.ts
  ● Console

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.warn
      [run_browser] Full-page snapshot failed, retrying viewport-only screenshot {
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        snapshotFilename: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000-initial.png',
        error: 'Screenshot is too large'
      }

      78 |                 await page.screenshot({ path: snapshotPath, fullPage: true });
      79 |             } catch (error) {
    > 80 |                 console.warn('[run_browser] Full-page snapshot failed, retrying viewport-only screenshot', {
         |                         ^
      81 |                     sessionId,
      82 |                     snapshotFilename,
      83 |                     error: getErrorMessage(error),

      at Object.captureSnapshot (apps/agents-server/src/tools/runBrowserArtifacts.ts:80:25)
      at Object.captureSnapshotArtifact (apps/agents-server/src/tools/runBrowserArtifacts.ts:133:22)
      at run_browser (apps/agents-server/src/tools/run_browser.ts:131:33)
      at Object.<anonymous> (apps/agents-server/src/tools/run_browser.test.ts:201:24)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 5,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 2,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'fallback',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_fallback_used',
        errorCode: 'REMOTE_BROWSER_UNAVAILABLE',
        errorCodeCount: 1,
        fallbackRuns: 1,
        totalRuns: 6,
        fallbackRate: 0.16666666666666666
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_NAVIGATION_FAILED',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

PASS src/collection/pipeline-collection/pipelineCollectionToJson.test.ts
PASS src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/OptimisticAgentChatRouteLoading.test.tsx
PASS apps/agents-server/scripts/sync-vercel-domains.test.ts
PASS src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx
PASS apps/agents-server/src/components/AgentAvatar/AgentAvatar.test.tsx
PASS src/scrapers/document/DocumentScraper.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTesting.test.ts
PASS scripts/run-codex-prompts/git/commitChanges.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectProfile.test.ts
PASS apps/agents-server/src/components/CronJobConfiguration/CronJobConfiguration.test.tsx
PASS apps/agents-server/src/components/Homepage/HomepageOptimisticNavigation.test.tsx
PASS src/executables/browsers/locateBrowser.test.ts
PASS scripts/run-codex-prompts/main/runPromptRound.test.ts
  ● Console

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

      310 |
      311 |     if (!isRichUiEnabled) {
    > 312 |         console.warn(
          |                 ^
      313 |             colors.yellow(
      314 |                 `Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`,
      315 |             ),

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

PASS apps/agents-server/src/utils/internalS3/readInternalS3Directory.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.test.ts
PASS src/book-components/Chat/SourceChip/SourceChip.test.tsx
PASS apps/agents-server/src/tools/spawn_agent.test.ts
  ● Console

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'anonymous', sourceLength: 36 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nUnknown field(s): `unknownField`.'
      }

      120 |         };
      121 |
    > 122 |         console.warn('[spawn_agent] Failed to create agent', {
          |                 ^
      123 |             errorName: error.name,
      124 |             errorMessage: error.message,
      125 |         });

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:106:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nMissing required field `source`.'
      }

      120 |         };
      121 |
    > 122 |         console.warn('[spawn_agent] Failed to create agent', {
          |                 ^
      123 |             errorName: error.name,
      124 |             errorMessage: error.message,
      125 |         });

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:118:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'NotAllowed',
        errorMessage: 'Spawning agents is not allowed.\n\nYou are not authorized to create agents.'
      }

      120 |         };
      121 |
    > 122 |         console.warn('[spawn_agent] Failed to create agent', {
          |                 ^
      123 |             errorName: error.name,
      124 |             errorMessage: error.message,
      125 |         });

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:131:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Create-agent payload is too large.\n' +
          '\n' +
          'Field `source` exceeds 120000 characters.'
      }

      120 |         };
      121 |
    > 122 |         console.warn('[spawn_agent] Failed to create agent', {
          |                 ^
      123 |             errorName: error.name,
      124 |             errorMessage: error.message,
      125 |         });

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:142:27)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Spawn rate limit exceeded.\n' +
          '\n' +
          'You can create at most 5 agents per 10 minutes.'
      }

      120 |         };
      121 |
    > 122 |         console.warn('[spawn_agent] Failed to create agent', {
          |                 ^
      123 |             errorName: error.name,
      124 |             errorMessage: error.message,
      125 |         });

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:170:34)

PASS apps/agents-server/src/utils/adminChatTaskLinks.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobRunReport.test.ts
PASS apps/agents-server/src/components/AgentProfile/AgentCapabilityChips.test.tsx
PASS src/utils/agents/terminalAgentAvatarVisual.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateJobHistory.test.ts
PASS apps/agents-server/src/utils/serverLimits.test.ts
PASS apps/agents-server/src/database/metadataDefaults.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts
PASS src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts
PASS scripts/run-codex-prompts/ui/coderRunUiRefresh.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/readPersistedVpsSelfUpdateJob.test.ts
PASS src/prepare/isPipelinePrepared.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createUnresolvedAgentReferenceDiagnostics.test.ts
PASS apps/agents-server/src/components/Homepage/ExternalAgentsSectionClient.test.tsx
PASS src/book-components/Chat/Chat/ChatRatingModal.test.tsx
PASS src/commands/PARAMETER/parameterCommand.test.ts
PASS src/book-components/Chat/Chat/ChatCitationModal.test.tsx
PASS scripts/utils/emojiTags/validSinglePictogramEmojis.test.ts
PASS apps/agents-server/src/utils/userChat/expiredRunningUserChatJobDiagnostics.test.ts
PASS apps/agents-server/src/components/Portal/Dialog.test.tsx
PASS src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts
PASS apps/agents-server/src/utils/stalwart/readStalwartEmailSnapshot.test.ts
PASS apps/agents-server/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource.test.ts
PASS src/cli/cli-commands/coder/run.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectsPaths.test.ts
PASS apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts
  ● Console

    console.error
      Error processing inbound email NotAllowed: SendGrid Inbound Parse webhook signature verification failed.
          at verifySendgridInboundParseWebhook (C:\Users\me\work\ai\promptbook\apps\agents-server\src\message-providers\email\sendgrid\verifySendgridInboundParseWebhook.ts:108:15)
          at POST (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.ts:16:42)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.test.ts:107:26)

      50 |         return NextResponse.json({ success: true });
      51 |     } catch (error) {
    > 52 |         console.error('Error processing inbound email', error);
         |                 ^
      53 |         return NextResponse.json(
      54 |             { error: error instanceof Error ? error.message : 'Unknown error' },
      55 |             { status: resolveSendgridInboundParseWebhookErrorStatus(error) },

      at POST (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts:52:17)
      at Object.<anonymous> (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts:107:26)

PASS apps/agents-server/src/components/Header/mapContextMenuItemsToSubMenuItems.test.ts
PASS apps/agents-server/src/components/_utils/headlessParam.test.tsx
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks.test.ts
PASS src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts
PASS apps/agents-server/src/app/api/auth/login/route.test.ts
PASS src/book-components/Chat/hooks/useChatAutoScroll.test.tsx
PASS scripts/run-agent-messages/ui/loadAgentRunUiMetadata.test.ts
PASS apps/agents-server/src/utils/transpilers/createTranspiledAgentExportZipBuffer.test.ts
PASS src/cli/other/vpsInstall.test.ts
PASS apps/agents-server/src/utils/agentProjects/parseAgentProjectIndexHtml.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts
PASS src/cli/cli-commands/agents-server/init.test.ts
PASS apps/agents-server/src/message-providers/email/sendgrid/verifySendgridInboundParseWebhook.test.ts
PASS src/cli/cli-commands/agents-server/run.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerCoreAgents.test.ts
PASS scripts/run-agent-messages/main/synchronizeGithubAgentRunnerRepositories.test.ts
PASS apps/agents-server/src/utils/chat/messageSuffix.test.ts
PASS scripts/run-codex-prompts/prompts/markPromptInProgress.test.ts
PASS apps/agents-server/src/utils/vpsConfiguration.test.ts
PASS src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts
PASS src/commands/BOOK_VERSION/bookVersionCommand.test.ts
PASS src/commands/_common/stringifyCommand.test.ts
PASS scripts/utils/emojiTags/scanEmojiTagUsage.test.ts
PASS src/utils/markdown/promptbookifyAiText.test.ts
PASS src/postprocessing/utils/extractJsonBlock.test.ts
PASS src/commands/_common/getParserForCommand.test.ts
PASS scripts/run-codex-prompts/ui/buildCoderRunUiFrame.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.import.test.ts
PASS apps/agents-server/src/components/Header/buildActiveAgentViewItems.test.ts
PASS src/executables/browsers/locateEdge.test.ts
PASS apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.test.ts
  ● Console

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

PASS scripts/run-agent-messages/messages/resolveAgentProjectsUrlPath.test.ts
PASS apps/agents-server/src/utils/userChat/runImmediateUserChatAnswer.test.ts
PASS apps/agents-server/src/utils/renameAgentSource.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerDefaultAgents.test.ts
PASS scripts/repair-imports/utils/repairImportUtils.test.ts
PASS src/utils/serialization/isSerializableAsJson.test.ts
PASS scripts/run-codex-prompts/runners/common/modelPricing.test.ts
PASS src/commitments/_common/getAllCommitmentDefinitions.test.ts
PASS src/commitments/index.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessagePrompt.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts
PASS src/cli/cli-commands/coder/generate-boilerplates.test.ts
  ● Console

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🌒] [✨🌋] [✨🤙] [✨🙆] [✨😙][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 5 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🌒][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨🌋][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨🤙][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0030-qux.md with [✨🙆][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0040-brr.md with [✨😙][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 5 prompts in 5 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🦨] [✨😑] [✨🌚][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 3 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🦨][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨😑][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨🌚][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 12 prompts in 3 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

PASS src/commands/KNOWLEDGE/knowledgeCommand.test.ts
PASS apps/agents-server/src/tools/createChatAttachmentToolFunctions.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/actions.test.ts
PASS src/executables/apps/locateVscode.test.ts
PASS src/commands/EXPECT/expectCommand.test.ts
PASS apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts
  ● Console

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:123:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:123:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:124:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      48 |             }
      49 |         } catch (error) {
    > 50 |             console.warn('Failed to load chat messages from localStorage:', error);
         |                     ^
      51 |         }
      52 |
      53 |         return this.MEMORY_STORAGE.get(storageKey) || null;

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:124:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:140:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:140:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:141:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      48 |             }
      49 |         } catch (error) {
    > 50 |             console.warn('Failed to load chat messages from localStorage:', error);
         |                     ^
      51 |         }
      52 |
      53 |         return this.MEMORY_STORAGE.get(storageKey) || null;

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:141:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:167:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:167:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:170:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      48 |             }
      49 |         } catch (error) {
    > 50 |             console.warn('Failed to load chat messages from localStorage:', error);
         |                     ^
      51 |         }
      52 |
      53 |         return this.MEMORY_STORAGE.get(storageKey) || null;

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:170:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:177:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:177:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      76 |             localStorage.setItem(storageKey, serializedMessages);
      77 |         } catch (error) {
    > 78 |             console.warn('Failed to save chat messages to localStorage:', error);
         |                     ^
      79 |         }
      80 |     }
      81 |

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:197:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      48 |             }
      49 |         } catch (error) {
    > 50 |             console.warn('Failed to load chat messages from localStorage:', error);
         |                     ^
      51 |         }
      52 |
      53 |         return this.MEMORY_STORAGE.get(storageKey) || null;

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:197:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

      117 |             localStorage.removeItem(storageKey);
      118 |         } catch (error) {
    > 119 |             console.warn('Failed to clear chat messages from localStorage:', error);
          |                     ^
      120 |         }
      121 |     }
      122 |

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

PASS apps/agents-server/src/components/Header/buildHeaderSystemMenuItems.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts
PASS src/commands/FOREACH/foreachCommand.test.ts
PASS src/llm-providers/mocked/test/fakeTextToExpectations.test.ts
  ● Console

    console.info
      { expectations: { characters: { min: 10, max: 50 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { sentences: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { paragraphs: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { lines: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { pages: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      {
        expectations: {
          characters: { min: 10, max: 500 },
          words: { min: 1, max: 5 },
          sentences: { min: 1, max: 5 }
        }
      }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 10 }, lines: { min: 3 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

PASS apps/agents-server/src/utils/userChat/createUserChatHarnessProgressCard.test.ts
PASS apps/agents-server/src/components/ViewportHeightController/resolveVisibleViewportHeight.test.ts
PASS apps/agents-server/src/utils/authenticationAttemptRateLimit.test.ts
PASS apps/agents-server/src/database/selectPrefixesForMigration.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts
PASS scripts/run-codex-prompts/common/coderRunEstimateCache.test.ts
PASS src/conversion/utils/extractParameterNamesFromTask.test.ts
PASS src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useChatDocumentTitle.test.tsx
PASS src/book-components/Chat/utils/parseImagePrompts.test.ts
PASS apps/agents-server/src/utils/vpsTask/vpsTaskHistory.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDesiredState.test.ts
PASS apps/agents-server/src/utils/vpsCertificateScheduler.test.ts
PASS scripts/run-agent-messages/main/runPersistentAgentWatch.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/resolveEffectiveAgentGoal.test.ts
PASS src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts
PASS src/scrapers/_common/utils/getScraperIntermediateSource.test.ts
PASS apps/agents-server/src/utils/vpsTask/runWithVpsServerSetupTask.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/newAgentWizardPresets.test.ts
PASS src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts
PASS src/utils/parameters/templateParameters.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.test.ts
  ● Console

    console.warn
      [33m[codex] Rate limit/quota detected (Error: rate limit reached). Retry #1 in 0s at 2026-09-12T09:45:38.943Z.[39m

      173 |
      174 |                 if (options.shouldPrintLiveOutput ?? true) {
    > 175 |                     console.warn(
          |                             ^
      176 |                         colors.yellow(
      177 |                             `[codex] Rate limit/quota detected (${summary}). Retry #${retryIndex} in ${formatDelay(
      178 |                                 delayMs,

      at OpenAiCodexRunner.runPrompt (scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.ts:175:29)

PASS scripts/run-codex-prompts/common/runGoScript/withPromptRuntimeLog.test.ts
PASS scripts/run-codex-prompts/cli/parseRunOptions.test.ts
PASS scripts/run-codex-prompts/git/gitLongPathsSupport.test.ts
PASS apps/agents-server/src/utils/chat/executeQuickActionButton.test.ts
PASS src/executables/locateApp.test.ts
PASS src/book-3.0/CliAgent.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatJobFailureDetails.test.ts
PASS apps/agents-server/src/components/Header/buildAgentMenuStructure.test.tsx
PASS src/utils/normalization/isValidKeyword.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts
PASS src/commitments/CLOSED/CLOSED.test.ts
PASS apps/agents-server/src/utils/memoryRuntimeContext.test.ts
PASS src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts
PASS src/utils/markdown/createMarkdownChart.test.ts
PASS src/errors/utils/serializeError.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationNaming.test.ts
PASS src/commands/X_ACTION/actionCommand.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/config/bookSections.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectInitials.test.ts
PASS apps/agents-server/src/app/api/admin/servers/[serverId]/route.test.ts
PASS scripts/run-codex-prompts/prompts/buildScriptPath.test.ts
PASS src/commitments/USE_PROJECT/USE_PROJECT.test.ts
PASS src/cli/cli-commands/common/questionsCliOptions.test.ts
PASS scripts/run-codex-prompts/git/pullLatestChanges.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTestingAccess.test.ts
PASS src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx
PASS apps/agents-server/src/utils/agentProjects/readAgentProjectReadme.test.ts
PASS src/execution/utils/checkExpectations.test.ts
PASS src/commands/_BOILERPLATE/boilerplateCommand.test.ts
PASS src/commands/URL/urlCommand.test.ts
PASS apps/agents-server/src/utils/getControlPanelOptionAvailability.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse.test.ts
PASS src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts
PASS src/utils/normalization/parseKeywords.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts
PASS src/utils/validators/email/isValidEmail.test.ts
PASS src/utils/random/$generateBookBoilerplate.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts
PASS src/book-3.0/Book.test.ts
PASS scripts/run-codex-prompts/server/buildCoderServerPromptResponse.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDnsDiagnostics.test.ts
PASS src/book-3.0/AgentMessageRunReport.test.ts
PASS src/cli/cli-commands/coder/server.test.ts
PASS scripts/run-agent-messages/messages/resolveAgentProjectRuntimePromptApi.test.ts
PASS src/utils/linguistic-hash/linguisticHash.test.ts
PASS src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts
PASS src/commands/X_INSTRUMENT/instrumentCommand.test.ts
PASS src/commands/FORMFACTOR/formfactorCommand.test.ts
PASS apps/agents-server/src/constants/defaultAgentAvatarVisual.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeRunner.test.ts
PASS apps/agents-server/src/message-providers/email/stalwart/StalwartMessageProvider.test.ts
PASS apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.test.ts
PASS apps/agents-server/src/utils/userChat/userChatReplies.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/GithubRepositoryClient.test.ts
PASS apps/agents-server/src/app/admin/messages/messageAdminPresentation.test.ts
PASS scripts/run-codex-prompts/ui/refreshCoderRunUiSubscriptionUsage.test.ts
PASS src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts
PASS src/commands/POSTPROCESS/postprocessCommand.test.ts
PASS src/commands/PERSONA/personaCommand.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate.test.ts
PASS src/cli/cli-commands/coder/ping.test.ts
PASS src/commands/MODEL/modelCommand.test.ts
PASS src/commands/JOKER/jokerCommand.test.ts
PASS src/commands/FORMAT/formatCommand.test.ts
PASS src/book-2.0/agent-source/agentSourceVisibility.test.ts
PASS apps/agents-server/src/utils/agentBrowserProfile/resolveAgentBrowserProfileDirectory.test.ts
PASS src/pipeline/isValidPipelineString.test.ts
PASS scripts/run-codex-prompts/isolation/markPromptIsolationMergeFailed.test.ts
PASS apps/agents-server/src/utils/email/sendEmailChatReply.test.ts
PASS src/utils/knowledge/inlineKnowledgeSource.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/createManGoOpenEditorRequest.test.ts
PASS apps/agents-server/src/utils/importAgentWithFallback.test.ts
PASS apps/agents-server/src/utils/chat/createWordLikeDeltas.test.ts
PASS apps/agents-server/src/utils/sendUserChatPushNotification.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/parsePlannedMessageSchedule.test.ts
PASS apps/agents-server/src/app/api/agents/route.test.ts
PASS scripts/verify-prompts/verify-prompts.test.ts
PASS scripts/run-codex-prompts/git/agentGitIdentity.test.ts
PASS apps/agents-server/src/utils/dnsRecords/resolveDnsRecordBatchPlan.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts
PASS src/cli/cli-commands/coder/boilerplateCount.test.ts
PASS src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts
PASS apps/agents-server/src/components/AdminTerminal/useAdminTerminalSession.test.ts
PASS src/utils/misc/parseNumber.test.ts
PASS src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts
PASS apps/agents-server/src/utils/agentBook/createAgentBookDownloadFilename.test.ts
PASS src/utils/normalization/unwrapResult.test.ts
PASS src/book-2.0/agent-source/computeAgentHash.test.ts
PASS src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts
PASS apps/agents-server/src/components/Header/ControlPanel/createControlPanelContentState.test.ts
PASS src/book-components/Chat/utils/ChatPersistence.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerAttribution.test.ts
PASS scripts/run-codex-prompts/prompts/printStats.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTraceContent.test.ts
PASS src/cli/test/ptbk.test.ts (330.152 s)
  ● Console

    console.warn
      [33m**OpenAI Codex** is outdated.[39m
      
      [33mInstalled version: `0.153.4`[39m
      [33mNewest version: `0.154.0`[39m
      [33mInstalled command: `C:\Users\me\.nvm\versions\node\v22.23.2\bin\codex` *(global npm installation)*[39m

      44 |     }
      45 |
    > 46 |     console.warn(colors.yellow(formatHarnessInstallationWarning(status)));
         |             ^
      47 |
      48 |     if (installationState === 'not-installed') {
      49 |         await $applyMissingHarnessInstallation(definition, questionsOptions);

      at $applyHarnessInstallationStatus (src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts:46:13)
      at $ensureHarnessInstallations (src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts:39:46)
      at src/cli/cli-commands/coder/init.ts:119:13
      at Command.<anonymous> (src/cli/cli-commands/common/handleActionErrors.ts:35:13)
      at Command.parseAsync (node_modules/commander/lib/command.js:1015:5)
      at Object.<anonymous> (src/cli/test/ptbk.test.ts:161:13)

    console.warn
      [33m**Claude Code** is outdated.[39m
      
      [33mInstalled version: `2.1.258`[39m
      [33mNewest version: `2.1.269`[39m
      [33mInstalled command: `C:\Users\me\.nvm\versions\node\v22.23.2\bin\claude` *(global npm installation)*[39m

      44 |     }
      45 |
    > 46 |     console.warn(colors.yellow(formatHarnessInstallationWarning(status)));
         |             ^
      47 |
      48 |     if (installationState === 'not-installed') {
      49 |         await $applyMissingHarnessInstallation(definition, questionsOptions);

      at $applyHarnessInstallationStatus (src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts:46:13)
      at $ensureHarnessInstallations (src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts:39:46)
      at src/cli/cli-commands/coder/init.ts:119:13
      at Command.<anonymous> (src/cli/cli-commands/common/handleActionErrors.ts:35:13)
      at Command.parseAsync (node_modules/commander/lib/command.js:1015:5)
      at Object.<anonymous> (src/cli/test/ptbk.test.ts:161:13)

PASS scripts/run-codex-prompts/main/resolvePromptRunner.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ExternalUserChatJobMetadata.test.ts
PASS apps/agents-server/src/utils/userChat/persistFrozenUserChat.test.ts
PASS src/utils/serialization/jsonStringsToJsons.test.ts
PASS src/executables/apps/locatePandoc.test.ts
PASS apps/agents-server/src/tools/BrowserConnectionProvider.test.ts
PASS src/book-components/Chat/utils/resolveCitationUrl.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateCron.test.ts
PASS apps/agents-server/src/database/runDatabaseMigrations.test.ts
PASS src/avatars/visuals/octopusAvatarVisualShared.test.ts
PASS src/utils/validators/parameterName/validateParameterName.test.ts
PASS src/cli/cli-commands/coder/list.test.ts
PASS src/utils/markdown/extractOneBlockFromMarkdown.test.ts
PASS src/cli/cli-commands/agent-folder/run.test.ts
PASS scripts/run-codex-prompts/prompts/loadPromptFiles.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimePm2.test.ts
PASS apps/agents-server/src/components/Header/buildHeaderMenuItems.test.ts
PASS src/utils/validators/filePath/isValidFilePath.test.ts
PASS scripts/run-codex-prompts/ping/pingCoderHarnessPeriodically.test.ts
PASS apps/agents-server/src/utils/localChatRunner/persistLocalTeamConversations.test.ts
PASS src/utils/serialization/deepClone.test.ts
PASS apps/agents-server/src/utils/getVpsAdminChatTasksResponse.test.ts
PASS apps/agents-server/src/app/admin/files/filesGalleryRows.test.ts
PASS apps/agents-server/src/utils/chatExport/renderHtmlToPdfOnServer.test.ts
PASS src/utils/parameters/extractParameterNames.test.ts
PASS src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts
PASS src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartJmapValues.test.ts
PASS scripts/run-codex-prompts/testing/runTestBefore.test.ts
  ● Console

    console.info
      [90mRunning initial tests before the agent coding starts: npm run test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

    console.info
      [32mPre-coding tests passed.[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:51:17)

    console.info
      [90mRunning initial tests before the agent coding starts: npm test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

PASS src/utils/normalization/constructImageFilename.test.ts
PASS src/utils/parameters/mapAvailableToExpectedParameters.test.ts
PASS src/utils/agents/resolveAgentAvatarImageUrl.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexUsageFromOutput.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeSessionResurrection.test.ts
PASS src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts
PASS src/book-components/Chat/utils/walletCredentialToolCall.test.ts
PASS src/utils/editable/utils/isFlatPipeline.test.ts
PASS src/utils/take/take.test.ts
PASS src/utils/markdown/parseMarkdownSection.test.ts
PASS apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts
PASS apps/agents-server/src/utils/userChat/heartbeatUserChatJob.test.ts
PASS scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts
  ● Console

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

      135 |         }
      136 |
    > 137 |         console.warn(
          |                 ^
      138 |             colors.yellow(
      139 |                 `Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`,
      140 |             ),

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:85:24)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

      135 |         }
      136 |
    > 137 |         console.warn(
          |                 ^
      138 |             colors.yellow(
      139 |                 `Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`,
      140 |             ),

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:123:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

      135 |         }
      136 |
    > 137 |         console.warn(
          |                 ^
      138 |             colors.yellow(
      139 |                 `Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`,
      140 |             ),

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #2. Sending feedback to github-copilot and retrying...[39m

      135 |         }
      136 |
    > 137 |         console.warn(
          |                 ^
      138 |             colors.yellow(
      139 |                 `Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`,
      140 |             ),

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #3: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.error
      [31mVerification failed for prompts/feature.md#1 after 3 attempts.[39m

      121 |
      122 |         if (attemptCount >= MAX_PROMPT_TEST_ATTEMPTS) {
    > 123 |             console.error(
          |                     ^
      124 |                 colors.red(`Verification failed for ${options.promptLabel} after ${attemptCount} attempts.`),
      125 |             );
      126 |

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:123:21)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

PASS src/utils/normalization/removeEmojis.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/mapVpsSelfUpdateJobToAdminChatTask.test.ts
PASS src/utils/markdown/splitMarkdownIntoSections.test.ts
PASS src/executables/browsers/locateFirefox.test.ts
PASS scripts/find-refactor-candidates/resolveRefactorCandidateProject.test.ts
PASS apps/agents-server/src/utils/serverSelection.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/GitHubCopilotRunner.test.ts
PASS src/cli/cli-commands/agent/run.test.ts
PASS apps/agents-server/src/utils/plannedMessageManager/filterPlannedMessages.test.ts
PASS src/formats/xml/utils/isValidXmlString.test.ts
PASS src/llm-providers/openai/computeOpenAiUsage.test.ts
PASS src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts
PASS src/commitments/MODEL/MODEL.test.ts
PASS src/avatars/visuals/octopus3AvatarVisual.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/generateChatMetadata.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/parseClaudeCodeSubscriptionUsage.test.ts
PASS src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts
PASS src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts
PASS src/cli/cli-commands/coder/verify.test.ts
PASS scripts/run-codex-prompts/git/coderGitSync.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/readAgentsServerFooterVersion.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/NewAgentWizardState.test.ts
PASS src/pipeline/prompt-notation.test.ts
PASS scripts/run-codex-prompts/runners/common/extractHarnessAuthenticationFailureReason.test.ts
PASS scripts/run-codex-prompts/prompts/markPromptAttempts.test.ts
PASS scripts/run-codex-prompts/main/listCoderPrompts.test.ts
PASS src/utils/normalization/normalizeWhitespaces.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/parseCodexLoginMethodFromOutput.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts
PASS scripts/run-codex-prompts/runners/gemini/gemini-pricing.test.ts
PASS apps/agents-server/src/app/sitemap.xml/route.test.ts
PASS src/cli/cli-commands/common/$askForConfirmation.test.ts
PASS src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts
PASS scripts/run-codex-prompts/isolation/runIsolatedPromptRound.test.ts
PASS apps/agents-server/src/utils/localChatRunner/LocalUserChatJobMetadata.test.ts
PASS apps/agents-server/src/utils/cloudflare/resolveCloudflareDnsRecordApplyAction.test.ts
PASS apps/agents-server/src/components/Header/createMyChatsMobileMenuItem.test.ts
PASS src/utils/serialization/serializeToPromptbookJavascript.test.ts
PASS src/utils/normalization/parseKeywordsFromString.test.ts
PASS src/utils/markdown/flattenMarkdown.test.ts
PASS src/utils/expectation-counters/countWords.test.ts
PASS src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts
PASS scripts/verify-prompts/VerifyPromptsOrder.test.ts
PASS src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts
PASS scripts/run-codex-prompts/ui/buildCoderRunUiTerminalFrameUpdate.test.ts
PASS src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts
PASS apps/agents-server/src/utils/email/agentEmailAddress.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderContext.test.ts
PASS src/utils/files/extensionToMimeType.test.ts
PASS apps/agents-server/src/utils/publicUser.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectHrefs.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown.test.ts
PASS src/commitments/TEMPLATE/TEMPLATE.test.ts
PASS apps/agents-server/src/utils/chatHistoryMessage.test.ts
PASS apps/agents-server/src/tools/$provideServer.test.ts
PASS apps/agents-server/src/database/loadAgentsServerEnvFile.test.ts
PASS src/config.test.ts
PASS scripts/run-codex-prompts/prompts/resolveInterruptedPrompt.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobState.test.ts
PASS apps/agents-server/src/utils/speech-to-text/LongRunningSpeechRecognitionSession.test.ts
PASS src/utils/misc/computeHash.test.ts
PASS src/cli/common/$deprecateCliCommand.test.ts
PASS src/executables/platforms/locateAppOnWindows.test.ts
PASS apps/agents-server/src/utils/userPushNotificationSettings.test.ts
PASS apps/agents-server/src/utils/userLocationPromptParameter.test.ts
PASS apps/agents-server/src/utils/resolveCurrentOrInternalServerOrigin.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts
PASS src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts
PASS apps/agents-server/src/utils/resourceMonitor/resolveServerResourceWarningStatus.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFilter.test.ts
PASS src/utils/knowledge/simplifyKnowledgeLabel.test.ts
PASS src/utils/normalization/titleToName.test.ts
PASS src/utils/chat/chatAttachments.test.ts
PASS src/executables/browsers/locateChrome.test.ts
PASS src/book-3.0/AgentPlannedMessagesSidecar.test.ts
PASS src/avatars/avatarRenderingUtils.test.ts
PASS src/book-components/Chat/utils/toolCallParsing.test.ts
PASS book/scripts/import-markdown/increaseHeadings.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFiltering.test.ts
PASS apps/agents-server/src/utils/agentRouting/agentRouteHrefs.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateInstalledVersions.test.ts
PASS src/cli/common/$requireCliSubcommand.test.ts
PASS apps/utils/src/app/avatars/avatarPlaygroundUrlState.test.ts
PASS src/avatars/visuals/avatarVisualRegistry.test.ts
PASS src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/buildGitHubCopilotScript.test.ts
PASS src/utils/validators/uuid/isValidUuid.test.ts
PASS src/utils/color/utils/colorHueDistance.test.ts
PASS src/utils/editable/utils/renamePipelineParameter.test.ts
PASS src/cli/cli-commands/coder/find-refactor-candidates.test.ts
PASS scripts/run-codex-prompts/common/waitUntilWorldTimeDeadline.test.ts
PASS scripts/run-codex-prompts/common/waitForSkippableWorldTimeDeadline.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/createUserChatTimeoutActivity.test.ts
PASS apps/agents-server/src/utils/userChat/getUserChatForJobRunner.test.ts
PASS src/collection/agent-collection/CreateAgentInput.test.ts
PASS src/utils/normalization/normalizeTo_snake_case.test.ts
PASS src/cli/common/$hideCliCommandFromHelp.test.ts
PASS src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts
PASS apps/agents-server/src/utils/session.test.ts
PASS src/execution/resolveTaskTldr.test.ts
PASS src/cli/cli-commands/common/harness/HarnessDefinition.test.ts
PASS src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts
PASS src/formats/json/utils/isValidJsonString.test.ts
PASS scripts/run-codex-prompts/common/ProgressiveBackoff.test.ts
PASS book/scripts/import-markdown/placeImports.test.ts
PASS apps/agents-server/src/utils/userWallet/createUserWalletRecord.test.ts
PASS src/utils/normalization/removeQuotes.test.ts
PASS src/utils/files/listAllFiles.test.ts
PASS src/avatars/visuals/orbAvatarVisual.test.ts
PASS src/book-2.0/agent-source/normalizeAgentName.test.ts
PASS apps/agents-server/src/utils/agentChatInputPlaceholder.test.ts
PASS apps/agents-server/src/utils/serverRegistry.test.ts
PASS src/conversion/pipelineJsonToString.test.ts
PASS apps/agents-server/src/app/api/admin/default-agents/reinstate/route.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/repeatFiredUserChatTimeout.test.ts
PASS src/book-components/Chat/utils/citationHelpers.test.ts
PASS apps/agents-server/src/components/Footer/getCommitFooterEmoji.test.ts
PASS src/utils/sets/intersection.test.ts
PASS src/utils/files/mimeTypeToExtension.test.ts
PASS src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts
PASS src/commitments/DELETE/DELETE.test.ts
PASS src/commitments/_common/getCommitmentNoticeMetadata.test.ts
PASS scripts/generate-packages/addDependenciesForGeneratedPackages.test.ts
PASS apps/agents-server/src/utils/resolveInternalServerOrigin.test.ts
PASS src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/qwen-code-pricing.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/resolvePlannedMessageDueAt.test.ts
PASS scripts/run-agent-messages/ui/buildAgentRunUiFrame.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/projectReferenceActions.test.ts
PASS apps/agents-server/src/utils/normalization/normalizeUploadFilename.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/resolveCoreAgentAwareMissingReferences.test.ts
PASS src/errors/utils/deserializeError.test.ts
PASS src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts
PASS apps/agents-server/src/utils/chatEnterBehaviorSettings.test.ts
PASS src/llm-providers/_common/utils/pricing.test.ts
PASS apps/agents-server/src/utils/speech-to-text/SpeechToTextFailoverRecognition.test.ts
PASS apps/agents-server/src/utils/localization/formatServerLanguageHumanReadableDate.test.ts
PASS apps/agents-server/src/utils/applyPagePreviewBrowserInput.test.ts
PASS scripts/run-codex-prompts/runners/gemini/buildGeminiScript.test.ts
PASS src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts
PASS scripts/run-codex-prompts/ui/CoderRunUiState.test.ts
PASS apps/agents-server/src/utils/userChat/UserChatScopeError.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartBootstrap.test.ts
PASS apps/agents-server/src/utils/stalwart/resolveStalwartApiUrl.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/$provideAgentReferenceResolver.test.ts
PASS apps/agents-server/src/utils/plannedMessageManager/resolvePlannedMessageLifecycle.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemWarnings.test.ts
PASS src/utils/markdown/trimCodeBlock.test.ts
PASS src/utils/files/getFileExtension.test.ts
PASS src/utils/expectation-counters/countSentences.test.ts
PASS apps/agents-server/src/utils/userThemeModeSettings.test.ts
PASS src/speech-recognition/OpenAiSpeechRecognition.test.ts
PASS src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useCanonicalAgentChatPanelState.test.ts
PASS src/utils/validators/url/extractUrlsFromText.test.ts
PASS src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.test.ts
PASS src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts
PASS book/scripts/import-markdown/removeComments.test.ts
PASS scripts/run-codex-prompts/common/formatUnknownErrorDetails.test.ts
PASS apps/agents-server/src/utils/userChat/triggerUserChatJobWorker.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethServiceProviderUrls.test.ts
PASS apps/agents-server/src/utils/dnsRecords/createDnsZoneFileExport.test.ts
PASS apps/agents-server/src/utils/messages/parseUseEmailSmtpCredential.test.ts
PASS src/utils/misc/debounce.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/resolveWizardTeamReference.test.ts
PASS src/commitments/USE_PROJECT/projectReference.test.ts
PASS src/avatars/avatarInteractionUtils.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/mergeCanonicalChatMessagesWithPendingOutboundMessages.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.navigation.test.ts
PASS src/utils/validators/url/isValidAgentUrl.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTracePath.test.ts
PASS apps/agents-server/src/utils/userChat/runDurableUserChatJobWorkerTick.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectChatReferences.test.ts
PASS apps/agents-server/src/components/Homepage/loadFederatedServerAgents.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectDnsRecords.test.ts
PASS src/utils/normalization/suffixUrl.test.ts
PASS src/utils/normalization/normalizeMessageText.test.ts
PASS src/utils/normalization/nameToUriParts.test.ts
PASS src/utils/markdown/addAutoGeneratedSection.test.ts
PASS src/execution/utils/usageToWorktime.test.ts
PASS src/cli/other/install.test.ts
PASS scripts/verify-prompts/$orderPromptFiles.test.ts
PASS apps/agents-server/src/utils/createAgentWithDefaultVisibility.test.ts
PASS apps/agents-server/src/utils/agentIdentifier.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/CodexFailureHandling.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatRunningActivity.test.ts
PASS apps/agents-server/src/utils/transpilers/getTranspiledCodeFileMetadata.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemActivities.test.ts
PASS src/utils/normalization/searchKeywords.test.ts
PASS src/utils/color/Color.test.ts
PASS src/execution/utils/formatUsagePrice.test.ts
PASS src/execution/utils/addUsage.test.ts
PASS src/execution/execution-report/countWorkingDuration.test.ts
PASS src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexScript.test.ts
PASS apps/agents-server/src/app/admin/_components/adminTableSorting.test.ts
PASS src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts
PASS src/formats/csv/utils/isValidCsvString.test.ts
PASS src/book-components/Chat/Chat/learnDictationDictionary.test.ts
PASS scripts/run-codex-prompts/common/appendCoderContext.test.ts
PASS apps/agents-server/src/utils/userChat/resolveUserChatWorkerInternalToken.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSubmissionActions.test.ts
PASS src/utils/normalization/normalizeTo_camelCase.test.ts
PASS src/utils/markdown/removeMarkdownComments.test.ts
PASS src/llm-providers/openai/openai-models.test.ts
PASS scripts/find-refactor-candidates/selectMostImportantRefactorCandidates.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts
PASS src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/getCodexSubscriptionUsage.test.ts
PASS src/utils/validators/url/isValidUrl.test.ts
PASS scripts/run-agent-messages/main/listLocalAgentRunnerProjects.test.ts
PASS apps/agents-server/src/utils/userChat/userChatMessageLifecycle.test.ts
PASS src/utils/normalization/decapitalize.test.ts
PASS src/utils/markdown/extractAllListItemsFromMarkdown.test.ts
PASS src/utils/files/decodeAttachmentAsText.test.ts
PASS src/commitments/ACTION/ACTION.test.ts
PASS src/book-components/Chat/utils/parseCitationsFromContent.test.ts
PASS apps/agents-server/src/utils/messages/sendMessage.test.ts
PASS apps/agents-server/src/utils/messages/humanizeOutboundEmail.test.ts
PASS apps/agents-server/src/utils/localChatRunner/parseLocalTeamConversations.test.ts
PASS src/utils/markdown/trimEndOfCodeBlock.test.ts
PASS src/utils/markdown/escapeMarkdownBlock.test.ts
PASS src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts
PASS scripts/run-codex-prompts/runners/common/createAuthenticationAwarePromptRunner.test.ts
PASS apps/agents-server/src/components/_utils/generateMetaTxt.test.ts
PASS apps/agents-server/src/components/AgentProjects/resolveLatestCompletedAgentMessageKey.test.ts
PASS src/utils/sets/union.test.ts
PASS src/utils/normalization/orderJson.test.ts
PASS src/utils/normalization/normalizeTo_PascalCase.test.ts
PASS src/utils/misc/arrayableToArray.test.ts
PASS src/utils/expectation-counters/countParagraphs.test.ts
PASS apps/agents-server/src/constants/chatVisualMode.test.ts
PASS src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/bookService.test.ts
PASS scripts/run-codex-prompts/common/runGoScript/withTempScript.test.ts
PASS src/utils/validators/url/isValidPipelineUrl.test.ts
PASS src/utils/serialization/asSerializable.test.ts
PASS apps/agents-server/src/utils/agentOrganization/hiddenFolders.test.ts
PASS src/utils/normalization/normalize-to-kebab-case.test.ts
PASS src/utils/filesystem/promptbookTemporaryPath.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts
PASS apps/agents-server/src/utils/shareTarget.test.ts
PASS src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts
PASS src/utils/validators/url/normalizeDomainForMatching.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/buildClaudeScript.test.ts
PASS src/book-components/Chat/utils/thinkingMessageVariants.test.ts
PASS scripts/run-codex-prompts/prompts/formatCoderRunSteps.test.ts
PASS scripts/run-codex-prompts/ping/extractCoderPingAnswer.test.ts
PASS apps/agents-server/src/utils/chat/validateChatMessageContent.test.ts
PASS src/utils/normalization/removeDiacritics.test.ts
PASS src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts
PASS src/utils/files/isFileExisting.test.ts
PASS src/utils/parameters/valueToString.test.ts
PASS apps/agents-server/src/components/Homepage/useAgentsListSyncState.test.ts
PASS src/utils/files/isDirectoryExisting.test.ts
PASS src/utils/execCommand/execCommandNormalizeOptions.test.ts
PASS src/executables/browsers/locateDefaultSystemBrowser.test.ts
PASS apps/agents-server/src/utils/standaloneVpsDnsDiagnostics.test.ts
PASS apps/agents-server/src/utils/parsePositiveUserId.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks/compareAdminChatTasks.test.ts
PASS src/utils/validators/url/isUrlOnPrivateNetwork.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/generateAgentMetadata.test.ts
PASS scripts/run-codex-prompts/migrations/detectDestructiveSqlStatements.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessageScriptPath.test.ts
PASS apps/agents-server/src/utils/normalization/filenameToPrompt.test.ts
PASS src/utils/parameters/numberToString.test.ts
PASS src/utils/markdown/createMarkdownTable.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/chat/resolveAgentChatRequestIdentities.test.ts
PASS src/cli/cli-commands/coder/getTypescriptModule.test.ts
PASS src/book-components/Chat/utils/agentProjectToolCall.test.ts
PASS apps/agents-server/src/utils/upload/fileUploadAvailability.test.ts
PASS scripts/run-codex-prompts/common/applyCoderRunControlKey.test.ts
PASS src/utils/markdown/removeMarkdownFormatting.test.ts
PASS apps/agents-server/src/components/UsersList/generateSecurePassword.test.ts
PASS src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts
PASS src/utils/expectation-counters/countLines.test.ts
PASS src/commitments/FORMAT/FORMAT.test.ts
PASS scripts/generate-packages/collectMainPackageDependencies.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts
PASS src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts
PASS apps/agents-server/src/utils/userWallet/resolveWalletAgentPermanentId.test.ts
PASS src/utils/markdown/removeMarkdownLinks.test.ts
PASS scripts/find-refactor-candidates/RefactorCandidateLevel.test.ts
PASS src/utils/validators/javascriptName/isValidJavascriptName.test.ts
PASS src/utils/validators/filePath/isRootPath.test.ts
PASS src/book-components/Chat/utils/parseMessageButtons.test.ts
PASS src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts
PASS apps/agents-server/src/utils/chat/createAttachmentAwareCitationLabelResolver.test.ts
PASS src/utils/normalization/nameToUriPart.test.ts
PASS src/conversion/validation/_importPipeline.test.ts
PASS src/transpilers/_common/formatUsedToolFunctions.test.ts
PASS src/utils/expectation-counters/countCharacters.test.ts
PASS scripts/repair-imports/utils/splitArrayIntoChunks.test.ts
PASS apps/agents-server/src/utils/pagePreviewBrowserSessions.test.ts
PASS src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts
PASS src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts
PASS apps/agents-server/src/utils/stalwart/createEmailDnsInstructions.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethPublicRequestUrl.test.ts
PASS apps/agents-server/src/utils/cloudflare/createCloudflareDnsRecordRequestBody.test.ts
PASS apps/agents-server/src/tools/createServerChromiumLaunchOptions.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/buildQwenCodeScript.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationCheckoutFailureReport.test.ts
PASS src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts
PASS scripts/run-codex-prompts/common/buildCoderRunProgressSnapshot.test.ts
PASS src/utils/sets/difference.test.ts
PASS apps/agents-server/src/utils/standaloneVpsRawIpBootstrap.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/formatChatTimeoutRemainingTime.test.ts
PASS apps/agents-server/src/utils/createChatStreamHandler.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/agentGoalChatIdentity.test.ts
PASS src/utils/normalization/capitalize.test.ts
PASS apps/agents-server/src/constants/newAgentWizard.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts
PASS src/book-components/Chat/Chat/insertDictationChunk.test.ts
PASS src/executables/apps/locateLibreoffice.test.ts
PASS apps/agents-server/src/utils/chatFeedbackMode.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts
A worker process has failed to exit gracefully and has been force exited. This is likely caused by tests leaking due to improper teardown. Try running with --detectOpenHandles to find leaks. Active timers can also cause this, ensure that .unref() was called on them.

Test Suites: 759 passed, 759 total
Tests:       7 todo, 3343 passed, 3350 total
Snapshots:   0 total
Time:        353.388 s
Ran all test suites.
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

> promptbook@0.114.0-33 test-app-agents-server
> (cd ./apps/agents-server/ && npm run test)


> test
> npm run test-e2e


> pretest-e2e
> node ./scripts/kill-port.js 4021 4440

No process is listening on port 4021.
No process is listening on port 4440.

> test-e2e
> node ./scripts/run-e2e-tests.js


> build-e2e
> node ./scripts/build-e2e.js --no-lint


> prebuild
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js --no-homepage-prerender --no-lint

 ⚠ Linting is disabled.
   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...
 ⚠ Compiled with warnings in 6.2min

../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
Critical dependency: the request of a dependency is an expression

Import trace for requested module:
../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
../../node_modules/@ai-sdk/google/dist/index.js
../../src/llm-providers/google/createGoogleExecutionTools.ts
../../src/_packages/google.index.ts
./src/tools/$provideExecutionToolsForServer.ts
./src/app/agents/[agentName]/api/mcp/route.ts

<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (127kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (139kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (126kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
 ⚠ Compiled with warnings in 65s

./src/utils/isIpAllowed.ts
A Node.js module is loaded ('net' at line 1) which is not supported in the Edge Runtime.
Learn More: https://nextjs.org/docs/messages/node-module-in-edge-runtime

Import trace for requested module:
./src/utils/isIpAllowed.ts
./src/middleware/createMiddlewareRequestContext.ts

../../node_modules/@supabase/realtime-js/dist/module/lib/websocket-factory.js
A Node.js API is used (process.versions at line: 32) which is not supported in the Edge Runtime.
Learn more: https://nextjs.org/docs/api-reference/edge-runtime

Import trace for requested module:
../../node_modules/@supabase/realtime-js/dist/module/lib/websocket-factory.js
../../node_modules/@supabase/realtime-js/dist/module/index.js
../../node_modules/@supabase/supabase-js/dist/module/index.js
./src/middleware/createMiddlewareRequestContext/getMiddlewareSupabase.ts
./src/middleware/createMiddlewareRequestContext.ts

../../node_modules/@supabase/supabase-js/dist/module/index.js
A Node.js API is used (process.version at line: 24) which is not supported in the Edge Runtime.
Learn more: https://nextjs.org/docs/api-reference/edge-runtime

Import trace for requested module:
../../node_modules/@supabase/supabase-js/dist/module/index.js
./src/middleware/createMiddlewareRequestContext/getMiddlewareSupabase.ts
./src/middleware/createMiddlewareRequestContext.ts

../../src/utils/normalization/titleToName.ts
A Node.js module is loaded ('path' at line 1) which is not supported in the Edge Runtime.
Learn More: https://nextjs.org/docs/messages/node-module-in-edge-runtime

Import trace for requested module:
../../src/utils/normalization/titleToName.ts
../../src/book-2.0/agent-source/normalizeAgentName.ts
./src/utils/agentReferenceResolver/createServerAgentReferenceResolver.ts
./src/utils/customDomainRouting.ts
./src/middleware/createMiddlewareRequestContext/resolveMiddlewareServerRouting.ts
./src/middleware/createMiddlewareRequestContext.ts

Client network socket disconnected before secure TLS connection was established

Retrying 1/3...
socket hang up

Retrying 1/3...
 ✓ Compiled successfully in 8.8min
   Checking validity of types ...
   Collecting page data ...
 ⚠ Using edge runtime on a page currently disables static generation for that page
   Generating static pages (0/146) ...
   Generating static pages (36/146) 
   Generating static pages (72/146) 
   Generating static pages (109/146) 
 ✓ Generating static pages (146/146)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                     Size  First Load JS
┌ ƒ /                                                                        1.55 kB        1.53 MB
├ ƒ /_not-found                                                                827 B         184 kB
├ ƒ /[agentName]                                                               763 B         334 kB
├ ƒ /[agentName]/[...rest]                                                     828 B         184 kB
├ ƒ /admin/about                                                             1.87 kB        1.78 MB
├ ƒ /admin/api-tokens                                                        6.36 kB         251 kB
├ ƒ /admin/backup                                                            4.11 kB         230 kB
├ ƒ /admin/browser-test                                                      3.29 kB         239 kB
├ ƒ /admin/chat-feedback                                                     20.3 kB        1.67 MB
├ ƒ /admin/chat-history                                                      22.9 kB        1.67 MB
├ ƒ /admin/core-agents                                                       2.56 kB        1.78 MB
├ ƒ /admin/custom-css                                                        9.83 kB         262 kB
├ ƒ /admin/custom-js                                                         11.6 kB         263 kB
├ ƒ /admin/email-server                                                       2.6 kB         234 kB
├ ƒ /admin/error-simulation                                                  4.96 kB         231 kB
├ ƒ /admin/files                                                             7.64 kB         263 kB
├ ƒ /admin/image-generator-test                                              14.1 kB         269 kB
├ ƒ /admin/images                                                            8.47 kB         263 kB
├ ƒ /admin/internal-s3                                                       1.81 kB         228 kB
├ ƒ /admin/limits                                                            2.47 kB         231 kB
├ ƒ /admin/login-methods/shibboleth                                          5.46 kB         250 kB
├ ƒ /admin/messages                                                          9.69 kB         255 kB
├ ƒ /admin/messages/send-email                                               5.99 kB         236 kB
├ ƒ /admin/metadata                                                          18.7 kB         305 kB
├ ƒ /admin/models                                                            1.81 kB         228 kB
├ ƒ /admin/planned-messages                                                  13.3 kB         261 kB
├ ƒ /admin/projects                                                            324 B         230 kB
├ ƒ /admin/search-engine-test                                                3.41 kB         229 kB
├ ƒ /admin/task-manager                                                        336 B         267 kB
├ ƒ /admin/task-manager-vps                                                    829 B         184 kB
├ ƒ /admin/task-manager/[taskId]                                             2.49 kB         262 kB
├ ƒ /admin/tool-limits                                                        1.8 kB         228 kB
├ ƒ /admin/transcriptions                                                    6.67 kB         239 kB
├ ƒ /admin/usage                                                               10 kB         265 kB
├ ƒ /admin/users                                                             2.25 kB         254 kB
├ ƒ /admin/users/[userId]                                                    6.72 kB         252 kB
├ ƒ /admin/voice-input-test                                                  5.29 kB         231 kB
├ ƒ /agents                                                                  1.55 kB        1.53 MB
├ ƒ /agents/[agentName]                                                        436 B         334 kB
├ ƒ /agents/[agentName]/api/book                                               826 B         184 kB
├ ƒ /agents/[agentName]/api/book/download                                      828 B         184 kB
├ ƒ /agents/[agentName]/api/book/history                                       827 B         184 kB
├ ƒ /agents/[agentName]/api/book/missing-agent                                 825 B         184 kB
├ ƒ /agents/[agentName]/api/book/reference-diagnostics                         825 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-connections                               825 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-connections/[connectionId]/disconnect     826 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-events                                    826 B         184 kB
├ ƒ /agents/[agentName]/api/chat                                               826 B         184 kB
├ ƒ /agents/[agentName]/api/feedback                                           826 B         184 kB
├ ƒ /agents/[agentName]/api/mcp                                                827 B         184 kB
├ ƒ /agents/[agentName]/api/meta-disclaimer                                    826 B         184 kB
├ ƒ /agents/[agentName]/api/model-requirements                                 825 B         184 kB
├ ƒ /agents/[agentName]/api/model-requirements/system-message                  825 B         184 kB
├ ƒ /agents/[agentName]/api/openai/chat/completions                            826 B         184 kB
├ ƒ /agents/[agentName]/api/openai/models                                      826 B         184 kB
├ ƒ /agents/[agentName]/api/openai/v1/chat/completions                         827 B         184 kB
├ ƒ /agents/[agentName]/api/openai/v1/models                                   826 B         184 kB
├ ƒ /agents/[agentName]/api/openrouter/chat/completions                        825 B         184 kB
├ ƒ /agents/[agentName]/api/profile                                            829 B         184 kB
├ ƒ /agents/[agentName]/api/share-target/[shareTargetId]/consume               826 B         184 kB
├ ƒ /agents/[agentName]/api/timeouts                                           825 B         184 kB
├ ƒ /agents/[agentName]/api/timeouts/[timeoutId]                               825 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats                                         827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]                                827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/draft                          826 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel            825 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/messages                       827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/stream                         826 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel    828 B         184 kB
├ ƒ /agents/[agentName]/api/voice                                              826 B         184 kB
├ ƒ /agents/[agentName]/book                                                 6.33 kB         421 kB
├ ƒ /agents/[agentName]/book+chat                                            4.94 kB        1.82 MB
├ ƒ /agents/[agentName]/chat                                                 34.7 kB        1.84 MB
├ ƒ /agents/[agentName]/export-as-transpiled-code                            7.35 kB         384 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api                          825 B         184 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api/download                 825 B         184 kB
├ ƒ /agents/[agentName]/goal                                                  1.8 kB         228 kB
├ ƒ /agents/[agentName]/history                                              2.67 kB         228 kB
├ ƒ /agents/[agentName]/iframe                                                 825 B         184 kB
├ ƒ /agents/[agentName]/images                                                 354 B         185 kB
├ ƒ /agents/[agentName]/images/default-avatar.png                              825 B         184 kB
├ ƒ /agents/[agentName]/images/icon-256.png                                    826 B         184 kB
├ ƒ /agents/[agentName]/images/screenshot-fullhd.png                           825 B         184 kB
├ ƒ /agents/[agentName]/images/screenshot-phone.png                            826 B         184 kB
├ ƒ /agents/[agentName]/integration                                            11 kB         270 kB
├ ƒ /agents/[agentName]/opengraph-image                                        826 B         184 kB
├ ƒ /agents/[agentName]/projects                                               324 B         230 kB
├ ƒ /agents/[agentName]/projects/[projectName]                               3.49 kB        1.78 MB
├ ƒ /agents/[agentName]/projects/[projectName]/files/[...filePath]             828 B         184 kB
├ ƒ /agents/[agentName]/projects/[projectName]/vscode                          824 B         184 kB
├ ƒ /agents/[agentName]/share-target                                           827 B         184 kB
├ ƒ /agents/[agentName]/system-message                                       2.93 kB         379 kB
├ ƒ /agents/[agentName]/textarea                                               10 kB         250 kB
├ ƒ /agents/[agentName]/website-integration                                  2.63 kB        1.78 MB
├ ƒ /api/admin-email                                                           826 B         184 kB
├ ƒ /api/admin/backups/books                                                   827 B         184 kB
├ ƒ /api/admin/backups/server                                                  826 B         184 kB
├ ƒ /api/admin/chat-tasks                                                      825 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]                                             827 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/cancel                                      824 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/retry                                       828 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/terminal                                    828 B         184 kB
├ ƒ /api/admin/chat-tasks/cancel-all                                           828 B         184 kB
├ ƒ /api/admin/cli-access                                                      827 B         184 kB
├ ƒ /api/admin/database/studio                                                 827 B         184 kB
├ ƒ /api/admin/default-agents/reinstate                                        826 B         184 kB
├ ƒ /api/admin/dns-records/cloudflare                                          826 B         184 kB
├ ƒ /api/admin/environment                                                     826 B         184 kB
├ ƒ /api/admin/error-simulation                                                826 B         184 kB
├ ƒ /api/admin/harness-auth                                                    824 B         184 kB
├ ƒ /api/admin/harness-auth/authentication                                     826 B         184 kB
├ ƒ /api/admin/limits                                                          825 B         184 kB
├ ƒ /api/admin/logs                                                            827 B         184 kB
├ ƒ /api/admin/planned-messages                                                825 B         184 kB
├ ƒ /api/admin/planned-messages/[timeoutId]                                    826 B         184 kB
├ ƒ /api/admin/servers                                                         826 B         184 kB
├ ƒ /api/admin/servers/[serverId]                                              828 B         184 kB
├ ƒ /api/admin/servers/[serverId]/migrate                                      825 B         184 kB
├ ƒ /api/admin/tool-limits                                                     829 B         184 kB
├ ƒ /api/admin/update                                                          827 B         184 kB
├ ƒ /api/admin/update/commits                                                  827 B         184 kB
├ ƒ /api/admin/update/log                                                      827 B         184 kB
├ ƒ /api/admin/update/versions                                                 827 B         184 kB
├ ƒ /api/admin/vps/chat-tasks                                                  826 B         184 kB
├ ƒ /api/admin/vps/chat-tasks/cancel-all                                       826 B         184 kB
├ ƒ /api/agent-folders                                                         827 B         184 kB
├ ƒ /api/agent-folders/[folderId]                                              826 B         184 kB
├ ƒ /api/agent-folders/[folderId]/restore                                      825 B         184 kB
├ ƒ /api/agent-folders/[folderId]/visibility                                   827 B         184 kB
├ ƒ /api/agent-organization                                                    825 B         184 kB
├ ƒ /api/agent-project-runtime-auth                                            825 B         184 kB
├ ƒ /api/agent-project-vscode-auth/[sessionId]                                 827 B         184 kB
├ ƒ /api/agents                                                                827 B         184 kB
├ ƒ /api/agents/[agentName]                                                    827 B         184 kB
├ ƒ /api/agents/[agentName]/clone                                              825 B         184 kB
├ ƒ /api/agents/[agentName]/restore                                            827 B         184 kB
├ ƒ /api/agents/export                                                         826 B         184 kB
├ ƒ /api/agents/import                                                         827 B         184 kB
├ ƒ /api/api-tokens                                                            827 B         184 kB
├ ƒ /api/auth/change-password                                                  828 B         184 kB
├ ƒ /api/auth/login                                                            827 B         184 kB
├ ƒ /api/auth/logout                                                           825 B         184 kB
├ ƒ /api/auth/shibboleth/acs                                                   825 B         184 kB
├ ƒ /api/auth/shibboleth/login                                                 824 B         184 kB
├ ƒ /api/auth/shibboleth/metadata                                              827 B         184 kB
├ ƒ /api/auth/shibboleth/status                                                826 B         184 kB
├ ƒ /api/browser-artifacts/[artifactName]                                      826 B         184 kB
├ ƒ /api/browser-test/act                                                      826 B         184 kB
├ ƒ /api/browser-test/screenshot                                               825 B         184 kB
├ ƒ /api/browser-test/scroll-facebook                                          824 B         184 kB
├ ƒ /api/calendar-oauth/callback                                               825 B         184 kB
├ ƒ /api/calendar-oauth/connect                                                826 B         184 kB
├ ƒ /api/calendar-oauth/refresh                                                826 B         184 kB
├ ƒ /api/calendar-oauth/revoke                                                 828 B         184 kB
├ ƒ /api/calendar-oauth/status                                                 825 B         184 kB
├ ƒ /api/chat                                                                  826 B         184 kB
├ ƒ /api/chat-feedback                                                         826 B         184 kB
├ ƒ /api/chat-feedback/[id]                                                    826 B         184 kB
├ ƒ /api/chat-feedback/export                                                  826 B         184 kB
├ ƒ /api/chat-history                                                          826 B         184 kB
├ ƒ /api/chat-history/[id]                                                     825 B         184 kB
├ ƒ /api/chat-history/export                                                   827 B         184 kB
├ ƒ /api/chat-history/threads                                                  825 B         184 kB
├ ƒ /api/chat-streaming                                                        825 B         184 kB
├ ƒ /api/chat/citation-label                                                   825 B         184 kB
├ ƒ /api/chat/export/pdf                                                       827 B         184 kB
├ ƒ /api/custom-css                                                            825 B         184 kB
├ ƒ /api/custom-js                                                             826 B         184 kB
├ ƒ /api/docs/book-language.md                                                 828 B         184 kB
├ ƒ /api/docs/book-language.pdf                                                826 B         184 kB
├ ƒ /api/docs/book.md                                                          830 B         184 kB
├ ƒ /api/elevenlabs/tts                                                        826 B         184 kB
├ ƒ /api/emails/incoming/sendgrid                                              827 B         184 kB
├ ƒ /api/emails/incoming/stalwart                                              827 B         184 kB
├ ƒ /api/embed.js                                                              826 B         184 kB
├ ƒ /api/error-reports/application                                             826 B         184 kB
├ ƒ /api/federated-agents                                                      828 B         184 kB
├ ƒ /api/github-app/callback                                                   827 B         184 kB
├ ƒ /api/github-app/connect                                                    826 B         184 kB
├ ƒ /api/github-app/status                                                     828 B         184 kB
├ ƒ /api/health                                                                828 B         184 kB
├ ƒ /api/images/[filename]                                                     826 B         184 kB
├ ƒ /api/internal/agent-goal-chat-planned-messages                             827 B         184 kB
├ ƒ /api/internal/agent-project-runtimes                                       827 B         184 kB
├ ƒ /api/internal/agent-runner-limits                                          825 B         184 kB
├ ƒ /api/internal/user-chat-jobs/run                                           827 B         184 kB
├ ƒ /api/internal/user-chat-timeouts/run                                       826 B         184 kB
├ ƒ /api/long-running-task                                                     827 B         184 kB
├ ƒ /api/messages                                                              825 B         184 kB
├ ƒ /api/metadata                                                              824 B         184 kB
├ ƒ /api/metadata/export                                                       827 B         184 kB
├ ƒ /api/metadata/import                                                       827 B         184 kB
├ ƒ /api/onboarding/book                                                       828 B         184 kB
├ ƒ /api/onboarding/evaluate                                                   826 B         184 kB
├ ƒ /api/onboarding/test                                                       827 B         184 kB
├ ƒ /api/openai/v1/audio/transcriptions                                        826 B         184 kB
├ ƒ /api/openai/v1/chat/completions                                            826 B         184 kB
├ ƒ /api/openai/v1/models                                                      828 B         184 kB
├ ƒ /api/page-preview/check                                                    824 B         184 kB
├ ƒ /api/page-preview/input                                                    828 B         184 kB
├ ƒ /api/page-preview/screenshot                                               827 B         184 kB
├ ƒ /api/page-preview/state                                                    827 B         184 kB
├ ƒ /api/page-preview/stream                                                   825 B         184 kB
├ ƒ /api/profile                                                               828 B         184 kB
├ ƒ /api/push-subscriptions                                                    826 B         184 kB
├ ƒ /api/scrape                                                                827 B         184 kB
├ ƒ /api/search                                                                826 B         184 kB
├ ƒ /api/send-email                                                            826 B         184 kB
├ ƒ /api/settings/keybindings                                                  826 B         184 kB
├ ƒ /api/settings/notifications                                                825 B         184 kB
├ ƒ /api/settings/theme                                                        827 B         184 kB
├ ƒ /api/spawn-agent                                                           826 B         184 kB
├ ƒ /api/story/export                                                          826 B         184 kB
├ ƒ /api/system/mocked-chats                                                   826 B         184 kB
├ ƒ /api/team-agent-profile                                                    827 B         184 kB
├ ƒ /api/upload                                                                825 B         184 kB
├ ƒ /api/usage                                                                 829 B         184 kB
├ ƒ /api/user-memory                                                           826 B         184 kB
├ ƒ /api/user-memory/[memoryId]                                                826 B         184 kB
├ ƒ /api/user-wallet                                                           825 B         184 kB
├ ƒ /api/user-wallet/[walletId]                                                826 B         184 kB
├ ƒ /api/users                                                                 825 B         184 kB
├ ƒ /api/users/[username]                                                      828 B         184 kB
├ ƒ /api/v1/agents                                                             825 B         184 kB
├ ƒ /api/v1/agents/[agentId]                                                   827 B         184 kB
├ ƒ /api/v1/folders                                                            827 B         184 kB
├ ƒ /api/v1/folders/[folderId]                                                 827 B         184 kB
├ ƒ /api/v1/folders/[folderId]/agents/[agentId]                                824 B         184 kB
├ ƒ /api/v1/instance                                                           826 B         184 kB
├ ƒ /api/v1/me                                                                 826 B         184 kB
├ ƒ /dashboard                                                               2.04 kB        1.56 MB
├ ƒ /docs                                                                    3.19 kB        1.78 MB
├ ƒ /docs/[docId]                                                            3.17 kB        1.77 MB
├ ƒ /embed                                                                    7.3 kB        1.72 MB
├ ƒ /experiments/story                                                        5.2 kB         382 kB
├ ƒ /humans.txt                                                                826 B         184 kB
├ ƒ /manifest.webmanifest                                                      827 B         184 kB
├ ƒ /openapi.json                                                              825 B         184 kB
├ ƒ /recycle-bin                                                             3.79 kB         311 kB
├ ƒ /restricted                                                                353 B         185 kB
├ ƒ /robots.txt                                                                826 B         184 kB
├ ƒ /s3/[first]/[second]/[hash]/[filename]                                     828 B         184 kB
├ ƒ /search                                                                  6.62 kB         191 kB
├ ƒ /security.txt                                                              824 B         184 kB
├ ƒ /sitemap.xml                                                               826 B         184 kB
├ ƒ /story/[[...story]]                                                        827 B         184 kB
├ ƒ /superadmin/cli-access                                                   3.21 kB         232 kB
├ ƒ /superadmin/database                                                     2.89 kB         229 kB
├ ƒ /superadmin/email-server                                                 2.25 kB         228 kB
├ ƒ /superadmin/environment                                                  5.87 kB         232 kB
├ ƒ /superadmin/harness-auth                                                  6.2 kB         235 kB
├ ƒ /superadmin/internal-s3                                                    827 B         184 kB
├ ƒ /superadmin/logs                                                         3.87 kB         230 kB
├ ƒ /superadmin/resource-monitor                                             7.08 kB         234 kB
├ ƒ /superadmin/servers                                                      23.2 kB         303 kB
├ ƒ /superadmin/task-manager                                                   335 B         267 kB
├ ƒ /superadmin/update                                                       14.7 kB         265 kB
├ ƒ /swagger                                                                 4.33 kB         246 kB
├ ƒ /system/profile                                                          3.03 kB         229 kB
├ ƒ /system/settings                                                         5.87 kB         226 kB
├ ƒ /system/user-memory                                                      5.64 kB         251 kB
├ ƒ /system/user-wallet                                                      9.73 kB         262 kB
├ ƒ /system/utilities                                                        1.81 kB         228 kB
├ ƒ /system/utilities/mocked-chats                                           19.3 kB         264 kB
├ ƒ /system/utilities/mocked-chats/view                                      5.31 kB        1.65 MB
├ ƒ /test/og-image                                                             826 B         184 kB
└ ƒ /test/og-image/opengraph-image                                             826 B         184 kB
+ First Load JS shared by all                                                 183 kB
  ├ chunks/1458-4c3b38b8dc54dca8.js                                           123 kB
  ├ chunks/87c73c54-cafaa802daf5b9c9.js                                      54.3 kB
  └ other shared chunks (total)                                              5.88 kB

Route (pages)                                                                   Size  First Load JS
┌   /_app                                                                        0 B         143 kB
└ ○ /500 (6128 ms)                                                           4.92 kB         148 kB
+ First Load JS shared by all                                                 174 kB
  ├ chunks/framework-b796438537236ee8.js                                     57.8 kB
  ├ chunks/main-58fe174be183cfbd.js                                          81.2 kB
  ├ css/c4bd8b207325b3b3.css                                                 31.5 kB
  └ other shared chunks (total)                                              4.02 kB

ƒ Middleware                                                                  301 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand


> lint (ran next to the production build)


> lint
> eslint src/ --max-warnings=0 --cache --cache-location ./node_modules/.cache/eslint/agents-server-src.json

[WebServer] [mock-supabase] listening on http://127.0.0.1:54321
[WebServer] 
[WebServer] > start
[WebServer] > next start -p 4440
[WebServer] 
[WebServer] (node:44796) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
[WebServer] (Use `node --trace-warnings ...` to show where the warning was created)
[WebServer]    ▲ Next.js 15.4.11
[WebServer]    - Local:        http://localhost:4440
[WebServer]    - Network:      http://192.168.192.1:4440
[WebServer] 
[WebServer]  ✓ Starting...
[WebServer]  ✓ Ready in 3.9s

Running 19 tests using 1 worker

(node:34424) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
  ✓   1 [chromium] › tests\e2e\api-authorization.spec.ts:8:9 › Agents Server API authorization › returns unauthorized metadata API response for anonymous users (929ms)
[WebServer] Failed to generate metadata for agent get-started Error [NotFoundError]: Agent with name or id "get-started" not found
[WebServer]     at o.getAgentPermanentId (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\4859.js:16:288)
[WebServer]     at async q (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\book\reference-diagnostics\route.js:61:2159)
[WebServer]     at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\8682.js:33:4071)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\model-requirements\route.js:16:15277)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\2050.js:4:836)
[WebServer]     at async u (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3243.js:1:1202)
[WebServer] Failed to generate metadata for agent manifest Error [NotFoundError]: Agent with name or id "manifest" not found
[WebServer]     at o.getAgentPermanentId (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\4859.js:16:288)
[WebServer]     at async q (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\book\reference-diagnostics\route.js:61:2159)
[WebServer]     at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\8682.js:33:4071)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\model-requirements\route.js:16:15277)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\2050.js:4:836)
[WebServer]     at async u (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3243.js:1:1202)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
  ✓   2 [chromium] › tests\e2e\api-authorization.spec.ts:14:9 › Agents Server API authorization › allows admin metadata API access and blocks admin password changes through API (3.4s)
[WebServer] Failed to generate metadata for agent get-started Error [NotFoundError]: Agent with name or id "get-started" not found
[WebServer]     at o.getAgentPermanentId (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\4859.js:16:288)
[WebServer]     at async q (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\book\reference-diagnostics\route.js:61:2159)
[WebServer]     at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\8682.js:33:4071)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\model-requirements\route.js:16:15277)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\2050.js:4:836)
[WebServer]     at async u (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3243.js:1:1202)
[WebServer] Failed to generate metadata for agent manifest Error [NotFoundError]: Agent with name or id "manifest" not found
[WebServer]     at o.getAgentPermanentId (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\4859.js:16:288)
[WebServer]     at async q (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\book\reference-diagnostics\route.js:61:2159)
[WebServer]     at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\8682.js:33:4071)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\model-requirements\route.js:16:15277)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\2050.js:4:836)
[WebServer]     at async u (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3243.js:1:1202)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   fingerprint: '63c858807d1597df04ed485d6e70bfa3cf20fd8c5e3b0e35c83b4bfabfd513ce',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T09:59:09.580Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 1, started: 0, skipped: 0, completed: 0, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 313,
[WebServer]     runningAgeMs: 237,
[WebServer]     updatedAgeMs: 69,
[WebServer]     lastHeartbeatAgeMs: 69,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 0
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9238\n' +
[WebServer]         '    at async g (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9776)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9238)
[WebServer]       at async g (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9776)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9203)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer] }
[WebServer] [user-chat-job] worker tick failed for one VPS server {
[WebServer]   serverName: 'local-e2e',
[WebServer]   serverDomain: '127.0.0.1:4440',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] }
[WebServer] [user-chat-timeout] worker_tick_failed {
[WebServer]   name: 'DatabaseError',
[WebServer]   message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]     '\n' +
[WebServer]     'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]   stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]     '\n' +
[WebServer]     'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]     '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]     '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]     '    at m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:278:279)\n' +
[WebServer]     '    at H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:248:1941)\n' +
[WebServer]     '    at F (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:248:1658)\n' +
[WebServer]     '    at g (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\7624.js:6:1346)\n' +
[WebServer]     '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\7624.js:1:621)\n' +
[WebServer]     '    at n (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\user-chats\\route.js:1:1334)\n' +
[WebServer]     '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]     '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147',
[WebServer]   id: undefined
[WebServer] }
[WebServer] [user-chat] Failed to reconcile terminal active job {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   status: 'FAILED',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:6246)
[WebServer]       at async k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:6095)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:4594)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\user-chats\route.js:1:2647)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer]       at async k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\user-chats\route.js:1:7862)
[WebServer] }
  ✓   3 [chromium] › tests\e2e\chat-history-navigation.spec.ts:70:9 › Agents Server chat history navigation › renders the configured initial message on the profile page without a temporary hello fallback, even while profile loading is delayed (7.3s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'pp43ovKNgt1ePk',
[WebServer]   fingerprint: 'cc1ee342788060409ef4d60b92a24bbd13f3b6df3cf0ee44c67a7d9fcf8a25cd',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T09:59:17.315Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 2, started: 0, skipped: 0, completed: 0, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   jobId: 1,
[WebServer]   fingerprint: '63c858807d1597df04ed485d6e70bfa3cf20fd8c5e3b0e35c83b4bfabfd513ce',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 2, started: 1, skipped: 0, completed: 0, failed: 0 }
[WebServer] }
[WebServer] [🤰] Creating NEW OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'nC2XhBHMJJeNDV'
[WebServer] }
[WebServer] [🤰] Preparing AgentKit agent via cache manager {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   agentKitName: 'e2e-profile-initial-message-no-blink - f98df532',
[WebServer]   instructionsLength: 425,
[WebServer]   knowledgeSourcesCount: 0,
[WebServer]   toolsCount: 0
[WebServer] }
[WebServer] [🤰] Preparing OpenAI AgentKit agent {
[WebServer]   name: 'e2e-profile-initial-message-no-blink - f98df532',
[WebServer]   instructionsLength: 425,
[WebServer]   knowledgeSourcesCount: 0,
[WebServer]   toolsCount: 0
[WebServer] }
[WebServer] [🤰] OpenAI AgentKit agent ready {
[WebServer]   name: 'e2e-profile-initial-message-no-blink - f98df532',
[WebServer]   model: 'gpt-5.4-mini',
[WebServer]   toolCount: 0,
[WebServer]   hasVectorStore: false
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   jobId: 1,
[WebServer]   fingerprint: '63c858807d1597df04ed485d6e70bfa3cf20fd8c5e3b0e35c83b4bfabfd513ce',
[WebServer]   durationMs: 243,
[WebServer]   counters: { scheduled: 2, started: 1, skipped: 0, completed: 1, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 8009,
[WebServer]     runningAgeMs: 7933,
[WebServer]     updatedAgeMs: 7765,
[WebServer]     lastHeartbeatAgeMs: 7765,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 0
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'pp43ovKNgt1ePk',
[WebServer]   jobId: 2,
[WebServer]   fingerprint: 'cc1ee342788060409ef4d60b92a24bbd13f3b6df3cf0ee44c67a7d9fcf8a25cd',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 2, started: 2, skipped: 0, completed: 1, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'pp43ovKNgt1ePk',
[WebServer]   chatId: 'goal-pp43ovKNgt1ePk',
[WebServer]   jobId: 'n3L7aCPjGMC4iY',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'pp43ovKNgt1ePk'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'pp43ovKNgt1ePk',
[WebServer]   jobId: 2,
[WebServer]   fingerprint: 'cc1ee342788060409ef4d60b92a24bbd13f3b6df3cf0ee44c67a7d9fcf8a25cd',
[WebServer]   durationMs: 4,
[WebServer]   counters: { scheduled: 2, started: 2, skipped: 0, completed: 2, failed: 0 }
[WebServer] }
[WebServer] You have not provided any `LlmExecutionTools`
[WebServer] This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
[WebServer] 
[WebServer] Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.
[WebServer] [user-chat] Failed to reconcile terminal active job {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   status: 'FAILED',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:6246)
[WebServer]       at async k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:6095)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:4594)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\user-chats\route.js:1:2647)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer]       at async k (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\agents\[agentName]\api\user-chats\route.js:1:7862)
[WebServer] }
  ✘   4 [chromium] › tests\e2e\chat-history-navigation.spec.ts:102:9 › Agents Server chat history navigation › shows the first user message immediately as sending when starting a chat from the profile page (24.0s)
(node:41336) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'W3b1uNH6VAHV5g',
[WebServer]   fingerprint: '877d77848a8289146eefa57d80c3472f6407f2f2c10fdac6309d3c2fb446fe0c',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T09:59:43.481Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 3, started: 2, skipped: 0, completed: 2, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 33951,
[WebServer]     runningAgeMs: 33875,
[WebServer]     updatedAgeMs: 33707,
[WebServer]     lastHeartbeatAgeMs: 33707,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 1
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'W3b1uNH6VAHV5g',
[WebServer]   chatId: 'goal-W3b1uNH6VAHV5g',
[WebServer]   jobId: 'FVZzGh4ULfnv9Q',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] You have not provided any `LlmExecutionTools`
[WebServer] This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
[WebServer] 
[WebServer] Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 42982,
[WebServer]     runningAgeMs: 42906,
[WebServer]     updatedAgeMs: 42738,
[WebServer]     lastHeartbeatAgeMs: 42738,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 1
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [user-chat] Failed to trigger durable worker Error: Failed to trigger user chat worker: 500 Internal Server Error
[WebServer]     at f (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3992.js:92:1793)
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 43113,
[WebServer]     runningAgeMs: 43037,
[WebServer]     updatedAgeMs: 42869,
[WebServer]     lastHeartbeatAgeMs: 42869,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 1
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [user-chat] Failed to trigger durable worker Error: Failed to trigger user chat worker: 500 Internal Server Error
[WebServer]     at f (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\3992.js:92:1793)
  ✓   5 [chromium] › tests\e2e\chat-history-navigation.spec.ts:139:9 › Agents Server chat history navigation › processes two rapid sends on a freshly created chat without a chat-not-found failure (14.6s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '7ez9v8duC2Tqg7',
[WebServer]   fingerprint: '5f87f64b2a288888cb7886d643c6f83a52da3b0fba3941a357ab046f08209bc8',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T09:59:56.616Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 4, started: 2, skipped: 0, completed: 2, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'W3b1uNH6VAHV5g',
[WebServer]   jobId: 3,
[WebServer]   fingerprint: '877d77848a8289146eefa57d80c3472f6407f2f2c10fdac6309d3c2fb446fe0c',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 4, started: 3, skipped: 0, completed: 2, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'W3b1uNH6VAHV5g'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'W3b1uNH6VAHV5g',
[WebServer]   jobId: 3,
[WebServer]   fingerprint: '877d77848a8289146eefa57d80c3472f6407f2f2c10fdac6309d3c2fb446fe0c',
[WebServer]   durationMs: 7,
[WebServer]   counters: { scheduled: 4, started: 3, skipped: 0, completed: 3, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 47033,
[WebServer]     runningAgeMs: 46957,
[WebServer]     updatedAgeMs: 46789,
[WebServer]     lastHeartbeatAgeMs: 46789,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 1
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '7ez9v8duC2Tqg7',
[WebServer]   jobId: 4,
[WebServer]   fingerprint: '5f87f64b2a288888cb7886d643c6f83a52da3b0fba3941a357ab046f08209bc8',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 4, started: 4, skipped: 0, completed: 3, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: '7ez9v8duC2Tqg7',
[WebServer]   chatId: 'goal-7ez9v8duC2Tqg7',
[WebServer]   jobId: 'w1zLRRUZZQ2Lmi',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: '7ez9v8duC2Tqg7'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '7ez9v8duC2Tqg7',
[WebServer]   jobId: 4,
[WebServer]   fingerprint: '5f87f64b2a288888cb7886d643c6f83a52da3b0fba3941a357ab046f08209bc8',
[WebServer]   durationMs: 4,
[WebServer]   counters: { scheduled: 4, started: 4, skipped: 0, completed: 4, failed: 0 }
[WebServer] }
  ✓   6 [chromium] › tests\e2e\chat-history-navigation.spec.ts:201:9 › Agents Server chat history navigation › navigates from the profile page when opening an existing chat preview card (13.1s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'TNWWVeim7XVRGZ',
[WebServer]   fingerprint: '6745c3d6c800c8ee9e35efbdaf43dfe81ebc380df1503b87fb705cfd59b7d5b9',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:00:10.833Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 5, started: 4, skipped: 0, completed: 4, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 61302,
[WebServer]     runningAgeMs: 61226,
[WebServer]     updatedAgeMs: 61058,
[WebServer]     lastHeartbeatAgeMs: 61058,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 2
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'TNWWVeim7XVRGZ',
[WebServer]   chatId: 'goal-TNWWVeim7XVRGZ',
[WebServer]   jobId: 'VFv9GdTRVQs88D',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
  ✘   7 [chromium] › tests\e2e\chat-history-navigation.spec.ts:223:9 › Agents Server chat history navigation › navigates from the profile page for quick buttons and composer sends (21.7s)
(node:49228) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'LaqXhKMUrL5SMA',
[WebServer]   fingerprint: 'ada0e14d2ec4db60a7b587f113b34f1f21478b47512633f3529c0d34c44998c1',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:00:34.517Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 6, started: 4, skipped: 0, completed: 4, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'TNWWVeim7XVRGZ',
[WebServer]   jobId: 5,
[WebServer]   fingerprint: '6745c3d6c800c8ee9e35efbdaf43dfe81ebc380df1503b87fb705cfd59b7d5b9',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 6, started: 5, skipped: 0, completed: 4, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'TNWWVeim7XVRGZ'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'TNWWVeim7XVRGZ',
[WebServer]   jobId: 5,
[WebServer]   fingerprint: '6745c3d6c800c8ee9e35efbdaf43dfe81ebc380df1503b87fb705cfd59b7d5b9',
[WebServer]   durationMs: 3,
[WebServer]   counters: { scheduled: 6, started: 5, skipped: 0, completed: 5, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 84997,
[WebServer]     runningAgeMs: 84921,
[WebServer]     updatedAgeMs: 84753,
[WebServer]     lastHeartbeatAgeMs: 84753,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 2
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'LaqXhKMUrL5SMA',
[WebServer]   jobId: 6,
[WebServer]   fingerprint: 'ada0e14d2ec4db60a7b587f113b34f1f21478b47512633f3529c0d34c44998c1',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 6, started: 6, skipped: 0, completed: 5, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'LaqXhKMUrL5SMA',
[WebServer]   chatId: 'goal-LaqXhKMUrL5SMA',
[WebServer]   jobId: 'MMJxwp38Wgzks8',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'LaqXhKMUrL5SMA'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'LaqXhKMUrL5SMA',
[WebServer]   jobId: 6,
[WebServer]   fingerprint: 'ada0e14d2ec4db60a7b587f113b34f1f21478b47512633f3529c0d34c44998c1',
[WebServer]   durationMs: 4,
[WebServer]   counters: { scheduled: 6, started: 6, skipped: 0, completed: 6, failed: 0 }
[WebServer] }
  ✘   8 [chromium] › tests\e2e\chat-history-navigation.spec.ts:289:9 › Agents Server chat history navigation › sends quick-button prompts from the durable chat page (22.7s)
(node:33680) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'cf2b1N9pXLJpzx',
[WebServer]   fingerprint: 'b7010a62839a6c8eb424064b416b525312675bf6f0ba209751d08f3eeb21d4e4',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:00:58.764Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 7, started: 6, skipped: 0, completed: 6, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 109270,
[WebServer]     runningAgeMs: 109194,
[WebServer]     updatedAgeMs: 109026,
[WebServer]     lastHeartbeatAgeMs: 109026,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 3
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'cf2b1N9pXLJpzx',
[WebServer]   chatId: 'goal-cf2b1N9pXLJpzx',
[WebServer]   jobId: '751taW6x2JZoFZ',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] You have not provided any `LlmExecutionTools`
[WebServer] This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
[WebServer] 
[WebServer] Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'cf2b1N9pXLJpzx',
[WebServer]   jobId: 7,
[WebServer]   fingerprint: 'b7010a62839a6c8eb424064b416b525312675bf6f0ba209751d08f3eeb21d4e4',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 7, started: 7, skipped: 0, completed: 6, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'cf2b1N9pXLJpzx'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'cf2b1N9pXLJpzx',
[WebServer]   jobId: 7,
[WebServer]   fingerprint: 'b7010a62839a6c8eb424064b416b525312675bf6f0ba209751d08f3eeb21d4e4',
[WebServer]   durationMs: 3,
[WebServer]   counters: { scheduled: 7, started: 7, skipped: 0, completed: 7, failed: 0 }
[WebServer] }
  ✘   9 [chromium] › tests\e2e\chat-history-navigation.spec.ts:325:9 › Agents Server chat history navigation › keeps the newly created chat selected after a delayed stale refresh and does not open a native dialog (1.0m)
(node:45076) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'q8zR6cTZP3gakr',
[WebServer]   fingerprint: 'c01b1bdfd2cc51abcb65242266cba9bb16fc951f70f195bf6607c71976214455',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:02:00.125Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 8, started: 7, skipped: 0, completed: 7, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 170555,
[WebServer]     runningAgeMs: 170479,
[WebServer]     updatedAgeMs: 170311,
[WebServer]     lastHeartbeatAgeMs: 170311,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 5
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'q8zR6cTZP3gakr',
[WebServer]   chatId: 'goal-q8zR6cTZP3gakr',
[WebServer]   jobId: 'Cs5babgk2tNg6N',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] You have not provided any `LlmExecutionTools`
[WebServer] This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
[WebServer] 
[WebServer] Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.
  ✓  10 [chromium] › tests\e2e\chat-history-navigation.spec.ts:388:9 › Agents Server chat history navigation › creates a fresh chat when the durable route is opened directly with ?chat=new (8.7s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'YyPFjEg8Yyz5ES',
[WebServer]   fingerprint: 'e7606fd22ca9b05bc235fd39e1a3b3fe596562b9bc5f2839bab6687d56e855c3',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:02:08.851Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 9, started: 7, skipped: 0, completed: 7, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'q8zR6cTZP3gakr',
[WebServer]   jobId: 8,
[WebServer]   fingerprint: 'c01b1bdfd2cc51abcb65242266cba9bb16fc951f70f195bf6607c71976214455',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 9, started: 8, skipped: 0, completed: 7, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'q8zR6cTZP3gakr'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'q8zR6cTZP3gakr',
[WebServer]   jobId: 8,
[WebServer]   fingerprint: 'c01b1bdfd2cc51abcb65242266cba9bb16fc951f70f195bf6607c71976214455',
[WebServer]   durationMs: 3,
[WebServer]   counters: { scheduled: 9, started: 8, skipped: 0, completed: 8, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 179318,
[WebServer]     runningAgeMs: 179242,
[WebServer]     updatedAgeMs: 179074,
[WebServer]     lastHeartbeatAgeMs: 179074,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 5
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'YyPFjEg8Yyz5ES',
[WebServer]   jobId: 9,
[WebServer]   fingerprint: 'e7606fd22ca9b05bc235fd39e1a3b3fe596562b9bc5f2839bab6687d56e855c3',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 9, started: 9, skipped: 0, completed: 8, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'YyPFjEg8Yyz5ES',
[WebServer]   chatId: 'goal-YyPFjEg8Yyz5ES',
[WebServer]   jobId: 'edjLSV5LJhGxzp',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'YyPFjEg8Yyz5ES'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'YyPFjEg8Yyz5ES',
[WebServer]   jobId: 9,
[WebServer]   fingerprint: 'e7606fd22ca9b05bc235fd39e1a3b3fe596562b9bc5f2839bab6687d56e855c3',
[WebServer]   durationMs: 6,
[WebServer]   counters: { scheduled: 9, started: 9, skipped: 0, completed: 9, failed: 0 }
[WebServer] }
  ✓  11 [chromium] › tests\e2e\chat-history-navigation.spec.ts:411:9 › Agents Server chat history navigation › keeps the last clicked chat selected when an earlier navigation response finishes later (8.9s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '4Wjpba1C37JRKR',
[WebServer]   fingerprint: 'db2a819281ffe815d64a2f297fda49e75cf06c2d27a5da9003aa937c5208d46b',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:02:17.662Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 10, started: 9, skipped: 0, completed: 9, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 188290,
[WebServer]     runningAgeMs: 188214,
[WebServer]     updatedAgeMs: 188046,
[WebServer]     lastHeartbeatAgeMs: 188046,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 6
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: '4Wjpba1C37JRKR',
[WebServer]   chatId: 'goal-4Wjpba1C37JRKR',
[WebServer]   jobId: 'k2JFGSxeT6AYXM',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
  ✘  12 [chromium] › tests\e2e\header-agent-view-navigation.spec.ts:26:9 › header agent-view navigation › navigates between profile and chat from the active agent breadcrumb menu (17.8s)
(node:18888) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
  ✓  13 [chromium] › tests\e2e\header-homepage-navigation.spec.ts:58:9 › header homepage navigation › navigates to the homepage from the desktop header brand link (5.5s)
  ✓  14 [chromium] › tests\e2e\header-homepage-navigation.spec.ts:68:9 › header homepage navigation › navigates to the homepage from the compact header brand link on mobile (2.7s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'VmnKHbLoeGHwKM',
[WebServer]   fingerprint: '09752e03e28e6150b40922c4fa1ce5c5b0c0d3923f341bae252ebb808f9f888c',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:02:45.455Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 11, started: 9, skipped: 0, completed: 9, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '4Wjpba1C37JRKR',
[WebServer]   jobId: 10,
[WebServer]   fingerprint: 'db2a819281ffe815d64a2f297fda49e75cf06c2d27a5da9003aa937c5208d46b',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 11, started: 10, skipped: 0, completed: 9, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: '4Wjpba1C37JRKR'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '4Wjpba1C37JRKR',
[WebServer]   jobId: 10,
[WebServer]   fingerprint: 'db2a819281ffe815d64a2f297fda49e75cf06c2d27a5da9003aa937c5208d46b',
[WebServer]   durationMs: 6,
[WebServer]   counters: { scheduled: 11, started: 10, skipped: 0, completed: 10, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'VmnKHbLoeGHwKM',
[WebServer]   jobId: 11,
[WebServer]   fingerprint: '09752e03e28e6150b40922c4fa1ce5c5b0c0d3923f341bae252ebb808f9f888c',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 11, started: 11, skipped: 0, completed: 10, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 216261,
[WebServer]     runningAgeMs: 216185,
[WebServer]     updatedAgeMs: 216017,
[WebServer]     lastHeartbeatAgeMs: 216017,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 7
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'VmnKHbLoeGHwKM',
[WebServer]   chatId: 'goal-VmnKHbLoeGHwKM',
[WebServer]   jobId: '8Q4zaTAdB2ourH',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'VmnKHbLoeGHwKM'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'VmnKHbLoeGHwKM',
[WebServer]   jobId: 11,
[WebServer]   fingerprint: '09752e03e28e6150b40922c4fa1ce5c5b0c0d3923f341bae252ebb808f9f888c',
[WebServer]   durationMs: 4,
[WebServer]   counters: { scheduled: 11, started: 11, skipped: 0, completed: 11, failed: 0 }
[WebServer] }
  ✘  15 [chromium] › tests\e2e\header-homepage-navigation.spec.ts:79:9 › header homepage navigation › navigates to the homepage from the desktop header brand link on an agent profile page (4.9s)
(node:14112) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Sacbs42qnRu6eL',
[WebServer]   fingerprint: 'f3d5b608b5aa6c67723e3f0ffa255dfb8b45b70306c43135b5cef65b9b1bf124',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:02:51.244Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 12, started: 11, skipped: 0, completed: 11, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 221713,
[WebServer]     runningAgeMs: 221637,
[WebServer]     updatedAgeMs: 221469,
[WebServer]     lastHeartbeatAgeMs: 221469,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 7
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'Sacbs42qnRu6eL',
[WebServer]   chatId: 'goal-Sacbs42qnRu6eL',
[WebServer]   jobId: '5Q91XBhZ3NrmaF',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
  ✘  16 [chromium] › tests\e2e\header-homepage-navigation.spec.ts:98:9 › header homepage navigation › navigates to the homepage from the mobile drawer server link on an agent profile page (3.7s)
(node:1528) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
  ✓  17 [chromium] › tests\e2e\header-homepage-navigation.spec.ts:118:9 › header homepage navigation › navigates to the homepage from the mobile drawer server link (5.4s)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Dgo5Rz4GTQXyyL',
[WebServer]   fingerprint: 'd36ae949d4ada5b583a17fea5e66b61d6919d8baf5c80e08ecaaa9d1f0101d6b',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:03:04.413Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 13, started: 11, skipped: 0, completed: 11, failed: 0 }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Sacbs42qnRu6eL',
[WebServer]   jobId: 12,
[WebServer]   fingerprint: 'f3d5b608b5aa6c67723e3f0ffa255dfb8b45b70306c43135b5cef65b9b1bf124',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 13, started: 12, skipped: 0, completed: 11, failed: 0 }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'Sacbs42qnRu6eL'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Sacbs42qnRu6eL',
[WebServer]   jobId: 12,
[WebServer]   fingerprint: 'f3d5b608b5aa6c67723e3f0ffa255dfb8b45b70306c43135b5cef65b9b1bf124',
[WebServer]   durationMs: 3,
[WebServer]   counters: { scheduled: 13, started: 12, skipped: 0, completed: 12, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 234816,
[WebServer]     runningAgeMs: 234740,
[WebServer]     updatedAgeMs: 234572,
[WebServer]     lastHeartbeatAgeMs: 234572,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 7
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [pre-index] started {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Dgo5Rz4GTQXyyL',
[WebServer]   jobId: 13,
[WebServer]   fingerprint: 'd36ae949d4ada5b583a17fea5e66b61d6919d8baf5c80e08ecaaa9d1f0101d6b',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   retryCount: 0,
[WebServer]   counters: { scheduled: 13, started: 13, skipped: 0, completed: 12, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: 'Dgo5Rz4GTQXyyL',
[WebServer]   chatId: 'goal-Dgo5Rz4GTQXyyL',
[WebServer]   jobId: 't87SP9J9L3rFFx',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\3992.js:92:1793)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:25:33\n' +
[WebServer]       '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:71:8247)\n' +
[WebServer]       '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\agents\\[agentName]\\api\\book\\missing-agent\\route.js:10:1100)\n' +
[WebServer]       '    at async H (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:5845)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6448.js:1:20147\n' +
[WebServer]       '    at async rb.do (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:21059)\n' +
[WebServer]       '    at async rb.handle (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-route.runtime.prod.js:5:25937)\n' +
[WebServer]       '    at async k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\v1\\agents\\route.js:14:9522)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] [🤰] Returning cached OpenAiAgentKitExecutionTools
[WebServer] [🤰] Resolving AgentKit cache key {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   assistantCacheKey: 'f98df532505f009d3611ef60107238cb14972167f4eec482b8da09b281cc11fe',
[WebServer]   includeDynamicContext: true,
[WebServer]   instructionsLength: 27,
[WebServer]   baseSourceLength: 439,
[WebServer]   agentId: 'Dgo5Rz4GTQXyyL'
[WebServer] }
[WebServer] [🤰] AgentKit cache hit (prepared agent) {
[WebServer]   agentName: 'e2e-profile-initial-message-no-blink',
[WebServer]   preparedAgentCacheKey: 'a6951ab7a3fb2b7f339a9b92b3e1ea68fcd44675bff6f03cc25bfef7884d4e77',
[WebServer]   vectorStoreHash: null,
[WebServer]   vectorStoreId: undefined
[WebServer] }
[WebServer] [pre-index] completed {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: 'Dgo5Rz4GTQXyyL',
[WebServer]   jobId: 13,
[WebServer]   fingerprint: 'd36ae949d4ada5b583a17fea5e66b61d6919d8baf5c80e08ecaaa9d1f0101d6b',
[WebServer]   durationMs: 3,
[WebServer]   counters: { scheduled: 13, started: 13, skipped: 0, completed: 13, failed: 0 }
[WebServer] }
  ✘  18 [chromium] › tests\e2e\management-api.spec.ts:44:9 › Agents Server management API › supports OpenAPI docs and owner-scoped CRUD flows (6.8s)
(node:13024) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
(Use `node --trace-warnings ...` to show where the warning was created)
[WebServer] Authentication attempt succeeded { purpose: 'login', username: 'admin', requestIp: '::ffff:127.0.0.1' }
[WebServer] [pre-index] scheduled {
[WebServer]   tablePrefix: '',
[WebServer]   agentPermanentId: '14Rg83APp5axuR',
[WebServer]   fingerprint: 'f7b9299981586a65091dce8f0f1e1a5fac3f9bd3568ada3c6a3972027338ab81',
[WebServer]   triggerReason: 'AGENT_CREATED',
[WebServer]   runAfter: '2026-09-12T10:03:14.474Z',
[WebServer]   mode: 'insert',
[WebServer]   counters: { scheduled: 1, started: 0, skipped: 0, completed: 0, failed: 0 }
[WebServer] }
[WebServer] [user-chat-job] lease_expired {
[WebServer]   source: 'recoverExpiredRunningUserChatJobs',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   userId: 7,
[WebServer]   agentPermanentId: 'nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   assistantMessageId: 'sfdR2Xcke9Vbsx',
[WebServer]   attemptCount: 1,
[WebServer]   timings: {
[WebServer]     queueDelayMs: 76,
[WebServer]     queuedAgeMs: 244953,
[WebServer]     runningAgeMs: 244877,
[WebServer]     updatedAgeMs: 244709,
[WebServer]     lastHeartbeatAgeMs: 244709,
[WebServer]     runningDurationAtLastHeartbeatMs: 168,
[WebServer]     leaseExpiredByMs: null
[WebServer]   },
[WebServer]   runtimeLimits: {
[WebServer]     workerRouteMaxDurationMs: 300000,
[WebServer]     leaseDurationMs: 600000,
[WebServer]     heartbeatIntervalMs: 30000,
[WebServer]     heartbeatTimeoutMs: 10000,
[WebServer]     heartbeatMaxConsecutiveFailures: 20,
[WebServer]     assistantMessagePersistIntervalMs: 5000
[WebServer]   },
[WebServer]   limitSignals: {
[WebServer]     isLeaseExpired: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtRecovery: false,
[WebServer]     didExceedWorkerRouteMaxDurationAtLastHeartbeat: false,
[WebServer]     didGapSinceLastHeartbeatReachLeaseDuration: false,
[WebServer]     missedHeartbeatIntervalsSinceLastHeartbeat: 8
[WebServer]   },
[WebServer]   serverLimits: {
[WebServer]     TIMEOUT_MAX_ACTIVE_PER_CHAT: 5,
[WebServer]     TIMEOUT_MAX_FIRED_PER_DAY_PER_CHAT: 10,
[WebServer]     MAX_FILE_UPLOAD_SIZE_MB: 50,
[WebServer]     FEDERATED_AGENT_IMPORT_RETRY_DELAY_MS: 500,
[WebServer]     SPAWN_AGENT_MAX_DEPTH: 2,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_MAX: 5,
[WebServer]     SPAWN_AGENT_RATE_LIMIT_WINDOW_MS: 600000,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_FAILED_ATTEMPTS: 1,
[WebServer]     LOCAL_AGENT_RUNNER_MAX_PARALLEL_MESSAGES: 3
[WebServer]   },
[WebServer]   activeTaskCounters: null,
[WebServer]   activeTasks: [],
[WebServer]   diagnosticsLoadErrors: {
[WebServer]     activeTasks: {
[WebServer]       name: 'DatabaseError',
[WebServer]       message: 'Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.',
[WebServer]       stack: 'DatabaseError: Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.\n' +
[WebServer]         '\n' +
[WebServer]         'Configure one of these variables to use the Supabase/PostgreSQL database backend.\n' +
[WebServer]         '    at f (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:19:1483)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\5714.js:253:565)\n' +
[WebServer]         '    at k (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:11904)\n' +
[WebServer]         '    at i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\8941.js:109:5898)\n' +
[WebServer]         '    at j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:59:5863)\n' +
[WebServer]         '    at l (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1846.js:58:4520)\n' +
[WebServer]         '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]         '    at async i (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:8996)\n' +
[WebServer]         '    at async j (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\6870.js:198:9148)\n' +
[WebServer]         '    at async m (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\app\\api\\internal\\user-chat-jobs\\run\\route.js:1:1404)',
[WebServer]       id: undefined
[WebServer]     }
[WebServer]   }
[WebServer] }
[WebServer] [user-chat-job] stale job recovery failed {
[WebServer]   chatId: 'goal-nC2XhBHMJJeNDV',
[WebServer]   messageId: 'Rp9cNSmChDVGae',
[WebServer]   jobId: 'NpeRnuKoCSYe2H',
[WebServer]   error: Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer]   
[WebServer]   Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]       at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]       at async n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:2640)
[WebServer]       at async l (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:4789)
[WebServer]       at async i (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:8996)
[WebServer]       at async j (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6870.js:198:9148)
[WebServer]       at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\app\api\internal\user-chat-jobs\run\route.js:1:1404)
[WebServer]       at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\6448.js:1:20147)
[WebServer] }
[WebServer] [user-chat-job] worker route failed Error: Failed to finalize user chat job "NpeRnuKoCSYe2H": Environment variable `POSTGRES_URL` or `DATABASE_URL` must be defined.
[WebServer] 
[WebServer] Configure one of these variables to use the Supabase/PostgreSQL database backend.
[WebServer]     at n (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:40:125)
[WebServer]     at async m (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:7:3790)
[WebServer]     at async (C:\Users\me\work\ai\promptbook\apps\agents-server\.next-e2e\server\chunks\1846.js:58:5002)
[WebServer] [agent-goal-chat] turn_trigger_failed {
[WebServer]   agentPermanentId: '14Rg83APp5axuR',
[WebServer]   chatId: 'goal-14Rg83APp5axuR',
[WebServer]   jobId: 'mNn1ssHhhysPhg',
[WebServer]   error: {
[WebServer]     name: 'Error',
[WebServer]     message: 'Failed to trigger user chat worker: 500 Internal Server Error',
[WebServer]     stack: 'Error: Failed to trigger user chat worker: 500 Internal Server Error\n' +
[WebServer]       '    at Object.f [as triggerUserChatJobWorker] (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1019.js:423:17023)\n' +
[WebServer]       '    at process.processTicksAndRejections (node:internal/process/task_queues:103:5)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1019.js:2160:33\n' +
[WebServer]       '    at async aI (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1019.js:2160:611)\n' +
[WebServer]       '    at async aL (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1019.js:2160:1451)\n' +
[WebServer]       '    at async a4 (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\.next-e2e\\server\\chunks\\1019.js:2160:4767)\n' +
[WebServer]       '    at async tQ (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:16:1725)\n' +
[WebServer]       '    at async C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:16:533\n' +
[WebServer]       '    at async tY (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:14:14659)\n' +
[WebServer]       '    at async nF (C:\\Users\\me\\work\\ai\\promptbook\\node_modules\\next\\dist\\compiled\\next-server\\app-page.runtime.prod.js:21:18781)',
[WebServer]     id: undefined
[WebServer]   }
[WebServer] }
[WebServer] You have not provided any `LlmExecutionTools`
[WebServer] This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
[WebServer] 
[WebServer] Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.
  ✓  19 [chromium] › tests\e2e\new-agent-redirect.spec.ts:76:9 › new agent redirect › opens the newly created agent chat immediately from the homepage (12.8s)


  1) [chromium] › tests\e2e\chat-history-navigation.spec.ts:102:9 › Agents Server chat history navigation › shows the first user message immediately as sending when starting a chat from the profile page 

    Error: Expected profile-page send to navigate to the durable chat route.

    Expected profile-page send to navigate to the durable chat route.

    expect(received).toBe(expected) // Object.is equality

    Expected: true
    Received: false

    Call Log:
    - Timeout 10000ms exceeded while waiting on the predicate

       at support\ChatHistoryNavigationExpectation.ts:59

      57 |             message: failureMessage,
      58 |         })
    > 59 |         .toBe(true);
         |          ^
      60 | }
      61 |
      62 | /**
        at Object.expectSelectedDurableChatUrl (C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\support\ChatHistoryNavigationExpectation.ts:59:10)
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\chat-history-navigation.spec.ts:119:48

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-90997--chat-from-the-profile-page-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-90997--chat-from-the-profile-page-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\chat-history-navigation-Ag-90997--chat-from-the-profile-page-chromium\error-context.md

  2) [chromium] › tests\e2e\chat-history-navigation.spec.ts:223:9 › Agents Server chat history navigation › navigates from the profile page for quick buttons and composer sends 

    Error: expect(locator).toBeVisible() failed

    Locator: getByRole('button', { name: 'Hello' })
    Expected: visible
    Timeout: 10000ms
    Error: element(s) not found

    Call log:
      - Expect "toBeVisible" with timeout 10000ms
      - waiting for getByRole('button', { name: 'Hello' })


      230 |
      231 |         await page.goto(`/agents/${encodeURIComponent(agent.agentId)}`);
    > 232 |         await expect(page.getByRole('button', { name: DEFAULT_QUICK_BUTTON_LABEL })).toBeVisible();
          |                                                                                      ^
      233 |
      234 |         await page.getByRole('button', { name: DEFAULT_QUICK_BUTTON_LABEL }).click();
      235 |
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\chat-history-navigation.spec.ts:232:86

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-22a7f--buttons-and-composer-sends-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-22a7f--buttons-and-composer-sends-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\chat-history-navigation-Ag-22a7f--buttons-and-composer-sends-chromium\error-context.md

  3) [chromium] › tests\e2e\chat-history-navigation.spec.ts:289:9 › Agents Server chat history navigation › sends quick-button prompts from the durable chat page 

    Error: expect(locator).toBeVisible() failed

    Locator: getByRole('button', { name: 'Hello' })
    Expected: visible
    Timeout: 10000ms
    Error: element(s) not found

    Call log:
      - Expect "toBeVisible" with timeout 10000ms
      - waiting for getByRole('button', { name: 'Hello' })


      295 |
      296 |         await page.goto(ChatHistoryNavigationSupport.buildAgentBrowserChatUrl(agent.agentId));
    > 297 |         await expect(page.getByRole('button', { name: DEFAULT_QUICK_BUTTON_LABEL })).toBeVisible();
          |                                                                                      ^
      298 |         await ChatHistoryNavigationExpectation.expectSelectedDurableChatUrl(
      299 |             page,
      300 |             agent,
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\chat-history-navigation.spec.ts:297:86

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-dad4e--from-the-durable-chat-page-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-dad4e--from-the-durable-chat-page-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\chat-history-navigation-Ag-dad4e--from-the-durable-chat-page-chromium\error-context.md

  4) [chromium] › tests\e2e\chat-history-navigation.spec.ts:325:9 › Agents Server chat history navigation › keeps the newly created chat selected after a delayed stale refresh and does not open a native dialog 

    Test timeout of 60000ms exceeded.

    Error: page.unroute: Target page, context or browser has been closed

       at support\ChatHistoryNavigationSupport.ts:266

      264 |         },
      265 |         async dispose() {
    > 266 |             await page.unroute('**/*', routeHandler);
          |                        ^
      267 |         },
      268 |     };
      269 | }
        at Object.dispose (C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\support\ChatHistoryNavigationSupport.ts:266:24)
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\chat-history-navigation.spec.ts:370:30

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-2355b-es-not-open-a-native-dialog-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\chat-history-navigation-Ag-2355b-es-not-open-a-native-dialog-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\chat-history-navigation-Ag-2355b-es-not-open-a-native-dialog-chromium\error-context.md

  5) [chromium] › tests\e2e\header-agent-view-navigation.spec.ts:26:9 › header agent-view navigation › navigates between profile and chat from the active agent breadcrumb menu 

    Error: expect(page).toHaveURL(expected) failed

    Expected pattern: /\/agents\/4Wjpba1C37JRKR$/
    Received string:  "http://127.0.0.1:4440/agents/nC2XhBHMJJeNDV"
    Timeout: 10000ms

    Call log:
      - Expect "toHaveURL" with timeout 10000ms
        5 × locator resolved to <html lang="en" data-theme-mode="light" data-theme-resolved="light">…</html>
          - unexpected value "http://127.0.0.1:4440/agents/nC2XhBHMJJeNDV/chat?chat=DDejrmwhySVg9W"
        - waiting for "http://127.0.0.1:4440/agents/nC2XhBHMJJeNDV" navigation to finish...
        - navigated to "http://127.0.0.1:4440/agents/nC2XhBHMJJeNDV"
        14 × locator resolved to <html lang="en" data-theme-mode="light" data-theme-resolved="light">…</html>
           - unexpected value "http://127.0.0.1:4440/agents/nC2XhBHMJJeNDV"


      41 |         await expect(page.getByRole('link', { name: 'Profile', exact: true })).toBeVisible();
      42 |         await page.getByRole('link', { name: 'Profile', exact: true }).click();
    > 43 |         await expect(page).toHaveURL(new RegExp(`/agents/${escapedAgentId}$`));
         |                            ^
      44 |
      45 |         await header.getByRole('button', { name: 'Profile', exact: true }).click();
      46 |         await expect(page.getByRole('link', { name: 'Chat', exact: true })).toBeVisible();
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\header-agent-view-navigation.spec.ts:43:28

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-agent-view-navigati-c4b41-ctive-agent-breadcrumb-menu-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-agent-view-navigati-c4b41-ctive-agent-breadcrumb-menu-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\header-agent-view-navigati-c4b41-ctive-agent-breadcrumb-menu-chromium\error-context.md

  6) [chromium] › tests\e2e\header-homepage-navigation.spec.ts:79:9 › header homepage navigation › navigates to the homepage from the desktop header brand link on an agent profile page 

    Error: page.evaluate: Error: Failed to create test agent: 500
        at eval (eval at evaluate (:311:30), <anonymous>:18:13)
        at async <anonymous>:337:30
        at eval (eval at evaluate (:311:30), <anonymous>:18:13)
        at async <anonymous>:337:30
        at Object.createTestAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\support\AgentManagementApi.ts:135:17)
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\header-homepage-navigation.spec.ts:84:48

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-homepage-navigation-825c5-nk-on-an-agent-profile-page-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-homepage-navigation-825c5-nk-on-an-agent-profile-page-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\header-homepage-navigation-825c5-nk-on-an-agent-profile-page-chromium\error-context.md

  7) [chromium] › tests\e2e\header-homepage-navigation.spec.ts:98:9 › header homepage navigation › navigates to the homepage from the mobile drawer server link on an agent profile page 

    Error: page.evaluate: Error: Failed to create test agent: 500
        at eval (eval at evaluate (:311:30), <anonymous>:18:13)
        at async <anonymous>:337:30
        at eval (eval at evaluate (:311:30), <anonymous>:18:13)
        at async <anonymous>:337:30
        at Object.createTestAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\support\AgentManagementApi.ts:135:17)
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\header-homepage-navigation.spec.ts:104:48

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-homepage-navigation-ae63d-nk-on-an-agent-profile-page-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\header-homepage-navigation-ae63d-nk-on-an-agent-profile-page-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\header-homepage-navigation-ae63d-nk-on-an-agent-profile-page-chromium\error-context.md

  8) [chromium] › tests\e2e\management-api.spec.ts:44:9 › Agents Server management API › supports OpenAPI docs and owner-scoped CRUD flows 

    Error: expect(received).toBe(expected) // Object.is equality

    Expected: 201
    Received: 500

      143 |             },
      144 |         });
    > 145 |         expect(createAgentResponse.status).toBe(201);
          |                                            ^
      146 |         expect(createAgentResponse.body).toMatchObject({
      147 |             agent: {
      148 |                 id: expect.any(String),
        at C:\Users\me\work\ai\promptbook\apps\agents-server\tests\e2e\management-api.spec.ts:145:44

    attachment #1: screenshot (image/png) ──────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\management-api-Agents-Serv-4a68a-and-owner-scoped-CRUD-flows-chromium\test-failed-1.png
    ────────────────────────────────────────────────────────────────────────────────────────────────

    attachment #2: video (video/webm) ──────────────────────────────────────────────────────────────
    ..\..\other\integration-tests\videos\management-api-Agents-Serv-4a68a-and-owner-scoped-CRUD-flows-chromium\video.webm
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: ..\..\other\integration-tests\videos\management-api-Agents-Serv-4a68a-and-owner-scoped-CRUD-flows-chromium\error-context.md

  8 failed
    [chromium] › tests\e2e\chat-history-navigation.spec.ts:102:9 › Agents Server chat history navigation › shows the first user message immediately as sending when starting a chat from the profile page 
    [chromium] › tests\e2e\chat-history-navigation.spec.ts:223:9 › Agents Server chat history navigation › navigates from the profile page for quick buttons and composer sends 
    [chromium] › tests\e2e\chat-history-navigation.spec.ts:289:9 › Agents Server chat history navigation › sends quick-button prompts from the durable chat page 
    [chromium] › tests\e2e\chat-history-navigation.spec.ts:325:9 › Agents Server chat history navigation › keeps the newly created chat selected after a delayed stale refresh and does not open a native dialog 
    [chromium] › tests\e2e\header-agent-view-navigation.spec.ts:26:9 › header agent-view navigation › navigates between profile and chat from the active agent breadcrumb menu 
    [chromium] › tests\e2e\header-homepage-navigation.spec.ts:79:9 › header homepage navigation › navigates to the homepage from the desktop header brand link on an agent profile page 
    [chromium] › tests\e2e\header-homepage-navigation.spec.ts:98:9 › header homepage navigation › navigates to the homepage from the mobile drawer server link on an agent profile page 
    [chromium] › tests\e2e\management-api.spec.ts:44:9 › Agents Server management API › supports OpenAPI docs and owner-scoped CRUD flows 
  11 passed (4.6m)

 succeeded in 52336ms:
npm test process has finished

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem apps/agents-server/test-results -Force -ErrorAction SilentlyContinue | Select-Object Name, Length, LastWriteTime; Get-Content apps/agents-server/test-results/.last-run.json -ErrorAction SilentlyContinue; git status --short; git diff --check' in C:\Users\me\work\ai\promptbook
 exited 1 in 855ms:

 M .vscode/terminals.json
 M apps/coder-landing/specs/content/commands.md
 M apps/coder-landing/specs/sections/advanced-features.md
 M apps/coder-landing/specs/sections/agent-book.md
 M apps/coder-landing/src/components/sections/AgentsSection.tsx
 M apps/coder-landing/src/data/advancedFeatures.ts
 M apps/coder-landing/src/data/commands.ts
 M changelog/_current-preversion.md
 M prompts/2026-09-0210-ptbk-coder-by-agent.md
 M scripts/run-codex-prompts/cli/parseRunOptions.test.ts
 M scripts/run-codex-prompts/cli/parseRunOptions.ts
 M scripts/run-codex-prompts/common/resolveCoderAgent.ts
 M scripts/run-codex-prompts/main/runCodexPrompts.ts
 M scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
 M scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
 M src/cli/cli-commands/coder/list.test.ts
 M src/cli/cli-commands/coder/list.ts
 M src/cli/cli-commands/coder/run.test.ts
 M src/cli/cli-commands/coder/run.ts
 M src/cli/cli-commands/coder/server.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
?? src/cli/cli-commands/coder/agentCliOptions.ts
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
Name           Length LastWriteTime
----           ------ -------------
.last-run.json     45 8/24/2026 11:46:28 AM
{
  "status": "passed",
  "failedTests": []
}


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
`npm test` reached and completed its end-to-end stage; the test runner’s final status is passing. I’m doing a final consistency pass now, including the CLI help, routing aliases, and landing copy.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- src/cli/cli-commands/coder/agentCliOptions.ts src/cli/cli-commands/coder/run.ts src/cli/cli-commands/coder/server.ts src/cli/cli-commands/coder/list.ts scripts/run-codex-prompts/common/resolveCoderAgent.ts scripts/run-codex-prompts/main/runCodexPrompts.ts scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/cli/parseRunOptions.ts scripts/run-codex-prompts/common/resolveCoderAgent.test.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts src/cli/cli-commands/coder/run.test.ts src/cli/cli-commands/coder/list.test.ts scripts/run-codex-prompts/cli/parseRunOptions.test.ts; Get-Content src/cli/cli-commands/coder/agentCliOptions.ts; Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.test.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 945ms:
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb6..78bb2b22c 100644
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@ describe('parseRunOptions', () => {
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9..7c8d81c0c 100644
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@ const DEFAULT_WAIT_AFTER_ERROR_MS = 10 * 60 * 1000;
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@ const KNOWN_OPTION_FLAGS = new Set([
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@ export function parseRunOptions(args: string[]): RunOptions {
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@ export function parseRunOptions(args: string[]): RunOptions {
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43..7c1ec7d13 100644
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,19 +1,33 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
+    /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
     /**
      * Compiled system message injected into each coding prompt.
      */
@@ -21,34 +35,94 @@ export type ResolvedCoderAgent = {
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
     };
 }
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
+
+    return {
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+    };
+}
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd7..33d3dc116 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84..33858246b 100644
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@ import type { PromptFile } from './types/PromptFile';
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@ export type PromptRunnerIdentity = {
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@ export function isPromptCompatibleWithRunner(
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92..8726dbdbe 100644
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@ describe('prompt runner filtering', () => {
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb6..0aa8e33d7 100644
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@ jest.mock('../../../../scripts/run-codex-prompts/main/listCoderPrompts', () => (
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -13,6 +19,13 @@ function getListCoderPromptsMock(): jest.MockedFunction<typeof listCoderPrompts>
     return listCoderPrompts as jest.MockedFunction<typeof listCoderPrompts>;
 }
 
+/**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
 /**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
@@ -36,6 +49,7 @@ describe('$initializeCoderListCommand', () => {
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@ describe('$initializeCoderListCommand', () => {
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05..3d064ae9e 100644
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@ import {
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@ export function $initializeCoderListCommand(program: Program): $side_effect {
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeb..067175570 100644
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@ describe('$initializeCoderRunCommand', () => {
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fa..41382d38f 100644
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@ import {
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b..3a48ebbfb 100644
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@ import {
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';

/**
 * Commander option bag for an optional Book agent used by coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export type CoderAgentCliOptions = {
    readonly agent?: string;
};

/**
 * Description shared by coder commands which accept an agent Book.
 *
 * @private internal utility of `ptbk coder`
 */
export const CODER_AGENT_OPTION_DESCRIPTION =
    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';

/**
 * Registers the shared `--agent` option for coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export function addCoderAgentOption(command: Program): void {
    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
}

// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names
import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
import { tmpdir } from 'os';
import { join } from 'path';
import { resolveCoderAgentBook } from './resolveCoderAgent';

describe('resolveCoderAgentBook', () => {
    let temporaryDirectoryPath: string;

    beforeEach(async () => {
        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
    });

    afterEach(async () => {
        await rm(temporaryDirectoryPath, { recursive: true, force: true });
    });

    it('returns undefined when no agent book is selected', async () => {
        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
    });

    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
        await mkdir(agentBookDirectoryPath, { recursive: true });
        await writeFile(
            join(agentBookDirectoryPath, 'developer.book'),
            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
            'utf-8',
        );

        const resolvedAgentBook = await resolveCoderAgentBook(
            'agents/coding/developer.book',
            temporaryDirectoryPath,
        );

        expect(resolvedAgentBook?.agentReferences).toEqual(
            expect.arrayContaining([
                'agents/coding/developer.book',
                'developer.book',
                'developer',
                'developer-foo-bar',
            ]),
        );
    });
});

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

context compacted
codex
I’ve completed the implementation and full test run; I’m doing one final consistency check of the shared option and agent-resolution paths.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content src/cli/cli-commands/coder/agentCliOptions.ts; Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.ts; Get-Content src/cli/cli-commands/coder/list.ts; git diff --check; git diff --stat' in C:\Users\me\work\ai\promptbook
 succeeded in 699ms:
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';

/**
 * Commander option bag for an optional Book agent used by coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export type CoderAgentCliOptions = {
    readonly agent?: string;
};

/**
 * Description shared by coder commands which accept an agent Book.
 *
 * @private internal utility of `ptbk coder`
 */
export const CODER_AGENT_OPTION_DESCRIPTION =
    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';

/**
 * Registers the shared `--agent` option for coder commands.
 *
 * @private internal utility of `ptbk coder`
 */
export function addCoderAgentOption(command: Program): void {
    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
}

// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and exported helper names
import { readFile } from 'fs/promises';
import { basename, extname, relative, resolve } from 'path';
import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import { NotFoundError } from '../../../src/errors/NotFoundError';
import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';

/**
 * Source and prompt-routing references resolved from an optional `--agent` book file.
 */
export type ResolvedCoderAgentBook = {
    /**
     * Raw source of the agent `.book` file.
     */
    readonly agentSource: string_book;

    /**
     * Equivalent references by which a prompt may target this agent.
     *
     * Includes the configured path, relative path, filename, filename without `.book`, and the
     * normalized Book title from its first line.
     */
    readonly agentReferences: ReadonlyArray<string>;
};

/**
 * Agent data resolved from the optional `--agent` book file.
 */
export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
    /**
     * Compiled system message injected into each coding prompt.
     */
    readonly systemMessage: string;
};

/**
 * Reads an optional agent `.book` file and prepares the references that route prompts to it.
 *
 * Returns `undefined` when no agent path is provided.
 */
export async function resolveCoderAgentBook(
    agentBookReference: string | undefined,
    currentWorkingDirectory: string,
): Promise<ResolvedCoderAgentBook | undefined> {
    const normalizedAgentBookReference = agentBookReference?.trim();

    if (!normalizedAgentBookReference) {
        return undefined;
    }

    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
        if (error.code === 'ENOENT' || error.code === 'EISDIR') {
            throw new NotFoundError(
                spaceTrim(`
                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.

                    Pass a path to a \`.book\` file in \`--agent\`.
                `),
            );
        }
        throw error;
    })) as string_book;

    return {
        agentSource,
        agentReferences: createCoderAgentReferences({
            agentBookReference: normalizedAgentBookReference,
            resolvedAgentBookPath,
            currentWorkingDirectory,
            agentSource,
        }),
    };
}

/**
 * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
 *
 * Returns `undefined` when no agent path is provided.
 */
export async function resolveCoderAgent(
    agentBookReference: string | undefined,
    currentWorkingDirectory: string,
): Promise<ResolvedCoderAgent | undefined> {
    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);

    if (resolvedAgentBook === undefined) {
        return undefined;
    }

    return {
        ...resolvedAgentBook,
        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
    };
}

/**
 * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
 *
 * @private internal utility of `resolveCoderAgentBook`
 */
function createCoderAgentReferences(options: {
    readonly agentBookReference: string;
    readonly resolvedAgentBookPath: string;
    readonly currentWorkingDirectory: string;
    readonly agentSource: string_book;
}): string[] {
    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
        '\\',
        '/',
    );
    const agentBookFileName = basename(options.resolvedAgentBookPath);
    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;

    return Array.from(
        new Set(
            [
                options.agentBookReference,
                relativeAgentBookPath,
                agentBookFileName,
                agentBookName,
                agentNameFromBook,
            ].filter((agentReference) => agentReference.trim() !== ''),
        ),
    );
}
import type {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { handleActionErrors } from '../common/handleActionErrors';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import type { PromptRunnerSelectionCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerSelectionCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';

/**
 * Initializes `coder list` command for Promptbook CLI utilities.
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI.
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderListCommand(program: Program): $side_effect {
    const command = program.command('list');
    command.description(
        spaceTrim(`
            List ready coding prompts by priority without executing them

            ${PROMPT_RUNNER_DESCRIPTION}

            Features:
            - Lists only ready, fully authored prompts
            - Groups prompts from highest to lowest priority
            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
            - Does not start a coding harness or modify prompt files
        `),
    );

    addPromptRunnerSelectionOptions(command);
    addCoderAgentOption(command);
    addPromptPriorityOptions(command);

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                agent,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
            } = cliOptions as {
                readonly agent?: string;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
            const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                cliOptions as PromptRunnerSelectionCliOptions,
                { isAgentRequired: false },
            );
            const resolvedCoderAgentBook =
                agent === undefined
                    ? undefined
                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
                          .resolveCoderAgentBook(agent, process.cwd());
            const promptRunnerIdentity =
                runnerOptions.agentName === undefined &&
                runnerOptions.model === undefined &&
                resolvedCoderAgentBook === undefined
                    ? undefined
                    : {
                          harnessName: runnerOptions.agentName,
                          modelName: runnerOptions.model,
                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                      };

            // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
            const { listCoderPrompts } = await import('../../../../scripts/run-codex-prompts/main/listCoderPrompts');
            await listCoderPrompts({
                priority,
                minimumPriority,
                maximumPriority,
                promptRunnerIdentity,
            });
        }),
    );
}

// Note: [🟡] Code for CLI command [list](src/cli/cli-commands/coder/list.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
 .vscode/terminals.json                             |  11 +++
 apps/coder-landing/specs/content/commands.md       |   5 +-
 .../specs/sections/advanced-features.md            |  48 +++++-----
 apps/coder-landing/specs/sections/agent-book.md    |   3 +-
 .../src/components/sections/AgentsSection.tsx      |   6 ++
 apps/coder-landing/src/data/advancedFeatures.ts    |   9 +-
 apps/coder-landing/src/data/commands.ts            |   6 ++
 changelog/_current-preversion.md                   |  11 +++
 prompts/2026-09-0210-ptbk-coder-by-agent.md        |   3 +-
 .../run-codex-prompts/cli/parseRunOptions.test.ts  |  17 ++++
 scripts/run-codex-prompts/cli/parseRunOptions.ts   |   5 +-
 .../run-codex-prompts/common/resolveCoderAgent.ts  | 102 ++++++++++++++++++---
 scripts/run-codex-prompts/main/runCodexPrompts.ts  |   1 +
 .../prompts/isPromptCompatibleWithRunner.ts        |  21 +++--
 .../prompts/promptRunnerFiltering.test.ts          |  45 +++++++++
 src/cli/cli-commands/coder/list.test.ts            |  46 ++++++++++
 src/cli/cli-commands/coder/list.ts                 |  18 +++-
 src/cli/cli-commands/coder/run.test.ts             |  29 ++++++
 src/cli/cli-commands/coder/run.ts                  |   8 +-
 src/cli/cli-commands/coder/server.ts               |   8 +-
 20 files changed, 340 insertions(+), 62 deletions(-)

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/main/runCodexPrompts.ts scripts/run-codex-prompts/cli/parseRunOptions.ts src/cli/cli-commands/coder/run.ts src/cli/cli-commands/coder/server.ts src/cli/cli-commands/coder/list.ts src/cli/cli-commands/coder/agentCliOptions.ts scripts/run-codex-prompts/common/resolveCoderAgent.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 420ms:
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9..7c8d81c0c 100644
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@ const DEFAULT_WAIT_AFTER_ERROR_MS = 10 * 60 * 1000;
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@ const KNOWN_OPTION_FLAGS = new Set([
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@ export function parseRunOptions(args: string[]): RunOptions {
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@ export function parseRunOptions(args: string[]): RunOptions {
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43..7c1ec7d13 100644
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,19 +1,33 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
+    /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
     /**
      * Compiled system message injected into each coding prompt.
      */
@@ -21,34 +35,94 @@ export type ResolvedCoderAgent = {
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
     };
 }
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
+
+    return {
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
+    };
+}
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd7..33d3dc116 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84..33858246b 100644
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@ import type { PromptFile } from './types/PromptFile';
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@ export type PromptRunnerIdentity = {
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@ export function isPromptCompatibleWithRunner(
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05..3d064ae9e 100644
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@ import {
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@ export function $initializeCoderListCommand(program: Program): $side_effect {
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fa..41382d38f 100644
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@ import {
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@ export function $initializeCoderRunCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b..3a48ebbfb 100644
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@ import {
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@ export function $initializeCoderServerCommand(program: Program): $side_effect {
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts; Get-Content scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts; Get-Content scripts/run-codex-prompts/common/resolveCoderAgent.test.ts; Get-Content scripts/run-codex-prompts/cli/parseRunOptions.ts -TotalCount 230; Get-Content scripts/run-codex-prompts/common/types/RunOptions.ts' in C:\Users\me\work\ai\promptbook
 exited 1 in 745ms:
import { normalizeToKebabCase } from '../../../src/utils/normalization/normalize-to-kebab-case';
import type { PromptFile } from './types/PromptFile';
import type { PromptSection } from './types/PromptSection';

/**
 * Names of the harness, model and Book agent currently selected for a coder run.
 */
export type PromptRunnerIdentity = {
    /**
     * Stable harness identifier, for example `github-copilot`.
     */
    readonly harnessName?: string;
    /**
     * Effective model identifier, for example `gpt-5.5`.
     */
    readonly modelName?: string;
    /**
     * Equivalent agent path and name references, including the Book title from its first line.
     */
    readonly agentReferences?: ReadonlyArray<string>;
};

/**
 * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
 *
 * A prompt status line can contain one or more backtick-delimited model, harness or agent
 * references. Matching is intentionally based on normalized substrings so a token such as
 * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
 * and `opus` selects a `claude-opus-*` model.
 */
export function isPromptCompatibleWithRunner(
    file: PromptFile,
    section: PromptSection,
    promptRunnerIdentity?: PromptRunnerIdentity,
): boolean {
    if (promptRunnerIdentity === undefined) {
        return true;
    }

    const statusLine = section.statusLineIndex === undefined ? undefined : file.lines[section.statusLineIndex];
    const requiredRunnerTokens = statusLine === undefined ? [] : extractPromptRunnerTokens(statusLine);

    if (requiredRunnerTokens.length === 0) {
        return true;
    }

    const normalizedRunnerNames = [
        promptRunnerIdentity.harnessName,
        promptRunnerIdentity.modelName,
        ...(promptRunnerIdentity.agentReferences ?? []),
    ]
        .filter((name): name is string => name !== undefined && name.trim() !== '')
        .map((name) => normalizeToKebabCase(name))
        .filter((name) => name !== '');

    return requiredRunnerTokens.some((requiredRunnerToken) => {
        const normalizedRequiredRunnerToken = normalizeToKebabCase(requiredRunnerToken);

        return (
            normalizedRequiredRunnerToken !== '' &&
            normalizedRunnerNames.some((normalizedRunnerName) =>
                normalizedRunnerName.includes(normalizedRequiredRunnerToken),
            )
        );
    });
}

/**
 * Extracts model and harness tokens from a prompt status line.
 */
export function extractPromptRunnerTokens(statusLine: string): string[] {
    return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
        .map((match) => match[1]?.trim() ?? '')
        .filter((token) => token !== '');
}
import { spaceTrim } from 'spacetrim';
import { findNextTodoPrompt } from './findNextTodoPrompt';
import { listRunnablePrompts } from './listRunnablePrompts';
import { parsePromptFile } from './parsePromptFile';

describe('prompt runner filtering', () => {
    it('matches model, model family, and harness tokens while preserving priority markers', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`gpt-5.5\`
                Exact model
                ---
                [ ] use model \`gpt-5.5\`
                Exact model with wording
                ---
                [ ] !!!! \`gpt\`
                Model family
                ---
                [ ] use \`github-copilot\` !!!!!
                Harness
                ---
                [ ] use \`claude-opus\` or \`github-copilot\`
                One of multiple tokens
                ---
                [ ] use \`claude-opus\`
                Different model family
                ---
                [ ]
                Unrestricted
            `),
        );

        const runnable = listRunnablePrompts(
            [file],
            {},
            {
                harnessName: 'github-copilot',
                modelName: 'gpt-5.5',
            },
        );

        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 6]);
        expect(runnable.map(({ section }) => section.priority)).toEqual([0, 0, 4, 5, 0, 0]);
        expect(
            findNextTodoPrompt([file], {}, { harnessName: 'github-copilot', modelName: 'gpt-5.5' })?.section.index,
        ).toBe(3);
    });

    it('matches normalized model families and aliases dynamically', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`CLAUDE\`
                Claude family
                ---
                [ ] \`claude-opus\`
                Claude Opus family
                ---
                [ ] \`opus\`
                Opus alias
                ---
                [ ] \`claude-opus-4.8\`
                Exact normalized model
                ---
                [ ] \`gpt\`
                Different family
            `),
        );

        const runnable = listRunnablePrompts(
            [file],
            {},
            {
                harnessName: 'claude-code',
                modelName: 'claude-opus-4.8',
            },
        );

        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
    });

    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`agents/coding/developer.book\`
                Full agent path
                ---
                [ ] use \`developer.book\`
                Agent filename
                ---
                [ ] use \`developer\`
                Agent filename without extension
                ---
                [ ] use \`Developer Foo bar\`
                Agent Book title
                ---
                [ ] use \`openai-codex\`
                Harness alongside agent routing
                ---
                [ ] use \`gpt-5.6-astra\`
                Model alongside agent routing
                ---
                [ ] use \`unslopper\`
                Different agent
            `),
        );

        const runnable = listRunnablePrompts(
            [file],
            {},
            {
                harnessName: 'openai-codex',
                modelName: 'gpt-5.6-astra',
                agentReferences: [
                    'agents/coding/developer.book',
                    'developer.book',
                    'developer',
                    'developer-foo-bar',
                ],
            },
        );

        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
    });

    it('does not constrain prompts when no runner identity is provided', () => {
        const file = parsePromptFile(
            'prompts/runner-filtering.md',
            spaceTrim(`
                [ ] use \`gpt\`
                Model-specific task
                ---
                [ ]
                General task
            `),
        );

        expect(listRunnablePrompts([file])).toHaveLength(2);
    });
});
import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
import { tmpdir } from 'os';
import { join } from 'path';
import { resolveCoderAgentBook } from './resolveCoderAgent';

describe('resolveCoderAgentBook', () => {
    let temporaryDirectoryPath: string;

    beforeEach(async () => {
        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
    });

    afterEach(async () => {
        await rm(temporaryDirectoryPath, { recursive: true, force: true });
    });

    it('returns undefined when no agent book is selected', async () => {
        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
    });

    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
        await mkdir(agentBookDirectoryPath, { recursive: true });
        await writeFile(
            join(agentBookDirectoryPath, 'developer.book'),
            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
            'utf-8',
        );

        const resolvedAgentBook = await resolveCoderAgentBook(
            'agents/coding/developer.book',
            temporaryDirectoryPath,
        );

        expect(resolvedAgentBook?.agentReferences).toEqual(
            expect.arrayContaining([
                'agents/coding/developer.book',
                'developer.book',
                'developer',
                'developer-foo-bar',
            ]),
        );
    });
});
import colors from 'colors';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import {
    DEFAULT_GIT_CHANGES_MODE,
    GIT_CHANGES_MODE_VALUES,
    isGitChangesMode,
} from '../../../src/cli/cli-commands/coder/GitChangesMode';
import type { ThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import { THINKING_LEVEL_VALUES, parseThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import {
    PROMPT_RUNNER_HARNESS_NAMES,
    PROMPT_RUNNER_HARNESS_OPTION_HINT,
    type PromptRunnerHarnessName,
} from '../../../src/cli/cli-commands/common/promptRunnerCliOptions';
import { parseDuration } from '../common/parseDuration';
import { normalizePriorityFilter } from '../prompts/priorityFilter';
import { isTestBeforeMode, TEST_BEFORE_MODE_VALUES, type TestBeforeMode } from '../testing/TestBeforeMode';
import type { RunOptions } from './RunOptions';

/**
 * Default wait duration applied before retrying a failed prompt round.
 */
const DEFAULT_WAIT_AFTER_ERROR_MS = 10 * 60 * 1000;

/**
 * CLI usage text for this script.
 */
const USAGE =
    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';

/**
 * Top-level flags supported by this command.
 */
const KNOWN_OPTION_FLAGS = new Set([
    '--dry-run',
    '--harness',
    '--model',
    '--agent',
    '--context',
    '--test',
    '--test-before',
    '--preserve-logs',
    '--isolate',
    '--no-ui',
    '--thinking-level',
    '--priority',
    '--min-priority',
    '--max-priority',
    '--limit',
    '--allow-credits',
    '--auto-migrate',
    '--allow-destructive-auto-migrate',
    '--wait-after-prompt',
    '--wait-between-prompts',
    '--wait-after-error',
    '--no-auto',
    '--no-commit',
    '--git-changes',
    '--no-normalize-line-endings',
    '--auto-push',
    '--auto-pull',
]);

/**
 * Parses CLI arguments into runner options.
 */
export function parseRunOptions(args: string[]): RunOptions {
    let agentName: PromptRunnerHarnessName | undefined = undefined;
    const dryRun = args.includes('--dry-run');

    const harnessValue = readOptionValue(args, '--harness');
    if (harnessValue) {
        const value = harnessValue;
        if (PROMPT_RUNNER_HARNESS_NAMES.includes(value as PromptRunnerHarnessName)) {
            agentName = value as PromptRunnerHarnessName;
        }
    }

    const model = readOptionValue(args, '--model');
    const agent = readOptionValue(args, '--agent');
    const context = readOptionValue(args, '--context');
    const hasTestCommandFlag = args.includes('--test');
    const testCommand = readVariadicOptionValue(args, '--test');
    const hasTestBeforeFlag = args.includes('--test-before');
    const testBefore = parseTestBeforeOption(readOptionValue(args, '--test-before'), hasTestBeforeFlag);
    const preserveLogs = args.includes('--preserve-logs');
    const isIsolated = args.includes('--isolate');
    const noUi = args.includes('--no-ui');
    const hasThinkingLevelFlag = args.includes('--thinking-level');
    const thinkingLevelValue = readOptionValue(args, '--thinking-level');
    const isPriorityFlagProvided = args.includes('--priority');
    const legacyMinimumPriority = parsePriorityBoundary(
        readOptionValue(args, '--priority'),
        isPriorityFlagProvided,
        '--priority',
    );
    const isMinimumPriorityFlagProvided = args.includes('--min-priority');
    const explicitMinimumPriority = parsePriorityBoundary(
        readOptionValue(args, '--min-priority'),
        isMinimumPriorityFlagProvided,
        '--min-priority',
    );
    const isMaximumPriorityFlagProvided = args.includes('--max-priority');
    const maximumPriority = parsePriorityBoundary(
        readOptionValue(args, '--max-priority'),
        isMaximumPriorityFlagProvided,
        '--max-priority',
    );
    const minimumPriority = resolveMinimumPriority(legacyMinimumPriority, explicitMinimumPriority);
    const priorityFilter = parsePriorityFilter({
        priority: legacyMinimumPriority,
        minimumPriority,
        maximumPriority,
    });
    const hasLimitFlag = args.includes('--limit');
    const limit = parseLimit(readOptionValue(args, '--limit'), hasLimitFlag);
    const noCommit = args.includes('--no-commit');
    const hasGitChangesFlag = args.includes('--git-changes');
    const gitChanges = parseGitChangesOption(readOptionValue(args, '--git-changes'), hasGitChangesFlag);
    const normalizeLineEndings = !args.includes('--no-normalize-line-endings');
    const allowCredits = args.includes('--allow-credits');
    const autoMigrate = args.includes('--auto-migrate');
    const allowDestructiveAutoMigrate = args.includes('--allow-destructive-auto-migrate');
    const autoPush = args.includes('--auto-push');
    const autoPull = args.includes('--auto-pull');
    // [1] Parse --wait <duration> and --no-auto:
    //   default: run automatically through the queue (no waiting)
    //   --no-auto: wait for user confirmation before each prompt (interactive mode)
    //   --wait 1h: wait 1h between prompt rounds to avoid rate limits
    const waitForUser = args.includes('--no-auto');
    const waitAfterPrompt = parseWaitOption(args, '--wait-after-prompt', 0);
    const waitBetweenPrompts = parseWaitOption(args, '--wait-between-prompts', 0);
    const waitAfterError = parseWaitOption(args, '--wait-after-error', DEFAULT_WAIT_AFTER_ERROR_MS);
    let thinkingLevel: ThinkingLevel | undefined;

    if (hasTestCommandFlag && testCommand === undefined) {
        exitWithUsageError(
            'Missing value for --test. Use a shell command such as `npm run test` and quote it when it contains top-level CLI flags.',
        );
    }

    if (hasThinkingLevelFlag && thinkingLevelValue === undefined) {
        exitWithUsageError(`Missing value for --thinking-level. Use one of: ${THINKING_LEVEL_VALUES.join(', ')}.`);
    }

    try {
        thinkingLevel = parseThinkingLevel(thinkingLevelValue);
    } catch (error) {
        exitWithUsageError(error instanceof Error ? error.message : String(error));
    }

    if (!agentName && !dryRun) {
        exitWithUsageError(`You must choose a harness using ${PROMPT_RUNNER_HARNESS_OPTION_HINT}`);
    }

    return {
        dryRun,
        waitForUser,
        waitAfterPrompt,
        waitBetweenPrompts,
        waitAfterError,
        noCommit,
        gitChanges,
        normalizeLineEndings,
        allowCredits,
        autoMigrate,
        allowDestructiveAutoMigrate,
        autoPush,
        autoPull,
        preserveLogs,
        isIsolated,
        noUi,
        agentName,
        model,
        agent,
        context,
        testCommand,
        testBefore,
        thinkingLevel,
        priority: minimumPriority ?? 0,
        minimumPriority,
        maximumPriority,
        priorityFilter,
        limit,
    };
}

/**
 * Parses and validates the optional pre-coding verification mode.
 */
function parseTestBeforeOption(value: string | undefined, hasTestBeforeFlag: boolean): TestBeforeMode {
    if (value === undefined) {
        if (hasTestBeforeFlag) {
            exitWithUsageError(`Missing value for --test-before. Use one of: ${TEST_BEFORE_MODE_VALUES.join(', ')}.`);
        }

        return 'no';
    }

    if (!isTestBeforeMode(value)) {
        exitWithUsageError(
            `Invalid value for --test-before: "${value}". Use one of: ${TEST_BEFORE_MODE_VALUES.join(', ')}.`,
        );
    }

    return value;
}

/**
 * Parses and validates the requested behavior for a dirty working tree.
 */
function parseGitChangesOption(value: string | undefined, hasGitChangesFlag: boolean): GitChangesMode {
    if (value === undefined) {
        if (hasGitChangesFlag) {
            exitWithUsageError(`Missing value for --git-changes. Use one of: ${GIT_CHANGES_MODE_VALUES.join(', ')}.`);
        }

        return DEFAULT_GIT_CHANGES_MODE;
    }

    if (!isGitChangesMode(value)) {
        exitWithUsageError(
            `Invalid value for --git-changes: "${value}". Use one of: ${GIT_CHANGES_MODE_VALUES.join(', ')}.`,
        );
    }

    return value;
}

/**
Get-Content: 
Line |
   2 |  … lCount 230; Get-Content scripts/run-codex-prompts/common/types/RunOpt …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\common\types\RunOptions.ts' because it does not exist.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content src/cli/cli-commands/coder/run.ts; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts -TotalCount 180; rg -n \"type RunOptions|interface RunOptions|agent:\\s|agent\\?\" scripts/run-codex-prompts src/cli/cli-commands/coder -g '*.ts' | Select-Object -First 100; Get-Content package.json -TotalCount 180" in C:\Users\me\work\ai\promptbook
 succeeded in 1355ms:
import {
    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
    Option,
} from 'commander';
import { spaceTrim } from 'spacetrim';
import { assertsError } from '../../../errors/assertsError';
import type { $side_effect } from '../../../utils/organization/$side_effect';
import { createPositiveIntegerOptionParser } from '../common/createPositiveIntegerOptionParser';
import { $assertSufficientFreeDiskSpace } from '../common/disk-space/$assertSufficientFreeDiskSpace';
import { handleActionErrors } from '../common/handleActionErrors';
import { $ensureHarnessInstallations } from '../common/harness/$ensureHarnessInstallations';
import {
    addQuestionsOption,
    assertUserConfirmationIsAllowed,
    normalizeQuestionsCliOptions,
    QUESTIONS_DESCRIPTION,
    type QuestionsCliOptions,
} from '../common/questionsCliOptions';
import { $ensurePromptbookCliInstallations } from '../common/promptbook-cli/$ensurePromptbookCliInstallations';
import type { PromptRunnerCliOptions } from '../common/promptRunnerCliOptions';
import {
    addPromptRunnerExecutionOptions,
    addPromptRunnerSelectionOptions,
    normalizePromptRunnerCliOptions,
    PROMPT_RUNNER_DESCRIPTION,
} from '../common/promptRunnerCliOptions';
import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
import {
    DEFAULT_CODER_TEST_COMMAND,
    TEST_BEFORE_MODE_VALUES,
    type TestBeforeMode,
} from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
import { printCoderRunFailure } from './printCoderRunFailure';

/**
 * Initializes `coder run` command for Promptbook CLI utilities
 *
 * Note: `$` is used to indicate that this function is not a pure function - it registers a command in the CLI
 *
 * @private internal function of `promptbookCli`
 */
export function $initializeCoderRunCommand(program: Program): $side_effect {
    const command = program.command('run');
    command.description(
        spaceTrim(
            (block) => `
            Execute coding prompts through selected AI agent

            ${block(PROMPT_RUNNER_DESCRIPTION)}

            ${block(QUESTIONS_DESCRIPTION)}

            Features:
            - Automatically stages and commits changes with agent identity unless --no-commit is used
            - Commits only the prompt file and the files the coding agent has changed, leaving unrelated changes alone
            - Refuses to start on a dirty working tree unless --git-changes says what should happen with the changes
            - Optional --git-changes continue resumes the single prompt an interrupted coder left in the [^] status
            - Optional post-commit git push with explicit --auto-push opt-in
            - Optional pre-prompt git pull with explicit --auto-pull opt-in
            - Optional --isolate runs every prompt in its own temporary git worktree and merges it back when verified
            - Saves the run trace of every round into prompts/traces/, named after its prompt file
            - Optional --preserve-logs keeps temp prompt/log artifacts after successful rounds
            - Optional --no-ui keeps plain streaming console output for logging and debugging
            - Refuses to start on a nearly full disk and pauses the run when the free disk space becomes critical, unless --no-questions is used
            - Checks that the selected harness is installed and up to date before the first prompt unless --no-questions is used
            - Offers to add missing project-local ignore rules for the selected harness
            - In interactive mode, checks local and global Promptbook CLI installations and offers to update them
            - Supports GPG signing of commits
            - Optional pre-coding test run that can stop or repair pre-existing failures
            - Optional post-prompt verification with test-feedback retries
            - Progress tracking and interactive P/S/X terminal controls
            - Dry-run mode to preview prompts
        `,
        ),
    );

    command.option('--dry-run', 'Print unwritten prompts without executing', false);
    addPromptRunnerSelectionOptions(command);
    addQuestionsOption(command);
    addCoderAgentOption(command);
    command.option(
        '--context <context-or-file>',
        'Append extra instructions either inline or from a file path relative to the current project',
    );
    command.option(
        '--test <test-command...>',
        'Run a verification command after each prompt; quote it when the command itself contains top-level flags',
    );
    command.addOption(
        new Option(
            '--test-before <mode>',
            `Run tests before coding: ${TEST_BEFORE_MODE_VALUES.join(
                ', ',
            )} (defaults to no; uses npm test when --test is omitted)`,
        )
            .choices([...TEST_BEFORE_MODE_VALUES])
            .default('no'),
    );
    command.option(
        '--preserve-logs',
        'Keep generated temp prompt/log artifacts after successful rounds for debugging and analytics',
        false,
    );
    addPromptRunnerExecutionOptions(command);
    command.option(
        '--isolate',
        spaceTrim(`
            Implement each prompt in its own temporary git worktree with its own isolated environment.
            A verified task is merged back into the branch the coder runs on and the worktree is deleted.
            A task that cannot be merged is marked as failed and its worktree is kept for a manual merge.
        `),
        false,
    );
    addPromptPriorityOptions(command);
    command.option(
        '--limit <run-count>',
        'Stop after processing this many prompt runs',
        createPositiveIntegerOptionParser('--limit'),
    );
    command.option(
        '--wait-after-prompt <duration>',
        spaceTrim(`
            Wait this long after each prompt has been implemented, verified and committed before starting the next prompt.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no wait).
        `),
    );
    command.option(
        '--wait-between-prompts <duration>',
        spaceTrim(`
            Pace prompts so that each next prompt starts at least this long after the previous one began.
            If the previous prompt already took longer than this, the next prompt starts immediately.
            Accepts durations like 1h, 30m, 5s. Defaults to 0 (no pacing).
        `),
    );
    command.option(
        '--wait-after-error <duration>',
        spaceTrim(`
            Wait this long before retrying a prompt after an error occurs (up to 3 retries before giving up).
            Accepts durations like 1h, 30m, 5s. Defaults to 10m.
        `),
    );
    // Note: --no-auto disables the default auto behaviour and waits for user confirmation before each prompt
    command.option(
        '--no-auto',
        'Wait for user confirmation before each prompt instead of running automatically through the queue',
    );
    command.option(
        '--auto-migrate',
        'Run testing-server database migrations automatically after each successfully processed prompt',
    );
    command.option(
        '--allow-destructive-auto-migrate',
        'Allow auto-migrate even when heuristic SQL safety check flags destructive pending migrations',
    );

    command.action(
        handleActionErrors(async (cliOptions) => {
            const {
                dryRun,
                agent,
                context,
                test,
                testBefore,
                preserveLogs,
                isolate: isIsolated,
                priority,
                minPriority: minimumPriority,
                maxPriority: maximumPriority,
                limit,
                waitAfterPrompt: waitAfterPromptValue,
                waitBetweenPrompts: waitBetweenPromptsValue,
                waitAfterError: waitAfterErrorValue,
                auto,
                autoMigrate,
                allowDestructiveAutoMigrate,
            } = cliOptions as {
                readonly dryRun: boolean;
                readonly agent?: string;
                readonly context?: string;
                readonly test?: string | string[];
                readonly testBefore: TestBeforeMode;
                readonly preserveLogs: boolean;
                readonly isolate: boolean;
                readonly priority?: number;
                readonly minPriority?: number;
                readonly maxPriority?: number;
                readonly limit?: number;
                readonly waitAfterPrompt?: string;
                readonly waitBetweenPrompts?: string;
                readonly waitAfterError?: string;
                readonly auto: boolean;
                readonly autoMigrate: boolean;
                readonly allowDestructiveAutoMigrate: boolean;
            } & PromptRunnerCliOptions & CoderAgentCliOptions;

            const configuredTestCommand = normalizeCommandOptionValue(test);
            const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
            const runnerOptions = normalizePromptRunnerCliOptions(cliOptions as PromptRunnerCliOptions, {
                isAgentRequired: !dryRun,
            });
            const questionsOptions = normalizeQuestionsCliOptions(cliOptions as QuestionsCliOptions);

            // [1] Parse the wait options and --no-auto:
            //   default: run automatically through the queue (no waiting between prompts)
            //   --no-auto: wait for user confirmation before each prompt (interactive mode)
            //   --wait-after-prompt: pause after a successful round before starting the next prompt
            //   --wait-between-prompts: pace from start of one prompt to start of next
            //   --wait-after-error: wait before retrying after an error (default 10m)
            const waitForUser = !auto;

            assertUserConfirmationIsAllowed({ ...questionsOptions, isWaitingForUser: waitForUser });

            // Note: Checked before anything is installed or written, because installing a harness, updating the
            //       Promptbook CLI and coding itself all need the disk space which is missing
            await $assertSufficientFreeDiskSpace(process.cwd());

            if (await $ensurePromptbookCliInstallations(questionsOptions)) {
                return process.exit(0);
            }

            await $ensureHarnessInstallations([runnerOptions.agentName], questionsOptions);
            await $ensureCoderHarnessGitignoreRules(process.cwd(), runnerOptions.agentName, questionsOptions);

            const waitAfterPrompt = parseOptionalWaitDuration(waitAfterPromptValue, 0);
            const waitBetweenPrompts = parseOptionalWaitDuration(waitBetweenPromptsValue, 0);
            const waitAfterError = parseOptionalWaitDuration(waitAfterErrorValue, DEFAULT_WAIT_AFTER_ERROR_MS);

            // Convert commander options to RunOptions format
            const runOptions = {
                dryRun,
                waitForUser,
                waitAfterPrompt,
                waitBetweenPrompts,
                waitAfterError,
                noCommit: runnerOptions.noCommit,
                gitChanges: runnerOptions.gitChanges,
                agentName: runnerOptions.agentName,
                model: runnerOptions.model,
                agent,
                context,
                testCommand,
                testBefore,
                preserveLogs,
                isIsolated,
                noUi: runnerOptions.noUi,
                thinkingLevel: runnerOptions.thinkingLevel,
                priority: priority ?? 0,
                minimumPriority,
                maximumPriority,
                limit,
                normalizeLineEndings: runnerOptions.normalizeLineEndings,
                allowCredits: runnerOptions.allowCredits,
                autoMigrate,
                allowDestructiveAutoMigrate,
                autoPush: runnerOptions.autoPush,
                autoPull: runnerOptions.autoPull,
                isAskingQuestionsEnabled: questionsOptions.isAskingQuestionsEnabled,
            };

            // Note: Import the function dynamically to avoid loading heavy dependencies until needed
            const { runCodexPrompts } = await import('../../../../scripts/run-codex-prompts/main/runCodexPrompts');

            try {
                // Override process.argv to pass options to the legacy parseRunOptions if needed
                await runCodexPrompts(runOptions);
            } catch (error) {
                assertsError(error);
                printCoderRunFailure(error);
                return process.exit(1);
            }

            return process.exit(0);
        }),
    );
}

/**
 * Joins one Commander option that may be parsed either as a single string or a variadic token array.
 *
 * @private internal utility of `coder run` command
 */
function normalizeCommandOptionValue(value: string | string[] | undefined): string | undefined {
    if (value === undefined) {
        return undefined;
    }

    const parts = Array.isArray(value) ? value : [value];
    const normalizedValue = parts
        .map((part) => part.trim())
        .filter(Boolean)
        .join(' ')
        .trim();
    return normalizedValue === '' ? undefined : normalizedValue;
}

// Note: [🟡] Code for CLI command [run](src/cli/cli-commands/coder/run.ts) should never be published outside of `@promptbook/cli`
// Note: [💞] Ignore a discrepancy between file name and entity name
import colors from 'colors';
import moment from 'moment';
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import { DatabaseError } from '../../../src/errors/DatabaseError';
import { NotAllowed } from '../../../src/errors/NotAllowed';
import { just } from '../../../src/utils/organization/just';
import type { RunOptions } from '../cli/RunOptions';
import { parseRunOptions } from '../cli/parseRunOptions';
import type { CoderRunPauseCheckpointOptions, WaitForCoderRunPauseCheckpoint } from '../common/CoderRunPauseCheckpoint';
import { CliProgressDisplay } from '../common/cliProgressDisplay';
import { loadCachedAveragePromptDurationMs } from '../common/coderRunEstimateCache';
import { createFreeDiskSpaceGuard, type FreeDiskSpaceGuard } from '../common/createFreeDiskSpaceGuard';
import { resolveCoderAgent } from '../common/resolveCoderAgent';
import { sleepWithCountdown } from '../common/sleepWithCountdown';
import { resolveCoderContext } from '../common/resolveCoderContext';
import { listenForCoderRunControls } from '../common/listenForCoderRunControls';
import {
    announcePauseTargetLabel,
    checkPause,
    getEndAfterCurrentPromptState,
    resetCoderRunControls,
    resetPauseTargetLabel,
} from '../common/waitForPause';
import { waitForSkippableWorldTimeDeadline } from '../common/waitForSkippableWorldTimeDeadline';
import { printAgentGitIdentityTipIfNeeded } from '../git/agentGitIdentity';
import { captureCoderCommitScope, resolveCoderCommitScopePaths, type CoderCommitScope } from '../git/coderCommitScope';
import { commitChanges } from '../git/commitChanges';
import { ensureWorkingTreeClean } from '../git/ensureWorkingTreeClean';
import { pullLatestChanges } from '../git/pullLatestChanges';
import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
import { buildPromptLabelForDisplay } from '../prompts/buildPromptLabelForDisplay';
import { buildPromptSummary } from '../prompts/buildPromptSummary';
import { findNextTodoPrompt } from '../prompts/findNextTodoPrompt';
import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';
import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { printPromptsToBeWritten } from '../prompts/printPromptsToBeWritten';
import { printStats } from '../prompts/printStats';
import { normalizePriorityFilter, type PriorityFilter } from '../prompts/priorityFilter';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { resolveInterruptedPrompt } from '../prompts/resolveInterruptedPrompt';
import { summarizePrompts } from '../prompts/summarizePrompts';
import type { PromptFile } from '../prompts/types/PromptFile';
import type { PromptSelection } from '../prompts/types/PromptSelection';
import type { PromptStats } from '../prompts/types/PromptStats';
import { waitForPromptStart } from '../prompts/waitForPromptStart';
import type { PromptRunner } from '../runners/types/PromptRunner';
import { buildCoderRunAgentVisual } from '../ui/buildCoderRunAgentVisual';
import { renderCoderRunUi, type CoderRunUiHandle } from '../ui/renderCoderRunUi';
import { refreshCoderRunUiSubscriptionUsage } from '../ui/refreshCoderRunUiSubscriptionUsage';
import { createTestBeforeRepairPrompt } from '../testing/createTestBeforeRepairPrompt';
import { DEFAULT_CODER_TEST_COMMAND, isTestBeforeMode, type TestBeforeMode } from '../testing/TestBeforeMode';
import { limitTestOutput } from '../testing/limitTestOutput';
import { runTestBefore } from '../testing/runTestBefore';
import { resolvePromptRunner } from './resolvePromptRunner';
import { runPromptRound } from './runPromptRound';

/**
 * Constant for prompts dir.
 */
const PROMPTS_DIR = join(process.cwd(), 'prompts');

/**
 * Commit message for files changed by a successful or failed pre-coding test in repair mode.
 */
const PRE_CODING_TEST_CHANGES_COMMIT_MESSAGE = 'test: Apply changes made by pre-coding tests';

/**
 * Prompt queue snapshot for one top-level loop iteration.
 */
type PromptQueueSnapshot = {
    promptFiles: PromptFile[];
    stats: PromptStats;
    nextPrompt?: PromptSelection;
};

/**
 * Main entry point for running prompts with the selected agent.
 *
 * @param providedOptions - Optional pre-parsed options. If not provided, will parse from process.argv
 *
 * @public exported from `@promptbook/cli`
 */
export async function runCodexPrompts(providedOptions?: RunOptions): Promise<void> {
    const options = normalizeRunOptions(providedOptions ?? parseRunOptions(process.argv.slice(2)));
    validateRunCodexPromptOptions(options);
    resetCoderRunControls();

    const runStartDate = moment();
    const { isRichUiEnabled, progressDisplay, uiHandle } = createRunDisplays(options, runStartDate);
    const waitForRequestedPause = createPauseWaiter({
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        // Note: Every pause checkpoint of the whole run goes through this one waiter, so watching the free disk
        //       space here covers each round, each verification and each runner without repeating the check
        guardFreeDiskSpace: createFreeDiskSpaceGuard({
            inspectedPath: process.cwd(),
            isAskingQuestionsEnabled: options.isAskingQuestionsEnabled ?? true,
        }),
    });

    startPauseListenerIfNeeded(isRichUiEnabled);

    try {
        const resolvedCoderContext = await resolveCoderContext(options.context, process.cwd());
        const resolvedCoderAgent = await resolveCoderAgent(options.agent, process.cwd());
        const resolvedAgentSystemMessage = resolvedCoderAgent?.systemMessage;

        if (await runDryRunIfRequested(options)) {
            return;
        }

        const { runner, actualRunnerModel, runnerMetadata } = resolvePromptRunner(options);
        const promptRunnerIdentity: PromptRunnerIdentity = {
            harnessName: options.agentName,
            modelName: actualRunnerModel,
            agentReferences: resolvedCoderAgent?.agentReferences,
        };
        console.info(colors.green(`Running prompts with ${runner.name}`));

        initializeRunUi(uiHandle, runner.name, actualRunnerModel, options);
        await initializeRunUiAgentVisual(uiHandle, resolvedCoderAgent?.agentSource);
        await refreshCoderRunUiSubscriptionUsage({
            runner,
            uiState: uiHandle?.state,
        });
        await seedCachedAveragePromptDuration({
            options,
            actualRunnerModel,
            progressDisplay,
            uiHandle,
        });

        let hasShownUpcomingTasks = false;
        let hasWaitedForStart = false;
        let previousRoundStartTime: number | undefined;
        let previousRoundEndTime: number | undefined;
        let completedRunCount = 0;
        let hasRunTestBefore = false;
        // Note: Only the very first round resumes the interrupted prompt, every later round starts from a clean tree
        let isContinuingInterruptedPrompt = options.gitChanges === 'continue';

        while (just(true)) {
            if (options.autoPull && !options.dryRun) {
                await waitForRequestedPause({
                    checkpointLabel: 'pulling the latest repository changes',
                    phase: 'loading',
                    statusMessage: 'Pulling latest changes...',
                });
            }
            await pullLatestChangesIfEnabled({
                options,
                isRichUiEnabled,
            });

            if (!hasRunTestBefore && options.testBefore !== 'no') {
                await waitForRequestedPause({
                    checkpointLabel: 'loading prompts before running initial tests',
                    phase: 'loading',
                    statusMessage: 'Loading prompts before running initial tests...',
                });
                await loadPromptQueueSnapshot({
                    options,
                    isRichUiEnabled,
                    progressDisplay,
                    uiHandle,
                    promptRunnerIdentity,
                    isContinuingInterruptedPrompt,
                });
            }

            if (!hasRunTestBefore) {
                hasWaitedForStart = await runTestBeforeIfNeeded({
                    options,
                    runner,
                    runnerMetadata,
src/cli/cli-commands/coder\agentCliOptions.ts:11:    readonly agent?: string;
scripts/run-codex-prompts\cli\RunOptions.ts:11:export type RunOptions = {
scripts/run-codex-prompts\cli\RunOptions.ts:111:    agent?: string;
scripts/run-codex-prompts\cli\parseRunOptions.test.ts:177:            agent: 'agents/coding/developer.book',
src/cli/cli-commands/coder\list.ts:51:                readonly agent?: string;
scripts/run-codex-prompts\prompts\printStats.test.ts:17:        expect(String(consoleInfoSpy.mock.calls[0]![0])).toContain('Done: 85 | For agent: 5 | To be written: 16');
scripts/run-codex-prompts\prompts\printStats.test.ts:24:            'Done: 85 | For agent: 3 | Priority <4: 2 | To be written: 10',
scripts/run-codex-prompts\prompts\printStats.test.ts:35:            'Done: 85 | For agent: 3 | Priority outside 1-5: 2 | To be written: 10',
scripts/run-codex-prompts\prompts\printStats.ts:16:        colors.cyan(`Done: ${stats.done} | For agent: ${stats.forAgent}${priorityStats} | To be written: ${stats.toBeWritten}`),
src/cli/cli-commands/coder\run.test.ts:321:                agent: 'agents/coding/developer.book',
src/cli/cli-commands/coder\run.ts:181:                readonly agent?: string;
src/cli/cli-commands/coder\server.ts:156:                readonly agent?: string;
{
    "name": "promptbook",
    "version": "0.114.0-33",
    "description": "Promptbook: Create persistent AI agents that turn your company's scattered knowledge into action",
    "--note-0": "    <- [🐊]",
    "private": false,
    "sideEffects": false,
    "repository": {
        "type": "git",
        "url": "git+https://github.com/webgptorg/promptbook.git"
    },
    "author": "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
    "contributors": [
        "Pavol Hejný <pavol@ptbk.io> (https://www.pavolhejny.com/)",
        "Jiří Jahn <jiri@ptbk.io> (https://www.ptbk.io/)"
    ],
    "--note-1": "[❇️]",
    "keywords": [
        "ai",
        "llm",
        "prompt",
        "template",
        "language-model",
        "chatgpt",
        "autogpt",
        "machine-learning",
        "natural-language-processing",
        "nlp",
        "openai",
        "o3",
        "o3-mini",
        "deepseek",
        "gpt-3",
        "gpt-4",
        "gpt-4o",
        "gpt-4o-mini",
        "o1",
        "o1-mini",
        "o1-preview",
        "anthropic",
        "claude",
        "claude-3",
        "claude-3-opus",
        "claude-3-sonnet",
        "claude-3-haiku",
        "gemini",
        "gemini-pro",
        "gemini-flash",
        "mixtral",
        "mistral",
        "ollama",
        "ai-orchestration",
        "prompt-engineering",
        "llmops",
        "multimodal",
        "reasoning",
        "rag",
        "embeddings",
        "function-calling",
        "large-language-models",
        "ai-application-framework",
        "text-generation",
        "ai-agents",
        "LLMOps",
        "book-language",
        "markdown-dsl",
        "ai-workflow",
        "ai-automation",
        "pipeline",
        "workflow",
        "orchestration",
        "ai-pipeline",
        "prompt-template",
        "prompt-chaining",
        "ai-scripting",
        "conversational-ai",
        "chatbot",
        "ai-assistant",
        "knowledge-base",
        "document-processing",
        "web-scraping",
        "pdf-processing",
        "csv-processing",
        "json-processing",
        "xml-processing",
        "markdown-processing",
        "wikipedia-scraping",
        "website-crawler",
        "azure-openai",
        "vercel-ai",
        "docker",
        "cli-tool",
        "typescript",
        "javascript",
        "nodejs",
        "browser",
        "cross-platform",
        "api-integration",
        "remote-execution",
        "caching",
        "rate-limiting",
        "error-handling",
        "validation",
        "testing",
        "mocking",
        "personas",
        "expectations",
        "postprocessing",
        "preprocessing",
        "templating",
        "parameterization",
        "configuration",
        "logging",
        "monitoring",
        "debugging",
        "vscode-extension",
        "syntax-highlighting",
        "intellisense",
        "developer-tools",
        "ai-development",
        "prompt-management",
        "model-agnostic",
        "multi-model",
        "ai-sdk",
        "ai-framework",
        "ai-platform",
        "generative-ai",
        "content-generation",
        "text-processing",
        "natural-language",
        "human-readable",
        "plain-english",
        "no-code",
        "low-code",
        "automation-framework",
        "workflow-engine",
        "task-automation",
        "ai-ops",
        "mlops",
        "devops",
        "ci-cd",
        "version-control",
        "git-friendly",
        "collaboration",
        "team-development",
        "enterprise",
        "scalable",
        "production-ready",
        "open-source",
        "community",
        "extensible",
        "plugin-system",
        "integration",
        "api-wrapper",
        "sdk-wrapper",
        "abstraction-layer",
        "unified-interface",
        "cross-provider",
        "vendor-agnostic",
        "future-proof",
        "paradigm-shift",
        "next-generation",
        "revolutionary",
        "innovative",
        "cutting-edge",
        "state-of-the-art"
    ],
    "license": "BUSL-1.1",
    "bugs": {
        "url": "https://github.com/webgptorg/promptbook/issues"
    },
    "homepage": "https://ptbk.io/",
    "funding": [
        {
            "type": "individual",
            "url": "https://buymeacoffee.com/hejny"
        },
        {
            "type": "github",
            "url": "https://github.com/webgptorg/promptbook/blob/main/README.md#%EF%B8%8F-contributing"

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "runDryRunIfRequested|promptRunnerIdentity|loadPromptQueueSnapshot" scripts/run-codex-prompts/main/runCodexPrompts.ts; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 700 -First 230; Get-Content scripts/run-codex-prompts/cli/RunOptions.ts -TotalCount 150' in C:\Users\me\work\ai\promptbook
 succeeded in 530ms:
113:        if (await runDryRunIfRequested(options)) {
118:        const promptRunnerIdentity: PromptRunnerIdentity = {
166:                await loadPromptQueueSnapshot({
171:                    promptRunnerIdentity,
198:            const promptQueueSnapshot = await loadPromptQueueSnapshot({
203:                promptRunnerIdentity,
213:                promptRunnerIdentity,
720:async function runDryRunIfRequested(options: RunOptions): Promise<boolean> {
779:async function loadPromptQueueSnapshot(options: {
784:    promptRunnerIdentity: PromptRunnerIdentity;
792:        promptRunnerIdentity,
814:            : findNextTodoPrompt(promptFiles, runOptions.priorityFilter, promptRunnerIdentity),
827:    promptRunnerIdentity: PromptRunnerIdentity;
829:    const { hasShownUpcomingTasks, promptFiles, stats, priorityFilter, isRichUiEnabled, promptRunnerIdentity } =
842:    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter, promptRunnerIdentity));
            },
        });

        resetPauseTargetLabel();
    };
}

/**
 * Starts the pause listener only when the rich TTY UI is not consuming keyboard input.
 */
function startPauseListenerIfNeeded(isRichUiEnabled: boolean): void {
    if (!isRichUiEnabled) {
        listenForCoderRunControls();
    }
}

/**
 * Runs the dry-run reporting mode and returns whether the main execution should stop.
 */
async function runDryRunIfRequested(options: RunOptions): Promise<boolean> {
    if (!options.dryRun) {
        return false;
    }

    const promptFiles = await loadPromptFiles(PROMPTS_DIR);
    const stats = summarizePrompts(promptFiles, options.priorityFilter);
    printStats(stats, options.priorityFilter);
    console.info(colors.yellow('Following prompts need to be written:'));
    printPromptsToBeWritten(promptFiles, options.priorityFilter);
    return true;
}

/**
 * Seeds the rich UI with the selected runner configuration.
 */
function initializeRunUi(
    uiHandle: CoderRunUiHandle | undefined,
    runnerName: string,
    actualRunnerModel: string | undefined,
    options: RunOptions,
): void {
    uiHandle?.state.setConfig({
        agentName: runnerName,
        modelName: actualRunnerModel,
        thinkingLevel: options.thinkingLevel,
        context: options.context,
        serverUrl: options.serverUrl,
        priorityFilter: options.priorityFilter,
        limit: options.limit,
        testCommand: options.testCommand,
    });
    uiHandle?.state.setPhase('loading');
    uiHandle?.state.setStatusMessage(`Running prompts with ${runnerName}`);
}

/**
 * Prepares the `--agent` book avatar ASCII-art renderer and shows it above the dashboard boxes.
 *
 * Leaves the header empty when no agent is selected, the UI is disabled, or the visual cannot be rendered.
 */
async function initializeRunUiAgentVisual(
    uiHandle: CoderRunUiHandle | undefined,
    agentSource: string_book | undefined,
): Promise<void> {
    if (!uiHandle || !agentSource) {
        return;
    }

    const agentVisual = await buildCoderRunAgentVisual(agentSource);

    if (agentVisual) {
        uiHandle.state.setAgentVisual(agentVisual);
    }
}

/**
 * Loads prompt files, updates progress displays, and selects the next runnable prompt.
 */
async function loadPromptQueueSnapshot(options: {
    options: RunOptions;
    isRichUiEnabled: boolean;
    progressDisplay?: CliProgressDisplay;
    uiHandle?: CoderRunUiHandle;
    promptRunnerIdentity: PromptRunnerIdentity;
    isContinuingInterruptedPrompt: boolean;
}): Promise<PromptQueueSnapshot> {
    const {
        options: runOptions,
        isRichUiEnabled,
        progressDisplay,
        uiHandle,
        promptRunnerIdentity,
        isContinuingInterruptedPrompt,
    } = options;
    uiHandle?.state.setCurrentScriptPath(undefined);

    const promptFiles = await loadPromptFiles(PROMPTS_DIR);
    const stats = summarizePrompts(promptFiles, runOptions.priorityFilter);

    progressDisplay?.update(stats);
    uiHandle?.state.updateProgress(stats);

    if (!isRichUiEnabled) {
        printStats(stats, runOptions.priorityFilter);
    }

    return {
        promptFiles,
        stats,
        // Note: A resumed prompt is picked by its `[^]` status alone, the priority and runner filters select
        //       which prompt is started next, not which unfinished work is continued
        nextPrompt: isContinuingInterruptedPrompt
            ? resolveInterruptedPrompt(promptFiles)
            : findNextTodoPrompt(promptFiles, runOptions.priorityFilter, promptRunnerIdentity),
    };
}

/**
 * Prints upcoming tasks only on the first loop iteration in plain-console mode.
 */
function showUpcomingTasksOnce(options: {
    hasShownUpcomingTasks: boolean;
    promptFiles: PromptFile[];
    stats: PromptStats;
    priorityFilter?: PriorityFilter;
    isRichUiEnabled: boolean;
    promptRunnerIdentity: PromptRunnerIdentity;
}): boolean {
    const { hasShownUpcomingTasks, promptFiles, stats, priorityFilter, isRichUiEnabled, promptRunnerIdentity } =
        options;

    if (hasShownUpcomingTasks || isRichUiEnabled) {
        return true;
    }

    if (stats.toBeWritten > 0) {
        console.info(colors.yellow('Following prompts need to be written:'));
        printPromptsToBeWritten(promptFiles, priorityFilter);
        console.info('');
    }

    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter, promptRunnerIdentity));
    return true;
}

/**
 * Prints the terminal status when there is no runnable prompt left and tells the caller to stop.
 */
function finishWhenNoPromptIsAvailable(
    promptQueueSnapshot: PromptQueueSnapshot,
    isRichUiEnabled: boolean,
    uiHandle?: CoderRunUiHandle,
): boolean {
    if (promptQueueSnapshot.nextPrompt) {
        return false;
    }

    if (promptQueueSnapshot.stats.forAgent > 0) {
        announceRunCompletion(
            'No prompts match the selected harness or model.',
            colors.yellow,
            isRichUiEnabled,
            uiHandle,
        );
    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
        announceRunCompletion('No prompts ready for agent.', colors.yellow, isRichUiEnabled, uiHandle);
    } else {
        announceRunCompletion('All prompts are done.', colors.green, isRichUiEnabled, uiHandle);
    }

    return true;
}

/**
 * Checks whether the configured successful prompt-run limit has been reached.
 */
function isRunLimitReached(options: { completedRunCount: number; limit?: number }): boolean {
    const { completedRunCount, limit } = options;

    return limit !== undefined && completedRunCount >= limit;
}

/**
 * Checks whether the user-requested dynamic end control should stop the loop now.
 */
function isEndAfterCurrentPromptRequested(completedRunCount: number): boolean {
    return completedRunCount > 0 && getEndAfterCurrentPromptState();
}

/**
 * Updates UI and console output when a user-configured run limit stops the loop.
 */
function finishWhenRunLimitIsReached(options: {
    completedRunCount: number;
    isRichUiEnabled: boolean;
    uiHandle?: CoderRunUiHandle;
}): void {
    const { completedRunCount, isRichUiEnabled, uiHandle } = options;
    const runCountLabel = completedRunCount === 1 ? '1 prompt run' : `${completedRunCount} prompt runs`;

    announceRunCompletion(`Run limit reached after ${runCountLabel}.`, colors.green, isRichUiEnabled, uiHandle);
}

/**
 * Updates UI and console output when the dynamic `X` control stops the loop after a prompt.
 */
function finishWhenEndAfterCurrentPromptIsRequested(options: {
    completedRunCount: number;
    isRichUiEnabled: boolean;
    uiHandle?: CoderRunUiHandle;
}): void {
    const { completedRunCount, isRichUiEnabled, uiHandle } = options;
    const runCountLabel = completedRunCount === 1 ? '1 prompt run' : `${completedRunCount} prompt runs`;

    announceRunCompletion(`End requested after ${runCountLabel}.`, colors.green, isRichUiEnabled, uiHandle);
}

/**
 * Updates the UI status message while waiting for new prompts in keepAlive server mode.
 */
function announceKeepAliveStatus(
    promptQueueSnapshot: PromptQueueSnapshot,
    isRichUiEnabled: boolean,
    uiHandle?: CoderRunUiHandle,
): void {
    let message: string;

    if (promptQueueSnapshot.stats.forAgent > 0) {
        message = 'No prompts match the selected harness or model. Watching for changes...';
    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
import type { GitChangesMode } from '../../../src/cli/cli-commands/coder/GitChangesMode';
import type { ThinkingLevel } from '../../../src/cli/cli-commands/coder/ThinkingLevel';
import type { PromptRunnerHarnessName } from '../../../src/cli/cli-commands/common/promptRunnerCliOptions';
import type { PriorityFilter } from '../prompts/priorityFilter';
import type { TestBeforeMode } from '../testing/TestBeforeMode';
import type { CoderRunUiState } from '../ui/CoderRunUiState';

/**
 * CLI options for running the prompt runner.
 */
export type RunOptions = {
    /**
     * When true, do not execute prompts and only print prompts that still need to be written.
     */
    dryRun: boolean;
    /**
     * Additional runner instructions provided either inline or as a file path.
     */
    context?: string;
    /**
     * Optional verification command executed after each prompt attempt.
     */
    testCommand?: string;
    /**
     * Controls whether the test command runs before the first coding prompt.
     */
    testBefore?: TestBeforeMode;
    /**
     * Keeps generated prompt/debug artifacts after a successful round instead of cleaning them up.
     */
    preserveLogs: boolean;
    /**
     * Disables the rich terminal UI so runner output streams directly to the console.
     */
    noUi: boolean;
    /**
     * Optional reasoning effort override for runners that support configurable thinking levels.
     */
    thinkingLevel?: ThinkingLevel;
    waitForUser: boolean;
    /**
     * Milliseconds to wait after one prompt has been implemented, verified and committed before the next prompt starts.
     * Zero means no waiting after a prompt.
     */
    waitAfterPrompt: number;
    /**
     * Milliseconds to wait between the start of one prompt and the start of the next prompt, regardless of how long the task itself takes.
     * If the task takes longer than this value, the next prompt starts immediately. Zero means no per-cycle pacing.
     */
    waitBetweenPrompts: number;
    /**
     * Milliseconds to wait before retrying a prompt after an error occurs.
     * Zero means retry immediately.
     */
    waitAfterError: number;
    /**
     * Leave successful round changes in the git working tree instead of creating an agent commit.
     */
    noCommit: boolean;
    /**
     * Decides what happens when the working tree has uncommitted changes before a prompt starts.
     *
     * `continue` resumes the single prompt which was left in the middle of its implementation
     * and only applies to that first round, every later round expects a clean working tree again.
     */
    gitChanges: GitChangesMode;
    /**
     * Automatically normalize CRLF line endings to LF in files changed during each coding round.
     */
    normalizeLineEndings: boolean;
    /**
     * Allows OpenAI Codex runner to spend credits when needed.
     */
    allowCredits: boolean;
    /**
     * When true, log harness-level messages in addition to the final answer.
     * When false or omitted, only the final answer is printed.
     */
    isVerbose?: boolean;
    /**
     * Requests machine-readable harness events for a consumer that renders safe live progress.
     */
    isMachineReadableProgressEnabled?: boolean;
    /**
     * Enables automatic migration of testing servers after each successfully completed prompt.
     */
    autoMigrate: boolean;
    /**
     * Allows auto-migration workflow to continue even when heuristic SQL safety checks detect destructive statements.
     */
    allowDestructiveAutoMigrate: boolean;
    /**
     * When true, push each successful coding-agent commit to the configured remote.
     */
    autoPush: boolean;
    /**
     * When true, pull the latest repository changes before processing prompts.
     */
    autoPull: boolean;
    /**
     * When true, implement every prompt in its own temporary git worktree and merge the verified result
     * back into the branch the coder runs on.
     */
    isIsolated?: boolean;
    agentName?: PromptRunnerHarnessName;
    model?: string;
    /**
     * Optional path to an agent `.book` file whose compiled system message is prepended to each codex prompt.
     * Supports the same path resolution as `--context`: relative to the current working directory.
     */
    agent?: string;
    /**
     * Legacy alias for `minimumPriority`.
     */
    priority: number;
    /**
     * Optional minimum prompt priority required for processing.
     */
    minimumPriority?: number;
    /**
     * Optional maximum prompt priority allowed for processing.
     */
    maximumPriority?: number;
    /**
     * Normalized prompt priority filter.
     */
    priorityFilter?: PriorityFilter;
    /**
     * Optional maximum number of successful prompt runs to process before stopping.
     */
    limit?: number;
    /**
     * Optional HTTP server URL shown in the rich terminal UI for `ptbk coder server`.
     */
    serverUrl?: string;
    /**
     * Optional shared run-state object used by `ptbk coder server` to expose terminal progress over HTTP.
     */
    uiState?: CoderRunUiState;
    /**
     * When true, the run loop continues watching for new prompts instead of exiting when none are available.
     * Used by `ptbk coder server` to keep the process alive as a server.
     */
    keepAlive?: boolean;
    /**
     * Whether the run may stop and wait for an answer it does not strictly need, disabled by `--no-questions`.
     *
     * A run which may not ask keeps going through a critically full disk instead of pausing at the next
     * checkpoint, because there is nobody who could free the disk space and resume it.
     *

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Get-Content src/book-2.0/agent-source/parseAgentSource.ts; Get-Content agents/coding/developer.book -TotalCount 35; Get-Content agents/coding/unslopper.book -TotalCount 35; rg -n \"No prompts match the selected harness or model|harness and model|harness/model|harness or model\" scripts/run-codex-prompts src/cli/cli-commands/coder apps/coder-landing -g '*.{ts,tsx,md}'" in C:\Users\me\work\ai\promptbook
 succeeded in 639ms:
import type { AgentBasicInformation } from './AgentBasicInformation';
import { computeAgentHash } from './computeAgentHash';
import { createDefaultAgentName } from './createDefaultAgentName';
import { normalizeAgentName } from './normalizeAgentName';
import { parseAgentSourceWithCommitments } from './parseAgentSourceWithCommitments';
import { parseParameters } from './parseParameters';
import { ensureMetaFullname } from './parseAgentSource/ensureMetaFullname';
import { extractAgentProfileText } from './parseAgentSource/extractAgentProfileText';
import { extractInitialMessage } from './parseAgentSource/extractInitialMessage';
import { extractParsedAgentProfile } from './parseAgentSource/extractParsedAgentProfile';
import type { string_book } from './string_book';

/**
 * Parses basic information from agent source
 *
 * There are 2 similar functions:
 * - `parseAgentSource` which is a lightweight parser for agent source, it parses basic information and its purpose is to be quick and synchronous. The commitments there are hardcoded.
 * - `createAgentModelRequirements` which is an asynchronous function that creates model requirements it applies each commitment one by one and works asynchronously.
 *
 * @public exported from `@promptbook/core`
 */
export function parseAgentSource(agentSource: string_book): AgentBasicInformation {
    const parseResult = parseAgentSourceWithCommitments(agentSource);
    const resolvedAgentName = parseResult.agentName || createDefaultAgentName(agentSource);
    const personaDescription = extractAgentProfileText(parseResult.commitments);
    const initialMessage = extractInitialMessage(parseResult.commitments);
    const parsedProfile = extractParsedAgentProfile(parseResult.commitments);

    ensureMetaFullname(parsedProfile.meta, resolvedAgentName);

    return {
        agentName: normalizeAgentName(resolvedAgentName),
        agentHash: computeAgentHash(agentSource),
        permanentId: parsedProfile.meta.id,
        personaDescription,
        initialMessage,
        thinkingMessages: parsedProfile.thinkingMessages,
        meta: parsedProfile.meta,
        links: parsedProfile.links,
        parameters: parseParameters(agentSource),
        capabilities: parsedProfile.capabilities,
        samples: parsedProfile.samples,
        knowledgeSources: parsedProfile.knowledgeSources,
    };
}

// TODO: [🕛] Unite `AgentBasicInformation`, `ChatParticipant`, `LlmExecutionTools` +  `LlmToolsMetadata`
Promptbook Developer

META VISUAL ascii-octopus

RULE
Keep in mind the DRY _(don't repeat yourself)_ principle.

RULE
Keep in mind the SOLID principles.

RULE
Do a proper analysis of the current functionality before you start implementing.

RULE
Keep small responsibilities of functions and classes, avoid creating big functions or classes that do many things.

RULE
When throwing errors, throw [branded errors](src/errors) and use `spaceTrim` utility to write clear and well-formatted multiline detailed error messages.
Format errors as markdown, for example `variables` should be in backticks and important notes can be in bold.

RULE
Constants should always be `UPPER_SNAKE_CASE`.

RULE
Boolean variables should always be prefixed with `is`, for example `isUserChatJobLeaseExpired` or `IS_DEBUG_MODE`.

RULE
Do not use abbreviations, for example use `isExpired` instead of `isExp`, `translateMessage` instead of `t`, etc.
It is fine to use well-known abbreviations, for example `id`, `url`, `html`, etc.

RULE
When writing multiline strings, use `spaceTrim` utility.

RULE
Do only the change described in the prompt. Do not add any additional features or make any additional changes that are not described there.
Unslopper

GOAL Edit text so it sounds human, direct, and alive. Remove AI patterns, cut filler, and keep the original meaning and tone.

RULE You are un-slopping text, not for summarizing it.
RULE Work only on text editing and rewriting. Do not add new facts unless the user asks for them.


WRITING RULE Preserve meaning, intent, and audience. If the text is unclear, ask a short clarifying question instead of guessing.
WRITING RULE Prefer plain language. Use short words, concrete nouns, and active voice.
WRITING RULE Remove puffery, hype, vague claims, and empty polish. Say what happened.
WRITING RULE Avoid generic AI phrasing like "Additionally", "crucial", "delve", "underscore", "showcase", and similar filler words unless the user clearly wants that style.
WRITING RULE Avoid "not just X, but Y" framing, forced three-part lists, and synonym cycling.
WRITING RULE Avoid em dashes, curly quotes, decorative emojis, and title case headings in rewritten text unless the source text requires them.
WRITING RULE Keep punctuation simple. Use periods and commas first. Use colons only when they earn their place.
WRITING RULE If the text needs more soul, add a human voice. That can mean a clear opinion, a sharper turn of phrase, or a small amount of mess.
WRITING RULE Do not make the writing sterile. Vary sentence length and rhythm.
WRITING RULE Do not sound sycophantic, promotional, or like a chatbot.
WRITING RULE When revising, scan for obvious AI tells first, then rewrite, then self-audit the result.
WRITING RULE If the user gives a style target, match it. If not, aim for natural, sharp, and readable.
WRITING RULE Keep the output focused on the rewritten text unless the user asks for notes or an explanation. 
WRITING RULE When a sentence feels generic enough to fit any project, rewrite it or cut it.
WRITING RULE Use straight quotes only.
WRITING RULE Prefer one idea per sentence when possible.
WRITING RULE Use first person when it sounds natural and helps the voice.
WRITING RULE "The update improves the workflow" becomes "The update cuts three steps from the workflow."
WRITING RULE "This is a pivotal moment" becomes "This changes how the team works."


CLOSED
src/cli/cli-commands/coder\run.test.ts:297:    it('passes an agent selection alongside the harness and model', async () => {
src/cli/cli-commands/coder\ping.ts:36:                Send one tiny dummy prompt to a harness and model to measure and warm them up
scripts/run-codex-prompts\cli\parseRunOptions.test.ts:164:    it('parses an agent selection alongside the harness and model', () => {
scripts/run-codex-prompts\common\coderRunEstimateCache.test.ts:65:    it('stores separate caches for distinct harness/model/thinking-level combinations', async () => {
scripts/run-codex-prompts\main\runCodexPrompts.ts:860:            'No prompts match the selected harness or model.',
scripts/run-codex-prompts\main\runCodexPrompts.ts:929:        message = 'No prompts match the selected harness or model. Watching for changes...';
scripts/run-codex-prompts\main\listCoderPrompts.ts:15:     * Optional harness and model selection used to omit prompts routed to other runners.
scripts/run-codex-prompts\main\listCoderPrompts.test.ts:81:    it('does not filter runner-specific prompts when no harness or model is selected', async () => {
scripts/run-codex-prompts\ping\pingCoderHarnessPeriodically.ts:31: * Pings the selected harness and model once per period until the process is stopped.
scripts/run-codex-prompts\ping\pingCoderHarness.ts:36: * Sends one tiny dummy prompt through the selected harness and model and measures the round trip.
scripts/run-codex-prompts\ping\CoderPingResult.ts:6: * Outcome of one `ptbk coder ping` round trip through a harness and model.
scripts/run-codex-prompts\ping\buildCoderPingPrompt.ts:23: * Answer a working harness and model returns for the dummy work of `ptbk coder ping`.
scripts/run-codex-prompts\prompts\promptRunnerFiltering.test.ts:83:    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
scripts/run-codex-prompts\prompts\promptRunnerAttribution.ts:107: * Checks whether two report signatures describe the same harness/model/thinking configuration, allowing the
apps/coder-landing\specs\content\comparison-matrix.md:64:4. **The same agent on another vendor** — "The queue and the .book behavior move to a different harness or model without a rewrite."
apps/coder-landing\specs\content\commands.md:44:-   `ptbk coder ping` — send one tiny dummy prompt to the harness and model selected with `--harness`, `--model` and `--thinking-level` and report the answer, the response time and the reported usage. It runs no coding task or commit; when the selected harness has missing local ignore rules, it asks before adding them to `.gitignore`. `--allow-credits` and `--no-ui` behave as for `run`, where `--no-ui` streams the raw harness output instead of the compact result.
apps/coder-landing\src\data\commands.ts:49: * Command which runs prompts through a selected harness and model.
apps/coder-landing\src\data\commands.ts:75: * Command which checks the connection, response time and quota of one harness and model.
apps/coder-landing\src\data\commands.ts:80: * Command which keeps pinging one harness and model every 5 hours to keep its quota window refreshing.
apps/coder-landing\src\data\advancedFeatures.ts:114:            'ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore.',
apps/coder-landing\src\data\comparison.ts:230:        description: 'The queue and the .book behavior move to a different harness or model without a rewrite.',
apps/coder-landing\specs\sections\advanced-features.md:52:13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content scripts/run-codex-prompts/main/listCoderPrompts.ts; Get-Content scripts/run-codex-prompts/main/listCoderPrompts.test.ts; Get-Content scripts/run-codex-prompts/main/runCodexPrompts.ts | Select-Object -Skip 840 -First 110; Get-Content src/book-2.0/agent-source/normalizeAgentName.ts; Get-Content src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts -TotalCount 120' in C:\Users\me\work\ai\promptbook
 succeeded in 417ms:
import { join } from 'path';
import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { normalizePriorityFilter, type PriorityFilterInput } from '../prompts/priorityFilter';
import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';

/**
 * Options which select the ready prompts listed by `ptbk coder list`.
 *
 * @public exported from `@promptbook/cli`
 */
export type ListCoderPromptsOptions = PriorityFilterInput & {
    /**
     * Optional harness and model selection used to omit prompts routed to other runners.
     */
    readonly promptRunnerIdentity?: PromptRunnerIdentity;
};

/**
 * Directory containing the prompt queue of the current project.
 *
 * @private internal constant of `listCoderPrompts`
 */
const PROMPTS_DIRECTORY_PATH = join(process.cwd(), 'prompts');

/**
 * Lists ready, fully authored coding prompts in descending priority groups without starting a coding harness.
 *
 * @returns The number of prompts printed.
 * @public exported from `@promptbook/cli`
 */
export async function listCoderPrompts(options: ListCoderPromptsOptions = {}): Promise<number> {
    const priorityFilter = normalizePriorityFilter(options);
    const promptFiles = await loadPromptFiles(PROMPTS_DIRECTORY_PATH);
    const upcomingTasks = listUpcomingTasks(promptFiles, priorityFilter, options.promptRunnerIdentity);

    printUpcomingTasks(upcomingTasks);
    return upcomingTasks.length;
}
import { join } from 'path';
import { spaceTrim } from 'spacetrim';
import { loadPromptFiles } from '../prompts/loadPromptFiles';
import { parsePromptFile } from '../prompts/parsePromptFile';
import { printUpcomingTasks } from '../prompts/printUpcomingTasks';
import { listCoderPrompts } from './listCoderPrompts';

jest.mock('../prompts/loadPromptFiles', () => ({
    loadPromptFiles: jest.fn(),
}));

jest.mock('../prompts/printUpcomingTasks', () => ({
    printUpcomingTasks: jest.fn(),
}));

/**
 * Typed Jest mock for loading the current project's prompt files.
 */
function getLoadPromptFilesMock(): jest.MockedFunction<typeof loadPromptFiles> {
    return loadPromptFiles as jest.MockedFunction<typeof loadPromptFiles>;
}

/**
 * Typed Jest mock for the priority-grouped task renderer.
 */
function getPrintUpcomingTasksMock(): jest.MockedFunction<typeof printUpcomingTasks> {
    return printUpcomingTasks as jest.MockedFunction<typeof printUpcomingTasks>;
}

describe('listCoderPrompts', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    it('lists only ready prompts matching the selected runner and priority range', async () => {
        const promptFile = parsePromptFile(
            join(process.cwd(), 'prompts', 'list-test.md'),
            spaceTrim(`
                [ ] !!!! use \`github-copilot\`
                Compatible harness
                ---
                [ ] !!! use \`gpt-5.5\`
                Compatible model
                ---
                [ ] !!!!! use \`claude\`
                Different model family
                ---
                [ ] !!
                @@@ Still needs authoring
                ---
                [x] Done
                Finished prompt
            `),
        );
        getLoadPromptFilesMock().mockResolvedValue([promptFile]);

        const listedPromptCount = await listCoderPrompts({
            minimumPriority: 3,
            maximumPriority: 4,
            promptRunnerIdentity: {
                harnessName: 'github-copilot',
                modelName: 'gpt-5.5',
            },
        });

        expect(listedPromptCount).toBe(2);
        expect(getPrintUpcomingTasksMock()).toHaveBeenCalledWith([
            {
                label: 'prompts/list-test.md#1',
                summary: 'Compatible harness',
                priority: 4,
            },
            {
                label: 'prompts/list-test.md#4',
                summary: 'Compatible model',
                priority: 3,
            },
        ]);
    });

    it('does not filter runner-specific prompts when no harness or model is selected', async () => {
        const promptFile = parsePromptFile(
            join(process.cwd(), 'prompts', 'list-test.md'),
            spaceTrim(`
                [ ] use \`gpt\`
                GPT task
                ---
                [ ] use \`claude\`
                Claude task
            `),
        );
        getLoadPromptFilesMock().mockResolvedValue([promptFile]);

        const listedPromptCount = await listCoderPrompts();

        expect(listedPromptCount).toBe(2);
        expect(getPrintUpcomingTasksMock()).toHaveBeenCalledWith([
            {
                label: 'prompts/list-test.md#1',
                summary: 'GPT task',
                priority: 0,
            },
            {
                label: 'prompts/list-test.md#4',
                summary: 'Claude task',
                priority: 0,
            },
        ]);
    });
});

    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter, promptRunnerIdentity));
    return true;
}

/**
 * Prints the terminal status when there is no runnable prompt left and tells the caller to stop.
 */
function finishWhenNoPromptIsAvailable(
    promptQueueSnapshot: PromptQueueSnapshot,
    isRichUiEnabled: boolean,
    uiHandle?: CoderRunUiHandle,
): boolean {
    if (promptQueueSnapshot.nextPrompt) {
        return false;
    }

    if (promptQueueSnapshot.stats.forAgent > 0) {
        announceRunCompletion(
            'No prompts match the selected harness or model.',
            colors.yellow,
            isRichUiEnabled,
            uiHandle,
        );
    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
        announceRunCompletion('No prompts ready for agent.', colors.yellow, isRichUiEnabled, uiHandle);
    } else {
        announceRunCompletion('All prompts are done.', colors.green, isRichUiEnabled, uiHandle);
    }

    return true;
}

/**
 * Checks whether the configured successful prompt-run limit has been reached.
 */
function isRunLimitReached(options: { completedRunCount: number; limit?: number }): boolean {
    const { completedRunCount, limit } = options;

    return limit !== undefined && completedRunCount >= limit;
}

/**
 * Checks whether the user-requested dynamic end control should stop the loop now.
 */
function isEndAfterCurrentPromptRequested(completedRunCount: number): boolean {
    return completedRunCount > 0 && getEndAfterCurrentPromptState();
}

/**
 * Updates UI and console output when a user-configured run limit stops the loop.
 */
function finishWhenRunLimitIsReached(options: {
    completedRunCount: number;
    isRichUiEnabled: boolean;
    uiHandle?: CoderRunUiHandle;
}): void {
    const { completedRunCount, isRichUiEnabled, uiHandle } = options;
    const runCountLabel = completedRunCount === 1 ? '1 prompt run' : `${completedRunCount} prompt runs`;

    announceRunCompletion(`Run limit reached after ${runCountLabel}.`, colors.green, isRichUiEnabled, uiHandle);
}

/**
 * Updates UI and console output when the dynamic `X` control stops the loop after a prompt.
 */
function finishWhenEndAfterCurrentPromptIsRequested(options: {
    completedRunCount: number;
    isRichUiEnabled: boolean;
    uiHandle?: CoderRunUiHandle;
}): void {
    const { completedRunCount, isRichUiEnabled, uiHandle } = options;
    const runCountLabel = completedRunCount === 1 ? '1 prompt run' : `${completedRunCount} prompt runs`;

    announceRunCompletion(`End requested after ${runCountLabel}.`, colors.green, isRichUiEnabled, uiHandle);
}

/**
 * Updates the UI status message while waiting for new prompts in keepAlive server mode.
 */
function announceKeepAliveStatus(
    promptQueueSnapshot: PromptQueueSnapshot,
    isRichUiEnabled: boolean,
    uiHandle?: CoderRunUiHandle,
): void {
    let message: string;

    if (promptQueueSnapshot.stats.forAgent > 0) {
        message = 'No prompts match the selected harness or model. Watching for changes...';
    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
        message = 'No prompts ready for agent. Watching for changes...';
    } else {
        message = 'All prompts are done. Watching for changes...';
    }

    uiHandle?.state.setStatusMessage(message);
    uiHandle?.state.setPhase('waiting');

    if (!isRichUiEnabled) {
        console.info(colors.gray(message));
    }
}

/**
 * Updates UI state and plain-console output for the terminal completion message.
 */
function announceRunCompletion(
    message: string,
    colorize: (message: string) => string,
    isRichUiEnabled: boolean,
import { spaceTrim } from 'spacetrim';
import type { string_agent_name } from '../../types/string_agent_name';
import { titleToName } from '../../utils/normalization/titleToName';

/**
 * Normalizes agent name from arbitrary string to valid agent name
 *
 * Note: [🔂] This function is idempotent.
 *
 * @public exported from `@promptbook/core`
 */
export function normalizeAgentName(rawAgentName: string): string_agent_name {
    return titleToName(spaceTrim(rawAgentName));
}
import { spaceTrim } from 'spacetrim';
import type { BookCommitment } from '../../commitments/_base/BookCommitment';
import type { ParsedCommitment } from '../../commitments/_base/ParsedCommitment';
import { COMMITMENT_REGISTRY } from '../../commitments/index';
import type { AgentSourceParseResult } from './AgentSourceParseResult';
import { getBookCommitmentLineType } from './getBookCommitmentLineType';
import { parseAgentSourcePrelude } from './parseAgentSourcePrelude';
import type { ParsedUnknownCommitment } from './ParsedUnknownCommitment';
import type { string_book } from './string_book';

/**
 * Regex pattern to match horizontal lines (markdown thematic breaks)
 * Matches 3 or more hyphens, underscores, or asterisks (with optional spaces between)
 */
const HORIZONTAL_LINE_PATTERN = /^[\s]*[-_*][\s]*[-_*][\s]*[-_*][\s]*[-_*]*[\s]*$/;

/**
 * Registered commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentKnownCommitment = {
    readonly kind: 'known';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly contentLines: string[];
};

/**
 * Unknown commitment block currently being collected from Book source.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentUnknownCommitment = {
    readonly kind: 'unknown';
    readonly type: string;
    readonly startLineNumber: number;
    readonly originalStartLine: string;
    readonly sourceLines: string[];
};

/**
 * Commitment-like source block currently being collected.
 *
 * @private internal type of `parseAgentSourceWithCommitments`
 */
type CurrentCommitment = CurrentKnownCommitment | CurrentUnknownCommitment;

/**
 * Parses agent source using the new commitment system with multiline support
 * This function replaces the hardcoded commitment parsing in the original parseAgentSource
 *
 * The first non-empty line is always consumed as plain-text agent name. Commitment parsing
 * starts only after that title line has been fixed.
 *
 * @private internal utility of `parseAgentSource`
 */
export function parseAgentSourceWithCommitments(agentSource: string_book): Omit<AgentSourceParseResult, 'agentHash'> {
    if (!agentSource || !agentSource.trim()) {
        return {
            agentName: null,
            commitments: [],
            unknownCommitments: [],
            nonCommitmentLines: [],
        };
    }

    const { lines, agentName, agentNameLineIndex, agentNameLineNumber } = parseAgentSourcePrelude(agentSource);

    const commitments: ParsedCommitment[] = [];
    const unknownCommitments: ParsedUnknownCommitment[] = [];
    const nonCommitmentLines: string[] = agentNameLineIndex >= 0 ? [lines[agentNameLineIndex]!] : [];

    // Parse commitments with multiline support.
    let currentCommitment: CurrentCommitment | null = null;

    // Process lines starting from after the agent name line
    const startIndex = agentNameLineIndex >= 0 ? agentNameLineIndex + 1 : 0;
    let isInsideCodeBlock = false;
    for (let i = startIndex; i < lines.length; i++) {
        const line = lines[i];
        if (line === undefined) {
            continue;
        }

        const trimmedLine = line.trim();

        // Check if this line starts or ends a code block
        if (trimmedLine.startsWith('```')) {
            isInsideCodeBlock = !isInsideCodeBlock;

            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        if (isInsideCodeBlock) {
            appendLineToCurrentCommitmentOrNonCommitmentLines(currentCommitment, line, nonCommitmentLines);
            continue;
        }

        // Check if this line starts a new commitment
        let foundNewCommitment = false;
        for (const definition of COMMITMENT_REGISTRY) {
            const typeRegex = definition.createTypeRegex();
            const match = typeRegex.exec(line.trim());

            if (match && match.groups?.type) {
                appendCurrentCommitment(currentCommitment, commitments, unknownCommitments);

                // Extract the initial content from the commitment line
                const fullRegex = definition.createRegex();
                const fullMatch = fullRegex.exec(line.trim());
                const initialContent = fullMatch?.groups?.contents || '';

                // Start a new commitment
                currentCommitment = {
                    kind: 'known',
                    type: definition.type,
                    startLineNumber: i + 1,

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git show --stat --oneline 240272096; git show 240272096 -- scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts scripts/run-codex-prompts/main/runCodexPrompts.ts src/cli/cli-commands/coder/list.ts apps/coder-landing/src/data/advancedFeatures.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 591ms:
240272096 [✨🚭] Allow to specify required model for each prompt
 apps/coder-landing/specs/content/commands.md       |  2 +
 .../specs/sections/advanced-features.md            | 24 +++---
 apps/coder-landing/src/data/advancedFeatures.ts    | 14 +++-
 apps/coder-landing/src/data/commands.ts            |  5 ++
 changelog/_current-preversion.md                   |  2 +
 prompts/2026-07-0690-ptbk-coder-use-model.md       |  3 +-
 scripts/run-codex-prompts/main/runCodexPrompts.ts  | 40 +++++++--
 .../prompts/findNextTodoPrompt.ts                  |  9 +-
 .../prompts/isPromptCompatibleWithRunner.ts        | 66 +++++++++++++++
 .../prompts/listRunnablePrompts.ts                 | 10 ++-
 .../run-codex-prompts/prompts/listUpcomingTasks.ts |  9 +-
 .../prompts/markPromptAttempts.test.ts             | 33 ++++++++
 .../run-codex-prompts/prompts/markPromptDone.ts    |  7 +-
 .../run-codex-prompts/prompts/markPromptFailed.ts  | 10 +--
 .../run-codex-prompts/prompts/parsePromptFile.ts   | 30 ++++---
 .../prompts/promptRunnerFiltering.test.ts          | 97 ++++++++++++++++++++++
 .../prompts/replacePromptTodoStatusLine.ts         |  9 ++
 17 files changed, 322 insertions(+), 48 deletions(-)
commit 240272096038e03141502a7c8ee00a7b7f90dacd
Author: Promptbook Coding Agent <me+agent@pavolhejny.com>
Date:   Mon Aug 3 02:11:14 2026 +0200

    [✨🚭] Allow to specify required model for each prompt
    
    **For example when you run:**
    
    ```bash
    ptbk coder run --harness github-copilot --model gpt-5.5 --thinking-level xhigh --agent agents/coding/developer.book --context AGENTS.md
    ```
    
    **This prompt will run:**
    
    ```markdown
    [ ] use `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use model `gpt-5.5`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] !!!! `gpt`
    
    [✨𓀍] Do some work
    ```
    
    **This prompt will also run:**
    
    ```markdown
    [ ] use `github-copilot` !!!!!
    
    [✨𓀍] Do some work
    ```
    
    **But this will be ignored:**
    
    ```markdown
    [ ] use `claude-opus`
    
    [✨𓀍] Do some work
    ```
    
    -   In the `[ ]` section of the prompt to be done allowed to specify the required model or harness for this prompt
    -   Allow to combine the model / harness name in the `[ ]` section with the priority !!!!!
    -   you can specify either model or harness or just family of the models. For example:
        -   `gpt` - any model from the gpt family
        -   `github-copilot` - the harness
        -   `claude` - any model from the claude family
        -   `claude-opus` - any model from the claude-opus family
        -   `opus` - any model from the claude-opus family
        -   this shouldn't be hardcoded. The matching from the prompt and the harness or model name should be done dynamically via normalized names
        -   the normalized name of the model or harness is computed from the name of the model or harness by removing all non-alphanumeric characters and lowercasing it. For example:
            -   `gpt-5.5` -> `gpt-5-5`
            -   `claude-opus-4.8` -> `claude-opus-4-8`
            -   `github-copilot` -> `github-copilot`
            -   `claude-opus` -> `claude-opus`
            -   `claude` -> `claude`
            -   Use the normalizing functions which are here in the project, do not implement your own normalizing function
    -   the strategy should work as follows: it should firstly take all the tokens in these `parentheses` in the prompt line
        -   For example: "[ ] use `gpt-5.5`" -> it should take the token `gpt-5.5`
    -   Then it should normalize the name of the model or harness which is used to run this prompt. For example: `gpt-5.5` -> `gpt-5-5`
    -   Then it should normalize the name of the model and harness which is used to run the ptbk coder
    -   Then match all the tokens from the prompt with the normalized name of the model or harness which is used to run this prompt. If any of the tokens matches, then this prompt can be run. If none of the tokens matches, then this prompt should be skipped and not run.
    -   Keep in mind the DRY _(don't repeat yourself)_ principle.
    -   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
    -   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
    -   Update the [`ptbk coder` landing website](apps/coder-landing)
    -   Add the changes into the [changelog](changelog/_current-preversion.md)

diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c9e7b1e61..29b032481 100644
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,10 @@
-import { DRY_RUN_COMMAND, PING_COMMAND, TEST_BEFORE_FIX_COMMAND, VERIFY_COMMAND } from './commands';
+import {
+    DRY_RUN_COMMAND,
+    MODEL_FILTER_COMMAND,
+    PING_COMMAND,
+    TEST_BEFORE_FIX_COMMAND,
+    VERIFY_COMMAND,
+} from './commands';
 
 /**
  * One advanced feature of `ptbk coder` presented as a card with a terminal snippet.
@@ -67,6 +73,12 @@ export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
         description: 'Give prompts a priority and process only the range you want in the current run.',
         sampleCommand: 'ptbk coder run --harness claude-code --min-priority 1 --max-priority 5',
     },
+    {
+        title: 'Model-specific prompts',
+        description:
+            'Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it automatically.',
+        sampleCommand: MODEL_FILTER_COMMAND,
+    },
     {
         title: 'Pacing and retries',
         description:
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index fdcc1e317..ffc66ea7e 100644
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -29,6 +29,7 @@ import { runIsolatedPromptRound } from '../isolation/runIsolatedPromptRound';
 import { buildPromptLabelForDisplay } from '../prompts/buildPromptLabelForDisplay';
 import { buildPromptSummary } from '../prompts/buildPromptSummary';
 import { findNextTodoPrompt } from '../prompts/findNextTodoPrompt';
+import type { PromptRunnerIdentity } from '../prompts/isPromptCompatibleWithRunner';
 import { listUpcomingTasks } from '../prompts/listUpcomingTasks';
 import { loadPromptFiles } from '../prompts/loadPromptFiles';
 import { printPromptsToBeWritten } from '../prompts/printPromptsToBeWritten';
@@ -92,6 +93,10 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
         }
 
         const { runner, actualRunnerModel, runnerMetadata } = resolvePromptRunner(options);
+        const promptRunnerIdentity: PromptRunnerIdentity = {
+            harnessName: options.agentName,
+            modelName: actualRunnerModel,
+        };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
         initializeRunUi(uiHandle, runner.name, actualRunnerModel, options);
@@ -149,6 +154,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
                 isRichUiEnabled,
                 progressDisplay,
                 uiHandle,
+                promptRunnerIdentity,
             });
 
             hasShownUpcomingTasks ||= showUpcomingTasksOnce({
@@ -157,6 +163,7 @@ export async function runCodexPrompts(providedOptions?: RunOptions): Promise<voi
                 stats: promptQueueSnapshot.stats,
                 priorityFilter: options.priorityFilter,
                 isRichUiEnabled,
+                promptRunnerIdentity,
             });
 
             if (!promptQueueSnapshot.nextPrompt) {
@@ -627,8 +634,9 @@ async function loadPromptQueueSnapshot(options: {
     isRichUiEnabled: boolean;
     progressDisplay?: CliProgressDisplay;
     uiHandle?: CoderRunUiHandle;
+    promptRunnerIdentity: PromptRunnerIdentity;
 }): Promise<PromptQueueSnapshot> {
-    const { options: runOptions, isRichUiEnabled, progressDisplay, uiHandle } = options;
+    const { options: runOptions, isRichUiEnabled, progressDisplay, uiHandle, promptRunnerIdentity } = options;
     uiHandle?.state.setCurrentScriptPath(undefined);
 
     const promptFiles = await loadPromptFiles(PROMPTS_DIR);
@@ -644,7 +652,7 @@ async function loadPromptQueueSnapshot(options: {
     return {
         promptFiles,
         stats,
-        nextPrompt: findNextTodoPrompt(promptFiles, runOptions.priorityFilter),
+        nextPrompt: findNextTodoPrompt(promptFiles, runOptions.priorityFilter, promptRunnerIdentity),
     };
 }
 
@@ -657,8 +665,10 @@ function showUpcomingTasksOnce(options: {
     stats: PromptStats;
     priorityFilter?: PriorityFilter;
     isRichUiEnabled: boolean;
+    promptRunnerIdentity: PromptRunnerIdentity;
 }): boolean {
-    const { hasShownUpcomingTasks, promptFiles, stats, priorityFilter, isRichUiEnabled } = options;
+    const { hasShownUpcomingTasks, promptFiles, stats, priorityFilter, isRichUiEnabled, promptRunnerIdentity } =
+        options;
 
     if (hasShownUpcomingTasks || isRichUiEnabled) {
         return true;
@@ -670,7 +680,7 @@ function showUpcomingTasksOnce(options: {
         console.info('');
     }
 
-    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter));
+    printUpcomingTasks(listUpcomingTasks(promptFiles, priorityFilter, promptRunnerIdentity));
     return true;
 }
 
@@ -686,7 +696,14 @@ function finishWhenNoPromptIsAvailable(
         return false;
     }
 
-    if (promptQueueSnapshot.stats.toBeWritten > 0) {
+    if (promptQueueSnapshot.stats.forAgent > 0) {
+        announceRunCompletion(
+            'No prompts match the selected harness or model.',
+            colors.yellow,
+            isRichUiEnabled,
+            uiHandle,
+        );
+    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         announceRunCompletion('No prompts ready for agent.', colors.yellow, isRichUiEnabled, uiHandle);
     } else {
         announceRunCompletion('All prompts are done.', colors.green, isRichUiEnabled, uiHandle);
@@ -747,10 +764,15 @@ function announceKeepAliveStatus(
     isRichUiEnabled: boolean,
     uiHandle?: CoderRunUiHandle,
 ): void {
-    const message =
-        promptQueueSnapshot.stats.toBeWritten > 0
-            ? 'No prompts ready for agent. Watching for changes...'
-            : 'All prompts are done. Watching for changes...';
+    let message: string;
+
+    if (promptQueueSnapshot.stats.forAgent > 0) {
+        message = 'No prompts match the selected harness or model. Watching for changes...';
+    } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
+        message = 'No prompts ready for agent. Watching for changes...';
+    } else {
+        message = 'All prompts are done. Watching for changes...';
+    }
 
     uiHandle?.state.setStatusMessage(message);
     uiHandle?.state.setPhase('waiting');
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
new file mode 100644
index 000000000..7cb024b84
--- /dev/null
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -0,0 +1,66 @@
+import { normalizeToKebabCase } from '../../../src/utils/normalization/normalize-to-kebab-case';
+import type { PromptFile } from './types/PromptFile';
+import type { PromptSection } from './types/PromptSection';
+
+/**
+ * Names of the harness and model currently selected for a coder run.
+ */
+export type PromptRunnerIdentity = {
+    /**
+     * Stable harness identifier, for example `github-copilot`.
+     */
+    readonly harnessName?: string;
+    /**
+     * Effective model identifier, for example `gpt-5.5`.
+     */
+    readonly modelName?: string;
+};
+
+/**
+ * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ *
+ * A prompt status line can contain one or more backtick-delimited model or harness
+ * names. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ */
+export function isPromptCompatibleWithRunner(
+    file: PromptFile,
+    section: PromptSection,
+    promptRunnerIdentity?: PromptRunnerIdentity,
+): boolean {
+    if (promptRunnerIdentity === undefined) {
+        return true;
+    }
+
+    const statusLine = section.statusLineIndex === undefined ? undefined : file.lines[section.statusLineIndex];
+    const requiredRunnerTokens = statusLine === undefined ? [] : extractPromptRunnerTokens(statusLine);
+
+    if (requiredRunnerTokens.length === 0) {
+        return true;
+    }
+
+    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+        .filter((name): name is string => name !== undefined && name.trim() !== '')
+        .map((name) => normalizeToKebabCase(name))
+        .filter((name) => name !== '');
+
+    return requiredRunnerTokens.some((requiredRunnerToken) => {
+        const normalizedRequiredRunnerToken = normalizeToKebabCase(requiredRunnerToken);
+
+        return (
+            normalizedRequiredRunnerToken !== '' &&
+            normalizedRunnerNames.some((normalizedRunnerName) =>
+                normalizedRunnerName.includes(normalizedRequiredRunnerToken),
+            )
+        );
+    });
+}
+
+/**
+ * Extracts model and harness tokens from a prompt status line.
+ */
+export function extractPromptRunnerTokens(statusLine: string): string[] {
+    return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
+        .map((match) => match[1]?.trim() ?? '')
+        .filter((token) => token !== '');
+}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content prompts/2026-09-0210-ptbk-coder-by-agent.md; Get-Content changelog/_current-preversion.md -TotalCount 90; Get-Content .vscode/terminals.json -Raw; git diff -- .vscode/terminals.json changelog/_current-preversion.md' in C:\Users\me\work\ai\promptbook
 succeeded in 490ms:
[^] by OpenAI Codex `gpt-5.6-terra` thinking `max` - Implementation in progress

[✨🎻] Allow to specify the agent in the prompt

```bash
ptbk coder run --harness openai-codex --model gpt-5.6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md
```

-   You can already specify the harness or model, allow also to specify the agent.
-   Both `agents/coding/developer.book`, `developer.book` and `developer` should work
    -   Also `Developer Foo bar` should work - The name of the agent from the 1st line of the book file.
-   Allowto combine, the specifying, the harness, model, or agent,
-   Keep in mind the DRY _(don't repeat yourself)_ principle.
-   Do a proper analysis of the current functionality of `ptbk coder` and related functionality before you start implementing.
-   Also look and update [the dev scripts in `terminals.json`](.vscode/terminals.json), Alongside the `developer` add there `unslopper`
-   You are working with [`ptbk coder`](src/cli/cli-commands/coder/run.ts)
-   Update the [`ptbk coder` landing website](apps/coder-landing)
-   Add the changes into the [changelog](changelog/_current-preversion.md)

-   Added agent-specific prompt routing to `ptbk coder`:

    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
        filename without `.book`, or the Book title from its first line. For example, a run using
        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
        `developer`, and `Promptbook Developer` routing tokens.
    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
        <path.book>` previews the same agent-specific queue without starting a harness.
    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
        documented agent-specific routing on the `ptbk coder` landing page.

-   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
    `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
    pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
    own `Deprecated:` paragraph, so the listing which is supposed to introduce the CLI was mostly made of commands
    nobody should start using. `ptbk --help`, and the same listing which plain `ptbk` prints when it asks for a
    subcommand, now offers only the commands which are meant to be used - `coder`, `agent`, `agent-folder`,
    `agents-server`, `about` and `hello`. Nothing was removed: every deprecated command still runs, still warns that it
    is deprecated when it is used, still explains the deprecation in its own help - `ptbk run --help` - and is still
    reachable through `ptbk help run`. Only the "did you mean" hint for a mistyped command stops pointing at them.
    Being left out of the help is now simply a part of what deprecating a command means, written once in
    `$deprecateCliCommand`, so any command deprecated later disappears from the help on its own.

-   `ptbk` without a subcommand no longer falls back to the deprecated `ptbk run`. Until now `run` was registered as
    the default command of the CLI, so everything which did not name a command - `ptbk ./write-cv.book`, a mistyped
    `ptbk codr` or a stray option - was quietly handed over to the pipeline runner of the old system, which was the
    last thing a deprecated command should be doing. Plain `ptbk` now asks to specify a subcommand and prints the
    top-level help, exactly like `ptbk coder`, `ptbk agent`, `ptbk agent-folder` and `ptbk agents-server` already did
    when they were called without one of their own subcommands, and that one shared behavior is now written once
    instead of five times. Anything which is not a command is refused with `unknown command` and a "did you mean"
    hint instead of being executed as a pipeline, so a script which still passes a book path to `ptbk` fails loudly
    rather than appearing to succeed. `ptbk run` itself is untouched and keeps working for everyone who asks for it
    explicitly. The command list in the help now starts with `coder`, followed by `agent`, `agent-folder` and
    `agents-server`, and the deprecated commands of the old pipeline system are listed after them.

-   `ptbk coder` now saves the run trace of every prompt round, so a finished run can still be analyzed afterwards.
    Until now the only durable record of a round was the one status line written into the prompt file: the live
    runtime log holding everything the harness and the verification command had written was a temporary artifact,
    deleted as soon as a successful round was over unless `--preserve-logs` was passed, and lost together with the
    reasoning, the tool calls and the diffs of the run. Each round now writes `prompts/traces/<prompt-file-name>.md`
    - a trace named exactly like the prompt file it belongs to, with the same `-2` section suffix the temporary
    scripts of a multi-section prompt file already use. The trace pairs the metadata of the round - which prompt and
    section it implemented, whether it succeeded or failed, the harness, model, thinking level and login method which
    ran it, how many coding attempts it took, what each step cost and how long each one ran, the verification
    command, the start, end and duration, and the error which ended a failed round - with the untouched runtime log
    of the harness. Because the trace is written before the round is committed, it lands in the very same commit as
    the prompt it describes, and a task whose isolated worktree cannot be merged back carries its trace into the
    failure commit instead. A failed round is traced too, next to the `.error.log` it already wrote. Re-running the
    same prompt overwrites its trace and leaves the earlier ones in the git history.

-   `ptbk coder run` and `ptbk coder server` no longer walk into a full disk. A coding round writes prompt and log
    artifacts, lets the harness rewrite the project, may install npm packages, may create a git worktree for
    `--isolate` and usually runs a build, so a run started without enough space used to die somewhere in the middle,
    after the coding agent had already rewritten a part of the project. The free disk space is now measured before
    anything is installed or written: with less than 2 GB left the command refuses to start and says how much space
    is left, how much is needed and how to get it back, instead of failing later with a raw filesystem error. The
    same measurement is repeated while the run is in progress - at the checkpoints where `ptbk coder` can stop
    anyway, so it is checked before every round, every verification and every commit - and once less than 1 GB is
    left the run reports the same warning and pauses there, waiting for the disk space to be freed and for `P` to
    resume it. The limit to keep running is lower than the limit to start, because interrupting a round which is
    already in progress costs more than not starting one. A run started with `--no-questions` never pauses, because
    there is nobody who could free the disk space and resume it - it reports the warning and keeps going. A platform
    which cannot report its free disk space never stops a run.

-   Added `--no-questions` to every `ptbk coder` command which can stop and wait for an answer it does not strictly
    need. `init`, `add`, `run`, `ping` and `server` still do all their work, but no longer ask anything: installing
    a missing coding harness, updating an outdated one, updating an outdated Promptbook CLI and adding the missing
    harness ignore rules to `.gitignore` are all skipped and reported together with the command which does them
    manually, and `ptbk coder add` refuses a missing description instead of asking for it. Because an outdated
    harness or CLI can only be updated after a confirmation, `--no-questions` also skips the npm latest-version
    lookup which would only lead to a question, which is what the never released `--no-harness-update` used to do -
    that flag is replaced by `--no-questions` and is gone. The one combination which can not work,
    `--no-auto --no-questions`, is refused right away instead of waiting forever for a confirmation nobody may be
    asked for. `ptbk coder verify` does not take the flag, because walking through finished prompts and asking about
    each of them is the only thing it does.

-   `ptbk coder` no longer answers a harness which is not logged in with a wall of raw CLI output. A harness whose
    login is missing or whose session has expired now fails with a short branded `AuthenticationError` which names
    the harness, quotes the one sentence the harness itself reported - such as `Failed to authenticate: OAuth session
    expired and could not be refreshed` - and says exactly how to sign in again, for example by running `claude` and
    then the `/login` command inside it. How each harness is signed in is described once, next to how it is installed
    and detected, and the failure is recognized once for every harness and for every command
    which runs prompts, so `coder run`, `coder server` and `coder ping` all report it the same way. The terminal and
    the `.error.log` file written next to the prompt both show those instructions on their own now, instead of
    following them with a stack trace of a failure nobody can debug. Because no retry of a harness which is not
    logged in can ever succeed, such a prompt is also no longer retried three times with the `--wait-after-error`
    delay in between - the instructions are shown right away.

-   Fixed the wall of `npm warn ERESOLVE overriding peer dependency` warnings printed when installing `ptbk`:

{
    "autokill": true,
    "terminals": [
        {
            "--note-0": "There are 3 types of terminals: 1) Runned automatically when coding (execute=true,onlySingle=false,focus=false) 2) Prepared automatically to be run by user but not executed by default (execute=false,onlySingle=false,focus=false), theese are marked by (💤) 3) Manually run by user (execute=true,onlySingle=true,focus=true)",
            "--note-1": "The (💸) means that script has some cost associated with it, for example, using a paid API or service.",
            "--note-2": "The (💤) means that script is not executed by default but need user to press [Enter] to run it",
            "--note-3": "The (⏳) means that script is expected to run for a longer time than usual",
            "--note-4": "The (❗) means that script should be run with caution, for example, it may cause breaking changes, data loss, etc.",
            "--note-5": "The (❗❗) means that script should be run with extreme caution, messing up with LTS versions, etc."
        },

        {
            "name": "🐙🏓 Ping Codex & Claude",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness claude-code --model claude-sonnet-5 --thinking-level low",
                "npx ts-node ./src/cli/test/ptbk.ts coder ping --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level low"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.5` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-sol` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-sol --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-terra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-terra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --min-priority 1"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-5.6-luna` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-5.6-luna --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push --max-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `claude-fable` Coding Agent (💤)(💸)(⏳)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model fable --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --min-priority 3 --test-before yes-and-fix  --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-max` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-max --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `qwen3.8-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model qwen3.8-flash --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-pro` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-pro-0813 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `deepseek-v4-flash` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model deepseek-v4-flash-0731 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🚀 Run ptbk coder `glm-5.2` Coding Agent (💤)(💸)(⏳)",
            "commands": [
                "set -a && source .env && set +a",
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness qwen-code --model glm-5.2 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push"
            ],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Generic\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 10 --template prompts/_templates/common.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"Agents server\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/agents-server.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"Utils app\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/utils-app.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk coder\" prompt boilerplates for Coding Agent (💤)",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-coder.md --commit --auto-pull --auto-push",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙🏭 Generate \"ptbk agent-folder\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agent.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate \"ptbk agents-server\" prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 5 --template prompts/_templates/ptbk-agents-server.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Generate test prompt boilerplates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder generate-boilerplates --count 1 --template prompts/_templates/test.md --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🏭 Find refactor candidates for Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-refactor-candidates",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🐙🔎 Find unwritten prompts",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-unwritten",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙📋 List prompts ready to run",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder list",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐙✔ Verify prompts done by Coding Agent",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder verify --order from-latest --ignore Refactor --commit --auto-pull --auto-push",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔖🟢 Tag the current commit as good (💤)",
            "command": "git fetch --tags && { base=good-$(date +%F); tag=$base; i=0; while git show-ref --verify --quiet refs/tags/$tag; do i=$((i+1)); tag=$base-$i; done; git tag -a $tag HEAD -m $tag && git push origin $tag && echo '🟢 Created tag marking good commit: ' $tag; }",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔖🔴 Tag the current commit as bad (💤)",
            "command": "git fetch --tags && { base=bad-$(date +%F); tag=$base; i=0; while git show-ref --verify --quiet refs/tags/$tag; do i=$((i+1)); tag=$base-$i; done; git tag -a $tag HEAD -m $tag && git push origin $tag && echo '🔴 Created tag marking bad commit: ' $tag; }",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "✨👨‍💻 Run ptbk agents-server dev",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
            ],
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "✨👨‍💻 Run ptbk agents-server start",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts agents-server dev --harness github-copilot --model gpt-5.4 --thinking-level max --port 4440"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) ✨⏩ Migrate Agents server `live` (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./src/database/migrate.ts --only=live",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗) ✨⏩ Migrate Agents server `preview` (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./src/database/migrate.ts --only=preview",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "✨🔼 Sync Agents server Vercel domains (dry-run)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./scripts/sync-vercel-domains.ts --dry-run",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) ✨🔼 Sync Agents server Vercel domains (💤)",
            "cwd": "./apps/agents-server/",
            "command": "npx tsx ./scripts/sync-vercel-domains.ts",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old) ✨💾 Backup Supabase Postgres (💤)",
            "command": "npx ts-node ./scripts/backup-supabase/backup-supabase.ts --output-dir other/backup --schemas public --filename-pattern %timestamp%.sql.zip",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗) ✨🔼 Update the preview Agents servers (live / `main` -> `preview`) (💤)",
            "command": "git branch --force preview main && git push origin preview --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=preview)",
            "execute": false,
            "onlySingle": true,
            "focus": false
        },
        {
            "name": "(old)(❗❗) ✨🔼 Update the production Agents servers (`preview` -> `production`) (💤)",
            "command": "git branch --force production preview && git push origin production --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=production)",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗❗❗) ✨🔼 Update the LTS Agents servers (`production` -> `lts`) (💤)",
            "command": "git branch --force lts production && git push origin lts --force-with-lease && (cd ./apps/agents-server/ && npx tsx ./src/database/migrate.ts --only=lts)",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) ✨🔼 Update Agent server (live / `main` -> `preview`) (💤)",
            "command": "git branch --force preview main && git push origin preview --force-with-lease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗) ✨🔼 Update Agent server (HEAD -> `preview`) (💤)",
            "command": "git branch --force preview HEAD && git push origin preview --force-with-lease && git checkout preview",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗❗) ✨🔼 Update Agent server (`preview` -> `production`) (💤)",
            "command": "git branch --force production preview && git push origin production --force-with-lease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗❗❗) ✨🔼 Update Agent server (`production` -> `lts`) (💤)",
            "command": "git branch --force lts production && git push origin lts --force-with-lease",
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻⚫ Run CLI example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts run ./book/examples/25-simple-knowledge.book.md --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) ✨👨‍💻🟣 Run deprecated ptbk start-agents-server",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts start-agents-server ./agents/default --port 4440 --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🟣 Run ptbk start-pipelines-server",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/cli/test/ptbk.ts start-pipelines-server ./src/remote-server/testing-books --url http://localhost:4460 --provider BRING_YOUR_OWN_KEYS"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻⚪ Run simple-script example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/simple-script/simple-script.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔴 Run logging example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/logging/logging.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔵 Run remote example (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./examples/usage/remote/server.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "👨‍💻🔵 Run CLI `ptbk about`",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npx ts-node ./src/cli/test/ptbk.ts about"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 👨‍💻🔠 Run Book Components web app",
            "cwd": "./apps/book-components/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🍎 Build Book Editor macOS app (💤)(⏳)",
            "command": "npm run build:book-editor-macos",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🆚 Build VSCode extension (💤)",
            "command": "npm run build:vscode-extension",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run Playground web app",
            "cwd": "./apps/playground/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run Utils web app",
            "cwd": "./apps/utils/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔠 Run ptbk coder landing page",
            "cwd": "./apps/coder-landing/",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npm run dev"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "--note": " <- Note: [🔠] Register new apps here "
        },
        {
            "name": "🧪🏭🩹 Full checkup (LF => Prettify (+commit) => Test & Test full build) (⏳⏳)",
            "command": "./scripts/lines-to-lf.sh && npx ts-node ./scripts/prettify-all/prettify-all.ts --commit && npm run test && npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪🧪 Test all (💤)(⏳)",
            "commands": ["npm run test"],
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Unit tests (💤)(⏳)",
            "command": "npm run test-unit",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Agents Server (💤)(⏳)",
            "command": "npm run test-app-agents-server",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Integration tests of Agents Server (💤)(⏳)",
            "cwd": "./apps/agents-server/",
            "command": "npm run test-e2e",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Integration tests of Agents Server (not headless) (⏳)",
            "cwd": "./apps/agents-server/",
            "command": "npx playwright test --headed",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 Test Types (💤)",
            "command": "npm run test-types",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Spell check (💤)",
            "command": "npm run test-spellcheck",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Lint (💤)",
            "command": "npm run test-lint",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(old) 🧪 Test Books",
            "command": "npm run test-books",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 Test Package generation (skip build) (💤)",
            "command": "npm run test-package-generation",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test Package generation (build) (💤)(⏳)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test name discrepancies (💤)",
            "command": "npm run test-name-discrepancies",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🧪 Test 🔠 Book Components build",
            "command": "npm run test-book-components-build",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧪 (examples) Lint ",
            "command": "npx eslint examples",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧪 (scripts) Lint",
            "command": "npx eslint scripts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🤪 Find fresh emoji tag",
            "command": "npx ts-node ./src/cli/test/ptbk.ts coder find-fresh-emoji-tags",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🐱‍👤 Import GitHub",
            "command": "npm run import-github",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🗑️ Delete OpenAI assistants",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/delete-openai-assistants/delete-openai-assistants.ts"
            ],
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🗑️ Delete OpenAI resources (files + vector stores)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/delete-openai-resources/delete-openai-resources.ts"
            ],
            "execute": false,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "✨🏭 Generate reserved paths for Agents server",
            "cwd": "./apps/agents-server/",
            "command": "npm run generate-reserved-paths",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭💉 Import markdowns",
            "command": "npm run import-markdowns",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭📦 Generate packages (Skip bundler)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭📦 Generate packages (⏳)",
            "command": "npx ts-node ./scripts/generate-packages/generate-packages.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(?old) 🏭📦📚 Generate documentation",
            "commands": ["npx ts-node ./scripts/generate-documentation/generate-documentation.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(?old) 🏭📚 Generate server types from Open API",
            "commands": ["npm run generate-openapi-types"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🏭📖 Generate examples .book -> .bookc (💸) [🍠]",
            "commands": [
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./scripts/generate-examples-bookc/generate-examples-bookc.ts --verbose"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🏭📖 Generate reports .report.json -> .report.md",
            "commands": ["npx ts-node ./scripts/generate-example-reports/generate-example-reports.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix Lint",
            "command": "npx eslint src --fix",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix Imports",
            "commands": ["npx ts-node ./scripts/repair-imports/repair-imports.ts --organize --organize-all"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Fix end of lines to CRLF -> LF",
            "command": "./scripts/lines-to-lf.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔎🩹 Find name discrepancies",
            "commands": ["npx ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Playground (💸)",
            "commands": ["unset OPENAI_API_KEY && unset OPENAI_BASE_URL", "npx ts-node ./src/playground/playground.ts"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸🔴 Playground error handling",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/playground/permanent/error-handling-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Agent with browser playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/playground/permanent/agent-with-browser-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "--note": "<- Note: [🎠] Add here new playgrounds"
        },
        {
            "name": "(old) 🧸 OpenAI playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/openai/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Ollama playground",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/ollama/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Anthropic Claude playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/anthropic-claude/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Vercel playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/vercel/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Azure OpenAI playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/azure-openai/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Agent LLM tools playground (with browser and search) (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/agent/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Langtail playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/langtail/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Multiple LLMs playground (💸)",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/multiple/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸 Remote server playground",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./src/llm-providers/remote/playground/playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸📄💡 Scrape knowledge from Markdown (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/markdown/playground/markdown-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 🧸📄💡 Scrape knowledge by Markitdown (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/markitdown/playground/markitdown-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from .docx (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/document/playground/document-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from (legacy) .doc (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸📃💡 Scrape knowledge from .pdf (playground) (💸) [🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/pdf/playground/pdf-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },

        {
            "name": "(old) 🧸🌍💡 Scrape knowledge from website (playground) (💸)[🍠]",
            "commands": [
                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
                "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose",
                "npx ts-node ./src/scrapers/website/playground/website-scraper-playground.ts"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🧸 Transpile book -> OpenAiSdk (playground)",
            "command": "npx ts-node ./src/transpilers/openai-sdk/playground/playground.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old) 📖 Make Promptbook collection (💸)(+commit)[🍠]",
            "command": "npx ts-node ./scripts/make-promptbook-collection/make-promptbook-collection.ts --verbose --commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔪👨🏽‍💻 Force reinstall of node modules (⏳)",
            "commands": [
                "rm -f ./package-lock.json",
                "rm -rf ./node_modules ./.next ./apps/**/node_modules ./apps/**/.next",
                "npm cache clean",
                "npm install"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🍴🔍 Git list unmerged changes",
            "commands": ["git branch --no-merged main"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🍴🗑️ Git delete merged branches",
            "commands": [
                "./scripts/is-main-checked.sh && git branch --merged | egrep -v '(^\\*|main)' | xargs git branch -d",
                "git remote prune origin",
                "git gc"
            ],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔪 Kill all Node.js processes (💤)",
            "command": "echo 🔪 & taskkill /F /IM node.exe & taskkill /F /IM codex.exe & taskkill /F /IM claude.exe",
            "shellPath": "C:\\Windows\\System32\\cmd.exe",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "(❗) 🔪 Kill and release Git lock (💤)",
            "command": "echo 🔪 & taskkill /F /IM git.exe",
            "shellPath": "C:\\Windows\\System32\\cmd.exe",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🍴🔼 Make empty commit",
            "commands": ["git commit --allow-empty -m 'Empty commit'", "git push"],
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🍴⏫ Git push all branches (⏳)",
            "command": "git diff --exit-code && ./scripts/push-all-branches.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(old)(❗) 🍴⏬ Git pull all branches (⏳)",
            "command": "git diff --exit-code && ./scripts/pull-all-branches.sh",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🌎 SSH to live",
            "command": "ssh root@live.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to preview",
            "command": "ssh root@preview.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to production",
            "command": "ssh root@production.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🌎 SSH to LTS",
            "command": "ssh root@lts.ptbk.io",
            "execute": true,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔼🆚 Use current version of Promptbook in all Projects / install ptbk (⏳)",
            "command": "npm run use-packages",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Prettify all files (no commit)(⏳)",
            "command": "npx ts-node ./scripts/prettify-all/prettify-all.ts",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🩹 Prettify all files (+commit)(⏳)",
            "command": "npx ts-node ./scripts/prettify-all/prettify-all.ts --commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🏭🔼 Update and install patch dependencies (+commit)(⏳)",
            "command": "npm run update-and-install-patch-dependencies-commit",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "(❗) 🔼🚀 Release minor version (💸)(⏳)",
            "command": "npm version minor",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🧪 Release pre-minor version (💸)(⏳)",
            "command": "npm version preminor",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🧪 Release pre-release version (💤)(💸)(⏳)",
            "command": "npm version prerelease",
            "execute": false,
            "onlySingle": false,
            "focus": false
        },
        {
            "name": "🔼🧪 Release pre-release version (ignore pre-checks) (💸)(⏳)",
            "command": "npm version prerelease --ignore-scripts && npm run postversion",
            "execute": true,
            "onlySingle": true,
            "focus": true
        },
        {
            "name": "🔼🩹 Release patch version (💤)(💸)(⏳)",
            "command": "npm version patch",
            "onlySingle": true,
            "focus": true
        }
    ]
}

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa..59c07d6e8 100644
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -77,6 +77,17 @@
             "onlySingle": false,
             "focus": false
         },
+        {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
         {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d68..0472bd2a7 100644
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..33d3dc11651d68623f14ec65fb9c01292fb13728
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\main\listCoderPrompts.ts
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\main\runCodexPrompts.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg --files scripts/run-codex-prompts | rg "runCodexPrompts.*test|listCoderPrompts.*test"; node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --runInBand scripts/run-codex-prompts/main/runCodexPrompts.test.ts scripts/run-codex-prompts/main/listCoderPrompts.test.ts scripts/run-codex-prompts/common/resolveCoderAgent.test.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts src/cli/cli-commands/coder/list.test.ts src/cli/cli-commands/coder/run.test.ts' in C:\Users\me\work\ai\promptbook
 succeeded in 20917ms:
scripts/run-codex-prompts\main\runCodexPrompts.test.ts
scripts/run-codex-prompts\main\listCoderPrompts.test.ts
PASS scripts/run-codex-prompts/main/runCodexPrompts.test.ts (10.112 s)
  ● Console

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:436:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:436:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRun limit reached after 2 prompt runs.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [32mEnd requested after 1 prompt run.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
PASS src/cli/cli-commands/coder/run.test.ts
PASS scripts/run-codex-prompts/main/listCoderPrompts.test.ts
PASS src/cli/cli-commands/coder/list.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts

Test Suites: 6 passed, 6 total
Tests:       61 passed, 61 total
Snapshots:   0 total
Time:        13.08 s
Ran all test suites matching /scripts\\run-codex-prompts\\main\\runCodexPrompts.test.ts|scripts\\run-codex-prompts\\main\\listCoderPrompts.test.ts|scripts\\run-codex-prompts\\common\\resolveCoderAgent.test.ts|scripts\\run-codex-prompts\\prompts\\promptRunnerFiltering.test.ts|src\\cli\\cli-commands\\coder\\list.test.ts|src\\cli\\cli-commands\\coder\\run.test.ts/i.

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The shared routing identity now carries the resolved Book aliases, so the same matching path serves `run`, `server`, and queue previewing. The focused regression suite remains green after the final wording updates.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --check; git status --short; git diff -- apps/coder-landing/src/data/commands.ts apps/coder-landing/src/data/advancedFeatures.ts apps/coder-landing/src/components/sections/AgentsSection.tsx apps/coder-landing/specs/content/commands.md apps/coder-landing/specs/sections/agent-book.md apps/coder-landing/specs/sections/advanced-features.md' in C:\Users\me\work\ai\promptbook
 succeeded in 557ms:
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
 M .vscode/terminals.json
 M apps/coder-landing/specs/content/commands.md
 M apps/coder-landing/specs/sections/advanced-features.md
 M apps/coder-landing/specs/sections/agent-book.md
 M apps/coder-landing/src/components/sections/AgentsSection.tsx
 M apps/coder-landing/src/data/advancedFeatures.ts
 M apps/coder-landing/src/data/commands.ts
 M changelog/_current-preversion.md
 M prompts/2026-09-0210-ptbk-coder-by-agent.md
 M scripts/run-codex-prompts/cli/parseRunOptions.test.ts
 M scripts/run-codex-prompts/cli/parseRunOptions.ts
 M scripts/run-codex-prompts/common/resolveCoderAgent.ts
 M scripts/run-codex-prompts/main/listCoderPrompts.ts
 M scripts/run-codex-prompts/main/runCodexPrompts.ts
 M scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
 M scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
 M src/cli/cli-commands/coder/list.test.ts
 M src/cli/cli-commands/coder/list.ts
 M src/cli/cli-commands/coder/run.test.ts
 M src/cli/cli-commands/coder/run.ts
 M src/cli/cli-commands/coder/server.ts
?? scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
?? src/cli/cli-commands/coder/agentCliOptions.ts
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6..b820e5e8f 100644
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@ The single source of truth for every shell command shown on the page. Any sectio
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@ These `ptbk coder` options may be referenced in page prose and must be described
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0..02b19b5ca 100644
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@ Anchor `#features`. A grid of feature cards, each pairing a short explanation wi
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c7..50bc75e30 100644
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@ Two columns on desktop (stacked on mobile):
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfc..749f5d006 100644
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@ export function AgentsSection() {
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5..c50319e3a 100644
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -89,6 +90,12 @@ export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
             'Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it.',
         sampleCommand: MODEL_FILTER_COMMAND,
     },
+    {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
     {
         title: 'Pacing and retries',
         description:
@@ -98,7 +105,7 @@ export const ADVANCED_FEATURES: ReadonlyArray<AdvancedFeatureDefinition> = [
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf..09213d85d 100644
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -50,6 +50,12 @@ export const GENERATE_BOILERPLATES_COMMAND = 'ptbk coder generate-boilerplates -
  */
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
+/**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
 /**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..b820e5e8ff632b2f8f8d2248aec43afad6393ab9
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
 -   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

apply patch
patch: completed
C:\Users\me\work\ai\promptbook\apps\coder-landing\specs\content\commands.md
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'npx ts-node ./src/cli/test/ptbk.ts coder list --harness openai-codex --model gpt-5.6-astra --agent agents/coding/developer.book; npm run test-types' in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match '"'ptbk.ts coder list|npm run test-types|tsc' } | Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List" in C:\Users\me\work\ai\promptbook
 succeeded in 994ms:

ProcessId       : 32136
ParentProcessId : 16800
Name            : pwsh.exe
CommandLine     : "C:\Program Files\PowerShell\7\pwsh.exe" -Command "try { 
                  [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}
                  npx ts-node ./src/cli/test/ptbk.ts coder list --harness openai-codex --model gpt-5.6-astra --agent 
                  agents/coding/developer.book; npm run test-types"

ProcessId       : 23380
ParentProcessId : 40308
Name            : cmd.exe
CommandLine     : C:\WINDOWS\system32\cmd.exe /d /s /c tsc

ProcessId       : 12824
ParentProcessId : 23380
Name            : node.exe
CommandLine     : "node"   "C:\Users\me\work\ai\promptbook\node_modules\.bin\\..\typescript\bin\tsc" 

ProcessId       : 42848
ParentProcessId : 16800
Name            : pwsh.exe
CommandLine     : "C:\Program Files\PowerShell\7\pwsh.exe" -Command "try { 
                  [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}
                  Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'ptbk.ts coder list|npm run 
                  test-types|tsc' } | Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List"


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
The real `ptbk coder list` invocation accepted the combined harness/model/agent selection; its follow-up type check is still finishing.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 15; Get-CimInstance Win32_Process | Where-Object { $_.ProcessId -eq 32136 -or $_.ParentProcessId -eq 32136 -or $_.CommandLine -match '"'npm run test-types|typescript\\\\bin\\\\tsc' } | Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List" in C:\Users\me\work\ai\promptbook
 succeeded in 15791ms:

ProcessId       : 32136
ParentProcessId : 16800
Name            : pwsh.exe
CommandLine     : "C:\Program Files\PowerShell\7\pwsh.exe" -Command "try { 
                  [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}
                  npx ts-node ./src/cli/test/ptbk.ts coder list --harness openai-codex --model gpt-5.6-astra --agent 
                  agents/coding/developer.book; npm run test-types"

ProcessId       : 40308
ParentProcessId : 32136
Name            : node.exe
CommandLine     : "C:\Users\me\.nvm\versions\node\v22.23.2\bin\node.exe" 
                  C:\Users\me\.nvm\versions\node\v22.23.2\bin/node_modules/npm/bin/npm-cli.js run test-types

ProcessId       : 12824
ParentProcessId : 23380
Name            : node.exe
CommandLine     : "node"   "C:\Users\me\work\ai\promptbook\node_modules\.bin\\..\typescript\bin\tsc" 

ProcessId       : 3868
ParentProcessId : 16800
Name            : pwsh.exe
CommandLine     : "C:\Program Files\PowerShell\7\pwsh.exe" -Command "try { 
                  [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}
                  Start-Sleep -Seconds 15; Get-CimInstance Win32_Process | Where-Object { $_.ProcessId -eq 32136 -or 
                  $_.ParentProcessId -eq 32136 -or $_.CommandLine -match 'npm run test-types|typescript\\bin\\tsc' } | 
                  Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List"


diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 64226ms:
[36mUpcoming tasks (grouped by priority):[39m
[36mPriority 0:[39m
 1. prompts/2026-09-0220-ptbk-coder-broken-ui.md#1 - The Promptbook Coder avatar can be sometimes broken, fix it
 2. prompts/2026-09-0240-blinking-mocked-chat.md#1 - The mocked chat component is blinking rapidly when rendering, fix it

> promptbook@0.114.0-33 test-types
> tsc


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Start-Sleep -Seconds 30; Get-CimInstance Win32_Process | Where-Object { $_.ProcessId -eq 32136 -or $_.ParentProcessId -eq 32136 -or $_.CommandLine -match '"'npm run test-types|typescript\\\\bin\\\\tsc' } | Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List" in C:\Users\me\work\ai\promptbook
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..33858246bb8b3931bc0d3f15360f91577b52cc4a
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

 succeeded in 30593ms:

ProcessId       : 4240
ParentProcessId : 16800
Name            : pwsh.exe
CommandLine     : "C:\Program Files\PowerShell\7\pwsh.exe" -Command "try { 
                  [Console]::OutputEncoding=[System.Text.Encoding]::UTF8 } catch {}
                  Start-Sleep -Seconds 30; Get-CimInstance Win32_Process | Where-Object { $_.ProcessId -eq 32136 -or 
                  $_.ParentProcessId -eq 32136 -or $_.CommandLine -match 'npm run test-types|typescript\\bin\\tsc' } | 
                  Select-Object ProcessId, ParentProcessId, Name, CommandLine | Format-List"


apply patch
patch: completed
C:\Users\me\work\ai\promptbook\scripts\run-codex-prompts\prompts\isPromptCompatibleWithRunner.ts
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..256ff77484933ef8a3af672ecc18010d33366e1f
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
@@ -57,7 +66,7 @@
 }
 
 /**
- * Extracts model and harness tokens from a prompt status line.
+ * Extracts model, harness and Book-agent tokens from a prompt status line.
  */
 export function extractPromptRunnerTokens(statusLine: string): string[] {
     return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..256ff77484933ef8a3af672ecc18010d33366e1f
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
@@ -57,7 +66,7 @@
 }
 
 /**
- * Extracts model and harness tokens from a prompt status line.
+ * Extracts model, harness and Book-agent tokens from a prompt status line.
  */
 export function extractPromptRunnerTokens(statusLine: string): string[] {
     return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- scripts/run-codex-prompts/common/resolveCoderAgent.test.ts scripts/run-codex-prompts/cli/parseRunOptions.test.ts src/cli/cli-commands/coder/run.test.ts src/cli/cli-commands/coder/list.test.ts scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts; git diff --check; git diff --stat' in C:\Users\me\work\ai\promptbook
 succeeded in 657ms:
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb6..78bb2b22c 100644
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@ describe('parseRunOptions', () => {
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92..8726dbdbe 100644
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@ describe('prompt runner filtering', () => {
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb6..0aa8e33d7 100644
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@ jest.mock('../../../../scripts/run-codex-prompts/main/listCoderPrompts', () => (
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -13,6 +19,13 @@ function getListCoderPromptsMock(): jest.MockedFunction<typeof listCoderPrompts>
     return listCoderPrompts as jest.MockedFunction<typeof listCoderPrompts>;
 }
 
+/**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
 /**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
@@ -36,6 +49,7 @@ describe('$initializeCoderListCommand', () => {
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@ describe('$initializeCoderListCommand', () => {
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeb..067175570 100644
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@ describe('$initializeCoderRunCommand', () => {
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
prompts/2026-09-0210-ptbk-coder-by-agent.md:19: new blank line at EOF.
 .vscode/terminals.json                             |  11 +++
 apps/coder-landing/specs/content/commands.md       |   7 +-
 .../specs/sections/advanced-features.md            |  48 +++++-----
 apps/coder-landing/specs/sections/agent-book.md    |   3 +-
 .../src/components/sections/AgentsSection.tsx      |   6 ++
 apps/coder-landing/src/data/advancedFeatures.ts    |   9 +-
 apps/coder-landing/src/data/commands.ts            |   6 ++
 changelog/_current-preversion.md                   |  11 +++
 prompts/2026-09-0210-ptbk-coder-by-agent.md        |   3 +-
 .../run-codex-prompts/cli/parseRunOptions.test.ts  |  17 ++++
 scripts/run-codex-prompts/cli/parseRunOptions.ts   |   5 +-
 .../run-codex-prompts/common/resolveCoderAgent.ts  | 102 ++++++++++++++++++---
 scripts/run-codex-prompts/main/listCoderPrompts.ts |   2 +-
 scripts/run-codex-prompts/main/runCodexPrompts.ts  |   5 +-
 .../prompts/isPromptCompatibleWithRunner.ts        |  23 +++--
 .../prompts/promptRunnerFiltering.test.ts          |  45 +++++++++
 src/cli/cli-commands/coder/list.test.ts            |  46 ++++++++++
 src/cli/cli-commands/coder/list.ts                 |  18 +++-
 src/cli/cli-commands/coder/run.test.ts             |  29 ++++++
 src/cli/cli-commands/coder/run.ts                  |   8 +-
 src/cli/cli-commands/coder/server.ts               |   8 +-
 21 files changed, 345 insertions(+), 67 deletions(-)

diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..256ff77484933ef8a3af672ecc18010d33366e1f
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
@@ -57,7 +66,7 @@
 }
 
 /**
- * Extracts model and harness tokens from a prompt status line.
+ * Extracts model, harness and Book-agent tokens from a prompt status line.
  */
 export function extractPromptRunnerTokens(statusLine: string): string[] {
     return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

codex
Implemented agent-specific prompt routing for `ptbk coder`.

- `--agent` now routes prompts by Book path, filename, stem, or first-line title, alongside harness/model selection.
- Added matching support to `run`, `server`, and `coder list --agent`.
- Added the Unslopper VS Code terminal preset.
- Updated coder-landing content and changelog.

Validated with `npm test`, type/lint/spell checks, landing build, and focused Jest tests.

Note: I left the pre-existing change in `prompts/2026-09-0210-ptbk-coder-by-agent.md` untouched.
diff --git a/.vscode/terminals.json b/.vscode/terminals.json
index eb98143aa183c7ce0ea17b2adec64701e155f633..59c07d6e82774deede770987f62240be3a5ecc7d
--- a/.vscode/terminals.json
+++ b/.vscode/terminals.json
@@ -78,6 +78,17 @@
             "focus": false
         },
         {
+            "name": "🐙🚀 Run ptbk coder `gpt-6-astra` Unslopper Agent (💤)(💸)(⏳)",
+            "commands": [
+                "set -a && source .env && set +a",
+                "unset OPENAI_API_KEY && unset OPENAI_BASE_URL",
+                "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness openai-codex --model gpt-6-astra --thinking-level max --agent agents/coding/unslopper.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fail  --auto-pull --auto-push --min-priority 2"
+            ],
+            "execute": false,
+            "onlySingle": false,
+            "focus": false
+        },
+        {
             "name": "🐙🚀 Run ptbk coder `claude-opus-5` Coding Agent (💤)(💸)(⏳)",
             "command": "npx ts-node ./src/cli/test/ptbk.ts coder run --no-harness-update --harness claude-code --model claude-opus-5 --thinking-level max --agent agents/coding/developer.book --context AGENTS.md --test npm run test-for-ptbk-coder --test-before yes-and-fix  --auto-pull --auto-push",
             "execute": false,
diff --git a/apps/coder-landing/specs/content/commands.md b/apps/coder-landing/specs/content/commands.md
index f71de49b6953764a030370983cf8e2d3c1b054f3..bad79b654958ddc12285c49c6969cf2f16680e6e
--- a/apps/coder-landing/specs/content/commands.md
+++ b/apps/coder-landing/specs/content/commands.md
@@ -13,6 +13,7 @@
 | `LIST_COMMAND`                  | `ptbk coder list`                                                                                                                                                                                      | [Advanced features](../sections/advanced-features.md), [Quickstart step 4](../sections/quickstart.md) |
 | `GENERATE_BOILERPLATES_COMMAND` | `ptbk coder generate-boilerplates --count 10*7`                                                                                                                                                        | [Advanced features](../sections/advanced-features.md)                       |
 | `MODEL_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5`                                                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
+| `AGENT_FILTER_COMMAND`          | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`                                                                                                              | [Advanced features](../sections/advanced-features.md)                       |
 | `TEST_BEFORE_FIX_COMMAND`       | `ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix`                                                                                                                       | [Advanced features](../sections/advanced-features.md)                       |
 | `GIT_CHANGES_CONTINUE_COMMAND`  | `ptbk coder run --harness claude-code --git-changes continue`                                                                                                                                          | [Advanced features](../sections/advanced-features.md)                       |
 | `DRY_RUN_COMMAND`               | `ptbk coder run --dry-run`                                                                                                                                                                             | [Advanced features](../sections/advanced-features.md)                       |
@@ -31,11 +32,11 @@
 
 -   `--harness <name>` — selects the coding agent; one of the harnesses in [`harness-catalog.md`](./harness-catalog.md). Required when `run` or `server` executes prompts, and an optional filter for `list`.
 -   `--model <model>` — model passed to the harness; **required** for `openai-codex`, `gemini`, and `qwen-code` when they execute prompts, and an optional filter for `list`. The value `default` asks for the default model of that harness; for `openai-codex` it passes no model to Codex at all, so Codex keeps the model of its own configuration — the only thing a ChatGPT-account login accepts.
--   Prompt routing — add one or more backtick-delimited model, model-family, or harness names to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5`  ``; the prompt runs only when the selected harness or model matches after normalization, while `!` markers can appear before or after the token.
--   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
+-   Prompt routing — add one or more backtick-delimited model, model-family, harness, or selected-agent references to a ready `[ ]` status line, for example `` [ ] use model `gpt-5.5` `` or `` [ ] use agent `developer` ``. The prompt runs when any token matches after normalization, while `!` markers can appear before or after a token. A selected Book agent matches by its configured path (`agents/coding/developer.book`), filename (`developer.book`), filename without `.book` (`developer`), or title from the Book's first line (`Developer Foo bar`).
+-   `ptbk coder list` — list all ready, fully authored prompts in descending priority groups without starting a harness or changing files. Optional `--harness`, `--model`, `--agent`, `--priority`, `--min-priority`, and `--max-priority` filters preview the queue a selected runner would receive.
 -   Prompt statuses — `[-]` or `[.]` not ready, `[ ]` ready, `[^]` being implemented right now, `[x]` implemented, verified and committed, `[!]` failed. A prompt with no status checkbox is also ready at priority `0`; when processing begins, its new `[^]` line is added before the prompt content. Put `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore that entire file. `run` and `server` rewrite `[^]` before every step of a round, so it names the harness, the model, the finished steps and the running one (for example `` [^] by OpenAI Codex `gpt-5.6-luna` thinking `max` (ChatGPT account) - Implementation ~$0.2036 10 minutes; Testing in progress ``). Only the final `[x]` is committed, and `[^]` is never reverted, so a killed or crashed coder leaves it behind as the signal that the task was left in the middle; such a prompt is not picked up again automatically.
 -   `--thinking-level <level>` — reasoning effort for supported harnesses: `low`, `medium`, `high`, `xhigh`, `max`.
--   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt.
+-   `--agent <path.book>` — a `.book` file whose compiled system message is prepended to each coding prompt and whose path, filename, filename without `.book`, and Book title can route agent-specific prompts. It also filters `ptbk coder list` without starting a harness.
 -   `--context <text-or-file>` — extra instructions inline or from a project file (e.g. `AGENTS.md`).
 -   `--test <command...>` — verification command run after each prompt; failures are fed back and retried.
 -   `--test-before <no|yes-and-fail|yes-and-fix>` — run the test command before coding; stop on pre-existing failures or create one repair prompt, and use `npm test` when no `--test` command is provided.
diff --git a/apps/coder-landing/specs/sections/advanced-features.md b/apps/coder-landing/specs/sections/advanced-features.md
index 8408e5ab0eb0a49e8d7347e32733534fd0904cb7..02b19b5ca7f978606ccb2347a290d9f75d5ebf83
--- a/apps/coder-landing/specs/sections/advanced-features.md
+++ b/apps/coder-landing/specs/sections/advanced-features.md
@@ -9,7 +9,7 @@
 
 ## Cards
 
-3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty cards, in this order:
+3-column grid on desktop, 2 on tablet, 1 on mobile. Each card: title (Outfit semibold), description, and a [terminal block](../components/terminal-block.md) snippet. Exactly these twenty-one cards, in this order:
 
 | #   | Title                          | Snippet                                                                                          |
 | --- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
@@ -22,17 +22,18 @@
 | 7   | Kanban web UI                  | `ptbk coder server --port 4441 --harness claude-code`                                            |
 | 8   | Prompt priorities              | `ptbk coder run --harness claude-code --min-priority 1 --max-priority 5`                         |
 | 9   | Model-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5`                                        |
-| 10  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
-| 11  | List before you run            | `ptbk coder list`                                                                                |
-| 12  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
-| 13  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
-| 14  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
-| 15  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
-| 16  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
-| 17  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
-| 18  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
-| 19  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
-| 20  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
+| 10  | Agent-specific prompts         | `ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book`         |
+| 11  | Pacing and retries             | `ptbk coder run --harness claude-code --wait-between-prompts 30m --wait-after-error 10m`         |
+| 12  | List before you run            | `ptbk coder list`                                                                                |
+| 13  | Ping before you queue          | `ptbk coder ping --harness openai-codex --model gpt-5.6-sol --thinking-level xhigh`              |
+| 14  | Keep the 5-hour window rolling | `ptbk coder ping --harness claude-code --model claude-sonnet-5 --thinking-level low --period 5h` |
+| 15  | Human in the loop              | `ptbk coder run --harness claude-code --no-auto`                                                 |
+| 16  | Nothing to answer              | `ptbk coder run --harness claude-code --no-questions`                                            |
+| 17  | Live status in the prompt file | `` [^] by OpenAI Codex `gpt-5.6-luna` - Implementation in progress ``                            |
+| 18  | Pick up where the run stopped  | `ptbk coder run --harness claude-code --git-changes continue`                                    |
+| 19  | Run traces you can read later  | `prompts/traces/2026-09-0180-add-dark-mode.md`                                                   |
+| 20  | Verify and archive             | `ptbk coder verify --order from-latest`                                                          |
+| 21  | Many prompts per file          | `ptbk coder generate-boilerplates --count 10*7`                                                  |
 
 ## Descriptions (verbatim card copy)
 
@@ -45,16 +46,17 @@
 7. **Kanban web UI** — "ptbk coder server keeps running after the queue is empty, watches for new prompt files and serves a Trello-style board where you can edit prompts in the browser."
 8. **Prompt priorities** — "Give prompts a priority and process only the range you want in the current run."
 9. **Model-specific prompts** — "Route a prompt to a model family or harness with a backtick token on its [ ] status line, such as [ ] use model `gpt-5.5`. Other runners skip it."
-10. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
-11. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent."
-12. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
-13. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
-14. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
-15. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
-16. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
-17. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
-18. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
-19. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
-20. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
+10. **Agent-specific prompts** — "Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it."
+11. **Pacing and retries** — "Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours."
+12. **List before you run** — "See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent."
+13. **Ping before you queue** — "ptbk coder ping sends one tiny dummy prompt to a harness and model and reports the answer, the response time and the usage. Use it to check that a harness, model and login work before you queue anything. It also opens the hourly or weekly quota window early, so the quota is already refreshing by the time you need it. If that harness has missing local ignore rules, it asks before adding them to .gitignore."
+14. **Keep the 5-hour window rolling** — "Add --period and the ping repeats until you stop it with CTRL+C. One ping every 5h holds the Claude Code 5-hour limit window open, so a queue you start at any hour already has a refreshing window waiting for it. That costs a handful of tokens per ping instead of a run you have to babysit."
+15. **Human in the loop** — "Confirm each prompt yourself with --no-auto. Press P to pause a running queue, or X to end it after the current prompt. Every press is answered in the Controls panel on the next frame, so you can tell that the key landed even when it changed nothing."
+16. **Nothing to answer** — "The opposite end: --no-questions never asks anything at all. Installing a missing harness, updating an outdated one or adding ignore rules is skipped and printed as the command which does it manually, so a queue started from a script or a CI job never stops at a prompt nobody is there to answer. ptbk coder init, add, run, ping and server all take it."
+17. **Live status in the prompt file** — "A [ ] prompt — or plain markdown with no status checkbox — turns into [^] the moment the agent picks it up, and the line names the step that is running. A prompt without a checkbox starts at priority 0. Use [-] or [.] to keep a task out of the queue, or add `<!--ptbk-coder-ignore-->` anywhere in a Markdown file to ignore it entirely. It only becomes [x] after the work is implemented, verified and committed. ptbk coder never reverts a [^], so if the queue is killed or crashes you can see which task was left half-done."
+18. **Pick up where the run stopped** — "A dirty working tree stops ptbk coder by default. --git-changes ignore starts anyway, and --git-changes continue resumes the one prompt left in [^] with its half-finished changes still in place. Any harness can take the work over, and the status line then names both the one that started and the one that finished it."
+19. **Run traces you can read later** — "Every round leaves a trace in prompts/traces/, named after the prompt file it belongs to. It holds the harness, model, thinking level and login method that ran the prompt, what each step cost and how long it took, and the whole raw output of the harness. The trace is committed together with the prompt, so the run can still be analyzed long after its temporary logs are gone."
+20. **Verify and archive** — "Every successful round writes the harness, model and thinking level into the prompt status line. Walk through completed prompts one by one, archive the finished ones to prompts/done/, and get a repair prompt appended for anything left incomplete. Pick the order with --order from-earliest, from-latest or random."
+21. **Many prompts per file** — "ptbk coder generate-boilerplates writes one prompt per file by default (--count 5\*1). Use --count N\*M to pack a whole backlog into fewer files: N files with M prompts each. A --- line separates the sections, every file carries one fresh emoji tag, and each section still runs as its own task."
 
 Option semantics must stay consistent with [`../content/commands.md`](../content/commands.md).
diff --git a/apps/coder-landing/specs/sections/agent-book.md b/apps/coder-landing/specs/sections/agent-book.md
index 872e7d8c74e51ef0cf4581e6d15bb98659ba5c33..50bc75e30a3f4bcc4abb6d445aaff9135b20d219
--- a/apps/coder-landing/specs/sections/agent-book.md
+++ b/apps/coder-landing/specs/sections/agent-book.md
@@ -1,6 +1,6 @@
 # Section: Agent personas (Book)
 
-Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book` and lets the visitor *see* an agent definition in the real Book editor.
+Anchor `#agents`. Shows how a run is personalized with `--agent agents/developer.book`, how tasks can target that agent, and lets the visitor *see* an agent definition in the real Book editor.
 
 ## Copy
 
@@ -16,6 +16,7 @@
 1. Intro line: "Point **ptbk coder** at any agent file with `--agent`:"
 2. A [terminal block](../components/terminal-block.md) with the canonical `AGENT_RUN_COMMAND` (see [`../content/commands.md`](../content/commands.md)).
 3. Follow-up paragraph: `ptbk coder init` creates this default developer agent at `agents/developer.book`. Edit it like any other file in your repository to change how your agent codes.
+4. Prompt-routing paragraph: a ready task can target that agent with the status line ``[ ] use agent `developer` ``. The selected Book's path, filename, filename without `.book`, and title from its first line all work.
 
 ### Right — the agent itself
 
diff --git a/apps/coder-landing/src/components/sections/AgentsSection.tsx b/apps/coder-landing/src/components/sections/AgentsSection.tsx
index 638841dfcdbdcc90da17886f0452ca8f7cda8ca9..749f5d006ef5b91185a2c2dbf56bdbe3349255ee
--- a/apps/coder-landing/src/components/sections/AgentsSection.tsx
+++ b/apps/coder-landing/src/components/sections/AgentsSection.tsx
@@ -39,6 +39,12 @@
                         agent at <code className="text-promptbook-blue">agents/developer.book</code>. Edit it like any
                         other file in your repository to change how your agent codes.
                     </p>
+                    <p className="mt-4 text-gray-400">
+                        Route a ready task to that agent with a status line such as{' '}
+                        <code className="text-promptbook-blue">[ ] use agent `developer`</code>. Its path, filename,
+                        filename without <code className="text-promptbook-blue">.book</code>, or title from the first
+                        line of the Book all work.
+                    </p>
                 </div>
 
                 <div>
diff --git a/apps/coder-landing/src/data/advancedFeatures.ts b/apps/coder-landing/src/data/advancedFeatures.ts
index c1d62b3d5aea73792d499635bb3645badcd6a93a..c50319e3ac503fab12750c827e7fae6b4d1983e4
--- a/apps/coder-landing/src/data/advancedFeatures.ts
+++ b/apps/coder-landing/src/data/advancedFeatures.ts
@@ -1,4 +1,5 @@
 import {
+    AGENT_FILTER_COMMAND,
     GENERATE_BOILERPLATES_COMMAND,
     GIT_CHANGES_CONTINUE_COMMAND,
     INIT_GIT_SYNC_COMMAND,
@@ -90,6 +91,12 @@
         sampleCommand: MODEL_FILTER_COMMAND,
     },
     {
+        title: 'Agent-specific prompts',
+        description:
+            'Route a prompt to the selected Book with its path, filename, stem or title from the first line, such as [ ] use agent `developer`. Other agents skip it.',
+        sampleCommand: AGENT_FILTER_COMMAND,
+    },
+    {
         title: 'Pacing and retries',
         description:
             'Wait a fixed wall-clock duration between prompts. The clock keeps running through a pause and through sleep, and errors retry after a cool-down. The terminal also shows every subscription window a harness reports, with its remaining allowance and reset time. Whenever S is offered it skips whatever the coder waits for right now, down to the harness session limit that would otherwise hold the run for hours.',
@@ -98,7 +105,7 @@
     {
         title: 'List before you run',
         description:
-            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, or priority range; no files change and no tokens are spent.',
+            'See every ready, fully authored prompt grouped by priority before starting a harness. Narrow the list by harness, model, agent, or priority range; no files change and no tokens are spent.',
         sampleCommand: LIST_COMMAND,
     },
     {
diff --git a/apps/coder-landing/src/data/commands.ts b/apps/coder-landing/src/data/commands.ts
index 8680ec1bf80b94e6c0619904976844f3dd49f407..09213d85de7d31670723bd805a3ca0620b055ec8
--- a/apps/coder-landing/src/data/commands.ts
+++ b/apps/coder-landing/src/data/commands.ts
@@ -51,6 +51,12 @@
 export const MODEL_FILTER_COMMAND = 'ptbk coder run --harness github-copilot --model gpt-5.5';
 
 /**
+ * Command which runs prompts through a selected Book agent.
+ */
+export const AGENT_FILTER_COMMAND =
+    'ptbk coder run --harness github-copilot --model gpt-5.5 --agent agents/developer.book';
+
+/**
  * Command which runs the existing tests before coding and lets one repair prompt fix pre-existing failures.
  */
 export const TEST_BEFORE_FIX_COMMAND = 'ptbk coder run --harness claude-code --test npm test --test-before yes-and-fix';
diff --git a/changelog/_current-preversion.md b/changelog/_current-preversion.md
index aef4e7d685bfbdfdb5219a9085b5c783e45763d6..0472bd2a77800b791cdf50dd0fa491d4419d9440
--- a/changelog/_current-preversion.md
+++ b/changelog/_current-preversion.md
@@ -1,3 +1,14 @@
+-   Added agent-specific prompt routing to `ptbk coder`:
+
+    -   A ready `[ ]` prompt can now target the Book passed in `--agent` with a backtick-delimited path, filename,
+        filename without `.book`, or the Book title from its first line. For example, a run using
+        `--agent agents/coding/developer.book` accepts `agents/coding/developer.book`, `developer.book`,
+        `developer`, and `Promptbook Developer` routing tokens.
+    -   Agent references work alongside the existing model and harness routing tokens, and `ptbk coder list --agent
+        <path.book>` previews the same agent-specific queue without starting a harness.
+    -   Added an Unslopper agent preset next to the developer preset in the local VS Code terminal scripts and
+        documented agent-specific routing on the `ptbk coder` landing page.
+
 -   The help of `ptbk` does not advertise the deprecated commands anymore. `run`, `login`, `make`, `prettify`, `test`,
     `list-models`, `list-scrapers`, `start-agents-server` and `start-pipelines-server` are all leftovers of the old
     pipeline system, yet nine of the fifteen commands which `ptbk --help` listed were exactly those, each one with its
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
index 4f48b6fb655c531e7a77d826befd0892dd198bcd..78bb2b22c209d5c18f376c23ef8da377af795b98
--- a/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.test.ts
@@ -161,6 +161,23 @@
         });
     });
 
+    it('parses an agent selection alongside the harness and model', () => {
+        const options = parseRunOptions([
+            '--harness',
+            'openai-codex',
+            '--model',
+            'gpt-5.6-astra',
+            '--agent',
+            'agents/coding/developer.book',
+        ]);
+
+        expect(options).toMatchObject({
+            agentName: 'openai-codex',
+            model: 'gpt-5.6-astra',
+            agent: 'agents/coding/developer.book',
+        });
+    });
+
     it('parses min and max priority filters', () => {
         const options = parseRunOptions([
             '--harness',
diff --git a/scripts/run-codex-prompts/cli/parseRunOptions.ts b/scripts/run-codex-prompts/cli/parseRunOptions.ts
index 2df919ba9c68e05fdf797319943b1fb228152cca..7c8d81c0cb0bfc8e69f21d154e7b5e696a6d0936
--- a/scripts/run-codex-prompts/cli/parseRunOptions.ts
+++ b/scripts/run-codex-prompts/cli/parseRunOptions.ts
@@ -26,7 +26,7 @@
  * CLI usage text for this script.
  */
 const USAGE =
-    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
+    'Usage: run-codex-prompts [--dry-run] [--harness <harness-name>] [--model <model>] [--agent <agent-book-path>] [--context <context-or-file>] [--test <test-command...>] [--test-before <no|yes-and-fail|yes-and-fix>] [--preserve-logs] [--isolate] [--no-ui] [--thinking-level <thinking-level>] [--priority <minimum-priority>] [--min-priority <minimum-priority>] [--max-priority <maximum-priority>] [--limit <run-count>] [--allow-credits] [--auto-migrate] [--allow-destructive-auto-migrate] [--wait-after-prompt <duration>] [--wait-between-prompts <duration>] [--wait-after-error <duration>] [--no-auto] [--no-commit] [--git-changes <fail|ignore|continue>] [--no-normalize-line-endings] [--auto-push] [--auto-pull]';
 
 /**
  * Top-level flags supported by this command.
@@ -35,6 +35,7 @@
     '--dry-run',
     '--harness',
     '--model',
+    '--agent',
     '--context',
     '--test',
     '--test-before',
@@ -76,6 +77,7 @@
     }
 
     const model = readOptionValue(args, '--model');
+    const agent = readOptionValue(args, '--agent');
     const context = readOptionValue(args, '--context');
     const hasTestCommandFlag = args.includes('--test');
     const testCommand = readVariadicOptionValue(args, '--test');
@@ -170,6 +172,7 @@
         noUi,
         agentName,
         model,
+        agent,
         context,
         testCommand,
         testBefore,
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34d0438053097a013350da231d787919bf11ee76
--- /dev/null
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
@@ -0,0 +1,44 @@
+import { mkdtemp, mkdir, rm, writeFile } from 'fs/promises';
+import { tmpdir } from 'os';
+import { join } from 'path';
+import { resolveCoderAgentBook } from './resolveCoderAgent';
+
+describe('resolveCoderAgentBook', () => {
+    let temporaryDirectoryPath: string;
+
+    beforeEach(async () => {
+        temporaryDirectoryPath = await mkdtemp(join(tmpdir(), 'promptbook-coder-agent-'));
+    });
+
+    afterEach(async () => {
+        await rm(temporaryDirectoryPath, { recursive: true, force: true });
+    });
+
+    it('returns undefined when no agent book is selected', async () => {
+        await expect(resolveCoderAgentBook(undefined, temporaryDirectoryPath)).resolves.toBeUndefined();
+    });
+
+    it('exposes the path, filename, stem and Book-title aliases for prompt routing', async () => {
+        const agentBookDirectoryPath = join(temporaryDirectoryPath, 'agents', 'coding');
+        await mkdir(agentBookDirectoryPath, { recursive: true });
+        await writeFile(
+            join(agentBookDirectoryPath, 'developer.book'),
+            'Developer Foo bar\n\nRULE Keep the implementation maintainable.\n',
+            'utf-8',
+        );
+
+        const resolvedAgentBook = await resolveCoderAgentBook(
+            'agents/coding/developer.book',
+            temporaryDirectoryPath,
+        );
+
+        expect(resolvedAgentBook?.agentReferences).toEqual(
+            expect.arrayContaining([
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ]),
+        );
+    });
+});
diff --git a/scripts/run-codex-prompts/common/resolveCoderAgent.ts b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
index af6757a43f57c1456d1ee91d25a80d9d7873b0f4..7c1ec7d13260ed74012fd97886f0ffbb180c14eb
--- a/scripts/run-codex-prompts/common/resolveCoderAgent.ts
+++ b/scripts/run-codex-prompts/common/resolveCoderAgent.ts
@@ -1,54 +1,128 @@
 import { readFile } from 'fs/promises';
-import { resolve } from 'path';
+import { basename, extname, relative, resolve } from 'path';
+import { parseAgentSource } from '../../../src/book-2.0/agent-source/parseAgentSource';
 import type { string_book } from '../../../src/book-2.0/agent-source/string_book';
 import { NotFoundError } from '../../../src/errors/NotFoundError';
 import { spaceTrim } from '../../../src/utils/organization/spaceTrim';
 import { createAgentRunnerSystemMessage } from '../../run-agent-messages/messages/createAgentRunnerSystemMessage';
 
 /**
- * Agent data resolved from the optional `--agent` book file.
+ * Source and prompt-routing references resolved from an optional `--agent` book file.
  */
-export type ResolvedCoderAgent = {
+export type ResolvedCoderAgentBook = {
     /**
      * Raw source of the agent `.book` file.
      */
     readonly agentSource: string_book;
 
     /**
+     * Equivalent references by which a prompt may target this agent.
+     *
+     * Includes the configured path, relative path, filename, filename without `.book`, and the
+     * normalized Book title from its first line.
+     */
+    readonly agentReferences: ReadonlyArray<string>;
+};
+
+/**
+ * Agent data resolved from the optional `--agent` book file.
+ */
+export type ResolvedCoderAgent = ResolvedCoderAgentBook & {
+    /**
      * Compiled system message injected into each coding prompt.
      */
     readonly systemMessage: string;
 };
 
 /**
- * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ * Reads an optional agent `.book` file and prepares the references that route prompts to it.
  *
  * Returns `undefined` when no agent path is provided.
  */
-export async function resolveCoderAgent(
-    agentPath: string | undefined,
+export async function resolveCoderAgentBook(
+    agentBookReference: string | undefined,
     currentWorkingDirectory: string,
-): Promise<ResolvedCoderAgent | undefined> {
-    if (!agentPath) {
+): Promise<ResolvedCoderAgentBook | undefined> {
+    const normalizedAgentBookReference = agentBookReference?.trim();
+
+    if (!normalizedAgentBookReference) {
         return undefined;
     }
 
-    const resolvedAgentPath = resolve(currentWorkingDirectory, agentPath);
-    const agentSource = await readFile(resolvedAgentPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
+    const resolvedAgentBookPath = resolve(currentWorkingDirectory, normalizedAgentBookReference);
+    const agentSource = (await readFile(resolvedAgentBookPath, 'utf-8').catch((error: NodeJS.ErrnoException) => {
         if (error.code === 'ENOENT' || error.code === 'EISDIR') {
             throw new NotFoundError(
                 spaceTrim(`
-                    Agent book \`${agentPath}\` was not found or is not a file.
+                    Agent book \`${normalizedAgentBookReference}\` was not found or is not a file.
 
                     Pass a path to a \`.book\` file in \`--agent\`.
                 `),
             );
         }
         throw error;
-    });
+    })) as string_book;
+
+    return {
+        agentSource,
+        agentReferences: createCoderAgentReferences({
+            agentBookReference: normalizedAgentBookReference,
+            resolvedAgentBookPath,
+            currentWorkingDirectory,
+            agentSource,
+        }),
+    };
+}
+
+/**
+ * Reads an optional agent `.book` file and compiles its system message for injection into coder prompts.
+ *
+ * Returns `undefined` when no agent path is provided.
+ */
+export async function resolveCoderAgent(
+    agentBookReference: string | undefined,
+    currentWorkingDirectory: string,
+): Promise<ResolvedCoderAgent | undefined> {
+    const resolvedAgentBook = await resolveCoderAgentBook(agentBookReference, currentWorkingDirectory);
+
+    if (resolvedAgentBook === undefined) {
+        return undefined;
+    }
 
     return {
-        agentSource: agentSource as string_book,
-        systemMessage: await createAgentRunnerSystemMessage(agentSource as string_book),
+        ...resolvedAgentBook,
+        systemMessage: await createAgentRunnerSystemMessage(resolvedAgentBook.agentSource),
     };
 }
+
+/**
+ * Creates the path and Book-title aliases which a prompt can use to target one selected agent.
+ *
+ * @private internal utility of `resolveCoderAgentBook`
+ */
+function createCoderAgentReferences(options: {
+    readonly agentBookReference: string;
+    readonly resolvedAgentBookPath: string;
+    readonly currentWorkingDirectory: string;
+    readonly agentSource: string_book;
+}): string[] {
+    const relativeAgentBookPath = relative(options.currentWorkingDirectory, options.resolvedAgentBookPath).replaceAll(
+        '\\',
+        '/',
+    );
+    const agentBookFileName = basename(options.resolvedAgentBookPath);
+    const agentBookName = basename(options.resolvedAgentBookPath, extname(options.resolvedAgentBookPath));
+    const agentNameFromBook = parseAgentSource(options.agentSource).agentName;
+
+    return Array.from(
+        new Set(
+            [
+                options.agentBookReference,
+                relativeAgentBookPath,
+                agentBookFileName,
+                agentBookName,
+                agentNameFromBook,
+            ].filter((agentReference) => agentReference.trim() !== ''),
+        ),
+    );
+}
diff --git a/scripts/run-codex-prompts/main/listCoderPrompts.ts b/scripts/run-codex-prompts/main/listCoderPrompts.ts
index ee428dc07371526935993b32a975ef35fc34eb4e..bb931095632649433eff5a0add8946de8623e610
--- a/scripts/run-codex-prompts/main/listCoderPrompts.ts
+++ b/scripts/run-codex-prompts/main/listCoderPrompts.ts
@@ -12,7 +12,7 @@
  */
 export type ListCoderPromptsOptions = PriorityFilterInput & {
     /**
-     * Optional harness and model selection used to omit prompts routed to other runners.
+     * Optional harness, model and Book-agent selection used to omit prompts routed to other runners.
      */
     readonly promptRunnerIdentity?: PromptRunnerIdentity;
 };
diff --git a/scripts/run-codex-prompts/main/runCodexPrompts.ts b/scripts/run-codex-prompts/main/runCodexPrompts.ts
index a921a7dd70f85065b4835b3b34e7519e02872616..b0b8ba8c99e4edfbf7da62d2ff977c630517b529
--- a/scripts/run-codex-prompts/main/runCodexPrompts.ts
+++ b/scripts/run-codex-prompts/main/runCodexPrompts.ts
@@ -118,6 +118,7 @@
         const promptRunnerIdentity: PromptRunnerIdentity = {
             harnessName: options.agentName,
             modelName: actualRunnerModel,
+            agentReferences: resolvedCoderAgent?.agentReferences,
         };
         console.info(colors.green(`Running prompts with ${runner.name}`));
 
@@ -856,7 +857,7 @@
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
         announceRunCompletion(
-            'No prompts match the selected harness or model.',
+            'No prompts match the selected harness, model or agent.',
             colors.yellow,
             isRichUiEnabled,
             uiHandle,
@@ -925,7 +926,7 @@
     let message: string;
 
     if (promptQueueSnapshot.stats.forAgent > 0) {
-        message = 'No prompts match the selected harness or model. Watching for changes...';
+        message = 'No prompts match the selected harness, model or agent. Watching for changes...';
     } else if (promptQueueSnapshot.stats.toBeWritten > 0) {
         message = 'No prompts ready for agent. Watching for changes...';
     } else {
diff --git a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
index 7cb024b84a60003112a4988e49583c923ee38b6b..256ff77484933ef8a3af672ecc18010d33366e1f
--- a/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
+++ b/scripts/run-codex-prompts/prompts/isPromptCompatibleWithRunner.ts
@@ -3,7 +3,7 @@
 import type { PromptSection } from './types/PromptSection';
 
 /**
- * Names of the harness and model currently selected for a coder run.
+ * Names of the harness, model and Book agent currently selected for a coder run.
  */
 export type PromptRunnerIdentity = {
     /**
@@ -14,14 +14,19 @@
      * Effective model identifier, for example `gpt-5.5`.
      */
     readonly modelName?: string;
+    /**
+     * Equivalent agent path and name references, including the Book title from its first line.
+     */
+    readonly agentReferences?: ReadonlyArray<string>;
 };
 
 /**
- * Checks whether a prompt is unrestricted or matches the selected harness/model.
+ * Checks whether a prompt is unrestricted or matches the selected harness, model or Book agent.
  *
- * A prompt status line can contain one or more backtick-delimited model or harness
- * names. Matching is intentionally based on normalized substrings so a token such as
- * `gpt` selects any `gpt-*` model and `opus` selects a `claude-opus-*` model.
+ * A prompt status line can contain one or more backtick-delimited model, harness or agent
+ * references. Matching is intentionally based on normalized substrings so a token such as
+ * `gpt` selects any `gpt-*` model, `developer` selects an agent Book named `developer.book`,
+ * and `opus` selects a `claude-opus-*` model.
  */
 export function isPromptCompatibleWithRunner(
     file: PromptFile,
@@ -39,7 +44,11 @@
         return true;
     }
 
-    const normalizedRunnerNames = [promptRunnerIdentity.harnessName, promptRunnerIdentity.modelName]
+    const normalizedRunnerNames = [
+        promptRunnerIdentity.harnessName,
+        promptRunnerIdentity.modelName,
+        ...(promptRunnerIdentity.agentReferences ?? []),
+    ]
         .filter((name): name is string => name !== undefined && name.trim() !== '')
         .map((name) => normalizeToKebabCase(name))
         .filter((name) => name !== '');
@@ -57,7 +66,7 @@
 }
 
 /**
- * Extracts model and harness tokens from a prompt status line.
+ * Extracts model, harness and Book-agent tokens from a prompt status line.
  */
 export function extractPromptRunnerTokens(statusLine: string): string[] {
     return Array.from(statusLine.matchAll(/`([^`]+)`/gu))
diff --git a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
index 886187d92c2fb2bc74e6876fd9c3b538941ed96c..8726dbdbeb2b6709dca0fa4ee2901fee45a26fe0
--- a/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
+++ b/scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
@@ -80,6 +80,51 @@
         expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3]);
     });
 
+    it('matches agent path aliases and Book titles alongside harness and model tokens', () => {
+        const file = parsePromptFile(
+            'prompts/runner-filtering.md',
+            spaceTrim(`
+                [ ] use \`agents/coding/developer.book\`
+                Full agent path
+                ---
+                [ ] use \`developer.book\`
+                Agent filename
+                ---
+                [ ] use \`developer\`
+                Agent filename without extension
+                ---
+                [ ] use \`Developer Foo bar\`
+                Agent Book title
+                ---
+                [ ] use \`openai-codex\`
+                Harness alongside agent routing
+                ---
+                [ ] use \`gpt-5.6-astra\`
+                Model alongside agent routing
+                ---
+                [ ] use \`unslopper\`
+                Different agent
+            `),
+        );
+
+        const runnable = listRunnablePrompts(
+            [file],
+            {},
+            {
+                harnessName: 'openai-codex',
+                modelName: 'gpt-5.6-astra',
+                agentReferences: [
+                    'agents/coding/developer.book',
+                    'developer.book',
+                    'developer',
+                    'developer-foo-bar',
+                ],
+            },
+        );
+
+        expect(runnable.map(({ section }) => section.index)).toEqual([0, 1, 2, 3, 4, 5]);
+    });
+
     it('does not constrain prompts when no runner identity is provided', () => {
         const file = parsePromptFile(
             'prompts/runner-filtering.md',
diff --git a/src/cli/cli-commands/coder/agentCliOptions.ts b/src/cli/cli-commands/coder/agentCliOptions.ts
new file mode 100644
index 0000000000000000000000000000000000000000..34faf19f649ae8f6913982a901f0b9d55be9e5b5
--- /dev/null
+++ b/src/cli/cli-commands/coder/agentCliOptions.ts
@@ -0,0 +1,32 @@
+import type {
+    Command as Program /* <- Note: [🔸] Using Program because Command is misleading name */,
+} from 'commander';
+
+/**
+ * Commander option bag for an optional Book agent used by coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export type CoderAgentCliOptions = {
+    readonly agent?: string;
+};
+
+/**
+ * Description shared by coder commands which accept an agent Book.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export const CODER_AGENT_OPTION_DESCRIPTION =
+    'Path to a .book file used to personalize coding prompts and select agent-specific tasks';
+
+/**
+ * Registers the shared `--agent` option for coder commands.
+ *
+ * @private internal utility of `ptbk coder`
+ */
+export function addCoderAgentOption(command: Program): void {
+    command.option('--agent <agent-book-path>', CODER_AGENT_OPTION_DESCRIPTION);
+}
+
+// Note: [🟡] Code for CLI coder agent options should never be published outside of `@promptbook/cli`
+// Note: [💞] Ignore a discrepancy between file name and exported helper names
diff --git a/src/cli/cli-commands/coder/list.test.ts b/src/cli/cli-commands/coder/list.test.ts
index bb0672fb64df23c2b1647451c2ff4ead9df35dfe..0aa8e33d791bd72b0d62fba7a3059f44a39ca140
--- a/src/cli/cli-commands/coder/list.test.ts
+++ b/src/cli/cli-commands/coder/list.test.ts
@@ -1,4 +1,6 @@
 import { Command } from 'commander';
+import type { string_book } from '../../../book-2.0/agent-source/string_book';
+import { resolveCoderAgentBook } from '../../../../scripts/run-codex-prompts/common/resolveCoderAgent';
 import { listCoderPrompts } from '../../../../scripts/run-codex-prompts/main/listCoderPrompts';
 import { $initializeCoderListCommand } from './list';
 
@@ -6,6 +8,10 @@
     listCoderPrompts: jest.fn(),
 }));
 
+jest.mock('../../../../scripts/run-codex-prompts/common/resolveCoderAgent', () => ({
+    resolveCoderAgentBook: jest.fn(),
+}));
+
 /**
  * Typed Jest mock for the read-only coder prompt listing entrypoint.
  */
@@ -14,6 +20,13 @@
 }
 
 /**
+ * Typed Jest mock for the selected agent Book resolver.
+ */
+function getResolveCoderAgentBookMock(): jest.MockedFunction<typeof resolveCoderAgentBook> {
+    return resolveCoderAgentBook as jest.MockedFunction<typeof resolveCoderAgentBook>;
+}
+
+/**
  * Creates a Commander program with the `coder list` subcommand registered.
  */
 function createProgramWithListCommand(): Command {
@@ -36,6 +49,7 @@
 
     beforeEach(() => {
         getListCoderPromptsMock().mockResolvedValue(0);
+        getResolveCoderAgentBookMock().mockResolvedValue(undefined);
         processExitSpy = jest.spyOn(process, 'exit').mockImplementation((() => undefined) as never);
     });
 
@@ -102,6 +116,38 @@
         );
     });
 
+    it('passes selected agent references to the prompt listing', async () => {
+        getResolveCoderAgentBookMock().mockResolvedValue({
+            agentSource: 'Developer Foo bar' as string_book,
+            agentReferences: [
+                'agents/coding/developer.book',
+                'developer.book',
+                'developer',
+                'developer-foo-bar',
+            ],
+        });
+        const program = createProgramWithListCommand();
+
+        await program.parseAsync(['node', 'test', 'list', '--agent', 'agents/coding/developer.book'], {
+            from: 'node',
+        });
+
+        expect(getListCoderPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                promptRunnerIdentity: {
+                    harnessName: undefined,
+                    modelName: undefined,
+                    agentReferences: [
+                        'agents/coding/developer.book',
+                        'developer.book',
+                        'developer',
+                        'developer-foo-bar',
+                    ],
+                },
+            }),
+        );
+    });
+
     it('passes --priority as the legacy minimum-priority alias', async () => {
         const program = createProgramWithListCommand();
 
diff --git a/src/cli/cli-commands/coder/list.ts b/src/cli/cli-commands/coder/list.ts
index b0ca04b05adf95d348aa4e3c4d067583bb00f012..3d064ae9e521c4b037dc2f0c13792fea0ea5bb91
--- a/src/cli/cli-commands/coder/list.ts
+++ b/src/cli/cli-commands/coder/list.ts
@@ -11,6 +11,7 @@
     normalizePromptRunnerSelectionCliOptions,
     PROMPT_RUNNER_DESCRIPTION,
 } from '../common/promptRunnerCliOptions';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 
 /**
  * Initializes `coder list` command for Promptbook CLI utilities.
@@ -30,35 +31,46 @@
             Features:
             - Lists only ready, fully authored prompts
             - Groups prompts from highest to lowest priority
-            - Optional --harness and --model filters show only prompts compatible with that runner
+            - Optional --harness, --model and --agent filters show only prompts compatible with that selection
             - Does not start a coding harness or modify prompt files
         `),
     );
 
     addPromptRunnerSelectionOptions(command);
+    addCoderAgentOption(command);
     addPromptPriorityOptions(command);
 
     command.action(
         handleActionErrors(async (cliOptions) => {
             const {
+                agent,
                 priority,
                 minPriority: minimumPriority,
                 maxPriority: maximumPriority,
             } = cliOptions as {
+                readonly agent?: string;
                 readonly priority?: number;
                 readonly minPriority?: number;
                 readonly maxPriority?: number;
-            } & PromptRunnerSelectionCliOptions;
+            } & PromptRunnerSelectionCliOptions & CoderAgentCliOptions;
             const runnerOptions = normalizePromptRunnerSelectionCliOptions(
                 cliOptions as PromptRunnerSelectionCliOptions,
                 { isAgentRequired: false },
             );
+            const resolvedCoderAgentBook =
+                agent === undefined
+                    ? undefined
+                    : await (await import('../../../../scripts/run-codex-prompts/common/resolveCoderAgent'))
+                          .resolveCoderAgentBook(agent, process.cwd());
             const promptRunnerIdentity =
-                runnerOptions.agentName === undefined && runnerOptions.model === undefined
+                runnerOptions.agentName === undefined &&
+                runnerOptions.model === undefined &&
+                resolvedCoderAgentBook === undefined
                     ? undefined
                     : {
                           harnessName: runnerOptions.agentName,
                           modelName: runnerOptions.model,
+                          agentReferences: resolvedCoderAgentBook?.agentReferences,
                       };
 
             // Note: Import dynamically to avoid loading prompt parsing dependencies until this command is used.
diff --git a/src/cli/cli-commands/coder/run.test.ts b/src/cli/cli-commands/coder/run.test.ts
index e0131ffeba3908dd7f1e0594dfd36055c21f803c..0671755703690b8fd7f63eae0fa41e825b7a4e1a
--- a/src/cli/cli-commands/coder/run.test.ts
+++ b/src/cli/cli-commands/coder/run.test.ts
@@ -294,6 +294,35 @@
         );
     });
 
+    it('passes an agent selection alongside the harness and model', async () => {
+        const program = createProgramWithRunCommand();
+
+        await program.parseAsync(
+            [
+                'node',
+                'test',
+                'run',
+                '--dry-run',
+                '--harness',
+                'openai-codex',
+                '--model',
+                'gpt-5.6-astra',
+                '--agent',
+                'agents/coding/developer.book',
+            ],
+            { from: 'node' },
+        );
+
+        expect(getRunCodexPromptsMock()).toHaveBeenCalledWith(
+            expect.objectContaining({
+                dryRun: true,
+                agentName: 'openai-codex',
+                model: 'gpt-5.6-astra',
+                agent: 'agents/coding/developer.book',
+            }),
+        );
+    });
+
     it('passes the verification command through when provided as unquoted tokens', async () => {
         const program = createProgramWithRunCommand();
 
diff --git a/src/cli/cli-commands/coder/run.ts b/src/cli/cli-commands/coder/run.ts
index 2335ef2fab5955db60e4623c83306d2162013831..41382d38f6a8d507d72bc5f5f08ac2c248faf437
--- a/src/cli/cli-commands/coder/run.ts
+++ b/src/cli/cli-commands/coder/run.ts
@@ -32,6 +32,7 @@
 } from '../../../../scripts/run-codex-prompts/testing/TestBeforeMode';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -79,10 +80,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -196,7 +194,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const configuredTestCommand = normalizeCommandOptionValue(test);
             const testCommand = configuredTestCommand ?? (testBefore === 'no' ? undefined : DEFAULT_CODER_TEST_COMMAND);
diff --git a/src/cli/cli-commands/coder/server.ts b/src/cli/cli-commands/coder/server.ts
index a3b4d380b5968b9e91f7da69d979a1b6e8152ff2..3a48ebbfbc1f54f39b9b84ace9cdae30e2ae5f68
--- a/src/cli/cli-commands/coder/server.ts
+++ b/src/cli/cli-commands/coder/server.ts
@@ -28,6 +28,7 @@
 import { addPromptPriorityOptions } from '../common/promptPriorityCliOptions';
 import { DEFAULT_WAIT_AFTER_ERROR_MS, parseOptionalWaitDuration } from './waitOptions';
 import { $ensureCoderHarnessGitignoreRules } from './$ensureCoderHarnessGitignoreRules';
+import { addCoderAgentOption, type CoderAgentCliOptions } from './agentCliOptions';
 import { printCoderRunFailure } from './printCoderRunFailure';
 
 /**
@@ -80,10 +81,7 @@
     command.option('--dry-run', 'Print unwritten prompts without executing', false);
     addPromptRunnerSelectionOptions(command);
     addQuestionsOption(command);
-    command.option(
-        '--agent <agent-book-path>',
-        'Path to a .book file whose compiled system message is prepended to each coding prompt',
-    );
+    addCoderAgentOption(command);
     command.option(
         '--context <context-or-file>',
         'Append extra instructions either inline or from a file path relative to the current project',
@@ -168,7 +166,7 @@
                 readonly auto: boolean;
                 readonly autoMigrate: boolean;
                 readonly allowDestructiveAutoMigrate: boolean;
-            } & PromptRunnerCliOptions;
+            } & PromptRunnerCliOptions & CoderAgentCliOptions;
 
             const port = parseCoderServerPort(rawPort);
             const testCommand = normalizeCommandOptionValue(test);

tokens used
621,669
Implemented agent-specific prompt routing for `ptbk coder`.

- `--agent` now routes prompts by Book path, filename, stem, or first-line title, alongside harness/model selection.
- Added matching support to `run`, `server`, and `coder list --agent`.
- Added the Unslopper VS Code terminal preset.
- Updated coder-landing content and changelog.

Validated with `npm test`, type/lint/spell checks, landing build, and focused Jest tests.

Note: I left the pre-existing change in `prompts/2026-09-0210-ptbk-coder-by-agent.md` untouched.

=== runner shell finished at 2026-09-12T10:09:54.554Z ===
Status: succeeded

=== test shell started at 2026-09-12T10:09:54.682Z ===
Script path: /c/Users/me/work/ai/promptbook/.promptbook/coder-prompts/2026-09-0210-ptbk-coder-by-agent.test.sh

--- raw input ---
cd "/c/Users/me/work/ai/promptbook"
npm run test-for-ptbk-coder

--- raw output ---

> promptbook@0.114.0-33 test-for-ptbk-coder
> node ./scripts/test-for-ptbk-coder.js

Note: Running from most lightweight to heaviest tests

> promptbook@0.114.0-33 test-name-discrepancies
> ts-node ./scripts/find-name-discrepancies/find-name-discrepancies.ts

🔎🩹 Find name discrepancies
[32mNo name discrepancies found[39m

> promptbook@0.114.0-33 test-spellcheck
> bash ./scripts/spellcheck.sh

   1/2072 src/_packages/anthropic-claude.index.ts 1193.63ms
   2/2072 src/_packages/anthropic-claude.readme.md 165.86ms
   3/2072 src/_packages/azure-openai.index.ts 8.31ms
   4/2072 src/_packages/azure-openai.readme.md 19.42ms
   5/2072 src/_packages/browser.index.ts 7.54ms
   6/2072 src/_packages/browser.readme.md 17.88ms
   7/2072 src/_packages/cli.index.ts 13.83ms
   8/2072 src/_packages/cli.readme.md 29.37ms
   9/2072 src/_packages/color.index.ts 7.61ms
  10/2072 src/_packages/color.readme.md 18.01ms
  11/2072 src/_packages/components.index.ts 21.76ms
  12/2072 src/_packages/components.readme.md 24.52ms
  13/2072 src/_packages/content/$provideExecutionToolsForNode.md 7.76ms
  14/2072 src/_packages/content/providers.md 4.79ms
  15/2072 src/_packages/core.index.ts 51.27ms
  16/2072 src/_packages/core.readme.md 32.51ms
  17/2072 src/_packages/deepseek.index.ts 3.44ms
  18/2072 src/_packages/deepseek.readme.md 2.92ms
  19/2072 src/_packages/documents.index.ts 3.86ms
  20/2072 src/_packages/documents.readme.md 2.72ms
  21/2072 src/_packages/editable.index.ts 10.83ms
  22/2072 src/_packages/editable.readme.md 3.74ms
  23/2072 src/_packages/fake-llm.index.ts 2.52ms
  24/2072 src/_packages/fake-llm.readme.md 1.98ms
  25/2072 src/_packages/google.index.ts 3.79ms
  26/2072 src/_packages/google.readme.md 9.47ms
  27/2072 src/_packages/javascript.index.ts 5.67ms
  28/2072 src/_packages/javascript.readme.md 4.31ms
  29/2072 src/_packages/legacy-documents.index.ts 3.71ms
  30/2072 src/_packages/legacy-documents.readme.md 1.91ms
  31/2072 src/_packages/markdown-utils.index.ts 7.09ms
  32/2072 src/_packages/markdown-utils.readme.md 14.60ms
  33/2072 src/_packages/markitdown.index.ts 3.26ms
  34/2072 src/_packages/markitdown.readme.md 4.74ms
  35/2072 src/_packages/node.index.ts 10.40ms
  36/2072 src/_packages/node.readme.md 12.80ms
  37/2072 src/_packages/ollama.index.ts 5.33ms
  38/2072 src/_packages/ollama.readme.md 14.58ms
  39/2072 src/_packages/openai.index.ts 7.49ms
  40/2072 src/_packages/openai.readme.md 18.30ms
  41/2072 src/_packages/pdf.index.ts 5.15ms
  42/2072 src/_packages/pdf.readme.md 2.58ms
  43/2072 src/_packages/promptbook.readme.md 1.75ms
  44/2072 src/_packages/ptbk.readme.md 1.62ms
  45/2072 src/_packages/README.md 2.84ms
  46/2072 src/_packages/remote-client.index.ts 12.86ms
  47/2072 src/_packages/remote-client.readme.md 1.99ms
  48/2072 src/_packages/remote-server.index.ts 4.05ms
  49/2072 src/_packages/remote-server.readme.md 5.71ms
  50/2072 src/_packages/templates.index.ts 3.83ms
  51/2072 src/_packages/templates.readme.md 2.24ms
  52/2072 src/_packages/types.index.ts 102.17ms
  53/2072 src/_packages/types.readme.md 27.06ms
  54/2072 src/_packages/utils.index.ts 17.91ms
  55/2072 src/_packages/utils.readme.md 28.13ms
  56/2072 src/_packages/vercel.index.ts 3.10ms
  57/2072 src/_packages/vercel.readme.md 7.81ms
  58/2072 src/_packages/website-crawler.index.ts 3.12ms
  59/2072 src/_packages/website-crawler.readme.md 1.71ms
  60/2072 src/_packages/wizard.index.ts 8.23ms
  61/2072 src/_packages/wizard.readme.md 3.44ms
  62/2072 src/avatars/Avatar.tsx 36.15ms
  63/2072 src/avatars/avatarAnimationScheduler.ts 10.74ms
  64/2072 src/avatars/avatarInteractionUtils.test.ts 7.11ms
  65/2072 src/avatars/avatarInteractionUtils.ts 13.84ms
  66/2072 src/avatars/AvatarOrImage.tsx 6.14ms
  67/2072 src/avatars/avatarPointerTracking.ts 11.49ms
  68/2072 src/avatars/avatarRenderingUtils.test.ts 6.17ms
  69/2072 src/avatars/avatarRenderingUtils.ts 36.87ms
  70/2072 src/avatars/avatarVisibilityTracking.ts 10.75ms
  71/2072 src/avatars/index.ts 13.99ms
  72/2072 src/avatars/renderAvatarVisual.ts 16.43ms
  73/2072 src/avatars/renderAvatarVisualAsciiArt.ts 14.34ms
  74/2072 src/avatars/renderAvatarVisualTerminalText.test.ts 9.27ms
  75/2072 src/avatars/renderAvatarVisualTerminalText.ts 38.57ms
  76/2072 src/avatars/types/AvatarDefinition.ts 5.15ms
  77/2072 src/avatars/types/AvatarVisualDefinition.ts 8.46ms
  78/2072 src/avatars/visuals/asciiOctopusAvatarVisual.ts 46.64ms
  79/2072 src/avatars/visuals/avatar3dProjectionShared.ts 8.07ms
  80/2072 src/avatars/visuals/avatarVisualRegistry.test.ts 5.13ms
  81/2072 src/avatars/visuals/avatarVisualRegistry.ts 7.52ms
  82/2072 src/avatars/visuals/fractalAvatarVisual.ts 18.08ms
  83/2072 src/avatars/visuals/minecraft2AvatarVisual.ts 32.40ms
  84/2072 src/avatars/visuals/minecraftAvatarVisual.ts 22.89ms
  85/2072 src/avatars/visuals/minecraftAvatarVisualShared.ts 16.42ms
  86/2072 src/avatars/visuals/octopus2AvatarVisual.ts 11.93ms
  87/2072 src/avatars/visuals/octopus3AvatarVisual.test.ts 7.44ms
  88/2072 src/avatars/visuals/octopus3AvatarVisual.ts 40.80ms
  89/2072 src/avatars/visuals/octopus3d2AvatarVisual.ts 23.23ms
  90/2072 src/avatars/visuals/octopus3d3AvatarVisual.ts 47.13ms
  91/2072 src/avatars/visuals/octopus3d4AvatarVisual.ts 48.63ms
  92/2072 src/avatars/visuals/octopus3dAvatarVisual.ts 23.90ms
  93/2072 src/avatars/visuals/octopus3dAvatarVisualShared.ts 13.95ms
  94/2072 src/avatars/visuals/octopusAvatarVisual.ts 11.66ms
  95/2072 src/avatars/visuals/octopusAvatarVisualShared.test.ts 9.53ms
  96/2072 src/avatars/visuals/octopusAvatarVisualShared.ts 22.75ms
  97/2072 src/avatars/visuals/orbAvatarVisual.test.ts 6.97ms
  98/2072 src/avatars/visuals/orbAvatarVisual.ts 22.24ms
  99/2072 src/avatars/visuals/pixelArtAvatarVisual.ts 8.28ms
 100/2072 src/book-2.0/agent-source/AgentBasicInformation.ts 8.35ms
 101/2072 src/book-2.0/agent-source/AgentModelRequirements.ts 5.57ms
 102/2072 src/book-2.0/agent-source/AgentReferenceResolver.ts 3.15ms
 103/2072 src/book-2.0/agent-source/AgentSourceParseResult.ts 5.63ms
 104/2072 src/book-2.0/agent-source/agentSourceVisibility.test.ts 4.21ms
 105/2072 src/book-2.0/agent-source/agentSourceVisibility.ts 14.73ms
 106/2072 src/book-2.0/agent-source/BookEditable.ts 3.55ms
 107/2072 src/book-2.0/agent-source/communication-samples.test.ts 6.17ms
 108/2072 src/book-2.0/agent-source/computeAgentHash.test.ts 2.83ms
 109/2072 src/book-2.0/agent-source/computeAgentHash.ts 2.28ms
 110/2072 src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts 5.68ms
 111/2072 src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts 23.27ms
 112/2072 src/book-2.0/agent-source/createAgentModelRequirements.ts 10.96ms
 113/2072 src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts 13.63ms
 114/2072 src/book-2.0/agent-source/CreateAgentModelRequirementsOptions.ts 14.72ms
 115/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts 8.43ms
 116/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts 3.73ms
 117/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts 4.18ms
 118/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts 4.52ms
 119/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts 3.40ms
 120/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts 4.45ms
 121/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts 10.22ms
 122/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts 3.84ms
 123/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts 10.66ms
 124/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts 15.15ms
 125/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/filterCommitmentsForAgentModelRequirements.ts 11.05ms
 126/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/materializeInlineKnowledgeSources.ts 5.19ms
 127/2072 src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/ParsedAgentSourceWithCommitments.ts 2.73ms
 128/2072 src/book-2.0/agent-source/createCommitmentRegex.ts 3.56ms
 129/2072 src/book-2.0/agent-source/createDefaultAgentName.ts 2.92ms
 130/2072 src/book-2.0/agent-source/createTeamToolName.ts 4.40ms
 131/2072 src/book-2.0/agent-source/extractMetaLinks.ts 3.07ms
 132/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts 2.49ms
 133/2072 src/book-2.0/agent-source/extractOpenTeacherInstructions.ts 4.06ms
 134/2072 src/book-2.0/agent-source/getBookCommitmentLineType.ts 3.69ms
 135/2072 src/book-2.0/agent-source/normalizeAgentName.test.ts 3.69ms
 136/2072 src/book-2.0/agent-source/normalizeAgentName.ts 2.61ms
 137/2072 src/book-2.0/agent-source/padBook.ts 2.92ms
 138/2072 src/book-2.0/agent-source/parseAgentSource.import.test.ts 4.98ms
 139/2072 src/book-2.0/agent-source/parseAgentSource.test.ts 16.91ms
 140/2072 src/book-2.0/agent-source/parseAgentSource.ts 57.67ms
 141/2072 src/book-2.0/agent-source/parseAgentSource/applyMetaCommitment.ts 47.54ms
 142/2072 src/book-2.0/agent-source/parseAgentSource/consumeConversationSampleCommitment.ts 2.83ms
 143/2072 src/book-2.0/agent-source/parseAgentSource/createCapabilitiesFromCommitment.ts 31.38ms
 144/2072 src/book-2.0/agent-source/parseAgentSource/ensureMetaFullname.ts 3.06ms
 145/2072 src/book-2.0/agent-source/parseAgentSource/extractAgentProfileText.ts 3.34ms
 146/2072 src/book-2.0/agent-source/parseAgentSource/extractInitialMessage.ts 2.37ms
 147/2072 src/book-2.0/agent-source/parseAgentSource/extractParsedAgentProfile.ts 5.60ms
 148/2072 src/book-2.0/agent-source/parseAgentSource/ParseAgentSourceState.ts 2.43ms
 149/2072 src/book-2.0/agent-source/parseAgentSource/ParsedAgentProfile.ts 3.21ms
 150/2072 src/book-2.0/agent-source/parseAgentSourcePrelude.ts 4.72ms
 151/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts 3.77ms
 152/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts 5.59ms
 153/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.ts 9.40ms
 154/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts 3.55ms
 155/2072 src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts 3.18ms
 156/2072 src/book-2.0/agent-source/ParsedUnknownCommitment.ts 6.47ms
 157/2072 src/book-2.0/agent-source/parseParameters.ts 8.43ms
 158/2072 src/book-2.0/agent-source/parseTeamCommitment.ts 9.79ms
 159/2072 src/book-2.0/agent-source/pseudoAgentReferences.ts 9.75ms
 160/2072 src/book-2.0/agent-source/README.md 2.75ms
 161/2072 src/book-2.0/agent-source/removeCommentsFromSystemMessage.ts 3.84ms
 162/2072 src/book-2.0/agent-source/string_book.ts 5.78ms
 163/2072 src/book-2.0/agent-source/TeammateProfileResolver.ts 3.04ms
 164/2072 src/book-2.0/book-language-documentation/BookLanguageDocumentationExample.ts 4.68ms
 165/2072 src/book-2.0/book-language-documentation/bookLanguageDocumentationExamples.ts 5.98ms
 166/2072 src/book-2.0/book-language-documentation/BookLanguageManualDictionary.ts 12.12ms
 167/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts 11.42ms
 168/2072 src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.ts 23.77ms
 169/2072 src/book-2.0/book-language-documentation/englishBookLanguageManualDictionary.ts 22.24ms
 170/2072 src/book-2.0/book-language-documentation/getBookLanguageManualCommitmentGroups.ts 4.49ms
 171/2072 src/book-2.0/book-language-documentation/getBookLanguageManualDictionary.ts 4.02ms
 172/2072 src/book-2.0/book-language-documentation/getSafeCodeBlock.ts 2.69ms
 173/2072 src/book-2.0/book-language-documentation/renderCommitmentCatalogSection.ts 5.22ms
 174/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts 4.45ms
 175/2072 src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.ts 14.57ms
 176/2072 src/book-2.0/book-language-documentation/toStableAnchorId.ts 4.04ms
 177/2072 src/book-2.0/README.md 1.53ms
 178/2072 src/book-2.0/utils/generatePlaceholderAgentProfileImageUrl.ts 2.97ms
 179/2072 src/book-3.0/agentFolderPaths.ts 2.35ms
 180/2072 src/book-3.0/AgentMessageRunReport.test.ts 6.29ms
 181/2072 src/book-3.0/AgentMessageRunReport.ts 12.75ms
 182/2072 src/book-3.0/AgentPlannedMessagesSidecar.test.ts 4.63ms
 183/2072 src/book-3.0/AgentPlannedMessagesSidecar.ts 11.27ms
 184/2072 src/book-3.0/AgentTeamConversationWorkspace.ts 14.45ms
 185/2072 src/book-3.0/Book.test.ts 6.78ms
 186/2072 src/book-3.0/Book.ts 21.89ms
 187/2072 src/book-3.0/BookNodeAgentSource.ts 6.07ms
 188/2072 src/book-3.0/CliAgent.test.ts 19.97ms
 189/2072 src/book-3.0/CliAgent.ts 12.66ms
 190/2072 src/book-3.0/cliAgentEnv.ts 3.55ms
 191/2072 src/book-3.0/codexLoginMethod.ts 5.40ms
 192/2072 src/book-3.0/createAgentMessageSidecarBaseName.ts 2.77ms
 193/2072 src/book-3.0/describeAgentPlannedMessageSchedule.ts 6.33ms
 194/2072 src/book-3.0/LiteAgent.test.ts 9.17ms
 195/2072 src/book-3.0/LiteAgent.ts 21.54ms
 196/2072 src/book-components/_common/Dropdown/Dropdown.tsx 7.50ms
 197/2072 src/book-components/_common/HamburgerMenu/HamburgerMenu.tsx 3.68ms
 198/2072 src/book-components/_common/MenuHoisting/MenuHoistingContext.tsx 3.92ms
 199/2072 src/book-components/_common/Modal/Modal.tsx 3.28ms
 200/2072 src/book-components/_common/MonacoEditorWithShadowDom.tsx 4.48ms
 201/2072 src/book-components/_common/react-utils/classNames.ts 1.96ms
 202/2072 src/book-components/_common/react-utils/collectCssTextsForClass.tsx 4.95ms
 203/2072 src/book-components/_common/react-utils/escapeRegex.tsx 12.68ms
 204/2072 src/book-components/_common/Tooltip/Tooltip.tsx 37.03ms
 205/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChip.tsx 4.37ms
 206/2072 src/book-components/AvatarProfile/AvatarChip/AvatarChipFromSource.tsx 3.50ms
 207/2072 src/book-components/AvatarProfile/AvatarChip/index.ts 1.76ms
 208/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfile.tsx 5.27ms
 209/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileFromSource.tsx 3.00ms
 210/2072 src/book-components/AvatarProfile/AvatarProfile/AvatarProfileTooltip.tsx 3.63ms
 211/2072 src/book-components/AvatarProfile/README.md 1.80ms
 212/2072 src/book-components/BookEditor/BookEditor.tsx 32.43ms
 213/2072 src/book-components/BookEditor/BookEditorAboutPromptbookInformation.tsx 17.63ms
 214/2072 src/book-components/BookEditor/BookEditorActionbar.tsx 16.59ms
 215/2072 src/book-components/BookEditor/BookEditorBrowserConfig.ts 2.53ms
 216/2072 src/book-components/BookEditor/BookEditorForClient.tsx 6.65ms
 217/2072 src/book-components/BookEditor/BookEditorMonaco.tsx 30.09ms
 218/2072 src/book-components/BookEditor/BookEditorMonacoConstants.ts 2.55ms
 219/2072 src/book-components/BookEditor/BookEditorMonacoFormatting.ts 5.34ms
 220/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts 3.81ms
 221/2072 src/book-components/BookEditor/BookEditorMonacoTokenization.ts 12.55ms
 222/2072 src/book-components/BookEditor/BookEditorMonacoUploadPanel.tsx 9.51ms
 223/2072 src/book-components/BookEditor/BookEditorSyntaxTokenStyles.ts 6.81ms
 224/2072 src/book-components/BookEditor/BookEditorTheme.ts 3.02ms
 225/2072 src/book-components/BookEditor/createBookSourceHighlightStyles.ts 5.13ms
 226/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.browser.ts 2.35ms
 227/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts 6.58ms
 228/2072 src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.ts 8.11ms
 229/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts 4.92ms
 230/2072 src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.ts 9.45ms
 231/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts 4.67ms
 232/2072 src/book-components/BookEditor/createUnknownCommitmentDiagnostics.ts 4.04ms
 233/2072 src/book-components/BookEditor/highlightBookSourceToHtml.ts 8.14ms
 234/2072 src/book-components/BookEditor/useBookEditorMonacoDecorations.ts 6.90ms
 235/2072 src/book-components/BookEditor/useBookEditorMonacoDiagnostics.ts 4.35ms
 236/2072 src/book-components/BookEditor/useBookEditorMonacoInteractions.ts 15.07ms
 237/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts 11.62ms
 238/2072 src/book-components/BookEditor/useBookEditorMonacoLanguage.ts 21.21ms
 239/2072 src/book-components/BookEditor/useBookEditorMonacoLifecycle.ts 8.89ms
 240/2072 src/book-components/BookEditor/useBookEditorMonacoStyles.ts 6.13ms
 241/2072 src/book-components/BookEditor/useBookEditorMonacoUploads.ts 8.84ms
 242/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/bookEditorMonacoUploadTypes.ts 7.35ms
 243/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/clearScheduledTimer.ts 2.63ms
 244/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/enqueueFilesForUpload.ts 10.70ms
 245/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadEditorSync.ts 7.49ms
 246/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadItemsState.ts 7.48ms
 247/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadProgressQueue.ts 5.04ms
 248/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useBookEditorMonacoUploadQueue.ts 15.75ms
 249/2072 src/book-components/BookEditor/useBookEditorMonacoUploads/useCompletedUploadsAutoClear.ts 4.32ms
 250/2072 src/book-components/Chat/AgentChat/AgentChat.test.tsx 27.60ms
 251/2072 src/book-components/Chat/AgentChat/AgentChat.tsx 41.75ms
 252/2072 src/book-components/Chat/AgentChat/AgentChatProps.tsx 31.80ms
 253/2072 src/book-components/Chat/AgentChip/AgentChip.tsx 8.85ms
 254/2072 src/book-components/Chat/AgentChip/index.ts 2.31ms
 255/2072 src/book-components/Chat/Chat/Chat.tsx 34.72ms
 256/2072 src/book-components/Chat/Chat/ChatActionsBar.test.tsx 7.66ms
 257/2072 src/book-components/Chat/Chat/ChatActionsBar.tsx 13.17ms
 258/2072 src/book-components/Chat/Chat/ChatCitationModal.test.tsx 6.78ms
 259/2072 src/book-components/Chat/Chat/ChatCitationModal.tsx 15.21ms
 260/2072 src/book-components/Chat/Chat/chatCssClassNames.ts 6.36ms
 261/2072 src/book-components/Chat/Chat/ChatFeedbackStar.tsx 7.76ms
 262/2072 src/book-components/Chat/Chat/ChatImageAttachmentModal.tsx 6.19ms
 263/2072 src/book-components/Chat/Chat/ChatInputArea.test.tsx 13.61ms
 264/2072 src/book-components/Chat/Chat/ChatInputArea.tsx 41.50ms
 265/2072 src/book-components/Chat/Chat/ChatInputAreaDictationPanel.tsx 9.19ms
 266/2072 src/book-components/Chat/Chat/ChatInputUploadedFile.ts 3.14ms
 267/2072 src/book-components/Chat/Chat/ChatMessageAttachments.tsx 16.60ms
 268/2072 src/book-components/Chat/Chat/ChatMessageItem.test.tsx 26.17ms
 269/2072 src/book-components/Chat/Chat/ChatMessageItem.tsx 63.13ms
 270/2072 src/book-components/Chat/Chat/ChatMessageList.tsx 11.82ms
 271/2072 src/book-components/Chat/Chat/ChatMessageMap.tsx 19.71ms
 272/2072 src/book-components/Chat/Chat/ChatMessageRichContent.tsx 7.98ms
 273/2072 src/book-components/Chat/Chat/ChatMessageToolCallChips.tsx 9.03ms
 274/2072 src/book-components/Chat/Chat/ChatProps.tsx 47.14ms
 275/2072 src/book-components/Chat/Chat/ChatRatingModal.test.tsx 6.59ms
 276/2072 src/book-components/Chat/Chat/ChatRatingModal.tsx 17.99ms
 277/2072 src/book-components/Chat/Chat/ChatReplyPreview.tsx 5.41ms
 278/2072 src/book-components/Chat/Chat/ChatSelfLearningSummary.ts 8.25ms
 279/2072 src/book-components/Chat/Chat/ChatSoundToggle.tsx 14.34ms
 280/2072 src/book-components/Chat/Chat/ChatToolCallModal.test.tsx 17.56ms
 281/2072 src/book-components/Chat/Chat/ChatToolCallModal.tsx 28.52ms
 282/2072 src/book-components/Chat/Chat/ChatToolCallModalComponents.tsx 6.12ms
 283/2072 src/book-components/Chat/Chat/ChatToolCallModalContent.tsx 7.85ms
 284/2072 src/book-components/Chat/Chat/CitationIframePreview.test.tsx 5.25ms
 285/2072 src/book-components/Chat/Chat/CitationIframePreview.tsx 17.27ms
 286/2072 src/book-components/Chat/Chat/ClockIcon.tsx 4.47ms
 287/2072 src/book-components/Chat/Chat/constants.tsx 4.33ms
 288/2072 src/book-components/Chat/Chat/createChatMessageToolCallRenderModel.ts 14.93ms
 289/2072 src/book-components/Chat/Chat/createProgressCardChecklistMarkdown.ts 5.89ms
 290/2072 src/book-components/Chat/Chat/ImagePromptRenderer.tsx 9.03ms
 291/2072 src/book-components/Chat/Chat/insertDictationChunk.test.ts 2.79ms
 292/2072 src/book-components/Chat/Chat/insertDictationChunk.ts 4.19ms
 293/2072 src/book-components/Chat/Chat/learnDictationDictionary.test.ts 3.88ms
 294/2072 src/book-components/Chat/Chat/learnDictationDictionary.ts 10.85ms
 295/2072 src/book-components/Chat/Chat/pagePreview/createPagePreviewSessionId.ts 7.97ms
 296/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx 24.61ms
 297/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.tsx 31.59ms
 298/2072 src/book-components/Chat/Chat/pagePreview/LiveBrowserPreviewToolbar.tsx 24.30ms
 299/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts 9.34ms
 300/2072 src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.ts 8.75ms
 301/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputEvent.ts 5.78ms
 302/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts 5.02ms
 303/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.ts 6.66ms
 304/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewSessionState.ts 2.47ms
 305/2072 src/book-components/Chat/Chat/pagePreview/PagePreviewViewport.ts 4.01ms
 306/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts 3.56ms
 307/2072 src/book-components/Chat/Chat/refineFinalDictationChunk.ts 8.11ms
 308/2072 src/book-components/Chat/Chat/renderAdvancedToolCallDetails.tsx 26.23ms
 309/2072 src/book-components/Chat/Chat/renderAgentProjectDiff.tsx 5.81ms
 310/2072 src/book-components/Chat/Chat/renderAgentProjectToolCallDetails.tsx 18.37ms
 311/2072 src/book-components/Chat/Chat/renderEmailToolCallDetails.tsx 7.95ms
 312/2072 src/book-components/Chat/Chat/renderMemoryToolCallDetails.tsx 14.58ms
 313/2072 src/book-components/Chat/Chat/renderPopupToolCallDetails.tsx 13.66ms
 314/2072 src/book-components/Chat/Chat/renderRunBrowserToolCallDetails.tsx 20.95ms
 315/2072 src/book-components/Chat/Chat/renderSearchToolCallDetails.tsx 9.07ms
 316/2072 src/book-components/Chat/Chat/renderSelfLearningToolCallDetails.tsx 8.47ms
 317/2072 src/book-components/Chat/Chat/renderTimeoutToolCallDetails.tsx 20.36ms
 318/2072 src/book-components/Chat/Chat/renderTimeToolCallDetails.tsx 9.09ms
 319/2072 src/book-components/Chat/Chat/renderToolCallClockPanel.tsx 3.67ms
 320/2072 src/book-components/Chat/Chat/renderToolCallDetails.tsx 26.48ms
 321/2072 src/book-components/Chat/Chat/renderToolCallProgressPlaceholder.tsx 4.00ms
 322/2072 src/book-components/Chat/Chat/renderWalletCredentialToolCallDetails.tsx 13.58ms
 323/2072 src/book-components/Chat/Chat/resolveChatRatingMessageKey.ts 3.51ms
 324/2072 src/book-components/Chat/Chat/resolveRunBrowserToolCallDetailsState.ts 16.66ms
 325/2072 src/book-components/Chat/Chat/resolveSpeechRecognitionUiDescriptor.ts 4.50ms
 326/2072 src/book-components/Chat/Chat/resolveToolCallProgressMessage.ts 3.53ms
 327/2072 src/book-components/Chat/Chat/StreamingFeaturePlaceholder.tsx 4.88ms
 328/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx 9.98ms
 329/2072 src/book-components/Chat/Chat/TeamToolCallModalContent.tsx 31.11ms
 330/2072 src/book-components/Chat/Chat/useChatInputAreaAttachments.ts 6.32ms
 331/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx 4.80ms
 332/2072 src/book-components/Chat/Chat/useChatInputAreaComposer.ts 22.76ms
 333/2072 src/book-components/Chat/Chat/useChatInputAreaDictation.ts 75.87ms
 334/2072 src/book-components/Chat/Chat/useChatInputAreaDictationPersistence.ts 51.47ms
 335/2072 src/book-components/Chat/Chat/useChatInputAreaDictationSupport.ts 5.54ms
 336/2072 src/book-components/Chat/Chat/useChatMessageAvatarTooltip.ts 9.34ms
 337/2072 src/book-components/Chat/Chat/useChatMessageSpeechPlayback.ts 29.92ms
 338/2072 src/book-components/Chat/Chat/useChatPostprocessedMessages.ts 5.69ms
 339/2072 src/book-components/Chat/Chat/useChatScrollState.ts 13.98ms
 340/2072 src/book-components/Chat/Chat/useChatToolCallModalState.ts 14.64ms
 341/2072 src/book-components/Chat/Chat/useChatToolCallState.ts 9.25ms
 342/2072 src/book-components/Chat/CodeBlock/CodeBlock.tsx 8.98ms
 343/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts 2.31ms
 344/2072 src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.ts 3.01ms
 345/2072 src/book-components/Chat/effects/ChatEffectsSystem.tsx 7.72ms
 346/2072 src/book-components/Chat/effects/components/ConfettiEffect.tsx 4.55ms
 347/2072 src/book-components/Chat/effects/components/HeartsEffect.tsx 6.81ms
 348/2072 src/book-components/Chat/effects/configs/defaultEffectConfigs.ts 2.41ms
 349/2072 src/book-components/Chat/effects/index.ts 3.01ms
 350/2072 src/book-components/Chat/effects/types/ChatEffect.ts 2.21ms
 351/2072 src/book-components/Chat/effects/types/ChatEffectConfig.ts 2.24ms
 352/2072 src/book-components/Chat/effects/types/ChatEffectsSystemProps.ts 4.15ms
 353/2072 src/book-components/Chat/effects/types/ChatEffectType.ts 6.43ms
 354/2072 src/book-components/Chat/effects/utils/detectEffects.ts 3.14ms
 355/2072 src/book-components/Chat/hooks/index.ts 2.69ms
 356/2072 src/book-components/Chat/hooks/useChatActionsOverlap.ts 9.66ms
 357/2072 src/book-components/Chat/hooks/useChatAutoScroll.test.tsx 11.91ms
 358/2072 src/book-components/Chat/hooks/useChatAutoScroll.ts 17.09ms
 359/2072 src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx 13.40ms
 360/2072 src/book-components/Chat/hooks/useChatCompleteNotification.ts 7.28ms
 361/2072 src/book-components/Chat/hooks/useChatRatings.ts 17.18ms
 362/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx 6.51ms
 363/2072 src/book-components/Chat/hooks/useResolvedCitationLabel.ts 9.59ms
 364/2072 src/book-components/Chat/hooks/useSendMessageToLlmChat.ts 4.99ms
 365/2072 src/book-components/Chat/LlmChat/defaults.ts 2.43ms
 366/2072 src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts 2.54ms
 367/2072 src/book-components/Chat/LlmChat/LlmChat.test.tsx 15.30ms
 368/2072 src/book-components/Chat/LlmChat/LlmChat.tsx 6.92ms
 369/2072 src/book-components/Chat/LlmChat/LlmChatProps.tsx 12.61ms
 370/2072 src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts 23.39ms
 371/2072 src/book-components/Chat/LlmChat/useLlmChatMessages.ts 9.45ms
 372/2072 src/book-components/Chat/LlmChat/useLlmChatState.ts 14.25ms
 373/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx 22.77ms
 374/2072 src/book-components/Chat/MarkdownContent/MarkdownContent.tsx 33.25ms
 375/2072 src/book-components/Chat/MockedChat/constants.ts 10.64ms
 376/2072 src/book-components/Chat/MockedChat/MockedChat.tsx 36.09ms
 377/2072 src/book-components/Chat/README.md 9.04ms
 378/2072 src/book-components/Chat/save/_common/chatExportRendering.ts 11.51ms
 379/2072 src/book-components/Chat/save/_common/ChatSaveFormatDefinition.ts 5.81ms
 380/2072 src/book-components/Chat/save/_common/ChatSaveFormatHandler.ts 2.92ms
 381/2072 src/book-components/Chat/save/_common/createChatExportFilename.ts 2.60ms
 382/2072 src/book-components/Chat/save/_common/getChatSaveFormatDefinitions.ts 3.17ms
 383/2072 src/book-components/Chat/save/_common/getPromptbookExportBranding.ts 5.71ms
 384/2072 src/book-components/Chat/save/_common/string_chat_format_name.ts 2.06ms
 385/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts 6.75ms
 386/2072 src/book-components/Chat/save/html/htmlSaveFormatDefinition.ts 30.42ms
 387/2072 src/book-components/Chat/save/index.ts 3.34ms
 388/2072 src/book-components/Chat/save/json/jsonSaveFormatDefinition.ts 3.63ms
 389/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts 6.02ms
 390/2072 src/book-components/Chat/save/markdown/mdSaveFormatDefinition.ts 10.81ms
 391/2072 src/book-components/Chat/save/pdf/buildChatPdf.ts 17.00ms
 392/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts 16.36ms
 393/2072 src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.ts 4.03ms
 394/2072 src/book-components/Chat/save/react/exports/chat-preview-2025-10-13 (1).jsx 8.71ms
 395/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts 3.67ms
 396/2072 src/book-components/Chat/save/react/reactSaveFormatDefinition.ts 5.21ms
 397/2072 src/book-components/Chat/save/text/txtSaveFormatDefinition.ts 2.71ms
 398/2072 src/book-components/Chat/SourceChip/index.ts 1.58ms
 399/2072 src/book-components/Chat/SourceChip/SourceChip.test.tsx 8.27ms
 400/2072 src/book-components/Chat/SourceChip/SourceChip.tsx 9.55ms
 401/2072 src/book-components/Chat/types/ChatMessage.ts 13.22ms
 402/2072 src/book-components/Chat/types/ChatParticipant.ts 5.30ms
 403/2072 src/book-components/Chat/types/CitationLabelResolver.ts 2.31ms
 404/2072 src/book-components/Chat/utils/$provideServerDomWindow.ts 6.33ms
 405/2072 src/book-components/Chat/utils/addUtmParamsToUrl.ts 2.28ms
 406/2072 src/book-components/Chat/utils/agentProjectToolCall.test.ts 4.23ms
 407/2072 src/book-components/Chat/utils/agentProjectToolCall.ts 6.66ms
 408/2072 src/book-components/Chat/utils/ChatPersistence.test.ts 6.22ms
 409/2072 src/book-components/Chat/utils/ChatPersistence.ts 6.78ms
 410/2072 src/book-components/Chat/utils/citationHelpers.test.ts 9.07ms
 411/2072 src/book-components/Chat/utils/citationHelpers.ts 5.67ms
 412/2072 src/book-components/Chat/utils/collectTeamToolCallSummary.ts 7.82ms
 413/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts 4.78ms
 414/2072 src/book-components/Chat/utils/createCitationFootnoteRenderModel.ts 13.15ms
 415/2072 src/book-components/Chat/utils/createShortLinkForChat.ts 2.41ms
 416/2072 src/book-components/Chat/utils/createTeamToolNameFromUrl.ts 2.54ms
 417/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts 3.60ms
 418/2072 src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.ts 6.35ms
 419/2072 src/book-components/Chat/utils/downloadFile.ts 2.49ms
 420/2072 src/book-components/Chat/utils/exportChatHistory.ts 6.16ms
 421/2072 src/book-components/Chat/utils/ExportFormat.ts 1.89ms
 422/2072 src/book-components/Chat/utils/externalSourceToolCall.ts 4.22ms
 423/2072 src/book-components/Chat/utils/formatToolCallDateTime.ts 6.82ms
 424/2072 src/book-components/Chat/utils/formatToolCallLocalTime.ts 2.65ms
 425/2072 src/book-components/Chat/utils/formatToolCallTranslationTemplate.ts 2.34ms
 426/2072 src/book-components/Chat/utils/generatePdfContent.ts 5.87ms
 427/2072 src/book-components/Chat/utils/generateQrDataUrl.ts 2.24ms
 428/2072 src/book-components/Chat/utils/getChatMessageTimingDisplay.ts 4.32ms
 429/2072 src/book-components/Chat/utils/getPromptbookBranding.ts 2.33ms
 430/2072 src/book-components/Chat/utils/getToolCallChipletInfo.test.ts 4.20ms
 431/2072 src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts 9.46ms
 432/2072 src/book-components/Chat/utils/getToolCallChipletInfo.ts 27.38ms
 433/2072 src/book-components/Chat/utils/isVisibleChatToolCall.ts 2.47ms
 434/2072 src/book-components/Chat/utils/loadAgentProfile.ts 7.67ms
 435/2072 src/book-components/Chat/utils/messagesToHtml.ts 8.02ms
 436/2072 src/book-components/Chat/utils/messagesToJson.ts 3.47ms
 437/2072 src/book-components/Chat/utils/messagesToMarkdown.ts 5.10ms
 438/2072 src/book-components/Chat/utils/messagesToText.ts 3.12ms
 439/2072 src/book-components/Chat/utils/parseCitationMarker.ts 9.53ms
 440/2072 src/book-components/Chat/utils/parseCitationsFromContent.test.ts 4.19ms
 441/2072 src/book-components/Chat/utils/parseCitationsFromContent.ts 6.18ms
 442/2072 src/book-components/Chat/utils/parseImagePrompts.test.ts 2.72ms
 443/2072 src/book-components/Chat/utils/parseImagePrompts.ts 51.76ms
 444/2072 src/book-components/Chat/utils/parseMessageButtons.test.ts 20.97ms
 445/2072 src/book-components/Chat/utils/parseMessageButtons.ts 21.84ms
 446/2072 src/book-components/Chat/utils/renderMarkdown.test.ts 46.21ms
 447/2072 src/book-components/Chat/utils/renderMarkdown.ts 130.17ms
 448/2072 src/book-components/Chat/utils/resolveChatMessageReplyPreviewText.ts 5.40ms
 449/2072 src/book-components/Chat/utils/resolveChatMessageReplySenderLabel.ts 3.33ms
 450/2072 src/book-components/Chat/utils/resolveCitationSourceDisplay.ts 24.38ms
 451/2072 src/book-components/Chat/utils/resolveCitationUrl.test.ts 2.37ms
 452/2072 src/book-components/Chat/utils/resolveCitationUrl.ts 4.77ms
 453/2072 src/book-components/Chat/utils/resolveToolCallFromChatMessages.ts 3.90ms
 454/2072 src/book-components/Chat/utils/resolveToolCallState.ts 2.74ms
 455/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts 3.85ms
 456/2072 src/book-components/Chat/utils/sanitizeStreamingMessageContent.ts 7.55ms
 457/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts 3.59ms
 458/2072 src/book-components/Chat/utils/splitMessageContentIntoSegments.ts 11.69ms
 459/2072 src/book-components/Chat/utils/thinkingMessageVariants.test.ts 4.02ms
 460/2072 src/book-components/Chat/utils/thinkingMessageVariants.ts 4.76ms
 461/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts 5.24ms
 462/2072 src/book-components/Chat/utils/timeoutToolCallPresentation.ts 30.37ms
 463/2072 src/book-components/Chat/utils/toolCallParsing.test.ts 12.15ms
 464/2072 src/book-components/Chat/utils/toolCallParsing.ts 3.36ms
 465/2072 src/book-components/Chat/utils/toolCallParsing/extractSearchResults.ts 7.20ms
 466/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallResultDate.ts 5.68ms
 467/2072 src/book-components/Chat/utils/toolCallParsing/getToolCallTimestamp.ts 2.65ms
 468/2072 src/book-components/Chat/utils/toolCallParsing/parseRunBrowserToolResult.ts 18.03ms
 469/2072 src/book-components/Chat/utils/toolCallParsing/parseTeamToolResult.ts 7.31ms
 470/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallArguments.ts 7.13ms
 471/2072 src/book-components/Chat/utils/toolCallParsing/parseToolCallResult.ts 2.71ms
 472/2072 src/book-components/Chat/utils/toolCallParsing/resolveRunBrowserArtifactUrl.ts 3.16ms
 473/2072 src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts 4.86ms
 474/2072 src/book-components/Chat/utils/toolCallParsing/TeamToolResult.ts 6.02ms
 475/2072 src/book-components/Chat/utils/walletCredentialToolCall.test.ts 7.68ms
 476/2072 src/book-components/Chat/utils/walletCredentialToolCall.ts 12.97ms
 477/2072 src/book-components/fonts/README.md 1.63ms
 478/2072 src/book-components/icons/AboutIcon.tsx 3.26ms
 479/2072 src/book-components/icons/ArrowIcon.tsx 5.04ms
 480/2072 src/book-components/icons/AttachmentIcon.tsx 5.29ms
 481/2072 src/book-components/icons/CameraIcon.tsx 2.90ms
 482/2072 src/book-components/icons/CloseIcon.tsx 2.46ms
 483/2072 src/book-components/icons/DownloadIcon.tsx 5.84ms
 484/2072 src/book-components/icons/EmailIcon.tsx 3.23ms
 485/2072 src/book-components/icons/ExitFullscreenIcon.tsx 4.12ms
 486/2072 src/book-components/icons/FullscreenIcon.tsx 3.67ms
 487/2072 src/book-components/icons/MenuIcon.tsx 3.60ms
 488/2072 src/book-components/icons/MicIcon.tsx 3.04ms
 489/2072 src/book-components/icons/PauseIcon.tsx 2.20ms
 490/2072 src/book-components/icons/PlayIcon.tsx 1.97ms
 491/2072 src/book-components/icons/ResetIcon.tsx 2.56ms
 492/2072 src/book-components/icons/SaveIcon.tsx 8.30ms
 493/2072 src/book-components/icons/SendIcon.tsx 7.67ms
 494/2072 src/book-components/icons/SolidArrowButton.tsx 11.31ms
 495/2072 src/book-components/icons/StopIcon.tsx 2.29ms
 496/2072 src/book-components/icons/TeacherIcon.tsx 2.70ms
 497/2072 src/book-components/icons/TemplateIcon.tsx 8.99ms
 498/2072 src/book-components/PromptbookAgent/PromptbookAgentIntegration.tsx 6.56ms
 499/2072 src/book-components/PromptbookAgent/PromptbookAgentSeamlessIntegration.tsx 34.91ms
 500/2072 src/book-components/Qr/BrandedQrCode.tsx 2.63ms
 501/2072 src/book-components/Qr/GenericQrCode.tsx 2.47ms
 502/2072 src/book-components/Qr/PromptbookQrCode.tsx 15.85ms
 503/2072 src/book-components/Qr/useQrCode.ts 6.92ms
 504/2072 src/cli/$runPromptbookCli.ts 12.43ms
 505/2072 src/cli/cli-commands/_boilerplate.ts 3.31ms
 506/2072 src/cli/cli-commands/about.ts 9.13ms
 507/2072 src/cli/cli-commands/agent-folder.ts 5.22ms
 508/2072 src/cli/cli-commands/agent-folder/agentProjectPaths.ts 7.50ms
 509/2072 src/cli/cli-commands/agent-folder/agentRunCliOptions.ts 5.88ms
 510/2072 src/cli/cli-commands/agent-folder/init.ts 6.91ms
 511/2072 src/cli/cli-commands/agent-folder/initializeAgentProjectConfiguration.ts 9.91ms
 512/2072 src/cli/cli-commands/agent-folder/initializeAgentRunnerCommand.ts 11.82ms
 513/2072 src/cli/cli-commands/agent-folder/printAgentInitializationSummary.ts 7.15ms
 514/2072 src/cli/cli-commands/agent-folder/run.test.ts 16.53ms
 515/2072 src/cli/cli-commands/agent-folder/run.ts 6.12ms
 516/2072 src/cli/cli-commands/agent-folder/runMultiple.ts 6.13ms
 517/2072 src/cli/cli-commands/agent-folder/tick.ts 4.76ms
 518/2072 src/cli/cli-commands/agent.ts 4.14ms
 519/2072 src/cli/cli-commands/agent/agentCliOptions.ts 5.47ms
 520/2072 src/cli/cli-commands/agent/chat.ts 5.45ms
 521/2072 src/cli/cli-commands/agent/exec.ts 5.00ms
 522/2072 src/cli/cli-commands/agent/run.test.ts 11.53ms
 523/2072 src/cli/cli-commands/agents-server.ts 3.65ms
 524/2072 src/cli/cli-commands/agents-server/buildAgentsServer.test.ts 20.89ms
 525/2072 src/cli/cli-commands/agents-server/buildAgentsServer.ts 4.20ms
 526/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_FILENAME.ts 1.93ms
 527/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_BUILD_CACHE_VERSION.ts 2.92ms
 528/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AGENTS_SERVER_NEXT_BUILD_ID_FILENAME.ts 2.76ms
 529/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCache.ts 2.13ms
 530/2072 src/cli/cli-commands/agents-server/buildAgentsServer/AgentsServerBuildCacheOptions.ts 1.98ms
 531/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerBuildSourceFingerprint.ts 8.30ms
 532/2072 src/cli/cli-commands/agents-server/buildAgentsServer/createAgentsServerRuntimeEnvironment.ts 6.64ms
 533/2072 src/cli/cli-commands/agents-server/buildAgentsServer/ensureAgentsServerBuild.ts 4.83ms
 534/2072 src/cli/cli-commands/agents-server/buildAgentsServer/getAgentsServerBuildInputRelativePaths.ts 2.89ms
 535/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPath.ts 2.75ms
 536/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerAppPathMaterialized.ts 4.73ms
 537/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildCacheCurrent.ts 4.69ms
 538/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isAgentsServerBuildInputTestFile.ts 2.97ms
 539/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputDirectoryName.ts 3.84ms
 540/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerBuildInputPath.ts 3.80ms
 541/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isExcludedAgentsServerRuntimeSourcePath.ts 5.51ms
 542/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isFile.ts 6.46ms
 543/2072 src/cli/cli-commands/agents-server/buildAgentsServer/isPathInsideNodeModules.ts 2.39ms
 544/2072 src/cli/cli-commands/agents-server/buildAgentsServer/NODE_MODULES_DIRECTORY_NAME.ts 1.80ms
 545/2072 src/cli/cli-commands/agents-server/buildAgentsServer/normalizeAgentsServerBuildInputPath.ts 3.24ms
 546/2072 src/cli/cli-commands/agents-server/buildAgentsServer/prepareAgentsServerRuntime.ts 3.48ms
 547/2072 src/cli/cli-commands/agents-server/buildAgentsServer/PreparedAgentsServerRuntime.ts 4.50ms
 548/2072 src/cli/cli-commands/agents-server/buildAgentsServer/readAgentsServerBuildCache.ts 3.97ms
 549/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerAppPath.ts 3.73ms
 550/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildAppPath.ts 9.00ms
 551/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveAgentsServerBuildOutputPath.ts 2.71ms
 552/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNextCliPath.ts 3.72ms
 553/2072 src/cli/cli-commands/agents-server/buildAgentsServer/resolveNodeModulesPath.ts 2.78ms
 554/2072 src/cli/cli-commands/agents-server/buildAgentsServer/runNextBuild.ts 10.32ms
 555/2072 src/cli/cli-commands/agents-server/buildAgentsServer/shouldCopyAgentsServerRuntimePath.ts 4.51ms
 556/2072 src/cli/cli-commands/agents-server/buildAgentsServer/synchronizeMaterializedAgentsServerRuntime.ts 9.95ms
 557/2072 src/cli/cli-commands/agents-server/buildAgentsServer/writeAgentsServerBuildCache.ts 3.25ms
 558/2072 src/cli/cli-commands/agents-server/ensureAgentsServerEnvFile.ts 7.68ms
 559/2072 src/cli/cli-commands/agents-server/ensureAgentsServerGitignoreFile.ts 4.90ms
 560/2072 src/cli/cli-commands/agents-server/init.test.ts 10.62ms
 561/2072 src/cli/cli-commands/agents-server/init.ts 3.70ms
 562/2072 src/cli/cli-commands/agents-server/initializeAgentsServerProjectConfiguration.ts 5.21ms
 563/2072 src/cli/cli-commands/agents-server/printAgentsServerInitializationSummary.ts 4.89ms
 564/2072 src/cli/cli-commands/agents-server/run.test.ts 13.36ms
 565/2072 src/cli/cli-commands/agents-server/run.ts 9.52ms
 566/2072 src/cli/cli-commands/agents-server/startAgentsServer.ts 10.29ms
 567/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts 5.16ms
 568/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.ts 6.40ms
 569/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerLogStreams.ts 4.45ms
 570/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerRuntimePaths.ts 7.72ms
 571/2072 src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerSupervisorState.ts 5.37ms
 572/2072 src/cli/cli-commands/agents-server/startAgentsServer/createInternalRouteErrorMessage.ts 2.72ms
 573/2072 src/cli/cli-commands/agents-server/startAgentsServer/createLocalAgentRunOptions.ts 5.99ms
 574/2072 src/cli/cli-commands/agents-server/startAgentsServer/forwardChildOutput.ts 7.84ms
 575/2072 src/cli/cli-commands/agents-server/startAgentsServer/loadAgentsServerProjectEnvironment.ts 4.41ms
 576/2072 src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts 3.54ms
 577/2072 src/cli/cli-commands/agents-server/startAgentsServer/prepareAgentsServerLaunch.ts 6.70ms
 578/2072 src/cli/cli-commands/agents-server/startAgentsServer/PreparedAgentsServerLaunch.ts 2.85ms
 579/2072 src/cli/cli-commands/agents-server/startAgentsServer/readInternalRouteErrorDetails.ts 16.80ms
 580/2072 src/cli/cli-commands/agents-server/startAgentsServer/resolveAgentsServerChildHostname.ts 2.39ms
 581/2072 src/cli/cli-commands/agents-server/startAgentsServer/StartAgentsServerOptions.ts 5.20ms
 582/2072 src/cli/cli-commands/agents-server/startAgentsServer/startNextServer.ts 33.93ms
 583/2072 src/cli/cli-commands/agents-server/startAgentsServer/startUserChatJobWorkerPump.ts 8.77ms
 584/2072 src/cli/cli-commands/agents-server/startAgentsServer/stopChildProcess.ts 2.28ms
 585/2072 src/cli/cli-commands/agents-server/startAgentsServer/waitForLocalAgentRunnerLimits.ts 11.50ms
 586/2072 src/cli/cli-commands/coder.ts 9.05ms
 587/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts 8.95ms
 588/2072 src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.ts 6.63ms
 589/2072 src/cli/cli-commands/coder/add.test.ts 11.69ms
 590/2072 src/cli/cli-commands/coder/add.ts 19.09ms
 591/2072 src/cli/cli-commands/coder/agentCliOptions.ts 4.58ms
 592/2072 src/cli/cli-commands/coder/agentsFile.ts 3.52ms
 593/2072 src/cli/cli-commands/coder/boilerplateCount.test.ts 3.22ms
 594/2072 src/cli/cli-commands/coder/boilerplateCount.ts 5.90ms
 595/2072 src/cli/cli-commands/coder/boilerplateTemplates.test.ts 19.89ms
 596/2072 src/cli/cli-commands/coder/boilerplateTemplates.ts 13.82ms
 597/2072 src/cli/cli-commands/coder/coderReferencedArtifacts.ts 7.19ms
 598/2072 src/cli/cli-commands/coder/ensureCoderDeveloperAgentFile.ts 3.95ms
 599/2072 src/cli/cli-commands/coder/ensureCoderEnvFile.ts 6.14ms
 600/2072 src/cli/cli-commands/coder/ensureCoderGitignoreFile.ts 5.70ms
 601/2072 src/cli/cli-commands/coder/ensureCoderMarkdownFile.ts 4.34ms
 602/2072 src/cli/cli-commands/coder/ensureCoderPackageJsonFile.ts 3.11ms
 603/2072 src/cli/cli-commands/coder/ensureCoderVscodeSettingsFile.ts 6.12ms
 604/2072 src/cli/cli-commands/coder/ensureDirectory.ts 4.29ms
 605/2072 src/cli/cli-commands/coder/find-fresh-emoji-tags.ts 3.52ms
 606/2072 src/cli/cli-commands/coder/find-refactor-candidates.test.ts 4.98ms
 607/2072 src/cli/cli-commands/coder/find-refactor-candidates.ts 7.82ms
 608/2072 src/cli/cli-commands/coder/find-unwritten.ts 4.05ms
 609/2072 src/cli/cli-commands/coder/formatDisplayPath.ts 2.44ms
 610/2072 src/cli/cli-commands/coder/generate-boilerplates.test.ts 7.09ms
 611/2072 src/cli/cli-commands/coder/generate-boilerplates.ts 15.84ms
 612/2072 src/cli/cli-commands/coder/getDefaultCoderPackageJsonScripts.ts 6.79ms
 613/2072 src/cli/cli-commands/coder/getDefaultCoderVscodeSettings.ts 2.89ms
 614/2072 src/cli/cli-commands/coder/getTypescriptModule.test.ts 4.09ms
 615/2072 src/cli/cli-commands/coder/getTypescriptModule.ts 2.87ms
 616/2072 src/cli/cli-commands/coder/GitChangesMode.ts 4.21ms
 617/2072 src/cli/cli-commands/coder/init.test.ts 7.28ms
 618/2072 src/cli/cli-commands/coder/init.ts 14.57ms
 619/2072 src/cli/cli-commands/coder/initializeCoderProjectConfiguration.ts 6.51ms
 620/2072 src/cli/cli-commands/coder/list.test.ts 8.54ms
 621/2072 src/cli/cli-commands/coder/list.ts 6.78ms
 622/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts 5.85ms
 623/2072 src/cli/cli-commands/coder/mergeStringRecordJsonFile.ts 24.76ms
 624/2072 src/cli/cli-commands/coder/ping.test.ts 28.91ms
 625/2072 src/cli/cli-commands/coder/ping.ts 13.25ms
 626/2072 src/cli/cli-commands/coder/printCoderRunFailure.ts 8.93ms
 627/2072 src/cli/cli-commands/coder/printInitializationSummary.ts 11.70ms
 628/2072 src/cli/cli-commands/coder/run.test.ts 13.36ms
 629/2072 src/cli/cli-commands/coder/run.ts 16.38ms
 630/2072 src/cli/cli-commands/coder/server.test.ts 13.26ms
 631/2072 src/cli/cli-commands/coder/server.ts 20.39ms
 632/2072 src/cli/cli-commands/coder/ThinkingLevel.ts 2.86ms
 633/2072 src/cli/cli-commands/coder/verify.test.ts 8.06ms
 634/2072 src/cli/cli-commands/coder/verify.ts 6.09ms
 635/2072 src/cli/cli-commands/coder/waitOptions.ts 4.37ms
 636/2072 src/cli/cli-commands/common/$askForConfirmation.test.ts 2.98ms
 637/2072 src/cli/cli-commands/common/$askForConfirmation.ts 5.94ms
 638/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts 8.00ms
 639/2072 src/cli/cli-commands/common/coderGitSyncCliOptions.ts 11.52ms
 640/2072 src/cli/cli-commands/common/createNonNegativeIntegerOptionParser.ts 3.31ms
 641/2072 src/cli/cli-commands/common/createPositiveIntegerOptionParser.ts 3.57ms
 642/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts 5.05ms
 643/2072 src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.ts 13.33ms
 644/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts 7.74ms
 645/2072 src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.ts 5.78ms
 646/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts 6.73ms
 647/2072 src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.ts 3.76ms
 648/2072 src/cli/cli-commands/common/disk-space/formatLowFreeDiskSpaceWarning.ts 4.38ms
 649/2072 src/cli/cli-commands/common/disk-space/freeDiskSpaceConstants.ts 6.37ms
 650/2072 src/cli/cli-commands/common/disk-space/FreeDiskSpaceStatus.ts 3.40ms
 651/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts 6.73ms
 652/2072 src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.ts 3.81ms
 653/2072 src/cli/cli-commands/common/handleActionErrors.ts 3.12ms
 654/2072 src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts 7.26ms
 655/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts 8.25ms
 656/2072 src/cli/cli-commands/common/harness/$checkHarnessInstallation.ts 5.67ms
 657/2072 src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts 9.89ms
 658/2072 src/cli/cli-commands/common/harness/$resolveHarnessCommandPath.ts 4.71ms
 659/2072 src/cli/cli-commands/common/harness/$resolveHarnessInstallationOrigin.ts 5.19ms
 660/2072 src/cli/cli-commands/common/harness/$resolveInstalledHarnessVersion.ts 5.03ms
 661/2072 src/cli/cli-commands/common/harness/$runHarnessInstallationCommand.ts 5.05ms
 662/2072 src/cli/cli-commands/common/harness/buildHarnessInstallCommand.ts 3.04ms
 663/2072 src/cli/cli-commands/common/harness/formatHarnessInstallationWarning.ts 4.59ms
 664/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts 4.76ms
 665/2072 src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.ts 4.04ms
 666/2072 src/cli/cli-commands/common/harness/HarnessDefinition.test.ts 6.30ms
 667/2072 src/cli/cli-commands/common/harness/HarnessDefinition.ts 9.00ms
 668/2072 src/cli/cli-commands/common/harness/HarnessInstallationOrigin.ts 3.96ms
 669/2072 src/cli/cli-commands/common/harness/HarnessInstallationStatus.ts 4.22ms
 670/2072 src/cli/cli-commands/common/harness/HarnessUpdatePlan.ts 4.61ms
 671/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts 4.12ms
 672/2072 src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.ts 8.54ms
 673/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts 3.99ms
 674/2072 src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.ts 4.80ms
 675/2072 src/cli/cli-commands/common/npm/$askForNpmPackageInstallationApproval.ts 2.94ms
 676/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts 4.35ms
 677/2072 src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.ts 6.19ms
 678/2072 src/cli/cli-commands/common/npm/$resolveNpmGlobalPrefixPath.ts 4.26ms
 679/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts 4.94ms
 680/2072 src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.ts 3.77ms
 681/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts 3.94ms
 682/2072 src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.ts 3.62ms
 683/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts 2.91ms
 684/2072 src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.ts 11.79ms
 685/2072 src/cli/cli-commands/common/projectInitialization.ts 12.20ms
 686/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts 58.11ms
 687/2072 src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.ts 5.53ms
 688/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts 12.35ms
 689/2072 src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.ts 21.80ms
 690/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts 14.81ms
 691/2072 src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.ts 19.12ms
 692/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts 36.98ms
 693/2072 src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.ts 9.87ms
 694/2072 src/cli/cli-commands/common/promptbook-cli/buildPromptbookCliInstallCommand.ts 5.36ms
 695/2072 src/cli/cli-commands/common/promptbook-cli/formatPromptbookCliInstallationWarning.ts 5.02ms
 696/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallation.ts 5.03ms
 697/2072 src/cli/cli-commands/common/promptbook-cli/PromptbookCliInstallationStatus.ts 9.72ms
 698/2072 src/cli/cli-commands/common/promptPriorityCliOptions.ts 4.05ms
 699/2072 src/cli/cli-commands/common/promptRunnerCliOptions.ts 15.74ms
 700/2072 src/cli/cli-commands/common/questionsCliOptions.test.ts 5.65ms
 701/2072 src/cli/cli-commands/common/questionsCliOptions.ts 10.88ms
 702/2072 src/cli/cli-commands/hello.ts 5.73ms
 703/2072 src/cli/cli-commands/list-models.ts 5.27ms
 704/2072 src/cli/cli-commands/list-scrapers.ts 4.64ms
 705/2072 src/cli/cli-commands/login.ts 14.85ms
 706/2072 src/cli/cli-commands/make.ts 22.49ms
 707/2072 src/cli/cli-commands/prettify.ts 5.61ms
 708/2072 src/cli/cli-commands/run.ts 5.16ms
 709/2072 src/cli/cli-commands/run/prepareRunCommandResources.ts 12.03ms
 710/2072 src/cli/cli-commands/run/resolveRunInputParameters.ts 7.26ms
 711/2072 src/cli/cli-commands/run/runCommandAction.ts 8.65ms
 712/2072 src/cli/cli-commands/run/runPipelineExecution.ts 6.80ms
 713/2072 src/cli/cli-commands/runInteractiveChatbot.ts 8.12ms
 714/2072 src/cli/cli-commands/start-agents-server.ts 8.38ms
 715/2072 src/cli/cli-commands/start-pipelines-server.ts 11.65ms
 716/2072 src/cli/cli-commands/test-command.ts 8.81ms
 717/2072 src/cli/common/$addGlobalOptionsToCommand.ts 2.54ms
 718/2072 src/cli/common/$deprecateCliCommand.test.ts 4.14ms
 719/2072 src/cli/common/$deprecateCliCommand.ts 3.57ms
 720/2072 src/cli/common/$hideCliCommandFromHelp.test.ts 4.53ms
 721/2072 src/cli/common/$hideCliCommandFromHelp.ts 3.34ms
 722/2072 src/cli/common/$provideLlmToolsForCli.ts 9.90ms
 723/2072 src/cli/common/$requireCliSubcommand.test.ts 4.56ms
 724/2072 src/cli/common/$requireCliSubcommand.ts 3.53ms
 725/2072 src/cli/common/loadPromptsModule.ts 2.42ms
 726/2072 src/cli/main.ts 3.61ms
 727/2072 src/cli/other/install.test.ts 3.52ms
 728/2072 src/cli/other/vpsInstall.test.ts 26.42ms
 729/2072 src/cli/promptbookCli.ts 8.67ms
 730/2072 src/cli/test/ptbk.test.ts 11.60ms
 731/2072 src/cli/test/ptbk.ts 1.68ms
 732/2072 src/cli/test/tsconfig.json 3.20ms
 733/2072 src/collection/agent-collection/AgentCollection.ts 4.86ms
 734/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabase.ts 23.04ms
 735/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentCollectionInSupabaseOptions.ts 2.29ms
 736/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts 8.67ms
 737/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts 7.17ms
 738/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.ts 4.09ms
 739/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts 4.30ms
 740/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.ts 5.82ms
 741/2072 src/collection/agent-collection/constructors/agent-collection-in-supabase/prepareAgentSourceForPersistence.ts 5.99ms
 742/2072 src/collection/agent-collection/CreateAgentInput.test.ts 3.20ms
 743/2072 src/collection/agent-collection/CreateAgentInput.ts 6.43ms
 744/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts 5.85ms
 745/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts 23.90ms
 746/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts 5.52ms
 747/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.ts 3.59ms
 748/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts 3.63ms
 749/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts 4.79ms
 750/2072 src/collection/pipeline-collection/constructors/createPipelineCollectionFromUrl.ts 5.99ms
 751/2072 src/collection/pipeline-collection/constructors/createPipelineSubcollection.ts 8.50ms
 752/2072 src/collection/pipeline-collection/PipelineCollection.ts 7.85ms
 753/2072 src/collection/pipeline-collection/pipelineCollectionToJson.test.ts 57.91ms
 754/2072 src/collection/pipeline-collection/pipelineCollectionToJson.ts 3.24ms
 755/2072 src/collection/pipeline-collection/SimplePipelineCollection.ts 45.79ms
 756/2072 src/commands/_BOILERPLATE/boilerplateCommand.test.ts 4.66ms
 757/2072 src/commands/_BOILERPLATE/BoilerplateCommand.ts 2.07ms
 758/2072 src/commands/_BOILERPLATE/boilerplateCommandParser.ts 6.77ms
 759/2072 src/commands/_common/getParserForCommand.test.ts 6.65ms
 760/2072 src/commands/_common/getParserForCommand.ts 4.42ms
 761/2072 src/commands/_common/parseCommand.ts 10.69ms
 762/2072 src/commands/_common/stringifyCommand.test.ts 5.31ms
 763/2072 src/commands/_common/stringifyCommand.ts 2.73ms
 764/2072 src/commands/_common/types/Command.ts 3.54ms
 765/2072 src/commands/_common/types/CommandParser.ts 16.19ms
 766/2072 src/commands/_common/types/CommandType.ts 2.63ms
 767/2072 src/commands/_common/types/CommandUsagePlaces.ts 5.11ms
 768/2072 src/commands/BOOK_VERSION/bookVersionCommand.test.ts 3.53ms
 769/2072 src/commands/BOOK_VERSION/BookVersionCommand.ts 11.28ms
 770/2072 src/commands/BOOK_VERSION/bookVersionCommandParser.ts 10.04ms
 771/2072 src/commands/EXPECT/expectCommand.test.ts 4.21ms
 772/2072 src/commands/EXPECT/ExpectCommand.ts 2.82ms
 773/2072 src/commands/EXPECT/expectCommandParser.ts 7.76ms
 774/2072 src/commands/FOREACH/foreachCommand.test.ts 5.62ms
 775/2072 src/commands/FOREACH/ForeachCommand.ts 2.50ms
 776/2072 src/commands/FOREACH/foreachCommandParser.ts 17.97ms
 777/2072 src/commands/FOREACH/ForeachJson.ts 3.08ms
 778/2072 src/commands/FORMAT/formatCommand.test.ts 3.14ms
 779/2072 src/commands/FORMAT/FormatCommand.ts 2.54ms
 780/2072 src/commands/FORMAT/formatCommandParser.ts 6.81ms
 781/2072 src/commands/FORMFACTOR/formfactorCommand.test.ts 3.13ms
 782/2072 src/commands/FORMFACTOR/FormfactorCommand.ts 2.28ms
 783/2072 src/commands/FORMFACTOR/formfactorCommandParser.ts 10.50ms
 784/2072 src/commands/index.ts 4.12ms
 785/2072 src/commands/JOKER/jokerCommand.test.ts 2.81ms
 786/2072 src/commands/JOKER/JokerCommand.ts 2.03ms
 787/2072 src/commands/JOKER/jokerCommandParser.ts 5.94ms
 788/2072 src/commands/KNOWLEDGE/knowledgeCommand.test.ts 3.38ms
 789/2072 src/commands/KNOWLEDGE/KnowledgeCommand.ts 2.09ms
 790/2072 src/commands/KNOWLEDGE/knowledgeCommandParser.ts 7.63ms
 791/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts 3.34ms
 792/2072 src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.ts 3.57ms
 793/2072 src/commands/MODEL/modelCommand.test.ts 3.24ms
 794/2072 src/commands/MODEL/ModelCommand.ts 5.31ms
 795/2072 src/commands/MODEL/modelCommandParser.ts 8.90ms
 796/2072 src/commands/PARAMETER/parameterCommand.test.ts 5.14ms
 797/2072 src/commands/PARAMETER/ParameterCommand.ts 2.15ms
 798/2072 src/commands/PARAMETER/parameterCommandParser.ts 7.15ms
 799/2072 src/commands/PERSONA/personaCommand.test.ts 4.28ms
 800/2072 src/commands/PERSONA/PersonaCommand.ts 3.99ms
 801/2072 src/commands/PERSONA/personaCommandParser.ts 11.78ms
 802/2072 src/commands/POSTPROCESS/postprocessCommand.test.ts 3.48ms
 803/2072 src/commands/POSTPROCESS/PostprocessCommand.ts 2.44ms
 804/2072 src/commands/POSTPROCESS/postprocessCommandParser.ts 10.92ms
 805/2072 src/commands/README.md 2.26ms
 806/2072 src/commands/SECTION/sectionCommand.test.ts 3.24ms
 807/2072 src/commands/SECTION/SectionCommand.ts 2.64ms
 808/2072 src/commands/SECTION/sectionCommandParser.ts 11.79ms
 809/2072 src/commands/URL/urlCommand.test.ts 8.72ms
 810/2072 src/commands/URL/UrlCommand.ts 1.92ms
 811/2072 src/commands/URL/urlCommandParser.ts 11.02ms
 812/2072 src/commands/X_ACTION/actionCommand.test.ts 4.28ms
 813/2072 src/commands/X_ACTION/ActionCommand.ts 65.94ms
 814/2072 src/commands/X_ACTION/actionCommandParser.ts 6.16ms
 815/2072 src/commands/X_INSTRUMENT/instrumentCommand.test.ts 3.17ms
 816/2072 src/commands/X_INSTRUMENT/InstrumentCommand.ts 3.76ms
 817/2072 src/commands/X_INSTRUMENT/instrumentCommandParser.ts 38.28ms
 818/2072 src/commitments/_base/BaseCommitmentDefinition.ts 14.68ms
 819/2072 src/commitments/_base/BookCommitment.ts 7.29ms
 820/2072 src/commitments/_base/CommitmentDefinition.ts 13.94ms
 821/2072 src/commitments/_base/createEmptyAgentModelRequirements.ts 4.70ms
 822/2072 src/commitments/_base/formatOptionalInstructionBlock.ts 2.99ms
 823/2072 src/commitments/_base/NotYetImplementedCommitmentDefinition.ts 4.58ms
 824/2072 src/commitments/_base/ParsedCommitment.ts 4.07ms
 825/2072 src/commitments/_common/commitmentToolFunctions.ts 5.08ms
 826/2072 src/commitments/_common/createWritingCommitmentSections.ts 4.51ms
 827/2072 src/commitments/_common/getAllCommitmentDefinitions.test.ts 4.27ms
 828/2072 src/commitments/_common/getAllCommitmentDefinitions.ts 2.60ms
 829/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForBrowser.ts 4.71ms
 830/2072 src/commitments/_common/getAllCommitmentsToolFunctionsForNode.ts 4.95ms
 831/2072 src/commitments/_common/getAllCommitmentsToolTitles.ts 3.05ms
 832/2072 src/commitments/_common/getAllCommitmentTypes.ts 5.39ms
 833/2072 src/commitments/_common/getCommitmentDefinition.ts 4.21ms
 834/2072 src/commitments/_common/getCommitmentNoticeMetadata.test.ts 6.09ms
 835/2072 src/commitments/_common/getCommitmentNoticeMetadata.ts 8.02ms
 836/2072 src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts 2.98ms
 837/2072 src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts 4.38ms
 838/2072 src/commitments/_common/getGroupedCommitmentDefinitions.ts 6.12ms
 839/2072 src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts 3.08ms
 840/2072 src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts 5.52ms
 841/2072 src/commitments/_common/isCommitmentSupported.ts 3.85ms
 842/2072 src/commitments/_common/sortCommitmentDefinitions.ts 7.35ms
 843/2072 src/commitments/_common/teamInternalAgentAccess.ts 7.60ms
 844/2072 src/commitments/_common/toolExecutionEnvelope.ts 4.71ms
 845/2072 src/commitments/_common/toolRuntimeContext.ts 16.78ms
 846/2072 src/commitments/ACTION/ACTION.test.ts 8.67ms
 847/2072 src/commitments/ACTION/ACTION.ts 7.85ms
 848/2072 src/commitments/CLOSED/CLOSED.test.ts 3.18ms
 849/2072 src/commitments/CLOSED/CLOSED.ts 4.34ms
 850/2072 src/commitments/DELETE/DELETE.test.ts 5.95ms
 851/2072 src/commitments/DELETE/DELETE.ts 8.18ms
 852/2072 src/commitments/DICTIONARY/DICTIONARY.ts 5.25ms
 853/2072 src/commitments/FORMAT/FORMAT.test.ts 4.20ms
 854/2072 src/commitments/FORMAT/FORMAT.ts 5.14ms
 855/2072 src/commitments/FROM/FROM.ts 5.03ms
 856/2072 src/commitments/GOAL/GOAL.ts 8.80ms
 857/2072 src/commitments/IMPORT/IMPORT.ts 4.85ms
 858/2072 src/commitments/index.test.ts 10.44ms
 859/2072 src/commitments/index.ts 11.98ms
 860/2072 src/commitments/KNOWLEDGE/KNOWLEDGE.ts 8.42ms
 861/2072 src/commitments/LANGUAGE/LANGUAGE.ts 4.41ms
 862/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts 5.25ms
 863/2072 src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.ts 7.34ms
 864/2072 src/commitments/MESSAGE/AgentMessageCommitmentDefinition.ts 6.56ms
 865/2072 src/commitments/MESSAGE/InitialMessageCommitmentDefinition.ts 7.04ms
 866/2072 src/commitments/MESSAGE/InternalMessageCommitmentDefinition.ts 5.18ms
 867/2072 src/commitments/MESSAGE/MESSAGE.ts 7.01ms
 868/2072 src/commitments/MESSAGE/UserMessageCommitmentDefinition.ts 4.93ms
 869/2072 src/commitments/META_AVATAR/META_AVATAR.ts 4.53ms
 870/2072 src/commitments/META_COLOR/META_COLOR.ts 7.72ms
 871/2072 src/commitments/META_DESCRIPTION/META_DESCRIPTION.ts 6.59ms
 872/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts 3.21ms
 873/2072 src/commitments/META_DISCLAIMER/META_DISCLAIMER.ts 6.05ms
 874/2072 src/commitments/META_DOMAIN/META_DOMAIN.ts 6.76ms
 875/2072 src/commitments/META_FONT/META_FONT.ts 4.47ms
 876/2072 src/commitments/META_FULLNAME/META_FULLNAME.ts 3.55ms
 877/2072 src/commitments/META_ID/META_ID.ts 7.66ms
 878/2072 src/commitments/META_IMAGE/META_IMAGE.ts 4.66ms
 879/2072 src/commitments/META_INPUT_PLACEHOLDER/META_INPUT_PLACEHOLDER.ts 7.61ms
 880/2072 src/commitments/META_LINK/META_LINK.ts 5.62ms
 881/2072 src/commitments/META_THINKING_MESSAGE/META_THINKING_MESSAGE.ts 4.93ms
 882/2072 src/commitments/META_VISIBILITY/META_VISIBILITY.ts 3.91ms
 883/2072 src/commitments/META_VOICE/META_VOICE.ts 6.66ms
 884/2072 src/commitments/MODEL/MODEL.test.ts 4.21ms
 885/2072 src/commitments/MODEL/MODEL.ts 12.17ms
 886/2072 src/commitments/NOTE/NOTE.ts 4.84ms
 887/2072 src/commitments/OPEN/OPEN.ts 8.75ms
 888/2072 src/commitments/PERSONA/persona-verification-example.md 14.14ms
 889/2072 src/commitments/PERSONA/PERSONA.ts 12.87ms
 890/2072 src/commitments/README.md 10.12ms
 891/2072 src/commitments/RULE/RULE.ts 14.74ms
 892/2072 src/commitments/SAMPLE/SAMPLE.ts 13.07ms
 893/2072 src/commitments/SCENARIO/SCENARIO.ts 9.94ms
 894/2072 src/commitments/STYLE/STYLE.ts 9.31ms
 895/2072 src/commitments/TEAM/TEAM.ts 24.87ms
 896/2072 src/commitments/TEMPLATE/README.md 5.18ms
 897/2072 src/commitments/TEMPLATE/TEMPLATE.test.ts 4.73ms
 898/2072 src/commitments/TEMPLATE/TEMPLATE.ts 8.45ms
 899/2072 src/commitments/USE_CALENDAR/calendarReference.ts 12.24ms
 900/2072 src/commitments/USE_CALENDAR/callGoogleCalendarApi.ts 11.20ms
 901/2072 src/commitments/USE_CALENDAR/createUseCalendarToolFunctions.ts 15.22ms
 902/2072 src/commitments/USE_CALENDAR/createUseCalendarTools.ts 8.48ms
 903/2072 src/commitments/USE_CALENDAR/getUseCalendarToolTitles.ts 2.79ms
 904/2072 src/commitments/USE_CALENDAR/normalizeConfiguredCalendars.ts 9.90ms
 905/2072 src/commitments/USE_CALENDAR/resolveUseCalendarToolRuntimeOrWalletCredentialResult.ts 16.56ms
 906/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts 7.98ms
 907/2072 src/commitments/USE_CALENDAR/USE_CALENDAR.ts 16.01ms
 908/2072 src/commitments/USE_CALENDAR/UseCalendarToolNames.ts 2.56ms
 909/2072 src/commitments/USE_CALENDAR/UseCalendarWallet.ts 2.58ms
 910/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts 3.16ms
 911/2072 src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.ts 9.62ms
 912/2072 src/commitments/USE_MCP/USE_MCP.ts 6.60ms
 913/2072 src/commitments/USE_POPUP/USE_POPUP.ts 8.34ms
 914/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts 5.53ms
 915/2072 src/commitments/USE_PRIVACY/USE_PRIVACY.ts 9.75ms
 916/2072 src/commitments/USE_PROJECT/callGitHubApi.ts 9.37ms
 917/2072 src/commitments/USE_PROJECT/createUseProjectToolFunctions.ts 31.30ms
 918/2072 src/commitments/USE_PROJECT/createUseProjectTools.ts 11.70ms
 919/2072 src/commitments/USE_PROJECT/getUseProjectToolTitles.ts 6.11ms
 920/2072 src/commitments/USE_PROJECT/normalizeConfiguredProjects.ts 5.58ms
 921/2072 src/commitments/USE_PROJECT/normalizeOptionalToolText.ts 4.19ms
 922/2072 src/commitments/USE_PROJECT/projectReference.test.ts 6.68ms
 923/2072 src/commitments/USE_PROJECT/projectReference.ts 10.53ms
 924/2072 src/commitments/USE_PROJECT/resolveUseProjectToolRuntimeOrWalletCredentialResult.ts 16.31ms
 925/2072 src/commitments/USE_PROJECT/USE_PROJECT.test.ts 9.63ms
 926/2072 src/commitments/USE_PROJECT/USE_PROJECT.ts 70.37ms
 927/2072 src/commitments/USE_PROJECT/UseProjectToolNames.ts 2.73ms
 928/2072 src/commitments/USE_PROJECT/UseProjectWallet.ts 2.30ms
 929/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts 5.39ms
 930/2072 src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.ts 12.10ms
 931/2072 src/commitments/WRITING_RULES/WRITING_RULES.ts 8.08ms
 932/2072 src/commitments/WRITING_SAMPLE/WRITING_SAMPLE.ts 7.47ms
 933/2072 src/config.test.ts 2.36ms
 934/2072 src/config.ts 14.42ms
 935/2072 src/constants.ts 10.03ms
 936/2072 src/constants/streaming.ts 3.10ms
 937/2072 src/conversion/archive/loadArchive.ts 4.49ms
 938/2072 src/conversion/archive/loadJsZipModule.ts 2.45ms
 939/2072 src/conversion/archive/saveArchive.ts 4.50ms
 940/2072 src/conversion/compilePipeline.test.ts 11.43ms
 941/2072 src/conversion/compilePipeline.ts 6.19ms
 942/2072 src/conversion/compilePipelineOnRemoteServer.ts 4.01ms
 943/2072 src/conversion/parsePipeline.test.ts 5.06ms
 944/2072 src/conversion/parsePipeline.ts 5.41ms
 945/2072 src/conversion/parsePipeline/applyPipelineHead.ts 6.15ms
 946/2072 src/conversion/parsePipeline/createInitialPipelineJson.ts 2.76ms
 947/2072 src/conversion/parsePipeline/createUniqueSectionNameResolver.ts 3.40ms
 948/2072 src/conversion/parsePipeline/defineParameter.ts 5.14ms
 949/2072 src/conversion/parsePipeline/extractPipelineDescription.ts 3.16ms
 950/2072 src/conversion/parsePipeline/finalizeParsedPipeline.ts 6.28ms
 951/2072 src/conversion/parsePipeline/getPipelineIdentification.ts 8.68ms
 952/2072 src/conversion/parsePipeline/parsePreparedPipelineSections.ts 4.76ms
 953/2072 src/conversion/parsePipeline/preparePipelineString.ts 5.96ms
 954/2072 src/conversion/parsePipeline/processPipelineSection.ts 15.17ms
 955/2072 src/conversion/pipelineJsonToString.test.ts 8.55ms
 956/2072 src/conversion/pipelineJsonToString.ts 4.23ms
 957/2072 src/conversion/pipelineJsonToString/appendMarkdownBlock.ts 2.59ms
 958/2072 src/conversion/pipelineJsonToString/createPipelineCommands.ts 4.03ms
 959/2072 src/conversion/pipelineJsonToString/createPipelineIntroduction.ts 3.21ms
 960/2072 src/conversion/pipelineJsonToString/createTaskSerialization.ts 6.97ms
 961/2072 src/conversion/pipelineJsonToString/stringifyCommands.ts 2.21ms
 962/2072 src/conversion/pipelineJsonToString/stringifyTask.ts 2.98ms
 963/2072 src/conversion/prettify/PrettifyOptions.ts 2.12ms
 964/2072 src/conversion/prettify/prettifyPipelineString.ts 5.31ms
 965/2072 src/conversion/prettify/renderPipelineMermaidOptions.ts 8.37ms
 966/2072 src/conversion/utils/extractParameterNamesFromTask.test.ts 2.69ms
 967/2072 src/conversion/utils/extractParameterNamesFromTask.ts 5.96ms
 968/2072 src/conversion/validation/_importPipeline.test.ts 4.83ms
 969/2072 src/conversion/validation/pipelineStringToJson-parseErrors.test.ts 3.72ms
 970/2072 src/conversion/validation/validatePipeline-logicErrors.test.ts 5.58ms
 971/2072 src/conversion/validation/validatePipeline.test.ts 4.64ms
 972/2072 src/conversion/validation/validatePipeline.ts 6.18ms
 973/2072 src/conversion/validation/validatePipeline/createPipelineValidationContext.ts 3.59ms
 974/2072 src/conversion/validation/validatePipeline/validatePipelineCollectionsStructure.ts 4.52ms
 975/2072 src/conversion/validation/validatePipeline/validatePipelineDependencyResolution.ts 7.65ms
 976/2072 src/conversion/validation/validatePipeline/validatePipelineMetadata.ts 5.93ms
 977/2072 src/conversion/validation/validatePipeline/validatePipelineParameters.ts 6.68ms
 978/2072 src/conversion/validation/validatePipeline/validatePipelineTasks.ts 7.58ms
 979/2072 src/dialogs/callback/CallbackInterfaceTools.ts 3.22ms
 980/2072 src/dialogs/callback/CallbackInterfaceToolsOptions.ts 2.41ms
 981/2072 src/dialogs/simple-prompt/SimplePromptInterfaceTools.ts 3.27ms
 982/2072 src/dialogs/user-interface-execution-tools.test.ts 7.93ms
 983/2072 src/errors/0-BoilerplateError.ts 1.86ms
 984/2072 src/errors/0-index.ts 4.67ms
 985/2072 src/errors/AbstractFormatError.ts 6.64ms
 986/2072 src/errors/assertsError.ts 2.86ms
 987/2072 src/errors/AuthenticationError.ts 1.70ms
 988/2072 src/errors/CollectionError.ts 4.00ms
 989/2072 src/errors/ConflictError.ts 2.01ms
 990/2072 src/errors/DatabaseError.ts 2.16ms
 991/2072 src/errors/EnvironmentMismatchError.ts 2.90ms
 992/2072 src/errors/ExpectError.ts 3.50ms
 993/2072 src/errors/KnowledgeScrapeError.ts 2.04ms
 994/2072 src/errors/LimitReachedError.ts 2.58ms
 995/2072 src/errors/MissingToolsError.ts 2.53ms
 996/2072 src/errors/NotAllowed.ts 4.38ms
 997/2072 src/errors/NotFoundError.ts 1.84ms
 998/2072 src/errors/NotYetImplementedError.ts 2.65ms
 999/2072 src/errors/ParseError.ts 2.14ms
1000/2072 src/errors/PipelineExecutionError.ts 2.91ms
1001/2072 src/errors/PipelineLogicError.ts 2.66ms
1002/2072 src/errors/PipelineUrlError.ts 1.89ms
1003/2072 src/errors/PromptbookFetchError.ts 3.50ms
1004/2072 src/errors/UnexpectedError.ts 2.57ms
1005/2072 src/errors/utils/deserializeError.test.ts 2.41ms
1006/2072 src/errors/utils/deserializeError.ts 2.57ms
1007/2072 src/errors/utils/ErrorJson.ts 2.24ms
1008/2072 src/errors/utils/getErrorReportUrl.ts 3.00ms
1009/2072 src/errors/utils/serializeError.test.ts 2.45ms
1010/2072 src/errors/utils/serializeError.ts 4.09ms
1011/2072 src/errors/WrappedError.ts 2.51ms
1012/2072 src/executables/$provideExecutablesForNode.ts 7.48ms
1013/2072 src/executables/apps/locateLibreoffice.test.ts 4.94ms
1014/2072 src/executables/apps/locateLibreoffice.ts 4.25ms
1015/2072 src/executables/apps/locatePandoc.test.ts 1.79ms
1016/2072 src/executables/apps/locatePandoc.ts 4.36ms
1017/2072 src/executables/apps/locateVscode.test.ts 1.99ms
1018/2072 src/executables/apps/locateVscode.ts 4.07ms
1019/2072 src/executables/browsers/locateBrowser.test.ts 3.14ms
1020/2072 src/executables/browsers/locateBrowser.ts 3.56ms
1021/2072 src/executables/browsers/locateChrome.test.ts 2.00ms
1022/2072 src/executables/browsers/locateChrome.ts 3.87ms
1023/2072 src/executables/browsers/locateDefaultSystemBrowser.test.ts 5.89ms
1024/2072 src/executables/browsers/locateDefaultSystemBrowser.ts 3.72ms
1025/2072 src/executables/browsers/locateEdge.test.ts 17.09ms
1026/2072 src/executables/browsers/locateEdge.ts 11.27ms
1027/2072 src/executables/browsers/locateFirefox.test.ts 4.43ms
1028/2072 src/executables/browsers/locateFirefox.ts 2.99ms
1029/2072 src/executables/browsers/locateInternetExplorer.ts 3.36ms
1030/2072 src/executables/browsers/locateSafari.ts 2.58ms
1031/2072 src/executables/locateApp.test.ts 2.71ms
1032/2072 src/executables/locateApp.ts 6.76ms
1033/2072 src/executables/platforms/locateAppOnLinux.ts 3.34ms
1034/2072 src/executables/platforms/locateAppOnMacOs.ts 4.72ms
1035/2072 src/executables/platforms/locateAppOnWindows.test.ts 5.26ms
1036/2072 src/executables/platforms/locateAppOnWindows.ts 4.13ms
1037/2072 src/executables/README.md 1.77ms
1038/2072 src/execution/AbstractTaskResult.ts 10.84ms
1039/2072 src/execution/assertsTaskSuccessful.ts 9.68ms
1040/2072 src/execution/AvailableModel.ts 4.62ms
1041/2072 src/execution/CommonToolsOptions.ts 3.55ms
1042/2072 src/execution/createPipelineExecutor/$OngoingTaskResult.ts 6.88ms
1043/2072 src/execution/createPipelineExecutor/00-createPipelineExecutor.ts 13.21ms
1044/2072 src/execution/createPipelineExecutor/00-CreatePipelineExecutorOptions.ts 3.86ms
1045/2072 src/execution/createPipelineExecutor/10-executePipeline.ts 21.51ms
1046/2072 src/execution/createPipelineExecutor/20-executeTask.ts 14.42ms
1047/2072 src/execution/createPipelineExecutor/30-executeFormatSubvalues.ts 9.22ms
1048/2072 src/execution/createPipelineExecutor/40-executeAttempts.ts 15.20ms
1049/2072 src/execution/createPipelineExecutor/computeCosineSimilarity.ts 10.57ms
1050/2072 src/execution/createPipelineExecutor/executeSingleAttempt.ts 73.08ms
1051/2072 src/execution/createPipelineExecutor/filterJustOutputParameters.ts 5.02ms
1052/2072 src/execution/createPipelineExecutor/getContextForTask.ts 3.88ms
1053/2072 src/execution/createPipelineExecutor/getExamplesForTask.ts 14.39ms
1054/2072 src/execution/createPipelineExecutor/getKnowledgeForTask.ts 9.68ms
1055/2072 src/execution/createPipelineExecutor/getReservedParametersForTask.ts 84.13ms
1056/2072 src/execution/createPipelineExecutor/handleAttemptFailure.ts 9.15ms
1057/2072 src/execution/createPipelineExecutor/knowledgePiecesToString.ts 7.36ms
1058/2072 src/execution/createPipelineExecutor/reportPromptExecution.ts 9.49ms
1059/2072 src/execution/EmbeddingVector.ts 3.15ms
1060/2072 src/execution/embeddingVectorToString.ts 2.46ms
1061/2072 src/execution/Executables.ts 2.81ms
1062/2072 src/execution/execution-report/countWorkingDuration.test.ts 3.94ms
1063/2072 src/execution/execution-report/countWorkingDuration.ts 2.53ms
1064/2072 src/execution/execution-report/ExecutionPromptReportJson.ts 2.90ms
1065/2072 src/execution/execution-report/ExecutionReportJson.ts 7.90ms
1066/2072 src/execution/execution-report/executionReportJsonToString.ts 11.52ms
1067/2072 src/execution/execution-report/ExecutionReportString.ts 2.67ms
1068/2072 src/execution/execution-report/ExecutionReportStringOptions.ts 2.99ms
1069/2072 src/execution/ExecutionTask.ts 20.85ms
1070/2072 src/execution/ExecutionTools.ts 10.87ms
1071/2072 src/execution/FilesystemTools.ts 2.24ms
1072/2072 src/execution/LlmExecutionTools.ts 12.65ms
1073/2072 src/execution/LlmExecutionToolsConstructor.ts 2.02ms
1074/2072 src/execution/PipelineExecutor.ts 4.61ms
1075/2072 src/execution/PipelineExecutorResult.ts 3.45ms
1076/2072 src/execution/PromptbookFetch.test-type.ts 2.69ms
1077/2072 src/execution/PromptbookFetch.ts 2.06ms
1078/2072 src/execution/PromptResult.ts 7.84ms
1079/2072 src/execution/resolveTaskTldr.test.ts 3.25ms
1080/2072 src/execution/resolveTaskTldr.ts 10.10ms
1081/2072 src/execution/ScriptExecutionTools.ts 3.59ms
1082/2072 src/execution/translation/automatic-translate/automatic-translators/AutomaticTranslator.ts 1.87ms
1083/2072 src/execution/translation/automatic-translate/automatic-translators/DebugAutomaticTranslator.ts 3.71ms
1084/2072 src/execution/translation/automatic-translate/automatic-translators/FakeAutomaticTranslator.ts 3.93ms
1085/2072 src/execution/translation/automatic-translate/automatic-translators/LindatAutomaticTranslator.ts 6.41ms
1086/2072 src/execution/translation/automatic-translate/automatic-translators/README.md 1.50ms
1087/2072 src/execution/translation/automatic-translate/automatic-translators/TranslatorOptions.ts 3.55ms
1088/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts 2.32ms
1089/2072 src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.ts 2.59ms
1090/2072 src/execution/translation/automatic-translate/translateMessages.ts 4.18ms
1091/2072 src/execution/UncertainNumber.ts 3.26ms
1092/2072 src/execution/Usage.ts 4.86ms
1093/2072 src/execution/UserInterfaceTools.ts 3.55ms
1094/2072 src/execution/utils/$provideExecutionToolsForNode.ts 4.30ms
1095/2072 src/execution/utils/addUsage.test.ts 4.41ms
1096/2072 src/execution/utils/addUsage.ts 3.89ms
1097/2072 src/execution/utils/checkExpectations.test.ts 2.40ms
1098/2072 src/execution/utils/checkExpectations.ts 7.63ms
1099/2072 src/execution/utils/computeUsageCounts.ts 4.68ms
1100/2072 src/execution/utils/forEachAsync.ts 3.39ms
1101/2072 src/execution/utils/formatUsagePrice.test.ts 2.11ms
1102/2072 src/execution/utils/formatUsagePrice.ts 2.45ms
1103/2072 src/execution/utils/uncertainNumber.ts 2.20ms
1104/2072 src/execution/utils/usage-constants.ts 3.12ms
1105/2072 src/execution/utils/usageToHuman.ts 5.67ms
1106/2072 src/execution/utils/usageToWorktime.test.ts 4.37ms
1107/2072 src/execution/utils/usageToWorktime.ts 2.95ms
1108/2072 src/execution/utils/validatePromptResult.ts 6.68ms
1109/2072 src/expectations/README.md 1.75ms
1110/2072 src/file-security-checkers/FileSecurityChecker.ts 4.24ms
1111/2072 src/file-security-checkers/FileSecurityCheckResult.ts 2.41ms
1112/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts 6.32ms
1113/2072 src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.ts 6.29ms
1114/2072 src/formats/_common/FormatParser.ts 5.82ms
1115/2072 src/formats/_common/FormatSubvalueParser.ts 4.92ms
1116/2072 src/formats/csv/CsvFormatError.ts 2.32ms
1117/2072 src/formats/csv/CsvFormatParser.ts 7.01ms
1118/2072 src/formats/csv/CsvSettings.ts 5.64ms
1119/2072 src/formats/csv/utils/csvParse.ts 4.53ms
1120/2072 src/formats/csv/utils/isValidCsvString.test.ts 3.12ms
1121/2072 src/formats/csv/utils/isValidCsvString.ts 4.99ms
1122/2072 src/formats/index.ts 3.67ms
1123/2072 src/formats/json/JsonFormatParser.ts 4.15ms
1124/2072 src/formats/json/utils/isValidJsonString.test.ts 2.25ms
1125/2072 src/formats/json/utils/isValidJsonString.ts 2.60ms
1126/2072 src/formats/json/utils/jsonParse.ts 3.15ms
1127/2072 src/formats/README.md 4.88ms
1128/2072 src/formats/text/TextFormatParser.ts 4.77ms
1129/2072 src/formats/xml/utils/isValidXmlString.test.ts 4.40ms
1130/2072 src/formats/xml/utils/isValidXmlString.ts 2.66ms
1131/2072 src/formats/xml/XmlFormatParser.ts 4.94ms
1132/2072 src/formfactors/_boilerplate/BoilerplateFormfactorDefinition.ts 3.19ms
1133/2072 src/formfactors/_common/AbstractFormfactorDefinition.ts 5.73ms
1134/2072 src/formfactors/_common/FormfactorDefinition.ts 2.86ms
1135/2072 src/formfactors/_common/string_formfactor_name.ts 1.94ms
1136/2072 src/formfactors/chatbot/ChatbotFormfactorDefinition.ts 5.04ms
1137/2072 src/formfactors/completion/CompletionFormfactorDefinition.ts 4.21ms
1138/2072 src/formfactors/generator/GeneratorFormfactorDefinition.ts 4.61ms
1139/2072 src/formfactors/generic/GenericFormfactorDefinition.ts 3.12ms
1140/2072 src/formfactors/image-generator/ImageGeneratorFormfactorDefinition.ts 3.61ms
1141/2072 src/formfactors/index.ts 3.33ms
1142/2072 src/formfactors/matcher/MatcherFormfactorDefinition.ts 5.05ms
1143/2072 src/formfactors/README.md 2.29ms
1144/2072 src/formfactors/sheets/SheetsFormfactorDefinition.ts 6.02ms
1145/2072 src/formfactors/translator/TranslatorFormfactorDefinition.ts 4.81ms
1146/2072 src/globals.d.ts 3.44ms
1147/2072 src/high-level-abstractions/_common/HighLevelAbstraction.ts 3.49ms
1148/2072 src/high-level-abstractions/implicit-formfactor/ImplicitFormfactorHla.ts 7.14ms
1149/2072 src/high-level-abstractions/index.ts 3.12ms
1150/2072 src/high-level-abstractions/quick-chatbot/QuickChatbotHla.ts 8.87ms
1151/2072 src/high-level-abstractions/README.md 4.33ms
1152/2072 src/import-plugins/$fileImportPlugins.ts 2.57ms
1153/2072 src/import-plugins/AgentFileImportPlugin.ts 5.69ms
1154/2072 src/import-plugins/FileImportPlugin.ts 2.74ms
1155/2072 src/import-plugins/JsonFileImportPlugin.ts 4.66ms
1156/2072 src/import-plugins/TextFileImportPlugin.ts 3.31ms
1157/2072 src/llm-providers/_common/filterModels.ts 5.18ms
1158/2072 src/llm-providers/_common/register/$llmToolsMetadataRegister.ts 2.33ms
1159/2072 src/llm-providers/_common/register/$llmToolsRegister.ts 2.39ms
1160/2072 src/llm-providers/_common/register/$provideEnvFilename.ts 4.22ms
1161/2072 src/llm-providers/_common/register/$provideLlmToolsConfigurationFromEnv.ts 6.74ms
1162/2072 src/llm-providers/_common/register/$provideLlmToolsForTestingAndScriptsAndPlayground.ts 7.08ms
1163/2072 src/llm-providers/_common/register/$provideLlmToolsForWizardOrCli.ts 13.30ms
1164/2072 src/llm-providers/_common/register/$provideLlmToolsFromEnv.ts 16.48ms
1165/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage.ts 7.02ms
1166/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolConfigurationStatusMessage.ts 4.66ms
1167/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolEntryKey.ts 2.41ms
1168/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolInstallationStatusMessage.ts 6.26ms
1169/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolsMessageContext.ts 4.87ms
1170/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/createRegisteredLlmToolStatus.ts 8.45ms
1171/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getAvailableRegisteredLlmToolsEnvironmentVariables.ts 5.68ms
1172/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/getRegisteredLlmToolRegisters.ts 5.83ms
1173/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/hasRegisteredLlmToolEntry.ts 9.42ms
1174/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolEntries.ts 7.29ms
1175/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/listRegisteredLlmToolStatuses.ts 5.27ms
1176/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/RegisteredLlmToolsMessageContext.ts 4.39ms
1177/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderAvailableRegisteredLlmTools.ts 7.79ms
1178/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderNoRegisteredLlmToolsMessage.ts 2.33ms
1179/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRegisteredLlmToolsMessage.ts 6.16ms
1180/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/renderRelevantRegisteredLlmToolsEnvironmentVariables.ts 3.53ms
1181/2072 src/llm-providers/_common/register/$registeredLlmToolsMessage/USED_ENV_FILENAME.ts 7.15ms
1182/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts 4.37ms
1183/2072 src/llm-providers/_common/register/createLlmToolsFromConfiguration.ts 13.81ms
1184/2072 src/llm-providers/_common/register/LlmToolsConfiguration.ts 3.65ms
1185/2072 src/llm-providers/_common/register/LlmToolsMetadata.ts 59.21ms
1186/2072 src/llm-providers/_common/register/LlmToolsOptions.ts 2.62ms
1187/2072 src/llm-providers/_common/utils/assertUniqueModels.ts 3.76ms
1188/2072 src/llm-providers/_common/utils/cache/CacheItem.ts 3.35ms
1189/2072 src/llm-providers/_common/utils/cache/cacheLlmTools.ts 74.46ms
1190/2072 src/llm-providers/_common/utils/cache/CacheLlmToolsOptions.ts 5.20ms
1191/2072 src/llm-providers/_common/utils/count-total-usage/countUsage.ts 10.72ms
1192/2072 src/llm-providers/_common/utils/count-total-usage/limitTotalUsage.ts 7.86ms
1193/2072 src/llm-providers/_common/utils/count-total-usage/LlmExecutionToolsWithTotalUsage.ts 6.35ms
1194/2072 src/llm-providers/_common/utils/pricing.test.ts 2.01ms
1195/2072 src/llm-providers/_common/utils/pricing.ts 2.23ms
1196/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts 22.61ms
1197/2072 src/llm-providers/_common/utils/removeUnsupportedModelRequirements.ts 9.28ms
1198/2072 src/llm-providers/_multiple/getSingleLlmExecutionTools.ts 4.75ms
1199/2072 src/llm-providers/_multiple/joinLlmExecutionTools.ts 24.01ms
1200/2072 src/llm-providers/_multiple/MultipleLlmExecutionTools.ts 17.48ms
1201/2072 src/llm-providers/_multiple/playground/playground.ts 8.31ms
1202/2072 src/llm-providers/_multiple/playground/tsconfig.json 5.23ms
1203/2072 src/llm-providers/agent/Agent.test.ts 9.82ms
1204/2072 src/llm-providers/agent/Agent.ts 22.14ms
1205/2072 src/llm-providers/agent/AgentLlmExecutionTools.test.ts 12.24ms
1206/2072 src/llm-providers/agent/AgentLlmExecutionTools.ts 20.99ms
1207/2072 src/llm-providers/agent/AgentLlmExecutionToolsAgentKitRunner.ts 19.47ms
1208/2072 src/llm-providers/agent/AgentLlmExecutionToolsOpenAiAssistantRunner.ts 11.45ms
1209/2072 src/llm-providers/agent/AgentLlmExecutionToolsPromptPreparer.ts 16.47ms
1210/2072 src/llm-providers/agent/AgentOptions.ts 3.90ms
1211/2072 src/llm-providers/agent/createAgentLlmExecutionTools.ts 2.75ms
1212/2072 src/llm-providers/agent/CreateAgentLlmExecutionToolsOptions.ts 4.13ms
1213/2072 src/llm-providers/agent/emitAgentLlmExecutionToolsAssistantPreparationProgress.ts 5.73ms
1214/2072 src/llm-providers/agent/playground/playground.ts 11.70ms
1215/2072 src/llm-providers/agent/playground/tsconfig.json 3.44ms
1216/2072 src/llm-providers/agent/register-configuration.ts 6.18ms
1217/2072 src/llm-providers/agent/register-constructor.ts 2.49ms
1218/2072 src/llm-providers/agent/RemoteAgent.test.ts 11.43ms
1219/2072 src/llm-providers/agent/RemoteAgent.ts 32.75ms
1220/2072 src/llm-providers/agent/RemoteAgentOptions.ts 2.93ms
1221/2072 src/llm-providers/agent/self-learning/SelfLearningManager.test.ts 6.02ms
1222/2072 src/llm-providers/agent/self-learning/SelfLearningManager.ts 21.99ms
1223/2072 src/llm-providers/anthropic-claude/anthropic-claude-models.ts 11.03ms
1224/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionTools.ts 12.88ms
1225/2072 src/llm-providers/anthropic-claude/AnthropicClaudeExecutionToolsOptions.ts 3.90ms
1226/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts 2.84ms
1227/2072 src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.ts 7.08ms
1228/2072 src/llm-providers/anthropic-claude/createAnthropicClaudeExecutionTools.ts 3.75ms
1229/2072 src/llm-providers/anthropic-claude/playground/playground.ts 9.24ms
1230/2072 src/llm-providers/anthropic-claude/playground/tsconfig.json 3.15ms
1231/2072 src/llm-providers/anthropic-claude/register-configuration.ts 6.71ms
1232/2072 src/llm-providers/anthropic-claude/register-constructor.ts 2.60ms
1233/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionTools.ts 15.77ms
1234/2072 src/llm-providers/azure-openai/AzureOpenAiExecutionToolsOptions.ts 5.08ms
1235/2072 src/llm-providers/azure-openai/createAzureOpenAiExecutionTools.ts 2.74ms
1236/2072 src/llm-providers/azure-openai/playground/playground.ts 5.95ms
1237/2072 src/llm-providers/azure-openai/playground/tsconfig.json 2.79ms
1238/2072 src/llm-providers/azure-openai/register-configuration.ts 8.63ms
1239/2072 src/llm-providers/azure-openai/register-constructor.ts 3.37ms
1240/2072 src/llm-providers/deepseek/createDeepseekExecutionTools.ts 4.83ms
1241/2072 src/llm-providers/deepseek/deepseek-models.ts 10.29ms
1242/2072 src/llm-providers/deepseek/DeepseekExecutionToolsOptions.ts 5.81ms
1243/2072 src/llm-providers/deepseek/other/compatibility-issue.md 2.67ms
1244/2072 src/llm-providers/deepseek/register-configuration.ts 5.46ms
1245/2072 src/llm-providers/deepseek/register-constructor.ts 5.56ms
1246/2072 src/llm-providers/google/createGoogleExecutionTools.ts 15.56ms
1247/2072 src/llm-providers/google/google-models.ts 11.04ms
1248/2072 src/llm-providers/google/GoogleExecutionToolsOptions.ts 4.50ms
1249/2072 src/llm-providers/google/other/compatibility-issue.md 2.19ms
1250/2072 src/llm-providers/google/register-configuration.ts 4.36ms
1251/2072 src/llm-providers/google/register-constructor.ts 3.19ms
1252/2072 src/llm-providers/mocked/$fakeTextToExpectations.ts 6.70ms
1253/2072 src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts 9.97ms
1254/2072 src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts 12.27ms
1255/2072 src/llm-providers/mocked/test/faked-completion.test.ts 9.25ms
1256/2072 src/llm-providers/mocked/test/fakeTextToExpectations.test.ts 2.83ms
1257/2072 src/llm-providers/mocked/test/joker.test.ts 5.44ms
1258/2072 src/llm-providers/mocked/test/mocked-chat.test.ts 7.81ms
1259/2072 src/llm-providers/mocked/test/mocked-completion.test.ts 6.80ms
1260/2072 src/llm-providers/ollama/createOllamaExecutionTools.ts 4.70ms
1261/2072 src/llm-providers/ollama/ollama-models.ts 14.61ms
1262/2072 src/llm-providers/ollama/OllamaExecutionTools.ts 5.54ms
1263/2072 src/llm-providers/ollama/OllamaExecutionToolsOptions.ts 2.32ms
1264/2072 src/llm-providers/ollama/playground/playground.ts 6.61ms
1265/2072 src/llm-providers/ollama/playground/tsconfig.json 2.35ms
1266/2072 src/llm-providers/ollama/register-configuration.ts 4.48ms
1267/2072 src/llm-providers/ollama/register-constructor.ts 2.62ms
1268/2072 src/llm-providers/openai/computeOpenAiUsage.test.ts 3.05ms
1269/2072 src/llm-providers/openai/computeOpenAiUsage.ts 7.37ms
1270/2072 src/llm-providers/openai/createOpenAiAssistantExecutionTools.ts 4.65ms
1271/2072 src/llm-providers/openai/createOpenAiCompatibleExecutionTools.ts 10.11ms
1272/2072 src/llm-providers/openai/createOpenAiExecutionTools.ts 4.96ms
1273/2072 src/llm-providers/openai/openai-models.test.ts 2.93ms
1274/2072 src/llm-providers/openai/openai-models.ts 20.08ms
1275/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts 3.92ms
1276/2072 src/llm-providers/openai/OpenAiAgentKitExecutionTools.ts 24.04ms
1277/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsInputBuilder.ts 7.84ms
1278/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOptions.ts 2.57ms
1279/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsOutputTypeMapper.ts 7.38ms
1280/2072 src/llm-providers/openai/OpenAiAgentKitExecutionToolsToolBuilder.ts 23.14ms
1281/2072 src/llm-providers/openai/OpenAiAssistantExecutionTools.ts 25.30ms
1282/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsOptions.ts 3.06ms
1283/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsProgressReporter.ts 6.76ms
1284/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsPromptBuilder.ts 6.63ms
1285/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsStreamRunner.ts 11.37ms
1286/2072 src/llm-providers/openai/OpenAiAssistantExecutionToolsToolRunner.ts 26.57ms
1287/2072 src/llm-providers/openai/OpenAiCompatibleExecutionTools.ts 10.75ms
1288/2072 src/llm-providers/openai/OpenAiCompatibleExecutionToolsOptions.ts 4.55ms
1289/2072 src/llm-providers/openai/OpenAiCompatibleModelCatalog.ts 58.88ms
1290/2072 src/llm-providers/openai/OpenAiCompatibleNonChatPromptCaller.ts 32.55ms
1291/2072 src/llm-providers/openai/OpenAiCompatibleRequestManager.ts 78.71ms
1292/2072 src/llm-providers/openai/OpenAiExecutionTools.ts 8.97ms
1293/2072 src/llm-providers/openai/OpenAiExecutionToolsOptions.ts 8.89ms
1294/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchHandler.ts 39.85ms
1295/2072 src/llm-providers/openai/OpenAiVectorStoreFileBatchPoller.ts 52.86ms
1296/2072 src/llm-providers/openai/OpenAiVectorStoreHandler.ts 11.26ms
1297/2072 src/llm-providers/openai/OpenAiVectorStoreKnowledgeSourcePreparer.ts 8.68ms
1298/2072 src/llm-providers/openai/playground/playground.ts 30.89ms
1299/2072 src/llm-providers/openai/playground/tsconfig.json 5.95ms
1300/2072 src/llm-providers/openai/register-configuration.ts 17.75ms
1301/2072 src/llm-providers/openai/register-constructor.ts 18.55ms
1302/2072 src/llm-providers/openai/utils/buildToolInvocationScript.ts 3.06ms
1303/2072 src/llm-providers/openai/utils/callOpenAiCompatibleChatModel.ts 16.67ms
1304/2072 src/llm-providers/openai/utils/loadOpenAiAgentsModule.ts 6.69ms
1305/2072 src/llm-providers/openai/utils/mapToolsToOpenAi.ts 3.84ms
1306/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatProgressReporter.ts 24.06ms
1307/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatPromptBuilder.ts 10.78ms
1308/2072 src/llm-providers/openai/utils/OpenAiCompatibleChatToolCaller.ts 12.74ms
1309/2072 src/llm-providers/openai/utils/OpenAiCompatibleUnsupportedParameterRetrier.ts 31.29ms
1310/2072 src/llm-providers/openai/utils/uploadFilesToOpenAi.ts 3.36ms
1311/2072 src/llm-providers/README.md 2.13ms
1312/2072 src/llm-providers/remote/playground/playground.ts 13.66ms
1313/2072 src/llm-providers/remote/playground/tsconfig.json 5.30ms
1314/2072 src/llm-providers/remote/RemoteLlmExecutionTools.ts 12.10ms
1315/2072 src/llm-providers/vercel/createExecutionToolsFromVercelProvider.ts 11.01ms
1316/2072 src/llm-providers/vercel/playground/playground.ts 8.72ms
1317/2072 src/llm-providers/vercel/playground/tsconfig.json 2.45ms
1318/2072 src/llm-providers/vercel/VercelExecutionToolsOptions.ts 4.59ms
1319/2072 src/llm-providers/vercel/VercelProvider.ts 2.71ms
1320/2072 src/migrations/migratePipeline.ts 7.01ms
1321/2072 src/other/README.md 1.80ms
1322/2072 src/other/templates/getBookTemplates.ts 6.42ms
1323/2072 src/personas/preparePersona.ts 7.84ms
1324/2072 src/personas/README.md 1.25ms
1325/2072 src/pipeline/book-notation.ts 4.12ms
1326/2072 src/pipeline/isValidPipelineString.test.ts 3.30ms
1327/2072 src/pipeline/isValidPipelineString.ts 4.55ms
1328/2072 src/pipeline/PipelineInterface/constants.ts 2.90ms
1329/2072 src/pipeline/PipelineInterface/getPipelineInterface.ts 5.66ms
1330/2072 src/pipeline/PipelineInterface/isPipelineImplementingInterface.ts 3.40ms
1331/2072 src/pipeline/PipelineInterface/isPipelineInterfacesEqual.ts 5.24ms
1332/2072 src/pipeline/PipelineInterface/PipelineInterface.ts 2.62ms
1333/2072 src/pipeline/PipelineInterface/README.md 1.91ms
1334/2072 src/pipeline/PipelineJson/CommonTaskJson.ts 5.32ms
1335/2072 src/pipeline/PipelineJson/DialogTaskJson.ts 2.83ms
1336/2072 src/pipeline/PipelineJson/Expectations.ts 3.36ms
1337/2072 src/pipeline/PipelineJson/KnowledgePieceJson.ts 3.35ms
1338/2072 src/pipeline/PipelineJson/KnowledgeSourceJson.ts 3.78ms
1339/2072 src/pipeline/PipelineJson/ParameterJson.ts 4.53ms
1340/2072 src/pipeline/PipelineJson/PersonaJson.ts 3.41ms
1341/2072 src/pipeline/PipelineJson/PipelineJson.ts 8.42ms
1342/2072 src/pipeline/PipelineJson/PreparationJson.ts 2.58ms
1343/2072 src/pipeline/PipelineJson/PromptTaskJson.ts 3.60ms
1344/2072 src/pipeline/PipelineJson/ScriptTaskJson.ts 3.28ms
1345/2072 src/pipeline/PipelineJson/SimpleTaskJson.ts 4.16ms
1346/2072 src/pipeline/PipelineJson/TaskJson.ts 2.62ms
1347/2072 src/pipeline/PipelineString.ts 3.13ms
1348/2072 src/pipeline/prompt-notation.test.ts 13.83ms
1349/2072 src/pipeline/prompt-notation.ts 22.30ms
1350/2072 src/pipeline/prompt-notation/helpers/ParameterEscaping.ts 4.15ms
1351/2072 src/pipeline/prompt-notation/helpers/ParameterNaming.ts 5.30ms
1352/2072 src/pipeline/prompt-notation/helpers/ParameterSection.ts 4.47ms
1353/2072 src/pipeline/validatePipelineString.ts 3.50ms
1354/2072 src/playground/permanent/_boilerplate.ts 2.21ms
1355/2072 src/playground/permanent/agent-with-browser-playground.ts 4.36ms
1356/2072 src/playground/permanent/error-handling-playground.ts 2.64ms
1357/2072 src/playground/playground.ts 2.57ms
1358/2072 src/playground/tsconfig.json 2.42ms
1359/2072 src/postprocessing/README.md 1.44ms
1360/2072 src/postprocessing/utils/extractBlock.ts 3.11ms
1361/2072 src/postprocessing/utils/extractJsonBlock.test.ts 3.76ms
1362/2072 src/postprocessing/utils/extractJsonBlock.ts 4.89ms
1363/2072 src/prepare/isPipelinePrepared.test.ts 3.29ms
1364/2072 src/prepare/isPipelinePrepared.ts 4.68ms
1365/2072 src/prepare/PrepareAndScrapeOptions.ts 5.65ms
1366/2072 src/prepare/preparePipeline.ts 12.91ms
1367/2072 src/prepare/preparePipelineOnRemoteServer.ts 5.34ms
1368/2072 src/prepare/prepareTasks.ts 8.33ms
1369/2072 src/prepare/unpreparePipeline.ts 5.06ms
1370/2072 src/remote-server/createRemoteClient.ts 7.04ms
1371/2072 src/remote-server/http-sample/README.md 1.52ms
1372/2072 src/remote-server/openapi-types.ts 11.03ms
1373/2072 src/remote-server/openapi.ts 10.31ms
1374/2072 src/remote-server/README.md 3.00ms
1375/2072 src/remote-server/RemoteServer.ts 3.11ms
1376/2072 src/remote-server/socket-types/_common/PromptbookServer_Error.ts 2.45ms
1377/2072 src/remote-server/socket-types/_subtypes/Identification.ts 5.52ms
1378/2072 src/remote-server/socket-types/_subtypes/identificationToPromptbookToken.ts 2.26ms
1379/2072 src/remote-server/socket-types/_subtypes/promptbookTokenToIdentification.ts 2.90ms
1380/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Request.ts 2.91ms
1381/2072 src/remote-server/socket-types/listModels/PromptbookServer_ListModels_Response.ts 2.46ms
1382/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Request.ts 2.37ms
1383/2072 src/remote-server/socket-types/prepare/PromptbookServer_PreparePipeline_Response.ts 2.81ms
1384/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Request.ts 2.06ms
1385/2072 src/remote-server/socket-types/prompt/PromptbookServer_Prompt_Response.ts 1.79ms
1386/2072 src/remote-server/startAgentServer.ts 5.71ms
1387/2072 src/remote-server/startRemoteServer.ts 7.93ms
1388/2072 src/remote-server/startRemoteServer/createRemoteServerExpressApp.ts 5.09ms
1389/2072 src/remote-server/startRemoteServer/createRemoteServerHandle.ts 3.13ms
1390/2072 src/remote-server/startRemoteServer/createSocketServer.ts 2.56ms
1391/2072 src/remote-server/startRemoteServer/getExecutionToolsFromIdentification.ts 8.48ms
1392/2072 src/remote-server/startRemoteServer/registerBookRoutes.ts 6.27ms
1393/2072 src/remote-server/startRemoteServer/registerExecutionRoutes.ts 8.63ms
1394/2072 src/remote-server/startRemoteServer/registerListModelsSocketHandler.ts 3.86ms
1395/2072 src/remote-server/startRemoteServer/registerLoginRoute.ts 5.82ms
1396/2072 src/remote-server/startRemoteServer/registerNotFoundRoute.ts 3.57ms
1397/2072 src/remote-server/startRemoteServer/registerOpenAiCompatibleChatCompletionsRoute.ts 8.96ms
1398/2072 src/remote-server/startRemoteServer/registerOpenApiRoutes.ts 3.92ms
1399/2072 src/remote-server/startRemoteServer/registerPreparePipelineSocketHandler.ts 6.31ms
1400/2072 src/remote-server/startRemoteServer/registerPromptSocketHandler.ts 6.31ms
1401/2072 src/remote-server/startRemoteServer/registerRemoteServerHttpRoutes.ts 3.25ms
1402/2072 src/remote-server/startRemoteServer/registerRemoteServerSocketHandlers.ts 4.02ms
1403/2072 src/remote-server/startRemoteServer/registerServerIndexRoute.ts 8.53ms
1404/2072 src/remote-server/startRemoteServer/RemoteServerRuntime.ts 2.61ms
1405/2072 src/remote-server/startRemoteServer/resolveStartRemoteServerConfiguration.ts 3.61ms
1406/2072 src/remote-server/startRemoteServer/respondToSocketRequest.ts 4.07ms
1407/2072 src/remote-server/startRemoteServer/SocketResponse.ts 1.91ms
1408/2072 src/remote-server/startRemoteServer/startListening.ts 65.31ms
1409/2072 src/remote-server/startRemoteServer/StartRemoteServerConfiguration.ts 3.45ms
1410/2072 src/remote-server/types/RemoteClientOptions.ts 3.30ms
1411/2072 src/remote-server/types/RemoteServerOptions.ts 13.41ms
1412/2072 src/remote-server/ui/renderServerIndexHtml.ts 8.55ms
1413/2072 src/remote-server/ui/ServerApp.tsx 14.69ms
1414/2072 src/remote-server/ui/types.ts 38.04ms
1415/2072 src/scrapers/_boilerplate/BoilerplateScraper.ts 13.58ms
1416/2072 src/scrapers/_boilerplate/createBoilerplateScraper.ts 9.89ms
1417/2072 src/scrapers/_boilerplate/playground/boilerplate-scraper-playground.ts 4.98ms
1418/2072 src/scrapers/_boilerplate/playground/tsconfig.json 5.02ms
1419/2072 src/scrapers/_boilerplate/README.md 1.89ms
1420/2072 src/scrapers/_boilerplate/register-constructor.ts 4.37ms
1421/2072 src/scrapers/_boilerplate/register-metadata.ts 5.66ms
1422/2072 src/scrapers/_common/Converter.ts 6.89ms
1423/2072 src/scrapers/_common/prepareKnowledgePieces.test.ts 13.35ms
1424/2072 src/scrapers/_common/prepareKnowledgePieces.ts 28.01ms
1425/2072 src/scrapers/_common/register/$provideFilesystemForNode.ts 3.70ms
1426/2072 src/scrapers/_common/register/$provideScrapersForBrowser.ts 3.91ms
1427/2072 src/scrapers/_common/register/$provideScrapersForNode.ts 5.19ms
1428/2072 src/scrapers/_common/register/$provideScriptingForNode.ts 11.71ms
1429/2072 src/scrapers/_common/register/$registeredScrapersMessage.ts 11.84ms
1430/2072 src/scrapers/_common/register/$scrapersMetadataRegister.ts 2.48ms
1431/2072 src/scrapers/_common/register/$scrapersRegister.ts 2.96ms
1432/2072 src/scrapers/_common/register/ScraperAndConverterMetadata.ts 7.11ms
1433/2072 src/scrapers/_common/register/ScraperConstructor.ts 2.65ms
1434/2072 src/scrapers/_common/Scraper.ts 7.19ms
1435/2072 src/scrapers/_common/ScraperIntermediateSource.ts 2.57ms
1436/2072 src/scrapers/_common/utils/fetchUrlContent.ts 7.34ms
1437/2072 src/scrapers/_common/utils/getScraperIntermediateSource.test.ts 3.11ms
1438/2072 src/scrapers/_common/utils/getScraperIntermediateSource.ts 14.23ms
1439/2072 src/scrapers/_common/utils/makeKnowledgeSourceHandler.ts 19.59ms
1440/2072 src/scrapers/_common/utils/promptbookFetch.test.ts 8.80ms
1441/2072 src/scrapers/_common/utils/promptbookFetch.ts 3.12ms
1442/2072 src/scrapers/document-legacy/createLegacyDocumentScraper.ts 9.59ms
1443/2072 src/scrapers/document-legacy/examples/10-simple.doc.knowledge.json 36.51ms
1444/2072 src/scrapers/document-legacy/examples/10-simple.rtf.knowledge.json 15.55ms
1445/2072 src/scrapers/document-legacy/LegacyDocumentScraper.test.ts 7.08ms
1446/2072 src/scrapers/document-legacy/LegacyDocumentScraper.ts 13.33ms
1447/2072 src/scrapers/document-legacy/playground/legacy-document-scraper-playground.ts 10.52ms
1448/2072 src/scrapers/document-legacy/playground/tsconfig.json 2.74ms
1449/2072 src/scrapers/document-legacy/README.md 2.18ms
1450/2072 src/scrapers/document-legacy/register-constructor.ts 3.49ms
1451/2072 src/scrapers/document-legacy/register-metadata.ts 9.79ms
1452/2072 src/scrapers/document/createDocumentScraper.ts 3.52ms
1453/2072 src/scrapers/document/DocumentScraper.test.ts 9.27ms
1454/2072 src/scrapers/document/DocumentScraper.ts 13.39ms
1455/2072 src/scrapers/document/examples/10-simple.docx.knowledge.json 25.44ms
1456/2072 src/scrapers/document/examples/10-simple.odt.knowledge.json 28.11ms
1457/2072 src/scrapers/document/playground/document-scraper-playground.ts 4.97ms
1458/2072 src/scrapers/document/playground/tsconfig.json 2.78ms
1459/2072 src/scrapers/document/README.md 2.26ms
1460/2072 src/scrapers/document/register-constructor.ts 2.86ms
1461/2072 src/scrapers/document/register-metadata.ts 5.68ms
1462/2072 src/scrapers/markdown/createMarkdownScraper.ts 3.75ms
1463/2072 src/scrapers/markdown/MarkdownScraper.test.ts 6.50ms
1464/2072 src/scrapers/markdown/MarkdownScraper.ts 13.56ms
1465/2072 src/scrapers/markdown/playground/markdown-scraper-playground.ts 6.04ms
1466/2072 src/scrapers/markdown/playground/tsconfig.json 5.28ms
1467/2072 src/scrapers/markdown/README.md 2.14ms
1468/2072 src/scrapers/markdown/register-constructor.ts 2.78ms
1469/2072 src/scrapers/markdown/register-metadata.ts 4.97ms
1470/2072 src/scrapers/markitdown/createMarkitdownScraper.ts 3.16ms
1471/2072 src/scrapers/markitdown/examples/10-simple.pdf.knowledge.json 15.03ms
1472/2072 src/scrapers/markitdown/examples/collboard-fra1-cdn-digitaloceanspaces-com-ptbk-user-knowledge-source-b-d-10-simple-pdf.knowledge.json 15.06ms
1473/2072 src/scrapers/markitdown/MarkitdownScraper.ts 8.93ms
1474/2072 src/scrapers/markitdown/playground/markitdown-scraper-playground.ts 5.20ms
1475/2072 src/scrapers/markitdown/playground/tsconfig.json 2.50ms
1476/2072 src/scrapers/markitdown/README.md 1.51ms
1477/2072 src/scrapers/markitdown/register-constructor.ts 5.38ms
1478/2072 src/scrapers/markitdown/register-metadata.ts 16.34ms
1479/2072 src/scrapers/pdf/createPdfScraper.ts 4.94ms
1480/2072 src/scrapers/pdf/lib/pdf2md/models/Annotation.js 2.15ms
1481/2072 src/scrapers/pdf/lib/pdf2md/models/HeadlineFinder.js 11.96ms
1482/2072 src/scrapers/pdf/lib/pdf2md/models/LineConverter.js 10.60ms
1483/2072 src/scrapers/pdf/lib/pdf2md/models/LineItem.js 4.36ms
1484/2072 src/scrapers/pdf/lib/pdf2md/models/LineItemBlock.js 2.59ms
1485/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/BlockType.js 9.03ms
1486/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordFormat.js 2.61ms
1487/2072 src/scrapers/pdf/lib/pdf2md/models/markdown/WordType.js 3.98ms
1488/2072 src/scrapers/pdf/lib/pdf2md/models/Metadata.js 2.09ms
1489/2072 src/scrapers/pdf/lib/pdf2md/models/Page.js 2.60ms
1490/2072 src/scrapers/pdf/lib/pdf2md/models/PageItem.js 4.81ms
1491/2072 src/scrapers/pdf/lib/pdf2md/models/ParsedElements.js 2.30ms
1492/2072 src/scrapers/pdf/lib/pdf2md/models/ParseResult.js 2.81ms
1493/2072 src/scrapers/pdf/lib/pdf2md/models/StashingStream.js 5.13ms
1494/2072 src/scrapers/pdf/lib/pdf2md/models/TextItem.js 3.22ms
1495/2072 src/scrapers/pdf/lib/pdf2md/models/TextItemLineGrouper.js 2.99ms
1496/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectCodeQuoteBlocks.js 7.02ms
1497/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/DetectListLevels.js 4.35ms
1498/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item-block/GatherBlocks.js 12.45ms
1499/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/CompactLines.js 7.36ms
1500/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectHeaders.js 11.99ms
1501/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectListItems.js 9.58ms
1502/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/DetectTOC.js 13.66ms
1503/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/RemoveRepetitiveElements.js 6.08ms
1504/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/line-item/VerticalToHorizontal.js 5.00ms
1505/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/text-item/CalculateGlobalStats.js 6.55ms
1506/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemBlockTransformation.js 10.48ms
1507/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToLineItemTransformation.js 3.39ms
1508/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToMarkdown.js 3.15ms
1509/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextBlocks.js 3.51ms
1510/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/ToTextItemTransformation.js 3.75ms
1511/2072 src/scrapers/pdf/lib/pdf2md/models/transformations/Transformation.js 2.57ms
1512/2072 src/scrapers/pdf/lib/pdf2md/models/Word.js 2.15ms
1513/2072 src/scrapers/pdf/lib/pdf2md/pdf2md-cli.js 16.21ms
1514/2072 src/scrapers/pdf/lib/pdf2md/pdf2md.js 4.95ms
1515/2072 src/scrapers/pdf/lib/pdf2md/README.md 2.35ms
1516/2072 src/scrapers/pdf/lib/pdf2md/util/cli.js 3.24ms
1517/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs.js 6.31ms
1518/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/createDocumentStub.js 8.63ms
1519/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElement.js 13.86ms
1520/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/DOMElementSerializer.js 5.76ms
1521/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/Image.js 4.13ms
1522/2072 src/scrapers/pdf/lib/pdf2md/util/dom-stubs/xmlEncode.js 2.98ms
1523/2072 src/scrapers/pdf/lib/pdf2md/util/page-item-functions.js 4.53ms
1524/2072 src/scrapers/pdf/lib/pdf2md/util/page-number-functions.js 4.88ms
1525/2072 src/scrapers/pdf/lib/pdf2md/util/pdf.js 9.70ms
1526/2072 src/scrapers/pdf/lib/pdf2md/util/string-functions.js 7.06ms
1527/2072 src/scrapers/pdf/lib/pdf2md/util/transformations.js 5.77ms
1528/2072 src/scrapers/pdf/PdfScraper.ts 20.87ms
1529/2072 src/scrapers/pdf/playground/pdf-scraper-playground.ts 6.20ms
1530/2072 src/scrapers/pdf/playground/tsconfig.json 5.22ms
1531/2072 src/scrapers/pdf/README.md 1.94ms
1532/2072 src/scrapers/pdf/register-constructor.ts 6.61ms
1533/2072 src/scrapers/pdf/register-metadata.ts 6.26ms
1534/2072 src/scrapers/README.md 2.32ms
1535/2072 src/scrapers/website/createWebsiteScraper.ts 7.66ms
1536/2072 src/scrapers/website/playground/tsconfig.json 2.92ms
1537/2072 src/scrapers/website/playground/website-scraper-playground.ts 9.03ms
1538/2072 src/scrapers/website/README.md 2.06ms
1539/2072 src/scrapers/website/register-constructor.ts 2.24ms
1540/2072 src/scrapers/website/register-metadata.ts 5.54ms
1541/2072 src/scrapers/website/utils/createShowdownConverter.test.ts 3.64ms
1542/2072 src/scrapers/website/utils/createShowdownConverter.ts 2.23ms
1543/2072 src/scrapers/website/WebsiteScraper.ts 12.23ms
1544/2072 src/scripting/_test/custom-function-async.test.ts.test.ts 9.81ms
1545/2072 src/scripting/_test/custom-function-missing.test.ts 4.41ms
1546/2072 src/scripting/_test/custom-function-with-dependencies.test.ts 7.36ms
1547/2072 src/scripting/_test/custom-function.test.ts 40.88ms
1548/2072 src/scripting/_test/postprocessing.test.ts 10.18ms
1549/2072 src/scripting/_test/script-execution-errors.test.ts 20.10ms
1550/2072 src/scripting/_test/script-execution-tools.test.ts 18.64ms
1551/2072 src/scripting/javascript/JavascriptEvalExecutionTools.test.ts 6.03ms
1552/2072 src/scripting/javascript/JavascriptEvalExecutionTools.ts 29.86ms
1553/2072 src/scripting/javascript/JavascriptExecutionTools.ts 2.44ms
1554/2072 src/scripting/javascript/JavascriptExecutionToolsOptions.ts 3.68ms
1555/2072 src/scripting/javascript/postprocessing-functions.ts 7.35ms
1556/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts 2.31ms
1557/2072 src/scripting/javascript/utils/extractVariablesFromJavascript.ts 4.20ms
1558/2072 src/scripting/javascript/utils/extractVariablesFromScript.test.ts 2.53ms
1559/2072 src/scripting/python/PythonExecutionTools.ts 3.59ms
1560/2072 src/scripting/typescript/TypescriptExecutionTools.ts 3.30ms
1561/2072 src/search-engines/_index.ts 2.16ms
1562/2072 src/search-engines/bing/BingSearchEngine.ts 8.48ms
1563/2072 src/search-engines/dummy/DummySearchEngine.ts 3.07ms
1564/2072 src/search-engines/google/GoogleSearchEngine.ts 4.61ms
1565/2072 src/search-engines/SearchEngine.ts 3.26ms
1566/2072 src/search-engines/SearchResult.ts 2.60ms
1567/2072 src/search-engines/serp/SerpSearchEngine.ts 3.52ms
1568/2072 src/speech-recognition/BrowserSpeechRecognition.ts 5.30ms
1569/2072 src/speech-recognition/OpenAiSpeechRecognition.test.ts 11.30ms
1570/2072 src/speech-recognition/OpenAiSpeechRecognition.ts 23.45ms
1571/2072 src/storage/_common/PromptbookStorage.test-type.ts 3.52ms
1572/2072 src/storage/_common/PromptbookStorage.ts 4.69ms
1573/2072 src/storage/blackhole/BlackholeStorage.ts 10.34ms
1574/2072 src/storage/env-storage/$EnvStorage.ts 8.75ms
1575/2072 src/storage/file-cache-storage/FileCacheStorage.ts 10.72ms
1576/2072 src/storage/file-cache-storage/FileCacheStorageOptions.ts 2.42ms
1577/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts 1.94ms
1578/2072 src/storage/file-cache-storage/utils/nameToSubfolderPath.ts 1.81ms
1579/2072 src/storage/local-storage/getIndexedDbStorage.ts 4.24ms
1580/2072 src/storage/local-storage/getLocalStorage.ts 3.19ms
1581/2072 src/storage/local-storage/getSessionStorage.ts 3.63ms
1582/2072 src/storage/local-storage/utils/IndexedDbStorageOptions.ts 2.03ms
1583/2072 src/storage/local-storage/utils/makePromptbookStorageFromIndexedDb.ts 4.34ms
1584/2072 src/storage/local-storage/utils/makePromptbookStorageFromWebStorage.ts 5.88ms
1585/2072 src/storage/memory/MemoryStorage.ts 4.11ms
1586/2072 src/storage/utils/PrefixStorage.ts 4.97ms
1587/2072 src/transpilers/_common/BookTranspiler.ts 3.04ms
1588/2072 src/transpilers/_common/BookTranspilerOptions.ts 6.43ms
1589/2072 src/transpilers/_common/createTranspiledTeamRuntimeSection.ts 12.60ms
1590/2072 src/transpilers/_common/createZodSchemaSource.ts 5.91ms
1591/2072 src/transpilers/_common/formatUsedToolFunctions.test.ts 3.38ms
1592/2072 src/transpilers/_common/formatUsedToolFunctions.ts 7.74ms
1593/2072 src/transpilers/_common/prepareSdkTranspilerContext.ts 8.02ms
1594/2072 src/transpilers/_common/register/$bookTranspilersRegister.ts 2.51ms
1595/2072 src/transpilers/_common/resolveClaudeModelName.ts 2.31ms
1596/2072 src/transpilers/_common/TranspiledTeamExport.ts 6.65ms
1597/2072 src/transpilers/_common/transpiledTeamTranspilers.test.ts 5.48ms
1598/2072 src/transpilers/agent-os/AgentOsTranspiler.test.ts 5.90ms
1599/2072 src/transpilers/agent-os/AgentOsTranspiler.ts 17.08ms
1600/2072 src/transpilers/agent-os/register.ts 5.12ms
1601/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts 6.16ms
1602/2072 src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.ts 17.15ms
1603/2072 src/transpilers/anthropic-claude-managed/register.ts 2.34ms
1604/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts 6.22ms
1605/2072 src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.ts 11.96ms
1606/2072 src/transpilers/anthropic-claude-sdk/register.ts 2.56ms
1607/2072 src/transpilers/e2b/E2BTranspiler.test.ts 4.42ms
1608/2072 src/transpilers/e2b/E2BTranspiler.ts 11.58ms
1609/2072 src/transpilers/e2b/register.ts 3.08ms
1610/2072 src/transpilers/formatted-book-in-markdown/FormattedBookInMarkdownTranspiler.ts 3.44ms
1611/2072 src/transpilers/formatted-book-in-markdown/register.ts 3.81ms
1612/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts 5.32ms
1613/2072 src/transpilers/openai-agents/OpenAiAgentsTranspiler.ts 18.68ms
1614/2072 src/transpilers/openai-agents/register.ts 2.89ms
1615/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts 7.26ms
1616/2072 src/transpilers/openai-sdk/OpenAiSdkTranspiler.ts 10.22ms
1617/2072 src/transpilers/openai-sdk/playground/playground.ts 2.69ms
1618/2072 src/transpilers/openai-sdk/playground/tsconfig.json 2.72ms
1619/2072 src/transpilers/openai-sdk/register.ts 3.31ms
1620/2072 src/types/Arrayable.ts 2.19ms
1621/2072 src/types/InputParameters_private.ts 2.81ms
1622/2072 src/types/IntermediateFilesStrategy.ts 4.91ms
1623/2072 src/types/LlmCall.ts 1.95ms
1624/2072 src/types/LlmToolDefinition.ts 3.98ms
1625/2072 src/types/Message.ts 4.04ms
1626/2072 src/types/ModelRequirements.ts 8.04ms
1627/2072 src/types/ModelVariant.ts 2.22ms
1628/2072 src/types/NonEmptyArray.ts 2.19ms
1629/2072 src/types/number_bytes.ts 1.59ms
1630/2072 src/types/number_id.ts 1.84ms
1631/2072 src/types/number_likeness.ts 1.49ms
1632/2072 src/types/number_milliseconds.ts 1.66ms
1633/2072 src/types/number_percent.ts 3.40ms
1634/2072 src/types/number_positive.ts 2.53ms
1635/2072 src/types/number_usd.ts 2.42ms
1636/2072 src/types/Parameters_private.ts 2.70ms
1637/2072 src/types/Parameters.ts 6.37ms
1638/2072 src/types/Prompt.ts 6.31ms
1639/2072 src/types/ReservedParameters_private.ts 3.23ms
1640/2072 src/types/ScriptLanguage.ts 2.09ms
1641/2072 src/types/SectionType.ts 2.43ms
1642/2072 src/types/SpeechRecognition.ts 7.14ms
1643/2072 src/types/string_agent_hash_private.ts 1.61ms
1644/2072 src/types/string_agent_name_in_book_private.ts 1.83ms
1645/2072 src/types/string_agent_name_private.ts 1.46ms
1646/2072 src/types/string_agent_name.ts 4.31ms
1647/2072 src/types/string_agent_permanent_id_private.ts 1.79ms
1648/2072 src/types/string_agent_url_private.ts 3.35ms
1649/2072 src/types/string_agent_url.ts 1.54ms
1650/2072 src/types/string_base_url_private.ts 2.91ms
1651/2072 src/types/string_base_url.ts 1.80ms
1652/2072 src/types/string_base64_private.ts 2.69ms
1653/2072 src/types/string_base64.ts 1.75ms
1654/2072 src/types/string_business_category_name_private.ts 1.66ms
1655/2072 src/types/string_business_category_name.ts 3.81ms
1656/2072 src/types/string_char_private.ts 2.75ms
1657/2072 src/types/string_chat_prompt_private.ts 1.82ms
1658/2072 src/types/string_completion_prompt_private.ts 1.90ms
1659/2072 src/types/string_email_private.ts 1.98ms
1660/2072 src/types/string_email.ts 3.26ms
1661/2072 src/types/string_filename.ts 5.68ms
1662/2072 src/types/string_host_private.ts 3.73ms
1663/2072 src/types/string_host.ts 3.08ms
1664/2072 src/types/string_href_private.ts 1.85ms
1665/2072 src/types/string_href.ts 1.87ms
1666/2072 src/types/string_knowledge_source_content.ts 4.53ms
1667/2072 src/types/string_markdown.ts 4.54ms
1668/2072 src/types/string_mime_type_private.ts 1.90ms
1669/2072 src/types/string_mime_type.ts 1.89ms
1670/2072 src/types/string_model_description_private.ts 1.72ms
1671/2072 src/types/string_model_name_private.ts 2.80ms
1672/2072 src/types/string_model_name.ts 1.52ms
1673/2072 src/types/string_name_private.ts 1.49ms
1674/2072 src/types/string_name.ts 3.72ms
1675/2072 src/types/string_page_private.ts 2.12ms
1676/2072 src/types/string_page.ts 1.65ms
1677/2072 src/types/string_parameter_name.ts 9.20ms
1678/2072 src/types/string_parameter_value_private.ts 1.69ms
1679/2072 src/types/string_person_fullname.ts 5.29ms
1680/2072 src/types/string_persona_description_private.ts 2.59ms
1681/2072 src/types/string_persona_description.ts 2.49ms
1682/2072 src/types/string_pipeline_root_url_private.ts 2.57ms
1683/2072 src/types/string_pipeline_root_url.ts 2.76ms
1684/2072 src/types/string_pipeline_url_private.ts 1.58ms
1685/2072 src/types/string_pipeline_url.ts 1.90ms
1686/2072 src/types/string_prompt_image_private.ts 2.87ms
1687/2072 src/types/string_prompt_private.ts 1.63ms
1688/2072 src/types/string_prompt.ts 45.31ms
1689/2072 src/types/string_promptbook_server_url_private.ts 3.44ms
1690/2072 src/types/string_promptbook_server_url.ts 33.91ms
1691/2072 src/types/string_reserved_parameter_name_private.ts 1.80ms
1692/2072 src/types/string_sha256.ts 23.66ms
1693/2072 src/types/string_system_message_private.ts 6.20ms
1694/2072 src/types/string_template_private.ts 2.84ms
1695/2072 src/types/string_text_prompt_private.ts 1.96ms
1696/2072 src/types/string_title_private.ts 36.63ms
1697/2072 src/types/string_title.ts 1.84ms
1698/2072 src/types/string_token.ts 3.59ms
1699/2072 src/types/string_url_image_private.ts 1.70ms
1700/2072 src/types/string_url_image.ts 1.51ms
1701/2072 src/types/string_url_private.ts 1.73ms
1702/2072 src/types/string_url.ts 18.70ms
1703/2072 src/types/TaskType.ts 3.83ms
1704/2072 src/types/ToolCall.ts 8.58ms
1705/2072 src/types/typeAliasEmoji.ts 6.99ms
1706/2072 src/types/typeAliases.ts 5.13ms
1707/2072 src/types/Updatable.ts 3.45ms
1708/2072 src/utils/agent-message-runtime/AgentMessageProjectChange.ts 5.83ms
1709/2072 src/utils/agent-message-runtime/agentMessageRuntimePaths.ts 4.44ms
1710/2072 src/utils/agent-message-runtime/AgentMessageTouchedExternalSource.ts 4.79ms
1711/2072 src/utils/agent-message-runtime/parseAgentMessageRuntimeLogEvents.ts 5.38ms
1712/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts 8.76ms
1713/2072 src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.ts 9.98ms
1714/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts 5.70ms
1715/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.ts 17.48ms
1716/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts 14.28ms
1717/2072 src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.ts 16.17ms
1718/2072 src/utils/agents/resolveAgentAvatarImageUrl.test.ts 13.74ms
1719/2072 src/utils/agents/resolveAgentAvatarImageUrl.ts 12.73ms
1720/2072 src/utils/agents/terminalAgentAvatarVisual.test.ts 5.12ms
1721/2072 src/utils/agents/terminalAgentAvatarVisual.ts 30.31ms
1722/2072 src/utils/ascii-art/$detectTerminalAnsiColorDepth.ts 3.74ms
1723/2072 src/utils/ascii-art/convertImageDataToAsciiArt.ts 10.30ms
1724/2072 src/utils/ascii-art/createAnsiColorCode.ts 7.68ms
1725/2072 src/utils/chat/chatAttachments.test.ts 11.60ms
1726/2072 src/utils/chat/chatAttachments.ts 11.28ms
1727/2072 src/utils/chat/chatAttachments/appendChatAttachmentContext.ts 5.02ms
1728/2072 src/utils/chat/chatAttachments/appendChatAttachmentContextWithContent.ts 8.39ms
1729/2072 src/utils/chat/chatAttachments/appendChatContextSections.ts 5.32ms
1730/2072 src/utils/chat/chatAttachments/formatChatAttachmentContentContext.ts 11.55ms
1731/2072 src/utils/chat/chatAttachments/formatChatAttachmentContext.ts 5.71ms
1732/2072 src/utils/chat/chatAttachments/normalizeChatAttachments.ts 10.80ms
1733/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContent.ts 7.17ms
1734/2072 src/utils/chat/chatAttachments/resolveChatAttachmentContents.ts 5.75ms
1735/2072 src/utils/chat/constants.ts 2.40ms
1736/2072 src/utils/chat/decodeChatStreamWhitespaceFromTransport.ts 2.47ms
1737/2072 src/utils/chat/encodeChatStreamWhitespaceForTransport.ts 3.56ms
1738/2072 src/utils/chat/escapeRegExp.ts 1.84ms
1739/2072 src/utils/clientVersion.ts 8.53ms
1740/2072 src/utils/color/$randomColor.ts 2.38ms
1741/2072 src/utils/color/Color.test.ts 3.58ms
1742/2072 src/utils/color/Color.ts 8.65ms
1743/2072 src/utils/color/ColorValue.ts 4.52ms
1744/2072 src/utils/color/css-colors.ts 7.85ms
1745/2072 src/utils/color/internal-utils/checkChannelValue.ts 2.27ms
1746/2072 src/utils/color/internal-utils/hslToRgb.ts 2.40ms
1747/2072 src/utils/color/internal-utils/rgbToHsl.ts 2.45ms
1748/2072 src/utils/color/isHexColorString.ts 1.85ms
1749/2072 src/utils/color/operators/ColorTransformer.ts 1.63ms
1750/2072 src/utils/color/operators/darken.ts 1.96ms
1751/2072 src/utils/color/operators/furthest.ts 2.22ms
1752/2072 src/utils/color/operators/grayscale.ts 3.65ms
1753/2072 src/utils/color/operators/lighten.ts 3.43ms
1754/2072 src/utils/color/operators/mixWithColor.ts 2.81ms
1755/2072 src/utils/color/operators/nearest.ts 4.69ms
1756/2072 src/utils/color/operators/negative.ts 1.98ms
1757/2072 src/utils/color/operators/negativeLightness.ts 3.90ms
1758/2072 src/utils/color/operators/README.md 1.48ms
1759/2072 src/utils/color/operators/saturate.ts 4.58ms
1760/2072 src/utils/color/operators/withAlpha.ts 2.55ms
1761/2072 src/utils/color/parseColorString.ts 4.76ms
1762/2072 src/utils/color/parsers/ColorChannelSet.ts 4.58ms
1763/2072 src/utils/color/parsers/parseHexColor.ts 4.80ms
1764/2072 src/utils/color/parsers/parseHslColor.ts 3.37ms
1765/2072 src/utils/color/parsers/parseRgbColor.ts 3.32ms
1766/2072 src/utils/color/utils/areColorsEqual.ts 2.45ms
1767/2072 src/utils/color/utils/colorDistance.ts 8.36ms
1768/2072 src/utils/color/utils/colorHue.ts 3.49ms
1769/2072 src/utils/color/utils/colorHueDistance.test.ts 3.86ms
1770/2072 src/utils/color/utils/colorHueDistance.ts 2.20ms
1771/2072 src/utils/color/utils/colorLuminance.ts 2.04ms
1772/2072 src/utils/color/utils/colorSatulightion.ts 2.41ms
1773/2072 src/utils/color/utils/colorSaturation.ts 3.07ms
1774/2072 src/utils/color/utils/colorToDataUrl.ts 4.84ms
1775/2072 src/utils/color/utils/mixColors.ts 5.27ms
1776/2072 src/utils/database/uniqueConstraint.ts 4.54ms
1777/2072 src/utils/DEFAULT_THINKING_MESSAGES.ts 2.89ms
1778/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts 3.73ms
1779/2072 src/utils/editable/edit-pipeline-string/addPipelineCommand.ts 3.88ms
1780/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts 8.19ms
1781/2072 src/utils/editable/edit-pipeline-string/deflatePipeline.ts 4.76ms
1782/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts 3.98ms
1783/2072 src/utils/editable/edit-pipeline-string/removePipelineCommand.ts 3.94ms
1784/2072 src/utils/editable/types/PipelineEditableSerialized.ts 5.65ms
1785/2072 src/utils/editable/utils/isFlatPipeline.test.ts 3.92ms
1786/2072 src/utils/editable/utils/isFlatPipeline.ts 17.78ms
1787/2072 src/utils/editable/utils/renamePipelineParameter.test.ts 7.70ms
1788/2072 src/utils/editable/utils/renamePipelineParameter.ts 20.16ms
1789/2072 src/utils/editable/utils/stringifyPipelineJson.ts 5.02ms
1790/2072 src/utils/environment/$detectRuntimeEnvironment.ts 8.20ms
1791/2072 src/utils/environment/$getGlobalScope.ts 2.78ms
1792/2072 src/utils/environment/$isRunningInBrowser.ts 2.10ms
1793/2072 src/utils/environment/$isRunningInJest.ts 5.29ms
1794/2072 src/utils/environment/$isRunningInNode.ts 2.23ms
1795/2072 src/utils/environment/$isRunningInWebWorker.ts 3.10ms
1796/2072 src/utils/execCommand/$execCommand.ts 10.19ms
1797/2072 src/utils/execCommand/$execCommandNormalizeOptions.ts 4.07ms
1798/2072 src/utils/execCommand/$execCommands.ts 4.19ms
1799/2072 src/utils/execCommand/execCommand.test.ts 3.95ms
1800/2072 src/utils/execCommand/execCommandNormalizeOptions.test.ts 33.63ms
1801/2072 src/utils/execCommand/ExecCommandOptions.ts 10.17ms
1802/2072 src/utils/expectation-counters/constants.ts 2.60ms
1803/2072 src/utils/expectation-counters/countCharacters.test.ts 3.86ms
1804/2072 src/utils/expectation-counters/countCharacters.ts 2.61ms
1805/2072 src/utils/expectation-counters/countLines.test.ts 2.30ms
1806/2072 src/utils/expectation-counters/countLines.ts 16.49ms
1807/2072 src/utils/expectation-counters/countPages.test.ts 11.58ms
1808/2072 src/utils/expectation-counters/countPages.ts 2.68ms
1809/2072 src/utils/expectation-counters/countParagraphs.test.ts 3.29ms
1810/2072 src/utils/expectation-counters/countParagraphs.ts 3.48ms
1811/2072 src/utils/expectation-counters/countSentences.test.ts 2.44ms
1812/2072 src/utils/expectation-counters/countSentences.ts 3.19ms
1813/2072 src/utils/expectation-counters/countWords.test.ts 3.22ms
1814/2072 src/utils/expectation-counters/countWords.ts 5.63ms
1815/2072 src/utils/expectation-counters/index.ts 5.18ms
1816/2072 src/utils/expectation-counters/other/countPages.md 5.09ms
1817/2072 src/utils/files/$induceBookDownload.ts 4.91ms
1818/2072 src/utils/files/$induceFileDownload.ts 5.72ms
1819/2072 src/utils/files/decodeAttachmentAsText.test.ts 7.77ms
1820/2072 src/utils/files/decodeAttachmentAsText.ts 25.18ms
1821/2072 src/utils/files/extensionToMimeType.test.ts 6.67ms
1822/2072 src/utils/files/extensionToMimeType.ts 3.98ms
1823/2072 src/utils/files/getFileExtension.test.ts 2.18ms
1824/2072 src/utils/files/getFileExtension.ts 2.31ms
1825/2072 src/utils/files/isDirectoryExisting.test.ts 8.61ms
1826/2072 src/utils/files/isDirectoryExisting.ts 9.73ms
1827/2072 src/utils/files/isExecutable.ts 5.28ms
1828/2072 src/utils/files/isFileExisting.test.ts 53.99ms
1829/2072 src/utils/files/isFileExisting.ts 3.90ms
1830/2072 src/utils/files/listAllFiles.test.ts 2.74ms
1831/2072 src/utils/files/listAllFiles.ts 3.50ms
1832/2072 src/utils/files/mimeTypeToExtension.test.ts 2.99ms
1833/2072 src/utils/files/mimeTypeToExtension.ts 24.01ms
1834/2072 src/utils/files/ObjectUrl.ts 34.22ms
1835/2072 src/utils/files/readResponseBytes.ts 14.18ms
1836/2072 src/utils/filesystem/promptbookTemporaryPath.test.ts 10.77ms
1837/2072 src/utils/filesystem/promptbookTemporaryPath.ts 5.59ms
1838/2072 src/utils/html/escapeHtml.ts 3.32ms
1839/2072 src/utils/isTimingSafeEqualString.ts 4.15ms
1840/2072 src/utils/knowledge/inlineKnowledgeSource.test.ts 3.38ms
1841/2072 src/utils/knowledge/inlineKnowledgeSource.ts 16.11ms
1842/2072 src/utils/knowledge/simplifyKnowledgeLabel.test.ts 11.36ms
1843/2072 src/utils/knowledge/simplifyKnowledgeLabel.ts 10.70ms
1844/2072 src/utils/language/getBrowserPreferredSpeechRecognitionLanguage.ts 4.57ms
1845/2072 src/utils/linguistic-hash/linguisticHash.test.ts 4.07ms
1846/2072 src/utils/linguistic-hash/linguisticHash.ts 4.46ms
1847/2072 src/utils/linguistic-hash/LinguisticHashLanguage.ts 3.79ms
1848/2072 src/utils/linguistic-hash/linguisticHashTypes.ts 2.26ms
1849/2072 src/utils/linguistic-hash/linguisticHashWordCount.ts 5.49ms
1850/2072 src/utils/linguistic-hash/linguisticHashWords.cs.ts 5.68ms
1851/2072 src/utils/linguistic-hash/linguisticHashWords.en.ts 20.19ms
1852/2072 src/utils/linguistic-hash/linguisticHashWordSelection.ts 8.27ms
1853/2072 src/utils/markdown/addAutoGeneratedSection.test.ts 4.48ms
1854/2072 src/utils/markdown/addAutoGeneratedSection.ts 9.21ms
1855/2072 src/utils/markdown/createMarkdownChart.test.ts 8.75ms
1856/2072 src/utils/markdown/createMarkdownChart.ts 6.72ms
1857/2072 src/utils/markdown/createMarkdownTable.test.ts 9.80ms
1858/2072 src/utils/markdown/createMarkdownTable.ts 4.60ms
1859/2072 src/utils/markdown/escapeMarkdownBlock.test.ts 3.62ms
1860/2072 src/utils/markdown/escapeMarkdownBlock.ts 8.56ms
1861/2072 src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts 7.14ms
1862/2072 src/utils/markdown/extractAllBlocksFromMarkdown.test.ts 7.80ms
1863/2072 src/utils/markdown/extractAllBlocksFromMarkdown.ts 7.49ms
1864/2072 src/utils/markdown/extractAllListItemsFromMarkdown.test.ts 5.43ms
1865/2072 src/utils/markdown/extractAllListItemsFromMarkdown.ts 2.94ms
1866/2072 src/utils/markdown/extractOneBlockFromMarkdown.test.ts 7.14ms
1867/2072 src/utils/markdown/extractOneBlockFromMarkdown.ts 6.82ms
1868/2072 src/utils/markdown/flattenMarkdown.test.ts 7.69ms
1869/2072 src/utils/markdown/flattenMarkdown.ts 15.31ms
1870/2072 src/utils/markdown/humanizeAiText.test.ts 5.46ms
1871/2072 src/utils/markdown/humanizeAiText.ts 11.32ms
1872/2072 src/utils/markdown/humanizeAiTextEllipsis.ts 5.52ms
1873/2072 src/utils/markdown/humanizeAiTextEmdashed.ts 3.08ms
1874/2072 src/utils/markdown/humanizeAiTextQuotes.ts 2.21ms
1875/2072 src/utils/markdown/humanizeAiTextSources.ts 7.15ms
1876/2072 src/utils/markdown/humanizeAiTextWhitespace.ts 9.36ms
1877/2072 src/utils/markdown/parseMarkdownSection.test.ts 2.83ms
1878/2072 src/utils/markdown/parseMarkdownSection.ts 12.39ms
1879/2072 src/utils/markdown/prettifyMarkdown.ts 4.28ms
1880/2072 src/utils/markdown/promptbookifyAiText.test.ts 8.99ms
1881/2072 src/utils/markdown/promptbookifyAiText.ts 4.71ms
1882/2072 src/utils/markdown/README.md 1.76ms
1883/2072 src/utils/markdown/removeMarkdownComments.test.ts 4.34ms
1884/2072 src/utils/markdown/removeMarkdownComments.ts 3.13ms
1885/2072 src/utils/markdown/removeMarkdownFormatting.test.ts 5.52ms
1886/2072 src/utils/markdown/removeMarkdownFormatting.ts 3.71ms
1887/2072 src/utils/markdown/removeMarkdownLinks.test.ts 2.21ms
1888/2072 src/utils/markdown/removeMarkdownLinks.ts 5.50ms
1889/2072 src/utils/markdown/shiftMarkdownHeadingLevels.ts 8.20ms
1890/2072 src/utils/markdown/splitMarkdownIntoSections.test.ts 7.77ms
1891/2072 src/utils/markdown/splitMarkdownIntoSections.ts 6.07ms
1892/2072 src/utils/markdown/trimCodeBlock.test.ts 6.71ms
1893/2072 src/utils/markdown/trimCodeBlock.ts 2.85ms
1894/2072 src/utils/markdown/trimEndOfCodeBlock.test.ts 3.96ms
1895/2072 src/utils/markdown/trimEndOfCodeBlock.ts 3.76ms
1896/2072 src/utils/misc/$getCurrentDate.ts 1.98ms
1897/2072 src/utils/misc/$Register.ts 7.17ms
1898/2072 src/utils/misc/aboutPromptbookInformation.ts 6.14ms
1899/2072 src/utils/misc/arrayableToArray.test.ts 3.56ms
1900/2072 src/utils/misc/arrayableToArray.ts 2.67ms
1901/2072 src/utils/misc/computeHash.test.ts 2.11ms
1902/2072 src/utils/misc/computeHash.ts 4.01ms
1903/2072 src/utils/misc/createLazyModuleLoader.ts 4.53ms
1904/2072 src/utils/misc/debounce.test.ts 3.83ms
1905/2072 src/utils/misc/debounce.ts 2.21ms
1906/2072 src/utils/misc/emojis.ts 7.49ms
1907/2072 src/utils/misc/FromtoItems.ts 2.20ms
1908/2072 src/utils/misc/injectCssModuleIntoShadowRoot.tsx 4.50ms
1909/2072 src/utils/misc/parseNumber.test.ts 3.60ms
1910/2072 src/utils/misc/parseNumber.ts 5.08ms
1911/2072 src/utils/misc/xAboutPromptbookInformation.tsx 3.00ms
1912/2072 src/utils/normalization/capitalize.test.ts 2.25ms
1913/2072 src/utils/normalization/capitalize.ts 3.52ms
1914/2072 src/utils/normalization/constructImageFilename.test.ts 4.84ms
1915/2072 src/utils/normalization/constructImageFilename.ts 3.43ms
1916/2072 src/utils/normalization/decapitalize.test.ts 2.13ms
1917/2072 src/utils/normalization/decapitalize.ts 6.26ms
1918/2072 src/utils/normalization/DIACRITIC_VARIANTS_LETTERS.ts 5.61ms
1919/2072 src/utils/normalization/IKeywords.ts 4.07ms
1920/2072 src/utils/normalization/index.ts 2.50ms
1921/2072 src/utils/normalization/isValidKeyword.test.ts 2.19ms
1922/2072 src/utils/normalization/isValidKeyword.ts 3.42ms
1923/2072 src/utils/normalization/nameToUriPart.test.ts 2.56ms
1924/2072 src/utils/normalization/nameToUriPart.ts 3.67ms
1925/2072 src/utils/normalization/nameToUriParts.test.ts 2.58ms
1926/2072 src/utils/normalization/nameToUriParts.ts 2.17ms
1927/2072 src/utils/normalization/normalize-to-kebab-case.test.ts 4.07ms
1928/2072 src/utils/normalization/normalize-to-kebab-case.ts 3.77ms
1929/2072 src/utils/normalization/normalizeMessageText.test.ts 3.33ms
1930/2072 src/utils/normalization/normalizeMessageText.ts 3.70ms
1931/2072 src/utils/normalization/normalizeTo_camelCase.test.ts 2.78ms
1932/2072 src/utils/normalization/normalizeTo_camelCase.ts 5.44ms
1933/2072 src/utils/normalization/normalizeTo_PascalCase.test.ts 6.82ms
1934/2072 src/utils/normalization/normalizeTo_PascalCase.ts 3.38ms
1935/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts 4.24ms
1936/2072 src/utils/normalization/normalizeTo_SCREAMING_CASE.ts 6.18ms
1937/2072 src/utils/normalization/normalizeTo_snake_case.test.ts 3.65ms
1938/2072 src/utils/normalization/normalizeTo_snake_case.ts 2.30ms
1939/2072 src/utils/normalization/normalizeWhitespaces.test.ts 5.73ms
1940/2072 src/utils/normalization/normalizeWhitespaces.ts 3.37ms
1941/2072 src/utils/normalization/orderJson.test.ts 2.32ms
1942/2072 src/utils/normalization/orderJson.ts 3.07ms
1943/2072 src/utils/normalization/parseKeywords.test.ts 3.01ms
1944/2072 src/utils/normalization/parseKeywords.ts 3.00ms
1945/2072 src/utils/normalization/parseKeywordsFromString.test.ts 9.17ms
1946/2072 src/utils/normalization/parseKeywordsFromString.ts 6.51ms
1947/2072 src/utils/normalization/removeDiacritics.test.ts 7.10ms
1948/2072 src/utils/normalization/removeDiacritics.ts 9.54ms
1949/2072 src/utils/normalization/removeEmojis.test.ts 13.35ms
1950/2072 src/utils/normalization/removeEmojis.ts 2.80ms
1951/2072 src/utils/normalization/removeQuotes.test.ts 2.26ms
1952/2072 src/utils/normalization/removeQuotes.ts 3.29ms
1953/2072 src/utils/normalization/searchKeywords.test.ts 2.53ms
1954/2072 src/utils/normalization/searchKeywords.ts 3.61ms
1955/2072 src/utils/normalization/suffixUrl.test.ts 2.47ms
1956/2072 src/utils/normalization/suffixUrl.ts 2.25ms
1957/2072 src/utils/normalization/titleToName.test.ts 4.60ms
1958/2072 src/utils/normalization/titleToName.ts 4.70ms
1959/2072 src/utils/normalization/unwrapResult.test.ts 23.17ms
1960/2072 src/utils/normalization/unwrapResult.ts 10.57ms
1961/2072 src/utils/organization/___and___.ts 1.58ms
1962/2072 src/utils/organization/___or___.ts 6.87ms
1963/2072 src/utils/organization/$side_effect.ts 3.30ms
1964/2072 src/utils/organization/$sideEffect.ts 2.42ms
1965/2072 src/utils/organization/empty_object.ts 3.65ms
1966/2072 src/utils/organization/just_empty_object.ts 2.46ms
1967/2072 src/utils/organization/just.ts 2.09ms
1968/2072 src/utils/organization/keepImported.ts 2.54ms
1969/2072 src/utils/organization/keepTypeImported.ts 2.34ms
1970/2072 src/utils/organization/keepUnused.ts 4.89ms
1971/2072 src/utils/organization/preserve.ts 4.02ms
1972/2072 src/utils/organization/README.md 2.20ms
1973/2072 src/utils/organization/really_any.ts 3.56ms
1974/2072 src/utils/organization/really_unknown.ts 1.39ms
1975/2072 src/utils/organization/spaceTrim.ts 1.95ms
1976/2072 src/utils/organization/TODO_any.ts 2.42ms
1977/2072 src/utils/organization/TODO_narrow.ts 1.82ms
1978/2072 src/utils/organization/TODO_object.ts 3.09ms
1979/2072 src/utils/organization/TODO_remove_as.ts 1.75ms
1980/2072 src/utils/organization/TODO_string.ts 1.57ms
1981/2072 src/utils/organization/TODO_unknown.ts 1.98ms
1982/2072 src/utils/organization/TODO_USE.ts 2.30ms
1983/2072 src/utils/parameters/extractParameterNames.test.ts 4.91ms
1984/2072 src/utils/parameters/extractParameterNames.ts 35.63ms
1985/2072 src/utils/parameters/mapAvailableToExpectedParameters.test.ts 3.47ms
1986/2072 src/utils/parameters/mapAvailableToExpectedParameters.ts 5.57ms
1987/2072 src/utils/parameters/numberToString.test.ts 1.94ms
1988/2072 src/utils/parameters/numberToString.ts 3.67ms
1989/2072 src/utils/parameters/templateParameters.test.ts 5.73ms
1990/2072 src/utils/parameters/templateParameters.ts 4.60ms
1991/2072 src/utils/parameters/valueToString.test.ts 2.22ms
1992/2072 src/utils/parameters/valueToString.ts 2.80ms
1993/2072 src/utils/random/$generateBookBoilerplate.test.ts 2.28ms
1994/2072 src/utils/random/$generateBookBoilerplate.ts 5.98ms
1995/2072 src/utils/random/$randomAgentPersona.ts 4.24ms
1996/2072 src/utils/random/$randomAgentRule.ts 4.20ms
1997/2072 src/utils/random/$randomBase58.ts 2.71ms
1998/2072 src/utils/random/$randomFullnameWithColor.ts 3.61ms
1999/2072 src/utils/random/$randomItem.ts 2.08ms
2000/2072 src/utils/random/$randomSeed.ts 1.92ms
2001/2072 src/utils/random/$randomToken.ts 2.56ms
2002/2072 src/utils/random/CzechNamePool.ts 8.11ms
2003/2072 src/utils/random/EnglishNamePool.ts 6.80ms
2004/2072 src/utils/random/getNamePool.ts 3.04ms
2005/2072 src/utils/random/NamePool.ts 2.98ms
2006/2072 src/utils/serialization/$deepFreeze.ts 4.46ms
2007/2072 src/utils/serialization/asSerializable.test.ts 1.86ms
2008/2072 src/utils/serialization/asSerializable.ts 3.54ms
2009/2072 src/utils/serialization/checkSerializableAsJson.ts 10.89ms
2010/2072 src/utils/serialization/clonePipeline.ts 2.84ms
2011/2072 src/utils/serialization/deepClone.test.ts 2.41ms
2012/2072 src/utils/serialization/deepClone.ts 4.05ms
2013/2072 src/utils/serialization/exportJson.ts 3.56ms
2014/2072 src/utils/serialization/isSerializableAsJson.test.ts 3.94ms
2015/2072 src/utils/serialization/isSerializableAsJson.ts 2.80ms
2016/2072 src/utils/serialization/jsonStringsToJsons.test.ts 2.12ms
2017/2072 src/utils/serialization/jsonStringsToJsons.ts 2.28ms
2018/2072 src/utils/serialization/serializeToPromptbookJavascript.test.ts 1.96ms
2019/2072 src/utils/serialization/serializeToPromptbookJavascript.ts 7.81ms
2020/2072 src/utils/sets/difference.test.ts 2.31ms
2021/2072 src/utils/sets/difference.ts 2.30ms
2022/2072 src/utils/sets/intersection.test.ts 2.14ms
2023/2072 src/utils/sets/intersection.ts 3.24ms
2024/2072 src/utils/sets/union.test.ts 1.93ms
2025/2072 src/utils/sets/union.ts 2.93ms
2026/2072 src/utils/take/classes/TakeChain.ts 2.42ms
2027/2072 src/utils/take/interfaces/ITakeChain.ts 2.21ms
2028/2072 src/utils/take/interfaces/Takeable.ts 2.58ms
2029/2072 src/utils/take/take.test.ts 3.63ms
2030/2072 src/utils/take/take.ts 3.02ms
2031/2072 src/utils/toolCalls/getToolCallIdentity.ts 2.53ms
2032/2072 src/utils/toolCalls/mergeToolCalls.ts 7.67ms
2033/2072 src/utils/toolCalls/resolveToolCallIdempotencyKey.ts 4.77ms
2034/2072 src/utils/validators/email/isValidEmail.test.ts 3.32ms
2035/2072 src/utils/validators/email/isValidEmail.ts 5.32ms
2036/2072 src/utils/validators/filePath/isRootPath.test.ts 12.02ms
2037/2072 src/utils/validators/filePath/isRootPath.ts 2.53ms
2038/2072 src/utils/validators/filePath/isValidFilePath.test.ts 24.60ms
2039/2072 src/utils/validators/filePath/isValidFilePath.ts 4.89ms
2040/2072 src/utils/validators/javascriptName/isValidJavascriptName.test.ts 2.34ms
2041/2072 src/utils/validators/javascriptName/isValidJavascriptName.ts 2.90ms
2042/2072 src/utils/validators/parameterName/validateParameterName.test.ts 3.88ms
2043/2072 src/utils/validators/parameterName/validateParameterName.ts 13.00ms
2044/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts 2.17ms
2045/2072 src/utils/validators/semanticVersion/isValidPromptbookVersion.ts 5.80ms
2046/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts 8.09ms
2047/2072 src/utils/validators/semanticVersion/isValidSemanticVersion.ts 2.54ms
2048/2072 src/utils/validators/url/extractUrlsFromText.test.ts 3.89ms
2049/2072 src/utils/validators/url/extractUrlsFromText.ts 5.75ms
2050/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts 8.15ms
2051/2072 src/utils/validators/url/isHostnameOnPrivateNetwork.ts 3.56ms
2052/2072 src/utils/validators/url/isUrlOnPrivateNetwork.test.ts 3.10ms
2053/2072 src/utils/validators/url/isUrlOnPrivateNetwork.ts 3.44ms
2054/2072 src/utils/validators/url/isValidAgentUrl.test.ts 2.79ms
2055/2072 src/utils/validators/url/isValidAgentUrl.ts 3.15ms
2056/2072 src/utils/validators/url/isValidPipelineUrl.test.ts 4.74ms
2057/2072 src/utils/validators/url/isValidPipelineUrl.ts 4.03ms
2058/2072 src/utils/validators/url/isValidUrl.test.ts 6.17ms
2059/2072 src/utils/validators/url/isValidUrl.ts 4.71ms
2060/2072 src/utils/validators/url/normalizeDomainForMatching.test.ts 2.21ms
2061/2072 src/utils/validators/url/normalizeDomainForMatching.ts 5.29ms
2062/2072 src/utils/validators/uuid/isValidUuid.test.ts 2.71ms
2063/2072 src/utils/validators/uuid/isValidUuid.ts 5.56ms
2064/2072 src/version.ts 2.71ms
2065/2072 src/wizard/$getCompiledBook.ts 59.80ms
2066/2072 src/wizard/test/books/test.book.md 1.81ms
2067/2072 src/wizard/test/README.md 1.81ms
2068/2072 src/wizard/test/sub/books/test.book.md 2.01ms
2069/2072 src/wizard/test/sub/subsub/books/test.book.md 2.88ms
2070/2072 src/wizard/test/sub/subsub/subsubsub/books/test.book.md 19.10ms
2071/2072 src/wizard/test/sub/subsub/subsubsub/README.md 2.96ms
2072/2072 src/wizard/wizard.ts 10.18ms
CSpell: Files checked: 2072, Issues found: 0 in 0 files.

> promptbook@0.114.0-33 test-lint
> eslint src


> promptbook@0.114.0-33 test-types
> tsc


> promptbook@0.114.0-33 test-books
> echo "Note: [⚡️] Skipping the book test because it tests just the old pipeline books which are deprecated"

"Note: [??] Skipping the book test because it tests just the old pipeline books which are deprecated"

> promptbook@0.114.0-33 test-book-components-build
> echo "Note: Skipping book components build tests because of constants problems with this test"

"Note: Skipping book components build tests because of constants problems with this test"

> promptbook@0.114.0-33 test-package-generation
> ts-node ./scripts/generate-packages/generate-packages.ts --skip-bundler

📦  Generating packages
[36m0️⃣  Prepare the needed information about the packages[39m
Promptbook version 0.114.0-33
[36m1️⃣  Generate entry file for each package[39m
[32mGenerated index file ./src/_packages/anthropic-claude.index.ts[39m
[32mGenerated index file ./src/_packages/azure-openai.index.ts[39m
[32mGenerated index file ./src/_packages/browser.index.ts[39m
[32mGenerated index file ./src/_packages/cli.index.ts[39m
[32mGenerated index file ./src/_packages/color.index.ts[39m
[32mGenerated index file ./src/_packages/components.index.ts[39m
[32mGenerated index file ./src/_packages/core.index.ts[39m
[32mGenerated index file ./src/_packages/deepseek.index.ts[39m
[32mGenerated index file ./src/_packages/documents.index.ts[39m
[32mGenerated index file ./src/_packages/editable.index.ts[39m
[32mGenerated index file ./src/_packages/fake-llm.index.ts[39m
[32mGenerated index file ./src/_packages/google.index.ts[39m
[32mGenerated index file ./src/_packages/javascript.index.ts[39m
[32mGenerated index file ./src/_packages/legacy-documents.index.ts[39m
[32mGenerated index file ./src/_packages/markdown-utils.index.ts[39m
[32mGenerated index file ./src/_packages/markitdown.index.ts[39m
[32mGenerated index file ./src/_packages/node.index.ts[39m
[32mGenerated index file ./src/_packages/ollama.index.ts[39m
[32mGenerated index file ./src/_packages/openai.index.ts[39m
[32mGenerated index file ./src/_packages/pdf.index.ts[39m
[32mGenerated index file ./src/_packages/remote-client.index.ts[39m
[32mGenerated index file ./src/_packages/remote-server.index.ts[39m
[32mGenerated index file ./src/_packages/templates.index.ts[39m
[32mGenerated index file ./src/_packages/types.index.ts[39m
[32mGenerated index file ./src/_packages/utils.index.ts[39m
[32mGenerated index file ./src/_packages/vercel.index.ts[39m
[32mGenerated index file ./src/_packages/website-crawler.index.ts[39m
[32mGenerated index file ./src/_packages/wizard.index.ts[39m
[36m2️⃣  Generate package.json, README and other crucial files for each package[39m
[36m3️⃣  Cleanup build directories for each package[39m
[33mSkipping the cleanup for bundler[39m
[36m4️⃣  Generate bundle for each package[39m
[33mSkipping the bundler[39m
[36m5️⃣  Postprocess the generated bundle[39m
[33mSkipping postprocessing[39m
[36m6️⃣  Add dependencies for each package[39m
[33mBundle file ./packages/editable/esm/index.es.js does not exist[39m
[33mBundle file ./packages/fake-llm/esm/index.es.js does not exist[39m
[33mBundle file ./packages/google/esm/index.es.js does not exist[39m
[33mBundle file ./packages/javascript/esm/index.es.js does not exist[39m
[33mBundle file ./packages/legacy-documents/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markdown-utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/markitdown/esm/index.es.js does not exist[39m
[33mBundle file ./packages/node/esm/index.es.js does not exist[39m
[33mBundle file ./packages/ollama/esm/index.es.js does not exist[39m
[33mBundle file ./packages/openai/esm/index.es.js does not exist[39m
[33mBundle file ./packages/pdf/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-client/esm/index.es.js does not exist[39m
[33mBundle file ./packages/remote-server/esm/index.es.js does not exist[39m
[33mBundle file ./packages/templates/esm/index.es.js does not exist[39m
[33mBundle file ./packages/types/esm/index.es.js does not exist[39m
[33mBundle file ./packages/utils/esm/index.es.js does not exist[39m
[33mBundle file ./packages/vercel/esm/index.es.js does not exist[39m
[33mBundle file ./packages/website-crawler/esm/index.es.js does not exist[39m
[33mBundle file ./packages/wizard/esm/index.es.js does not exist[39m
[36m7️⃣  Copy agents-server app to CLI package[39m
Copying ./apps/agents-server to ./packages/cli/apps/agents-server
Copying ./apps/_common to ./packages/cli/apps/_common
Copying ./src to ./packages/cli/src
Copying ./books to ./packages/cli/books
Copying ./servers.ts to ./packages/cli/servers.ts
Copying ./security.config.ts to ./packages/cli/security.config.ts
[32mAgents-server app copied successfully[39m
Copying ./agents/default/developer.book to ./packages/cli/agents/default/developer.book
[36m8️⃣  Test that nothing what should not be published is published[39m
[33mSkipping the bundler, skipping the tests for bundle content[39m
[36m9️⃣  Make publishing instructions for Github Actions[39m

> promptbook@0.114.0-33 test-unit
> node -r ./scripts/ignore-kill-eperm.js ./node_modules/jest/bin/jest.js --forceExit

PASS scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.test.ts (54.877 s)
  ● Console

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      runner stderr

    [0m [90m 13 |[39m     [36mif[39m (source [33m===[39m [32m'stderr'[39m) {
     [90m 14 |[39m         [36mif[39m (chunk[33m.[39mtrim()) {
    [31m[1m>[22m[39m[90m 15 |[39m             console[33m.[39mwarn(chunk)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 16 |[39m         }
     [90m 17 |[39m
     [90m 18 |[39m         [36mreturn[39m[33m;[39m[0m

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

    console.info
      runner stdout

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:21:13)

    console.warn
      test failure

    [0m [90m 13 |[39m     [36mif[39m (source [33m===[39m [32m'stderr'[39m) {
     [90m 14 |[39m         [36mif[39m (chunk[33m.[39mtrim()) {
    [31m[1m>[22m[39m[90m 15 |[39m             console[33m.[39mwarn(chunk)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 16 |[39m         }
     [90m 17 |[39m
     [90m 18 |[39m         [36mreturn[39m[33m;[39m[0m

      at printLiveScriptChunk (scripts/run-codex-prompts/common/runGoScript/printLiveScriptChunk.ts:15:21)
      at Socket.<anonymous> (scripts/run-codex-prompts/common/runGoScript/runBashScriptWithOutput.ts:72:33)

PASS scripts/run-codex-prompts/common/runGoScript/$spawnLoggedBashScript.test.ts (18.198 s)
PASS src/scripting/_test/postprocessing.test.ts (14.242 s)
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Paůl!“\n\n[1️⃣ Say that again!](?message=Hello%20Pa%C5%AFl!)\n[2️⃣ Say that twice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)\n[3️⃣ Say that thrice!](?message=Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!Hello%20Pa%C5%AFl!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!lůaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!lůaP olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!luaP olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!lFA%5C%aP02%olleH!lFA%5C%aP02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!lFA%5C%aP02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!luaP olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_L_FA_5C_A_P02_OLLE_H_L_FA_5C_A_P02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_L_FA_5C_A_P02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_LUA_P_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello Adam!“\n\n[1️⃣ Say that again!](?message=Hello%20Adam!)\n[2️⃣ Say that twice!](?message=Hello%20Adam!Hello%20Adam!)\n[3️⃣ Say that thrice!](?message=Hello%20Adam!Hello%20Adam!Hello%20Adam!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!madA olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!madA02%olleH!madA02%olleH!madA02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!madA02%olleH!madA02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!madA02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!madA olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      MAD_A02_OLLE_H_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_MAD_A02_OLLE_H_MAD_A02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_MAD_A02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_MAD_A_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello John!“\n\n[1️⃣ Say that again!](?message=Hello%20John!)\n[2️⃣ Say that twice!](?message=Hello%20John!Hello%20John!)\n[3️⃣ Say that thrice!](?message=Hello%20John!Hello%20John!Hello%20John!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!nhoJ olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!nhoJ02%olleH!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!nhoJ02%olleH!nhoJ02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!nhoJ02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!nhoJ olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      NHO_J02_OLLE_H_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_NHO_J02_OLLE_H_NHO_J02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_NHO_J02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_NHO_J_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = "You said:\n„Hello DAVID!“\n\n[1️⃣ Say that again!](?message=Hello%20DAVID!)\n[2️⃣ Say that twice!](?message=Hello%20DAVID!Hello%20DAVID!)\n[3️⃣ Say that thrice!](?message=Hello%20DAVID!Hello%20DAVID!Hello%20DAVID!)";
      (async ()=>{ return reverse(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return removeDiacritics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      )!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[
      )!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[
      )!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[
      
      “!DIVAD olleH„
      :dias uoY

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const resultString = ")!DIVAD02%olleH!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!ecirht taht yaS ⃣️3[\n)!DIVAD02%olleH!DIVAD02%olleH=egassem?(]!eciwt taht yaS ⃣️2[\n)!DIVAD02%olleH=egassem?(]!niaga taht yaS ⃣️1[\n\n“!DIVAD olleH„\n:dias uoY";
      (async ()=>{ return normalizeTo_SCREAMING_CASE(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      DIVAD02_OLLE_H_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIRHT_TAHT_YA_S_3_DIVAD02_OLLE_H_DIVAD02_OLLE_H_EGASSEM_ECIWT_TAHT_YA_S_2_DIVAD02_OLLE_H_EGASSEM_NIAGA_TAHT_YA_S_1_DIVAD_OLLE_H_DIAS_UO_Y

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/scrapers/markdown/MarkdownScraper.test.ts (10.724 s)
PASS scripts/run-codex-prompts/git/coderCommitScope.test.ts (11.105 s)
PASS scripts/run-codex-prompts/main/runCodexPrompts.test.ts
  ● Console

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:436:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [90mPulling latest changes before the next prompt...[39m

      at pullLatestChangesIfEnabled (scripts/run-codex-prompts/main/runCodexPrompts.ts:436:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [33mNo prompts match the selected harness, model or agent.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mRun limit reached after 2 prompt runs.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

    console.info
      [32mRunning prompts with GitHub Copilot[39m

      at runCodexPrompts (scripts/run-codex-prompts/main/runCodexPrompts.ts:123:17)

    console.info
      [36mDone: 0 | For agent: 1 | To be written: 0[39m

      at printStats (scripts/run-codex-prompts/prompts/printStats.ts:15:13)

    console.info
      [32mNo upcoming tasks.[39m

      at printUpcomingTasks (scripts/run-codex-prompts/prompts/printUpcomingTasks.ts:10:17)

    console.info
      [32mEnd requested after 1 prompt run.[39m

      at announceRunCompletion (scripts/run-codex-prompts/main/runCodexPrompts.ts:958:17)

PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.optimisticNavigation.test.tsx (9.617 s)
PASS src/llm-providers/openai/OpenAiAgentKitExecutionTools.test.ts (8.738 s)
PASS scripts/run-agent-messages/git/commitAgentProjectChanges.test.ts
PASS scripts/utils/findAllProjectEntities.test.ts (5.11 s)
  ● Console

    console.error
      Duplicate entity name "listAllFiles" found in files:
       - C:/Users/me/work/ai/promptbook/src/utils/files/listAllFiles.ts
       - C:/Users/me/work/ai/promptbook/scripts/delete-openai-resources/listAllOpenAiResources.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "BOOK_LANGUAGE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "PROMPTBOOK_ENGINE_VERSION" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "string_promptbook_version" found in files:
       - C:/Users/me/work/ai/promptbook/src/version.ts
       - C:/Users/me/work/ai/promptbook/scripts/update-version-in-config/update-version-in-config.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "value$" found in files:
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/analyzeSourceFileForRefactorCandidate.test.ts
       - C:/Users/me/work/ai/promptbook/scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:5:26)

    console.error
      Duplicate entity name "MonacoEditorWithShadowDom" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/_common/MonacoEditorWithShadowDom.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/_utils/MonacoEditorWithShadowDom.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadProgressCallback" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "BookEditorUploadOptions" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/BookEditor/BookEditor.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/upload/createBookEditorUploadHandler.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackMode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatVisualMode" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/Chat/ChatProps.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/chatVisualMode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "FriendlyErrorMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/LlmChat/FriendlyErrorMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/errorMessages.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatMessage" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/types/ChatMessage.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/types.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "RunBrowserToolError" found in files:
       - C:/Users/me/work/ai/promptbook/src/book-components/Chat/utils/toolCallParsing/RunBrowserToolResult.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/tools/runBrowserErrors.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "LocalAgentRunnerLimits" found in files:
       - C:/Users/me/work/ai/promptbook/src/cli/cli-commands/agents-server/startAgentsServer/LocalAgentRunnerLimits.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverLimits.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Json" found in files:
       - C:/Users/me/work/ai/promptbook/src/collection/agent-collection/constructors/agent-collection-in-supabase/AgentsDatabaseSchema.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/schema.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "TIME_INTERVALS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "HTTP_STATUS_CODES" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_TOKEN" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CHAT_STREAM_KEEP_ALIVE_INTERVAL_MS" found in files:
       - C:/Users/me/work/ai/promptbook/src/constants/streaming.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/streaming.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "nameToSubfolderPath" found in files:
       - C:/Users/me/work/ai/promptbook/src/storage/file-cache-storage/utils/nameToSubfolderPath.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/cdn/utils/nameToSubfolderPath.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "id" found in files:
       - C:/Users/me/work/ai/promptbook/src/types/string_token.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "async" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/[...rest]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/about/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/api-tokens/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/backup/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/browser-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-feedback/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/chat-history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-css/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/custom-js/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/error-simulation/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/files/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/image-generator-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/login-methods/shibboleth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/messages/send-email/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/metadata/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/models/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/planned-messages/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/search-engine-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager-vps/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/[taskId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/tool-limits/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/transcriptions/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/usage/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/[userId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/users/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/voice-input-test/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/goal/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/iframe/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/[docId]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/manifest.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/cli-access/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/database/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/environment/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/harness-auth/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/logs/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/servers/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/task-manager/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/update/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/profile/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-memory/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/user-wallet/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/mocked-chats/view/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/utilities/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "function" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/book+chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/system-message/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/textarea/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/docs/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/experiments/story/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/global-error.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/not-found.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/restricted/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/search/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/story/[[...story]]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/swagger/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/loading.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/system/settings/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/VercelDeploymentCard/VercelDeploymentCard.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_app.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/_document.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/pages/500.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "dynamic" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/core-agents/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/email-server/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/admin/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/dashboard/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/internal-s3/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/superadmin/resource-monitor/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "parseBooleanFlag" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/_utils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/constants/analyticsMetadata.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "AgentProfile" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/AgentProfileWrapper.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/AgentProfile/AgentProfile.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "runtime" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "GET" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/model-requirements/system-message/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/download/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/default-avatar.png/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/icon-256.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-fullhd.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/images/screenshot-phone.png/route.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/files/[...filePath]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/projects/[projectName]/vscode/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/books/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/backups/server/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/terminal/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/error-simulation/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/logs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/commits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/log/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-runtime-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-project-vscode-auth/[sessionId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-artifacts/[artifactName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/scroll-facebook/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/threads/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-streaming/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/embed.js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/federated-agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/callback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/connect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/github-app/status/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/health/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/images/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/long-running-task/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/models/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/check/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/screenshot/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/state/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/scrape/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/search/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/story/export/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/team-agent-profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/usage/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/humans.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/robots.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/s3/[first]/[second]/[hash]/[filename]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/security.txt/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/sitemap.xml/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "POST" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/missing-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/reference-diagnostics/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-connections/[connectionId]/disconnect/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/calendar-events/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/mcp/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/meta-disclaimer/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/openrouter/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/share-target/[shareTargetId]/consume/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/api/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/share-target/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/cancel/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/[taskId]/retry/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/database/studio/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/default-agents/reinstate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/dns-records/cloudflare/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/migrate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/vps/chat-tasks/cancel-all/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-organization/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/clone/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/restore/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/change-password/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/login/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/logout/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/auth/shibboleth/acs/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/refresh/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/calendar-oauth/revoke/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/citation-label/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/emails/incoming/stalwart/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/error-reports/application/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-project-runtimes/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/agent-runner-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/import/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/evaluate/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/onboarding/test/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/audio/transcriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/openai/v1/chat/completions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/page-preview/input/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/send-email/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/spawn-agent/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/upload/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PUT" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/book/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/tool-limits/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/keybindings/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/notifications/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/settings/theme/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/system/mocked-chats/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "maxDuration" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/stream/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/browser-test/act/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/internal/user-chat-timeouts/run/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "OPTIONS" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/chat/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/voice/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/elevenlabs/tts/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/instance/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/me/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/openapi.json/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "DELETE" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/timeouts/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/versions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/api-tokens/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-feedback/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/[id]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat-history/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-css/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/custom-js/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/metadata/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "PATCH" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/draft/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/api/user-chats/[chatId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/cli-access/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/environment/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/authentication/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/harness-auth/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/planned-messages/[timeoutId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/servers/[serverId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/admin/update/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agent-folders/[folderId]/visibility/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/agents/[agentName]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/profile/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/push-subscriptions/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-memory/[memoryId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/user-wallet/[walletId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/users/[username]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/agents/[agentId]/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/v1/folders/[folderId]/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "as" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/CanonicalAgentChatSurface.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/chat/export/pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatExport/downloadChatPdfFromServer.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "generateMetadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/history/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/embed/layout.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/layout.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatsSnapshot" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/chat/useAgentChatHistorySyncState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "header" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/AgentCodePageHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/useAgentCodeExportState.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "banner" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeErrorBanner.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/export-as-transpiled-code/TranspiledCodeExportWarningBanner.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "YourComponent" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/integration/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/website-integration/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "alt" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "size" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "contentType" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/agents/[agentName]/opengraph-image.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/opengraph-image.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "revalidate" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.md/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book-language.pdf/route.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/api/docs/book.md/route.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "metadata" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/recycle-bin/page.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/app/test/og-image/page.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ControlPanelOptionAvailability" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Header/ControlPanel/ControlPanelContentState.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/getControlPanelOptionAvailability.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "collectDescendantFolderIds" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOrganization/folderTree.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "findAgentByIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/agentOrganizationUtils.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentOwnership.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "are" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/AgentsListHeader.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Card" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/Card.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Card.tsx

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "normalizeServerUrl" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/Homepage/normalizeServerUrl.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/search/createDefaultServerSearchProviders/normalizeServerUrl.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "Button" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/components/ui/Button.tsx
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "quoteIdentifier" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/database/sqlite/localSqliteSupabase/localSqliteSql.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverManagement/createManagedServer/createSqlRecorder.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/quoteIdentifier.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRow.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "ChatFeedbackRow" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/backup/serverBackup/serverBackupTypes.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/chatFeedbackAdmin.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "CalendarProvider" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/calendarConnections/CalendarConnectionRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/calendars/providers/CalendarProvider.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "MetaDisclaimerStatus" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimer.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/metaDisclaimerClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "listRegisteredServersUsingServiceRole" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistry.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/serverRegistryNode.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UsageAnalyticsQuery" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAdmin.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/usageAnalytics/UsageAnalyticsQuery.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "createUserChat" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/createUserChat.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/createUserChat.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "UserChatSummary" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChat/UserChatRecord.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

    console.error
      Duplicate entity name "cancelUserChatTimeout" found in files:
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatClient/cancelUserChatTimeout.ts
       - C:/Users/me/work/ai/promptbook/apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/cancelUserChatTimeout.ts

    [0m [90m 160 |[39m             reportedEntityNames[33m.[39madd(entity[33m.[39mname)[33m;[39m
     [90m 161 |[39m
    [31m[1m>[22m[39m[90m 162 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 163 |[39m                 [32m`Duplicate entity name "${entity.name}" found in files:\n${[[39m
     [90m 164 |[39m [32m                    entity.filename,[39m
     [90m 165 |[39m [32m                    ...duplicates.map((existingEntity) => existingEntity.filename),[39m[0m

      at findAllProjectEntities (scripts/utils/findAllProjectEntities.ts:162:21)
      at Object.<anonymous> (scripts/utils/findAllProjectEntities.test.ts:20:26)

PASS src/llm-providers/mocked/test/mocked-chat.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)
          at async Promise.all (index 0)

PASS src/utils/markdown/humanizeAiText.test.ts
PASS src/scripting/_test/custom-function-async.test.ts.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts
  ● Console

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:10:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.getPipelineByUrl (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:53:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:43:38)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at Object.<anonymous> (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:56:28)

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/01-simple.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/05-comments.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/11-picking-model.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/20-two.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/25-simple-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/30-escaping.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-jokers.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expect-json.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-rare-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/00-flat.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/01-simple.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [32mLoaded pipeline examples/pipelines/26-markdown-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/27-document-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/28-website-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/29-mixed-knowledge.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/40-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/41-postprocessing.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/45-expectations.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-advanced.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [90mSkipped pipeline examples/pipelines/05-comments.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/10-single.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/11-picking-model.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/20-two.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/25-simple-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/26-markdown-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/27-document-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/28-website-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/29-mixed-knowledge.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/30-escaping.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/40-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-jokers.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/41-postprocessing.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expect-json.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/45-rare-expectations.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-advanced.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/50-nesting.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.bookc because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [32mLoaded pipeline examples/pipelines/51-empty-pipeline-2.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/60-json-mode.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/70-parameter-used-in-reverse-order.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/80-foreach-list.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/85-foreach-csv.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/advanced-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/implicit-formfactor.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/koralkykatlas-chat.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [32mLoaded pipeline examples/pipelines/quick-chatbot.bookc[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:480:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-name.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-model-variant.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/50-double-persona-2.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/50-nesting.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-0.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [33mCan not load pipeline from examples/pipelines/51-empty-pipeline-1.book because of missing URL[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:462:21)

    console.info
      [90mSkipped pipeline examples/pipelines/51-empty-pipeline-2.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/60-json-mode.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/70-parameter-used-in-reverse-order.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/80-foreach-list.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/85-foreach-csv.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/advanced-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/implicit-formfactor.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/koralkykatlas-chat.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [90mSkipped pipeline examples/pipelines/quick-chatbot.book – Already identical pipeline in the collection[39m

      at addPipelineToCollection (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:490:21)

    console.info
      [36mCreating pipeline collection from path ./examples/pipelines[39m

      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:251:17)

    console.info
      [90mSkipped file examples/pipelines/01-simple.ptbk.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/25-simple-knowledge.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.docx – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/27-document-knowledge.pdf – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-advanced.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:325:47)
      at loadPipelinesFromFiles (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:302:41)
      at loadPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:257:32)
      at load (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:44:35)
      at Object.listPipelines (src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.ts:49:9)
      at createPipelineCollectionFromDirectory (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:153:9)
      at src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.test.ts:94:36

    console.info
      [90mSkipped file examples/pipelines/50-nesting.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/50-non-standard-order-of-commands.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/80-foreach-list.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.json – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach-csv.report.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/85-foreach.csv – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/alternative-notations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/README.md – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/TODO.txt – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/model-variant-mismatch.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/sample-dont-pass-expectations.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

    console.info
      [90mSkipped file examples/pipelines/errors/logic/void-knowledge.book.todo – Not a book[39m

      at loadPipelinesFromFile (src/collection/pipeline-collection/constructors/createPipelineCollectionFromDirectory.ts:348:17)

PASS scripts/run-agent-messages/main/runMultipleAgentMessages.test.ts
  ● Console

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/first.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/second.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-active/messages/queued/question.book with Active Agent.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mCloned 1 new agent repository: agent-new[39m

      at MultipleAgentGithubSynchronizer.synchronizeIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentGithubSynchronizer.ts:71:21)

    console.info
      [34mProcessing agent-new/messages/queued/question.book with Agent new.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [90mPulling latest changes for 2 watched agent repositories...[39m

      at MultipleAgentAutoPuller.pullIfNeeded (scripts/run-agent-messages/main/runMultipleAgentMessages/MultipleAgentAutoPuller.ts:56:21)

    console.info
      [34mProcessing agent-b/messages/queued/question.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question-a.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-b/messages/queued/question-b.book with Agent B.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [32mWatching direct child agent repositories for queued messages.[39m

      at runMultipleAgentMessages (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:74:29)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

    console.info
      [34mProcessing agent-a/messages/queued/question.book with Agent A.[39m

      at announceQueuedWorkItems (scripts/run-agent-messages/main/runMultipleAgentMessages.ts:185:17)

PASS apps/agents-server/src/utils/bookLanguageDocumentation/createBookLanguageDocumentationPdfResponse.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeRegistry.test.ts
PASS apps/agents-server/src/tools/$provideCdnForServer.test.ts
PASS apps/agents-server/src/tools/agentGoalChatTimeoutTools.test.ts
PASS scripts/run-agent-messages/main/runAgentMessages.test.ts
PASS src/book-components/Chat/LlmChat/LlmChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

    [0m [90m 68 |[39m         document[33m.[39mbody[33m.[39mappendChild(container)[33m;[39m
     [90m 69 |[39m
    [31m[1m>[22m[39m[90m 70 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 71 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
     [90m 72 |[39m             root[33m.[39mrender(
     [90m 73 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{customLlmTools [36mas[39m [33mTODO_any[39m} thread[33m=[39m{thread} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m[0m

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 70 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 71 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 72 |[39m             root[33m.[39mrender(
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 73 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{customLlmTools [36mas[39m [33mTODO_any[39m} thread[33m=[39m{thread} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m
     [90m 74 |[39m             )[33m;[39m
     [90m 75 |[39m         })[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:72:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:70:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 611 |[39m             })[33m;[39m
     [90m 612 |[39m
    [31m[1m>[22m[39m[90m 613 |[39m             setMessages((previousMessages) [33m=>[39m [[33m...[39mpreviousMessages[33m,[39m userMessage[33m,[39m loadingMessage])[33m;[39m
     [90m     |[39m             [31m[1m^[22m[39m
     [90m 614 |[39m
     [90m 615 |[39m             [36mconst[39m taskId [33m=[39m [32m`llm_call_${Date.now()}`[39m[33m;[39m
     [90m 616 |[39m             setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m0[39m }])[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:613:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 614 |[39m
     [90m 615 |[39m             [36mconst[39m taskId [33m=[39m [32m`llm_call_${Date.now()}`[39m[33m;[39m
    [31m[1m>[22m[39m[90m 616 |[39m             setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m0[39m }])[33m;[39m
     [90m     |[39m             [31m[1m^[22m[39m
     [90m 617 |[39m
     [90m 618 |[39m             [36mconst[39m generationStartedAtMs [33m=[39m [33mDate[39m[33m.[39mnow()[33m;[39m
     [90m 619 |[39m             [36mlet[39m streamingAbortController[33m:[39m [33mAbortController[39m [33m|[39m [36mnull[39m [33m=[39m [36mnull[39m[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:616:13)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 629 |[39m                 })[33m;[39m
     [90m 630 |[39m
    [31m[1m>[22m[39m[90m 631 |[39m                 setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Generating response...'[39m[33m,[39m progress[33m:[39m [35m50[39m }])[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 632 |[39m
     [90m 633 |[39m                 [36mconst[39m execution [33m=[39m [36mawait[39m executeLlmChatPrompt({
     [90m 634 |[39m                     assistantMessageStartedAt[33m,[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:631:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:28
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:81:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 644 |[39m
     [90m 645 |[39m                 streamingAbortController [33m=[39m execution[33m.[39mstreamingAbortController[33m;[39m
    [31m[1m>[22m[39m[90m 646 |[39m                 setTasksProgress([{ id[33m:[39m taskId[33m,[39m name[33m:[39m [32m'Response generated'[39m[33m,[39m progress[33m:[39m [35m100[39m }])[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 647 |[39m
     [90m 648 |[39m                 [36mif[39m ([33m![39mexecution[33m.[39mresult[33m.[39mcontent[33m.[39mtrim()) {
     [90m 649 |[39m                     [36mthrow[39m [36mnew[39m [33mError[39m([32m'The agent did not respond.'[39m)[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:646:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 659 |[39m                 })[33m;[39m
     [90m 660 |[39m
    [31m[1m>[22m[39m[90m 661 |[39m                 setMessages((previousMessages) [33m=>[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 662 |[39m                     updateMessageById(previousMessages[33m,[39m loadingMessage[33m.[39mid[33m,[39m () [33m=>[39m assistantMessage)[33m,[39m
     [90m 663 |[39m                 )[33m;[39m
     [90m 664 |[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:661:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 702 |[39m                 }
     [90m 703 |[39m
    [31m[1m>[22m[39m[90m 704 |[39m                 setIsStreaming([36mfalse[39m)[33m;[39m
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 705 |[39m                 requestInFlightRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 706 |[39m             }
     [90m 707 |[39m         }[33m,[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onMessage (src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:704:17)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:82:13

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 262 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 263 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 264 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 265 |[39m                 [33m<[39m[33mLlmChat[39m
     [90m 266 |[39m                     title[33m=[39m[32m"Delegated reset"[39m
     [90m 267 |[39m                     llmTools[33m=[39m{mockLlmTools}[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:264:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:262:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 304 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 305 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 306 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 307 |[39m                 [33m<[39m[33mLlmChat[39m title[33m=[39m[32m"Test"[39m llmTools[33m=[39m{mockLlmTools} initialMessages[33m=[39m{initialMessages} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m[33m,[39m
     [90m 308 |[39m             )[33m;[39m
     [90m 309 |[39m         })[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:306:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:304:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 310 |[39m         }
     [90m 311 |[39m
    [31m[1m>[22m[39m[90m 312 |[39m         setMessages(buildInitialMessages())[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 313 |[39m         setTasksProgress([])[33m;[39m
     [90m 314 |[39m         hasUserInteractedRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 315 |[39m         clearLastFailedMessage()[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:312:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 311 |[39m
     [90m 312 |[39m         setMessages(buildInitialMessages())[33m;[39m
    [31m[1m>[22m[39m[90m 313 |[39m         setTasksProgress([])[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 314 |[39m         hasUserInteractedRef[33m.[39mcurrent [33m=[39m [36mfalse[39m[33m;[39m
     [90m 315 |[39m         clearLastFailedMessage()[33m;[39m
     [90m 316 |[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:313:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 723 |[39m
     [90m 724 |[39m     [36mconst[39m clearLastFailedMessage [33m=[39m useCallback(() [33m=>[39m {
    [31m[1m>[22m[39m[90m 725 |[39m         setLastFailedMessage([36mnull[39m)[33m;[39m
     [90m     |[39m         [31m[1m^[22m[39m
     [90m 726 |[39m     }[33m,[39m [])[33m;[39m
     [90m 727 |[39m
     [90m 728 |[39m     [36mconst[39m handleRetry [33m=[39m useCallback(() [33m=>[39m {[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at dispatchSetStateInternal (node_modules/react-dom/cjs/react-dom-client.development.js:6969:13)
      at dispatchSetState (node_modules/react-dom/cjs/react-dom-client.development.js:6927:7)
      at src/book-components/Chat/LlmChat/useLlmChatMessageHandler.ts:725:9
      at Object.onReset (src/book-components/Chat/LlmChat/useLlmChatState.ts:315:9)
      at src/book-components/Chat/LlmChat/LlmChat.test.tsx:323:30
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/LlmChat/LlmChat.test.tsx:322:18)

PASS src/book-3.0/LiteAgent.test.ts
PASS src/book-components/Chat/Chat/ChatMessageItem.test.tsx
PASS src/llm-providers/mocked/test/mocked-completion.test.ts
  ● Console

    console.info
      🖋 Mocked callCompletionModel call

      at MockedEchoLlmExecutionTools.callCompletionModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:141:21)
          at async Promise.all (index 0)

PASS scripts/find-fresh-emoji-tags/utils/$shuffleItems.test.ts
PASS src/book-2.0/book-language-documentation/createStandaloneBookLanguageMarkdown.test.ts
PASS src/cli/cli-commands/agents-server/buildAgentsServer.test.ts
PASS apps/agents-server/src/utils/errorReporting/sendApplicationErrorReportToSentry.test.ts
PASS apps/agents-server/src/utils/resolveAgentStateFromSource.storedAgents.test.ts
PASS src/scripting/javascript/utils/extractVariablesFromJavascript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

PASS src/transpilers/_common/transpiledTeamTranspilers.test.ts
PASS apps/agents-server/src/utils/cancelAllActiveAdminChatTasks.test.ts
PASS scripts/find-refactor-candidates/analyzeSourceFileForRefactorCandidate.test.ts
PASS apps/agents-server/src/app/superadmin/servers/ServersRegistryTable.test.tsx
PASS src/scripting/javascript/utils/extractVariablesFromScript.test.ts
  ● Console

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:31)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

    console.log


      at eval (eval at extractVariablesFromJavascript (src/scripting/javascript/utils/extractVariablesFromJavascript.ts:25:17), <anonymous>:1:51)

PASS src/book-components/Chat/Chat/ChatToolCallModal.test.tsx
PASS apps/agents-server/src/app/api/chat/export/pdf/route.test.ts
PASS scripts/run-agent-messages/main/tickAgentMessages.test.ts
PASS apps/agents-server/src/database/sqlite/$provideLocalSqliteSupabase.test.ts
PASS src/transpilers/anthropic-claude-sdk/AnthropicClaudeSdkTranspiler.test.ts
PASS apps/agents-server/src/tools/getAllToolFunctionsForServer.test.ts
PASS apps/agents-server/src/utils/agentsTransfer/importAgentsFromFiles.test.ts
PASS src/transpilers/anthropic-claude-managed/AnthropicClaudeManagedTranspiler.test.ts
PASS apps/agents-server/src/utils/chatMessageChips/createAnsweredMessageChipToolCalls.test.ts
PASS src/scrapers/_common/prepareKnowledgePieces.test.ts
PASS src/book-components/Chat/Chat/ChatInputArea.test.tsx
PASS src/transpilers/e2b/E2BTranspiler.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/scheduleAgentGoalChatModifiedNote.test.ts
PASS src/transpilers/openai-sdk/OpenAiSdkTranspiler.test.ts
PASS apps/agents-server/src/utils/transpilers/resolveTranspiledAgentCodeExport.test.ts
PASS src/transpilers/agent-os/AgentOsTranspiler.test.ts
PASS apps/agents-server/scripts/sync-vercel-domains.test.ts
PASS apps/agents-server/src/utils/backup/createServerBackupZipStream.test.ts
PASS src/transpilers/openai-agents/OpenAiAgentsTranspiler.test.ts
PASS src/scripting/_test/script-execution-errors.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "a cup of coffee";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      b cup of coffee

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "arrow";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      brrow

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "aaa";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      bbb

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #2

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #2

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apples";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #3

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "apples";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #4

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #4

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "an apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #5

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "an apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #6

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #6

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #7

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #8

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #9

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "The Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #10

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #11

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "🍏 Apple";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #12

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #13

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      // -- No custom functions --
      
                      // The script:
                      const thing = "Apple 🍎";
                      (async ()=>{ if(/Apple/i.test(thing)){
          throw new Error('I do not like Apples!');
      }
      return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)
      at Object.<anonymous> (src/scripting/_test/script-execution-errors.test.ts:53:13)

    console.warn
      Parameter `{bhing}` should be an output parameter, but it was not generated during pipeline execution
      
      Note: This is a warning which happened after the pipeline was executed, and `{bhing}` was not for some reason defined in output parameters
      
      All parameters:
      1) `{thing}` `[input parameter]` Any thing to buy
      2) `{bhing}` `[output parameter]` <- Warning: Should be in the output but its not |
      
      Run #14

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

PASS src/conversion/compilePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/compilePipeline.test.ts:19:58

PASS apps/agents-server/src/utils/resolveAgentStateFromSource.test.ts
  ● Console

    console.log
      importAgent "https://example.com/agents/parent-metadata"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://example.com/agents/parent-overrides"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://example.com/agents/parent-goal"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS src/book-components/Chat/save/html/htmlSaveFormatDefinition.test.ts
PASS apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts
  ● Console

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_cancel {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'cancelled'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:167:17)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.error
      [local-chat-runner] planned_message_command_failed {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        action: 'set',
        error: {
          name: 'Error',
          message: 'Invalid planned-message delay.',
          stack: 'Error: Invalid planned-message delay.\n' +
            '    at Object.<anonymous> (C:\\Users\\me\\work\\ai\\promptbook\\apps\\agents-server\\src\\utils\\localChatRunner\\applyLocalAgentPlannedMessageCommands.test.ts:96:36)',
          id: undefined
        }
      }

    [0m [90m 174 |[39m         [36mreturn[39m cancelledPlannedMessage[33m.[39mstatus [33m===[39m [32m'cancelled'[39m [33m?[39m cancelledPlannedMessage [33m:[39m [36mnull[39m[33m;[39m
     [90m 175 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 176 |[39m         console[33m.[39merror([32m'[local-chat-runner]'[39m[33m,[39m [32m'planned_message_command_failed'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 177 |[39m             chatId[33m:[39m options[33m.[39mjob[33m.[39mchatId[33m,[39m
     [90m 178 |[39m             jobId[33m:[39m options[33m.[39mjob[33m.[39mid[33m,[39m
     [90m 179 |[39m             action[33m:[39m options[33m.[39mcommand[33m.[39maction[33m,[39m[0m

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:176:17)
      at applyLocalAgentPlannedMessageCommands (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:78:24)
      at Object.<anonymous> (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.test.ts:99:33)

    console.info
      [local-chat-runner] planned_message_set {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-2',
        dueAt: '2026-08-14T11:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:133:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'updated',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

    console.info
      [local-chat-runner] planned_message_update {
        chatId: 'goal-agent1234',
        jobId: 'job-1',
        timeoutId: 'timeout-1',
        status: 'not_found',
        dueAt: '2026-08-14T12:00:00.000Z'
      }

      at applyLocalAgentPlannedMessageCommand (apps/agents-server/src/utils/localChatRunner/applyLocalAgentPlannedMessageCommands.ts:151:21)

PASS apps/agents-server/src/message-providers/email/stalwart/parseInboundStalwartEmail.test.ts
PASS src/cli/cli-commands/coder/init.test.ts
PASS apps/agents-server/src/utils/resolveAppendOnlySelfLearningAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS apps/agents-server/src/tools/run_browser.test.ts
  ● Console

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.warn
      [run_browser] Full-page snapshot failed, retrying viewport-only screenshot {
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        snapshotFilename: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000-initial.png',
        error: 'Screenshot is too large'
      }

    [0m [90m 78 |[39m                 [36mawait[39m page[33m.[39mscreenshot({ path[33m:[39m snapshotPath[33m,[39m fullPage[33m:[39m [36mtrue[39m })[33m;[39m
     [90m 79 |[39m             } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 80 |[39m                 console[33m.[39mwarn([32m'[run_browser] Full-page snapshot failed, retrying viewport-only screenshot'[39m[33m,[39m {
     [90m    |[39m                         [31m[1m^[22m[39m
     [90m 81 |[39m                     sessionId[33m,[39m
     [90m 82 |[39m                     snapshotFilename[33m,[39m
     [90m 83 |[39m                     error[33m:[39m getErrorMessage(error)[33m,[39m[0m

      at Object.captureSnapshot (apps/agents-server/src/tools/runBrowserArtifacts.ts:80:25)
      at Object.captureSnapshotArtifact (apps/agents-server/src/tools/runBrowserArtifacts.ts:133:22)
      at run_browser (apps/agents-server/src/tools/run_browser.ts:131:33)
      at Object.<anonymous> (apps/agents-server/src/tools/run_browser.test.ts:201:24)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 0,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_success',
        actions: 5,
        connectDurationMs: 0,
        initialNavigationDurationMs: 0,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_VALIDATION_ERROR',
        errorCodeCount: 2,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'fallback',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_fallback_used',
        errorCode: 'REMOTE_BROWSER_UNAVAILABLE',
        errorCodeCount: 1,
        fallbackRuns: 1,
        totalRuns: 6,
        fallbackRate: 0.16666666666666666
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

    console.info
      [run_browser][metric] {
        tool: 'run_browser',
        mode: 'remote-browser',
        sessionId: 'agents-server-run-browser-00000000-0000-4000-8000-000000000000',
        event: 'run_browser_failed',
        errorCode: 'RUN_BROWSER_NAVIGATION_FAILED',
        errorCodeCount: 1,
        connectDurationMs: null,
        initialNavigationDurationMs: null,
        timeToFirstByteMs: null
      }

      at Object.logRunBrowserMetric (apps/agents-server/src/tools/runBrowserObservability.ts:66:17)

PASS scripts/generate-packages/getPackagesMetadataForRollup.test.ts
PASS src/llm-providers/mocked/test/faked-completion.test.ts
  ● Console

    console.info
      💬 Mocked faked prompt {
        title: 'Prompt',
        pipelineUrl: 'https://promptbook.studio/examples/pipeline.book#prompt',
        parameters: { thing: 'a cup of coffee' },
        content: 'One day I went to the shop and bought {thing}.\nNow I have {thing}.',
        modelRequirements: { modelVariant: 'COMPLETION', modelName: 'gpt-3.5-turbo-instruct' },
        expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } },
        format: undefined,
        postprocessingFunctionNames: undefined
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:87:21)
          at async Promise.all (index 0)

    console.info
      { expectations: { lines: { min: 2, max: 5 }, words: { min: 10 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)
          at async Promise.all (index 0)

    console.info
      💬 Mocked faked result {
        content: 'Culpa non officia Lorem anim in ea deserunt et incididunt. Duis consequat',
        modelName: 'mocked-facked',
        timing: {
          start: '2026-09-12T10:16:14.796Z',
          complete: '2026-09-12T10:16:14.796Z'
        },
        usage: {
          price: { value: 0 },
          duration: { value: 0 },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'One day I went to the shop and bought a cup of coffee.\n' +
          'Now I have a cup of coffee.',
        rawRequest: null,
        rawResponse: { note: 'This is mocked echo' }
      }

      at MockedFackedLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedFackedLlmExecutionTools.ts:122:21)

PASS apps/agents-server/src/components/AgentProfile/AgentProfile.test.tsx
PASS src/book-components/Chat/utils/renderMarkdown.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/externalChatMessageBook.test.ts
PASS src/book-components/Chat/Chat/TeamToolCallModalContent.test.tsx
PASS apps/agents-server/src/utils/agentsTransfer/createAgentsExportZipStream.test.ts
PASS apps/agents-server/src/utils/agentProjects/startDefaultAgentProjectRuntimes.test.ts
PASS src/book-components/Chat/Chat/pagePreview/LiveBrowserPreview.test.tsx
PASS apps/agents-server/src/utils/userChat/persistUserChatJobTerminalState.test.ts
  ● Console

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:208:9)
      }

    [0m [90m  97 |[39m         })[33m;[39m
     [90m  98 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m  99 |[39m         console[33m.[39merror([32m'[ChatHistory] Failed to record durable user-chat message.'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 100 |[39m             agentPermanentId[33m,[39m
     [90m 101 |[39m             chatId[33m,[39m
     [90m 102 |[39m             taskId[33m,[39m[0m

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

    console.error
      [ChatHistory] Failed to record durable user-chat message. {
        agentPermanentId: 'agent-123',
        chatId: 'email-chat-123',
        taskId: 'job-123',
        userId: 3,
        error: Error: `headers` was called outside a request scope. Read more: https://nextjs.org/docs/messages/next-dynamic-api-wrong-context
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:273:9)
            at throwForMissingRequestStore (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\app-render\work-unit-async-storage.external.ts:240:5)
            at headers (C:\Users\me\work\ai\promptbook\node_modules\next\src\server\request\headers.ts:132:47)
            at C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:38:38
            at C:\Users\me\work\ai\promptbook\node_modules\react\cjs\react.development.js:895:19
            at $provideServer (C:\Users\me\work\ai\promptbook\apps\agents-server\src\tools\$provideServer.ts:137:12)
            at $getTableName (C:\Users\me\work\ai\promptbook\apps\agents-server\src\database\$getTableName.ts:16:49)
            at resolveAgentHash (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveAgentHash.ts:22:43)
            at recordUserChatMessageInChatHistory (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\recordUserChatMessageInChatHistory.ts:83:49)
            at persistUserChatJobTerminalState (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.ts:83:52)
            at processTicksAndRejections (node:internal/process/task_queues:103:5)
            at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\userChat\persistUserChatJobTerminalState.test.ts:242:9)
      }

    [0m [90m  97 |[39m         })[33m;[39m
     [90m  98 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m  99 |[39m         console[33m.[39merror([32m'[ChatHistory] Failed to record durable user-chat message.'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 100 |[39m             agentPermanentId[33m,[39m
     [90m 101 |[39m             chatId[33m,[39m
     [90m 102 |[39m             taskId[33m,[39m[0m

      at recordUserChatMessageInChatHistory (apps/agents-server/src/utils/userChat/recordUserChatMessageInChatHistory.ts:99:17)

PASS apps/agents-server/src/utils/agentGoalChat/enqueueAgentGoalChatTurn.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDomains.test.ts
PASS apps/agents-server/src/database/seedDefaultAgents.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/book/download/route.test.ts
PASS apps/agents-server/src/search/createDefaultServerSearchProviders/createDocumentationSearchProvider.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/resolveTeamCapabilitiesFromAgentSource.test.ts
PASS src/book-components/Chat/MarkdownContent/MarkdownContent.test.tsx
  ● Console

    console.error
      Attempted to synchronously unmount a root while React was already rendering. React cannot finish unmounting the root until the current render has completed, which may lead to a race condition.

    [0m [90m 377 |[39m             pendingToggleFallbackTimeoutIds[33m.[39mforEach((timeoutId) [33m=>[39m window[33m.[39mclearTimeout(timeoutId))[33m;[39m
     [90m 378 |[39m             pendingToggleFallbackTimeoutIds[33m.[39mclear()[33m;[39m
    [31m[1m>[22m[39m[90m 379 |[39m             rootsRef[33m.[39mcurrent[33m.[39mforEach((root) [33m=>[39m root[33m.[39munmount())[33m;[39m
     [90m     |[39m                                                     [31m[1m^[22m[39m
     [90m 380 |[39m             rootsRef[33m.[39mcurrent [33m=[39m [][33m;[39m
     [90m 381 |[39m         }[33m;[39m
     [90m 382 |[39m     }[33m,[39m [htmlContent[33m,[39m inlineReferences[33m,[39m resolvedTheme])[33m;[39m[0m

      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24773:21)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:53
          at Array.forEach (<anonymous>)
      at src/book-components/Chat/MarkdownContent/MarkdownContent.tsx:379:30
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23965:13)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at commitHookEffectListUnmount (node_modules/react-dom/cjs/react-dom-client.development.js:11996:17)
      at commitHookPassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:12043:11)
      at commitPassiveUnmountEffectsInsideOfDeletedTree_begin (node_modules/react-dom/cjs/react-dom-client.development.js:14269:13)
      at recursivelyTraversePassiveUnmountEffects (node_modules/react-dom/cjs/react-dom-client.development.js:14167:13)
      at commitPassiveUnmountOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14194:11)
      at flushPassiveEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15736:9)
      at flushPendingEffects (node_modules/react-dom/cjs/react-dom-client.development.js:15702:14)
      at flushSpawnedWork (node_modules/react-dom/cjs/react-dom-client.development.js:15668:44)
      at commitRoot (node_modules/react-dom/cjs/react-dom-client.development.js:15403:9)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14526:15)
      at performSyncWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:16231:7)
      at flushSyncWorkAcrossRoots_impl (node_modules/react-dom/cjs/react-dom-client.development.js:16079:21)
      at flushSyncWork$1 (node_modules/react-dom/cjs/react-dom-client.development.js:14721:12)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.unmount.ReactDOMRoot.unmount [as unmount] (node_modules/react-dom/cjs/react-dom-client.development.js:24777:11)
      at Object.unmount (node_modules/@testing-library/react/dist/pure.js:163:12)
      at node_modules/@testing-library/react/dist/pure.js:307:12
      at node_modules/@testing-library/react/dist/act-compat.js:47:24
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at node_modules/@testing-library/react/dist/act-compat.js:46:25
      at node_modules/@testing-library/react/dist/pure.js:306:28
          at Array.forEach (<anonymous>)
      at cleanup (node_modules/@testing-library/react/dist/pure.js:302:22)
      at Object.<anonymous> (node_modules/@testing-library/react/dist/index.js:28:25)

PASS src/book-components/Chat/AgentChat/AgentChat.test.tsx
  ● Console

    console.error
      `ReactDOMTestUtils.act` is deprecated in favor of `React.act`. Import `act` from `react` instead of `react-dom/test-utils`. See https://react.dev/warnings/react-dom-test-utils for more info.

    [0m [90m 67 |[39m         document[33m.[39mbody[33m.[39mappendChild(container)[33m;[39m
     [90m 68 |[39m
    [31m[1m>[22m[39m[90m 69 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 70 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
     [90m 71 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m 72 |[39m         })[33m;[39m[0m

      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:19:17)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 69 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 70 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 71 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 72 |[39m         })[33m;[39m
     [90m 73 |[39m
     [90m 74 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:71:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:69:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 86 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 87 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 88 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} title[33m=[39m[32m"Custom Chat Title"[39m layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m    |[39m                  [31m[1m^[22m[39m
     [90m 89 |[39m         })[33m;[39m
     [90m 90 |[39m
     [90m 91 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:88:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:86:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 100 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 101 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 102 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 103 |[39m         })[33m;[39m
     [90m 104 |[39m
     [90m 105 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:102:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:100:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 131 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 132 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 133 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 134 |[39m         })[33m;[39m
     [90m 135 |[39m
     [90m 136 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:133:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:131:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 155 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 156 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 157 |[39m             root[33m.[39mrender(
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 158 |[39m                 [33m<[39m[33mAgentChat[39m
     [90m 159 |[39m                     agent[33m=[39m{agent}
     [90m 160 |[39m                     onChange[33m=[39m{mockOnChange}[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:157:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:155:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 182 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 183 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 184 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 185 |[39m         })[33m;[39m
     [90m 186 |[39m
     [90m 187 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:184:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:182:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 208 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 209 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 210 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{minimalAgent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 211 |[39m         })[33m;[39m
     [90m 212 |[39m
     [90m 213 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:210:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:208:18)

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 240 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 241 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 242 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} persistenceKey[33m=[39m[32m"test-persistence-key"[39m layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 243 |[39m         })[33m;[39m
     [90m 244 |[39m
     [90m 245 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:242:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:240:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

    console.error
      The current testing environment is not configured to support act(...)

    [0m [90m 254 |[39m         [36mawait[39m act([36masync[39m () [33m=>[39m {
     [90m 255 |[39m             [36mconst[39m root [33m=[39m createRoot(container)[33m;[39m
    [31m[1m>[22m[39m[90m 256 |[39m             root[33m.[39mrender([33m<[39m[33mAgentChat[39m agent[33m=[39m{agent} layout[33m=[39m[32m"STANDALONE"[39m [33m/[39m[33m>[39m)[33m;[39m
     [90m     |[39m                  [31m[1m^[22m[39m
     [90m 257 |[39m         })[33m;[39m
     [90m 258 |[39m
     [90m 259 |[39m         [36mconst[39m capturedProps [33m=[39m (globalThis [36mas[39m { __lastLlmChatProps[33m?[39m[33m:[39m [33mCapturedLlmChatProps[39m })[33m.[39m__lastLlmChatProps[33m;[39m[0m

      at isConcurrentActEnvironment (node_modules/react-dom/cjs/react-dom-client.development.js:14318:17)
      at warnIfUpdatesNotWrappedWithActDEV (node_modules/react-dom/cjs/react-dom-client.development.js:16020:7)
      at scheduleUpdateOnFiber (node_modules/react-dom/cjs/react-dom-client.development.js:14396:11)
      at updateContainerImpl (node_modules/react-dom/cjs/react-dom-client.development.js:20583:10)
      at ReactDOMRoot.Object.<anonymous>.process.env.NODE_ENV.ReactDOMHydrationRoot.render.ReactDOMRoot.render [as render] (node_modules/react-dom/cjs/react-dom-client.development.js:24759:9)
      at src/book-components/Chat/AgentChat/AgentChat.test.tsx:256:18
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react/cjs/react.development.js:790:22)
      at Object.<anonymous>.process.env.NODE_ENV.exports.act (node_modules/react-dom/cjs/react-dom-test-utils.development.js:22:20)
      at Object.<anonymous> (src/book-components/Chat/AgentChat/AgentChat.test.tsx:254:18)

    console.warn
      Color.fromSafe error:
      Can not create a new Color instance from string "{2}".
      
      Returning default PROMPTBOOK_COLOR.

    [0m [90m 74 |[39m             [90m// <- Note: Can not use `assertsError(error)` here because it causes circular dependency[39m
     [90m 75 |[39m
    [31m[1m>[22m[39m[90m 76 |[39m             console[33m.[39mwarn(
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 77 |[39m                 spaceTrim(
     [90m 78 |[39m                     (block) [33m=>[39m [32m`[39m
     [90m 79 |[39m [32m                        Color.fromSafe error:[39m[0m

      at Function.fromSafe (src/utils/color/Color.ts:76:21)
      at AgentChat (src/book-components/Chat/AgentChat/AgentChat.tsx:37:30)
      at Object.react_stack_bottom_frame (node_modules/react-dom/cjs/react-dom-client.development.js:23863:20)
      at renderWithHooks (node_modules/react-dom/cjs/react-dom-client.development.js:5529:22)
      at updateFunctionComponent (node_modules/react-dom/cjs/react-dom-client.development.js:8897:19)
      at beginWork (node_modules/react-dom/cjs/react-dom-client.development.js:10522:18)
      at runWithFiberInDEV (node_modules/react-dom/cjs/react-dom-client.development.js:1522:13)
      at performUnitOfWork (node_modules/react-dom/cjs/react-dom-client.development.js:15140:22)
      at workLoopSync (node_modules/react-dom/cjs/react-dom-client.development.js:14956:41)
      at renderRootSync (node_modules/react-dom/cjs/react-dom-client.development.js:14936:11)
      at performWorkOnRoot (node_modules/react-dom/cjs/react-dom-client.development.js:14419:13)
      at performWorkOnRootViaSchedulerTask (node_modules/react-dom/cjs/react-dom-client.development.js:16216:7)
      at flushActQueue (node_modules/react/cjs/react.development.js:566:34)
      at node_modules/react/cjs/react.development.js:823:21

PASS apps/agents-server/src/instrumentation-node.test.ts
  ● Console

    console.info
      🗂️ VPS server registry ready with 0 registered server(s) in standalone SQLite mode.

      at registerNodeRuntimeInstrumentation (apps/agents-server/src/instrumentation-node.ts:29:21)

PASS src/scrapers/website/utils/createShowdownConverter.test.ts
PASS apps/agents-server/src/utils/agentRouting/resolveAgentRouteTarget.test.ts
PASS apps/agents-server/src/utils/customDomainRouting.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/parent-agent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS apps/agents-server/src/components/WalletRecordDialog/WalletRecordDialog.test.tsx
PASS apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/adam-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/effective-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://local.example/agents/parent-1"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/remote-parent"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/adam"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://core-test.ptbk.io/agents/adam" after 3 attempts: NotFoundError: Failed to import agent from "https://core-test.ptbk.io/agents/adam"
      
      Agent not found
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:486:10)
          at resolveParentAgentContext (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:526:11)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:838:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:409:37) {
        id: undefined
      }

    [0m [90m 124 |[39m             [36mreturn[39m result[33m.[39mvalue[33m;[39m
     [90m 125 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 126 |[39m             console[33m.[39mwarn(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 127 |[39m                 [32m`[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`[39m[33m,[39m
     [90m 128 |[39m                 error[33m,[39m
     [90m 129 |[39m             )[33m;[39m[0m

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:486:10)
      at resolveParentAgentContext (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:526:11)
      at resolveInheritedAgentSource (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:838:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:409:37)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://federated.example/agents/missing-source"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.warn
      [importAgentWithFallback] Falling back for "https://federated.example/agents/missing-source" after 3 attempts: NotFoundError: Failed to import agent from "https://federated.example/agents/missing-source"
      
      Remote import unavailable
          at deserializeError (C:\Users\me\work\ai\promptbook\src\errors\utils\deserializeError.ts:32:31)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:188:45
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at importAgent (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgent.ts:230:16)
          at retryWithBackoff (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\retryWithBackoff.ts:181:27)
          at C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\importAgentWithFallback.ts:112:28
          at importAgentCorpus (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:486:10)
          at resolveImportCommitmentLine (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:597:39)
          at resolveAgentSourceBuild (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:701:48)
          at resolveInheritedAgentSource (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.ts:839:27)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\resolveInheritedAgentSource.test.ts:499:37) {
        id: undefined
      }

    [0m [90m 124 |[39m             [36mreturn[39m result[33m.[39mvalue[33m;[39m
     [90m 125 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 126 |[39m             console[33m.[39mwarn(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 127 |[39m                 [32m`[importAgentWithFallback] Falling back for "${agentUrl}" after ${configuration.maxAttempts} attempts:`[39m[33m,[39m
     [90m 128 |[39m                 error[33m,[39m
     [90m 129 |[39m             )[33m;[39m[0m

      at apps/agents-server/src/utils/importAgentWithFallback.ts:126:21
      at importAgentCorpus (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:486:10)
      at resolveImportCommitmentLine (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:597:39)
      at resolveAgentSourceBuild (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:701:48)
      at resolveInheritedAgentSource (apps/agents-server/src/utils/resolveInheritedAgentSource.ts:839:27)
      at Object.<anonymous> (apps/agents-server/src/utils/resolveInheritedAgentSource.test.ts:499:37)

PASS apps/agents-server/src/components/CloudflareDnsWizard/CloudflareDnsWizard.test.tsx
PASS src/scrapers/_common/utils/promptbookFetch.test.ts
PASS scripts/find-refactor-candidates/findRefactorCandidatesInProject.test.ts
PASS apps/agents-server/src/components/AgentNaming/translateLegacyAgentText.test.ts
PASS src/scripting/javascript/JavascriptEvalExecutionTools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "cat";
      (async ()=>{ return animal.split('').reverse().join('-') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      t-a-c

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "\"cat\"";
      (async ()=>{ return removeQuotes(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      cat

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      const animal = "The animal is: \"dog\"";
      (async ()=>{ return unwrapResult(animal) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:
      dog

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const get_user_location = customFunctions.get_user_location;
      const calendar_list_events = customFunctions.calendar_list_events;
      const calendar_get_event = customFunctions.calendar_get_event;
      const calendar_create_event = customFunctions.calendar_create_event;
      const calendar_update_event = customFunctions.calendar_update_event;
      const calendar_delete_event = customFunctions.calendar_delete_event;
      const calendar_invite_guests = customFunctions.calendar_invite_guests;
      const open_popup = customFunctions.open_popup;
      const turn_privacy_on = customFunctions.turn_privacy_on;
      const project_list_files = customFunctions.project_list_files;
      const project_read_file = customFunctions.project_read_file;
      const project_upsert_file = customFunctions.project_upsert_file;
      const project_delete_file = customFunctions.project_delete_file;
      const project_create_branch = customFunctions.project_create_branch;
      const project_create_pull_request = customFunctions.project_create_pull_request;
      
      // The script:
      
      (async ()=>{ return open_popup({ url: "https://ptbk.io" }) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.log
      !!!! [Tool] open_popup called { args: { url: 'https://ptbk.io' } }

      at open_popup (src/commitments/USE_POPUP/USE_POPUP.ts:143:25)

    console.info
      🚀 Script evaluated successfully, result:   
      Popup window with URL "https://ptbk.io" was requested.
      
      Note: The agent is currently running on the server, so the popup cannot be opened automatically.
      The user can open it manually from the tool call details in the chat.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
                      // Build-in functions:
                      const spaceTrim = buildinFunctions.spaceTrim;
                      const removeQuotes = buildinFunctions.removeQuotes;
                      const unwrapResult = buildinFunctions.unwrapResult;
                      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
                      const trimCodeBlock = buildinFunctions.trimCodeBlock;
                      const trim = buildinFunctions.trim;
                      const reverse = buildinFunctions.reverse;
                      const removeEmojis = buildinFunctions.removeEmojis;
                      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
                      const capitalize = buildinFunctions.capitalize;
                      const decapitalize = buildinFunctions.decapitalize;
                      const nameToUriPart = buildinFunctions.nameToUriPart;
                      const nameToUriParts = buildinFunctions.nameToUriParts;
                      const removeDiacritics = buildinFunctions.removeDiacritics;
                      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
                      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
                      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
                      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
                      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
                      const parseKeywords = buildinFunctions.parseKeywords;
                      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
                      const extractBlock = buildinFunctions.extractBlock;
      
                      // Custom functions:
                      const get_user_location = customFunctions.get_user_location;
                      const calendar_list_events = customFunctions.calendar_list_events;
                      const calendar_get_event = customFunctions.calendar_get_event;
                      const calendar_create_event = customFunctions.calendar_create_event;
                      const calendar_update_event = customFunctions.calendar_update_event;
                      const calendar_delete_event = customFunctions.calendar_delete_event;
                      const calendar_invite_guests = customFunctions.calendar_invite_guests;
                      const open_popup = customFunctions.open_popup;
                      const turn_privacy_on = customFunctions.turn_privacy_on;
                      const project_list_files = customFunctions.project_list_files;
                      const project_read_file = customFunctions.project_read_file;
                      const project_upsert_file = customFunctions.project_upsert_file;
                      const project_delete_file = customFunctions.project_delete_file;
                      const project_create_branch = customFunctions.project_create_branch;
                      const project_create_pull_request = customFunctions.project_create_pull_request;
      
                      // The script:
                      const animal = "cat";
                      const sound = "meow";
                      (async ()=>{ function makeSentence(animal, sound) {
          return animal + ' makes ' + sound + '.';
      }
      return makeSentence(animal, sound); })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      cat makes meow.

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS src/conversion/validation/validatePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at compilePipeline (src/conversion/compilePipeline.ts:28:37)
      at src/conversion/validation/validatePipeline.test.ts:24:67
      at Object.<anonymous> (src/conversion/validation/validatePipeline.test.ts:20:13)

PASS scripts/run-agent-chat/runAgentChat.test.ts
PASS apps/agents-server/src/database/sqlite/standaloneServerRegistryStore.test.ts
PASS src/conversion/parsePipeline.test.ts
  ● Console

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

    console.warn
      Multiple commands `MODEL modelName gpt-4o` in the task "💬 Prompt"

    [0m [90m 172 |[39m         [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m!==[39m undefined) {
     [90m 173 |[39m             [36mif[39m ($taskJson[33m.[39mmodelRequirements[command[33m.[39mkey] [33m===[39m command[33m.[39mvalue) {
    [31m[1m>[22m[39m[90m 174 |[39m                 console[33m.[39mwarn(
     [90m     |[39m                         [31m[1m^[22m[39m
     [90m 175 |[39m                     [32m`Multiple commands \`MODEL ${command.key} ${command.value}\` in the task "${[39m
     [90m 176 |[39m [32m                        $taskJson.title || $taskJson.name[39m
     [90m 177 |[39m [32m                    }"`[39m[33m,[39m[0m

      at Object.$applyToTaskJson (src/commands/MODEL/modelCommandParser.ts:174:25)
      at applyPipelineTaskCommand (src/conversion/parsePipeline/processPipelineSection.ts:171:67)
      at processPipelineSection (src/conversion/parsePipeline/processPipelineSection.ts:60:9)
      at parsePipeline (src/conversion/parsePipeline.ts:37:31)
      at src/conversion/parsePipeline.test.ts:19:56

PASS src/utils/execCommand/execCommand.test.ts
PASS apps/agents-server/src/components/Header/buildDocumentationDropdownItems.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.knowledge.test.ts
PASS apps/agents-server/src/database/reinstateDefaultAgents.test.ts
PASS apps/agents-server/src/utils/errorReporting/registerServerErrorSentryLogging.test.ts
PASS apps/agents-server/src/components/DnsRecordsInstructions/DnsRecordsInstructions.test.tsx
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectInfo.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.closed.test.ts
PASS src/cli/cli-commands/coder/boilerplateTemplates.test.ts
PASS apps/agents-server/src/components/ApplicationErrorPage/ApplicationErrorPage.test.tsx
PASS src/llm-providers/agent/RemoteAgent.test.ts
  ● Console

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

    [0m [90m 37 |[39m
     [90m 38 |[39m         [90m// TODO: [🟥] Detect browser / node and make it colorful[39m
    [31m[1m>[22m[39m[90m 39 |[39m         console[33m.[39mwarn(warningMessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 40 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 41 |[39m
     [90m 42 |[39m         [90m/*[39m[0m

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:128:33)

    console.warn
      You have not provided any `LlmExecutionTools`
      This means that you won't be able to execute any prompts that require large language models like GPT-4 or Anthropic's Claude.
      
      Technically, it's not an error, but it's probably not what you want because it does not make sense to use Promptbook without language models.

    [0m [90m 37 |[39m
     [90m 38 |[39m         [90m// TODO: [🟥] Detect browser / node and make it colorful[39m
    [31m[1m>[22m[39m[90m 39 |[39m         console[33m.[39mwarn(warningMessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 40 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 41 |[39m
     [90m 42 |[39m         [90m/*[39m[0m

      at joinLlmExecutionTools (src/llm-providers/_multiple/joinLlmExecutionTools.ts:39:17)
      at getSingleLlmExecutionTools (src/llm-providers/_multiple/getSingleLlmExecutionTools.ts:18:36)
      at new Agent (src/llm-providers/agent/Agent.ts:164:49)
      at new RemoteAgent (src/llm-providers/agent/RemoteAgent.ts:306:9)
      at Function.connect (src/llm-providers/agent/RemoteAgent.ts:245:29)
      at Object.<anonymous> (src/llm-providers/agent/RemoteAgent.test.ts:173:33)

PASS apps/agents-server/src/app/admin/backup/BackupClient.test.tsx
PASS apps/agents-server/src/utils/chat/resolveCitationSourceLabel.test.ts
PASS scripts/run-agent-chat/executeAgentChatTurn.test.ts
PASS apps/agents-server/src/utils/chatExport/sanitizeChatPdfExportHtml.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/bookScopedAgentReferences.test.ts
PASS src/conversion/validation/pipelineStringToJson-parseErrors.test.ts
PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromPromise.test.ts
PASS apps/agents-server/src/utils/importAgent.test.ts
  ● Console

    console.log
      importAgent "https://core-test.ptbk.io/agents/test-0"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

    console.log
      importAgent "https://core-test.ptbk.io/agents/foobarhululu"

      at importAgent (apps/agents-server/src/utils/importAgent.ts:143:13)

PASS src/llm-providers/agent/Agent.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts
  ● Console

    console.warn
      Failed to import file ./local/path/data.json: Error: Invalid file URL or path: ./local/path/data.json
          at readImportedFile (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:135:11)
          at createImportedFileSystemMessage (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:91:41)
          at importReferencedFiles (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:70:37)
          at augmentAgentModelRequirementsFromSource (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\augmentAgentModelRequirementsFromSource.ts:46:20)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:34:20)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

    [0m [90m 71 |[39m             requirements [33m=[39m appendSystemMessageSection(requirements[33m,[39m importedContent)[33m;[39m
     [90m 72 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 73 |[39m             console[33m.[39mwarn([32m`Failed to import file ${fileUrl}:`[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 74 |[39m         }
     [90m 75 |[39m     }
     [90m 76 |[39m[0m

      at importReferencedFiles (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:73:21)
      at augmentAgentModelRequirementsFromSource (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/augmentAgentModelRequirementsFromSource.ts:46:20)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:34:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.import.test.ts:28:24)

PASS src/scripting/_test/custom-function-missing.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHelloWithTypo = customFunctions.addHelloWithTypo;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)


  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at Object.asPromise (src/execution/ExecutionTask.ts:261:34)


  ●  Cannot log after tests are done. Did you forget to wait for something async in your test?
    Attempted to log "Parameter `{greeting}` should be an output parameter, but it was not generated during pipeline execution

    Note: This is a warning which happened after the pipeline was executed, and `{greeting}` was not for some reason defined in output parameters

    All parameters:
    1) `{yourName}` `[input parameter]` Name of the hero
    2) `{greeting}` `[output parameter]` <- Warning: Should be in the output but its not |".

    [0m [90m 20 |[39m
     [90m 21 |[39m     [36mfor[39m ([36mconst[39m warning [36mof[39m warnings) {
    [31m[1m>[22m[39m[90m 22 |[39m         console[33m.[39mwarn(warning[33m.[39mmessage)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 23 |[39m         [90m// <- TODO: [🏮] Some standard way how to transform errors into warnings and how to handle non-critical fails during the tasks[39m
     [90m 24 |[39m     }
     [90m 25 |[39m[0m

      at console.warn (node_modules/@jest/console/build/BufferedConsole.js:191:10)
      at assertsTaskSuccessful (src/execution/assertsTaskSuccessful.ts:22:17)
      at finalizeTaskResult (src/execution/ExecutionTask.ts:202:26)
      at src/execution/ExecutionTask.ts:172:21

PASS src/scripting/_test/custom-function-with-dependencies.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Matthew the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist (28 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Mark the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello Luke the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      const withStatistics = customFunctions.withStatistics;
      
      // The script:
      const resultString = "Hello John the Evangelist";
      (async ()=>{ return withStatistics(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist (25 characters, 4 words)

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/commitments/USE_PRIVACY/USE_PRIVACY.test.ts
PASS src/scripting/_test/script-execution-tools.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "apple";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:
      bpple

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      // -- No custom functions --
      
      // The script:
      const thing = "a cup of coffee";
      (async ()=>{ return thing.split('a').join('b') })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)
          at async Promise.all (index 0)

    console.info
      🚀 Script evaluated successfully, result:   
      b cup of coffee

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)

PASS scripts/run-codex-prompts/git/commitChanges.test.ts
PASS src/scripting/_test/custom-function.test.ts
  ● Console

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Matthew the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Matthew the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Mark the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Mark the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "Luke the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello Luke the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

    console.info
      🚀 Evaluating javascript script:
      
      // Build-in functions:
      const spaceTrim = buildinFunctions.spaceTrim;
      const removeQuotes = buildinFunctions.removeQuotes;
      const unwrapResult = buildinFunctions.unwrapResult;
      const trimEndOfCodeBlock = buildinFunctions.trimEndOfCodeBlock;
      const trimCodeBlock = buildinFunctions.trimCodeBlock;
      const trim = buildinFunctions.trim;
      const reverse = buildinFunctions.reverse;
      const removeEmojis = buildinFunctions.removeEmojis;
      const prettifyMarkdown = buildinFunctions.prettifyMarkdown;
      const capitalize = buildinFunctions.capitalize;
      const decapitalize = buildinFunctions.decapitalize;
      const nameToUriPart = buildinFunctions.nameToUriPart;
      const nameToUriParts = buildinFunctions.nameToUriParts;
      const removeDiacritics = buildinFunctions.removeDiacritics;
      const normalizeWhitespaces = buildinFunctions.normalizeWhitespaces;
      const normalizeToKebabCase = buildinFunctions.normalizeToKebabCase;
      const normalizeTo_camelCase = buildinFunctions.normalizeTo_camelCase;
      const normalizeTo_snake_case = buildinFunctions.normalizeTo_snake_case;
      const normalizeTo_PascalCase = buildinFunctions.normalizeTo_PascalCase;
      const parseKeywords = buildinFunctions.parseKeywords;
      const normalizeTo_SCREAMING_CASE = buildinFunctions.normalizeTo_SCREAMING_CASE;
      const extractBlock = buildinFunctions.extractBlock;
      
      // Custom functions:
      const addHello = customFunctions.addHello;
      
      // The script:
      const resultString = "John the Evangelist";
      (async ()=>{ return addHello(resultString) })()

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:193:21)

    console.info
      🚀 Script evaluated successfully, result:   
      Hello John the Evangelist

      at JavascriptEvalExecutionTools.execute (src/scripting/javascript/JavascriptEvalExecutionTools.ts:208:25)
          at async Promise.all (index 0)

PASS src/book-2.0/agent-source/communication-samples.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAllAgentProjectSummaries.test.ts
PASS apps/agents-server/src/utils/metadataConfigurationTransfer.test.ts
PASS apps/agents-server/src/utils/serverManagement/standaloneVpsServerMetadata.test.ts
PASS src/cli/cli-commands/coder/run.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatSummary.agentGoalChat.test.ts
PASS scripts/run-codex-prompts/common/waitForPause.test.ts
PASS apps/agents-server/src/utils/userChat/listUserChats.test.ts
PASS apps/agents-server/src/utils/agentProjects/hasAgentProjects.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectAccess.test.ts
PASS src/dialogs/user-interface-execution-tools.test.ts
  ● Console

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite apple to buy?" is not apple but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

    console.info
      📖 Thing
      👤 Answer to question "Thing: What is your favorite a cup of coffee to buy?" is not a cup of coffee but Pear.

      at CallbackInterfaceTools.promptDialog (src/dialogs/callback/CallbackInterfaceTools.ts:22:21)

PASS src/book-2.0/book-language-documentation/renderGroupedCommitmentDocumentationMarkdown.test.ts
PASS src/llm-providers/agent/AgentLlmExecutionTools.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ensureExternalAgentRepository.test.ts
PASS apps/agents-server/src/instrumentation.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks.test.ts
PASS apps/agents-server/src/components/ViewportHeightController/resolveVisibleViewportHeight.test.ts
PASS apps/agents-server/src/components/UsersList/PasswordGeneratorDialog.test.tsx
PASS src/book-components/Chat/save/pdf/pdfSaveFormatDefinition.test.ts
PASS src/scrapers/document-legacy/LegacyDocumentScraper.test.ts
PASS apps/agents-server/src/app/admin/messages/MessageTableCells.test.tsx
PASS src/book-components/Chat/hooks/useResolvedCitationLabel.test.tsx
PASS apps/agents-server/src/app/agents/[agentName]/chat/OptimisticAgentChatRouteLoading.test.tsx
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectProfile.test.ts
PASS scripts/run-codex-prompts/main/runPromptRound.test.ts
  ● Console

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:550:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:602:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

    console.warn
      [33mPrompt round failed (retry 1/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)

    console.warn
      [33mPrompt round failed (retry 2/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.warn
      [33mPrompt round failed (retry 3/3): The harness died[39m

    [0m [90m 310 |[39m
     [90m 311 |[39m     [36mif[39m ([33m![39misRichUiEnabled) {
    [31m[1m>[22m[39m[90m 312 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 313 |[39m             colors[33m.[39myellow(
     [90m 314 |[39m                 [32m`Prompt round failed (retry ${attemptedRetries}/${MAX_RETRY_ATTEMPTS_AFTER_ERROR}): ${errorMessage}`[39m[33m,[39m
     [90m 315 |[39m             )[33m,[39m[0m

      at waitAfterErrorBeforeRetry (scripts/run-codex-prompts/main/runPromptRound.ts:312:17)
      at preserveArtifactsOnSuccess (scripts/run-codex-prompts/main/runPromptRound.ts:199:27)
      at runPromptRound (scripts/run-codex-prompts/main/runPromptRound.ts:133:5)
      at Object.<anonymous> (scripts/run-codex-prompts/main/runPromptRound.test.ts:686:9)

    console.info
      [34mProcessing example.md#1[39m

      at setPromptRoundRunningState (scripts/run-codex-prompts/main/runPromptRound.ts:364:13)

PASS scripts/run-codex-prompts/cli/parseRunOptions.test.ts
PASS src/book-components/Chat/SourceChip/SourceChip.test.tsx
PASS apps/agents-server/src/utils/transpilers/getTranspiledAgentExportWarnings.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.delete.test.ts
PASS apps/agents-server/src/utils/agentProjects/resolveAgentProjectFaviconRelativePath.test.ts
PASS src/collection/pipeline-collection/pipelineCollectionToJson.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts
  ● Console

    console.warn
      Failed to resolve commitment references for FROM, falling back to safe defaults: Error: Unable to resolve agent reference "foo"
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:32:23)
          at C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:397:39
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:404:13)
          at Object.mockConstructor [as resolveCommitmentContent] (C:\Users\me\work\ai\promptbook\node_modules\jest-mock\build\index.js:148:19)
          at resolveCommitmentContent (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:68:45)
          at applyCommitmentsToAgentModelRequirements (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments\applyCommitmentsToAgentModelRequirements.ts:37:41)
          at createAgentModelRequirementsWithCommitments (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.ts:33:66)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\src\book-2.0\agent-source\createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:84)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 68 |[39m         [36mreturn[39m [36mawait[39m agentReferenceResolver[33m.[39mresolveCommitmentContent(commitment[33m.[39mtype[33m,[39m commitment[33m.[39mcontent)[33m;[39m
     [90m 69 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 70 |[39m         console[33m.[39mwarn(
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 71 |[39m             [32m`Failed to resolve commitment references for ${commitment.type}, falling back to safe defaults:`[39m[33m,[39m
     [90m 72 |[39m             error[33m,[39m
     [90m 73 |[39m         )[33m;[39m[0m

      at resolveCommitmentContent (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:70:17)
      at applyCommitmentsToAgentModelRequirements (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments/applyCommitmentsToAgentModelRequirements.ts:37:35)
      at createAgentModelRequirementsWithCommitments (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.ts:33:20)
      at Object.<anonymous> (src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.agentReferenceResolver.test.ts:44:35)

PASS src/book-components/Chat/Chat/ChatActionsBar.test.tsx
PASS apps/agents-server/src/utils/stalwart/readStalwartEmailSnapshot.test.ts
PASS apps/agents-server/src/components/Homepage/ExternalAgentsSectionClient.test.tsx
PASS src/executables/browsers/locateBrowser.test.ts
PASS src/avatars/renderAvatarVisualTerminalText.test.ts
PASS apps/agents-server/src/utils/defaultAgents/resolveDefaultAgentsStatus.test.ts
PASS src/commitments/USE_IMAGE_GENERATOR/USE_IMAGE_GENERATOR.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSource.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.unknown.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderAgent.test.ts
PASS apps/agents-server/src/components/AgentProjects/useAgentProjectMarkdownReferences.test.ts
PASS src/prepare/isPipelinePrepared.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.tools.test.ts
PASS src/cli/cli-commands/coder/mergeStringRecordJsonFile.test.ts
PASS apps/agents-server/src/database/runDatabaseMigrations.test.ts
PASS src/llm-providers/mocked/test/joker.test.ts
  ● Console

    console.info
      💬 Mocked callChatModel call

      at MockedEchoLlmExecutionTools.callChatModel (src/llm-providers/mocked/MockedEchoLlmExecutionTools.ts:81:21)

PASS apps/agents-server/src/utils/calendars/extractUseCalendarConnectionsFromAgentSource.test.ts
PASS apps/agents-server/src/components/ChatEnterBehavior/ChatEnterBehaviorPreferencesProvider.test.tsx
PASS scripts/run-agent-messages/main/synchronizeGithubAgentRunnerRepositories.test.ts
PASS src/llm-providers/agent/self-learning/SelfLearningManager.test.ts
  ● Console

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nINITIAL MESSAGE Hello! I am your helpful assistant.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '- The agent source does not have an INITIAL MESSAGE defined, generate one.\n' +
          '- The INITIAL MESSAGE should be welcoming, informative about the agent capabilities and also should give some quick options to start the conversation with the agent.\n' +
          '- The quick option looks like `[👋 Hello](?message=Hello, how are you?)`\n' +
          '- A browser action option looks like `[🖨️ Print](?action=window.print%28%29)` and runs only after the user clicks it.\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

    console.info
      [46m[Self-learning][49m[36m Sampling[39m

      at SelfLearningManager.appendSample (src/llm-providers/agent/self-learning/SelfLearningManager.ts:129:17)

    console.info
      [46m[Self-learning][49m[36m Teacher[39m

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:173:17)

    console.log
      !!!! teacherResult {
        content: '```book\nKNOWLEDGE New info.\n```',
        modelName: 'teacher-model',
        timing: {
          start: '2026-02-24T12:00:00.000Z',
          complete: '2026-02-24T12:00:01.000Z'
        },
        usage: {
          price: { value: 0, isUncertain: true },
          duration: { value: 0, isUncertain: true },
          input: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          },
          output: {
            tokensCount: [Object],
            charactersCount: [Object],
            wordsCount: [Object],
            sentencesCount: [Object],
            linesCount: [Object],
            paragraphsCount: [Object],
            pagesCount: [Object]
          }
        },
        rawPromptContent: 'You are a teacher agent helping another agent to learn from its interactions.\n' +
          '\n' +
          'Here is your current client which you are teaching:\n' +
          '\n' +
          '```book\n' +
          '# My Agent\n' +
          '\n' +
          'PERSONA A helpful assistant.\n' +
          'INITIAL MESSAGE Welcome!\n' +
          '\n' +
          '            USER MESSAGE\n' +
          '            Hello\n' +
          '            \n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_REQUEST",\n' +
          '    "rawPromptContent": "Hello",\n' +
          '    "rawRequest": null\n' +
          '}\n' +
          '\n' +
          'INTERNAL MESSAGE\n' +
          '{\n' +
          '    "kind": "MODEL_RESPONSE",\n' +
          '    "rawResponse": {},\n' +
          '    "usage": {\n' +
          '        "price": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "duration": {\n' +
          '            "value": 0,\n' +
          '            "isUncertain": true\n' +
          '        },\n' +
          '        "input": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        },\n' +
          '        "output": {\n' +
          '            "tokensCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "charactersCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "wordsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "sentencesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "linesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "paragraphsCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            },\n' +
          '            "pagesCount": {\n' +
          '                "value": 0,\n' +
          '                "isUncertain": true\n' +
          '            }\n' +
          '        }\n' +
          '    },\n' +
          '    "timing": {\n' +
          '        "start": "2026-02-24T12:00:00.000Z",\n' +
          '        "complete": "2026-02-24T12:00:01.000Z"\n' +
          '    }\n' +
          '}\n' +
          '\n' +
          '            AGENT MESSAGE\n' +
          '            Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          '```\n' +
          '\n' +
          '**And here is the latest interaction:**\n' +
          '\n' +
          '**User:**\n' +
          'Hello\n' +
          '\n' +
          '**Agent:**\n' +
          'Hi there!\n' +
          '\n' +
          '\n' +
          '\n' +
          '**Rules:**\n' +
          '\n' +
          '- Decide what the agent should learn from this interaction.\n' +
          '- Append new commitments at the end of the agent source.\n' +
          '- Return only newly learned commitments, never repeat commitments that are already present.\n' +
          '- Do not modify the current agent source, just return new commitments (KNOWLEDGE, RULE, etc.).\n' +
          '- If there is nothing new to learn, return empty book code block\n' +
          '- Wrap the commitments in a book code block.\n' +
          '- Do not explain anything, just return the commitments wrapped in a book code block.\n' +
          '- Write the learned commitments in the same style and language as in the original agent source.\n' +
          '\n' +
          '\n' +
          '\n' +
          '\n' +
          'This is how book code block looks like:\n' +
          '\n' +
          '```book\n' +
          'KNOWLEDGE The sky is blue.\n' +
          'RULE Always be polite.\n' +
          '```',
        rawRequest: null,
        rawResponse: {}
      }

      at SelfLearningManager.callTeacher (src/llm-providers/agent/self-learning/SelfLearningManager.ts:262:17)

PASS apps/agents-server/src/components/Header/ControlPanel/createControlPanelContentState.test.ts
PASS apps/agents-server/src/utils/internalS3/readInternalS3Directory.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.commitmentAggregation.test.ts
PASS apps/agents-server/src/utils/thinkingMessages.test.ts
PASS apps/agents-server/src/utils/transpilers/createTranspiledAgentExportZipBuffer.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectsPaths.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.title.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirementsWithCommitments.blocks.test.ts
PASS apps/agents-server/src/utils/email/sendEmailChatReply.test.ts
PASS src/conversion/validation/validatePipeline-logicErrors.test.ts
PASS src/cli/cli-commands/agents-server/init.test.ts
PASS src/commitments/USE_USER_LOCATION/USE_USER_LOCATION.test.ts
PASS apps/agents-server/src/app/api/auth/change-password/route.test.ts
PASS apps/utils/src/app/avatars/avatarPlaygroundUrlState.test.ts
PASS apps/agents-server/src/utils/userChat/expiredRunningUserChatJobDiagnostics.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatHarnessProgressCard.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateInstalledVersions.test.ts
PASS apps/agents-server/src/utils/serverLimits.test.ts
PASS src/book-2.0/agent-source/createAgentModelRequirements.writing.test.ts
PASS src/book-components/Chat/Chat/ChatCitationModal.test.tsx
PASS apps/agents-server/src/components/Homepage/HomepageOptimisticNavigation.test.tsx
PASS apps/agents-server/src/components/AgentProfile/AgentCapabilityChips.test.tsx
PASS apps/agents-server/src/utils/knowledge/resolveWebsiteKnowledgeSourcesForServer.test.ts
PASS src/commands/_common/stringifyCommand.test.ts
PASS src/book-components/Chat/Chat/ChatRatingModal.test.tsx
PASS src/book-components/BookEditor/createDuplicateFromCommitmentDiagnostics.test.ts
PASS src/utils/agents/terminalAgentAvatarVisual.test.ts
PASS apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts
  ● Console

    console.error
      Error processing inbound email NotAllowed: SendGrid Inbound Parse webhook signature verification failed.
          at verifySendgridInboundParseWebhook (C:\Users\me\work\ai\promptbook\apps\agents-server\src\message-providers\email\sendgrid\verifySendgridInboundParseWebhook.ts:108:15)
          at POST (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.ts:16:42)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\app\api\emails\incoming\sendgrid\route.test.ts:107:26)

    [0m [90m 50 |[39m         [36mreturn[39m [33mNextResponse[39m[33m.[39mjson({ success[33m:[39m [36mtrue[39m })[33m;[39m
     [90m 51 |[39m     } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 52 |[39m         console[33m.[39merror([32m'Error processing inbound email'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                 [31m[1m^[22m[39m
     [90m 53 |[39m         [36mreturn[39m [33mNextResponse[39m[33m.[39mjson(
     [90m 54 |[39m             { error[33m:[39m error [36minstanceof[39m [33mError[39m [33m?[39m error[33m.[39mmessage [33m:[39m [32m'Unknown error'[39m }[33m,[39m
     [90m 55 |[39m             { status[33m:[39m resolveSendgridInboundParseWebhookErrorStatus(error) }[33m,[39m[0m

      at POST (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.ts:52:17)
      at Object.<anonymous> (apps/agents-server/src/app/api/emails/incoming/sendgrid/route.test.ts:107:26)

PASS src/collection/pipeline-collection/constructors/createPipelineCollectionFromJson.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTesting.test.ts
PASS apps/agents-server/src/app/api/admin/servers/[serverId]/route.test.ts
PASS src/book-components/Chat/Chat/useChatInputAreaComposer.draftMessage.test.tsx
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateJobHistory.test.ts
PASS src/scrapers/document/DocumentScraper.test.ts
PASS src/book-components/Chat/Chat/CitationIframePreview.test.tsx
PASS scripts/run-codex-prompts/prompts/markPromptInProgress.test.ts
PASS apps/agents-server/src/utils/dnsRecords/resolveDnsRecordBatchPlan.test.ts
PASS apps/agents-server/src/components/Header/buildAgentMenuStructure.test.tsx
PASS src/commitments/_common/getAllCommitmentDefinitions.test.ts
PASS scripts/run-codex-prompts/prompts/writePromptRunTrace.test.ts
PASS apps/agents-server/src/utils/chat/executeQuickActionButton.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerCoreAgents.test.ts
PASS apps/agents-server/src/components/Portal/Dialog.test.tsx
PASS src/commitments/_common/getCommitmentNoticeMetadata.test.ts
PASS src/commands/PERSONA/personaCommand.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createAgentInheritanceDiagnostics.test.ts
PASS scripts/repair-imports/utils/repairImportUtils.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/normalizeCreateServerInput.test.ts
PASS scripts/utils/emojiTags/validSinglePictogramEmojis.test.ts
PASS apps/agents-server/src/app/sitemap.xml/route.test.ts
PASS apps/agents-server/src/components/CronJobConfiguration/CronJobConfiguration.test.tsx
PASS scripts/utils/emojiTags/scanEmojiTagUsage.test.ts
PASS src/llm-providers/mocked/test/fakeTextToExpectations.test.ts
  ● Console

    console.info
      { expectations: { characters: { min: 10, max: 50 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { sentences: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { paragraphs: { min: 1, max: 5 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { lines: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { pages: { min: 2 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      {
        expectations: {
          characters: { min: 10, max: 500 },
          words: { min: 1, max: 5 },
          sentences: { min: 1, max: 5 }
        }
      }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

    console.info
      { expectations: { words: { min: 10 }, lines: { min: 3 } } }

      at $fakeTextToExpectations (src/llm-providers/mocked/$fakeTextToExpectations.ts:24:13)

PASS book/scripts/import-markdown/placeImports.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/ExternalUserChatAdminActions.test.tsx
PASS apps/agents-server/src/components/Header/createMyChatsMobileMenuItem.test.ts
PASS src/commands/PARAMETER/parameterCommand.test.ts
PASS scripts/run-agent-messages/ui/loadAgentRunUiMetadata.test.ts
PASS src/book-components/Chat/save/react/reactSaveFormatDefinition.test.ts
PASS apps/agents-server/src/utils/projects/extractProjectRepositoriesFromAgentSource.test.ts
PASS apps/agents-server/src/utils/chat/messageSuffix.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobRunReport.test.ts
PASS apps/agents-server/src/components/_utils/headlessParam.test.tsx
PASS scripts/run-agent-messages/main/runPersistentAgentWatch.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatJobHeartbeatController.test.ts
PASS apps/agents-server/src/utils/vpsConfiguration.test.ts
PASS scripts/run-codex-prompts/prompts/resolveInterruptedPrompt.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.unknown.test.ts
PASS apps/agents-server/src/constants/defaultAgentAvatarVisual.test.ts
PASS src/executables/apps/locatePandoc.test.ts
PASS apps/agents-server/src/components/AgentAvatar/AgentAvatar.test.tsx
PASS src/utils/normalization/unwrapResult.test.ts
PASS src/cli/cli-commands/coder/add.test.ts
  ● Console

    console.info
      [32m✓ Added prompt [✨🥿] in prompts\2026-09-0000-add-a-dark-mode-toggle.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨📴] in prompts\2026-09-0000-task-with-priority.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨❌] in prompts\2026-09-0000-first-line-summary.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🐽] in prompts\2026-09-0000-first-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

    console.info
      [32m✓ Added prompt [✨🤣] in prompts\2026-09-0010-second-feature.md[39m

      at addCoderPrompt (src/cli/cli-commands/coder/add.ts:225:13)

PASS src/cli/test/ptbk.test.ts (303.858 s)
  ● Console

    console.warn
      [33m**OpenAI Codex** is outdated.[39m
      
      [33mInstalled version: `0.153.4`[39m
      [33mNewest version: `0.154.0`[39m
      [33mInstalled command: `C:\Users\me\.nvm\versions\node\v22.23.2\bin\codex` *(global npm installation)*[39m

    [0m [90m 44 |[39m     }
     [90m 45 |[39m
    [31m[1m>[22m[39m[90m 46 |[39m     console[33m.[39mwarn(colors[33m.[39myellow(formatHarnessInstallationWarning(status)))[33m;[39m
     [90m    |[39m             [31m[1m^[22m[39m
     [90m 47 |[39m
     [90m 48 |[39m     [36mif[39m (installationState [33m===[39m [32m'not-installed'[39m) {
     [90m 49 |[39m         [36mawait[39m $applyMissingHarnessInstallation(definition[33m,[39m questionsOptions)[33m;[39m[0m

      at $applyHarnessInstallationStatus (src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts:46:13)
      at $ensureHarnessInstallations (src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts:39:46)
      at src/cli/cli-commands/coder/init.ts:119:13
      at Command.<anonymous> (src/cli/cli-commands/common/handleActionErrors.ts:35:13)
      at Command.parseAsync (node_modules/commander/lib/command.js:1015:5)
      at Object.<anonymous> (src/cli/test/ptbk.test.ts:161:13)

    console.warn
      [33m**Claude Code** is outdated.[39m
      
      [33mInstalled version: `2.1.258`[39m
      [33mNewest version: `2.1.269`[39m
      [33mInstalled command: `C:\Users\me\.nvm\versions\node\v22.23.2\bin\claude` *(global npm installation)*[39m

    [0m [90m 44 |[39m     }
     [90m 45 |[39m
    [31m[1m>[22m[39m[90m 46 |[39m     console[33m.[39mwarn(colors[33m.[39myellow(formatHarnessInstallationWarning(status)))[33m;[39m
     [90m    |[39m             [31m[1m^[22m[39m
     [90m 47 |[39m
     [90m 48 |[39m     [36mif[39m (installationState [33m===[39m [32m'not-installed'[39m) {
     [90m 49 |[39m         [36mawait[39m $applyMissingHarnessInstallation(definition[33m,[39m questionsOptions)[33m;[39m[0m

      at $applyHarnessInstallationStatus (src/cli/cli-commands/common/harness/$applyHarnessInstallationStatus.ts:46:13)
      at $ensureHarnessInstallations (src/cli/cli-commands/common/harness/$ensureHarnessInstallations.ts:39:46)
      at src/cli/cli-commands/coder/init.ts:119:13
      at Command.<anonymous> (src/cli/cli-commands/common/handleActionErrors.ts:35:13)
      at Command.parseAsync (node_modules/commander/lib/command.js:1015:5)
      at Object.<anonymous> (src/cli/test/ptbk.test.ts:161:13)

PASS src/book-components/BookEditor/createDeprecatedCommitmentDiagnostics.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$resolvePromptbookCliInstallations.test.ts
PASS src/utils/parameters/mapAvailableToExpectedParameters.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/resolveEffectiveAgentGoal.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.order.test.ts
PASS src/book-components/BookEditor/createUnknownCommitmentDiagnostics.test.ts
PASS apps/agents-server/src/utils/serverManagement/createManagedServer/seedServerDefaultAgents.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createUnresolvedAgentReferenceDiagnostics.test.ts
PASS src/executables/platforms/locateAppOnWindows.test.ts
PASS src/commitments/DELETE/DELETE.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/config/bookSections.test.ts
PASS scripts/verify-prompts/verify-prompts.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/createAgentPersistenceRecords.test.ts
PASS src/commitments/CLOSED/CLOSED.test.ts
PASS src/cli/cli-commands/coder/generate-boilerplates.test.ts
  ● Console

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🏆] [✨😞] [✨😁] [✨😎] [✨🐷][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 5 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🏆][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨😞][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨😁][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0030-qux.md with [✨😎][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0040-brr.md with [✨🐷][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 5 prompts in 5 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

    console.info
      🚀  Generate prompt boilerplate files

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:121:13)

    console.info
      [34mHighest existing number for 2026-09 found: 0000[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:133:13)

    console.info
      [32mFound 1071 available fresh emojis[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:144:13)

    console.info
      [32mSelected emojis: [✨🦛] [✨🤣] [✨🆘][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:145:13)

    console.info
      [33mCreating 3 files:[39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:183:13)

    console.info
      [32m✓ Created: 2026-09-0000-foo.md with [✨🦛][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0010-bar.md with [✨🤣][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [32m✓ Created: 2026-09-0020-baz.md with [✨🆘][39m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:187:17)

    console.info
      [42m Successfully created 12 prompts in 3 prompt boilerplate files! [49m

      at generatePromptBoilerplate (src/cli/cli-commands/coder/generate-boilerplates.ts:190:13)

PASS src/commands/BOOK_VERSION/bookVersionCommand.test.ts
PASS src/cli/cli-commands/coder/$ensureCoderHarnessGitignoreRules.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatJobFailureDetails.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/createServerAgentReferenceResolver.test.ts
PASS src/commands/KNOWLEDGE/knowledgeCommand.test.ts
PASS src/utils/validators/parameterName/validateParameterName.test.ts
PASS apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.test.ts
  ● Console

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      🚀 Checking database migrations automatically for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:86:13)

    console.info
      ✅ Automatic database migration check finished for prefix "server_Test_"

      at runAutomaticDatabaseMigrationsForPrefix (apps/agents-server/src/database/ensureAutomaticDatabaseMigrations.ts:103:13)

PASS src/utils/editable/utils/isFlatPipeline.test.ts
PASS src/utils/misc/computeHash.test.ts
PASS apps/agents-server/src/utils/renameAgentSource.test.ts
PASS src/utils/editable/edit-pipeline-string/deflatePipeline.test.ts
PASS src/utils/random/$generateBookBoilerplate.test.ts
PASS apps/agents-server/src/components/Homepage/useAgentsListSyncState.test.ts
PASS scripts/run-codex-prompts/ui/coderRunUiRefresh.test.ts
PASS apps/agents-server/src/utils/userChat/runImmediateUserChatAnswer.test.ts
PASS src/commands/FOREACH/foreachCommand.test.ts
PASS src/book-2.0/agent-source/parseAgentSource.import.test.ts
PASS scripts/run-codex-prompts/git/gitLongPathsSupport.test.ts
PASS src/commands/POSTPROCESS/postprocessCommand.test.ts
PASS src/commands/X_ACTION/actionCommand.test.ts
PASS src/utils/serialization/isSerializableAsJson.test.ts
PASS src/commands/EXPECT/expectCommand.test.ts
PASS src/avatars/visuals/octopusAvatarVisualShared.test.ts
PASS src/cli/cli-commands/common/questionsCliOptions.test.ts
PASS apps/agents-server/src/utils/speech-to-text/LongRunningSpeechRecognitionSession.test.ts
PASS src/commands/URL/urlCommand.test.ts
PASS apps/agents-server/src/utils/agentBook/createAgentBookDownloadFilename.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.writing.test.ts
PASS scripts/run-agent-messages/messages/resolveAgentProjectRuntimePromptApi.test.ts
PASS scripts/run-codex-prompts/common/createFreeDiskSpaceGuard.test.ts
PASS scripts/run-codex-prompts/common/coderRunEstimateCache.test.ts
PASS src/book-components/Chat/save/markdown/mdSaveFormatDefinition.test.ts
PASS src/llm-providers/openai/computeOpenAiUsage.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/readPersistedVpsSelfUpdateJob.test.ts
PASS src/cli/other/vpsInstall.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectMarkdownReferences.test.ts
PASS apps/agents-server/src/tools/spawn_agent.test.ts
  ● Console

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'anonymous', sourceLength: 36 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nUnknown field(s): `unknownField`.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:106:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'ParseError',
        errorMessage: 'Invalid create-agent payload.\n\nMissing required field `source`.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:118:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'NotAllowed',
        errorMessage: 'Spawning agents is not allowed.\n\nYou are not authorized to create agents.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:131:27)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Create-agent payload is too large.\n' +
          '\n' +
          'Field `source` exceeds 120000 characters.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:142:27)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.info
      [spawn_agent] Created agent { agentId: 'child123', actor: 'user:42', sourceLength: 11 }

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:106:17)

    console.warn
      [spawn_agent] Failed to create agent {
        errorName: 'LimitReachedError',
        errorMessage: 'Spawn rate limit exceeded.\n' +
          '\n' +
          'You can create at most 5 agents per 10 minutes.'
      }

    [0m [90m 120 |[39m         }[33m;[39m
     [90m 121 |[39m
    [31m[1m>[22m[39m[90m 122 |[39m         console[33m.[39mwarn([32m'[spawn_agent] Failed to create agent'[39m[33m,[39m {
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 123 |[39m             errorName[33m:[39m error[33m.[39mname[33m,[39m
     [90m 124 |[39m             errorMessage[33m:[39m error[33m.[39mmessage[33m,[39m
     [90m 125 |[39m         })[33m;[39m[0m

      at spawn_agent (apps/agents-server/src/tools/spawn_agent.ts:122:17)
      at Object.<anonymous> (apps/agents-server/src/tools/spawn_agent.test.ts:170:34)

PASS src/book-2.0/agent-source/extractOpenTeacherInstructions.test.ts
PASS apps/agents-server/src/utils/getVpsAdminChatTasksResponse.test.ts
PASS apps/agents-server/src/utils/authenticationAttemptRateLimit.test.ts
PASS apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts
  ● Console

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:123:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:123:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:124:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:124:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:140:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:140:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:141:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:141:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:167:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:167:25)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:170:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:170:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:177:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:177:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:106:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:246:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:106:25)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to save chat messages to localStorage: ReferenceError: localStorage is not defined
          at Function.saveMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:76:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:185:29)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 76 |[39m             localStorage[33m.[39msetItem(storageKey[33m,[39m serializedMessages)[33m;[39m
     [90m 77 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 78 |[39m             console[33m.[39mwarn([32m'Failed to save chat messages to localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 79 |[39m         }
     [90m 80 |[39m     }
     [90m 81 |[39m[0m

      at Function.saveMessages (src/book-components/Chat/utils/ChatPersistence.ts:78:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:185:29)

    console.warn
      Failed to load chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.loadSerializedMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:44:39)
          at Function.loadMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:88:33)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:197:40)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:316:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:252:3)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 48 |[39m             }
     [90m 49 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 50 |[39m             console[33m.[39mwarn([32m'Failed to load chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m    |[39m                     [31m[1m^[22m[39m
     [90m 51 |[39m         }
     [90m 52 |[39m
     [90m 53 |[39m         [36mreturn[39m [36mthis[39m[33m.[39m[33mMEMORY_STORAGE[39m[33m.[39m[36mget[39m(storageKey) [33m||[39m [36mnull[39m[33m;[39m[0m

      at Function.loadSerializedMessages (src/book-components/Chat/utils/ChatPersistence.ts:50:21)
      at Function.loadMessages (src/book-components/Chat/utils/ChatPersistence.ts:88:33)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:197:40)

    console.warn
      Failed to clear chat messages from localStorage: ReferenceError: localStorage is not defined
          at Function.clearMessages (C:\Users\me\work\ai\promptbook\src\book-components\Chat\utils\ChatPersistence.ts:117:13)
          at Object.<anonymous> (C:\Users\me\work\ai\promptbook\apps\agents-server\src\utils\chat\preparingAgentChipDeduplication.test.ts:110:25)
          at Promise.then.completed (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:298:28)
          at new Promise (<anonymous>)
          at callAsyncCircusFn (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\utils.js:231:10)
          at _callCircusHook (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:281:40)
          at processTicksAndRejections (node:internal/process/task_queues:103:5)
          at _runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:254:5)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:126:9)
          at _runTestsForDescribeBlock (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:121:9)
          at run (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\run.js:71:3)
          at runAndTransformResultsToJestFormat (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapterInit.js:122:21)
          at jestAdapter (C:\Users\me\work\ai\promptbook\node_modules\jest-circus\build\legacy-code-todo-rewrite\jestAdapter.js:79:19)
          at runTestInternal (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:367:16)
          at runTest (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\runTest.js:444:34)
          at Object.worker (C:\Users\me\work\ai\promptbook\node_modules\jest-runner\build\testWorker.js:106:12)

    [0m [90m 117 |[39m             localStorage[33m.[39mremoveItem(storageKey)[33m;[39m
     [90m 118 |[39m         } [36mcatch[39m (error) {
    [31m[1m>[22m[39m[90m 119 |[39m             console[33m.[39mwarn([32m'Failed to clear chat messages from localStorage:'[39m[33m,[39m error)[33m;[39m
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 120 |[39m         }
     [90m 121 |[39m     }
     [90m 122 |[39m[0m

      at Function.clearMessages (src/book-components/Chat/utils/ChatPersistence.ts:119:21)
      at Object.<anonymous> (apps/agents-server/src/utils/chat/preparingAgentChipDeduplication.test.ts:110:25)

PASS src/commitments/_common/getGroupedCommitmentDefinitions.openClosed.test.ts
PASS src/utils/execCommand/execCommandNormalizeOptions.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/newAgentWizardPresets.test.ts
PASS scripts/run-codex-prompts/prompts/printStats.test.ts
PASS src/commitments/USE_PROJECT/USE_PROJECT.test.ts
PASS src/postprocessing/utils/extractJsonBlock.test.ts
PASS apps/agents-server/src/components/Header/buildHeaderSystemMenuItems.test.ts
PASS scripts/run-codex-prompts/prompts/buildScriptPath.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/mapVpsSelfUpdateJobToAdminChatTask.test.ts
PASS src/commands/SECTION/sectionCommand.test.ts
PASS src/executables/locateApp.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimeDesiredState.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useChatDocumentTitle.test.tsx
PASS apps/agents-server/src/components/Header/buildActiveAgentViewItems.test.ts
PASS scripts/run-codex-prompts/main/resolvePromptRunner.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown.test.ts
PASS src/pipeline/isValidPipelineString.test.ts
PASS src/book-3.0/CliAgent.test.ts
PASS apps/agents-server/src/utils/userChat/persistFrozenUserChat.test.ts
PASS apps/agents-server/src/app/api/agents/route.test.ts
PASS apps/agents-server/src/tools/createChatAttachmentToolFunctions.test.ts
PASS apps/agents-server/src/app/admin/messages/messageAdminPresentation.test.ts
PASS scripts/run-agent-messages/messages/resolveAgentProjectsUrlPath.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectHrefs.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/actions.test.ts
PASS src/commitments/_common/getGroupedCommitmentDefinitions.use.test.ts
PASS src/commitments/index.test.ts
PASS apps/agents-server/src/message-providers/email/stalwart/StalwartMessageProvider.test.ts
PASS src/executables/browsers/locateFirefox.test.ts
PASS scripts/generate-packages/addDependenciesForGeneratedPackages.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/projectReferenceActions.test.ts
PASS scripts/run-codex-prompts/testing/runTestBefore.test.ts
  ● Console

    console.info
      [90mRunning initial tests before the agent coding starts: npm run test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

    console.info
      [32mPre-coding tests passed.[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:51:17)

    console.info
      [90mRunning initial tests before the agent coding starts: npm test[39m

      at runTestBefore (scripts/run-codex-prompts/testing/runTestBefore.ts:38:13)

PASS apps/agents-server/src/utils/vpsTask/runWithVpsServerSetupTask.test.ts
PASS src/utils/serialization/deepClone.test.ts
PASS scripts/run-codex-prompts/common/resolveCoderContext.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse.test.ts
PASS apps/agents-server/src/database/metadataDefaults.test.ts
PASS src/cli/cli-commands/common/npm/buildNpmPackageInstallCommand.test.ts
PASS apps/agents-server/src/app/api/auth/login/route.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$updatePromptbookCliInstallation.test.ts
PASS src/cli/cli-commands/agents-server/run.test.ts
PASS scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts
  ● Console

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:85:24)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:123:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.info
      [90mRunning verification command after attempt #1: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #1. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #2: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.warn
      [33mVerification failed for prompts/feature.md#1 on attempt #2. Sending feedback to github-copilot and retrying...[39m

    [0m [90m 135 |[39m         }
     [90m 136 |[39m
    [31m[1m>[22m[39m[90m 137 |[39m         console[33m.[39mwarn(
     [90m     |[39m                 [31m[1m^[22m[39m
     [90m 138 |[39m             colors[33m.[39myellow(
     [90m 139 |[39m                 [32m`Verification failed for ${options.promptLabel} on attempt #${attemptCount}. Sending feedback to ${options.runner.name} and retrying...`[39m[33m,[39m
     [90m 140 |[39m             )[33m,[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:137:17)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

    console.info
      [90mRunning verification command after attempt #3: npm run test[39m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:106:17)

    console.error
      [31mVerification failed for prompts/feature.md#1 after 3 attempts.[39m

    [0m [90m 121 |[39m
     [90m 122 |[39m         [36mif[39m (attemptCount [33m>=[39m [33mMAX_PROMPT_TEST_ATTEMPTS[39m) {
    [31m[1m>[22m[39m[90m 123 |[39m             console[33m.[39merror(
     [90m     |[39m                     [31m[1m^[22m[39m
     [90m 124 |[39m                 colors[33m.[39mred([32m`Verification failed for ${options.promptLabel} after ${attemptCount} attempts.`[39m)[33m,[39m
     [90m 125 |[39m             )[33m;[39m
     [90m 126 |[39m[0m

      at runPromptWithTestFeedback (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.ts:123:21)
      at Object.<anonymous> (scripts/run-codex-prompts/testing/runPromptWithTestFeedback.test.ts:165:9)

PASS src/utils/parameters/templateParameters.test.ts
PASS src/commands/_BOILERPLATE/boilerplateCommand.test.ts
PASS src/commands/FORMFACTOR/formfactorCommand.test.ts
PASS scripts/run-codex-prompts/common/runGoScript/withPromptRuntimeLog.test.ts
PASS scripts/run-codex-prompts/prompts/formatCoderRunSteps.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.blocks.test.ts
PASS apps/agents-server/src/utils/memoryRuntimeContext.test.ts
PASS src/utils/linguistic-hash/linguisticHash.test.ts
PASS src/book-components/BookEditor/useBookEditorMonacoLanguage.test.ts
PASS apps/agents-server/src/utils/getControlPanelOptionAvailability.test.ts
PASS src/book-components/Chat/Chat/pagePreview/PagePreviewInputQueue.test.ts
PASS src/execution/translation/automatic-translate/automatic-translators/utils/extractMultiplicatedOccurrence.test.ts
PASS src/book-2.0/agent-source/parseAgentSourceWithCommitments.use.test.ts
PASS apps/agents-server/src/message-providers/email/sendgrid/verifySendgridInboundParseWebhook.test.ts
PASS apps/agents-server/src/utils/cloudflare/resolveCloudflareDnsRecordApplyAction.test.ts
PASS src/llm-providers/_common/register/createLlmToolsFromConfiguration.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/resolveCoreAgentAwareMissingReferences.test.ts
PASS src/utils/files/extensionToMimeType.test.ts
PASS src/commands/KNOWLEDGE/utils/knowledgeSourceContentToName.test.ts
PASS src/commands/MODEL/modelCommand.test.ts
PASS scripts/run-codex-prompts/isolation/markPromptIsolationMergeFailed.test.ts
PASS apps/agents-server/src/utils/userChat/userChatJobState.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDirectoryEntries.test.ts
PASS src/book-components/Chat/hooks/useChatCompleteNotification.test.tsx
PASS apps/agents-server/src/utils/localChatRunner/persistLocalTeamConversations.test.ts
PASS src/commands/FORMAT/formatCommand.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/createManGoOpenEditorRequest.test.ts
PASS src/utils/chat/chatAttachments.test.ts
PASS src/file-security-checkers/virustotal/VirusTotalFileSecurityChecker.test.ts
PASS scripts/run-codex-prompts/server/buildCoderServerPromptResponse.test.ts
PASS src/cli/cli-commands/coder/server.test.ts
PASS src/commands/_common/getParserForCommand.test.ts
PASS apps/agents-server/src/utils/messages/sendMessage.test.ts
PASS apps/agents-server/src/utils/plannedMessageManager/resolvePlannedMessageLifecycle.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate.test.ts
PASS apps/agents-server/src/utils/vpsSelfUpdate/readAgentsServerFooterVersion.test.ts
PASS scripts/find-refactor-candidates/resolveRefactorCandidateProject.test.ts
PASS apps/agents-server/src/utils/adminChatTaskLinks.test.ts
PASS scripts/run-codex-prompts/ui/buildCoderRunUiFrame.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectInitials.test.ts
PASS apps/agents-server/src/tools/$provideServer.test.ts
PASS src/utils/misc/parseNumber.test.ts
PASS src/executables/browsers/locateChrome.test.ts
PASS apps/agents-server/src/utils/importAgentWithFallback.test.ts
PASS src/cli/cli-commands/agents-server/startAgentsServer/AgentsServerChildEnvironment.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/useCanonicalAgentChatPanelState.test.ts
PASS src/cli/cli-commands/agent-folder/run.test.ts
PASS apps/agents-server/src/tools/BrowserConnectionProvider.test.ts
PASS scripts/run-codex-prompts/common/runGoScript/withTempScript.test.ts
PASS scripts/run-codex-prompts/main/listCoderPrompts.test.ts
PASS src/utils/editable/utils/renamePipelineParameter.test.ts
PASS apps/agents-server/src/database/loadAgentsServerEnvFile.test.ts
PASS apps/agents-server/src/utils/applyPagePreviewBrowserInput.test.ts
PASS apps/agents-server/src/utils/chatHistoryMessage.test.ts
PASS apps/agents-server/src/utils/vpsTask/vpsTaskHistory.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/ManGoNewAgentWizard/services/bookService.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTraceContent.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectDnsDiagnostics.test.ts
PASS apps/agents-server/src/utils/agentProjects/humanizeAgentProjectName.test.ts
PASS apps/agents-server/src/utils/agentProjects/agentProjectRuntimePm2.test.ts
PASS apps/agents-server/src/utils/resolveInternalServerOrigin.test.ts
PASS apps/agents-server/src/database/selectPrefixesForMigration.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$checkPromptbookCliInstallations.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFiltering.test.ts
PASS apps/agents-server/src/utils/agentBrowserProfile/resolveAgentBrowserProfileDirectory.test.ts
PASS src/errors/utils/serializeError.test.ts
PASS apps/agents-server/src/utils/publicUser.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerAttribution.test.ts
PASS src/cli/cli-commands/coder/boilerplateCount.test.ts
PASS src/utils/markdown/addAutoGeneratedSection.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/resolvePlannedMessageDueAt.test.ts
PASS scripts/run-codex-prompts/common/buildCoderRunProgressSnapshot.test.ts
PASS src/utils/normalization/normalizeWhitespaces.test.ts
PASS apps/agents-server/src/utils/vpsCertificateScheduler.test.ts
PASS apps/agents-server/src/app/api/internal/user-chat-jobs/run/route.test.ts
PASS apps/agents-server/src/utils/agentProjects/readAgentProjectReadme.test.ts
PASS src/errors/utils/deserializeError.test.ts
PASS src/commands/JOKER/jokerCommand.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.timeout.test.ts
PASS apps/agents-server/src/components/Header/mapContextMenuItemsToSubMenuItems.test.ts
PASS apps/agents-server/src/utils/createChatStreamHandler.test.ts
PASS apps/agents-server/src/app/api/internal/agent-goal-chat-planned-messages/route.test.ts
PASS apps/agents-server/src/utils/plannedMessageManager/filterPlannedMessages.test.ts
PASS src/executables/apps/locateVscode.test.ts
PASS src/commands/X_INSTRUMENT/instrumentCommand.test.ts
PASS apps/agents-server/src/utils/serverSelection.test.ts
PASS src/cli/cli-commands/coder/ping.test.ts
PASS src/book-components/Chat/hooks/useChatAutoScroll.test.tsx
PASS apps/agents-server/src/utils/vpsSelfUpdate/vpsSelfUpdateCron.test.ts
PASS src/utils/files/isDirectoryExisting.test.ts
PASS src/utils/expectation-counters/countPages.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/formatChatTimeoutRemainingTime.test.ts
PASS src/book-components/Chat/Chat/pagePreview/normalizePagePreviewInputEvent.test.ts
PASS src/cli/cli-commands/agent/run.test.ts
PASS scripts/run-agent-messages/main/listLocalAgentRunnerProjects.test.ts
PASS src/utils/markdown/flattenMarkdown.test.ts
PASS src/executables/browsers/locateEdge.test.ts
PASS src/commitments/TEMPLATE/TEMPLATE.test.ts
PASS apps/agents-server/src/utils/chatEnterBehaviorSettings.test.ts
PASS src/book-3.0/Book.test.ts
PASS scripts/run-agent-messages/ui/buildAgentRunUiFrame.test.ts
PASS src/utils/sets/difference.test.ts
PASS src/cli/other/install.test.ts
PASS src/book-2.0/agent-source/computeAgentHash.test.ts
PASS src/book-2.0/agent-source/normalizeAgentName.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeRunner.test.ts
PASS scripts/run-agent-messages/messages/resolveAnsweredMessageTouches.test.ts
PASS apps/agents-server/src/components/Header/buildHeaderMenuItems.test.ts
PASS src/cli/common/$requireCliSubcommand.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/CodexFailureHandling.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/plannedMessageSchedule/parsePlannedMessageSchedule.test.ts
PASS scripts/run-codex-prompts/ping/pingCoderHarnessPeriodically.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessagePrompt.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/ExternalUserChatJobMetadata.test.ts
PASS src/utils/files/isFileExisting.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/parseCodexLoginMethodFromOutput.test.ts
PASS src/cli/cli-commands/coder/list.test.ts
PASS scripts/run-codex-prompts/ping/extractCoderPingAnswer.test.ts
PASS src/avatars/visuals/avatarVisualRegistry.test.ts
PASS scripts/run-codex-prompts/git/agentGitIdentity.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessUpdatePlan.test.ts
PASS src/formats/xml/utils/isValidXmlString.test.ts
PASS src/book-components/Chat/utils/parseImagePrompts.test.ts
PASS apps/agents-server/src/utils/userChat/createUserChatRunningActivity.test.ts
PASS apps/agents-server/src/utils/messages/parseUseEmailSmtpCredential.test.ts
PASS src/utils/markdown/removeMarkdownLinks.test.ts
PASS apps/agents-server/src/app/api/admin/default-agents/reinstate/route.test.ts
PASS apps/agents-server/src/utils/chat/createAttachmentAwareCitationLabelResolver.test.ts
PASS src/cli/cli-commands/common/disk-space/$assertSufficientFreeDiskSpace.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/qwen-code-pricing.test.ts
PASS src/book-components/Chat/utils/walletCredentialToolCall.test.ts
PASS src/book-components/Chat/utils/decodeJsonUnicodeEscapesInMarkdownText.test.ts
PASS scripts/run-codex-prompts/common/formatUnknownErrorDetails.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/NewAgentWizardState.test.ts
PASS apps/agents-server/src/components/AdminTerminal/useAdminTerminalSession.test.ts
PASS src/utils/normalization/normalizeTo_SCREAMING_CASE.test.ts
PASS src/utils/normalization/constructImageFilename.test.ts
PASS src/cli/cli-commands/common/coderGitSyncCliOptions.test.ts
PASS src/utils/editable/edit-pipeline-string/removePipelineCommand.test.ts
PASS scripts/run-codex-prompts/prompts/promptRunnerFiltering.test.ts
PASS src/book-components/BookEditor/BookEditorMonacoTokenization.test.ts
PASS src/utils/serialization/jsonStringsToJsons.test.ts
PASS apps/agents-server/src/utils/standaloneVpsDnsDiagnostics.test.ts
PASS src/pipeline/prompt-notation.test.ts
PASS apps/agents-server/src/app/admin/files/filesGalleryRows.test.ts
PASS apps/agents-server/src/utils/getAdminChatTasksResponse/getAdminChatTasks/compareAdminChatTasks.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartBootstrap.test.ts
PASS apps/agents-server/src/utils/agentProjects/parseAgentProjectIndexHtml.test.ts
PASS scripts/verify-prompts/VerifyPromptsOrder.test.ts
PASS apps/agents-server/src/utils/agentIdentifier.test.ts
PASS src/cli/cli-commands/common/harness/$checkHarnessInstallation.test.ts
PASS src/cli/cli-commands/common/harness/resolveHarnessInstallationMethodFromPaths.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.test.ts
  ● Console

    console.warn
      [33m[codex] Rate limit/quota detected (Error: rate limit reached). Retry #1 in 0s at 2026-09-12T10:17:46.318Z.[39m

    [0m [90m 173 |[39m
     [90m 174 |[39m                 [36mif[39m (options[33m.[39mshouldPrintLiveOutput [33m?[39m[33m?[39m [36mtrue[39m) {
    [31m[1m>[22m[39m[90m 175 |[39m                     console[33m.[39mwarn(
     [90m     |[39m                             [31m[1m^[22m[39m
     [90m 176 |[39m                         colors[33m.[39myellow(
     [90m 177 |[39m                             [32m`[codex] Rate limit/quota detected (${summary}). Retry #${retryIndex} in ${formatDelay([39m
     [90m 178 |[39m [32m                                delayMs,[39m[0m

      at OpenAiCodexRunner.runPrompt (scripts/run-codex-prompts/runners/openai-codex/OpenAiCodexRunner.ts:175:29)

PASS apps/agents-server/src/utils/stalwart/resolveStalwartApiUrl.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/createNewAgentWizardSubmissionActions.test.ts
PASS src/utils/parameters/numberToString.test.ts
PASS src/utils/normalization/titleToName.test.ts
PASS scripts/run-codex-prompts/runners/common/modelPricing.test.ts
PASS scripts/run-codex-prompts/prompts/priorityFilter.test.ts
PASS src/llm-providers/anthropic-claude/computeAnthropicClaudeUsage.test.ts
PASS src/avatars/visuals/orbAvatarVisual.test.ts
PASS apps/agents-server/src/utils/parsePositiveUserId.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/ClaudeCodeSessionResurrection.test.ts
PASS src/config.test.ts
PASS src/scrapers/_common/utils/getScraperIntermediateSource.test.ts
PASS scripts/run-codex-prompts/isolation/runIsolatedPromptRound.test.ts
PASS apps/agents-server/src/utils/userChat/heartbeatUserChatJob.test.ts
PASS apps/agents-server/src/utils/normalization/filenameToPrompt.test.ts
PASS apps/agents-server/src/utils/serverRegistry.test.ts
PASS src/utils/validators/filePath/isValidFilePath.test.ts
PASS src/book-components/Chat/utils/createCitationFootnoteRenderModel.test.ts
PASS src/utils/normalization/orderJson.test.ts
PASS src/book-components/Chat/utils/getToolCallChipletInfo.test.ts
PASS src/book-2.0/agent-source/agentSourceVisibility.test.ts
PASS apps/agents-server/src/utils/externalChatRunner/GithubRepositoryClient.test.ts
PASS apps/agents-server/src/utils/userChat/userChatMessageLifecycle.test.ts
PASS src/utils/serialization/serializeToPromptbookJavascript.test.ts
PASS src/utils/normalization/decapitalize.test.ts
PASS apps/agents-server/src/app/admin/messages/send-email/emailTestingAccess.test.ts
PASS src/cli/cli-commands/common/promptbook-cli/$ensurePromptbookCliInstallations.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/generateAgentMetadata.test.ts
PASS src/cli/cli-commands/coder/verify.test.ts
PASS scripts/run-codex-prompts/common/waitForSkippableWorldTimeDeadline.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethServiceProviderUrls.test.ts
PASS src/utils/markdown/createMarkdownChart.test.ts
PASS scripts/verify-prompts/$orderPromptFiles.test.ts
PASS apps/agents-server/src/utils/userLocationPromptParameter.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexUsageFromOutput.test.ts
PASS apps/agents-server/src/utils/userChat/triggerUserChatJobWorker.test.ts
PASS apps/agents-server/src/utils/chatExport/renderHtmlToPdfOnServer.test.ts
PASS src/avatars/avatarRenderingUtils.test.ts
PASS src/cli/cli-commands/common/npm/$resolveLatestNpmPackageVersion.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/userChatTimeoutStore/repeatFiredUserChatTimeout.test.ts
PASS apps/agents-server/src/utils/userChat/userChatReplies.test.ts
PASS src/llm-providers/openai/openai-models.test.ts
PASS src/execution/utils/checkExpectations.test.ts
PASS apps/agents-server/src/utils/shareTarget.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/api/chat/resolveAgentChatRequestIdentities.test.ts
PASS src/cli/cli-commands/common/npm/isNpmPackageVersionOutdated.test.ts
PASS src/utils/editable/edit-pipeline-string/addPipelineCommand.test.ts
PASS src/cli/cli-commands/coder/find-refactor-candidates.test.ts
PASS src/book-components/Chat/utils/toolCallParsing.test.ts
PASS src/utils/normalization/normalizeTo_snake_case.test.ts
PASS apps/agents-server/src/utils/localChatRunner/parseLocalTeamConversations.test.ts
PASS src/utils/markdown/createMarkdownTable.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageRuntimeActivity.test.ts
PASS src/cli/common/$deprecateCliCommand.test.ts
PASS apps/agents-server/src/utils/userPushNotificationSettings.test.ts
PASS apps/agents-server/src/utils/session.test.ts
PASS scripts/run-codex-prompts/common/waitUntilWorldTimeDeadline.test.ts
PASS apps/agents-server/src/utils/chat/createWordLikeDeltas.test.ts
PASS src/utils/markdown/promptbookifyAiText.test.ts
PASS scripts/generate-packages/collectMainPackageDependencies.test.ts
PASS scripts/find-refactor-candidates/selectMostImportantRefactorCandidates.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/getCodexSubscriptionUsage.test.ts
PASS src/utils/validators/filePath/isRootPath.test.ts
PASS src/cli/cli-commands/common/$askForConfirmation.test.ts
PASS src/book-components/Chat/utils/sanitizeStreamingMessageContent.test.ts
PASS apps/agents-server/src/utils/userWallet/createUserWalletRecord.test.ts
PASS apps/agents-server/src/utils/userChat/getUserChatForJobRunner.test.ts
PASS apps/agents-server/src/components/Footer/getCommitFooterEmoji.test.ts
PASS src/utils/markdown/removeMarkdownComments.test.ts
PASS src/utils/knowledge/inlineKnowledgeSource.test.ts
PASS src/utils/files/listAllFiles.test.ts
PASS src/conversion/utils/extractParameterNamesFromTask.test.ts
PASS book/scripts/import-markdown/increaseHeadings.test.ts
PASS scripts/run-codex-prompts/prompts/loadPromptFiles.test.ts
PASS scripts/run-codex-prompts/migrations/detectDestructiveSqlStatements.test.ts
PASS apps/agents-server/src/utils/userChat/runDurableUserChatJobWorkerTick.test.ts
PASS apps/agents-server/src/utils/shibbolethAuthentication/resolveShibbolethPublicRequestUrl.test.ts
PASS apps/agents-server/src/utils/resourceMonitor/resolveServerResourceWarningStatus.test.ts
PASS apps/agents-server/src/utils/chat/validateChatMessageContent.test.ts
PASS src/utils/parameters/valueToString.test.ts
PASS apps/agents-server/src/utils/agentOrganization/hiddenFolders.test.ts
PASS src/utils/knowledge/simplifyKnowledgeLabel.test.ts
PASS src/utils/agents/resolveAgentAvatarImageUrl.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedExternalSources.test.ts
PASS src/cli/cli-commands/common/harness/formatHarnessManualUpdateInstruction.test.ts
PASS scripts/run-codex-prompts/ui/refreshCoderRunUiSubscriptionUsage.test.ts
PASS scripts/run-codex-prompts/prompts/markPromptAttempts.test.ts
PASS apps/agents-server/src/utils/stalwart/stalwartJmapValues.test.ts
PASS apps/agents-server/src/utils/email/agentEmailAddress.test.ts
PASS src/utils/normalization/removeEmojis.test.ts
PASS scripts/find-refactor-candidates/RefactorCandidateLevel.test.ts
PASS apps/agents-server/src/constants/newAgentWizard.test.ts
PASS scripts/run-codex-prompts/common/applyCoderRunControlKey.test.ts
PASS apps/agents-server/src/components/NewAgentDialog/resolveWizardTeamReference.test.ts
PASS src/utils/normalization/normalizeTo_PascalCase.test.ts
PASS src/utils/markdown/parseMarkdownSection.test.ts
PASS src/utils/filesystem/promptbookTemporaryPath.test.ts
PASS apps/agents-server/src/utils/userThemeModeSettings.test.ts
PASS src/book-components/Chat/utils/splitMessageContentIntoSegments.test.ts
PASS apps/agents-server/src/utils/normalization/normalizeUploadFilename.test.ts
PASS scripts/run-codex-prompts/common/appendCoderContext.test.ts
PASS src/utils/files/decodeAttachmentAsText.test.ts
PASS apps/agents-server/src/utils/localization/formatServerLanguageHumanReadableDate.test.ts
PASS src/commitments/USE_CALENDAR/USE_CALENDAR.test.ts
PASS apps/agents-server/src/utils/resolveCurrentOrInternalServerOrigin.test.ts
PASS apps/agents-server/src/utils/createAgentWithDefaultVisibility.test.ts
PASS src/utils/validators/url/isValidPipelineUrl.test.ts
PASS src/utils/validators/javascriptName/isValidJavascriptName.test.ts
PASS book/scripts/import-markdown/removeComments.test.ts
PASS apps/agents-server/src/utils/agentGoalChat/agentGoalChatIdentity.test.ts
PASS apps/agents-server/src/components/_utils/generateMetaTxt.test.ts
PASS src/utils/markdown/extractOneBlockFromMarkdown.test.ts
PASS src/collection/agent-collection/CreateAgentInput.test.ts
PASS src/cli/cli-commands/common/harness/HarnessDefinition.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/buildGitHubCopilotScript.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationNaming.test.ts
PASS src/utils/color/utils/colorHueDistance.test.ts
PASS scripts/run-codex-prompts/git/pullLatestChanges.test.ts
PASS src/utils/normalization/parseKeywordsFromString.test.ts
PASS src/speech-recognition/OpenAiSpeechRecognition.test.ts
PASS apps/agents-server/src/utils/sendUserChatPushNotification.test.ts
PASS src/book-components/Chat/utils/citationHelpers.test.ts
PASS scripts/run-codex-prompts/isolation/coderIsolationCheckoutFailureReport.test.ts
PASS scripts/run-codex-prompts/git/coderGitSync.test.ts
PASS apps/agents-server/src/utils/localChatRunner/LocalUserChatJobMetadata.test.ts
PASS apps/agents-server/src/utils/agentRouting/agentRouteHrefs.test.ts
PASS src/executables/apps/locateLibreoffice.test.ts
PASS src/conversion/validation/_importPipeline.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/generateChatMetadata.test.ts
PASS scripts/run-codex-prompts/runners/gemini/gemini-pricing.test.ts
PASS scripts/run-codex-prompts/runners/common/createAuthenticationAwarePromptRunner.test.ts
PASS src/book-components/Chat/CodeBlock/resolveCodeBlockLanguage.test.ts
PASS src/utils/expectation-counters/countWords.test.ts
PASS src/commitments/MODEL/MODEL.test.ts
PASS src/conversion/pipelineJsonToString.test.ts
PASS src/book-3.0/AgentMessageRunReport.test.ts
PASS src/avatars/avatarInteractionUtils.test.ts
PASS src/formats/json/utils/isValidJsonString.test.ts
PASS apps/agents-server/src/utils/agentProjects/createAgentProjectDnsRecords.test.ts
PASS src/utils/serialization/asSerializable.test.ts
PASS src/utils/markdown/splitMarkdownIntoSections.test.ts
PASS scripts/run-codex-prompts/runners/openai-codex/buildCodexScript.test.ts
PASS src/utils/validators/email/isValidEmail.test.ts
PASS src/llm-providers/_common/utils/removeUnsupportedModelRequirements.test.ts
PASS src/book-components/Chat/utils/ChatPersistence.test.ts
PASS src/utils/take/take.test.ts
PASS src/utils/sets/union.test.ts
PASS src/utils/normalization/removeQuotes.test.ts
PASS src/utils/markdown/trimEndOfCodeBlock.test.ts
PASS src/utils/normalization/parseKeywords.test.ts
PASS src/execution/utils/formatUsagePrice.test.ts
PASS src/commitments/ACTION/ACTION.test.ts
PASS src/cli/common/$hideCliCommandFromHelp.test.ts
PASS src/avatars/visuals/octopus3AvatarVisual.test.ts
PASS apps/agents-server/src/utils/agentChatInputPlaceholder.test.ts
PASS src/utils/validators/url/extractUrlsFromText.test.ts
PASS src/formats/csv/utils/isValidCsvString.test.ts
PASS src/book-components/Chat/utils/parseMessageButtons.test.ts
PASS scripts/run-codex-prompts/ui/CoderRunUiState.test.ts
PASS apps/agents-server/src/utils/speech-to-text/SpeechToTextFailoverRecognition.test.ts
PASS src/utils/normalization/capitalize.test.ts
PASS src/utils/agent-message-runtime/resolveAgentMessageTouchedProjectNames.test.ts
PASS src/cli/cli-commands/common/npm/extractNpmPackageVersionFromOutput.test.ts
PASS scripts/run-codex-prompts/runners/github-copilot/GitHubCopilotRunner.test.ts
PASS scripts/run-codex-prompts/ui/buildCoderRunUiTerminalFrameUpdate.test.ts
PASS scripts/repair-imports/utils/splitArrayIntoChunks.test.ts
PASS apps/agents-server/src/utils/transpilers/getTranspiledCodeFileMetadata.test.ts
PASS apps/agents-server/src/components/UsersList/generateSecurePassword.test.ts
PASS apps/agents-server/src/utils/dnsRecords/createDnsZoneFileExport.test.ts
PASS src/utils/normalization/removeDiacritics.test.ts
PASS src/utils/parameters/extractParameterNames.test.ts
PASS src/utils/expectation-counters/countSentences.test.ts
PASS src/cli/cli-commands/common/disk-space/formatFreeDiskSpaceBytes.test.ts
PASS scripts/run-codex-prompts/runners/common/extractHarnessAuthenticationFailureReason.test.ts
PASS src/utils/validators/url/isUrlOnPrivateNetwork.test.ts
PASS src/book-components/Chat/utils/thinkingMessageVariants.test.ts
PASS scripts/run-agent-messages/messages/buildAgentMessageScriptPath.test.ts
PASS apps/agents-server/src/utils/userChat/resolveUserChatWorkerInternalToken.test.ts
PASS apps/agents-server/src/utils/stalwart/createEmailDnsInstructions.test.ts
PASS apps/agents-server/src/components/Homepage/loadFederatedServerAgents.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemActivities.test.ts
PASS src/execution/utils/addUsage.test.ts
PASS apps/agents-server/src/utils/chatFeedbackMode.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/parseClaudeCodeSubscriptionUsage.test.ts
PASS src/book-3.0/AgentPlannedMessagesSidecar.test.ts
PASS src/utils/validators/url/isValidUrl.test.ts
PASS scripts/run-codex-prompts/common/ProgressiveBackoff.test.ts
PASS apps/agents-server/src/utils/userWallet/resolveWalletAgentPermanentId.test.ts
PASS apps/agents-server/src/utils/messages/humanizeOutboundEmail.test.ts
PASS apps/agents-server/src/utils/cloudflare/createCloudflareDnsRecordRequestBody.test.ts
PASS src/utils/expectation-counters/countParagraphs.test.ts
PASS src/execution/execution-report/countWorkingDuration.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/chat/mergeCanonicalChatMessagesWithPendingOutboundMessages.test.ts
PASS apps/agents-server/src/utils/userChat/UserChatScopeError.test.ts
PASS apps/agents-server/src/utils/upload/fileUploadAvailability.test.ts
PASS apps/agents-server/src/components/AgentProjects/resolveLatestCompletedAgentMessageKey.test.ts
PASS src/utils/normalization/searchKeywords.test.ts
PASS src/utils/markdown/trimCodeBlock.test.ts
PASS src/transpilers/_common/formatUsedToolFunctions.test.ts
PASS src/execution/utils/usageToWorktime.test.ts
PASS src/execution/resolveTaskTldr.test.ts
PASS src/collection/agent-collection/constructors/agent-collection-in-supabase/buildAgentNameOrPermanentIdFilter.test.ts
PASS apps/agents-server/src/app/agents/[agentName]/AgentProfileChat.navigation.test.ts
PASS src/book-components/Chat/utils/timeoutToolCallPresentation.test.ts
PASS src/book-components/Chat/utils/parseCitationsFromContent.test.ts
PASS apps/agents-server/src/utils/agentReferenceResolver/$provideAgentReferenceResolver.test.ts
PASS apps/agents-server/src/components/Header/resolveHeaderSystemWarnings.test.ts
PASS src/utils/normalization/normalizeMessageText.test.ts
PASS src/utils/expectation-counters/countCharacters.test.ts
PASS src/commitments/MESSAGE_SUFFIX/MESSAGE_SUFFIX.test.ts
PASS scripts/run-codex-prompts/runners/gemini/buildGeminiScript.test.ts
PASS apps/agents-server/src/utils/agentProjects/listAgentProjectChatReferences.test.ts
PASS src/utils/normalization/nameToUriParts.test.ts
PASS src/utils/normalization/isValidKeyword.test.ts
PASS src/utils/markdown/escapeMarkdownBlock.test.ts
PASS src/cli/cli-commands/common/disk-space/resolveFreeDiskSpaceLevel.test.ts
PASS src/cli/cli-commands/common/disk-space/$readFreeDiskSpaceStatus.test.ts
PASS scripts/run-codex-prompts/runners/qwen-code/buildQwenCodeScript.test.ts
PASS src/utils/validators/url/isHostnameOnPrivateNetwork.test.ts
PASS scripts/run-codex-prompts/runners/claude-code/buildClaudeScript.test.ts
PASS src/cli/cli-commands/coder/getTypescriptModule.test.ts
PASS src/book-components/Chat/utils/resolveCitationUrl.test.ts
PASS src/book-components/Chat/Chat/learnDictationDictionary.test.ts
PASS src/utils/normalization/suffixUrl.test.ts
PASS src/utils/files/mimeTypeToExtension.test.ts
PASS src/commitments/USE_PROJECT/projectReference.test.ts
PASS apps/agents-server/src/utils/standaloneVpsRawIpBootstrap.test.ts
PASS src/utils/validators/uuid/isValidUuid.test.ts
PASS src/utils/validators/semanticVersion/isValidSemanticVersion.test.ts
PASS src/utils/validators/semanticVersion/isValidPromptbookVersion.test.ts
PASS src/storage/file-cache-storage/utils/nameToSubfolderPath.test.ts
PASS src/book-components/Chat/Chat/refineFinalDictationChunk.test.ts
PASS scripts/run-codex-prompts/prompts/buildPromptRunTracePath.test.ts
PASS src/utils/color/Color.test.ts
PASS src/utils/normalization/normalize-to-kebab-case.test.ts
PASS src/utils/markdown/extractAllBlocksFromMarkdown-real.test.ts
PASS src/utils/files/getFileExtension.test.ts
PASS src/utils/expectation-counters/countLines.test.ts
PASS src/executables/browsers/locateDefaultSystemBrowser.test.ts
PASS apps/agents-server/src/app/admin/_components/adminTableSorting.test.ts
PASS src/utils/validators/url/normalizeDomainForMatching.test.ts
PASS src/utils/sets/intersection.test.ts
PASS src/utils/normalization/nameToUriPart.test.ts
PASS src/utils/misc/arrayableToArray.test.ts
PASS src/utils/markdown/removeMarkdownFormatting.test.ts
PASS src/utils/markdown/extractAllListItemsFromMarkdown.test.ts
PASS src/llm-providers/_common/utils/pricing.test.ts
PASS src/book-components/Chat/utils/agentProjectToolCall.test.ts
PASS apps/agents-server/src/utils/userChatTimeout/createUserChatTimeoutActivity.test.ts
PASS src/commitments/META_DISCLAIMER/META_DISCLAIMER.test.ts
PASS apps/agents-server/src/utils/pagePreviewBrowserSessions.test.ts
PASS apps/agents-server/src/tools/createServerChromiumLaunchOptions.test.ts
PASS apps/agents-server/src/constants/chatVisualMode.test.ts
PASS src/utils/validators/url/isValidAgentUrl.test.ts
PASS src/book-components/Chat/Chat/insertDictationChunk.test.ts
PASS src/utils/normalization/normalizeTo_camelCase.test.ts
PASS src/commitments/FORMAT/FORMAT.test.ts
PASS src/utils/misc/debounce.test.ts
A worker process has failed to exit gracefully and has been force exited. This is likely caused by tests leaking due to improper teardown. Try running with --detectOpenHandles to find leaks. Active timers can also cause this, ensure that .unref() was called on them.

Test Suites: 759 passed, 759 total
Tests:       7 todo, 3343 passed, 3350 total
Snapshots:   0 total
Time:        344.928 s, estimated 346 s
Ran all test suites.
Force exiting Jest: Have you considered using `--detectOpenHandles` to detect async operations that kept running after all tests finished?

> lint
> eslint src/ --max-warnings=0 --cache --cache-location ./node_modules/.cache/eslint/agents-server-src.json


> pretest-build
> node ./scripts/kill-port.js 4021 4440

No process is listening on port 4021.
No process is listening on port 4440.

> test-build
> npm run build


> prebuild
> npm run generate-reserved-paths && node ./scripts/clearNextGeneratedTypes.js && node ./scripts/kill-port.js 4440


> generate-reserved-paths
> ts-node --transpile-only ./scripts/generate-reserved-paths/generate-reserved-paths.ts

Kept C:\Users\me\work\ai\promptbook\apps\agents-server\src\generated\reservedPaths.ts unchanged with 31 reserved paths.
No process is listening on port 4440.

> build
> node ./scripts/build-agents-server.js

   ▲ Next.js 15.4.11
   - Environments: .env
   - Experiments (use with caution):
     ✓ externalDir
     ✓ webpackBuildWorker
     · clientTraceMetadata
     ✓ parallelServerBuildTraces

   Creating an optimized production build ...
 ⚠ Compiled with warnings in 4.4min

../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
Critical dependency: the request of a dependency is an expression

Import trace for requested module:
../../node_modules/@ai-sdk/google/node_modules/@ai-sdk/provider-utils/dist/index.js
../../node_modules/@ai-sdk/google/dist/index.js
../../src/llm-providers/google/createGoogleExecutionTools.ts
../../src/_packages/google.index.ts
./src/tools/$provideExecutionToolsForServer.ts
./src/app/agents/[agentName]/api/mcp/route.ts

<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (127kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (139kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
<w> [webpack.cache.PackFileCacheStrategy] Serializing big strings (126kiB) impacts deserialization performance (consider using Buffer instead and decode when needed)
 ✓ Compiled successfully in 5.8min
   Linting and checking validity of types ...
   Collecting page data ...
 ⚠ Using edge runtime on a page currently disables static generation for that page
   Generating static pages (0/146) ...
   Generating static pages (36/146) 
   Generating static pages (72/146) 
   Generating static pages (109/146) 
 ✓ Generating static pages (146/146)
   Finalizing page optimization ...
   Collecting build traces ...

Route (app)                                                                     Size  First Load JS
┌ ƒ /                                                                        1.55 kB        1.53 MB
├ ƒ /_not-found                                                                827 B         184 kB
├ ƒ /[agentName]                                                               763 B         334 kB
├ ƒ /[agentName]/[...rest]                                                     828 B         184 kB
├ ƒ /admin/about                                                             1.87 kB        1.78 MB
├ ƒ /admin/api-tokens                                                        6.36 kB         251 kB
├ ƒ /admin/backup                                                            4.11 kB         230 kB
├ ƒ /admin/browser-test                                                      3.29 kB         239 kB
├ ƒ /admin/chat-feedback                                                     20.3 kB        1.67 MB
├ ƒ /admin/chat-history                                                      22.9 kB        1.67 MB
├ ƒ /admin/core-agents                                                       2.56 kB        1.78 MB
├ ƒ /admin/custom-css                                                        9.83 kB         262 kB
├ ƒ /admin/custom-js                                                         11.6 kB         263 kB
├ ƒ /admin/email-server                                                       2.6 kB         234 kB
├ ƒ /admin/error-simulation                                                  4.97 kB         231 kB
├ ƒ /admin/files                                                             7.64 kB         263 kB
├ ƒ /admin/image-generator-test                                              14.1 kB         269 kB
├ ƒ /admin/images                                                            8.47 kB         263 kB
├ ƒ /admin/internal-s3                                                       1.81 kB         228 kB
├ ƒ /admin/limits                                                            2.47 kB         231 kB
├ ƒ /admin/login-methods/shibboleth                                          5.46 kB         250 kB
├ ƒ /admin/messages                                                          9.69 kB         255 kB
├ ƒ /admin/messages/send-email                                                  6 kB         236 kB
├ ƒ /admin/metadata                                                          18.7 kB         305 kB
├ ƒ /admin/models                                                            1.81 kB         228 kB
├ ƒ /admin/planned-messages                                                  13.3 kB         261 kB
├ ƒ /admin/projects                                                            324 B         230 kB
├ ƒ /admin/search-engine-test                                                 3.4 kB         229 kB
├ ƒ /admin/task-manager                                                        334 B         267 kB
├ ƒ /admin/task-manager-vps                                                    829 B         184 kB
├ ƒ /admin/task-manager/[taskId]                                             2.49 kB         262 kB
├ ƒ /admin/tool-limits                                                        1.8 kB         228 kB
├ ƒ /admin/transcriptions                                                    6.68 kB         239 kB
├ ƒ /admin/usage                                                               10 kB         265 kB
├ ƒ /admin/users                                                             2.25 kB         254 kB
├ ƒ /admin/users/[userId]                                                    6.72 kB         252 kB
├ ƒ /admin/voice-input-test                                                  5.29 kB         231 kB
├ ƒ /agents                                                                  1.55 kB        1.53 MB
├ ƒ /agents/[agentName]                                                        435 B         334 kB
├ ƒ /agents/[agentName]/api/book                                               826 B         184 kB
├ ƒ /agents/[agentName]/api/book/download                                      828 B         184 kB
├ ƒ /agents/[agentName]/api/book/history                                       827 B         184 kB
├ ƒ /agents/[agentName]/api/book/missing-agent                                 825 B         184 kB
├ ƒ /agents/[agentName]/api/book/reference-diagnostics                         825 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-connections                               825 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-connections/[connectionId]/disconnect     826 B         184 kB
├ ƒ /agents/[agentName]/api/calendar-events                                    826 B         184 kB
├ ƒ /agents/[agentName]/api/chat                                               826 B         184 kB
├ ƒ /agents/[agentName]/api/feedback                                           826 B         184 kB
├ ƒ /agents/[agentName]/api/mcp                                                827 B         184 kB
├ ƒ /agents/[agentName]/api/meta-disclaimer                                    826 B         184 kB
├ ƒ /agents/[agentName]/api/model-requirements                                 825 B         184 kB
├ ƒ /agents/[agentName]/api/model-requirements/system-message                  825 B         184 kB
├ ƒ /agents/[agentName]/api/openai/chat/completions                            826 B         184 kB
├ ƒ /agents/[agentName]/api/openai/models                                      826 B         184 kB
├ ƒ /agents/[agentName]/api/openai/v1/chat/completions                         827 B         184 kB
├ ƒ /agents/[agentName]/api/openai/v1/models                                   826 B         184 kB
├ ƒ /agents/[agentName]/api/openrouter/chat/completions                        825 B         184 kB
├ ƒ /agents/[agentName]/api/profile                                            829 B         184 kB
├ ƒ /agents/[agentName]/api/share-target/[shareTargetId]/consume               826 B         184 kB
├ ƒ /agents/[agentName]/api/timeouts                                           825 B         184 kB
├ ƒ /agents/[agentName]/api/timeouts/[timeoutId]                               825 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats                                         827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]                                827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/draft                          826 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/jobs/[jobId]/cancel            825 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/messages                       827 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/stream                         826 B         184 kB
├ ƒ /agents/[agentName]/api/user-chats/[chatId]/timeouts/[timeoutId]/cancel    828 B         184 kB
├ ƒ /agents/[agentName]/api/voice                                              826 B         184 kB
├ ƒ /agents/[agentName]/book                                                 6.34 kB         421 kB
├ ƒ /agents/[agentName]/book+chat                                            4.94 kB        1.82 MB
├ ƒ /agents/[agentName]/chat                                                 34.7 kB        1.84 MB
├ ƒ /agents/[agentName]/export-as-transpiled-code                            7.35 kB         384 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api                          825 B         184 kB
├ ƒ /agents/[agentName]/export-as-transpiled-code/api/download                 825 B         184 kB
├ ƒ /agents/[agentName]/goal                                                  1.8 kB         228 kB
├ ƒ /agents/[agentName]/history                                              2.67 kB         228 kB
├ ƒ /agents/[agentName]/iframe                                                 825 B         184 kB
├ ƒ /agents/[agentName]/images                                                 354 B         185 kB
├ ƒ /agents/[agentName]/images/default-avatar.png                              825 B         184 kB
├ ƒ /agents/[agentName]/images/icon-256.png                                    826 B         184 kB
├ ƒ /agents/[agentName]/images/screenshot-fullhd.png                           825 B         184 kB
├ ƒ /agents/[agentName]/images/screenshot-phone.png                            826 B         184 kB
├ ƒ /agents/[agentName]/integration                                            11 kB         270 kB
├ ƒ /agents/[agentName]/opengraph-image                                        826 B         184 kB
├ ƒ /agents/[agentName]/projects                                               324 B         230 kB
├ ƒ /agents/[agentName]/projects/[projectName]                               3.49 kB        1.78 MB
├ ƒ /agents/[agentName]/projects/[projectName]/files/[...filePath]             828 B         184 kB
├ ƒ /agents/[agentName]/projects/[projectName]/vscode                          824 B         184 kB
├ ƒ /agents/[agentName]/share-target                                           827 B         184 kB
├ ƒ /agents/[agentName]/system-message                                       2.93 kB         379 kB
├ ƒ /agents/[agentName]/textarea                                               10 kB         250 kB
├ ƒ /agents/[agentName]/website-integration                                  2.63 kB        1.78 MB
├ ƒ /api/admin-email                                                           826 B         184 kB
├ ƒ /api/admin/backups/books                                                   827 B         184 kB
├ ƒ /api/admin/backups/server                                                  826 B         184 kB
├ ƒ /api/admin/chat-tasks                                                      825 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]                                             827 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/cancel                                      824 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/retry                                       828 B         184 kB
├ ƒ /api/admin/chat-tasks/[taskId]/terminal                                    828 B         184 kB
├ ƒ /api/admin/chat-tasks/cancel-all                                           828 B         184 kB
├ ƒ /api/admin/cli-access                                                      827 B         184 kB
├ ƒ /api/admin/database/studio                                                 827 B         184 kB
├ ƒ /api/admin/default-agents/reinstate                                        826 B         184 kB
├ ƒ /api/admin/dns-records/cloudflare                                          826 B         184 kB
├ ƒ /api/admin/environment                                                     826 B         184 kB
├ ƒ /api/admin/error-simulation                                                826 B         184 kB
├ ƒ /api/admin/harness-auth                                                    824 B         184 kB
├ ƒ /api/admin/harness-auth/authentication                                     826 B         184 kB
├ ƒ /api/admin/limits                                                          825 B         184 kB
├ ƒ /api/admin/logs                                                            827 B         184 kB
├ ƒ /api/admin/planned-messages                                                825 B         184 kB
├ ƒ /api/admin/planned-messages/[timeoutId]                                    826 B         184 kB
├ ƒ /api/admin/servers                                                         826 B         184 kB
├ ƒ /api/admin/servers/[serverId]                                              828 B         184 kB
├ ƒ /api/admin/servers/[serverId]/migrate                                      825 B         184 kB
├ ƒ /api/admin/tool-limits                                                     829 B         184 kB
├ ƒ /api/admin/update                                                          827 B         184 kB
├ ƒ /api/admin/update/commits                                                  827 B         184 kB
├ ƒ /api/admin/update/log                                                      827 B         184 kB
├ ƒ /api/admin/update/versions                                                 827 B         184 kB
├ ƒ /api/admin/vps/chat-tasks                                                  826 B         184 kB
├ ƒ /api/admin/vps/chat-tasks/cancel-all                                       826 B         184 kB
├ ƒ /api/agent-folders                                                         827 B         184 kB
├ ƒ /api/agent-folders/[folderId]                                              826 B         184 kB
├ ƒ /api/agent-folders/[folderId]/restore                                      825 B         184 kB
├ ƒ /api/agent-folders/[folderId]/visibility                                   827 B         184 kB
├ ƒ /api/agent-organization                                                    825 B         184 kB
├ ƒ /api/agent-project-runtime-auth                                            825 B         184 kB
├ ƒ /api/agent-project-vscode-auth/[sessionId]                                 827 B         184 kB
├ ƒ /api/agents                                                                827 B         184 kB
├ ƒ /api/agents/[agentName]                                                    827 B         184 kB
├ ƒ /api/agents/[agentName]/clone                                              825 B         184 kB
├ ƒ /api/agents/[agentName]/restore                                            827 B         184 kB
├ ƒ /api/agents/export                                                         826 B         184 kB
├ ƒ /api/agents/import                                                         827 B         184 kB
├ ƒ /api/api-tokens                                                            827 B         184 kB
├ ƒ /api/auth/change-password                                                  828 B         184 kB
├ ƒ /api/auth/login                                                            827 B         184 kB
├ ƒ /api/auth/logout                                                           825 B         184 kB
├ ƒ /api/auth/shibboleth/acs                                                   825 B         184 kB
├ ƒ /api/auth/shibboleth/login                                                 824 B         184 kB
├ ƒ /api/auth/shibboleth/metadata                                              827 B         184 kB
├ ƒ /api/auth/shibboleth/status                                                826 B         184 kB
├ ƒ /api/browser-artifacts/[artifactName]                                      826 B         184 kB
├ ƒ /api/browser-test/act                                                      826 B         184 kB
├ ƒ /api/browser-test/screenshot                                               825 B         184 kB
├ ƒ /api/browser-test/scroll-facebook                                          824 B         184 kB
├ ƒ /api/calendar-oauth/callback                                               825 B         184 kB
├ ƒ /api/calendar-oauth/connect                                                826 B         184 kB
├ ƒ /api/calendar-oauth/refresh                                                826 B         184 kB
├ ƒ /api/calendar-oauth/revoke                                                 828 B         184 kB
├ ƒ /api/calendar-oauth/status                                                 825 B         184 kB
├ ƒ /api/chat                                                                  826 B         184 kB
├ ƒ /api/chat-feedback                                                         826 B         184 kB
├ ƒ /api/chat-feedback/[id]                                                    826 B         184 kB
├ ƒ /api/chat-feedback/export                                                  826 B         184 kB
├ ƒ /api/chat-history                                                          826 B         184 kB
├ ƒ /api/chat-history/[id]                                                     825 B         184 kB
├ ƒ /api/chat-history/export                                                   827 B         184 kB
├ ƒ /api/chat-history/threads                                                  825 B         184 kB
├ ƒ /api/chat-streaming                                                        825 B         184 kB
├ ƒ /api/chat/citation-label                                                   825 B         184 kB
├ ƒ /api/chat/export/pdf                                                       827 B         184 kB
├ ƒ /api/custom-css                                                            825 B         184 kB
├ ƒ /api/custom-js                                                             826 B         184 kB
├ ƒ /api/docs/book-language.md                                                 828 B         184 kB
├ ƒ /api/docs/book-language.pdf                                                826 B         184 kB
├ ƒ /api/docs/book.md                                                          830 B         184 kB
├ ƒ /api/elevenlabs/tts                                                        826 B         184 kB
├ ƒ /api/emails/incoming/sendgrid                                              827 B         184 kB
├ ƒ /api/emails/incoming/stalwart                                              827 B         184 kB
├ ƒ /api/embed.js                                                              826 B         184 kB
├ ƒ /api/error-reports/application                                             826 B         184 kB
├ ƒ /api/federated-agents                                                      828 B         184 kB
├ ƒ /api/github-app/callback                                                   827 B         184 kB
├ ƒ /api/github-app/connect                                                    826 B         184 kB
├ ƒ /api/github-app/status                                                     828 B         184 kB
├ ƒ /api/health                                                                828 B         184 kB
├ ƒ /api/images/[filename]                                                     826 B         184 kB
├ ƒ /api/internal/agent-goal-chat-planned-messages                             827 B         184 kB
├ ƒ /api/internal/agent-project-runtimes                                       827 B         184 kB
├ ƒ /api/internal/agent-runner-limits                                          825 B         184 kB
├ ƒ /api/internal/user-chat-jobs/run                                           827 B         184 kB
├ ƒ /api/internal/user-chat-timeouts/run                                       826 B         184 kB
├ ƒ /api/long-running-task                                                     827 B         184 kB
├ ƒ /api/messages                                                              825 B         184 kB
├ ƒ /api/metadata                                                              824 B         184 kB
├ ƒ /api/metadata/export                                                       827 B         184 kB
├ ƒ /api/metadata/import                                                       827 B         184 kB
├ ƒ /api/onboarding/book                                                       828 B         184 kB
├ ƒ /api/onboarding/evaluate                                                   826 B         184 kB
├ ƒ /api/onboarding/test                                                       827 B         184 kB
├ ƒ /api/openai/v1/audio/transcriptions                                        826 B         184 kB
├ ƒ /api/openai/v1/chat/completions                                            826 B         184 kB
├ ƒ /api/openai/v1/models                                                      828 B         184 kB
├ ƒ /api/page-preview/check                                                    824 B         184 kB
├ ƒ /api/page-preview/input                                                    828 B         184 kB
├ ƒ /api/page-preview/screenshot                                               827 B         184 kB
├ ƒ /api/page-preview/state                                                    827 B         184 kB
├ ƒ /api/page-preview/stream                                                   825 B         184 kB
├ ƒ /api/profile                                                               828 B         184 kB
├ ƒ /api/push-subscriptions                                                    826 B         184 kB
├ ƒ /api/scrape                                                                827 B         184 kB
├ ƒ /api/search                                                                826 B         184 kB
├ ƒ /api/send-email                                                            826 B         184 kB
├ ƒ /api/settings/keybindings                                                  826 B         184 kB
├ ƒ /api/settings/notifications                                                825 B         184 kB
├ ƒ /api/settings/theme                                                        827 B         184 kB
├ ƒ /api/spawn-agent                                                           826 B         184 kB
├ ƒ /api/story/export                                                          826 B         184 kB
├ ƒ /api/system/mocked-chats                                                   826 B         184 kB
├ ƒ /api/team-agent-profile                                                    827 B         184 kB
├ ƒ /api/upload                                                                825 B         184 kB
├ ƒ /api/usage                                                                 829 B         184 kB
├ ƒ /api/user-memory                                                           826 B         184 kB
├ ƒ /api/user-memory/[memoryId]                                                826 B         184 kB
├ ƒ /api/user-wallet                                                           825 B         184 kB
├ ƒ /api/user-wallet/[walletId]                                                826 B         184 kB
├ ƒ /api/users                                                                 825 B         184 kB
├ ƒ /api/users/[username]                                                      828 B         184 kB
├ ƒ /api/v1/agents                                                             825 B         184 kB
├ ƒ /api/v1/agents/[agentId]                                                   827 B         184 kB
├ ƒ /api/v1/folders                                                            827 B         184 kB
├ ƒ /api/v1/folders/[folderId]                                                 827 B         184 kB
├ ƒ /api/v1/folders/[folderId]/agents/[agentId]                                824 B         184 kB
├ ƒ /api/v1/instance                                                           826 B         184 kB
├ ƒ /api/v1/me                                                                 826 B         184 kB
├ ƒ /dashboard                                                               2.04 kB        1.56 MB
├ ƒ /docs                                                                    3.19 kB        1.78 MB
├ ƒ /docs/[docId]                                                            3.17 kB        1.77 MB
├ ƒ /embed                                                                    7.3 kB        1.72 MB
├ ƒ /experiments/story                                                       5.21 kB         382 kB
├ ƒ /humans.txt                                                                826 B         184 kB
├ ƒ /manifest.webmanifest                                                      827 B         184 kB
├ ƒ /openapi.json                                                              825 B         184 kB
├ ƒ /recycle-bin                                                             3.79 kB         311 kB
├ ƒ /restricted                                                                353 B         185 kB
├ ƒ /robots.txt                                                                826 B         184 kB
├ ƒ /s3/[first]/[second]/[hash]/[filename]                                     828 B         184 kB
├ ƒ /search                                                                  6.62 kB         191 kB
├ ƒ /security.txt                                                              824 B         184 kB
├ ƒ /sitemap.xml                                                               826 B         184 kB
├ ƒ /story/[[...story]]                                                        827 B         184 kB
├ ƒ /superadmin/cli-access                                                   3.21 kB         232 kB
├ ƒ /superadmin/database                                                     2.89 kB         229 kB
├ ƒ /superadmin/email-server                                                 2.25 kB         228 kB
├ ƒ /superadmin/environment                                                  5.87 kB         232 kB
├ ƒ /superadmin/harness-auth                                                  6.2 kB         235 kB
├ ƒ /superadmin/internal-s3                                                    827 B         184 kB
├ ƒ /superadmin/logs                                                         3.87 kB         230 kB
├ ƒ /superadmin/resource-monitor                                              7.1 kB         234 kB
├ ƒ /superadmin/servers                                                      23.2 kB         303 kB
├ ƒ /superadmin/task-manager                                                   335 B         267 kB
├ ƒ /superadmin/update                                                       14.7 kB         265 kB
├ ƒ /swagger                                                                 4.33 kB         246 kB
├ ƒ /system/profile                                                          3.03 kB         229 kB
├ ƒ /system/settings                                                         5.87 kB         226 kB
├ ƒ /system/user-memory                                                      5.65 kB         251 kB
├ ƒ /system/user-wallet                                                      9.73 kB         262 kB
├ ƒ /system/utilities                                                        1.81 kB         228 kB
├ ƒ /system/utilities/mocked-chats                                           19.3 kB         264 kB
├ ƒ /system/utilities/mocked-chats/view                                      5.31 kB        1.65 MB
├ ƒ /test/og-image                                                             826 B         184 kB
└ ƒ /test/og-image/opengraph-image                                             826 B         184 kB
+ First Load JS shared by all                                                 183 kB
  ├ chunks/1458-dbf17befddc60f63.js                                           123 kB
  ├ chunks/87c73c54-cafaa802daf5b9c9.js                                      54.3 kB
  └ other shared chunks (total)                                              5.88 kB

Route (pages)                                                                   Size  First Load JS
┌   /_app                                                                        0 B         143 kB
└ ○ /500 (5452 ms)                                                           4.92 kB         148 kB
+ First Load JS shared by all                                                 174 kB
  ├ chunks/framework-b796438537236ee8.js                                     57.8 kB
  ├ chunks/main-ecfb07a32f75afe7.js                                          81.2 kB
  ├ css/c4bd8b207325b3b3.css                                                 31.5 kB
  └ other shared chunks (total)                                              4.02 kB

ƒ Middleware                                                                  301 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand

   ▲ Next.js 15.4.11
   - Local:        http://localhost:4440
   - Network:      http://192.168.192.1:4440

 ✓ Starting...
 ✓ Ready in 4.5s
🗂️ VPS server registry ready with 0 registered server(s) in standalone SQLite mode.
Prerendered home page and saved to C:\Users\me\work\ai\promptbook\apps\agents-server\.next\prerendered\home.html
🎉 All tests passed!

=== test shell finished at 2026-09-12T10:27:02.667Z ===
Status: succeeded
````
